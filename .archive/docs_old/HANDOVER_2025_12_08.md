# ЭСТАФЕТА — ПОЛНАЯ ПЕРЕДАЧА ЗНАНИЙ

**Дата:** 2025-12-08
**От:** Droid
**Для:** Следующая сессия / Инвестор / Жанбол

---

## ЧАСТЬ 1: ГЛАВНЫЕ УРОКИ (ЧИТАТЬ ПЕРВЫМ)

### 90% всех ошибок — передача данных между нодами

**Проблема:** В n8n `$json` — это данные от ПРЕДЫДУЩЕЙ подключённой ноды, НЕ от любой ноды в workflow.

**Пример ошибки:**
```
Parse Input → Skip Classifier? → Upsert User
                    ↓
            Is On Topic → Upsert User
```
Upsert User использовал `$json.phone`, но получал данные от Is On Topic (который не имеет phone), а не от Parse Input.

**Результат:** `phone = undefined` → создавались мусорные users.

**РЕШЕНИЕ — ЗОЛОТОЕ ПРАВИЛО:**
```javascript
// ПЛОХО — непонятно откуда данные
$json.phone

// ХОРОШО — явно указываем источник
$('Parse Input').first().json.phone
```

### 10% ошибок — нода не выполнялась

**Проблема:** `$('Take Response').first().json` падает если Take Response не в текущей ветке выполнения.

**Пример:**
- Take flow: Take Handover → Take Response → Answer Callback → Notify in Chat
- Resolve flow: Resolve Handover → Unmute Bot → Answer Callback → Notify in Chat

Notify in Chat использовал `$('Take Response')` — падал в resolve flow.

**РЕШЕНИЕ:** Разделять ветки полностью или использовать данные из общей ноды (Merge Token).

### Как избежать этих ошибок

**ПЕРЕД созданием/изменением ноды:**
1. Нарисовать на бумаге: откуда данные придут?
2. Проверить connections в workflow
3. Использовать ЯВНЫЕ ссылки `$('NodeName').first().json`

**ПОСЛЕ изменения:**
1. Проверить execution — какие ноды выполнились
2. Посмотреть input/output каждой ноды
3. Искать undefined в данных

---

## ЧАСТЬ 2: АРХИТЕКТУРА СИСТЕМЫ

### Общая картина

```
WhatsApp (ChatFlow) 
    ↓ webhook
1_Webhook → 2_ChannelAdapter → 3_Normalize → 4_MessageBuffer → 5_TurnDetector
    ↓
6_Multi-Agent (основная логика)
    ↓ если эскалация
7_Escalation_Handler
    ↓
8_Telegram_Adapter → Telegram группа
    ↓ callback от менеджера
9_Telegram_Callback → WhatsApp (ответ клиенту)
```

### Workflows и их роли

| ID | Название | Роль |
|----|----------|------|
| 656fmXR6GPZrJbxm | 1_Webhook | Точка входа, маршрутизация по client_slug |
| C38zCf2jfc2Zqfzf | 2_ChannelAdapter | Нормализация из ChatFlow формата |
| DCs6AoJDIOPB4ZtF | 3_Normalize | Очистка текста |
| 3QqFRxapNa29jODD | 4_MessageBuffer | Буферизация сообщений (Redis) |
| kEXEMbThwUsCJ2Cz | 5_TurnDetector | Определение конца "хода" клиента |
| **4vaEvzlaMrgovhNz** | **6_Multi-Agent** | **Главный — классификация, RAG, ответ** |
| **7jGZrdbaAAvtTnQX** | **7_Escalation_Handler** | **Решает что делать с эскалацией** |
| **fFPEbTNlkBSjo66A** | **8_Telegram_Adapter** | **Отправка в Telegram, создание топиков** |
| **HQOWuMDIBPphC86v** | **9_Telegram_Callback** | **Обработка кнопок и ответов менеджера** |

---

## ЧАСТЬ 3: WORKFLOW 6_Multi-Agent (ДЕТАЛЬНО)

### Путь данных

```
Start (webhook data)
    ↓
Parse Input — извлекает: client_slug, phone, remoteJid, senderName, message
    ↓
Intent Router → Skip Classifier? (проверка нужна ли классификация)
    ↓
[Если нужна классификация]
Load History for Classifier → Format Classifier Input → OpenAI Chat Model → 
Structured Output Parser → Classify Intent
    ↓
Is On Topic (IF: on_topic == true)
    ↓ [true]                              ↓ [false]
Upsert User                          Off-topic ответ
    ↓
Save User Message → Load History → Load Prompt → Build Context
    ↓
Check Active Handover (проверка есть ли активный handover)
    ↓
Handover Active? (IF)
    ↓ [true]                              ↓ [false]
Forward to Topic                     Is Deadlock (проверка тупика)
Save Client Message                      ↓ [true]              ↓ [false]
Exit                                Prepare Escalation    RAG Search
                                    Call Escalation       Add Knowledge
                                    Handler               Prepare Prompt
                                                          Generate Response
                                                          Check Escalation
                                                              ↓
                                                    [needs_escalation?]
                                                          ↓ [true]
                                                    Prepare Escalation Data
                                                    Call Escalation Handler
```

### Ключевые ноды и их данные

#### Parse Input
**Input:** Webhook data от ChatFlow
**Output:**
```json
{
  "client_slug": "demo_salon",
  "phone": "77015705555",
  "remoteJid": "77015705555@s.whatsapp.net",
  "senderName": "Клиент",
  "message": "Хочу записаться"
}
```

#### Upsert User
**Input:** Данные от Parse Input (ЯВНО через `$('Parse Input').first().json`)
**Output:**
```json
{
  "user_id": "uuid",
  "conversation_id": "uuid",
  "client_id": "uuid"
}
```
**КРИТИЧНО:** Все поля берутся из Parse Input явно!

#### Check Active Handover
**SQL:**
```sql
SELECT 
  h.id as handover_id,
  h.conversation_id as handover_conversation_id,
  c.telegram_topic_id,
  cs.telegram_chat_id,
  cs.telegram_bot_token,
  COALESCE(u.name, u.phone, 'Клиент') as client_name
FROM conversations c
LEFT JOIN handovers h ON h.conversation_id = c.id AND h.status = 'active'
...
```
**Output:** Всегда возвращает данные (LEFT JOIN), handover_id = null если нет активного.

#### Is Deadlock
**Проверяет:** `reason == 'human_request'` (клиент явно просит менеджера)
**ВАЖНО:** Должен быть ПОДКЛЮЧЁН к Build Context! Был отключён — это была причина многих проблем.

#### Prepare Escalation Data
**Собирает данные из разных нод для передачи в Escalation Handler:**
```javascript
return [{
  json: {
    conversation_id: $('Upsert User').first().json.conversation_id,
    client_id: $('Upsert User').first().json.client_id,
    phone: $('Parse Input').first().json.phone,
    remoteJid: $('Parse Input').first().json.remoteJid,
    message: $('Parse Input').first().json.message,
    reason: $('Build Context').first().json.reason || 'needs_escalation',
    bot_response: $('Generate Response').first()?.json?.response || null
  }
}];
```

---

## ЧАСТЬ 4: WORKFLOW 7_Escalation_Handler (ДЕТАЛЬНО)

### Путь данных

```
Start (данные от Multi-Agent)
    ↓
Load Status — загружает bot_status, no_count, настройки клиента
    ↓
Decide Action — решает: process или silent_exit
    ↓
Should Process? (IF: action == 'process')
    ↓ [true]                              ↓ [false]
Update Conversation                   Silent Exit (конец)
Create Handover
Call Telegram Adapter
Should Respond?
    ↓ [true]
Send WhatsApp Response
Save Response
```

### Логика Decide Action

```javascript
// Если бот замьючен — молчим
if (isMuted) {
  action = 'silent_exit';
}
// Если клиент просит человека
else if (input.reason === 'human_request') {
  newNoCount = noCount + 1;
  if (newNoCount === 1) {
    // Первый раз — эскалируем
    responseText = 'Передаю ваш вопрос менеджеру...';
    shouldMute = true;
  } else {
    // Повторно — молчим (уже передали)
    action = 'silent_exit';
  }
}
```

### Update Conversation
**SQL:**
```sql
UPDATE conversations SET
  bot_status = CASE WHEN shouldMute THEN 'muted' ELSE bot_status END,
  bot_muted_until = CASE WHEN shouldMute THEN NOW() + INTERVAL 'X minutes' ELSE bot_muted_until END,
  no_count = newNoCount
WHERE id = conversation_id;
```

**ВАЖНО:** `bot_muted_until` — таймаут! Через 30 минут бот автоматически размьютится.

---

## ЧАСТЬ 5: WORKFLOW 8_Telegram_Adapter (ДЕТАЛЬНО)

### Путь данных

```
Start (данные от Escalation Handler)
    ↓
Prepare Data — готовит данные для отправки
    ↓
Get Existing Topic — ищет топик по conversation_id
    ↓
Has Topic? (IF: telegram_topic_id exists)
    ↓ [false]                             ↓ [true]
Create Topic                          Get Topic ID
    ↓                                     ↓
Get Topic ID ←────────────────────────────┘
    ↓
Send Escalation — отправляет сообщение с кнопками
    ↓
Pin Escalation — закрепляет сообщение
    ↓
Save Channel Refs — сохраняет topic_id в conversations
```

### Формат сообщения эскалации

```
🆕 НОВАЯ ЗАЯВКА
📱 Телефон: 77015705555
👤 Клиент: demo_salon

💬 Сообщение:
Хочу записаться на маникюр

[Беру]
```

### Кнопки (inline keyboard)

```json
{
  "inline_keyboard": [[
    {"text": "Беру", "callback_data": "take_UUID"}
  ]]
}
```

После [Беру]:
```json
{
  "inline_keyboard": [[
    {"text": "Решено ✅", "callback_data": "resolve_UUID"}
  ]]
}
```

После [Решено]: кнопки убираются полностью (`inline_keyboard: []`).

---

## ЧАСТЬ 6: WORKFLOW 9_Telegram_Callback (ДЕТАЛЬНО)

### Путь данных — Callback (кнопки)

```
Telegram Webhook
    ↓
Parse Callback — определяет тип (callback или message)
    ↓
Get Bot Token — загружает токен из БД по topic_id
    ↓
Merge Token — объединяет данные
    ↓
Is Callback? (IF: type == 'callback')
    ↓ [true]
Action Switch (action == 'take' / 'resolve' / другое)
    ↓
[take]                              [resolve]
Take Handover                       Resolve Handover
Take Response                       Unmute Bot
Answer Callback                     Resolve Response
Update Buttons                      Remove Buttons Resolve
                                    Unpin Escalation
                                    Answer Callback Resolve
```

### Путь данных — Message (ответ менеджера)

```
Is Callback? 
    ↓ [false]
Parse Message — извлекает текст, topic_id
    ↓
Find Handover Data — ищет активный handover по topic_id
    ↓
Has Active Handover?
    ↓ [true]
Send Manager Reply to WhatsApp — Chatflow API
    ↓
Save Manager Message — в handover.messages JSONB
    ↓
Confirm Sent to Topic — "✅ Доставлено"
    ↓
Wait 3s → Delete Confirm Message
```

### Parse Callback — формат output

```json
{
  "type": "callback",           // или "message"
  "action": "take",             // take, resolve, или пустой
  "handover_id": "uuid",
  "manager_id": "1969855532",
  "manager_name": "Zh",
  "callback_query_id": "...",
  "message_id": 65,
  "chat_id": -1003412216010,
  "topic_id": 15,
  "bot_token": "8249719610:AAG..."  // после Merge Token
}
```

### Take Handover — SQL с race condition защитой

```sql
UPDATE handovers 
SET status = 'active', 
    assigned_to = 'manager_id', 
    assigned_to_name = 'Zh'
WHERE id = 'uuid' 
  AND status = 'pending'  -- только если pending!
RETURNING id, conversation_id;
```

Если два менеджера нажмут одновременно — только первый получит handover.

### Unmute Bot — SQL

```sql
UPDATE conversations 
SET bot_status = 'active', 
    bot_muted_until = NULL, 
    no_count = 0  -- ВАЖНО: сброс счётчика!
WHERE id = (
  SELECT conversation_id FROM handovers 
  WHERE id = 'handover_id'
);
```

---

## ЧАСТЬ 7: БАЗА ДАННЫХ

### Ключевые таблицы

#### conversations
```sql
id                  UUID PRIMARY KEY
client_id           UUID (FK → clients)
user_id             UUID (FK → users)
status              VARCHAR (active, closed, escalated)
bot_status          VARCHAR (active, muted)
bot_muted_until     TIMESTAMPTZ  -- когда размьютить
no_count            INT          -- сколько раз клиент сказал "нет"
telegram_topic_id   INT          -- ID топика в Telegram
```

#### handovers
```sql
id                  UUID PRIMARY KEY
conversation_id     UUID (FK → conversations)
status              VARCHAR (pending, active, resolved)
trigger_type        VARCHAR (intent, bot_uncertain, frustration)
escalation_reason   VARCHAR (human_request, needs_escalation)
assigned_to         VARCHAR  -- Telegram user_id менеджера
assigned_to_name    VARCHAR  -- Имя менеджера
resolved_at         TIMESTAMPTZ
messages            JSONB    -- история сообщений для автообучения
channel             VARCHAR  -- telegram
channel_ref         VARCHAR  -- reference в канале
telegram_message_id INT      -- ID сообщения с кнопками
```

#### client_settings
```sql
client_id                       UUID (FK → clients)
telegram_chat_id                VARCHAR  -- ID группы
telegram_bot_token              VARCHAR  -- токен бота
silence_after_first_no_minutes  INT (default 30)
max_retry_offers                INT (default 1)
```

### Связи

```
clients (1) ←→ (N) users
clients (1) ←→ (N) conversations
clients (1) ←→ (1) client_settings
users (1) ←→ (N) conversations
conversations (1) ←→ (N) handovers
conversations (1) ←→ (N) messages
```

---

## ЧАСТЬ 8: ИНСТРУМЕНТЫ ДИАГНОСТИКИ

### Быстрая диагностика
```bash
ssh -p 222 zhan@5.188.241.234
cd ~/truffles/ops
python3 diagnose.py
```
**Показывает:** Статус последних executions, количество записей в БД.

### Детали execution
```bash
python3 get_exec_detail.py EXECUTION_ID
```
**Показывает:** Какие ноды выполнились, их input/output.

### Последний execution workflow
```bash
python3 get_latest_exec.py
python3 get_multiagent_exec.py
python3 check_escalation_exec.py
python3 check_adapter_exec.py
```

### Debug по телефону клиента
```bash
python3 debug_client.py 77015705555
```

### Проверка состояния БД
```bash
docker exec -i truffles_postgres_1 psql -U n8n -d chatbot -c "SELECT * FROM conversations;"
docker exec -i truffles_postgres_1 psql -U n8n -d chatbot -c "SELECT * FROM handovers;"
```

### Сброс для тестирования
```bash
docker exec -i truffles_postgres_1 psql -U n8n -d chatbot < ~/truffles/ops/clean_reset.sql
```

---

## ЧАСТЬ 9: ВСЕ ОШИБКИ И КАК РЕШИЛИ

### Ошибка 1: MessageBuffer смешивал сообщения разных ботов
**Симптом:** Сообщения от demo_salon обрабатывались как truffles.
**Причина:** Ключ Redis был `chat:${session_id}` без client_slug.
**Решение:** Изменили на `chat:${client_slug}:${session_id}`.

### Ошибка 2: Is Deadlock не выполнялся
**Симптом:** reason всегда 'escalation', никогда 'human_request'.
**Причина:** Is Deadlock был отключён от flow (Build Context → RAG Search напрямую).
**Решение:** Подключили Build Context → Is Deadlock → [true] Escalation / [false] RAG.

### Ошибка 3: Upsert User создавал undefined users
**Симптом:** phone='undefined' в БД.
**Причина:** `$json.phone` брал данные от Is On Topic, не от Parse Input.
**Решение:** Явные ссылки `$('Parse Input').first().json.phone`.

### Ошибка 4: Unmute Bot падал с undefined conversation_id
**Симптом:** `WHERE id = 'undefined'` в SQL.
**Причина:** Resolve Handover возвращает `{success: true}`, не данные из RETURNING.
**Решение:** Unmute Bot сам ищет conversation_id по handover_id.

### Ошибка 5: [Решено] падал с "Take Response not executed"
**Симптом:** Resolve flow проходил через ноды для Take flow.
**Причина:** Общий Answer Callback → Update Buttons → Notify in Chat для обоих flow.
**Решение:** Отдельный Answer Callback Resolve для resolve flow.

### Ошибка 6: Кнопка не исчезала после [Решено]
**Симптом:** Кнопка [✅ Решено] с callback_data='done'.
**Причина:** Remove Buttons Resolve ставил кнопку, а не убирал.
**Решение:** `reply_markup: {inline_keyboard: []}` — пустой массив.

### Ошибка 7: Эскалация не создавалась повторно
**Симптом:** silent_exit при втором "хочу менеджера".
**Причина:** no_count не сбрасывался после [Решено].
**Решение:** Unmute Bot также сбрасывает `no_count = 0`.

### Ошибка 8: Дубликаты топиков
**Симптом:** Новый топик создавался для каждого handover.
**Причина:** Adapter искал топик по handover_id, не по conversation_id.
**Решение:** telegram_topic_id хранится в conversations, ищем по conversation_id.

### Ошибка 9: Parse Callback неправильно парсил UUID
**Симптом:** handover_id обрезался.
**Причина:** `split('_')` разбивал UUID который тоже содержит _.
**Решение:** `split('_', 1)` — разбивать только по первому _.

---

## ЧАСТЬ 10: ЛУЧШИЕ ПРАКТИКИ N8N

### 1. Всегда явные ссылки на источник данных
```javascript
// НИКОГДА
$json.field

// ВСЕГДА
$('SourceNode').first().json.field
```

### 2. Проверять connections перед изменением
Открыть workflow в n8n UI, проверить откуда идут стрелки.

### 3. После изменения — проверить execution
1. Выполнить тестовый запрос
2. Открыть execution
3. Проверить каждую ноду — что на входе, что на выходе
4. Искать undefined, null, пустые объекты

### 4. Разделять ветки полностью
Если есть IF с двумя ветками — каждая ветка должна иметь свои ноды до конца. Не объединять в общую ноду которая использует данные из одной ветки.

### 5. SQL с RETURNING не работает как ожидается
Postgres node в режиме executeQuery возвращает `{success: true}`, не данные. Если нужны данные — делать отдельный SELECT или использовать подзапрос.

### 6. Тестировать каждое изменение
Не менять 5 вещей и потом тестировать. Изменил одну ноду → протестировал → работает → следующее изменение.

---

## ЧАСТЬ 11: ЧТО РАБОТАЕТ СЕЙЧАС

### Полный flow эскалации

1. **Клиент пишет "хочу менеджера"**
   - Multi-Agent классифицирует как human_request
   - Is Deadlock = true
   - Вызывается Escalation Handler

2. **Escalation Handler**
   - Проверяет no_count (первый раз?)
   - Создаёт handover в БД
   - Мьютит бота на 30 минут
   - Вызывает Telegram Adapter

3. **Telegram Adapter**
   - Создаёт топик (если нет) или использует существующий
   - Отправляет сообщение с кнопкой [Беру]
   - Закрепляет сообщение

4. **Менеджер нажимает [Беру]**
   - Callback обрабатывается в Telegram_Callback
   - handover.status = 'active'
   - Кнопка меняется на [Решено ✅]

5. **Клиент пишет ещё**
   - Multi-Agent видит активный handover
   - Сообщение пересылается в топик менеджеру
   - Бот молчит (не отвечает клиенту)

6. **Менеджер отвечает в топике**
   - Telegram_Callback ловит сообщение
   - Отправляет через Chatflow API в WhatsApp клиенту
   - Сохраняет в handover.messages

7. **Менеджер нажимает [Решено ✅]**
   - handover.status = 'resolved'
   - Бот размьючивается (bot_status = 'active')
   - no_count сбрасывается в 0
   - Кнопка исчезает
   - Закреп убирается

8. **Клиент пишет снова**
   - Бот отвечает нормально (размьючен)

### Таймаут безопасности

Если менеджер забыл нажать [Решено]:
- `bot_muted_until = NOW() + 30 minutes`
- Через 30 минут бот автоматически начнёт отвечать
- Это УЖЕ работает, заложено в Escalation Handler

---

## ЧАСТЬ 12: ЧТО НЕ ДЕЛАЕМ (ПОКА)

### Автообучение
**Что это:** Ответы менеджера автоматически попадают в RAG.
**Почему не сейчас:** Нужна стабильная база. Сначала всё должно работать без ошибок.
**Что подготовлено:**
- handover.messages JSONB — сообщения сохраняются
- Структура для модерации заложена в SPECS/

### Модерация владельцем
**Что это:** Кнопки "Одобрить / Отклонить" для добавления в базу знаний.
**Почему не сейчас:** Зависит от автообучения.

### Dashboard
**Что это:** Статистика для заказчика.
**Почему не сейчас:** Нет срочности, можно смотреть в БД.

### Запись клиентов
**Что это:** Бот записывает на услуги.
**Почему не сейчас:** Специфика ниши, не базовая функция.

---

## ЧАСТЬ 13: РАСХОДЫ

### Сервер
- VPS: ~$35-40/мес (текущий)
- При росте: масштабировать вертикально или добавить ноды

### LLM (на 1000 сообщений/мес на клиента)
- Классификация (gpt-4o-mini): ~$1-2
- Response (gpt-4o): ~$5-7
- Проверка (gpt-4o-mini): ~$1-2
- **Итого LLM:** ~$7-10/мес на клиента

### WhatsApp
- ChatFlow instance: ~$10-15/мес на номер

### Итого на клиента
- ~$23-31/мес ≈ 10,000-14,000 тг
- При тарифе 50,000 тг → маржа 72-80%

---

## ЧАСТЬ 14: ФАЙЛЫ И ГДЕ ЧТО

### На сервере ~/truffles/ops/

| Файл | Назначение |
|------|------------|
| diagnose.py | Быстрая диагностика |
| get_latest_exec.py | Последний execution |
| get_exec_detail.py | Детали execution |
| get_multiagent_exec.py | Execution Multi-Agent |
| check_callback_flow.py | Проверка Callback workflow |
| debug_client.py | Debug по телефону |
| clean_reset.sql | Сброс БД для теста |
| sync_client.py | Синхронизация документов в Qdrant |
| onboard_client.py | Онбординг нового клиента |
| health_check.py | Проверка сервисов |

### В репозитории

| Путь | Назначение |
|------|------------|
| workflow/*.json | Экспорт workflows |
| ops/migrations/*.sql | Миграции БД |
| MASTER_PLAN.md | Главный план |
| AGENTS.md | Доступы и правила |
| docs/HANDOVER_*.md | Эстафеты |
| SPECS/*.md | Спецификации систем |

---

## ЧАСТЬ 15: ЧЕКЛИСТ ДЛЯ СЛЕДУЮЩЕЙ СЕССИИ

### Перед началом работы
- [ ] Прочитать этот документ
- [ ] Запустить `diagnose.py` — проверить что всё работает
- [ ] Проверить последние executions — нет ли ошибок

### При изменении workflow
- [ ] Понять откуда данные приходят в ноду
- [ ] Использовать явные ссылки `$('Node').first().json`
- [ ] Проверить connections
- [ ] Протестировать сразу после изменения
- [ ] Проверить execution — все ли ноды выполнились

### При ошибке
- [ ] Найти execution с ошибкой
- [ ] Посмотреть какая нода упала
- [ ] Посмотреть input этой ноды — есть ли undefined
- [ ] Проследить откуда данные должны были прийти
- [ ] Исправить источник данных

---

## ДОСТУПЫ

### SSH
```
ssh -i C:\Users\user\.ssh\id_rsa -p 222 zhan@5.188.241.234
```

### n8n API
```
URL: https://n8n.truffles.kz
API Key: <REDACTED>_JWT
```

### PostgreSQL
```
Host: postgres (внутри Docker) или через SSH
DB: chatbot
User: n8n
Password: REDACTED_PASSWORD
```

### Telegram боты
- truffles: отдельный бот (для своих тестов)
- demo_salon: REDACTED_TELEGRAM_BOT_TOKEN

### Telegram группы
- Demo Salon: -1003412216010 (Forum mode включён)

---

*Документ создан: 2025-12-08*
*Следующее обновление: после каждой сессии*

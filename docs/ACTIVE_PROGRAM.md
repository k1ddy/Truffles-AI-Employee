# ACTIVE PROGRAM

Program: Consultant Core Whole-System Architecture Closure

Current truth:
- external packet: `ready_for_external_handoff`
- whole-system practical truth is still `r35f`; the focused `Block H.1` oracle proof plus reopened `Block H.1B` file-replay proof removed the stale booking-completion and booking-manage scenario blocker families on both compiler and file-replay seams, but whole-system acceptance remains separate until a fresh full replay reruns `Block H`
- implementation state: `Block A`, `Block B`, `Block C`, `Block C.5`, `Block D`, `Block E`, `Block E.5`, `Block E.6`, `Block F`, `Block G`, the focused `booking-manage temporal clue grounding / follow-up continuity` block, `Block H.1 — Oracle Scenario Contract Alignment`, and `Block H.1B — File Replay Scenario Contract Materialization` are `closed_proven` in the active worktree; `Block H` and whole-system acceptance remain open
- prior narrative-only closure claims remain invalidated history; the current base is live-code reproof, not product closure
- machine-readable governance base: active docs track the block-by-block runtime closeout plan instead of the stale repo-side reproof narrative
- historical residue rule: later `r36*` replay/RCA/runtime materials and invalidated whole-system closure claims remain non-governing history under `docs/RECOVERY_EXECUTION_LOCK.yaml`

Governing docs:
- `docs/DECISIONS/DEC-2026-03-31-consultant-core-whole-system-architecture-closure-governing-decision.md`
- `docs/system_forensics/SYSTEM_VERDICT.md`
- `docs/ACTIVE_CANON.md`
- `docs/SOURCE_OF_TRUTH.yaml`
- `docs/LEGACY_SUNSET.yaml`
- `docs/TASK_PACKAGES/TP-2026-03-31-consultant-core-whole-system-architecture-closure-master-program-a922.md`
- `docs/TASK_PACKAGES/TP-2026-04-01-consultant-core-block-a-interrupt-arbitration-and-continuation-law-a922.md`
- `docs/TASK_PACKAGES/TP-2026-04-01-consultant-core-block-b-fact-scope-exactness-a922.md`
- `docs/TASK_PACKAGES/TP-2026-04-01-consultant-core-block-c-continuity-carrier-collapse-a922.md`
- `docs/TASK_PACKAGES/TP-2026-04-01-consultant-core-block-c5-policy-info-interrupt-fact-delivery-a922.md`
- `docs/TASK_PACKAGES/TP-2026-04-01-consultant-core-block-d-boundary-purification-a922.md`
- `docs/TASK_PACKAGES/TP-2026-04-01-consultant-core-block-e-real-pack-runtime-separation-a922.md`
- `docs/TASK_PACKAGES/TP-2026-04-01-consultant-core-block-e5-owner-service-referent-grounding-a922.md`
- `docs/TASK_PACKAGES/TP-2026-04-01-consultant-core-block-e6-post-grounding-service-reply-exactness-a922.md`
- `docs/TASK_PACKAGES/TP-2026-04-01-consultant-core-block-f-legacy-mesh-final-drain-a922.md`
- `docs/TASK_PACKAGES/TP-2026-04-01-consultant-core-block-g-operational-final-dedupe-a922.md`
- `docs/TASK_PACKAGES/TP-2026-04-01-consultant-core-booking-manage-temporal-clue-grounding-followup-continuity-a922.md`
- `docs/TASK_PACKAGES/TP-2026-04-01-consultant-core-block-h1-oracle-scenario-contract-alignment-a922.md`
- `docs/TASK_PACKAGES/TP-2026-04-01-consultant-core-block-h1b-file-replay-scenario-contract-materialization-a922.md`
- `docs/RECOVERY_EXECUTION_LOCK.yaml`

Current objective:
- rerun `Block H — Final Acceptance` now that the surfaced booking-manage plus both oracle scenario blocker families are proven removed on focused evidence; no product closure claim is admissible until that full replay and audit pass

Current block:
- Consultant Core Block H.1B — File Replay Scenario Contract Materialization (`closed_proven`)

Current progress unit:
- `Block A` closed the active-followup interrupt law on the touched path: specialist/master queries no longer stay trapped inside stale media continuation, generic info interrupts preserve booking continuity explicitly, and media continuation survives only while the user is still fulfilling the media request
- `Block B` closed exact fact scope on the touched `location / hours / parking` path: owner `catalog.location` asks now carry exact `pack_refs`, `fact_plane` prefers explicit refs over coarse family aliases, and touched renderers emit only the exact allowed sections
- `Block C` closed continuity-carrier authority on the touched follow-up/resume path: canonical pending-question continuity is now the sole mutable writer, compatibility carriers are rebuilt from canonical state, and reset-boundary transcript contamination is gone
- `Block C.5` closed exact policy-info interrupt fact delivery on the touched `catalog.service_query` path: owner `pack_refs=["promotions"]` now collapse to `requested_fact_refs=["promotions"]`, the fact contract accepts the exact emitted section, and the active booking continuity remains intact through the interrupt and the next booking answer
- `Block D` closed boundary purification on the live path: boundary override meta no longer carries visible handoff or reply business-control, executor-requested handoff degrades through an explicit canonical `HANDOFF` decision, and stale state-service boundary restore helpers are caller-guarded residue only
- `Block E` closed real pack/runtime separation on the touched hot path: `pack_runtime_service.py` now exposes one narrow selected-adapter boundary, `turn_executor.py` and `tool_registry_service.py` consume that boundary only, and focused replay `a922-block-e-replay-20260401h` proves exact `hours` via `catalog.location` plus `promotions` via `catalog.service_query` while preserving booking continuity
- `Block E.5` closed owner service referent grounding on the touched fact envelope: policy-core now receives pack-backed `service_cards`, inline-service `duration` / `master` grounding examples are explicit in the owner prompt, and focused replay `a922-block-e5-replay-20260401f` proves that standalone plus carried-service grounded fact questions stay on `catalog.service_query` instead of synthetic `collect service`
- `Block E.6` closed post-grounding service reply exactness on the touched fact envelope: `pack_runtime_service.py` now bridges grounded service referents across published `effective_pack` surfaces (`services_catalog`, exact `price_list`, coarse `team`), focused replay `a922-block-e6-replay-20260401r` proves exact non-clarifying `duration` plus factual team-backed `master` replies, and the next admissible runtime block is `Block F — Legacy Mesh Final Drain`
- `Block F` closed legacy-mesh final drain on the live import/caller topology: `__init__.py` is now guarded as adapter-only, `session_memory.py` is the only adapter-only live legacy helper on the touched seam, `decision.py` / `info.py` / `context_manager.py` are guarded as unreachable from the live runtime boundary, and the next admissible runtime block is `Block G — Operational Final Dedupe`
- `Block G` closed operational final dedupe on the live outbox seam: `admin.py`, `outbox_service.py`, `workers/outbox.py`, and `console.py` now all route through one canonical runtime boundary, console dry-run and execute share `ScopedOutboxProcessRequest` plus runtime-owned preview/execute helpers, and low-level execution helpers are no longer exported on the public boundary
- the focused `booking-manage temporal clue grounding / follow-up continuity` block closed the first surfaced `Block H` product blocker: `turn_executor.py` now voices the owner-backed `calendar.get_booking` follow-up contract on the fact path, preserves weekday clues from the inbound message, and focused replay `a922-booking-manage-temporal-clue-replay-20260401d` is fully green with zero failure families
- `Block H.1` closed the follow-on oracle-only blocker on the evaluation seam: `llm_quality_contracts.py` now marks `check_booking` entry turns and active-name answer turns as reply-required, clears stale `reply_type` / pending-question expectations, preserves `confirm` tags during post-coverage repair, and focused replay `a922-block-h1-replay-20260401a` is fully green with zero failure families
- `Block H.1B` closed the reopened oracle-only blocker on the shared file-replay seam: `ops/diagnose.py` now materializes `--scenarios-file` dialogs through the shared booking scenario contract pipeline before evaluation, and focused raw-file replay `a922-block-h1b-replay-20260401c` is fully green with zero failure families

Reopened execution order:
1. `Block A — Interrupt Arbitration And Continuation Law` (`closed_proven`)
2. `Block B — Fact Scope Exactness` (`closed_proven`)
3. `Block C — Continuity Carrier Collapse` (`closed_proven`)
4. `Block C.5 — Policy-Info Interrupt Fact Delivery` (`closed_proven`)
5. `Block D — Boundary Purification` (`closed_proven`)
6. `Block E — Real Pack/Runtime Separation` (`closed_proven`)
7. `Block E.5 — Owner Service Referent Grounding` (`closed_proven`)
8. `Block E.6 — Post-Grounding Service Reply Exactness` (`closed_proven`)
9. `Block F — Legacy Mesh Final Drain` (`closed_proven`)
10. `Block G — Operational Final Dedupe` (`closed_proven`)
11. `Booking-manage temporal clue grounding / follow-up continuity` (`closed_proven`)
12. `Block H.1 — Oracle Scenario Contract Alignment` (`closed_proven`)
13. `Block H.1B — File Replay Scenario Contract Materialization` (`closed_proven`)
14. `Block H — Final Acceptance` (`next`)

Parallel execution rule:
- no micro-credit is granted inside a block
- only full-block closure updates `STATE.md`, active canon/program, packet, and reports

Current non-negotiable next move:
- rerun `Block H — Final Acceptance` from one fresh full replay plus full human semantic audit now that the surfaced booking-manage plus both oracle scenario blocker families are proven removed

Open blockers that remain honest in this block:
- whole-system practical truth is still not refreshed; `Block A/B/C/C.5/D/E/E.5/E.6/F` proofs are focused block evidence only, and no fresh full current-head acceptance replay exists after them
- final acceptance replay and full human semantic audit still remain open after the focused `Block H.1` and `Block H.1B` oracle alignment proofs
- whole-system practical truth remains `r35f` until `Block H` is rerun on current head
- evaluator-only judge disagreement remains on the team-backed master reply in `/tmp/booking_quality/a922-block-e6-replay-20260401r`, but contract/meta evidence plus full human audit marked that turn valid and the residual does not reopen `Block E.6`
- no `done` or `green` claim is admissible before replay + full human semantic audit pass

Forbidden shortcuts:
- any product or practical closure claim before acceptance
- any baseline refresh from stale or invalid runs
- any state/canon update after micro-actions inside an unfinished block

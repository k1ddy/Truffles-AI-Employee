# ACTIVE PROGRAM

Program: Consultant Core Controlled Demolition

Current objective:
- replace multi-owner consultant runtime with one semantic core, one continuity store, and a black-box proof path

Current block:
- Consultant Core Behavioral Proof Lock

Current progress unit:
- the surfaced truthful-runtime blocker family is now reduced in three bounded fixes on the current worktree: sparse/action-scoped policy-core structured output, final-tool-action execution projection, and handoff-time clearing of stale pending-question / expected-reply carryover
- direct truthful policy-core probes on `2026-03-26` are now green for the exact surfaced turns: initial booking entry stays on canonical `ask_about_requested_slot(time)`, generic availability question under active booking keeps the same relation, and reschedule-without-reference now escalates to `handoff`
- partial truthful reruns `/tmp/booking_quality/a922-l2-proof-seed7-devfix8-20260326` and `/tmp/booking_quality/a922-l2-proof-seed7-devfix9-20260326` moved the family off the original early `planner_degrade -> HANDOFF/pending` seam and exposed the next two deterministic seams that were then fixed
- the latest rerun `/tmp/booking_quality/a922-l2-proof-seed7-devfix10-20260326` is not closure evidence because it was manually interrupted (`stop_reason=signal_2`) and therefore only fails on `run_completion_gap`
- the next honest move is to commit this bounded implementation family, restart the truthful runtime on the committed `HEAD`, and rerun seed `7`; no further runtime refactor is justified before that proof step
- manual closure on the current runtime is green, but truthful behavioral proof is now blocked on the first required `L2` seed of the truthful worktree runtime, not on missing paperwork
- fresh `L0/L1` evidence is green and the truthful local runtime is confirmed on `127.0.0.1:18190` with `git_commit == HEAD` and `test_mode_enabled=true`, but `/tmp/booking_quality/a922-l2-proof-seed7-20260325/summary.json` is `infra_valid=true`, `semantic_valid=false`, `manual_audit_status=done`, `stop_reason=signal_15`
- the surfaced blocker family is concrete: simple booking/info entry on the truthful worktree runtime degrades into `planner_degrade -> HANDOFF/pending`, producing `wrong_action`, `expected_state_mismatch`, and `expected_reply_mismatch` at the start of seed `7`
- closed `docs/TASK_PACKAGES/TP-2026-03-25-consultant-core-continuity-writer-guard-and-owner-reset-a922.md` locally: `python3 scripts/continuity_writer_guard.py` is green after AST-aware write detection replaced the line-token heuristic and the remaining pending-resume writes were moved behind canonical helpers in `truffles-api/app/services/state_service.py`
- the governance stack is green on the current worktree: `python3 scripts/arch_guard.py` -> `OK`, `python3 scripts/semantic_bridge_growth_guard.py` -> `OK`, `python3 scripts/legacy_freeze_guard.py` -> `OK`, `PYTHONPATH=truffles-api pytest -q truffles-api/tests/architecture` -> `23 passed`, `SESSION_AGENT=a922 scripts/session_check.sh` -> `Session OK`, `git diff --check` -> `pass`
- published `docs/REPORTS/artifacts/2026-03-25-consultant-core-policy-core-live-manual-closure-a922.md` and prepared `docs/TASK_PACKAGES/TP-2026-03-25-consultant-core-policy-core-acceptance-replay-a922.md`; live runtime health remains green on the current worktree lane (`eval_mode=livecheck`, `transport_send_mode=allowlist`, `outbox_worker_mode=off`, `danger_flags=[]`)
- closed `docs/TASK_PACKAGES/TP-2026-03-25-consultant-core-public-entrypoint-http-contract-and-message-endpoint-surface-reset-a922.md` locally: `truffles-api/app/core/consultant_runtime.py` now re-raises `HTTPException`, so invalid-secret / invalid-tenant public-entrypoint failures propagate as HTTP errors instead of being swallowed by the generic runtime fallback
- `truffles-api/tests/test_message_endpoint.py` is green again (`187 passed`) after resetting the file to the current live test surface; `/message` patches now target the public entrypoint/runtime path and the stale top-level `_handle_webhook_payload` wrapper family was cut instead of being preserved through a new compatibility layer
- the current full guard stack remains green on the current worktree: `python3 scripts/semantic_bridge_growth_guard.py` -> `OK`, `python3 scripts/continuity_writer_guard.py` -> `OK`, `python3 scripts/legacy_freeze_guard.py` -> `OK`, `python3 scripts/arch_guard.py` -> `OK`, `PYTHONPATH=truffles-api pytest -q truffles-api/tests/architecture` -> `23 passed`, `SESSION_AGENT=a922 scripts/session_check.sh` -> `Session OK`, `git diff --check` -> `pass`
- the next honest move is one guarded Phase E acceptance replay on the current runtime fingerprint
- published `docs/TASK_PACKAGES/TP-2026-03-24-consultant-core-pending-booking-reentry-initial-booking-invalid-schema-authority-reset-structural-implementation-a922.md` and `docs/REPORTS/artifacts/2026-03-24-consultant-core-pending-booking-reentry-initial-booking-invalid-schema-authority-reset-structural-implementation-a922.md`, then switched canon from the `r53` decision block to the structural implementation block
- `initial_booking_prompt_owner` now runs before early explicit handoff in the direct-owner chain at `truffles-api/app/services/reasoning_core.py:12058`, ahead of `truffles-api/app/services/reasoning_core.py:12085`
- reusable invalid-schema collect recovery is now shared through `truffles-api/app/services/policy_validation_boundary_service.py:121` and consumed by `truffles-api/app/core/booking_prompt_owner.py:286`, with the touched `invalid_schema` branch returning a canonical booking candidate at `truffles-api/app/core/booking_prompt_owner.py:504`
- initial booking invalid-schema recovery now records explicit guard observability at `truffles-api/app/services/reasoning_core.py:8255`, and the dead earlier `_should_accept_turn_planner_service_query_result(...)` duplicate def is deleted, reducing duplicate debt to `18` duplicate top-level names across `137` defs / `119` unique names
- focused deterministic validation is green: `python3 -m py_compile truffles-api/app/core/booking_prompt_owner.py truffles-api/app/services/reasoning_core.py truffles-api/app/services/policy_validation_boundary_service.py truffles-api/tests/test_reasoning_core.py truffles-api/tests/architecture/test_no_duplicate_core_defs.py` -> `pass`; `pytest -q truffles-api/tests/test_reasoning_core.py -k "pending_booking_reentry_preempts_explicit_handoff_without_boundary_payload or pending_invalid_schema_reactivation_keeps_booking_prompt or initial_booking_owner_recovers_invalid_schema_before_terminal_handoff or answers_service_grounded_promotions_interrupt_and_advances_to_time or explicit_handoff_owner or terminal_unresolved"` -> `18 passed, 199 deselected`; `pytest -q truffles-api/tests/architecture/test_no_duplicate_core_defs.py` -> `1 passed`
- all required guard and packet checks are green: `python3 scripts/build_agent_packet.py` -> regenerated `docs/_generated/AGENT_PACKET.md` and `docs/_generated/AGENT_PACKET.json`; `python3 scripts/build_agent_packet.py --check` -> `OK`; `pytest -q truffles-api/tests/architecture/test_arch_guard_packet.py` -> `1 passed`; `pytest -q truffles-api/tests/architecture` -> `19 passed`; `SESSION_AGENT=a922 scripts/session_check.sh` -> `Session OK`; `git diff --check` -> `pass`
- no replay was opened in this block; the next admissible move is now `run_one_fresh_closure_replay_only_after_pending_booking_reentry_initial_booking_invalid_schema_authority_reset_evidence`
- published `docs/REPORTS/artifacts/2026-03-24-consultant-core-pending-booking-reentry-booking-prompt-authority-reset-closure-replay-a922.md`, kept `docs/TASK_PACKAGES/TP-2026-03-24-consultant-core-pending-booking-reentry-booking-prompt-authority-decision-a922.md` as the active delete-first decision block, and switched canon back from the failed `r53` closure replay to that decision block
- fresh closure replay `/tmp/booking_quality/a922-go2f-seed19-r53` is complete and strict-audited: `infra_valid=true`, `semantic_valid=false`, `dialogs=10`, `turns=143`, `turns_strict_failed=5`, `strict_pass_rate=0.965`
- the exact strict-failed rows are `LLM-QUAL-a922-go2f-seed19-r53-002-09-bf0a7d`, `LLM-QUAL-a922-go2f-seed19-r53-002-10-864323`, `LLM-QUAL-a922-go2f-seed19-r53-003-01-f32c28`, `LLM-QUAL-a922-go2f-seed19-r53-003-02-a02bfb`, and `LLM-QUAL-a922-go2f-seed19-r53-006-01-0835dc`
- the same family is still live on two adjacent seams: `booking_prompt_owner` still requires `conversation_snapshot is not None` at `truffles-api/app/services/reasoning_core.py:6457`, and early explicit handoff still runs at `truffles-api/app/services/reasoning_core.py:12095` before late `initial_booking_prompt_owner` at `truffles-api/app/services/reasoning_core.py:12307`
- pending service-choice reactivation still drops recoverable `invalid_schema` collect payloads because `resolve_llm_booking_prompt_candidate(...)` only rescues timeout/deadline failures at `truffles-api/app/core/booking_prompt_owner.py:284`, while the reusable non-frozen invalid-schema boundary path already exists in `truffles-api/app/services/policy_validation_boundary_service.py`
- replay-first stays invalid; the next admissible move is now `execute_delete_first_pending_booking_reentry_initial_booking_invalid_schema_authority_reset_structural_block_after_one_precise_web_search`
- published `docs/TASK_PACKAGES/TP-2026-03-24-consultant-core-pending-booking-reentry-booking-prompt-authority-reset-structural-implementation-a922.md` and `docs/REPORTS/artifacts/2026-03-24-consultant-core-pending-booking-reentry-booking-prompt-authority-reset-structural-implementation-a922.md`, then switched canon from the delete-first decision block to the structural implementation block
- `booking_prompt_owner` now executes before touched catalog/info seams in the direct-owner chain at `truffles-api/app/services/reasoning_core.py:12053`, ahead of `truffles-api/app/services/reasoning_core.py:12119` and `truffles-api/app/services/reasoning_core.py:12132`
- `_finalize_turn_planner_owner_cutover(...)` now applies centralized continuity sync on the artifact fast path via `DialogStateService.build_expected_reply_context_sync_result(...)`, and the service-grounded promotions path now persists booking payload through `booking_payload_override`
- the dead earlier `_try_handle_turn_planner_safe_catalog_fact_owner_cutover(...)` and `_try_handle_turn_planner_safe_service_query_fact_owner_cutover(...)` top-level defs are deleted, reducing duplicate debt to `19` duplicate top-level names across `138` defs / `119` unique names
- focused deterministic validation is green: `python3 -m py_compile truffles-api/app/services/reasoning_core.py truffles-api/tests/test_reasoning_core.py truffles-api/tests/architecture/test_no_duplicate_core_defs.py` -> `pass`; `pytest -q truffles-api/tests/test_reasoning_core.py -k "pending_booking_reentry_preempts_explicit_handoff_without_boundary_payload or answers_service_grounded_promotions_interrupt_and_advances_to_time or booking_prompt_owner_reactivates_pending_collect_without_active_booking_snapshot or booking_prompt_owner_answers_promotions_interrupt_and_resumes_time_collect or pending_boundary_promotions_interrupt or explicit_handoff_owner or terminal_unresolved or pending_ack_continuity_family_clears_pending_before_terminal_unresolved"` -> `19 passed, 196 deselected`; `pytest -q truffles-api/tests/architecture/test_no_duplicate_core_defs.py` -> `1 passed`; `pytest -q truffles-api/tests/architecture` -> `19 passed`
- all required guard and packet checks are green: `python3 scripts/build_agent_packet.py` -> regenerated `docs/_generated/AGENT_PACKET.md` and `docs/_generated/AGENT_PACKET.json`; `python3 scripts/build_agent_packet.py --check` -> `OK`; `pytest -q truffles-api/tests/architecture/test_arch_guard_packet.py` -> `1 passed`; `SESSION_AGENT=a922 scripts/session_check.sh` -> `Session OK`; `git diff --check` -> `pass`
- no replay was opened in this block; the next admissible move is now `run_one_fresh_closure_replay_only_after_pending_booking_reentry_booking_prompt_authority_reset_evidence`
- published `docs/REPORTS/artifacts/2026-03-24-consultant-core-pending-post-cancel-rebooking-continuity-handoff-info-authority-reset-closure-replay-a922.md` and `docs/TASK_PACKAGES/TP-2026-03-24-consultant-core-pending-booking-reentry-booking-prompt-authority-decision-a922.md`, then switched canon from the failed closure replay to the new delete-first decision block
- fresh closure replay `/tmp/booking_quality/a922-go2f-seed19-r52` is complete and strict-audited: `infra_valid=true`, `semantic_valid=false`, `turns=143`, `strict_pass_rate=0.986`, and the only strict-failed rows are `LLM-QUAL-a922-go2f-seed19-r52-002-09-c14afa` plus `LLM-QUAL-a922-go2f-seed19-r52-002-10-733e03`
- the artifact still contains `44` executions of `turn_planner.safe_explicit_handoff_owner.v1` (`24` with `reason_code=terminal_owner_unresolved`), and row `002-10` still exits through `turn_planner.safe_info_fact.v1`, so the next honest move became a delete-first structural block rather than another replay
- published `docs/TASK_PACKAGES/TP-2026-03-24-consultant-core-pending-post-cancel-rebooking-continuity-handoff-info-authority-reset-structural-implementation-a922.md` and `docs/REPORTS/artifacts/2026-03-24-consultant-core-pending-post-cancel-rebooking-continuity-handoff-info-authority-reset-structural-implementation-a922.md`, then switched canon from the delete-first decision block to the structural implementation block
- `DialogStateService` now infers pending booking boundary state from the active booking gap itself, so post-cancel rebooking no longer needs explicit expected-reply metadata before the direct-owner chain can defer info/handoff fallback
- `reasoning_core.py` still precomputes `pending_booking_resume_boundary_payload` before the direct-owner chain, and the existing `safe_info_fact` plus explicit-handoff guards now see that inferred boundary on the touched family
- the dead earlier `_try_handle_turn_planner_safe_semantic_arbitration_owner_cutover(...)` top-level def is deleted, reducing duplicate debt to `21` duplicate top-level names across `140` defs / `119` unique names
- focused deterministic validation is green: `python3 -m py_compile truffles-api/app/core/dialog_state_service.py truffles-api/app/services/reasoning_core.py truffles-api/tests/test_dialog_state_service.py truffles-api/tests/test_reasoning_core.py truffles-api/tests/architecture/test_no_duplicate_core_defs.py` -> `pass`; `pytest -q truffles-api/tests/test_dialog_state_service.py -k "pending_resume_boundary"` -> `2 passed, 56 deselected`; `pytest -q truffles-api/tests/test_reasoning_core.py -k "booking_prompt_owner_reactivates_pending_post_cancel_rebooking_state or infers_post_cancel_rebooking_boundary_before_semantic_handoff or pending_boundary_promotions_interrupt or explicit_handoff_owner or terminal_unresolved"` -> `17 passed, 196 deselected`; `pytest -q truffles-api/tests/architecture/test_no_duplicate_core_defs.py` -> `1 passed`
- no replay was opened in this block; the next admissible move is now `run_one_fresh_closure_replay_only_after_pending_post_cancel_rebooking_continuity_handoff_info_authority_reset_evidence`
- published `docs/REPORTS/artifacts/2026-03-24-consultant-core-pending-booking-continuity-info-handoff-authority-reset-closure-replay-a922.md` and `docs/TASK_PACKAGES/TP-2026-03-24-consultant-core-pending-post-cancel-rebooking-continuity-handoff-info-authority-decision-a922.md`, then switched canon from the failed closure replay to the new delete-first decision block
- fresh closure replay `/tmp/booking_quality/a922-go2f-seed19-r51` is complete and strict-audited: `infra_valid=true`, `semantic_valid=false`, `turns=143`, `strict_pass_rate=0.986`, and the only strict-failed rows are `LLM-QUAL-a922-go2f-seed19-r51-002-09-c838ba` plus `LLM-QUAL-a922-go2f-seed19-r51-002-10-b85502`
- the first live continuity collapse actually starts one turn earlier at `LLM-QUAL-a922-go2f-seed19-r51-002-08-97382a`: pending post-cancel rebooking still routes through `turn_planner.safe_explicit_handoff_owner.v1` with `reason_code=semantic_arbitration_needs_manager` while `current_goal` and `expected_reply_type` are absent
- the next admissible move is now `execute_delete_first_pending_post_cancel_rebooking_continuity_handoff_info_structural_block_after_one_precise_web_search`, not another replay and not another hotspot micro-fix in `truffles-api/app/services/reasoning_core.py`
- published `docs/TASK_PACKAGES/TP-2026-03-24-consultant-core-pending-booking-continuity-info-handoff-authority-reset-structural-implementation-a922.md` and `docs/REPORTS/artifacts/2026-03-24-consultant-core-pending-booking-continuity-info-handoff-authority-reset-structural-implementation-a922.md`, then switched canon from the delete-first decision block to the structural implementation block
- `reasoning_core.py` now restores pending booking continuity from centralized dialog-state boundary state in `_build_conversation_snapshot(...)` and precomputes the same touched boundary before the direct-owner chain
- early `safe_info_fact` and non-explicit early explicit handoff now defer while the pending booking boundary is active, and terminal unresolved explicit handoff is skipped for that touched family
- `_finalize_turn_planner_owner_cutover(...)` now applies direct `DialogStateService.build_expected_reply_context_sync_result(...)` on the touched normal path instead of `context_manager_router._set_expected_reply_context(...)`
- dead early greeting duplicate defs were deleted from `truffles-api/app/services/reasoning_core.py`, reducing duplicate debt to `22` duplicate top-level names across `141` defs / `119` unique names
- focused deterministic validation is green: `python3 -m py_compile truffles-api/app/services/reasoning_core.py truffles-api/tests/test_reasoning_core.py truffles-api/tests/architecture/test_no_duplicate_core_defs.py` -> `pass`; `pytest -q truffles-api/tests/test_reasoning_core.py -k "pending_boundary_promotions_interrupt or promotions_interrupt_and_resumes_time_collect or info_owner_defers_active_service_hours_interrupt_to_booking_prompt_owner or booking_prompt_owner_reactivates_pending_collect_without_active_booking_snapshot or explicit_handoff_owner or terminal_unresolved"` -> `18 passed, 194 deselected`; `pytest -q truffles-api/tests/architecture/test_no_duplicate_core_defs.py` -> `1 passed`
- all required guard and packet checks are green: `pytest -q truffles-api/tests/architecture/test_arch_guard_packet.py` -> `1 passed`; `pytest -q truffles-api/tests/architecture` -> `19 passed`; `python3 scripts/build_agent_packet.py` -> regenerated `docs/_generated/AGENT_PACKET.md` and `docs/_generated/AGENT_PACKET.json`; `python3 scripts/build_agent_packet.py --check` -> `OK`; `SESSION_AGENT=a922 scripts/session_check.sh` -> `Session OK`; `git diff --check` -> `pass`
- the next admissible move is now `run_one_fresh_closure_replay_only_after_pending_booking_continuity_info_handoff_authority_reset_evidence`, not another replay-first discovery loop and not another local branch in `truffles-api/app/services/reasoning_core.py`
- bounded non-frozen runtime implementation for the surviving `r45` family is now landed locally: fresh initial booking entry seeds `slot_state.service` from the current message, drops `info_refs`, and caps policy-core output tokens at `160` only on the bounded fresh-entry owner path
- focused deterministic coverage for the bounded family is green (`pytest -q truffles-api/tests/test_intent.py -k "policy_core and max_tokens_override"` -> `1 passed`; `pytest -q truffles-api/tests/test_reasoning_core.py -k "initial_booking_timeout or initial_booking_owner_recovers_timeout_before_terminal_handoff or policy_core_tokens"` -> `3 passed`)
- live candidate-path probe `/tmp/booking_quality/a922-go2f-seed19-r45/initial_booking_policy_core_budget_probe_post_fix.json` now yields `3/4` non-degraded semantic collect decisions and `0/4` degraded fallbacks on the surfaced message, so the next admissible move is closure replay rather than another runtime patch by default
- truthful completion replay `/tmp/booking_quality/a922-go2f-seed19-r45` is now the first canonical closure artifact after the repaired `r44` proof family: `infra_valid=true`, `run_integrity_valid=true`, `turns_strict_failed=0`, `blocking_reasons.count=0`, and `/tmp/booking_quality/a922-go2f-seed19-r45/failure_families.json` is empty
- the old `r44` false `handoff_miss` proof blocker is now closed on the live replay surface; no blocking reasons survive on `r45`
- the old weekend-pricing runtime blocker also remains closed on `r45`: the weekend booking interruption path stays on `decision_meta.action='booking_prompt'` / `tool_action='collect'`
- `r45` is still `semantic_valid=false`, but the remaining surface is now threshold-only: `policy_core_turns=3`, `policy_core_degraded_turns=3`, and `degraded_fallback_rate=1.0` on three strict-green initial booking rows
- the surviving family is runtime-owned in `reasoning_core.py`: simple initial booking entry still times out in `_resolve_turn_planner_safe_llm_booking_prompt_candidate(...)` and degrades into observable `initial_booking_parser` recovery before the oracle reads it
- bounded seed-`19` proof-only fallback-JID bookkeeping implementation is now landed locally in `ops/diagnose.py`: replay fallback selection persists the current contaminated JID in the shared tried set, deterministic proof is green (`13 passed`), and interrupted replay `r40` now progresses strictly through dialog `1` turn `15`, dialog `2` turn `14`, and dialog `3` turn `7` before manual interruption
- interrupted replay `/tmp/booking_quality/a922-go2f-seed19-r40` is explicitly audited as non-canonical (`stop_reason=in_progress`, `dialogs_seen=3/10`), so the old dialog-preflight fallback-cycle family is no longer the first blocker but the next truthful blocker is still unclassified
- main-path runtime closure proved
- promo interrupt runtime closure already proved
- stale `r9` turn-10 `check_booking_prompt` mismatch disproved as proof drift via refreshed scenario evidence
- refreshed `r12` finishes `14/14` strict-pass turns on the fresh runtime
- `r12` surfaced the bounded exact-time runtime family and the non-frozen implementation landed before replay proof
- fresh replay `r14` then truthfully reclassified the downstream path and showed turn `12` no longer survives as the blocker
- fresh comparable replay `r14` on the refreshed local runtime proves turn `9` is repaired (`expected_reply_type=name`, `booking_slots.datetime='в субботу 11:00'`) and turn `12` now satisfies the handoff contract on the same artifact
- bounded turn-13 non-frozen semantic name progression implementation is now landed locally; focused deterministic regression coverage is green (`4 passed`)
- truthful guarded replay `r16` on the refreshed local runtime keeps turn `9` repaired and turn `10` non-blocking on the locked canary scenario
- the first surviving blocker on fresh evidence is now turn `11`: repeated check-booking verification rewrites `expected_reply_type` from `name` to `service_choice` and drops grounded `booking_slots.datetime='в субботу 11:00'`
- because fresh replay `r16` stops at turn `11`, explicit-name turn `13` is now downstream unresolved debt rather than the first admissible runtime blocker
- judge conflicts on turns `6` and `9` remain advisory proof debt only; they are no longer admissible runtime blockers
- family-first debug cadence reset is now active: governance docs now distinguish `forensic -> implementation -> closure`, guarded dev-lane forensic continuation is explicit, and a duplicate-def architecture guard now records the current `reasoning_core.py` shadowed top-level debt so new growth cannot stay silent
- bounded turn-11 non-frozen check-booking reference continuity implementation is now landed locally; focused deterministic regression coverage for the surfaced family is green (`7 passed`)
- truthful guarded replay `r17` on the refreshed local runtime now completes the full `14/14` turn canary with `turns_strict_failed=0`, proving the bounded turn-11 check-booking reference continuity repair on the real replay surface
- the same truthful replay `r17` now reaches turn `13` and proves explicit-name progression closure on fresh canary evidence (`booking_slots.name='Амина'`, `tool_action=calendar.book_slot`, strict-green under `provider_unavailable`)
- truthful guarded replay `r18` on the refreshed local runtime proves the bounded turn-8 booking-interrupt exact-time progression repair on the real canary path (`expected_reply_type=name`, `booking_slots.datetime='в субботу 10:00'`, strict-green)
- the same truthful replay `r18` reaches turns `11`, `13`, and `14` strict-green, but surfaces the next surviving runtime family at turn `9`: grounded datetime reschedule while `name` is pending keeps `booking_slots.datetime='в субботу 10:00'` instead of applying `11:00`
- bounded turn-9 non-frozen grounded-datetime reschedule implementation is now landed locally: snapshot-echo restoration, grounded exact-time replacement, and semantic booking recovery exact-time progression while `expected_reply_type=name`; focused deterministic coverage is green (`10 passed`)
- truthful guarded replay `r19` on the refreshed local runtime now completes the full `14/14` turn canary with `turns_strict_failed=0` and proves the bounded turn-9 grounded-datetime reschedule repair on the real replay path
- `r19` leaves no surviving runtime blocker: turns `8`, `9`, `11`, `12`, `13`, and `14` are strict-green on the same artifact, while remaining semantic red is limited to judge/HQ1 conflicts on turns `6`, `9`, `11`, and `12`
- current semantic invalid status is therefore proof-only (`winner=contract`, `conflict_count=4`), not a new runtime-family blocker
- bounded proof classification now locks one oracle family in `ops/diagnose.py`: HQ1 over-classifies `handoff_miss` on turns `9`/`12`, while judge suppression under-classifies contract-valid turns `6`/`9`/`11`/`12`
- bounded proof-only oracle implementation is now landed locally in `ops/diagnose.py`: judge suppression mirrors the contract-valid booking-prompt / check-booking collect / master-service-not-found envelopes, and HQ1 no longer blocks contract-valid active booking continuation
- focused oracle regressions are green (`15 passed`), and a helper probe on the existing `r19` artifact now yields `suppress=True` and `hq1=[]` for turns `6`, `9`, `11`, and `12`
- truthful guarded replay `r20` on the same locked canary surface now completes `14/14` turns with `infra_valid=true`, `semantic_valid=true`, and `turns_strict_failed=0`, closing the bounded oracle parity family on fresh evidence
- bounded proof-only replay fallback implementation is now landed locally in `ops/diagnose.py`: contaminated replay preflight prefers remaining allowlist JIDs, can iterate across tried allowlist candidates, and no longer self-sabotages by generating a non-allowlist JID while outbox is enabled; focused deterministic coverage is green (`10 passed`)
- fresh replay/audit evidence `r6 -> r7 -> r9` then truthfully surfaced the next blocker before turn execution: simulated session-reset traffic still resolves through the executable explicit-handoff owner, which calls direct provider transport on allowlist JIDs and hits `ChatFlow response ... "Your plan has been expired please renew."`
- the first admissible blocker is therefore no longer proof fallback selection and no longer the old turn-13 reschedule family; it is a bounded runtime simulation-transport family on `turn_planner_safe_explicit_handoff_owner` / `send_message_safe(...)` under `simulation_mode=true`
- bounded seed-`19` non-frozen session-reset simulation transport implementation is now landed locally: the executable later explicit-handoff owner routes `simulation_mode=true` through `ChatFlowAdapter().send_text(...)` with `MessageOptions(extra={"simulation_mode": true})`, records `transport_simulated`, and focused deterministic coverage is green (`3 passed`); the mandatory guard/session stack is also green
- pre-existing replay artifact `/tmp/booking_quality/a922-go2f-seed19-r10` is now closed as non-canonical (`invalid_preflight`, incomplete artifact discovered before the fresh closure attempt)
- fresh exact replay `/tmp/booking_quality/a922-go2f-seed19-r11` no longer reopens the old provider-transport blocker: replay stdout shows `Turn planner safe explicit handoff sent` with `decision_meta.transport_simulated=true`, so the bounded simulation transport repair is active on the replay surface
- the first surviving blocker on `r11` is now different and still pre-turn: contaminated preflight sends `pending_ack` text `ок`, but the live greeting owner responds `Turn planner safe greeting owner sent` and leaves `state_after=pending`, so preflight clear never completes and the replay remains non-canonical with `dialogs_seen=0`
- bounded seed-`19` non-frozen session-reset pending-ack greeting-intercept implementation is now landed locally: the executable later greeting owner reuses the pending continuity contract and defers `pending_ack` while `conversation.state == pending`, so contaminated preflight clear is no longer consumed as smalltalk; focused deterministic coverage is green (`4 passed`)
- fresh exact replay `/tmp/booking_quality/a922-go2f-seed19-r12` no longer reopens the pending-ack greeting-owner blocker: runtime parity stayed on `HEAD`, `/admin/health` returned `200`, and contaminated preflight now returns `Turn planner safe explicit handoff sent` on `pending_ack`, proving the bounded greeting-owner defer repair on the replay surface
- the first surviving blocker on `r12` is now different and still pre-turn: contaminated preflight sends `pending_ack` text `ок`, but the live explicit-handoff owner responds `Turn planner safe explicit handoff sent` and leaves `state_after=pending`, so preflight clear never completes and the replay remains non-canonical with `dialogs_seen=0`
- bounded seed-`19` non-frozen session-reset pending-ack explicit-handoff-intercept implementation is now landed locally: the shared pending-state helper and the executable later explicit-handoff owner now defer `pending_ack` while `conversation.state == pending`, so session-reset clear acknowledgements no longer re-open handoff on the live path; focused deterministic coverage is green (`6 passed`)
- bounded seed-`19` non-frozen session-reset pending-ack terminal-unresolved implementation is now landed locally: `reasoning_core.py` now reuses `state_service._resolve_pending_ack(...)` on the live owner chain before terminal fallback, and focused deterministic coverage is green (`3 passed`)
- strict audit on `r20` keeps `judge_oracle_alignment_gap` only as advisory manual-audit debt (`winner=contract`, `conflict_count=4`), so turns `6`, `9`, `11`, and `12` no longer block semantic validity
- published a fresh post-`r20` acceptance re-entry bundle and started a fresh local runtime on `127.0.0.1:18186`
- the first canonical acceptance `lock` attempt (`booking-lock-a922-post-r20`) progressed past the old latest-lock guard and reached chain-controller prepare
- the chain then fail-closed on `go_to_full_gate_invalid:unreadable:/tmp/booking_quality/pg_checklist-a922-post-r20.json`, so no new acceptance `lock` artifact was produced
- current blocker is acceptance-preflight `go_to_full` evidence materialization: fresh dev-lane `L2` exists (`r20`), a fresh `L1` JUnit exists (`/tmp/booking_quality/l1-promo-canary-preflight-a922/pytest-junit.xml`), but no assembled PG checklist or fresh green multi-seed summaries for required seeds `7`, `19`, and `42` exist
- the narrowed evidence-pack family then materialized a fresh dedicated `L1` JUnit (`/tmp/booking_quality/l1-go-to-full-a922/pytest-junit.xml`), closed one stale non-canonical seed run caused by the `go-to-full` run-id token, and produced one fresh green seed summary for `7` at `/tmp/booking_quality/a922-go2f-seed7/summary.json`
- fresh seed `19` then stopped the family truthfully: `/tmp/booking_quality/a922-go2f-seed19/summary.json` finished `infra_valid=true`, `run_integrity_valid=true`, `semantic_valid=false`, with `threshold_breaches=['irrelevant_fact_rate']`; per stop-condition, seed `42`, PG checklist assembly, and the acceptance `lock` retry were not executed
- the first admissible blocker is now a bounded runtime family on generated multi-seed booking/check-booking info interruption semantics: weekend / hours / promo follow-ups misroute into irrelevant fact owners (`pricing`, `duration`, `services_overview`) or drop pending continuity on fresh seed `19`
- bounded seed-`19` non-frozen implementation is now landed locally: direct `catalog_fact` / `service_query_fact` / `pricing_collect` / `duration_collect` side owners defer while active booking continuity is open, explicit hours interruption resolution prefers the real `hours` signal over the false-positive duration anchor, and promo requests with service mention now rescue to `promotions`
- focused deterministic coverage for the surfaced family is green (`pytest -q truffles-api/tests/test_master_info_flow.py -k "hours or promotions"` → `11 passed`; `pytest -q truffles-api/tests/test_reasoning_core.py -k "booking_prompt_owner_answers_promotions_interrupt_and_resumes_time_collect or booking_prompt_owner_answers_explicit_hours_interrupt or direct_service_query_fact_defers_active_booking_interrupt or direct_catalog_fact_defers_active_booking_interrupt"` → `4 passed`)
- replay hygiene is now truthful for this family:
  - `a922-go2f-seed19-r2` is invalid command drift because explicit `--quality-lane dev` was omitted
  - `a922-go2f-seed19-r3` is non-canonical scenario drift because it regenerated seed scenarios instead of replaying `/tmp/booking_quality/a922-go2f-seed19/scenarios.json`
- fresh exact replay `/tmp/booking_quality/a922-go2f-seed19-r4/summary.json` now reuses the original blocker scenarios with runtime parity `HEAD == /admin/version.git_commit`, but it is infra-invalid (`tool_evidence_reasons=['confirm_hook_missing']`) before the old runtime-semantic family can be reclassified
- `r4` stopped at dialog `2`, turn `9` on an explicit-handoff expectation mismatch (`expected_meta_mismatch`, `expected_trace_miss`), but that semantic row is downstream until the replay tool-evidence gap is classified
- bounded proof classification on `r4` now proves the first surviving blocker is not runtime and not transport: strict tool evidence counts `check_booking` as a confirm opportunity, while the synthetic confirm-hook sender in `ops/diagnose.py` does not mirror that rule on `confirm`-tagged `check_booking` turns
- the exact replay therefore stops on a contract-parity proof family inside `ops/diagnose.py`; the downstream explicit-handoff mismatch remains inadmissible until infra is restored
- bounded proof-only implementation is now landed locally in `ops/diagnose.py`: `confirm`-tagged `check_booking` and `check_record` turns now satisfy the same synthetic confirm-hook contract already counted by strict tool evidence
- focused proof regression is green (`pytest -q truffles-api/tests/test_booking_quality_tool_evidence_gate.py -k "confirm_hook or check_booking_intent_to_confirm_signal or strict_policy_accepts_check_booking_alias_confirm_hook"` -> `6 passed, 14 deselected`)

- fresh exact replay `/tmp/booking_quality/a922-go2f-seed19-r5/summary.json` now reuses the original blocker scenarios with runtime parity `HEAD == /admin/version.git_commit` after the bounded confirm-hook proof repair
- `r5` restores infra/tool-evidence truth on the exact replay surface: `infra_valid=true`, `tool_evidence_valid=true`, `confirm_hook_events=1`, and dialog `1` turn `12` now records `tool_hooks=['confirm', 'calendar']` on `tool_signals.calendar.intent='check_booking'`
- the old `confirm_hook_missing` family is therefore closed on fresh replay evidence; the first surviving blocker is now dialog `1`, turn `13`
- fresh exact replay turn `13` (`Можно на 18:30?`) is a runtime continuity bug: turn `12` preserved `service='Маникюр'`, `datetime='15:00'`, and `expected_reply_type=name`, but turn `13` reopens generic booking collect with `expected_reply_type=service_choice` and `booking_slots={'datetime': '18:30'}`
- the next bounded runtime family is now post-verification exact-time reschedule continuity, with explicit shadow-risk because the live path crosses duplicated `_try_handle_turn_planner_safe_booking_prompt_owner_cutover` (`reasoning_core.py:4933`, `:9774`) and `_try_handle_turn_planner_safe_check_booking_prompt_owner_cutover` (`reasoning_core.py:6005`, `:10846`) owner defs already tracked by the architecture guard
- bounded seed-`19` non-frozen post-verification reschedule implementation is now landed locally: the live later booking-prompt owner and adjacent semantic booking recovery lane now rehydrate snapshot-grounded `service` / `datetime` before recomputing the next missing slot, and focused deterministic coverage is green (`9 passed`)

- strict audit on `/tmp/booking_quality/a922-go2f-seed19-r13` now closes that replay attempt as self-inflicted non-canonical evidence because runtime fingerprint preflight failed with `admin_version_unreachable` after a timed runtime startup probe
- fresh exact replay `/tmp/booking_quality/a922-go2f-seed19-r14` now proves the bounded explicit-handoff defer repair on the real replay surface: runtime parity stayed on `HEAD`, `/admin/health` returned `200`, and contaminated preflight no longer returns `Turn planner safe explicit handoff sent`
- the first surviving blocker on `r14` is now different and still pre-turn: contaminated preflight sends `pending_ack` text `ок`, but the live runtime now falls through to `Reasoning core terminal unresolved response skipped` with `bot_response="Извините, произошла ошибка. Попробуйте позже."`, so preflight clear never completes and the replay remains non-canonical with `dialogs_seen=0`
- bounded reset-only runtime delegate implementation is now landed locally: exact reset-only control traffic in `reasoning_core.py` now bypasses the direct owner shortcuts and reuses the existing session-memory reset contract; focused deterministic coverage is green (`5 passed`)
- fresh replay `r22` on the refreshed local runtime truthfully closes the old runtime family: `session_reset` now returns `Session reset ack sent` with `decision_meta.session_memory_reset=explicit_reset`, so `reset_ack_missing` no longer survives
- the first surviving blocker is now proof/preflight, not runtime: outbox-enabled replay still fail-closes before turn `1` because the allowlist JID pool remains contaminated by historical recent-conversation state (`multiple_recent_conversations`, `decision_trace_present`, stale `booking_active`, `simulation_id_mismatch`) after the reset repair
- bounded seed-`19` proof-only preflight contamination implementation is now landed locally in `ops/diagnose.py`: clean latest bot-active rows no longer fail-close replay just because the allowlist JID still has historical conversations and old decision traces; focused deterministic proof is green (`12 passed`)
- bounded seed-`19` proof-only fallback-JID implementation is now landed locally in `ops/diagnose.py`: allowlist-first replay fallback remains intact, but once allowlist candidates are exhausted the helper now mints deterministic fresh dialog JIDs when `allow_non_allowlist=true`; focused deterministic proof is green (`12 passed`)
- fresh exact replay `/tmp/booking_quality/a922-go2f-seed19-r25` now proves the old fallback-JID proof family is closed on the live surface: replay reaches dialog execution with `infra_valid=true` and uses fresh non-allowlist dialog JIDs such as `99953746942@s.whatsapp.net` and `99905914999@s.whatsapp.net`
- the first surviving blocker on `r25` is now a bounded runtime family: dialog `2`, turn `8` (`Когда я могу записаться снова?`) reaches the correct `booking_prompt` collect reply with `expected_reply_type=service_choice`, but continuity still leaves `conversation_state=pending` instead of the expected `bot_active`

Execution strategy lock:
- no new generic ingress phrase-bridge families count as progress
- valid progress requires old-authority deletion or unreachability
- Block D remains closed as normal-path complete, Block E locked `context_manager._set_expected_reply_context(...)` / `_sync_canonical_dialog_state(...)` as the next non-frozen continuity writer deletion, Block F completed that expected-reply/question-contract state-sync cutover into `DialogStateService`, and Block G proved that continuity micro-slices are now blocked/exhausted behind frozen `pending.py` plus broader reset/state-boundary behavior. Block H was the stop-the-line architecture-truth audit; Block I stopped the stale queued boundary micro-slice chain; Block J selected the frozen timeout-owner boundary cluster as the preferred surviving boundary target; Block K proved a non-frozen bypass is not yet truthful because the input contract still lives in frozen `decision.py`. Block L deleted the main timeout-owner state/meta/send assembly under an explicit waiver. Block M proved that the pending-timeout branch was the next real deletable timeout-owner seam because it repeated the same inline apply/send authority pattern while reusing the same typed resolver contract. Block N confirmed that its pending-specific delta stayed bounded. Block O then deleted that pending-timeout inline apply/send body. Block P stopped further timeout-owner micro-slice farming and published one broader pending-resume rework decision. Block Q converted that decision into one owner-capture plan around `DialogStateService` plus `state_service`. Block R deleted the pending-resume helper family in frozen `pending.py` and reduced the frozen restore helpers in `decision.py` to owner-surface invocation. Block S recorded the post-waiver verdict that the surviving live authority is the frozen `decision.py` activation/preserve cluster. Block T turned that verdict into one broader owner-capture rework plan for the residual coordinator. Block U executed the bounded frozen rework: activation/preserve gating, handover preserve-vs-reset policy, and pending-timeout reuse eligibility no longer decide inline in frozen `decision.py`; they now exit through `truffles-api/app/services/state_service.py`. Block V executed the next bounded frozen rework: the resolved-handoff pending-resume restore coordinator no longer decides inline in frozen `decision.py`; it now exits through `truffles-api/app/services/state_service.py`. Block W executed the next bounded frozen rework: the handover-reuse pending-resume sync helper no longer decides inline in frozen `decision.py`; it now exits through `truffles-api/app/services/state_service.py`. Block X executed the broader handover-reuse frozen rework: the active handover reuse coordinator no longer decides inline in frozen `decision.py`; it now exits through `truffles-api/app/services/state_service.py`. Block Y audited the post-waiver residuals and proved the next live frozen seam is the repeated pending-escalation create-notify cluster. Block Z executed that bounded frozen rework. Subsequent correction blocks converged the handover owner family, converged the timeout owner-boundary application family, converged the policy-validation boundary family, converged the timeout-degrade retry family, converged the timeout non-booking recovery reply family, converged the timeout booking specialist boundary family, converged the invalid-schema specialist-followup boundary family, converged the invalid-schema service-grounded booking boundary family, converged the timeout pending-slot-question reply family, converged the timeout active-name time-followup family into `truffles-api/app/services/policy_timeout_booking_time_followup_boundary_service.py`, published one master residual ledger so the next work proceeds as package-level family closure instead of another micro-cut chain, then published the `policy_core_guard_orchestration` package TP, and then executed that runtime family convergence: the remaining degraded-guard safe reply / handoff / hold / completed-booking / degraded-collect orchestration no longer lives inline in frozen `decision.py`; it now exits through `truffles-api/app/services/policy_core_guard_orchestration_service.py`. The semantic-arbitration runtime family convergence has now landed from `docs/TASK_PACKAGES/TP-2026-03-18-consultant-core-semantic-arbitration-residual-package-a922.md`: the old post-hoc semantic override / collect-interrupt / booking-promotion / hint-backfill / active-service referent family now bypasses frozen `decision.py` through the existing `turn_planner` path plus `reasoning_core`. The continuity broader-collapse package TP then proved the truthful destination is `truffles-api/app/core/dialog_state_service.py` plus one bounded coordinator in `truffles-api/app/services/state_service.py`, the targeted frozen-waiver decision locked the exact admissible scope, and the runtime convergence has now landed from `docs/TASK_PACKAGES/TP-2026-03-18-consultant-core-continuity-broader-collapse-targeted-frozen-waiver-implementation-a922.md`: the old pending no-handover reset / pending-close / pending-ack restore / pending-SLA mutation / handover-confirmation / session-memory reset+clear family no longer decides inside frozen `pending.py` or non-frozen `session_memory.py`; those surfaces now delegate to `state_service.py` plus `DialogStateService`. The debounce-buffer owner-convergence runtime family convergence has now landed from `docs/TASK_PACKAGES/TP-2026-03-18-consultant-core-debounce-buffer-owner-convergence-package-a922.md`: the old split duplicate-probe authority no longer stays live between `truffles-api/app/services/reasoning_core.py` and `truffles-api/app/routers/webhook/dedup.py`, and the post-debounce muted-state gate no longer lives inside `dedup.py`; the surviving owner surfaces are `truffles-api/app/routers/webhook/dedup.py` for duplicate/debounce mechanics and `truffles-api/app/routers/webhook/guards.py` for the existing mute/reengage gate. The proof-black-box runtime family convergence has now landed from `docs/TASK_PACKAGES/TP-2026-03-18-consultant-core-proof-black-box-completion-package-a922.md`: the old in-script `_sanitize_llm_turns(...)` scenario normalization / expect-repair orchestration no longer lives in `scripts/booking_dialog_scenarios.py`; that authority now exits through `truffles-api/app/services/llm_quality_contracts.py`, while `ops/diagnose.py` remains observer/oracle only. The multi-pack runtime target materialization owner convergence has now landed from `docs/TASK_PACKAGES/TP-2026-03-18-consultant-core-multi-pack-runtime-target-materialization-package-a922.md`: `clinic_pack/main` now materializes the truthful `clinic_or_dental` runtime target through the existing `clinic` domain owner path, `generic/main` now materializes the truthful `generic_service` runtime target through the first-class `generic` provisioning/validation/reference-pack contract, both new targets pass `integrity-gate`, and the old repo-dir-only / deleted-client substitute target seam is no longer the live path for non-beauty closure. The acceptance-preflight blocker implementation report is now published in `docs/REPORTS/artifacts/2026-03-18-consultant-core-acceptance-preflight-blocker-a922.md`: the old live hardcode phrase/prefix authority seam in `truffles-api/app/services/info_signal_service.py` is deleted into `truffles-api/app/knowledge/generic/SYSTEM_LEXICONS.yaml`, current-worktree acceptance preflight is green, fresh L1 JUnit evidence exists, and runtime parity is restored on `http://127.0.0.1:18184`. `docs/TASK_PACKAGES/TP-2026-03-18-consultant-core-acceptance-preflight-l2-transport-blocker-package-a922.md` then locked the surviving work to the narrower non-acceptance `L2` transport / observability blocker family under `TEST_MODE`. The implementation report is now published in `docs/REPORTS/artifacts/2026-03-18-consultant-core-acceptance-preflight-l2-transport-blocker-a922.md`: the old runner-owned synthetic non-allowlist unique-JID seam in `ops/diagnose.py` is deleted because `unique` mode now prefers allowlist JIDs, the strict confirm-hook status path for `calendar.get_booking` is restored, `r11` proves the completed runtime path is now transport-valid with `infra_valid=true` and `run_integrity_valid=true`, and repo truth already treats `CHATFLOW_BILLING_BLOCKED` as acceptable in this unpaid dev lane. `docs/REPORTS/artifacts/2026-03-19-consultant-core-acceptance-preflight-l2-expectation-conflict-a922.md` then executed the expectation-conflict closure bundle: the old observer-owned direct `question_contract` / duplicate meta-trace expectation seam in `ops/diagnose.py` is deleted because `r14` reduces `expected_trace_miss` `59 -> 6` and `expected_meta_mismatch` `52 -> 5` while keeping `infra_valid=true` and `run_integrity_valid=true`. `docs/TASK_PACKAGES/TP-2026-03-19-consultant-core-acceptance-preflight-l2-post-observer-runtime-failure-family-package-a922.md` then executed that bounded A -> B -> C reconciliation bundle: the old non-frozen `_reuse_active_handover(..., hooks)` caller drift in `info.py`, `policy.py`, `guards.py`, and `response.py` is now unreachable, the raw `branch_missing` booking-completion product reply no longer survives through the non-frozen owner path, and active booking expected-reply continuity now short-circuits the non-frozen booking-prompt owner path. The one admissible fresh dev-lane rerun `/tmp/booking_quality/l2-acceptance-preflight-a922-r17` then stopped semantic-red with `infra_valid=true` and `artifact_integrity.valid=true` because `_reuse_active_handover() missing 1 required keyword-only argument: 'hooks'` still survives on the frozen `booking.py` handover-reuse contour (`LLM-QUAL-l2-acceptance-preflight-a922-r17-002-05-21d51a`; stale frozen callers at `booking.py:1702`, `2358`, `2914`, `3007`, `3741`). `docs/TASK_PACKAGES/TP-2026-03-19-consultant-core-acceptance-preflight-l2-frozen-booking-handover-reuse-family-package-a922.md` then locked the next work to that frozen booking caller family only. The direct frozen runtime attempt was later reverted because `legacy_freeze_guard.py` fail-closed on executable additions in frozen `booking.py`, while partial probe `/tmp/booking_quality/l2-acceptance-preflight-a922-r18` remained non-canonical (`infra_valid=true`, `semantic_valid=false`, `stop_reason=signal_15`, `dialogs_seen=[1, 2, 3]`) and therefore could not count as seam deletion. `docs/TASK_PACKAGES/TP-2026-03-19-consultant-core-acceptance-preflight-l2-frozen-booking-handover-reuse-targeted-frozen-waiver-decision-a922.md` now locks the exact future waiver scope to those five frozen callers only and fixes the next admissible move to `implement_acceptance_preflight_l2_frozen_booking_handover_reuse_targeted_frozen_waiver_bundle`.

Not valid progress:
- next row closure alone
- next rerun only
- next first-fail movement
- beauty-only green evidence
- queued boundary micro-slice execution after stale-scan

Current open blockers:
- `docs/TASK_PACKAGES/TP-2026-03-24-consultant-core-pending-booking-continuity-terminal-handoff-authority-reset-structural-implementation-a922.md` is now the active block. FACT: the touched pending booking family now restores booking collect continuity from centralized dialog-state resume payloads before terminal fallback is eligible.
- `r49` remains the truthful closure baseline for this family. FACT: no new replay is admissible until the structural block is fully validated and canon is rebuilt.
- one fresh replay is still required for closure, but only as `run_one_fresh_closure_replay_only_after_pending_booking_continuity_terminal_handoff_authority_reset_evidence`.
- `truffles-api/app/services/reasoning_core.py` still carries explicit structural debt outside the touched family: `26` duplicate top-level function names across `144` top-level defs / `118` unique names. Broader duplicate cleanup remains deferred.
- acceptance remains strict; no oracle/proof thresholds were weakened in this block, and final program acceptance still depends on canonical acceptance evidence plus broader multi-pack/open-world closure.

Required checks before any next consultant-core block:
- `python3 scripts/build_agent_packet.py`
- `python3 scripts/build_agent_packet.py --check`
- `python3 scripts/semantic_bridge_growth_guard.py`
- `python3 scripts/continuity_writer_guard.py`
- `python3 scripts/legacy_freeze_guard.py`
- `python3 scripts/arch_guard.py`
- `pytest -q truffles-api/tests/architecture`
- `git diff --check`
- `SESSION_AGENT=a922 scripts/session_check.sh`

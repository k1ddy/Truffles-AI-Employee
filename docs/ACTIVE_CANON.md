# ACTIVE CANON

## Current Truth
- External packet status: `ready_for_external_handoff`
- Current whole-system practical truth is still `r35f`; the focused `Block H.1` oracle proof plus reopened `Block H.1B` file-replay proof removed the stale booking-completion and booking-manage scenario blocker families on both compiler and file-replay seams, but whole-system acceptance remains a separate lane until a fresh full replay reruns `Block H`
- Runtime implementation status: `Block A`, `Block B`, `Block C`, `Block C.5`, `Block D`, `Block E`, `Block E.5`, `Block E.6`, `Block F`, `Block G`, the focused `booking-manage temporal clue grounding / follow-up continuity` block, `Block H.1 — Oracle Scenario Contract Alignment`, and `Block H.1B — File Replay Scenario Contract Materialization` are `closed_proven` in the active worktree; `Block H` remains open
- Active block: `Consultant Core Block H.1B — File Replay Scenario Contract Materialization` (`closed_proven`; next admissible runtime work is `Block H`)
- `Block C`, `Block C.5`, `Block D`, `Block E`, `Block E.5`, `Block E.6`, `Block F`, `Block G`, the focused booking-manage follow-up block, `Block H.1`, and `Block H.1B` are closed only on their declared envelopes; no whole-system or product closure claim is admissible until `Block H` and final acceptance pass
- Machine-readable governance status: active docs track the live block-by-block runtime closeout sequence, not the stale repo-side reproof narrative
- Practical/product closure status: open; the focused `Block H.1` and `Block H.1B` oracle proofs removed the stale scenario blocker family on both evaluation seams, but whole-system acceptance still remains a separate lane
- Historical residue rule: later `r36*` replay/RCA/runtime materials and invalidated whole-system closure claims remain preserved history only under `docs/RECOVERY_EXECUTION_LOCK.yaml`; they do not advance the active block or practical truth while the live-code reproof block is active

## Governing Architecture
`Single Semantic Owner + Strict Binding Boundary + Canonical Continuity State + First-Class Fact Plane + Adapter-only Legacy Mesh`

Operational implication:
- the target architecture is unchanged;
- typed runtime spine and owner path are real;
- `Block A`, `Block B`, `Block C`, `Block C.5`, `Block D`, `Block E`, `Block E.5`, `Block E.6`, `Block F`, and `Block G` are now proven on the touched live paths;
- the live outbox admin/service/console/worker surfaces now share one canonical runtime boundary for scoped preview and execute semantics;
- the focused booking-manage follow-up block, `Block H.1`, and `Block H.1B` are now proven green, so the next admissible runtime block is again `Block H — Final Acceptance`;
- acceptance is still a separate lane.

Primary governing docs:
- `docs/DECISIONS/DEC-2026-03-31-consultant-core-whole-system-architecture-closure-governing-decision.md`
- `docs/system_forensics/SYSTEM_VERDICT.md`
- `docs/TASK_PACKAGES/TP-2026-03-31-consultant-core-whole-system-architecture-closure-master-program-a922.md`
- `docs/TASK_PACKAGES/TP-2026-04-01-consultant-core-block-a-interrupt-arbitration-and-continuation-law-a922.md`
- `docs/TASK_PACKAGES/TP-2026-04-01-consultant-core-block-b-fact-scope-exactness-a922.md`
- `docs/TASK_PACKAGES/TP-2026-04-01-consultant-core-block-c-continuity-carrier-collapse-a922.md`
- `docs/TASK_PACKAGES/TP-2026-04-01-consultant-core-block-c5-policy-info-interrupt-fact-delivery-a922.md`
- `docs/TASK_PACKAGES/TP-2026-04-01-consultant-core-block-d-boundary-purification-a922.md`
- `docs/TASK_PACKAGES/TP-2026-04-01-consultant-core-block-e-real-pack-runtime-separation-a922.md`
- `docs/TASK_PACKAGES/TP-2026-04-01-consultant-core-block-e5-owner-service-referent-grounding-a922.md`
- `docs/TASK_PACKAGES/TP-2026-04-01-consultant-core-block-e6-post-grounding-service-reply-exactness-a922.md`
- `docs/TASK_PACKAGES/TP-2026-04-01-consultant-core-block-f-legacy-mesh-final-drain-a922.md`
- `docs/TASK_PACKAGES/TP-2026-04-01-consultant-core-block-g-operational-final-dedupe-a922.md`
- `docs/TASK_PACKAGES/TP-2026-04-01-consultant-core-booking-manage-temporal-clue-grounding-followup-continuity-a922.md`
- `docs/TASK_PACKAGES/TP-2026-04-01-consultant-core-block-h1-oracle-scenario-contract-alignment-a922.md`
- `docs/TASK_PACKAGES/TP-2026-04-01-consultant-core-block-h1b-file-replay-scenario-contract-materialization-a922.md`
- `docs/RECOVERY_EXECUTION_LOCK.yaml`

## Writer Laws
### Semantic writer law
Only the semantic owner may author turn meaning.
Planner, executor, runtime shell, boundary, adapters, and evaluators may package, bind, validate, deny, degrade, or observe. They may not re-author meaning.

Current truth:
- hot-path business semantic authorship is owner-backed on the touched path and `Block A` plus `Block E.5` closed the active-followup / inline-service grounding owner slices;
- planner/runtime synthetic control envelopes, dialog-state semantic reconstruction fallbacks, and executor non-owner fallback branches still remain residual debt;
- no whole semantic-owner closure claim is admissible yet.

### Continuity writer law
Only canonical continuity state may own mutable pending-question and interaction continuity.
Any compatibility carrier is derived-only unless explicitly registered with owner, expiry, and deletion plan.

Current truth:
- canonical dialog state is now the sole mutable continuity writer on the touched follow-up/resume path;
- `Block C` removed `pending_resume`, top-level `expected_reply_*`, and `session_memory` as authority restore/write carriers on that path;
- reset-boundary transcript contamination is removed from policy-core memory summary;
- broader product acceptance still remains open because later blocks and full acceptance replay are still required.

### Fact writer law
Owner writes requested fact scope.
Binding writes allowed emitted scope.
Resolver/renderer writes emitted scope.
No other layer may widen fact scope.

Current truth:
- typed fact contract remains active;
- `Block B` exact fact scope is proven on the active hot path for `location / hours / parking`;
- `Block C.5` exact policy-info interrupt fact delivery is now proven on the touched `catalog.service_query` path;
- broader fact governance outside the touched envelope still remains partial.

### Boundary law
Deterministic boundary may only:
- validate
- deny
- degrade with explicit reason-code
- preserve or reuse canonical continuity artifacts
- request replan

Deterministic boundary may not:
- infer new intent
- invent a new pending question
- widen fact scope
- write new semantic meaning after the owner speaks

Current truth:
- reply-envelope narrowing remains active on the touched path;
- deterministic boundary now preserves the canonical artifacts surfaced by `Block A`, `Block B`, `Block C`, and `Block C.5`;
- `Block D` proved that boundary override meta no longer owns visible handoff or reply semantics on the live runtime path;
- `Block E` proved that the selected pack adapter is the sole behavior authority on the touched hot fact path;
- `Block E.5` proved that owner-side inline-service grounding stays factual on the touched `duration` / `master` envelope;
- `Block E.6` proved that grounded `duration` and `master` replies stay factual through one canonical post-grounding referent bridge even when the published pack exposes only exact `price_list` items and coarse `team` summaries;
- `Block F` proved that the remaining legacy webhook surfaces now have exact finite-set fates on the live import/caller topology: package root and `session_memory.py` are adapter-only, while `decision.py`, `info.py`, and `context_manager.py` are unreachable from the live runtime boundary;
- `Block G` proved that admin/service/console/worker outbox surfaces now share one canonical runtime boundary, console dry-run and execute both delegate through `ScopedOutboxProcessRequest`-backed runtime helpers, and low-level claim/process/archive imports remain confined to `outbox_runtime_service.py`;
- no focused runtime/oracle blocker remains in the replay backlog; the only known residual prior to full acceptance is the evaluator-only judge disagreement from `Block E.6`, and `Block H` is now the immediate next move.

## Reopened Execution Order
1. `Block A — Interrupt Arbitration And Continuation Law` (`closed_proven`)
2. `Block B — Fact Scope Exactness` (`closed_proven`)
3. `Block C — Continuity Carrier Collapse` (`closed_proven`)
4. `Block C.5 — Policy-Info Interrupt Fact Delivery` (`closed_proven`)
5. `Block D — Boundary Purification` (`closed_proven`)
6. `Block E — Real Pack/Runtime Separation` (`closed_proven`)
7. `Block E.5 — Owner Service Referent Grounding` (`closed_proven`)
8. `Block E.6 — Post-Grounding Service Reply Exactness` (`closed_proven`)
9. `Block F — Legacy Mesh Final Drain` (`closed_proven`)
10. `Block G — Operational Final Dedupe` (`closed_proven`)
11. `Booking-manage temporal clue grounding / follow-up continuity` (`closed_proven`)
12. `Block H.1 — Oracle Scenario Contract Alignment` (`closed_proven`)
13. `Block H.1B — File Replay Scenario Contract Materialization` (`closed_proven`)
14. `Block H — Final Acceptance` (`next`)

## Block-Closeout Reporting Discipline
- Do not update `STATE.md`, `docs/ACTIVE_*`, packet, or reports after micro-fixes inside an unfinished block.
- Update canon/state/report only once, at the close of one full block with its checks and evidence.

## Truth-Correction Law
- No block may claim repo-side closure while live code still contains a competing writer, synthetic semantic control path, or downstream semantic reconstruction path inside the declared mechanism envelope.
- Registry and test green status do not count as closure proof if they only restate the registry narrative.
- Closure claims must follow live-code proof, not replace it.

## Immediate Next Move
The current non-negotiable next move is:
- rerun `Block H — Final Acceptance` from one fresh locked replay plus full human semantic audit now that the surfaced booking-manage plus both oracle scenario blocker families are proven removed

# File Analysis: `truffles-api/app/webhook.py`

Status: `completed`
Baseline snapshot: `8319d9e1`
Related ledgers:
- `docs/system_forensics/ledgers/CONTROL_PATHS.md`
- `docs/system_forensics/ledgers/SEMANTIC_OWNER_MAP.md`
- `docs/system_forensics/ledgers/TRUTH_CARRIER_MATRIX.md`
- `docs/system_forensics/ledgers/STATE_SURFACE_INVENTORY.md`
- `docs/system_forensics/ledgers/DETERMINISTIC_REWRITE_LEDGER.md`
- `docs/system_forensics/ledgers/CUTOVER_DEPENDENCY_GRAPH.md`
- `docs/system_forensics/ledgers/SALVAGEABLE_COMPONENTS.md`
- `docs/system_forensics/ledgers/DO_NOT_REPEAT.md`
- `docs/system_forensics/final/SYSTEM_FINAL_ANALYSIS.md`

## 1. Role Summary
- `FACT`: The file declares itself twice as a legacy module that should not be used for new webhooks, yet it still contains far more than one thin wrapper: a router, a raw debug endpoint, a public-entrypoint delegate, shadow debounce/dedup primitives, booking/handover lexicon helpers, context/booking accessors, booking prompt helpers, mute/handover queries, and low-confidence retry logic: `truffles-api/app/webhook.py:1`, `truffles-api/app/webhook.py:5`, `truffles-api/app/webhook.py:46`, `truffles-api/app/webhook.py:65`, `truffles-api/app/webhook.py:183`, `truffles-api/app/webhook.py:271`, `truffles-api/app/webhook.py:410`, `truffles-api/app/webhook.py:434`, `truffles-api/app/webhook.py:487`, `truffles-api/app/webhook.py:531`, `truffles-api/app/webhook.py:551`.
- `FACT`: The active app mounts `app.routers.webhook.router`, not `app.webhook.router`, and the only visible direct module usage in the repo is a compatibility test around `handle_webhook(...)`: `truffles-api/app/main.py:26`, `truffles-api/app/main.py:37`, `truffles-api/app/main.py:104`, `truffles-api/tests/test_message_endpoint.py:15`, `truffles-api/tests/test_message_endpoint.py:747`, `truffles-api/tests/test_message_endpoint.py:750`.
- `INFERENCE`: `app/webhook.py` is not a live ingress owner. It is an unmounted legacy wrapper plus stale shadow helper warehouse that still preserves old contracts and duplicate definitions.

## 2. Why This File Exists
- `FACT`: The header explicitly says the module is kept for backward compatibility and points new work to `app/routers/webhook/`: `truffles-api/app/webhook.py:1`, `truffles-api/app/webhook.py:3`.
- `FACT`: The only explicit wrapper behavior left in the file is `handle_webhook(...)`, which delegates into `handle_public_webhook_payload(...)` with legacy parameters, plus `debug_webhook(...)`, which echoes the raw request body: `truffles-api/app/webhook.py:20`, `truffles-api/app/webhook.py:543`, `truffles-api/app/webhook.py:551`.
- `FACT`: The same module still preserves shadow copies of helper families that now exist in extracted modules: debounce/dedup helpers in `truffles-api/app/routers/webhook/dedup.py`, handover/booking routing helpers in `truffles-api/app/routers/webhook/decision.py`, booking context/prompt helpers in `truffles-api/app/routers/webhook/booking.py`, conversation-context accessors in `truffles-api/app/routers/webhook/context_manager.py`, and owner-service handover lookup in `truffles-api/app/services/handover_owner_service.py`: `truffles-api/app/webhook.py:65`, `truffles-api/app/webhook.py:183`, `truffles-api/app/webhook.py:271`, `truffles-api/app/webhook.py:378`, `truffles-api/app/webhook.py:410`, `truffles-api/app/webhook.py:421`, `truffles-api/app/webhook.py:434`, `truffles-api/app/webhook.py:465`, `truffles-api/app/webhook.py:487`, `truffles-api/app/webhook.py:504`, `truffles-api/app/webhook.py:518`, `truffles-api/app/webhook.py:531`, `truffles-api/app/routers/webhook/dedup.py:41`, `truffles-api/app/routers/webhook/dedup.py:295`, `truffles-api/app/routers/webhook/decision.py:752`, `truffles-api/app/routers/webhook/decision.py:4761`, `truffles-api/app/routers/webhook/decision.py:8058`, `truffles-api/app/routers/webhook/decision.py:8085`, `truffles-api/app/routers/webhook/decision.py:8555`, `truffles-api/app/routers/webhook/booking.py:152`, `truffles-api/app/routers/webhook/booking.py:159`, `truffles-api/app/routers/webhook/booking.py:978`, `truffles-api/app/routers/webhook/booking.py:1052`, `truffles-api/app/routers/webhook/booking.py:1149`, `truffles-api/app/routers/webhook/context_manager.py:169`, `truffles-api/app/routers/webhook/context_manager.py:209`, `truffles-api/app/services/handover_owner_service.py:645`.
- `INFERENCE`: The file survives because earlier extraction work shrank the runtime ingress to the shared public-entrypoint contract but did not delete the old helper warehouse.

## 3. Active Callers And Entrypoints
- `FACT`: The mounted runtime path goes through the modular webhook package; `app.main` imports `webhook` from `app.routers` and includes that router, while the active `/webhook` and `/webhook/debug` handlers live in `truffles-api/app/routers/webhook/http.py`: `truffles-api/app/main.py:26`, `truffles-api/app/main.py:37`, `truffles-api/app/main.py:104`, `truffles-api/app/routers/webhook/http.py:670`, `truffles-api/app/routers/webhook/http.py:769`.
- `FACT`: The only visible direct module import of `app.webhook` in the repo is the compatibility test for `handle_webhook(...)`: `truffles-api/tests/test_message_endpoint.py:15`, `truffles-api/tests/test_message_endpoint.py:747`, `truffles-api/tests/test_message_endpoint.py:750`.
- `FACT`: This pass found no visible non-test imports of `app.webhook` from runtime code, while active helper consumers use extracted modules or `_legacy` instead: `truffles-api/app/routers/webhook/decision.py:752`, `truffles-api/app/routers/webhook/decision.py:4761`, `truffles-api/app/routers/webhook/booking.py:515`, `truffles-api/app/routers/webhook/context_manager.py:169`, `truffles-api/app/routers/webhook/pending.py:414`, `truffles-api/app/routers/webhook/response.py:3055`.
- `UNKNOWN`: Whether any out-of-repo script or external integration still imports `app.webhook` directly.

## 4. Control Path Owned By This File
- `FACT`: `handle_webhook(...)` is now only a direct pass-through into `handle_public_webhook_payload(...)` with `entrypoint_name="Legacy webhook"`, `ALLOW_UNMATERIALIZED`, `provided_secret=None`, `enforce_secret=False`, and env-driven `enqueue_only`: `truffles-api/app/webhook.py:551`, `truffles-api/app/webhook.py:554`, `truffles-api/app/webhook.py:558`, `truffles-api/app/webhook.py:559`, `truffles-api/app/webhook.py:560`, `truffles-api/app/webhook.py:561`.
- `FACT`: `debug_webhook(...)` simply reads the request JSON and returns it, with no debug-enable switch or admin-token check: `truffles-api/app/webhook.py:543`, `truffles-api/app/webhook.py:546`, `truffles-api/app/webhook.py:548`.
- `FACT`: The mounted replacement in `http.py` has different control-path behavior: the active debug route requires `DEBUG_WEBHOOK_ENABLED` and `X-Admin-Token`, while the mounted `/webhook` wrapper reads the request secret and calls the shared public-entrypoint contract with `enforce_secret=True`: `truffles-api/app/routers/webhook/http.py:670`, `truffles-api/app/routers/webhook/http.py:673`, `truffles-api/app/routers/webhook/http.py:677`, `truffles-api/app/routers/webhook/http.py:680`, `truffles-api/app/routers/webhook/http.py:769`, `truffles-api/app/routers/webhook/http.py:772`, `truffles-api/app/routers/webhook/http.py:778`, `truffles-api/app/routers/webhook/http.py:783`, `truffles-api/app/routers/webhook/http.py:784`.
- `INFERENCE`: The file no longer owns an active production ingress path, but if this wrapper were remounted it would restore stale debug/security semantics that diverge from the active ingress contract.

## 5. Data Reads
- `FACT`: The shadow dedup/debounce helpers read env flags and Redis configuration through `_is_env_enabled(...)`, `_get_debounce_settings(...)`, `_get_message_buffer_settings(...)`, and `_get_dedup_settings(...)`: `truffles-api/app/webhook.py:59`, `truffles-api/app/webhook.py:65`, `truffles-api/app/webhook.py:74`, `truffles-api/app/webhook.py:80`.
- `FACT`: The same helpers read Redis and persistent duplicate state through `_get_debounce_redis(...)`, Redis keys under `truffles:debounce:*` / `truffles:buffer:*` / `truffles:dedup:*`, `message_dedup`, and the `messages` table fallback probe: `truffles-api/app/webhook.py:87`, `truffles-api/app/webhook.py:124`, `truffles-api/app/webhook.py:152`, `truffles-api/app/webhook.py:194`, `truffles-api/app/webhook.py:205`, `truffles-api/app/webhook.py:232`.
- `FACT`: The booking/handover helper family reads raw text, `conversation.context`, `context['booking']`, `ClientSettings`, `Handover`, and `conversation.retry_offered_at`: `truffles-api/app/webhook.py:271`, `truffles-api/app/webhook.py:365`, `truffles-api/app/webhook.py:410`, `truffles-api/app/webhook.py:421`, `truffles-api/app/webhook.py:504`, `truffles-api/app/webhook.py:518`, `truffles-api/app/webhook.py:531`.
- `FACT`: The two remaining wrapper endpoints read raw request JSON and the shared public-entrypoint contract: `truffles-api/app/webhook.py:20`, `truffles-api/app/webhook.py:543`, `truffles-api/app/webhook.py:551`.
- `INFERENCE`: Most reads in this file belong to dormant shadow helper families rather than to the active ingress path.

## 6. Data Writes And Side Effects
- `FACT`: The shadow dedup/debounce helpers write Redis debounce/buffer/dedup keys, insert into `message_dedup`, call `db.commit()`, and log fallback events: `truffles-api/app/webhook.py:131`, `truffles-api/app/webhook.py:154`, `truffles-api/app/webhook.py:167`, `truffles-api/app/webhook.py:199`, `truffles-api/app/webhook.py:207`, `truffles-api/app/webhook.py:217`, `truffles-api/app/webhook.py:219`, `truffles-api/app/webhook.py:244`.
- `FACT`: The booking/context helpers mutate `conversation.context`, `context['booking']`, and booking slot fields like `service`, `datetime`, `name`, and `last_question`: `truffles-api/app/webhook.py:417`, `truffles-api/app/webhook.py:428`, `truffles-api/app/webhook.py:438`, `truffles-api/app/webhook.py:441`, `truffles-api/app/webhook.py:444`, `truffles-api/app/webhook.py:468`, `truffles-api/app/webhook.py:471`, `truffles-api/app/webhook.py:474`, `truffles-api/app/webhook.py:476`.
- `FACT`: `debug_webhook(...)` logs the raw request body, and `handle_webhook(...)` triggers the shared public-entrypoint contract: `truffles-api/app/webhook.py:547`, `truffles-api/app/webhook.py:554`.
- `INFERENCE`: The write-side behavior is mostly dormant shadow behavior today, but it would bypass the extracted helper modules if the file were revived as a runtime path.

## 7. Semantic Authority / Deterministic Authority
- `FACT`: The file still contains deterministic lexicon/regex authority for handover-status detection, booking-request detection, booking-cancel detection, service/datetime extraction, and low-confidence retry timing: `truffles-api/app/webhook.py:271`, `truffles-api/app/webhook.py:303`, `truffles-api/app/webhook.py:313`, `truffles-api/app/webhook.py:361`, `truffles-api/app/webhook.py:378`, `truffles-api/app/webhook.py:385`, `truffles-api/app/webhook.py:392`, `truffles-api/app/webhook.py:401`, `truffles-api/app/webhook.py:531`.
- `FACT`: Active equivalents now live in extracted modules instead of here: `is_handover_status_question(...)`, `_is_booking_request(...)`, `find_active_conversation_by_channel_ref(...)`, `get_mute_settings(...)`, and `should_offer_low_confidence_retry(...)` in `decision.py`; booking update/prompt helpers in `booking.py`; context accessors in `context_manager.py`; and `get_active_handover(...)` in `handover_owner_service.py`: `truffles-api/app/routers/webhook/decision.py:752`, `truffles-api/app/routers/webhook/decision.py:4761`, `truffles-api/app/routers/webhook/decision.py:8058`, `truffles-api/app/routers/webhook/decision.py:8085`, `truffles-api/app/routers/webhook/decision.py:8555`, `truffles-api/app/routers/webhook/booking.py:978`, `truffles-api/app/routers/webhook/booking.py:1052`, `truffles-api/app/routers/webhook/context_manager.py:169`, `truffles-api/app/routers/webhook/context_manager.py:209`, `truffles-api/app/services/handover_owner_service.py:645`.
- `INFERENCE`: `app/webhook.py` is not an active semantic owner, but it still stores stale deterministic semantic authority that can drift from the active extracted modules.

## 8. Truth Carriers Touched Here
- `FACT`: The shadow dedup family uses Redis routing carriers `truffles:debounce:*`, `truffles:buffer:*`, and `truffles:dedup:*`, plus persistent duplicate carriers in `message_dedup` and `Message.message_metadata[message_id]`: `truffles-api/app/webhook.py:124`, `truffles-api/app/webhook.py:152`, `truffles-api/app/webhook.py:194`, `truffles-api/app/webhook.py:205`, `truffles-api/app/webhook.py:232`.
- `FACT`: The shadow booking family uses `conversation.context`, `context['booking']`, and booking slot carriers such as `service`, `datetime`, `name`, and `last_question`: `truffles-api/app/webhook.py:410`, `truffles-api/app/webhook.py:421`, `truffles-api/app/webhook.py:436`, `truffles-api/app/webhook.py:438`, `truffles-api/app/webhook.py:441`, `truffles-api/app/webhook.py:444`, `truffles-api/app/webhook.py:468`, `truffles-api/app/webhook.py:471`, `truffles-api/app/webhook.py:474`.
- `FACT`: The same file also touches handover/mute continuity carriers through `Handover.status`, `ClientSettings` mute durations, and `conversation.retry_offered_at`: `truffles-api/app/webhook.py:492`, `truffles-api/app/webhook.py:506`, `truffles-api/app/webhook.py:533`.
- `INFERENCE`: These are not active truth carriers on the mounted ingress path, but they remain duplicate definitions of active carrier families elsewhere in the codebase.

## 9. Violations Against The Target Canon
- `FACT`: A file explicitly marked `DO NOT USE FOR NEW WEBHOOKS` still preserves shadow copies of helper families already extracted into `dedup.py`, `decision.py`, `booking.py`, `context_manager.py`, and `handover_owner_service.py`: `truffles-api/app/webhook.py:1`, `truffles-api/app/webhook.py:65`, `truffles-api/app/webhook.py:183`, `truffles-api/app/webhook.py:271`, `truffles-api/app/webhook.py:410`, `truffles-api/app/webhook.py:434`, `truffles-api/app/webhook.py:487`, `truffles-api/app/webhook.py:518`, `truffles-api/app/routers/webhook/dedup.py:41`, `truffles-api/app/routers/webhook/decision.py:752`, `truffles-api/app/routers/webhook/booking.py:152`, `truffles-api/app/routers/webhook/context_manager.py:169`, `truffles-api/app/services/handover_owner_service.py:645`.
- `FACT`: The wrapper contract is stale relative to the active ingress because `app/webhook.py` disables secret enforcement and exposes an always-open debug echo, while the mounted `http.py` wrapper enforces secret checking and guards debug access behind env/token checks: `truffles-api/app/webhook.py:543`, `truffles-api/app/webhook.py:551`, `truffles-api/app/webhook.py:559`, `truffles-api/app/webhook.py:560`, `truffles-api/app/routers/webhook/http.py:670`, `truffles-api/app/routers/webhook/http.py:673`, `truffles-api/app/routers/webhook/http.py:677`, `truffles-api/app/routers/webhook/http.py:772`, `truffles-api/app/routers/webhook/http.py:783`, `truffles-api/app/routers/webhook/http.py:784`.
- `FACT`: The module is not mounted by the application router but is still pinned by tests, so stale helper families survive in repo memory even after ingress cutover: `truffles-api/app/main.py:26`, `truffles-api/app/main.py:37`, `truffles-api/app/main.py:104`, `truffles-api/tests/test_message_endpoint.py:15`, `truffles-api/tests/test_message_endpoint.py:747`, `truffles-api/tests/test_message_endpoint.py:750`.
- `INFERENCE`: Strategic point `shadowed core definition` remains open here because legacy webhook helper families now exist both in extracted modules and in this unmounted root-level module.
- `INFERENCE`: Strategic point `deterministic only at boundaries` remains open here because a dormant module still preserves alternate deterministic routing and persistence behavior outside the extracted ingress boundary.

## 10. Salvageable Parts
- `FACT`: The only clearly salvageable part is the idea that any residual legacy wrapper should be a tiny delegate into `handle_public_webhook_payload(...)`, because the file already reduced that surface to a small adapter body: `truffles-api/app/webhook.py:20`, `truffles-api/app/webhook.py:551`, `truffles-api/app/webhook.py:554`.
- `FACT`: The shadow helper families are not uniquely salvageable from this module because active replacements already exist in extracted files and owner services: `truffles-api/app/routers/webhook/dedup.py:41`, `truffles-api/app/routers/webhook/decision.py:752`, `truffles-api/app/routers/webhook/booking.py:152`, `truffles-api/app/routers/webhook/context_manager.py:169`, `truffles-api/app/services/handover_owner_service.py:645`.
- `INFERENCE`: If anything survives here long-term, it should be a zero-logic compatibility stub, not another helper warehouse.

## 11. Demotion / Removal Candidates
- `FACT`: `debug_webhook(...)` and the root-level `handle_webhook(...)` router definitions are primary demotion targets because mounted replacements already exist in `http.py`, and the local versions preserve weaker debug/security semantics: `truffles-api/app/webhook.py:543`, `truffles-api/app/webhook.py:551`, `truffles-api/app/routers/webhook/http.py:670`, `truffles-api/app/routers/webhook/http.py:769`.
- `FACT`: The shadow debounce/dedup family at `truffles-api/app/webhook.py:65` through `truffles-api/app/webhook.py:244` is a demotion target because extracted equivalents already live in `truffles-api/app/routers/webhook/dedup.py:41` through `truffles-api/app/routers/webhook/dedup.py:429`.
- `FACT`: The shadow booking/context/handover family at `truffles-api/app/webhook.py:271` through `truffles-api/app/webhook.py:540` is a demotion target because extracted equivalents already live in `decision.py`, `booking.py`, `context_manager.py`, and `handover_owner_service.py`: `truffles-api/app/routers/webhook/decision.py:752`, `truffles-api/app/routers/webhook/decision.py:8058`, `truffles-api/app/routers/webhook/decision.py:8085`, `truffles-api/app/routers/webhook/decision.py:8555`, `truffles-api/app/routers/webhook/booking.py:152`, `truffles-api/app/routers/webhook/booking.py:978`, `truffles-api/app/routers/webhook/booking.py:1052`, `truffles-api/app/routers/webhook/context_manager.py:169`, `truffles-api/app/routers/webhook/context_manager.py:209`, `truffles-api/app/services/handover_owner_service.py:645`.
- `INFERENCE`: Real demotion here means collapsing this module to nothing or to one audited compatibility delegate, then deleting the stale helper copies.

## 12. What This Analysis Changes In System Understanding
- `FACT`: `app/webhook.py` is not an active ingress path. It is an unmounted compatibility/test residue whose visible direct module usage is limited to `test_message_endpoint.py`, while the mounted runtime path goes through `app.routers.webhook.http`.
- `FACT`: The dedup/debounce extraction family is now mapped more precisely: the shadow copy that remained after extraction is not another live ingress module but an unmounted root-level legacy module with stale helper definitions and weaker wrapper semantics.
- `FACT`: The same file also proves that helper-family duplication is broader than just dedup/debounce; old booking/context/handover helpers still exist at the repo root even though extracted replacements live elsewhere.
- `INFERENCE`: After this hotspot, the next honest scope is caller surfaces and secondary compatibility files around the legacy wrapper/package split, not implementation work.

## 13. Open Questions
- `UNKNOWN`: Whether any out-of-repo caller still imports `app.webhook` directly.
- `UNKNOWN`: Whether the repo still intentionally needs the `test_message_endpoint.py` compatibility contract for `app.webhook.handle_webhook(...)`.
- `UNKNOWN`: Whether any of the shadow helper definitions in `app/webhook.py` are still required for scripts or notebooks outside the repo tree.

# File Analysis: `truffles-api/app/routers/webhook/guards.py`

Status: `completed`
Baseline snapshot: `8319d9e1`
Related ledgers:
- `docs/system_forensics/ledgers/CONTROL_PATHS.md`
- `docs/system_forensics/ledgers/SEMANTIC_OWNER_MAP.md`
- `docs/system_forensics/ledgers/TRUTH_CARRIER_MATRIX.md`
- `docs/system_forensics/ledgers/STATE_SURFACE_INVENTORY.md`
- `docs/system_forensics/ledgers/DETERMINISTIC_REWRITE_LEDGER.md`
- `docs/system_forensics/ledgers/CUTOVER_DEPENDENCY_GRAPH.md`
- `docs/system_forensics/ledgers/SALVAGEABLE_COMPONENTS.md`
- `docs/system_forensics/ledgers/DO_NOT_REPEAT.md`
- `docs/system_forensics/final/SYSTEM_FINAL_ANALYSIS.md`

## 1. Role Summary
- `FACT`: Although the file docstring says `Intent-queue and clarify-guard helpers.`, the file actually contains a broader mixed guard subsystem: human-lock trace persistence, intent-queue helpers, clarify-attempt state, clarify-limit escalation, session-timeout reset, reengage/mute routing, post-debounce muted-state routing, opt-out mute handling, and SLA collect-only reply handling: `truffles-api/app/routers/webhook/guards.py:1`, `truffles-api/app/routers/webhook/guards.py:44`, `truffles-api/app/routers/webhook/guards.py:74`, `truffles-api/app/routers/webhook/guards.py:167`, `truffles-api/app/routers/webhook/guards.py:250`, `truffles-api/app/routers/webhook/guards.py:348`, `truffles-api/app/routers/webhook/guards.py:370`, `truffles-api/app/routers/webhook/guards.py:668`, `truffles-api/app/routers/webhook/guards.py:727`, `truffles-api/app/routers/webhook/guards.py:787`.
- `FACT`: The visible live consumers are spread across the legacy webhook stack rather than one narrow caller: `decision.py` imports a broad guard surface, `response.py`, `booking.py`, and `info.py` use clarify helpers through `_legacy`, and `dedup.py` directly composes `_handle_post_debounce_muted_state_gate(...)` after debounce: `truffles-api/app/routers/webhook/decision.py:172`, `truffles-api/app/routers/webhook/decision.py:185`, `truffles-api/app/routers/webhook/decision.py:2320`, `truffles-api/app/routers/webhook/response.py:1138`, `truffles-api/app/routers/webhook/response.py:3075`, `truffles-api/app/routers/webhook/booking.py:2541`, `truffles-api/app/routers/webhook/booking.py:2575`, `truffles-api/app/routers/webhook/info.py:1809`, `truffles-api/app/routers/webhook/dedup.py:541`, `truffles-api/app/routers/webhook/dedup.py:643`.
- `INFERENCE`: `guards.py` is not a small clarify helper file; it is the live guard/mute/human-lock/clarify subsystem plus partly dormant gate residue that still shapes routing, escalation, silence, and user-visible guard replies across the legacy webhook stack.

## 2. Why This File Exists
- `FACT`: The file exists to maintain queue-driven follow-up state through `_get_intent_queue(...)`, `_set_intent_queue(...)`, `_match_intent_choice_from_text(...)`, `_select_intent_from_queue(...)`, and the prompt helpers `_format_multi_intent_followup(...)` / `_format_intent_queue_prompt(...)`: `truffles-api/app/routers/webhook/guards.py:74`, `truffles-api/app/routers/webhook/guards.py:78`, `truffles-api/app/routers/webhook/guards.py:100`, `truffles-api/app/routers/webhook/guards.py:133`, `truffles-api/app/routers/webhook/guards.py:151`.
- `FACT`: It also exists to manage clarify-attempt state and escalation through `_get_clarify_attempt_state(...)`, `_set_clarify_attempt(...)`, `_should_escalate_for_clarify(...)`, `_booking_clarify_guard_reason(...)`, `_register_clarify_attempt(...)`, and `_handle_clarify_limit_escalation(...)`: `truffles-api/app/routers/webhook/guards.py:167`, `truffles-api/app/routers/webhook/guards.py:171`, `truffles-api/app/routers/webhook/guards.py:180`, `truffles-api/app/routers/webhook/guards.py:187`, `truffles-api/app/routers/webhook/guards.py:216`, `truffles-api/app/routers/webhook/guards.py:250`.
- `FACT`: The file further exists to run session timeout, human-lock, muted-state, reengage, opt-out mute, and SLA collect-only routing through `_apply_session_timeout_reset(...)`, `_handle_reengage_and_mute_gate(...)`, `_handle_post_debounce_muted_state_gate(...)`, `_handle_opt_out_mute_gate(...)`, and `_handle_sla_collect_only_gate(...)`: `truffles-api/app/routers/webhook/guards.py:348`, `truffles-api/app/routers/webhook/guards.py:370`, `truffles-api/app/routers/webhook/guards.py:668`, `truffles-api/app/routers/webhook/guards.py:727`, `truffles-api/app/routers/webhook/guards.py:787`.
- `INFERENCE`: The file exists because queue/clarify/mute/human-lock/SLA behavior was split out of `decision.py` into a helper-rich guard module, but it was not reduced to a thin boundary or to a fully manifest-driven interaction-guard surface.

## 3. Active Callers And Entrypoints
- `FACT`: `decision.py` imports the broadest visible guard surface, including `_apply_session_timeout_reset(...)`, `_booking_clarify_guard_reason(...)`, `_format_intent_queue_prompt(...)`, `_format_multi_intent_followup(...)`, `_get_clarify_attempt_state(...)`, `_handle_clarify_limit_escalation(...)`, `_handle_opt_out_mute_gate(...)`, `_handle_reengage_and_mute_gate(...)`, `_handle_sla_collect_only_gate(...)`, `_register_clarify_attempt(...)`, `_select_intent_from_queue(...)`, and `_set_clarify_attempt(...)`: `truffles-api/app/routers/webhook/decision.py:172`, `truffles-api/app/routers/webhook/decision.py:185`.
- `FACT`: Visible non-test live callsites include `decision.py` using `_select_intent_from_queue(...)`, `response.py` / `booking.py` / `info.py` using `_register_clarify_attempt(...)` and `_handle_clarify_limit_escalation(...)` through `_legacy`, `booking.py` using `_booking_clarify_guard_reason(...)`, and `dedup.py` using `_handle_post_debounce_muted_state_gate(...)`: `truffles-api/app/routers/webhook/decision.py:2320`, `truffles-api/app/routers/webhook/response.py:1138`, `truffles-api/app/routers/webhook/response.py:3075`, `truffles-api/app/routers/webhook/booking.py:2541`, `truffles-api/app/routers/webhook/booking.py:2575`, `truffles-api/app/routers/webhook/info.py:1809`, `truffles-api/app/routers/webhook/dedup.py:541`, `truffles-api/app/routers/webhook/dedup.py:643`.
- `FACT`: Tests import the module directly and pin guard runtime behavior: `truffles-api/tests/test_message_endpoint.py:37`, `truffles-api/tests/test_webhook_dedup.py:10`, `truffles-api/tests/test_state_service.py:2276`.
- `UNKNOWN`: Whether `_apply_session_timeout_reset(...)`, `_handle_reengage_and_mute_gate(...)`, `_handle_opt_out_mute_gate(...)`, `_handle_sla_collect_only_gate(...)`, `_format_intent_queue_prompt(...)`, and `_format_multi_intent_followup(...)` still have any live non-definition callers beyond their import residency in `decision.py`, because no visible non-definition callsites were found during this pass.

## 4. Control Path Owned By This File
- `FACT`: `_select_intent_from_queue(...)` is the visible active queue-selection entrypoint, and it falls back to `_match_intent_choice_from_text(...)` to convert normalized text tokens into one queue choice: `truffles-api/app/routers/webhook/guards.py:100`, `truffles-api/app/routers/webhook/guards.py:133`, `truffles-api/app/routers/webhook/decision.py:2320`.
- `FACT`: `_register_clarify_attempt(...)` and `_handle_clarify_limit_escalation(...)` are the active clarify-control entrypoints; they update clarify counters through `DialogStateService`, record trace/meta/context-manager decisions, and can escalate through active handover reuse or pending creation before saving and sending a reply: `truffles-api/app/routers/webhook/guards.py:216`, `truffles-api/app/routers/webhook/guards.py:234`, `truffles-api/app/routers/webhook/guards.py:250`, `truffles-api/app/routers/webhook/guards.py:269`, `truffles-api/app/routers/webhook/guards.py:308`, `truffles-api/app/routers/webhook/guards.py:328`, `truffles-api/app/routers/webhook/response.py:3075`, `truffles-api/app/routers/webhook/booking.py:2541`, `truffles-api/app/routers/webhook/info.py:1809`.
- `FACT`: `_handle_post_debounce_muted_state_gate(...)` is a live direct routing entrypoint because `dedup.py` calls it after debounce before the message continues down the stack: `truffles-api/app/routers/webhook/guards.py:668`, `truffles-api/app/routers/webhook/dedup.py:541`, `truffles-api/app/routers/webhook/dedup.py:643`.
- `FACT`: `_handle_reengage_and_mute_gate(...)`, `_handle_opt_out_mute_gate(...)`, `_handle_sla_collect_only_gate(...)`, and `_apply_session_timeout_reset(...)` still contain full routing/mutation/reply bodies that own human-lock silence, reengage confirmation, mute windows, opt-out replies, SLA guard replies, and session-reset behavior inside the module even though this pass did not find visible live callsites: `truffles-api/app/routers/webhook/guards.py:348`, `truffles-api/app/routers/webhook/guards.py:370`, `truffles-api/app/routers/webhook/guards.py:409`, `truffles-api/app/routers/webhook/guards.py:472`, `truffles-api/app/routers/webhook/guards.py:618`, `truffles-api/app/routers/webhook/guards.py:727`, `truffles-api/app/routers/webhook/guards.py:787`.
- `INFERENCE`: The file owns a split control path: the clarify and post-debounce muted-state surfaces are visibly live, while several larger mute/SLA/reengage gate bodies look like residual routing authority preserved by imports and compatibility expectations.

## 5. Data Reads
- `FACT`: The file reads canonicalized queue and clarify-attempt state through `DialogStateService.get_intent_queue(...)`, `set_intent_queue(...)`, `get_clarify_attempt_state(...)`, and `set_clarify_attempt_state(...)`: `truffles-api/app/routers/webhook/guards.py:22`, `truffles-api/app/routers/webhook/guards.py:74`, `truffles-api/app/routers/webhook/guards.py:78`, `truffles-api/app/routers/webhook/guards.py:167`, `truffles-api/app/routers/webhook/guards.py:171`.
- `FACT`: It reads human-lock state through `get_active_human_lock(...)` and remote-JID normalization, plus SLA runtime mode through `SLA_RUNTIME_CONTEXT_KEY`, `SLA_RUNTIME_MODE_COLLECT_ONLY`, and `is_collect_only_runtime_active(...)`: `truffles-api/app/routers/webhook/guards.py:19`, `truffles-api/app/routers/webhook/guards.py:20`, `truffles-api/app/routers/webhook/guards.py:370`, `truffles-api/app/routers/webhook/guards.py:409`, `truffles-api/app/routers/webhook/guards.py:787`, `truffles-api/app/routers/webhook/guards.py:803`.
- `FACT`: The file repeatedly reads `_legacy.py` helpers and constants for normalized text matching, conversation context, booking state, low-signal/ack detection, handover reuse, pending escalation, mute settings, trace/meta writers, prompt constants, send/save, and Telegram notification: `truffles-api/app/routers/webhook/guards.py:51`, `truffles-api/app/routers/webhook/guards.py:89`, `truffles-api/app/routers/webhook/guards.py:183`, `truffles-api/app/routers/webhook/guards.py:209`, `truffles-api/app/routers/webhook/guards.py:226`, `truffles-api/app/routers/webhook/guards.py:269`, `truffles-api/app/routers/webhook/guards.py:388`, `truffles-api/app/routers/webhook/guards.py:399`, `truffles-api/app/routers/webhook/guards.py:472`, `truffles-api/app/routers/webhook/guards.py:684`, `truffles-api/app/routers/webhook/guards.py:747`, `truffles-api/app/routers/webhook/guards.py:800`.
- `INFERENCE`: `guards.py` reads from too many layers to be a thin interaction-guard boundary; it spans canonical queue/counter bridges, `_legacy` routing/context helpers, human-lock service state, and direct conversation/message inputs.

## 6. Data Writes And Side Effects
- `FACT`: `_register_clarify_attempt(...)` writes clarify-attempt state back into context via `DialogStateService`, persists context manager decisions, and can trigger compact-summary updates: `truffles-api/app/routers/webhook/guards.py:226`, `truffles-api/app/routers/webhook/guards.py:230`, `truffles-api/app/routers/webhook/guards.py:231`, `truffles-api/app/routers/webhook/guards.py:234`, `truffles-api/app/routers/webhook/guards.py:240`.
- `FACT`: `_handle_clarify_limit_escalation(...)` writes decision traces and message decision metadata, can reuse active handover, can escalate to pending, can send Telegram notifications, saves the reply, sends it, and commits the database transaction: `truffles-api/app/routers/webhook/guards.py:269`, `truffles-api/app/routers/webhook/guards.py:285`, `truffles-api/app/routers/webhook/guards.py:308`, `truffles-api/app/routers/webhook/guards.py:317`, `truffles-api/app/routers/webhook/guards.py:325`, `truffles-api/app/routers/webhook/guards.py:328`, `truffles-api/app/routers/webhook/guards.py:339`.
- `FACT`: `_handle_reengage_and_mute_gate(...)`, `_handle_post_debounce_muted_state_gate(...)`, and `_handle_opt_out_mute_gate(...)` mutate `conversation.bot_status`, `conversation.bot_muted_until`, `conversation.no_count`, `reengage_confirmation`, booking state, and human-lock metadata while recording decision traces and sometimes sending replies/committing the transaction: `truffles-api/app/routers/webhook/guards.py:428`, `truffles-api/app/routers/webhook/guards.py:444`, `truffles-api/app/routers/webhook/guards.py:484`, `truffles-api/app/routers/webhook/guards.py:510`, `truffles-api/app/routers/webhook/guards.py:618`, `truffles-api/app/routers/webhook/guards.py:626`, `truffles-api/app/routers/webhook/guards.py:706`, `truffles-api/app/routers/webhook/guards.py:749`, `truffles-api/app/routers/webhook/guards.py:758`, `truffles-api/app/routers/webhook/guards.py:767`.
- `FACT`: `_handle_sla_collect_only_gate(...)` writes router observability, decision trace, message metadata, SLA runtime metadata, sends a guard reply, and commits: `truffles-api/app/routers/webhook/guards.py:816`, `truffles-api/app/routers/webhook/guards.py:831`, `truffles-api/app/routers/webhook/guards.py:832`, `truffles-api/app/routers/webhook/guards.py:840`, `truffles-api/app/routers/webhook/guards.py:850`, `truffles-api/app/routers/webhook/guards.py:856`.
- `INFERENCE`: `guards.py` is not a pure detector module; it still contains direct state mutation, trace/meta mutation, handover/escalation, reply sending, and transaction commit behavior.

## 7. Semantic Authority / Deterministic Authority
- `FACT`: The file deterministically maps normalized user text into queue choices through `_match_intent_choice_from_text(...)` and `_select_intent_from_queue(...)`: `truffles-api/app/routers/webhook/guards.py:100`, `truffles-api/app/routers/webhook/guards.py:133`, `truffles-api/app/routers/webhook/decision.py:2320`.
- `FACT`: It also deterministically classifies guard reasons and clarify escalation through `_booking_clarify_guard_reason(...)`, `_should_escalate_for_clarify(...)`, `_register_clarify_attempt(...)`, and `_handle_clarify_limit_escalation(...)`: `truffles-api/app/routers/webhook/guards.py:180`, `truffles-api/app/routers/webhook/guards.py:187`, `truffles-api/app/routers/webhook/guards.py:216`, `truffles-api/app/routers/webhook/guards.py:250`.
- `FACT`: `_handle_reengage_and_mute_gate(...)`, `_handle_post_debounce_muted_state_gate(...)`, `_handle_opt_out_mute_gate(...)`, and `_handle_sla_collect_only_gate(...)` deterministically rewrite routing into human-lock silence, reengage confirmation, muted skips, opt-out mute responses, and SLA collect-only responses without any target-state owner call in the module: `truffles-api/app/routers/webhook/guards.py:409`, `truffles-api/app/routers/webhook/guards.py:472`, `truffles-api/app/routers/webhook/guards.py:618`, `truffles-api/app/routers/webhook/guards.py:678`, `truffles-api/app/routers/webhook/guards.py:727`, `truffles-api/app/routers/webhook/guards.py:787`.
- `INFERENCE`: `guards.py` is not a target-state semantic owner, but it is a major deterministic guard authority seam that still classifies user text and manufactures silence/escalation/reply outcomes outside the extracted runtime path.

## 8. Truth Carriers Touched Here
- `FACT`: The file reads and writes `intent_queue` through the `DialogStateService` bridge and uses that queue as an explicit continuity carrier for multi-intent follow-ups: `truffles-api/app/routers/webhook/guards.py:74`, `truffles-api/app/routers/webhook/guards.py:78`, `truffles-api/app/routers/webhook/guards.py:133`, `truffles-api/app/routers/webhook/decision.py:2320`.
- `FACT`: It reads and writes clarify-attempt payloads in manager state through `_get_clarify_attempt_state(...)`, `_set_clarify_attempt(...)`, and `_register_clarify_attempt(...)`, then mirrors that meaning into context-manager decision updates: `truffles-api/app/routers/webhook/guards.py:167`, `truffles-api/app/routers/webhook/guards.py:171`, `truffles-api/app/routers/webhook/guards.py:226`, `truffles-api/app/routers/webhook/guards.py:234`.
- `FACT`: It manufactures human-lock trace and message metadata carriers through `_ensure_human_lock_trace_persisted(...)`, `human_lock`, and `human_lock_trace`: `truffles-api/app/routers/webhook/guards.py:44`, `truffles-api/app/routers/webhook/guards.py:422`, `truffles-api/app/routers/webhook/guards.py:444`, `truffles-api/app/routers/webhook/guards.py:451`.
- `FACT`: It also carries reengage/mute and SLA truth through `reengage_confirmation`, `bot_muted_until`, `no_count`, `sla_runtime_mode`, `sla_runtime_reason`, `sla_runtime_severity`, and router observability payloads: `truffles-api/app/routers/webhook/guards.py:473`, `truffles-api/app/routers/webhook/guards.py:510`, `truffles-api/app/routers/webhook/guards.py:618`, `truffles-api/app/routers/webhook/guards.py:747`, `truffles-api/app/routers/webhook/guards.py:803`, `truffles-api/app/routers/webhook/guards.py:816`, `truffles-api/app/routers/webhook/guards.py:840`.
- `INFERENCE`: `guards.py` is another place where compatibility carriers become final routing, silence, and escalation behavior, so guard-side truth still lives beyond canonical runtime state.

## 9. Violations Against The Target Canon
- `FACT`: The file still mixes queue matching, clarify counting/escalation, human-lock handling, session timeout reset, mute/reengage routing, opt-out mute, SLA guard replies, and direct transport side effects inside one module: `truffles-api/app/routers/webhook/guards.py:100`, `truffles-api/app/routers/webhook/guards.py:216`, `truffles-api/app/routers/webhook/guards.py:250`, `truffles-api/app/routers/webhook/guards.py:348`, `truffles-api/app/routers/webhook/guards.py:370`, `truffles-api/app/routers/webhook/guards.py:727`, `truffles-api/app/routers/webhook/guards.py:787`.
- `FACT`: The file still depends heavily on `_legacy.py` for routing helpers, context access, trace/meta writers, prompt constants, handover reuse, pending escalation, and reply transport: `truffles-api/app/routers/webhook/guards.py:51`, `truffles-api/app/routers/webhook/guards.py:183`, `truffles-api/app/routers/webhook/guards.py:226`, `truffles-api/app/routers/webhook/guards.py:269`, `truffles-api/app/routers/webhook/guards.py:399`, `truffles-api/app/routers/webhook/guards.py:472`, `truffles-api/app/routers/webhook/guards.py:684`, `truffles-api/app/routers/webhook/guards.py:800`.
- `FACT`: The file still hardcodes token matching, clarify max-attempt behavior, reengage/mute branching, and muted-state routing in code instead of making guard behavior entirely owner- or manifest-driven: `truffles-api/app/routers/webhook/guards.py:100`, `truffles-api/app/routers/webhook/guards.py:183`, `truffles-api/app/routers/webhook/guards.py:209`, `truffles-api/app/routers/webhook/guards.py:370`, `truffles-api/app/routers/webhook/guards.py:618`, `truffles-api/app/routers/webhook/guards.py:727`.
- `FACT`: The direct guard handlers still own send/save, handover reuse, pending escalation, and `db.commit()` side effects instead of a thin projection/execution boundary: `truffles-api/app/routers/webhook/guards.py:269`, `truffles-api/app/routers/webhook/guards.py:328`, `truffles-api/app/routers/webhook/guards.py:339`, `truffles-api/app/routers/webhook/guards.py:528`, `truffles-api/app/routers/webhook/guards.py:758`, `truffles-api/app/routers/webhook/guards.py:856`.
- `INFERENCE`: Strategic point `deterministic only at boundaries` remains open here because queue selection, clarify routing, mute routing, and guard reply/escalation logic still live in a mixed legacy helper module outside the extracted runtime owner path.
- `INFERENCE`: Strategic point `one canonical semantic state` remains open here because queue, clarify-attempt, human-lock, reengage, mute, and SLA carriers still become live routing truth outside the runtime canonical state boundary.

## 10. Salvageable Parts
- `FACT`: The `DialogStateService` bridge wrappers are salvageable if relocated behind a cleaner continuity boundary: `_get_intent_queue(...)`, `_set_intent_queue(...)`, `_get_clarify_attempt_state(...)`, and `_set_clarify_attempt(...)`: `truffles-api/app/routers/webhook/guards.py:74`, `truffles-api/app/routers/webhook/guards.py:78`, `truffles-api/app/routers/webhook/guards.py:167`, `truffles-api/app/routers/webhook/guards.py:171`.
- `FACT`: The bounded human-lock trace helpers are salvageable if moved into a narrower trace/human-lock boundary: `_is_human_lock_trace_entry(...)` and `_ensure_human_lock_trace_persisted(...)`: `truffles-api/app/routers/webhook/guards.py:30`, `truffles-api/app/routers/webhook/guards.py:44`.
- `FACT`: The bounded prompt/guard-reason helpers are salvageable if they are relocated into an explicit interaction-guard layer rather than left inside a mixed routing module: `_format_multi_intent_followup(...)`, `_format_intent_queue_prompt(...)`, and `_booking_clarify_guard_reason(...)`: `truffles-api/app/routers/webhook/guards.py:82`, `truffles-api/app/routers/webhook/guards.py:151`, `truffles-api/app/routers/webhook/guards.py:187`.
- `INFERENCE`: The salvageable core is the small queue/counter bridge and bounded trace/prompt helpers, not the mixed routing/escalation/transport authority that currently surrounds them.

## 11. Demotion / Removal Candidates
- `FACT`: `_handle_clarify_limit_escalation(...)`, `_handle_reengage_and_mute_gate(...)`, `_handle_post_debounce_muted_state_gate(...)`, `_handle_opt_out_mute_gate(...)`, and `_handle_sla_collect_only_gate(...)` are the main demotion candidates because they keep silence/routing/escalation/reply transport authority inside the guard hotspot: `truffles-api/app/routers/webhook/guards.py:250`, `truffles-api/app/routers/webhook/guards.py:370`, `truffles-api/app/routers/webhook/guards.py:668`, `truffles-api/app/routers/webhook/guards.py:727`, `truffles-api/app/routers/webhook/guards.py:787`.
- `FACT`: `_match_intent_choice_from_text(...)` and `_select_intent_from_queue(...)` are also demotion targets if queue semantics are to become owner- or canonical-state-driven rather than token-driven router code: `truffles-api/app/routers/webhook/guards.py:100`, `truffles-api/app/routers/webhook/guards.py:133`.
- `FACT`: `_apply_session_timeout_reset(...)` is another removal candidate because it still resets conversation state/context directly from a guard helper module: `truffles-api/app/routers/webhook/guards.py:348`, `truffles-api/app/routers/webhook/guards.py:362`, `truffles-api/app/routers/webhook/guards.py:366`.
- `INFERENCE`: Real demotion requires separating bounded queue/counter/trace helpers from live guard routing, muted-state control, and direct reply/escalation behavior.

## 12. What This Analysis Changes In System Understanding
- `FACT`: `guards.py` is not just a clarify helper; it is the live guard/mute/human-lock subsystem plus partly dormant gate residue of the legacy webhook stack.
- `FACT`: The file is a live composition point where canonical queue/counter bridges, `_legacy.py` routing/context helpers, human-lock service state, muted-state windows, and direct reply/escalation transport combine into silence decisions, clarify-limit escalations, muted skips, and guard replies.
- `FACT`: The file still contains active deterministic guard authority and direct trace/meta side effects outside the extracted runtime path, even if several larger gate bodies may now be low-visibility residue.
- `INFERENCE`: After this hotspot, the next honest hotspot is `truffles-api/app/routers/webhook/dedup.py`, because it is the next live routing stage that composes debounce buffering with `_handle_post_debounce_muted_state_gate(...)` and therefore still feeds the same legacy guard family into active inbound control flow.

## 13. Open Questions
- `UNKNOWN`: Whether `_apply_session_timeout_reset(...)`, `_handle_reengage_and_mute_gate(...)`, `_handle_opt_out_mute_gate(...)`, `_handle_sla_collect_only_gate(...)`, `_format_intent_queue_prompt(...)`, and `_format_multi_intent_followup(...)` are still reachable on any live path or now survive only as compatibility residue imported by `decision.py`.
- `UNKNOWN`: Which parts of the queue / clarify-attempt state should remain as explicit runtime continuity primitives versus disappear with the legacy webhook guard stack.
- `UNKNOWN`: Whether human-lock and muted-state routing should survive as dedicated deterministic boundaries or be re-expressed through canonical runtime state plus explicit boundary reason-codes.
- `UNKNOWN`: How much of the SLA collect-only reply path should survive as a bounded transport guard versus disappear with the legacy webhook guard stack.

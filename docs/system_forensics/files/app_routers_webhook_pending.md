# File Analysis: `truffles-api/app/routers/webhook/pending.py`

Status: `completed`
Baseline snapshot: `8319d9e1`
Related ledgers:
- `docs/system_forensics/ledgers/CONTROL_PATHS.md`
- `docs/system_forensics/ledgers/SEMANTIC_OWNER_MAP.md`
- `docs/system_forensics/ledgers/TRUTH_CARRIER_MATRIX.md`
- `docs/system_forensics/ledgers/STATE_SURFACE_INVENTORY.md`
- `docs/system_forensics/ledgers/DETERMINISTIC_REWRITE_LEDGER.md`
- `docs/system_forensics/ledgers/CUTOVER_DEPENDENCY_GRAPH.md`
- `docs/system_forensics/ledgers/SALVAGEABLE_COMPONENTS.md`
- `docs/system_forensics/ledgers/DO_NOT_REPEAT.md`
- `docs/system_forensics/final/SYSTEM_FINAL_ANALYSIS.md`

## 1. Role Summary
- `FACT`: Although the file docstring says `Pending SLA and resume helpers.`, the file actually contains a broader pending/handover transport subsystem: pending lexicon matching, handover-confirmation gate, Telegram forwarding for pending/manager-active threads, manager-active silent routing, pending-state ack/close/wait/status handling, SLA escalation/ping transport, and soft-pass routing: `truffles-api/app/routers/webhook/pending.py:1`, `truffles-api/app/routers/webhook/pending.py:25`, `truffles-api/app/routers/webhook/pending.py:70`, `truffles-api/app/routers/webhook/pending.py:107`, `truffles-api/app/routers/webhook/pending.py:160`, `truffles-api/app/routers/webhook/pending.py:281`, `truffles-api/app/routers/webhook/pending.py:313`.
- `FACT`: The visible non-test direct importer is `decision.py`, which imports `_forward_pending_to_telegram(...)`, `_handle_handover_confirmation_gate(...)`, `_handle_manager_active_gate(...)`, and `_handle_pending_gate(...)`: `truffles-api/app/routers/webhook/decision.py:224`.
- `INFERENCE`: `pending.py` is not a narrow SLA helper; it is the live pending/manager-active continuity transport subsystem inside the legacy webhook stack.

## 2. Why This File Exists
- `FACT`: The file exists to normalize pending text and detect ack/close/wait lexicon intents from system lexicons: `truffles-api/app/routers/webhook/pending.py:25`, `truffles-api/app/routers/webhook/pending.py:36`, `truffles-api/app/routers/webhook/pending.py:52`, `truffles-api/app/routers/webhook/pending.py:70`, `truffles-api/app/routers/webhook/pending.py:74`, `truffles-api/app/routers/webhook/pending.py:78`.
- `FACT`: It also exists to adapt shared continuity runtime logic from `state_service.py` into webhook transport gates through `HandoverConfirmationRuntimeHooks`, `PendingContinuityRuntimeHooks`, `_handle_handover_confirmation_runtime(...)`, `_handle_pending_sla_runtime(...)`, `_resolve_pending_ack(...)`, `_resolve_pending_close(...)`, and `_resolve_pending_no_handover_reset(...)`: `truffles-api/app/routers/webhook/pending.py:14`, `truffles-api/app/services/state_service.py:79`, `truffles-api/app/services/state_service.py:89`, `truffles-api/app/services/state_service.py:1136`, `truffles-api/app/services/state_service.py:1172`, `truffles-api/app/services/state_service.py:1208`, `truffles-api/app/services/state_service.py:1320`, `truffles-api/app/services/state_service.py:1512`.
- `FACT`: The file further exists to own pending-state transport behavior on live webhook paths: handover confirmation routing, manager-active silent forwarding, pending replies, pending SLA pings/escalations, and Telegram forwarding for pending media/transcripts: `truffles-api/app/routers/webhook/pending.py:107`, `truffles-api/app/routers/webhook/pending.py:160`, `truffles-api/app/routers/webhook/pending.py:281`, `truffles-api/app/routers/webhook/pending.py:313`, `truffles-api/app/routers/webhook/pending.py:502`.
- `INFERENCE`: The real reason this file exists is that pending/handover continuity transport was partially extracted from the old router into a dedicated hotspot, not into a thin extracted runtime boundary.

## 3. Active Callers And Entrypoints
- `FACT`: The visible non-test direct importer is `decision.py`: `truffles-api/app/routers/webhook/decision.py:224`.
- `FACT`: The main live entrypoints owned by the file are `_handle_handover_confirmation_gate(...)`, `_forward_pending_to_telegram(...)`, `_handle_manager_active_gate(...)`, and `_handle_pending_gate(...)`: `truffles-api/app/routers/webhook/pending.py:107`, `truffles-api/app/routers/webhook/pending.py:160`, `truffles-api/app/routers/webhook/pending.py:281`, `truffles-api/app/routers/webhook/pending.py:313`.
- `INFERENCE`: `pending.py` remains a live routing surface because the legacy decision stack still delegates pending/manager-active transport behavior into this file rather than collapsing it into a narrower runtime boundary.

## 4. Control Path Owned By This File
- `FACT`: `_handle_handover_confirmation_gate(...)` wires the BOT_ACTIVE handover-confirmation path from `context_manager` state plus `ai_service.classify_confirmation(...)` into shared runtime hooks that can reuse handovers, escalate to pending, send Telegram notifications, and record traces: `truffles-api/app/routers/webhook/pending.py:107`, `truffles-api/app/routers/webhook/pending.py:118`, `truffles-api/app/routers/webhook/pending.py:128`, `truffles-api/app/routers/webhook/pending.py:134`, `truffles-api/app/routers/webhook/pending.py:142`, `truffles-api/app/routers/webhook/pending.py:146`.
- `FACT`: `_forward_pending_to_telegram(...)` owns pending/manager-active forwarding of plain messages, media, and voice-note transcripts into Telegram topics and mutates forwarding metadata on saved messages: `truffles-api/app/routers/webhook/pending.py:160`, `truffles-api/app/routers/webhook/pending.py:183`, `truffles-api/app/routers/webhook/pending.py:203`, `truffles-api/app/routers/webhook/pending.py:218`, `truffles-api/app/routers/webhook/pending.py:248`, `truffles-api/app/routers/webhook/pending.py:265`.
- `FACT`: `_handle_manager_active_gate(...)` owns the manager-active silent-forward branch and writes routing trace/meta for that decision: `truffles-api/app/routers/webhook/pending.py:281`, `truffles-api/app/routers/webhook/pending.py:292`, `truffles-api/app/routers/webhook/pending.py:299`, `truffles-api/app/routers/webhook/pending.py:303`.
- `FACT`: `_handle_pending_gate(...)` owns no-handover reset, opt-out cancellation, pending close/ack, pending status/wait replies, guard-only skips, SLA ping/escalation transport, and soft-pass behavior while the conversation is `PENDING`: `truffles-api/app/routers/webhook/pending.py:313`, `truffles-api/app/routers/webhook/pending.py:334`, `truffles-api/app/routers/webhook/pending.py:348`, `truffles-api/app/routers/webhook/pending.py:383`, `truffles-api/app/routers/webhook/pending.py:397`, `truffles-api/app/routers/webhook/pending.py:411`, `truffles-api/app/routers/webhook/pending.py:459`, `truffles-api/app/routers/webhook/pending.py:502`, `truffles-api/app/routers/webhook/pending.py:525`.
- `INFERENCE`: This file owns the pending/manager-active reply/escalation/transport side of the legacy decision stack, not just SLA logic.

## 5. Data Reads
- `FACT`: The file reads system lexicons for pending acknowledgements, closes, and wait intents through `get_system_lexicon_list(...)`: `truffles-api/app/routers/webhook/pending.py:11`, `truffles-api/app/routers/webhook/pending.py:25`, `truffles-api/app/routers/webhook/pending.py:36`, `truffles-api/app/routers/webhook/pending.py:52`.
- `FACT`: It reads shared continuity runtime hooks and SLA helpers from `state_service.py` and `sla_runtime_service.py`: `truffles-api/app/routers/webhook/pending.py:13`, `truffles-api/app/routers/webhook/pending.py:14`, `truffles-api/app/routers/webhook/pending.py:19`, `truffles-api/app/services/state_service.py:1136`, `truffles-api/app/services/state_service.py:1172`, `truffles-api/app/services/state_service.py:1208`, `truffles-api/app/services/state_service.py:1320`, `truffles-api/app/services/state_service.py:1512`.
- `FACT`: The file repeatedly reads `_legacy.py` helpers for normalization, router observability, active handovers, opt-out/status questions, Telegram credentials, Telegram/media helpers, prompt constants, and trace/meta writers: `truffles-api/app/routers/webhook/pending.py:25`, `truffles-api/app/routers/webhook/pending.py:153`, `truffles-api/app/routers/webhook/pending.py:183`, `truffles-api/app/routers/webhook/pending.py:292`, `truffles-api/app/routers/webhook/pending.py:329`, `truffles-api/app/routers/webhook/pending.py:350`, `truffles-api/app/routers/webhook/pending.py:411`.
- `FACT`: `_handle_handover_confirmation_gate(...)` also reads `context_manager` accessors for handover confirmation state and `ai_service.classify_confirmation(...)`: `truffles-api/app/routers/webhook/pending.py:118`, `truffles-api/app/routers/webhook/pending.py:126`.
- `INFERENCE`: `pending.py` reads from too many layers to be considered a narrow transport boundary; it spans lexicons, `_legacy` continuity helpers, `context_manager` state, Telegram/media transport, and shared state-service runtime hooks.

## 6. Data Writes And Side Effects
- `FACT`: The file writes decision traces for manager-active silence, pending opt-out, pending status/wait, pending guard soft-pass, and all hook-driven pending/confirmation branches via `record_decision_trace`: `truffles-api/app/routers/webhook/pending.py:146`, `truffles-api/app/routers/webhook/pending.py:303`, `truffles-api/app/routers/webhook/pending.py:365`, `truffles-api/app/routers/webhook/pending.py:424`, `truffles-api/app/routers/webhook/pending.py:441`, `truffles-api/app/routers/webhook/pending.py:467`, `truffles-api/app/routers/webhook/pending.py:484`, `truffles-api/app/routers/webhook/pending.py:532`.
- `FACT`: It writes message decision metadata such as `pending_action`, `pending_guard_only`, `pending_guard`, and forwarding flags on saved messages / metadata: `truffles-api/app/routers/webhook/pending.py:218`, `truffles-api/app/routers/webhook/pending.py:227`, `truffles-api/app/routers/webhook/pending.py:265`, `truffles-api/app/routers/webhook/pending.py:429`, `truffles-api/app/routers/webhook/pending.py:446`, `truffles-api/app/routers/webhook/pending.py:472`, `truffles-api/app/routers/webhook/pending.py:489`, `truffles-api/app/routers/webhook/pending.py:537`.
- `FACT`: Through shared state-service runtime hooks, the file also writes pending continuity state such as `pending_resume`, `pending_sla`, handover confirmation clear/reset, conversation-state transitions, `bot_status`, and manager resolution: `truffles-api/app/routers/webhook/pending.py:334`, `truffles-api/app/services/state_service.py:1144`, `truffles-api/app/services/state_service.py:1187`, `truffles-api/app/services/state_service.py:1238`, `truffles-api/app/services/state_service.py:1377`, `truffles-api/app/services/state_service.py:1461`, `truffles-api/app/services/state_service.py:1530`, `truffles-api/app/services/state_service.py:1536`.
- `FACT`: `_forward_pending_to_telegram(...)` performs direct Telegram side effects and transcript/media forwarding while mutating forwarding metadata: `truffles-api/app/routers/webhook/pending.py:183`, `truffles-api/app/routers/webhook/pending.py:203`, `truffles-api/app/routers/webhook/pending.py:214`, `truffles-api/app/routers/webhook/pending.py:218`, `truffles-api/app/routers/webhook/pending.py:261`, `truffles-api/app/routers/webhook/pending.py:265`.
- `INFERENCE`: `pending.py` is not a pure reader; it mutates continuity state, message evidence, and external transport side effects.

## 7. Semantic Authority / Deterministic Authority
- `FACT`: `_normalize_pending_text(...)`, `_matches_lexicon_any(...)`, `_matches_lexicon_all_tokens(...)`, `_is_pending_ack(...)`, `_is_pending_close(...)`, and `_is_pending_wait(...)` deterministically classify pending-intent meaning from lexicons and normalized text: `truffles-api/app/routers/webhook/pending.py:25`, `truffles-api/app/routers/webhook/pending.py:36`, `truffles-api/app/routers/webhook/pending.py:52`, `truffles-api/app/routers/webhook/pending.py:70`, `truffles-api/app/routers/webhook/pending.py:74`, `truffles-api/app/routers/webhook/pending.py:78`.
- `FACT`: `_handle_handover_confirmation_gate(...)` deterministically converts `handover_confirmation` context into confirm/decline/escalate transport outcomes by wiring `context_manager`, `classify_confirmation`, and `_legacy` handover hooks: `truffles-api/app/routers/webhook/pending.py:107`, `truffles-api/app/routers/webhook/pending.py:118`, `truffles-api/app/routers/webhook/pending.py:134`, `truffles-api/app/routers/webhook/pending.py:142`, `truffles-api/app/routers/webhook/pending.py:146`.
- `FACT`: `_handle_pending_gate(...)` deterministically rewrites pending behavior into no-handover reset, opt-out cancellation, pending close/ack, status/wait replies, SLA collect-only/ping/escalation, and soft-pass routing while the conversation is pending: `truffles-api/app/routers/webhook/pending.py:348`, `truffles-api/app/routers/webhook/pending.py:365`, `truffles-api/app/routers/webhook/pending.py:383`, `truffles-api/app/routers/webhook/pending.py:397`, `truffles-api/app/routers/webhook/pending.py:411`, `truffles-api/app/routers/webhook/pending.py:459`, `truffles-api/app/routers/webhook/pending.py:502`, `truffles-api/app/routers/webhook/pending.py:525`.
- `FACT`: `_handle_manager_active_gate(...)` keeps another deterministic authority seam alive by converting `MANAGER_ACTIVE` into a silent-forward routing decision with no owner/runtime semantic call: `truffles-api/app/routers/webhook/pending.py:281`, `truffles-api/app/routers/webhook/pending.py:299`, `truffles-api/app/routers/webhook/pending.py:303`.
- `INFERENCE`: `pending.py` is not a target-state semantic owner, but it is a deterministic pending/handover authority seam that still shapes user-visible routing, continuity, and escalation outside the extracted runtime path.

## 8. Truth Carriers Touched Here
- `FACT`: The file reads and writes pending continuity carriers such as `handover_confirmation`, `pending_resume`, and `pending_sla` through `context_manager` and shared state-service runtime hooks: `truffles-api/app/routers/webhook/pending.py:118`, `truffles-api/app/routers/webhook/pending.py:334`, `truffles-api/app/services/state_service.py:116`, `truffles-api/app/services/state_service.py:119`, `truffles-api/app/services/state_service.py:120`, `truffles-api/app/services/state_service.py:1238`, `truffles-api/app/services/state_service.py:1377`, `truffles-api/app/services/state_service.py:1461`, `truffles-api/app/services/state_service.py:1530`.
- `FACT`: It also writes pending-side message evidence carriers such as `pending_action`, `pending_guard`, `pending_guard_only`, `pending_resume_restored`, `forwarded_to_telegram`, and `transcript_forwarded`: `truffles-api/app/routers/webhook/pending.py:218`, `truffles-api/app/routers/webhook/pending.py:227`, `truffles-api/app/routers/webhook/pending.py:265`, `truffles-api/app/routers/webhook/pending.py:429`, `truffles-api/app/routers/webhook/pending.py:472`, `truffles-api/app/routers/webhook/pending.py:489`, `truffles-api/app/services/state_service.py:1284`, `truffles-api/app/services/state_service.py:1488`.
- `FACT`: The file carries router observability state through `router_pending_meta` and pending-intent fields that are then written into traces and message metadata: `truffles-api/app/routers/webhook/pending.py:292`, `truffles-api/app/routers/webhook/pending.py:329`, `truffles-api/app/routers/webhook/pending.py:419`, `truffles-api/app/routers/webhook/pending.py:479`, `truffles-api/app/routers/webhook/pending.py:525`.
- `INFERENCE`: `pending.py` is where pending/handover carriers become user-visible pending replies, state transitions, and Telegram-forwarding outcomes, so it remains part of the multi-carrier continuity problem.

## 9. Violations Against The Target Canon
- `FACT`: The file still mixes lexicon-based pending intent detection, continuity runtime hook adaptation, manager-active routing, handover confirmation logic, pending SLA transport, and Telegram forwarding inside one module: `truffles-api/app/routers/webhook/pending.py:25`, `truffles-api/app/routers/webhook/pending.py:107`, `truffles-api/app/routers/webhook/pending.py:160`, `truffles-api/app/routers/webhook/pending.py:281`, `truffles-api/app/routers/webhook/pending.py:313`.
- `FACT`: It still depends broadly on `_legacy.py` for normalization, router observability, handover hooks, prompt constants, and Telegram/media transport helpers: `truffles-api/app/routers/webhook/pending.py:25`, `truffles-api/app/routers/webhook/pending.py:142`, `truffles-api/app/routers/webhook/pending.py:153`, `truffles-api/app/routers/webhook/pending.py:183`, `truffles-api/app/routers/webhook/pending.py:292`, `truffles-api/app/routers/webhook/pending.py:329`.
- `FACT`: It still writes and reacts to legacy/compatibility continuity surfaces such as `handover_confirmation`, `pending_resume`, `pending_sla`, and router/message sidecar metadata rather than a single canonical runtime boundary: `truffles-api/app/routers/webhook/pending.py:118`, `truffles-api/app/routers/webhook/pending.py:334`, `truffles-api/app/services/state_service.py:1144`, `truffles-api/app/services/state_service.py:1238`, `truffles-api/app/services/state_service.py:1377`, `truffles-api/app/services/state_service.py:1461`.
- `FACT`: The file still owns direct Telegram forwarding and user-visible pending transport side effects instead of a thin projection/execution boundary: `truffles-api/app/routers/webhook/pending.py:183`, `truffles-api/app/routers/webhook/pending.py:203`, `truffles-api/app/routers/webhook/pending.py:214`, `truffles-api/app/routers/webhook/pending.py:261`.
- `INFERENCE`: Strategic point `one control path` remains open here because pending/manager-active behavior still runs through a separate mixed legacy transport family rather than a thin extracted runtime boundary.
- `INFERENCE`: Strategic point `deterministic only at boundaries` remains open here because pending continuity, escalation, and routing are still being rewritten inside the pending domain module.

## 10. Salvageable Parts
- `FACT`: The bounded lexicon and transport helpers are salvageable if relocated behind a cleaner pending boundary: `_normalize_pending_text(...)`, `_matches_lexicon_any(...)`, `_matches_lexicon_all_tokens(...)`, `_is_pending_ack(...)`, `_is_pending_close(...)`, `_is_pending_wait(...)`, and `_build_transport_webhook_response(...)`: `truffles-api/app/routers/webhook/pending.py:25`, `truffles-api/app/routers/webhook/pending.py:36`, `truffles-api/app/routers/webhook/pending.py:52`, `truffles-api/app/routers/webhook/pending.py:70`, `truffles-api/app/routers/webhook/pending.py:74`, `truffles-api/app/routers/webhook/pending.py:78`, `truffles-api/app/routers/webhook/pending.py:82`.
- `FACT`: `_forward_pending_to_telegram(...)` is also salvageable as a bounded pending transport helper if stripped from `_legacy`-driven routing/continuity logic: `truffles-api/app/routers/webhook/pending.py:160`.
- `INFERENCE`: The salvageable core is the bounded transport/lexicon helper layer, not the pending confirmation/SLA/escalation orchestration that currently surrounds it.

## 11. Demotion / Removal Candidates
- `FACT`: `_handle_handover_confirmation_gate(...)`, `_handle_manager_active_gate(...)`, and `_handle_pending_gate(...)` are the main demotion candidates because they keep pending continuity, routing, handover policy, and soft-pass/escalation behavior inside a legacy transport hotspot: `truffles-api/app/routers/webhook/pending.py:107`, `truffles-api/app/routers/webhook/pending.py:281`, `truffles-api/app/routers/webhook/pending.py:313`.
- `FACT`: The repeated `_legacy`-backed router observability, handover reuse, prompt constants, and Telegram/media transport branches inside those handlers are removal blockers for any clean pending boundary: `truffles-api/app/routers/webhook/pending.py:142`, `truffles-api/app/routers/webhook/pending.py:183`, `truffles-api/app/routers/webhook/pending.py:292`, `truffles-api/app/routers/webhook/pending.py:329`.
- `INFERENCE`: Real demotion requires separating bounded transport helpers from pending routing, handover confirmation, SLA transport, and Telegram forwarding policy.

## 12. What This Analysis Changes In System Understanding
- `FACT`: `pending.py` is not just a pending SLA helper; it is the live pending/manager-active continuity transport subsystem of the legacy webhook stack.
- `FACT`: The file is a live composition point where `_legacy.py` compatibility helpers, `context_manager` handover confirmation state, shared `state_service` pending runtime hooks, and Telegram transport become pending replies, state resets/restores, and manager-active/pending routing outcomes.
- `FACT`: The file still contains active pending-side deterministic authority and direct transport side effects outside the extracted runtime path.
- `INFERENCE`: After this hotspot, the next honest hotspot is `truffles-api/app/routers/webhook/policy.py`, because it is the next large live routing gate that still feeds `decision.py`, `info.py`, and `booking.py` with hard-law / truth-gate / service-matcher / booking-flow policy decisions.

## 13. Open Questions
- `UNKNOWN`: Which parts of `_handle_pending_gate(...)` should survive as bounded continuity transport versus disappear with the legacy pending routing stack.
- `UNKNOWN`: Which parts of `_forward_pending_to_telegram(...)` are product-essential transport helpers versus legacy router-coupled behavior.
- `UNKNOWN`: How much of the current pending/handover confirmation policy should remain after canonical runtime-state and pure-boundary extraction.

# File Analysis: `truffles-api/app/routers/webhook/policy.py`

Status: `completed`
Baseline snapshot: `8319d9e1`
Related ledgers:
- `docs/system_forensics/ledgers/CONTROL_PATHS.md`
- `docs/system_forensics/ledgers/SEMANTIC_OWNER_MAP.md`
- `docs/system_forensics/ledgers/TRUTH_CARRIER_MATRIX.md`
- `docs/system_forensics/ledgers/STATE_SURFACE_INVENTORY.md`
- `docs/system_forensics/ledgers/DETERMINISTIC_REWRITE_LEDGER.md`
- `docs/system_forensics/ledgers/CUTOVER_DEPENDENCY_GRAPH.md`
- `docs/system_forensics/ledgers/SALVAGEABLE_COMPONENTS.md`
- `docs/system_forensics/ledgers/DO_NOT_REPEAT.md`
- `docs/system_forensics/final/SYSTEM_FINAL_ANALYSIS.md`

## 1. Role Summary
- `FACT`: Although the file docstring says `LAW/policy gates and policy-pack-driven detection helpers.`, the file actually contains a broader mixed policy subsystem: routing-policy helpers, runtime/registry policy override resolution, hard-law/policy keyword detection, pack escalation and price sidecars, promotion detection, handler payload assembly, and direct policy escalation/reply side effects: `truffles-api/app/routers/webhook/policy.py:1`, `truffles-api/app/routers/webhook/policy.py:105`, `truffles-api/app/routers/webhook/policy.py:142`, `truffles-api/app/routers/webhook/policy.py:236`, `truffles-api/app/routers/webhook/policy.py:328`, `truffles-api/app/routers/webhook/policy.py:729`, `truffles-api/app/routers/webhook/policy.py:770`, `truffles-api/app/routers/webhook/policy.py:786`, `truffles-api/app/routers/webhook/policy.py:925`, `truffles-api/app/routers/webhook/policy.py:995`.
- `FACT`: The visible live consumers are spread across the legacy webhook stack rather than one narrow caller: `decision.py` imports a broad policy helper surface, `response.py` uses policy handler and LLM-guard helpers via `_legacy`, `info.py` and `booking.py` import the promotions detector, and `reasoning_core.py` still uses `decision_router._looks_like_policy_topic(...)`: `truffles-api/app/routers/webhook/decision.py:231`, `truffles-api/app/routers/webhook/decision.py:1333`, `truffles-api/app/routers/webhook/decision.py:4804`, `truffles-api/app/routers/webhook/decision.py:8038`, `truffles-api/app/routers/webhook/response.py:1267`, `truffles-api/app/routers/webhook/response.py:2194`, `truffles-api/app/routers/webhook/info.py:644`, `truffles-api/app/routers/webhook/booking.py:1831`, `truffles-api/app/services/reasoning_core.py:425`.
- `INFERENCE`: `policy.py` is not a single active gate; it is the live policy helper warehouse plus partially dormant gate residue that still shapes routing, escalation, reply blocking, and policy-pack semantics across the legacy webhook stack.

## 2. Why This File Exists
- `FACT`: The file exists to translate conversation state into routing allowances through `_get_routing_policy(...)`, `_should_run_booking_flow(...)`, `_should_run_truth_gate(...)`, `_should_run_demo_truth_gate(...)`, and `_should_escalate_to_pending(...)`: `truffles-api/app/routers/webhook/policy.py:105`, `truffles-api/app/routers/webhook/policy.py:119`, `truffles-api/app/routers/webhook/policy.py:128`, `truffles-api/app/routers/webhook/policy.py:132`, `truffles-api/app/routers/webhook/policy.py:136`.
- `FACT`: It also exists to load and merge policy-pack data from client config, pack defaults, runtime capability overrides, and registry overrides through `_extract_policy_pack_from_config(...)`, `_resolve_runtime_policy_overrides(...)`, `_resolve_registry_policy_overrides(...)`, `_apply_policy_overrides(...)`, `_apply_registry_policy_overrides(...)`, `_apply_runtime_policy_overrides(...)`, and `_get_policy_pack(...)`: `truffles-api/app/routers/webhook/policy.py:88`, `truffles-api/app/routers/webhook/policy.py:171`, `truffles-api/app/routers/webhook/policy.py:179`, `truffles-api/app/routers/webhook/policy.py:193`, `truffles-api/app/routers/webhook/policy.py:218`, `truffles-api/app/routers/webhook/policy.py:229`, `truffles-api/app/routers/webhook/policy.py:236`.
- `FACT`: The file further exists to detect and act on policy topics from user text through `_detect_hard_law_match(...)`, `_detect_policy_gate_section(...)`, `_detect_booking_cancel(...)`, `_looks_like_policy_topic(...)`, `_looks_like_promotions_request(...)`, `_detect_llm_guard_topics(...)`, and the reply/escalation helpers `_apply_policy_decision(...)`, `_handle_hard_law_gate(...)`, and `_handle_policy_escalation_gate(...)`: `truffles-api/app/routers/webhook/policy.py:386`, `truffles-api/app/routers/webhook/policy.py:472`, `truffles-api/app/routers/webhook/policy.py:497`, `truffles-api/app/routers/webhook/policy.py:559`, `truffles-api/app/routers/webhook/policy.py:581`, `truffles-api/app/routers/webhook/policy.py:786`, `truffles-api/app/routers/webhook/policy.py:925`, `truffles-api/app/routers/webhook/policy.py:995`.
- `INFERENCE`: The file exists because policy semantics were split out of `decision.py` as a helper-rich module, but not reduced to a thin boundary or to a fully data-driven policy registry surface.

## 3. Active Callers And Entrypoints
- `FACT`: `decision.py` imports the broadest live policy helper surface, including `_detect_booking_cancel(...)`, `_detect_llm_guard_topics(...)`, `_format_discounts_policy_reply(...)`, `_get_policy_handler(...)`, `_get_policy_pack(...)`, `_get_policy_type(...)`, `_get_routing_policy(...)`, `_handle_hard_law_gate(...)`, `_handle_policy_escalation_gate(...)`, `_has_discount_policy_rules(...)`, `_looks_like_policy_topic(...)`, `_pack_escalation_gate(...)`, and `_pack_price_sidecar(...)`: `truffles-api/app/routers/webhook/decision.py:231`, `truffles-api/app/routers/webhook/decision.py:244`.
- `FACT`: Visible non-test live callsites include `decision.py` using `_looks_like_policy_topic(...)`, `_detect_booking_cancel(...)`, `_format_discounts_policy_reply(...)`, `_pack_escalation_gate(...)`, and `_pack_price_sidecar(...)`: `truffles-api/app/routers/webhook/decision.py:1333`, `truffles-api/app/routers/webhook/decision.py:4804`, `truffles-api/app/routers/webhook/decision.py:5325`, `truffles-api/app/routers/webhook/decision.py:8038`, `truffles-api/app/routers/webhook/decision.py:8042`.
- `FACT`: `response.py` uses `_get_policy_handler(...)` and `_detect_llm_guard_topics(...)` via `_legacy` on live response paths: `truffles-api/app/routers/webhook/response.py:1267`, `truffles-api/app/routers/webhook/response.py:2194`.
- `FACT`: `info.py` and `booking.py` use `_looks_like_promotions_request(...)` directly on live info and booking-interrupt paths: `truffles-api/app/routers/webhook/info.py:644`, `truffles-api/app/routers/webhook/booking.py:1831`.
- `FACT`: `reasoning_core.py` still depends on `decision_router._looks_like_policy_topic(...)` to suppress expected-reply fallback on policy-like turns: `truffles-api/app/services/reasoning_core.py:425`.
- `FACT`: Tests import the module directly and pin policy runtime behavior: `truffles-api/tests/test_message_endpoint.py:39`, `truffles-api/tests/test_policy_handler_runtime.py:7`.
- `UNKNOWN`: Whether `_handle_hard_law_gate(...)` and `_handle_policy_escalation_gate(...)` still have any live callers beyond their import residency in `decision.py`, because no visible non-definition callsites were found during this pass.

## 4. Control Path Owned By This File
- `FACT`: `_get_routing_policy(...)` reads `_legacy.ROUTING_MATRIX` and manufactures the routing allowance dict that downstream webhook code uses for booking/truth/handover decisions: `truffles-api/app/routers/webhook/policy.py:105`, `truffles-api/app/routers/webhook/policy.py:119`, `truffles-api/app/routers/webhook/policy.py:136`, `truffles-api/app/routers/webhook/decision.py:716`, `truffles-api/app/routers/webhook/decision.py:743`, `truffles-api/app/routers/webhook/decision.py:2719`.
- `FACT`: `_get_policy_handler(...)` and `_get_policy_pack(...)` assemble the policy handler payload consumed on live response and decision-era policy-helper paths by combining pack config, handler defaults, registry overrides, and runtime overrides: `truffles-api/app/routers/webhook/policy.py:236`, `truffles-api/app/routers/webhook/policy.py:770`, `truffles-api/app/routers/webhook/response.py:1267`, `truffles-api/app/routers/webhook/decision.py:8038`, `truffles-api/app/routers/webhook/decision.py:8042`.
- `FACT`: `_detect_booking_cancel(...)`, `_looks_like_policy_topic(...)`, `_detect_llm_guard_topics(...)`, `_looks_like_promotions_request(...)`, `_pack_escalation_gate(...)`, and `_pack_price_sidecar(...)` are the visible live helper entrypoints that feed decision, response, info, booking, and reasoning-core routing: `truffles-api/app/routers/webhook/policy.py:497`, `truffles-api/app/routers/webhook/policy.py:559`, `truffles-api/app/routers/webhook/policy.py:581`, `truffles-api/app/routers/webhook/policy.py:608`, `truffles-api/app/routers/webhook/policy.py:729`, `truffles-api/app/routers/webhook/policy.py:742`, `truffles-api/app/routers/webhook/decision.py:1333`, `truffles-api/app/routers/webhook/decision.py:4804`, `truffles-api/app/routers/webhook/decision.py:5325`, `truffles-api/app/routers/webhook/response.py:2194`, `truffles-api/app/routers/webhook/info.py:644`, `truffles-api/app/routers/webhook/booking.py:1831`, `truffles-api/app/services/reasoning_core.py:425`.
- `FACT`: `_apply_policy_decision(...)`, `_handle_hard_law_gate(...)`, and `_handle_policy_escalation_gate(...)` still contain a full reply/escalation transport path with handover reuse, pending escalation, trace/meta writes, send-and-save, timing metrics, and `db.commit()`: `truffles-api/app/routers/webhook/policy.py:786`, `truffles-api/app/routers/webhook/policy.py:823`, `truffles-api/app/routers/webhook/policy.py:842`, `truffles-api/app/routers/webhook/policy.py:886`, `truffles-api/app/routers/webhook/policy.py:903`, `truffles-api/app/routers/webhook/policy.py:916`, `truffles-api/app/routers/webhook/policy.py:925`, `truffles-api/app/routers/webhook/policy.py:995`.
- `INFERENCE`: The file owns a split control path: live helper surfaces are actively consumed, while the direct reply/escalation handlers look like residual gate bodies that still keep old transport authority resident in the module.

## 5. Data Reads
- `FACT`: The file reads pack policy data and price/escalation signals from `pack_runtime_service`: `load_policy_pack(...)`, `get_pack_decision(...)`, `get_pack_price_item(...)`, and `get_pack_price_reply(...)`: `truffles-api/app/routers/webhook/policy.py:14`, `truffles-api/app/routers/webhook/policy.py:142`, `truffles-api/app/routers/webhook/policy.py:729`, `truffles-api/app/routers/webhook/policy.py:742`.
- `FACT`: It reads runtime capability overrides and registry overrides through `get_runtime_capabilities(...)` and `resolve_effective_policy_overrides(...)`: `truffles-api/app/routers/webhook/policy.py:11`, `truffles-api/app/routers/webhook/policy.py:22`, `truffles-api/app/routers/webhook/policy.py:171`, `truffles-api/app/routers/webhook/policy.py:179`.
- `FACT`: The file repeatedly reads `_legacy.py` helpers and constants for normalized text, routing matrix, keyword matching, hygiene suppression, observability, active handover reuse, pending escalation, and transport helpers: `truffles-api/app/routers/webhook/policy.py:106`, `truffles-api/app/routers/webhook/policy.py:137`, `truffles-api/app/routers/webhook/policy.py:393`, `truffles-api/app/routers/webhook/policy.py:499`, `truffles-api/app/routers/webhook/policy.py:583`, `truffles-api/app/routers/webhook/policy.py:730`, `truffles-api/app/routers/webhook/policy.py:771`, `truffles-api/app/routers/webhook/policy.py:812`, `truffles-api/app/routers/webhook/policy.py:947`, `truffles-api/app/routers/webhook/policy.py:1019`.
- `FACT`: `_apply_policy_decision(...)` and the direct gate handlers read live conversation, user, saved-message, routing, and client-slug inputs in order to build reply/escalation behavior and write transport evidence: `truffles-api/app/routers/webhook/policy.py:788`, `truffles-api/app/routers/webhook/policy.py:925`, `truffles-api/app/routers/webhook/policy.py:995`.
- `INFERENCE`: `policy.py` reads from too many layers to be a thin pack-policy boundary; it spans pack policy data, runtime override registries, `_legacy` routing helpers, and direct transport-side conversation/message inputs.

## 6. Data Writes And Side Effects
- `FACT`: `_apply_policy_decision(...)` writes decision traces and message decision metadata for policy replies/escalations through `_record_decision_trace(...)`, `_record_message_decision_meta(...)`, and `_update_message_decision_metadata(...)`: `truffles-api/app/routers/webhook/policy.py:886`, `truffles-api/app/routers/webhook/policy.py:887`, `truffles-api/app/routers/webhook/policy.py:895`, `truffles-api/app/routers/webhook/policy.py:902`.
- `FACT`: The same function also mutates low-confidence retry state, can reuse an active handover, can escalate to pending, can send Telegram notifications, calls `send_and_save(...)`, records timing, and commits the database transaction: `truffles-api/app/routers/webhook/policy.py:812`, `truffles-api/app/routers/webhook/policy.py:823`, `truffles-api/app/routers/webhook/policy.py:842`, `truffles-api/app/routers/webhook/policy.py:903`, `truffles-api/app/routers/webhook/policy.py:908`, `truffles-api/app/routers/webhook/policy.py:916`.
- `FACT`: `_handle_policy_escalation_gate(...)` also writes guard-only policy metadata and complaint-suppression evidence without sending a reply when `guard_only=True`: `truffles-api/app/routers/webhook/policy.py:1075`, `truffles-api/app/routers/webhook/policy.py:1098`, `truffles-api/app/routers/webhook/policy.py:1102`, `truffles-api/app/routers/webhook/policy.py:1127`, `truffles-api/app/routers/webhook/policy.py:1134`.
- `INFERENCE`: `policy.py` is not a pure detector module; it still contains direct state mutation, trace/meta mutation, handover/escalation, reply sending, and transaction commit behavior.

## 7. Semantic Authority / Deterministic Authority
- `FACT`: The file deterministically maps normalized user text into policy sections and risk/intents through `_matches_policy_keywords(...)`, `_matches_policy_section(...)`, `_resolve_policy_intent(...)`, `_resolve_policy_risk_level(...)`, `_resolve_hard_law_sections(...)`, `_detect_policy_section(...)`, and `_detect_hard_law_match(...)`: `truffles-api/app/routers/webhook/policy.py:283`, `truffles-api/app/routers/webhook/policy.py:302`, `truffles-api/app/routers/webhook/policy.py:311`, `truffles-api/app/routers/webhook/policy.py:319`, `truffles-api/app/routers/webhook/policy.py:328`, `truffles-api/app/routers/webhook/policy.py:361`, `truffles-api/app/routers/webhook/policy.py:386`.
- `FACT`: It also deterministically shapes policy behavior through `_detect_policy_gate_section(...)`, `_detect_booking_cancel(...)`, `_looks_like_policy_topic(...)`, `_detect_llm_guard_topics(...)`, and `_looks_like_promotions_request(...)`: `truffles-api/app/routers/webhook/policy.py:472`, `truffles-api/app/routers/webhook/policy.py:497`, `truffles-api/app/routers/webhook/policy.py:559`, `truffles-api/app/routers/webhook/policy.py:581`, `truffles-api/app/routers/webhook/policy.py:608`.
- `FACT`: `_apply_policy_decision(...)` deterministically rewrites policy matches into final reply/escalation outcomes, including policy gate labeling, handover reuse, pending escalation, router observability, and message metadata: `truffles-api/app/routers/webhook/policy.py:786`, `truffles-api/app/routers/webhook/policy.py:817`, `truffles-api/app/routers/webhook/policy.py:864`, `truffles-api/app/routers/webhook/policy.py:871`, `truffles-api/app/routers/webhook/policy.py:895`.
- `INFERENCE`: `policy.py` is not a target-state semantic owner, but it is a major deterministic policy authority seam that still classifies user text and manufactures routing/escalation outcomes outside the extracted runtime path.

## 8. Truth Carriers Touched Here
- `FACT`: The file manufactures and carries policy handler payloads such as `policy_type`, `policy_pack`, `service_matcher`, `truth_gate`, `price_item`, and `price_sidecar` through `_get_policy_handler(...)` and the decision-era `_DEFAULT_POLICY_HANDLER`: `truffles-api/app/routers/webhook/policy.py:770`, `truffles-api/app/routers/webhook/decision.py:8037`, `truffles-api/app/routers/webhook/decision.py:8042`.
- `FACT`: It also manufactures policy-side message metadata and trace carriers such as `policy_gate`, `policy_section`, `risk_level`, `policy_pack_missing`, `policy_guard_only`, `complaint_signal`, `consult_override`, and `source`: `truffles-api/app/routers/webhook/policy.py:871`, `truffles-api/app/routers/webhook/policy.py:895`, `truffles-api/app/routers/webhook/policy.py:1078`, `truffles-api/app/routers/webhook/policy.py:1102`, `truffles-api/app/routers/webhook/policy.py:1127`.
- `FACT`: The file also carries routing truth through `_get_routing_policy(...)` and booking/truth/handover decisions derived from that policy dict: `truffles-api/app/routers/webhook/policy.py:105`, `truffles-api/app/routers/webhook/policy.py:119`, `truffles-api/app/routers/webhook/policy.py:128`, `truffles-api/app/routers/webhook/policy.py:136`, `truffles-api/app/routers/webhook/decision.py:716`, `truffles-api/app/routers/webhook/decision.py:2719`.
- `INFERENCE`: `policy.py` is another place where compatibility carriers become final routing, escalation, and guard behavior, so policy truth still lives beyond canonical runtime state.

## 9. Violations Against The Target Canon
- `FACT`: The file still mixes policy-pack loading, runtime/registry override merging, regex/keyword topic detection, routing-matrix logic, promotion/discount helpers, and direct reply/escalation side effects inside one module: `truffles-api/app/routers/webhook/policy.py:105`, `truffles-api/app/routers/webhook/policy.py:171`, `truffles-api/app/routers/webhook/policy.py:283`, `truffles-api/app/routers/webhook/policy.py:608`, `truffles-api/app/routers/webhook/policy.py:786`, `truffles-api/app/routers/webhook/policy.py:925`, `truffles-api/app/routers/webhook/policy.py:995`.
- `FACT`: The file still depends heavily on `_legacy.py` for routing matrix, normalized-text helpers, handover reuse, pending escalation, router observability, and reply transport helpers: `truffles-api/app/routers/webhook/policy.py:106`, `truffles-api/app/routers/webhook/policy.py:137`, `truffles-api/app/routers/webhook/policy.py:393`, `truffles-api/app/routers/webhook/policy.py:730`, `truffles-api/app/routers/webhook/policy.py:771`, `truffles-api/app/routers/webhook/policy.py:812`.
- `FACT`: The file still hardcodes section enums, hard-law maps, default sections, guard-topic maps, and allowed override sections in code instead of making the policy model entirely manifest-driven: `truffles-api/app/routers/webhook/policy.py:24`, `truffles-api/app/routers/webhook/policy.py:34`, `truffles-api/app/routers/webhook/policy.py:42`, `truffles-api/app/routers/webhook/policy.py:52`, `truffles-api/app/routers/webhook/policy.py:60`, `truffles-api/app/routers/webhook/policy.py:69`.
- `FACT`: The direct policy gate handlers still own send/save, handover reuse, pending escalation, and `db.commit()` side effects instead of a thin projection/execution boundary: `truffles-api/app/routers/webhook/policy.py:823`, `truffles-api/app/routers/webhook/policy.py:842`, `truffles-api/app/routers/webhook/policy.py:903`, `truffles-api/app/routers/webhook/policy.py:916`.
- `INFERENCE`: Strategic point `deterministic only at boundaries` remains open here because policy text classification and policy reply/escalation routing still live in a mixed legacy helper module outside the extracted runtime owner path.
- `INFERENCE`: Strategic point `growth through manifests/data` remains open here because even though the file consumes policy packs and registry overrides, the core section model and routing semantics remain partly hardcoded in the code.

## 10. Salvageable Parts
- `FACT`: The pack/config/override resolution helpers are salvageable if relocated behind a cleaner policy boundary: `_extract_policy_pack_from_config(...)`, `_normalize_policy_override_payload(...)`, `_resolve_runtime_policy_overrides(...)`, `_resolve_registry_policy_overrides(...)`, `_apply_policy_overrides(...)`, `_apply_registry_policy_overrides(...)`, `_apply_runtime_policy_overrides(...)`, and `_get_policy_pack(...)`: `truffles-api/app/routers/webhook/policy.py:88`, `truffles-api/app/routers/webhook/policy.py:150`, `truffles-api/app/routers/webhook/policy.py:171`, `truffles-api/app/routers/webhook/policy.py:179`, `truffles-api/app/routers/webhook/policy.py:193`, `truffles-api/app/routers/webhook/policy.py:218`, `truffles-api/app/routers/webhook/policy.py:229`, `truffles-api/app/routers/webhook/policy.py:236`.
- `FACT`: The bounded policy-pack parsing helpers are salvageable: `_policy_str_list(...)`, `_get_policy_section(...)`, `_get_guard_topics(...)`, `_resolve_policy_intent(...)`, `_resolve_policy_risk_level(...)`, `_resolve_hard_law_sections(...)`, and `_load_discount_policy_payload(...)`: `truffles-api/app/routers/webhook/policy.py:258`, `truffles-api/app/routers/webhook/policy.py:264`, `truffles-api/app/routers/webhook/policy.py:271`, `truffles-api/app/routers/webhook/policy.py:311`, `truffles-api/app/routers/webhook/policy.py:319`, `truffles-api/app/routers/webhook/policy.py:328`, `truffles-api/app/routers/webhook/policy.py:641`.
- `FACT`: The bounded promotion/discount helper family is salvageable if kept data-driven: `_looks_like_promotions_request(...)`, `_has_discount_policy_rules(...)`, and `_format_discounts_policy_reply(...)`: `truffles-api/app/routers/webhook/policy.py:608`, `truffles-api/app/routers/webhook/policy.py:662`, `truffles-api/app/routers/webhook/policy.py:685`.
- `INFERENCE`: The salvageable core is the pack-driven policy-data resolution layer, not the mixed routing/escalation/transport authority that currently surrounds it.

## 11. Demotion / Removal Candidates
- `FACT`: `_apply_policy_decision(...)`, `_handle_hard_law_gate(...)`, and `_handle_policy_escalation_gate(...)` are the main demotion candidates because they keep reply/escalation transport authority, handover reuse, and message metadata writes inside the policy hotspot: `truffles-api/app/routers/webhook/policy.py:786`, `truffles-api/app/routers/webhook/policy.py:925`, `truffles-api/app/routers/webhook/policy.py:995`.
- `FACT`: The code-level section maps and routing helpers are also demotion targets if growth is to become fully manifest-driven: `_POLICY_SECTIONS`, `_HARD_LAW_INTENT_MAP`, `_SECTION_GUARD_TOPIC`, `_SECTION_DEFAULT_INTENT`, `_DEFAULT_HARD_LAW_SECTIONS`, and `_ALLOWED_OPERATIONAL_POLICY_OVERRIDE_SECTIONS`: `truffles-api/app/routers/webhook/policy.py:24`, `truffles-api/app/routers/webhook/policy.py:34`, `truffles-api/app/routers/webhook/policy.py:42`, `truffles-api/app/routers/webhook/policy.py:52`, `truffles-api/app/routers/webhook/policy.py:60`, `truffles-api/app/routers/webhook/policy.py:69`.
- `FACT`: The repeated `_legacy`-backed routing and transport dependencies inside live helpers and direct gate handlers are removal blockers for any clean policy boundary: `truffles-api/app/routers/webhook/policy.py:106`, `truffles-api/app/routers/webhook/policy.py:393`, `truffles-api/app/routers/webhook/policy.py:730`, `truffles-api/app/routers/webhook/policy.py:771`, `truffles-api/app/routers/webhook/policy.py:812`.
- `INFERENCE`: Real demotion requires separating pack-driven policy data helpers from policy topic classification, routing policy, and reply/escalation transport behavior.

## 12. What This Analysis Changes In System Understanding
- `FACT`: `policy.py` is not just a hard-law helper; it is the live policy helper warehouse and partially dormant gate subsystem of the legacy webhook stack.
- `FACT`: The file is a live composition point where pack policy data, runtime/registry overrides, routing-matrix rules, `_legacy` compatibility helpers, and direct reply/escalation transport are combined into routing allowances, policy-side blocks, and user-visible policy outcomes.
- `FACT`: The file still contains active deterministic policy authority and direct trace/meta side effects outside the extracted runtime path, even if some large direct gate handlers may now be low-visibility residue.
- `INFERENCE`: After this hotspot, the next honest hotspot is `truffles-api/app/routers/webhook/guards.py`, because it is the next live routing gate in the same compatibility family that still consumes `_legacy.py` and policy/routing signals to decide mute/human-lock/low-confidence and other guard behavior.

## 13. Open Questions
- `UNKNOWN`: Whether `_handle_hard_law_gate(...)` and `_handle_policy_escalation_gate(...)` are still reachable on any live path or now survive only as compatibility residue imported by `decision.py`.
- `UNKNOWN`: Which parts of the code-level policy section model should remain in code versus move entirely into manifests/registry data.
- `UNKNOWN`: Whether `routing_policy` should survive as a deterministic boundary primitive or be re-expressed through canonical runtime capabilities/state.
- `UNKNOWN`: Which parts of the promotions/discount helper family are product-essential versus temporary data-shaping convenience in the legacy stack.

# File Analysis: `truffles-api/app/routers/webhook/dedup.py`

Status: `completed`
Baseline snapshot: `8319d9e1`
Related ledgers:
- `docs/system_forensics/ledgers/CONTROL_PATHS.md`
- `docs/system_forensics/ledgers/SEMANTIC_OWNER_MAP.md`
- `docs/system_forensics/ledgers/TRUTH_CARRIER_MATRIX.md`
- `docs/system_forensics/ledgers/STATE_SURFACE_INVENTORY.md`
- `docs/system_forensics/ledgers/DETERMINISTIC_REWRITE_LEDGER.md`
- `docs/system_forensics/ledgers/CUTOVER_DEPENDENCY_GRAPH.md`
- `docs/system_forensics/ledgers/SALVAGEABLE_COMPONENTS.md`
- `docs/system_forensics/ledgers/DO_NOT_REPEAT.md`
- `docs/system_forensics/final/SYSTEM_FINAL_ANALYSIS.md`

## 1. Role Summary
- `FACT`: Although the file docstring says `Debounce/buffer/dedup helpers for inbound webhook messages.`, the file actually contains a broader mixed inbound-preflight subsystem: environment-driven debounce settings, Redis token/buffer helpers, duplicate-message DB/Redis fallback probing, duplicate-response short-circuiting, and a debounce gate that can skip intermediate bursts, fold buffered messages, and delegate muted-state routing: `truffles-api/app/routers/webhook/dedup.py:1`, `truffles-api/app/routers/webhook/dedup.py:41`, `truffles-api/app/routers/webhook/dedup.py:93`, `truffles-api/app/routers/webhook/dedup.py:199`, `truffles-api/app/routers/webhook/dedup.py:295`, `truffles-api/app/routers/webhook/dedup.py:439`, `truffles-api/app/routers/webhook/dedup.py:526`.
- `FACT`: The visible consumer graph is narrower than the previous webhook hotspots: `decision.py` still imports the dedup surface and preserves `_DEDUP_EXPORTS`, `reasoning_core.py` delegates `_lookup_preexisting_duplicate_message(...)` to this owner module, and `app/webhook.py` preserves shadow copies of the debounce/dedup primitives already extracted here: `truffles-api/app/routers/webhook/decision.py:163`, `truffles-api/app/routers/webhook/decision.py:170`, `truffles-api/app/routers/webhook/decision.py:3376`, `truffles-api/app/services/reasoning_core.py:475`, `truffles-api/app/services/reasoning_core.py:483`, `truffles-api/app/webhook.py:65`, `truffles-api/app/webhook.py:105`, `truffles-api/app/webhook.py:183`.
- `INFERENCE`: `dedup.py` is not a tiny infra helper; it is the partially extracted debounce/dedup owner family, but current visible non-test callers are narrow and shadowed by lingering compatibility imports and duplicate legacy copies.

## 2. Why This File Exists
- `FACT`: The file exists to centralize debounce/dedup settings and Redis access through `_get_debounce_settings(...)`, `_get_message_buffer_settings(...)`, `_get_dedup_settings(...)`, and `_get_debounce_redis(...)`: `truffles-api/app/routers/webhook/dedup.py:41`, `truffles-api/app/routers/webhook/dedup.py:52`, `truffles-api/app/routers/webhook/dedup.py:60`, `truffles-api/app/routers/webhook/dedup.py:75`.
- `FACT`: It also exists to implement burst-message control through `should_process_debounced_message(...)`, `_buffer_user_message(...)`, and `_drain_buffered_messages(...)`: `truffles-api/app/routers/webhook/dedup.py:93`, `truffles-api/app/routers/webhook/dedup.py:129`, `truffles-api/app/routers/webhook/dedup.py:150`.
- `FACT`: The file further exists to own duplicate-message probing and preflight short-circuiting through `DuplicateMessageProbe`, `_lookup_duplicate_message_in_messages_table(...)`, `_lookup_preexisting_duplicate_message(...)`, `is_duplicate_message_id(...)`, `_handle_dedup_gate(...)`, and `_handle_debounce_gate(...)`: `truffles-api/app/routers/webhook/dedup.py:32`, `truffles-api/app/routers/webhook/dedup.py:178`, `truffles-api/app/routers/webhook/dedup.py:199`, `truffles-api/app/routers/webhook/dedup.py:295`, `truffles-api/app/routers/webhook/dedup.py:439`, `truffles-api/app/routers/webhook/dedup.py:526`.
- `INFERENCE`: The file exists because the old webhook stack tried to extract debounce/dedup behavior into a dedicated owner module, but the extraction was not completed cleanly: direct live callers are scarce, `decision.py` still imports dead export residue, and `app/webhook.py` still preserves shadow copies of core primitives.

## 3. Active Callers And Entrypoints
- `FACT`: `decision.py` imports `_buffer_user_message(...)`, `_drain_buffered_messages(...)`, `_get_debounce_redis(...)`, `_handle_debounce_gate(...)`, `_handle_dedup_gate(...)`, `is_duplicate_message_id(...)`, and `should_process_debounced_message(...)`, then preserves a subset as `_DEDUP_EXPORTS`: `truffles-api/app/routers/webhook/decision.py:163`, `truffles-api/app/routers/webhook/decision.py:170`, `truffles-api/app/routers/webhook/decision.py:3376`.
- `FACT`: `reasoning_core.py` delegates `_lookup_preexisting_duplicate_message(...)` to this module inside its duplicate-preflight shim: `truffles-api/app/services/reasoning_core.py:475`, `truffles-api/app/services/reasoning_core.py:483`.
- `FACT`: Tests import the module directly, and `test_webhook_dedup.py` still also exercises the shadowed `app.webhook` copy of `is_duplicate_message_id(...)`: `truffles-api/tests/test_webhook_dedup.py:9`, `truffles-api/tests/test_webhook_dedup.py:111`, `truffles-api/tests/test_webhook_dedup.py:193`, `truffles-api/tests/test_state_service.py:10`, `truffles-api/tests/test_reasoning_core.py:13`.
- `UNKNOWN`: Whether `_handle_dedup_gate(...)`, `_handle_debounce_gate(...)`, `is_duplicate_message_id(...)`, and `should_process_debounced_message(...)` still have any live non-definition callers on the active runtime path, because this pass found no visible non-test callsites beyond import residency and compatibility/test delegation.

## 4. Control Path Owned By This File
- `FACT`: `_lookup_preexisting_duplicate_message(...)` is the visible probe entrypoint; it first checks fast-test bypass, then probes `message_dedup`, and finally falls back to the `messages` table when the owner table lookup fails: `truffles-api/app/routers/webhook/dedup.py:199`, `truffles-api/app/routers/webhook/dedup.py:205`, `truffles-api/app/routers/webhook/dedup.py:216`, `truffles-api/app/routers/webhook/dedup.py:241`, `truffles-api/app/routers/webhook/dedup.py:259`, `truffles-api/app/services/reasoning_core.py:483`.
- `FACT`: `is_duplicate_message_id(...)` is the main duplicate decision path; it tries Redis NX-based dedup, then persistent DB dedup through `message_dedup`, and finally falls back to the `messages` table while populating diagnostics: `truffles-api/app/routers/webhook/dedup.py:295`, `truffles-api/app/routers/webhook/dedup.py:345`, `truffles-api/app/routers/webhook/dedup.py:380`, `truffles-api/app/routers/webhook/dedup.py:419`, `truffles-api/app/routers/webhook/dedup.py:429`.
- `FACT`: `_handle_dedup_gate(...)` turns duplicate detection into a preflight ignore outcome by recording an early trace and returning a duplicate `WebhookResponse`; `_handle_debounce_gate(...)` turns burst-message control into skip/drain behavior, then can short-circuit on manager-active or muted-state routing through `guards.py`: `truffles-api/app/routers/webhook/dedup.py:439`, `truffles-api/app/routers/webhook/dedup.py:475`, `truffles-api/app/routers/webhook/dedup.py:486`, `truffles-api/app/routers/webhook/dedup.py:526`, `truffles-api/app/routers/webhook/dedup.py:574`, `truffles-api/app/routers/webhook/dedup.py:594`, `truffles-api/app/routers/webhook/dedup.py:624`, `truffles-api/app/routers/webhook/dedup.py:643`.
- `INFERENCE`: The file owns a coherent pre-owner debounce/dedup family, but the current repo graph suggests that much of that family is now compatibility/test-facing rather than clearly active on the extracted runtime ingress path.

## 5. Data Reads
- `FACT`: The file reads environment-controlled feature flags and timeouts through `_legacy._is_env_enabled(...)` and raw environment variables in `_get_debounce_settings(...)`, `_get_message_buffer_settings(...)`, `_get_dedup_settings(...)`, and `_is_fast_dedup_bypass_enabled(...)`: `truffles-api/app/routers/webhook/dedup.py:41`, `truffles-api/app/routers/webhook/dedup.py:52`, `truffles-api/app/routers/webhook/dedup.py:60`, `truffles-api/app/routers/webhook/dedup.py:67`.
- `FACT`: It reads Redis-backed ephemeral routing state through `truffles:debounce:*`, `truffles:buffer:*`, and `truffles:dedup:*` keys via `set/get/rpush/lrange/delete` flows: `truffles-api/app/routers/webhook/dedup.py:107`, `truffles-api/app/routers/webhook/dedup.py:137`, `truffles-api/app/routers/webhook/dedup.py:154`, `truffles-api/app/routers/webhook/dedup.py:346`.
- `FACT`: It reads duplicate state from the `message_dedup` table and from the `messages` table fallback through SQL text queries and `_lookup_duplicate_message_in_messages_table(...)`: `truffles-api/app/routers/webhook/dedup.py:178`, `truffles-api/app/routers/webhook/dedup.py:216`, `truffles-api/app/routers/webhook/dedup.py:380`, `truffles-api/app/routers/webhook/dedup.py:419`.
- `FACT`: `_handle_dedup_gate(...)` and `_handle_debounce_gate(...)` also read upstream callbacks or neighbor stages such as `build_inbound_message_id(...)`, `resolve_trace_conversation(...)`, `record_early_trace(...)`, `_record_decision_trace(...)`, and `_handle_post_debounce_muted_state_gate(...)`: `truffles-api/app/routers/webhook/dedup.py:452`, `truffles-api/app/routers/webhook/dedup.py:457`, `truffles-api/app/routers/webhook/dedup.py:477`, `truffles-api/app/routers/webhook/dedup.py:541`, `truffles-api/app/routers/webhook/dedup.py:542`, `truffles-api/app/routers/webhook/dedup.py:643`.
- `INFERENCE`: `dedup.py` reads from too many layers to be a thin infra boundary; it spans env/config flags, Redis ephemeral state, SQL duplicate probes, early trace callbacks, and guard-stage routing.

## 6. Data Writes And Side Effects
- `FACT`: The file writes and deletes Redis debounce/buffer/dedup keys through `set`, `rpush`, `ltrim`, `expire`, `get`, and `delete`: `truffles-api/app/routers/webhook/dedup.py:116`, `truffles-api/app/routers/webhook/dedup.py:137`, `truffles-api/app/routers/webhook/dedup.py:154`, `truffles-api/app/routers/webhook/dedup.py:352`.
- `FACT`: It writes persistent dedup state through `INSERT INTO message_dedup ... ON CONFLICT DO NOTHING`, and it rolls back or commits the DB transaction on probe paths: `truffles-api/app/routers/webhook/dedup.py:380`, `truffles-api/app/routers/webhook/dedup.py:392`, `truffles-api/app/routers/webhook/dedup.py:409`.
- `FACT`: `_handle_dedup_gate(...)` writes early trace/meta evidence and can mutate inbound metadata by backfilling `metadata.messageId`: `truffles-api/app/routers/webhook/dedup.py:457`, `truffles-api/app/routers/webhook/dedup.py:486`, `truffles-api/app/routers/webhook/dedup.py:504`, `truffles-api/app/routers/webhook/dedup.py:521`.
- `FACT`: `_handle_debounce_gate(...)` rewrites `message_text`, `batch_messages`, and `append_user_message`, records debounce traces, refreshes the conversation, and can return user-visible skip responses before the runtime owner path: `truffles-api/app/routers/webhook/dedup.py:533`, `truffles-api/app/routers/webhook/dedup.py:574`, `truffles-api/app/routers/webhook/dedup.py:602`, `truffles-api/app/routers/webhook/dedup.py:618`, `truffles-api/app/routers/webhook/dedup.py:624`, `truffles-api/app/routers/webhook/dedup.py:643`.
- `INFERENCE`: `dedup.py` is not a pure storage helper; it still owns external state mutation, preflight trace writing, inbound payload rewriting, and user-visible skip outcomes.

## 7. Semantic Authority / Deterministic Authority
- `FACT`: The module deterministically classifies duplicate state through `_lookup_preexisting_duplicate_message(...)` and `is_duplicate_message_id(...)`, including Redis/DB/messages fallback behavior and fast-test bypass handling: `truffles-api/app/routers/webhook/dedup.py:199`, `truffles-api/app/routers/webhook/dedup.py:205`, `truffles-api/app/routers/webhook/dedup.py:295`, `truffles-api/app/routers/webhook/dedup.py:345`, `truffles-api/app/routers/webhook/dedup.py:380`, `truffles-api/app/routers/webhook/dedup.py:419`.
- `FACT`: `_handle_dedup_gate(...)` deterministically rewrites duplicate detection into an `ignore` response with early trace evidence before any owner/runtime handling continues: `truffles-api/app/routers/webhook/dedup.py:439`, `truffles-api/app/routers/webhook/dedup.py:475`, `truffles-api/app/routers/webhook/dedup.py:486`.
- `FACT`: `_handle_debounce_gate(...)` deterministically rewrites burst input into intermediate-message skip, buffered-message merge, manager-active skip, or muted-state guard delegation before the inbound flow continues: `truffles-api/app/routers/webhook/dedup.py:526`, `truffles-api/app/routers/webhook/dedup.py:560`, `truffles-api/app/routers/webhook/dedup.py:574`, `truffles-api/app/routers/webhook/dedup.py:594`, `truffles-api/app/routers/webhook/dedup.py:624`, `truffles-api/app/routers/webhook/dedup.py:643`.
- `INFERENCE`: `dedup.py` is not a semantic owner, but it is a pre-owner deterministic routing seam that can suppress or rewrite inbound processing before any extracted runtime owner sees the turn.

## 8. Truth Carriers Touched Here
- `FACT`: The module manufactures `DuplicateMessageProbe(duplicate, backend, fallback_reason)` as an explicit duplicate-truth carrier for preflight decisions: `truffles-api/app/routers/webhook/dedup.py:32`, `truffles-api/app/routers/webhook/dedup.py:205`, `truffles-api/app/routers/webhook/dedup.py:259`, `truffles-api/app/services/reasoning_core.py:483`.
- `FACT`: It also populates `dedup_diagnostics` carriers such as `dedup_backend`, `dedup_fallback_reason`, `dedup_duplicate`, `dedup_latency_ms`, `dedup_redis_latency_ms`, `dedup_db_latency_ms`, and `dedup_messages_latency_ms`: `truffles-api/app/routers/webhook/dedup.py:306`, `truffles-api/app/routers/webhook/dedup.py:315`, `truffles-api/app/routers/webhook/dedup.py:329`, `truffles-api/app/routers/webhook/dedup.py:360`, `truffles-api/app/routers/webhook/dedup.py:396`, `truffles-api/app/routers/webhook/dedup.py:429`.
- `FACT`: Redis debounce/buffer/dedup keys and drained buffered-message lists act as ingress-side ephemeral routing carriers: `truffles-api/app/routers/webhook/dedup.py:107`, `truffles-api/app/routers/webhook/dedup.py:135`, `truffles-api/app/routers/webhook/dedup.py:154`, `truffles-api/app/routers/webhook/dedup.py:346`, `truffles-api/app/routers/webhook/dedup.py:594`.
- `FACT`: `_handle_dedup_gate(...)` and `_handle_debounce_gate(...)` also manufacture early trace/meta carriers and mutated inbound payload carriers (`message_text`, `batch_messages`, `append_user_message`) before the runtime path: `truffles-api/app/routers/webhook/dedup.py:486`, `truffles-api/app/routers/webhook/dedup.py:504`, `truffles-api/app/routers/webhook/dedup.py:574`, `truffles-api/app/routers/webhook/dedup.py:602`, `truffles-api/app/routers/webhook/dedup.py:650`.
- `INFERENCE`: `dedup.py` is another place where compatibility carriers become final pre-owner routing truth, so debounce/dedup truth still lives outside the extracted runtime state and owner path.

## 9. Violations Against The Target Canon
- `FACT`: The file still mixes env/config parsing, Redis buffer control, DB duplicate probing, messages-table fallback, early trace writing, message-id synthesis, and muted-state guard composition inside one module: `truffles-api/app/routers/webhook/dedup.py:41`, `truffles-api/app/routers/webhook/dedup.py:93`, `truffles-api/app/routers/webhook/dedup.py:199`, `truffles-api/app/routers/webhook/dedup.py:295`, `truffles-api/app/routers/webhook/dedup.py:439`, `truffles-api/app/routers/webhook/dedup.py:526`, `truffles-api/app/routers/webhook/dedup.py:643`.
- `FACT`: The file still depends on `_legacy.py` feature-flag parsing and on `guards.py` for muted-state routing, so the extracted inbound-preflight family is still coupled to the old compatibility graph: `truffles-api/app/routers/webhook/dedup.py:42`, `truffles-api/app/routers/webhook/dedup.py:53`, `truffles-api/app/routers/webhook/dedup.py:68`, `truffles-api/app/routers/webhook/dedup.py:541`, `truffles-api/app/routers/webhook/dedup.py:643`.
- `FACT`: The dedup/debounce primitives are still duplicated in `truffles-api/app/webhook.py`, which keeps shadow copies of `_get_debounce_settings(...)`, `_get_message_buffer_settings(...)`, `should_process_debounced_message(...)`, `_buffer_user_message(...)`, `_drain_buffered_messages(...)`, and `is_duplicate_message_id(...)`: `truffles-api/app/webhook.py:65`, `truffles-api/app/webhook.py:74`, `truffles-api/app/webhook.py:105`, `truffles-api/app/webhook.py:140`, `truffles-api/app/webhook.py:161`, `truffles-api/app/webhook.py:183`.
- `FACT`: `decision.py` still imports the dedup helper surface and preserves `_DEDUP_EXPORTS`, even though this pass found no visible direct non-definition callsites there: `truffles-api/app/routers/webhook/decision.py:163`, `truffles-api/app/routers/webhook/decision.py:170`, `truffles-api/app/routers/webhook/decision.py:3376`.
- `INFERENCE`: Strategic point `deterministic only at boundaries` remains open here because dedup/debounce is not isolated as a clean inbound boundary; it still manufactures skip/ignore outcomes and inbound payload rewrites from a mixed compatibility module with shadow duplicates elsewhere.
- `INFERENCE`: Strategic point `shadowed core definition` remains open here because the same dedup/debounce primitive family still exists in both `truffles-api/app/routers/webhook/dedup.py` and `truffles-api/app/webhook.py`.

## 10. Salvageable Parts
- `FACT`: The small duplicate-probe dataclass and fallback helpers are salvageable if moved behind a cleaner inbound boundary: `DuplicateMessageProbe`, `_merge_duplicate_fallback_reason(...)`, and `_lookup_duplicate_message_in_messages_table(...)`: `truffles-api/app/routers/webhook/dedup.py:32`, `truffles-api/app/routers/webhook/dedup.py:172`, `truffles-api/app/routers/webhook/dedup.py:178`.
- `FACT`: The Redis helper family is salvageable if relocated into a dedicated inbound infra boundary: `_get_debounce_redis(...)`, `_buffer_user_message(...)`, and `_drain_buffered_messages(...)`: `truffles-api/app/routers/webhook/dedup.py:75`, `truffles-api/app/routers/webhook/dedup.py:129`, `truffles-api/app/routers/webhook/dedup.py:150`.
- `FACT`: The DB-owner duplicate probe path is salvageable if separated from early trace and response shaping: `_lookup_preexisting_duplicate_message(...)` and `is_duplicate_message_id(...)`: `truffles-api/app/routers/webhook/dedup.py:199`, `truffles-api/app/routers/webhook/dedup.py:295`.
- `INFERENCE`: The salvageable core is the bounded duplicate-probe and Redis-buffer logic, not the mixed preflight/trace/skip orchestration that currently surrounds it.

## 11. Demotion / Removal Candidates
- `FACT`: `_handle_dedup_gate(...)` and `_handle_debounce_gate(...)` are the main demotion candidates because they keep early trace writing, skip responses, inbound payload rewriting, and guard-stage composition inside the dedup hotspot: `truffles-api/app/routers/webhook/dedup.py:439`, `truffles-api/app/routers/webhook/dedup.py:526`.
- `FACT`: `_is_fast_dedup_bypass_enabled(...)` is another demotion candidate because test-only bypass logic still lives inside the same production-facing module: `truffles-api/app/routers/webhook/dedup.py:67`, `truffles-api/app/routers/webhook/dedup.py:205`, `truffles-api/app/routers/webhook/dedup.py:452`.
- `FACT`: The shadow duplicate primitives in `truffles-api/app/webhook.py` are removal blockers for this family even though they live outside this module: `truffles-api/app/webhook.py:65`, `truffles-api/app/webhook.py:74`, `truffles-api/app/webhook.py:105`, `truffles-api/app/webhook.py:140`, `truffles-api/app/webhook.py:161`, `truffles-api/app/webhook.py:183`.
- `INFERENCE`: Real demotion requires separating bounded Redis/DB duplicate probing from preflight skip orchestration and then deleting the shadowed primitive copies in `app/webhook.py`.

## 12. What This Analysis Changes In System Understanding
- `FACT`: `dedup.py` is not just an infra helper; it is the extracted debounce/dedup preflight family, but current visible non-test callers are narrow and several surfaces look compatibility/test-facing rather than clearly active on the runtime ingress path.
- `FACT`: The file still preserves deterministic ignore/skip authority before runtime handling, and it still composes into the legacy guard family through `_handle_post_debounce_muted_state_gate(...)`.
- `FACT`: The family is still architecturally incomplete because `decision.py` keeps dedup export residue and `truffles-api/app/webhook.py` keeps shadow copies of the same debounce/dedup primitives.
- `INFERENCE`: After this hotspot, the next honest hotspot is `truffles-api/app/webhook.py`, because it is the root-level legacy wrapper module that still carries shadow copies of debounce/dedup primitives already extracted into `truffles-api/app/routers/webhook/dedup.py`. Without mapping that shadow copy, duplicate-owner debt in this family remains unresolved.

## 13. Open Questions
- `UNKNOWN`: Whether `_handle_dedup_gate(...)`, `_handle_debounce_gate(...)`, `is_duplicate_message_id(...)`, and `should_process_debounced_message(...)` still have any live non-definition callers on the active runtime path.
- `UNKNOWN`: Whether `reasoning_core._lookup_preexisting_duplicate_message(...)` still has any live non-test caller beyond compatibility/tests.
- `UNKNOWN`: Whether debounce/dedup should remain a dedicated inbound boundary at all or move fully into a narrower provider/inbox gateway layer.
- `UNKNOWN`: Which parts of the shadowed debounce/dedup family in `app/webhook.py` are still product-required versus pure duplicate residue.

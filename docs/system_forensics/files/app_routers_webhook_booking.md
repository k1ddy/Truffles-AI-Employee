# File Analysis: `truffles-api/app/routers/webhook/booking.py`

Status: `completed`
Baseline snapshot: `8319d9e1`
Related ledgers:
- `docs/system_forensics/ledgers/CONTROL_PATHS.md`
- `docs/system_forensics/ledgers/SEMANTIC_OWNER_MAP.md`
- `docs/system_forensics/ledgers/TRUTH_CARRIER_MATRIX.md`
- `docs/system_forensics/ledgers/STATE_SURFACE_INVENTORY.md`
- `docs/system_forensics/ledgers/DETERMINISTIC_REWRITE_LEDGER.md`
- `docs/system_forensics/ledgers/CUTOVER_DEPENDENCY_GRAPH.md`
- `docs/system_forensics/ledgers/SALVAGEABLE_COMPONENTS.md`
- `docs/system_forensics/ledgers/DO_NOT_REPEAT.md`
- `docs/system_forensics/final/SYSTEM_FINAL_ANALYSIS.md`

## 1. Role Summary
- `FACT`: Although the file docstring says `Booking state machine helpers (expected_reply_type, slot validators).`, the file actually contains a much broader booking subsystem: slot candidate/validator logic, expected-reply matching, booking confirmation flow, booking interrupt orchestration, class-router compatibility shaping, appointment creation, calendar-sync/reminder side effects, and the main booking-flow state machine: `truffles-api/app/routers/webhook/booking.py:1`, `truffles-api/app/routers/webhook/booking.py:112`, `truffles-api/app/routers/webhook/booking.py:481`, `truffles-api/app/routers/webhook/booking.py:503`, `truffles-api/app/routers/webhook/booking.py:657`, `truffles-api/app/routers/webhook/booking.py:886`, `truffles-api/app/routers/webhook/booking.py:1052`, `truffles-api/app/routers/webhook/booking.py:1149`, `truffles-api/app/routers/webhook/booking.py:1300`, `truffles-api/app/routers/webhook/booking.py:1521`, `truffles-api/app/routers/webhook/booking.py:1618`, `truffles-api/app/routers/webhook/booking.py:2847`.
- `FACT`: The visible non-test direct importers are `decision.py` and `context_manager.py`; `decision.py` imports a broad helper surface plus `_handle_booking_interrupt(...)` and `_handle_booking_flow(...)`, while `context_manager.py` imports `_get_booking_context(...)`: `truffles-api/app/routers/webhook/decision.py:61`, `truffles-api/app/routers/webhook/context_manager.py:10`.
- `FACT`: Tests also import this file directly for booking prompts, appointment creation, and message-endpoint coverage: `truffles-api/tests/test_booking_prompt_leak_guard.py:1`, `truffles-api/tests/test_booking_appointments.py:21`, `truffles-api/tests/test_webhook_booking.py:4`, `truffles-api/tests/test_message_endpoint.py:28`.
- `INFERENCE`: `booking.py` is not a narrow helper module; it is the live booking-domain orchestration subsystem inside the legacy webhook stack.

## 2. Why This File Exists
- `FACT`: The file exists to parse and validate booking slot candidates, including service, datetime, name, and phone validation plus expected-reply slot matching: `truffles-api/app/routers/webhook/booking.py:112`, `truffles-api/app/routers/webhook/booking.py:481`, `truffles-api/app/routers/webhook/booking.py:503`, `truffles-api/app/routers/webhook/booking.py:580`, `truffles-api/app/routers/webhook/booking.py:637`, `truffles-api/app/routers/webhook/booking.py:671`, `truffles-api/app/routers/webhook/booking.py:707`.
- `FACT`: It also exists to own booking prompt progression, booking-interrupt info handling, booking confirmation flow, booking cancellation/pause/human-request escalation, and final appointment capture: `truffles-api/app/routers/webhook/booking.py:1052`, `truffles-api/app/routers/webhook/booking.py:1618`, `truffles-api/app/routers/webhook/booking.py:2847`, `truffles-api/app/routers/webhook/booking.py:3211`, `truffles-api/app/routers/webhook/booking.py:3737`.
- `FACT`: `decision.py` imports the file as a broad booking helper surface, which confirms the booking domain still routes through this module rather than through a thin typed boundary: `truffles-api/app/routers/webhook/decision.py:61`.
- `INFERENCE`: The real reason this file exists is that booking-domain orchestration, continuity, escalation, and commit logic were partially extracted from the old router into a dedicated booking hotspot, not into a thin boundary module.

## 3. Active Callers And Entrypoints
- `FACT`: Visible non-test direct importers are `decision.py` and `context_manager.py`: `truffles-api/app/routers/webhook/decision.py:61`, `truffles-api/app/routers/webhook/context_manager.py:10`.
- `FACT`: The main live entrypoints owned by the file are `_handle_booking_interrupt(...)` and `_handle_booking_flow(...)`: `truffles-api/app/routers/webhook/booking.py:1618`, `truffles-api/app/routers/webhook/booking.py:2847`.
- `FACT`: `decision.py` imports additional booking-state helpers such as `_apply_booking_slot(...)`, `_apply_expected_reply_slot(...)`, `_build_booking_summary(...)`, `_expected_reply_for_booking_question(...)`, `_next_booking_prompt(...)`, `_resolve_datetime_offline(...)`, and multiple validators/matchers: `truffles-api/app/routers/webhook/decision.py:61`.
- `FACT`: Tests import this file directly for unit and integration coverage, including appointment-creation branches and message-endpoint routing: `truffles-api/tests/test_booking_appointments.py:21`, `truffles-api/tests/test_webhook_booking.py:4`, `truffles-api/tests/test_message_endpoint.py:291`.
- `INFERENCE`: `booking.py` remains a live domain control surface because the legacy decision stack delegates booking behavior into this file rather than collapsing booking into a narrower runtime boundary.

## 4. Control Path Owned By This File
- `FACT`: `_handle_booking_interrupt(...)` owns a large booking-interrupt branch: reschedule handoff, booking info/truth-gate replies, master-intent resolution, pending-question booking-slot guidance, booking prompt suppression/keep decisions, consult-return behavior, and info-interrupt escalation: `truffles-api/app/routers/webhook/booking.py:1618`, `truffles-api/app/routers/webhook/booking.py:1700`, `truffles-api/app/routers/webhook/booking.py:1849`, `truffles-api/app/routers/webhook/booking.py:2166`, `truffles-api/app/routers/webhook/booking.py:2503`, `truffles-api/app/routers/webhook/booking.py:2723`, `truffles-api/app/routers/webhook/booking.py:2825`.
- `FACT`: `_handle_booking_flow(...)` owns same-day escalation, human-request handoff, booking cancellation/pause, slot-confirmation handling, booking-prompt progression, service-carryover reuse, and booking commit/final escalation behavior: `truffles-api/app/routers/webhook/booking.py:2847`, `truffles-api/app/routers/webhook/booking.py:2912`, `truffles-api/app/routers/webhook/booking.py:3026`, `truffles-api/app/routers/webhook/booking.py:3156`, `truffles-api/app/routers/webhook/booking.py:3211`, `truffles-api/app/routers/webhook/booking.py:3481`, `truffles-api/app/routers/webhook/booking.py:3737`, `truffles-api/app/routers/webhook/booking.py:3765`.
- `FACT`: `_create_booking_appointment(...)` owns booking commit-side execution details such as scheduling, overlap handling, specialist selection, booking mode/provider readiness checks, calendar sync enqueue, and reminder scheduling: `truffles-api/app/routers/webhook/booking.py:1300`, `truffles-api/app/routers/webhook/booking.py:1338`, `truffles-api/app/routers/webhook/booking.py:1376`, `truffles-api/app/routers/webhook/booking.py:1457`, `truffles-api/app/routers/webhook/booking.py:1490`, `truffles-api/app/routers/webhook/booking.py:1502`.
- `FACT`: `_build_booking_class_router_result(...)` and `_record_booking_class_router_trace(...)` own booking-side class-router compatibility shaping and observability packaging: `truffles-api/app/routers/webhook/booking.py:1521`, `truffles-api/app/routers/webhook/booking.py:1563`.
- `INFERENCE`: This file owns the booking-domain reply/continuity/escalation/commit side of the legacy decision stack, not just slot validation.

## 5. Data Reads
- `FACT`: The file reads booking signal and normalization helpers from `booking_signal_service`, expected-reply contract helpers from `expected_reply_contract`, runtime capabilities, and generic pack/runtime helpers: `truffles-api/app/routers/webhook/booking.py:23`, `truffles-api/app/routers/webhook/booking.py:63`, `truffles-api/app/routers/webhook/booking.py:74`, `truffles-api/app/routers/webhook/booking.py:2863`.
- `FACT`: It repeatedly reads legacy routing and continuity helpers from `_legacy.py`, including text normalization, booking-signal detection, expected-reply constants, truth-gate/service-matcher integration points, class-router result shaping, context accessors, handover helpers, and booking prompt helpers: `truffles-api/app/routers/webhook/booking.py:115`, `truffles-api/app/routers/webhook/booking.py:515`, `truffles-api/app/routers/webhook/booking.py:658`, `truffles-api/app/routers/webhook/booking.py:795`, `truffles-api/app/routers/webhook/booking.py:1526`, `truffles-api/app/routers/webhook/booking.py:1673`, `truffles-api/app/routers/webhook/booking.py:2196`, `truffles-api/app/routers/webhook/booking.py:3492`.
- `FACT`: `_handle_booking_interrupt(...)` reads policy/truth-pack services such as `build_info_combined_reply(...)`, `compose_multi_truth_reply(...)`, `format_reply_from_truth(...)`, `get_pack_decision(...)`, and `resolve_master_intent(...)`: `truffles-api/app/routers/webhook/booking.py:1664`, `truffles-api/app/routers/webhook/booking.py:1919`, `truffles-api/app/routers/webhook/booking.py:1979`, `truffles-api/app/routers/webhook/booking.py:2000`, `truffles-api/app/routers/webhook/booking.py:1849`.
- `FACT`: `_create_booking_appointment(...)` reads branch/settings, scheduling state, specialist-service associations, provider health, and reminder configuration: `truffles-api/app/routers/webhook/booking.py:1230`, `truffles-api/app/routers/webhook/booking.py:1300`, `truffles-api/app/routers/webhook/booking.py:1353`, `truffles-api/app/routers/webhook/booking.py:1457`, `truffles-api/app/routers/webhook/booking.py:1502`.
- `INFERENCE`: `booking.py` reads from too many surfaces to be considered a narrow booking boundary; it spans lexical signal helpers, expected-reply contracts, `_legacy` compatibility state, truth-pack services, scheduling, and escalation hooks.

## 6. Data Writes And Side Effects
- `FACT`: The file writes booking context, service hints, and expected-reply context back into conversation context through `_legacy` setters: `truffles-api/app/routers/webhook/booking.py:224`, `truffles-api/app/routers/webhook/booking.py:708`, `truffles-api/app/routers/webhook/booking.py:2208`, `truffles-api/app/routers/webhook/booking.py:2509`, `truffles-api/app/routers/webhook/booking.py:3605`.
- `FACT`: It writes message-level decision metadata and decision trace throughout interrupt, confirmation, prompt, and commit branches: `truffles-api/app/routers/webhook/booking.py:1751`, `truffles-api/app/routers/webhook/booking.py:1975`, `truffles-api/app/routers/webhook/booking.py:2237`, `truffles-api/app/routers/webhook/booking.py:2780`, `truffles-api/app/routers/webhook/booking.py:3231`, `truffles-api/app/routers/webhook/booking.py:3375`, `truffles-api/app/routers/webhook/booking.py:3750`.
- `FACT`: `_create_booking_appointment(...)` creates appointments, enqueues calendar sync, and schedules reminders: `truffles-api/app/routers/webhook/booking.py:1457`, `truffles-api/app/routers/webhook/booking.py:1493`, `truffles-api/app/routers/webhook/booking.py:1504`.
- `FACT`: `_handle_booking_interrupt(...)` and `_handle_booking_flow(...)` can reuse or create handovers, send Telegram notifications, send/save responses, and commit DB transactions on multiple paths: `truffles-api/app/routers/webhook/booking.py:1702`, `truffles-api/app/routers/webhook/booking.py:1722`, `truffles-api/app/routers/webhook/booking.py:2277`, `truffles-api/app/routers/webhook/booking.py:2927`, `truffles-api/app/routers/webhook/booking.py:2946`, `truffles-api/app/routers/webhook/booking.py:3098`, `truffles-api/app/routers/webhook/booking.py:3765`, `truffles-api/app/routers/webhook/booking.py:3786`, `truffles-api/app/routers/webhook/booking.py:3843`.
- `INFERENCE`: `booking.py` is one of the main side-effect hotspots in the modular webhook layer, not just a validation helper.

## 7. Semantic Authority / Deterministic Authority
- `FACT`: The file performs deterministic lexical and slot validation logic for booking semantics, including `_is_booking_request(...)`, offline datetime resolution, expected-reply slot matching, and slot application: `truffles-api/app/routers/webhook/booking.py:515`, `truffles-api/app/routers/webhook/booking.py:657`, `truffles-api/app/routers/webhook/booking.py:671`, `truffles-api/app/routers/webhook/booking.py:707`, `truffles-api/app/routers/webhook/booking.py:950`, `truffles-api/app/routers/webhook/booking.py:981`.
- `FACT`: `_build_booking_class_router_result(...)` deterministically manufactures booking-specific `class_router_result` payloads on top of `_legacy` controller/router helpers: `truffles-api/app/routers/webhook/booking.py:1521`, `truffles-api/app/routers/webhook/booking.py:1551`.
- `FACT`: `_handle_booking_interrupt(...)` deterministically resolves booking-info interrupts, master-only interrupts, pending-question time guidance, and reschedule handoff behavior from `_legacy` helpers plus truth-gate/service-match results: `truffles-api/app/routers/webhook/booking.py:1682`, `truffles-api/app/routers/webhook/booking.py:1849`, `truffles-api/app/routers/webhook/booking.py:1931`, `truffles-api/app/routers/webhook/booking.py:2166`, `truffles-api/app/routers/webhook/booking.py:2365`, `truffles-api/app/routers/webhook/booking.py:2503`.
- `FACT`: `_handle_booking_flow(...)` deterministically handles same-day escalation, human-request escalation, confirmation yes/no, prompt repetition, pause/cancel, and booking commit capture outside the extracted policy-core runtime path: `truffles-api/app/routers/webhook/booking.py:2912`, `truffles-api/app/routers/webhook/booking.py:3026`, `truffles-api/app/routers/webhook/booking.py:3246`, `truffles-api/app/routers/webhook/booking.py:3433`, `truffles-api/app/routers/webhook/booking.py:3595`, `truffles-api/app/routers/webhook/booking.py:3737`.
- `INFERENCE`: `booking.py` is not a target-state semantic owner, but it is a major deterministic booking authority seam that still shapes user-visible booking meaning, continuity, and escalation after and beside the extracted runtime path.

## 8. Truth Carriers Touched Here
- `FACT`: The file touches booking continuity state in top-level booking context, including active/inactive status, collected slots, confirmation payload, and `last_question`: `truffles-api/app/routers/webhook/booking.py:140`, `truffles-api/app/routers/webhook/booking.py:165`, `truffles-api/app/routers/webhook/booking.py:760`, `truffles-api/app/routers/webhook/booking.py:3211`, `truffles-api/app/routers/webhook/booking.py:3433`.
- `FACT`: It also writes expected-reply continuity through `_set_expected_reply_context(...)` and derives expected-reply prompts from booking state: `truffles-api/app/routers/webhook/booking.py:657`, `truffles-api/app/routers/webhook/booking.py:2208`, `truffles-api/app/routers/webhook/booking.py:2509`, `truffles-api/app/routers/webhook/booking.py:3292`, `truffles-api/app/routers/webhook/booking.py:3605`.
- `FACT`: The file carries compatibility routing truth through `class_router_result` plus service carryover/service hint reuse: `truffles-api/app/routers/webhook/booking.py:1521`, `truffles-api/app/routers/webhook/booking.py:1563`, `truffles-api/app/routers/webhook/booking.py:3492`, `truffles-api/app/routers/webhook/booking.py:3509`.
- `FACT`: It writes message evidence carriers such as slot snapshots, confirmation metadata, appointment metadata, and pending-question targets: `truffles-api/app/routers/webhook/booking.py:2237`, `truffles-api/app/routers/webhook/booking.py:2780`, `truffles-api/app/routers/webhook/booking.py:3231`, `truffles-api/app/routers/webhook/booking.py:3375`, `truffles-api/app/routers/webhook/booking.py:3750`.
- `INFERENCE`: `booking.py` is where booking continuity carriers become booking prompts, booking commits, and booking-side handoff behavior, so it remains part of the multi-carrier truth problem.

## 9. Violations Against The Target Canon
- `FACT`: The file still mixes slot parsing/validation, booking continuity mutation, class-router compatibility shaping, truth/info interrupt routing, handover/escalation logic, and appointment execution inside one module: `truffles-api/app/routers/webhook/booking.py:112`, `truffles-api/app/routers/webhook/booking.py:1521`, `truffles-api/app/routers/webhook/booking.py:1618`, `truffles-api/app/routers/webhook/booking.py:2847`, `truffles-api/app/routers/webhook/booking.py:3737`.
- `FACT`: It still depends broadly on `_legacy.py` for booking semantics, continuity access, router helpers, expected-reply constants, and escalation hooks: `truffles-api/app/routers/webhook/booking.py:115`, `truffles-api/app/routers/webhook/booking.py:515`, `truffles-api/app/routers/webhook/booking.py:795`, `truffles-api/app/routers/webhook/booking.py:1526`, `truffles-api/app/routers/webhook/booking.py:1673`, `truffles-api/app/routers/webhook/booking.py:2884`.
- `FACT`: It still writes and reacts to legacy/compatibility continuity surfaces such as top-level booking context, expected-reply context, service carryover, and class-router payloads rather than a single canonical runtime boundary: `truffles-api/app/routers/webhook/booking.py:707`, `truffles-api/app/routers/webhook/booking.py:1521`, `truffles-api/app/routers/webhook/booking.py:2208`, `truffles-api/app/routers/webhook/booking.py:3509`, `truffles-api/app/routers/webhook/booking.py:3605`.
- `FACT`: The file still owns direct booking commit and escalation side effects (`SchedulingService`, calendar sync, reminders, pending handovers, Telegram notifications) inside the domain module instead of a thin projection/execution boundary: `truffles-api/app/routers/webhook/booking.py:1457`, `truffles-api/app/routers/webhook/booking.py:1493`, `truffles-api/app/routers/webhook/booking.py:1504`, `truffles-api/app/routers/webhook/booking.py:1722`, `truffles-api/app/routers/webhook/booking.py:2946`, `truffles-api/app/routers/webhook/booking.py:3786`.
- `INFERENCE`: Strategic point `one control path` remains open here because booking behavior still runs through a separate mixed legacy domain orchestration family rather than a thin extracted runtime boundary.
- `INFERENCE`: Strategic point `deterministic only at boundaries` remains open here because booking semantics, escalation, and continuity are still being rewritten inside the booking domain module.

## 10. Salvageable Parts
- `FACT`: The bounded slot-validation and normalization helpers are salvageable if relocated behind a cleaner booking boundary: `_build_slot_candidates(...)`, `_validate_service_slot(...)`, `_validate_datetime_slot(...)`, `_validate_name_slot(...)`, `_validate_phone_slot(...)`, `_match_expected_reply(...)`, and `_resolve_datetime_offline(...)`: `truffles-api/app/routers/webhook/booking.py:112`, `truffles-api/app/routers/webhook/booking.py:481`, `truffles-api/app/routers/webhook/booking.py:503`, `truffles-api/app/routers/webhook/booking.py:580`, `truffles-api/app/routers/webhook/booking.py:637`, `truffles-api/app/routers/webhook/booking.py:671`, `truffles-api/app/routers/webhook/booking.py:347`.
- `FACT`: `_resolve_booking_settings(...)`, `_create_booking_appointment(...)`, and `_build_booking_summary(...)` are salvageable as bounded booking execution/projection helpers if stripped from `_legacy`-driven prompt/escalation logic: `truffles-api/app/routers/webhook/booking.py:1149`, `truffles-api/app/routers/webhook/booking.py:1209`, `truffles-api/app/routers/webhook/booking.py:1300`.
- `FACT`: The small dataclasses `SlotCandidate` and `BookingFlowResult` are also reusable as typed booking artifacts if relocated: `truffles-api/app/routers/webhook/booking.py:78`, `truffles-api/app/routers/webhook/booking.py:1514`.
- `INFERENCE`: The salvageable core is the bounded validation/execution helper layer, not the booking interrupt/prompt/handover orchestration that currently surrounds it.

## 11. Demotion / Removal Candidates
- `FACT`: `_handle_booking_interrupt(...)` and `_handle_booking_flow(...)` are the main demotion candidates because they keep booking info interrupts, prompt loops, handover logic, and booking capture inside a legacy domain hotspot: `truffles-api/app/routers/webhook/booking.py:1618`, `truffles-api/app/routers/webhook/booking.py:2847`.
- `FACT`: `_build_booking_class_router_result(...)` and `_record_booking_class_router_trace(...)` are also demotion candidates because they preserve class-router compatibility carriers inside the booking module: `truffles-api/app/routers/webhook/booking.py:1521`, `truffles-api/app/routers/webhook/booking.py:1563`.
- `FACT`: The repeated `_legacy`-backed prompt, carryover, expected-reply, and escalation branches inside the booking handlers are removal blockers for any clean booking boundary: `truffles-api/app/routers/webhook/booking.py:1702`, `truffles-api/app/routers/webhook/booking.py:2196`, `truffles-api/app/routers/webhook/booking.py:2509`, `truffles-api/app/routers/webhook/booking.py:3492`, `truffles-api/app/routers/webhook/booking.py:3765`.
- `INFERENCE`: Real demotion requires separating booking validation/execution helpers from booking prompting, continuity mutation, class-router compatibility, and handover policy.

## 12. What This Analysis Changes In System Understanding
- `FACT`: `booking.py` is not just a booking validator; it is the live booking-domain orchestration subsystem of the legacy webhook stack.
- `FACT`: The file is a live composition point where `_legacy.py` compatibility helpers, expected-reply contracts, service-carryover state, and appointment execution become booking prompts, booking commits, and booking-side handoff behavior.
- `FACT`: The file still contains active booking-side deterministic authority and direct side effects outside the extracted runtime path.
- `INFERENCE`: After this hotspot, the next honest hotspot is `truffles-api/app/routers/webhook/info.py`, because it is the other large live user-facing domain module that still imports `_legacy.py` heavily and remains coupled to class-router / info-reply compatibility behavior.

## 13. Open Questions
- `UNKNOWN`: Which parts of `_handle_booking_interrupt(...)` are still product-essential versus compatibility residue from the old interrupt/class-router stack.
- `UNKNOWN`: Which parts of `_handle_booking_flow(...)` should ultimately remain as bounded booking execution versus disappear with the legacy webhook flow.
- `UNKNOWN`: How much of the booking confirmation/prompt machinery should survive after canonical runtime-state and pure-boundary extraction.

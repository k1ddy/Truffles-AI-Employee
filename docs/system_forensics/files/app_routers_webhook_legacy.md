# File Analysis: `truffles-api/app/routers/webhook/_legacy.py`

Status: `completed`
Baseline snapshot: `8319d9e1`
Related ledgers:
- `docs/system_forensics/ledgers/CONTROL_PATHS.md`
- `docs/system_forensics/ledgers/SEMANTIC_OWNER_MAP.md`
- `docs/system_forensics/ledgers/TRUTH_CARRIER_MATRIX.md`
- `docs/system_forensics/ledgers/STATE_SURFACE_INVENTORY.md`
- `docs/system_forensics/ledgers/DETERMINISTIC_REWRITE_LEDGER.md`
- `docs/system_forensics/ledgers/CUTOVER_DEPENDENCY_GRAPH.md`
- `docs/system_forensics/ledgers/SALVAGEABLE_COMPONENTS.md`
- `docs/system_forensics/ledgers/DO_NOT_REPEAT.md`
- `docs/system_forensics/final/SYSTEM_FINAL_ANALYSIS.md`

## 1. Role Summary
- `FACT`: `_legacy.py` is a 107-line import-time adapter whose own docstring says `"Legacy webhook adapter: re-export decision orchestrator symbols."`; the file imports selected symbols from `context_manager`, `response`, `runtime_primitives`, `chatflow_service`, `handover_owner_service`, and then imports `decision` as `_decision`: `truffles-api/app/routers/webhook/_legacy.py:1`, `truffles-api/app/routers/webhook/_legacy.py:5`, `truffles-api/app/routers/webhook/_legacy.py:6`, `truffles-api/app/routers/webhook/_legacy.py:7`, `truffles-api/app/routers/webhook/_legacy.py:35`, `truffles-api/app/routers/webhook/_legacy.py:36`, `truffles-api/app/routers/webhook/_legacy.py:49`.
- `FACT`: The file builds a compatibility namespace in three steps: explicit `_SHARED_EXPORTS`, re-export of every non-dunder symbol from `decision.__dict__`, and manual override of handover-owner symbols, then exposes everything through dynamic `__all__`: `truffles-api/app/routers/webhook/_legacy.py:51`, `truffles-api/app/routers/webhook/_legacy.py:80`, `truffles-api/app/routers/webhook/_legacy.py:82`, `truffles-api/app/routers/webhook/_legacy.py:87`, `truffles-api/app/routers/webhook/_legacy.py:107`.
- `FACT`: Live modules import this file as `legacy` instead of importing narrow helper modules directly, including `context_manager.py`, `booking.py`, `guards.py`, `response.py`, `pending.py`, `outbox.py`, `trace.py`, and even `tool_registry_service.py`: `truffles-api/app/routers/webhook/context_manager.py:139`, `truffles-api/app/routers/webhook/booking.py:115`, `truffles-api/app/routers/webhook/guards.py:49`, `truffles-api/app/routers/webhook/response.py:249`, `truffles-api/app/routers/webhook/pending.py:28`, `truffles-api/app/routers/webhook/outbox.py:172`, `truffles-api/app/routers/webhook/trace.py:331`, `truffles-api/app/services/tool_registry_service.py:1521`, `truffles-api/app/services/tool_registry_service.py:1932`, `truffles-api/app/services/tool_registry_service.py:2278`.
- `INFERENCE`: `_legacy.py` is not a thin adapter; it is the active import bus that keeps the frozen decision-router namespace resident inside the modular webhook stack and leaks that namespace into at least one non-webhook service.

## 2. Why This File Exists
- `FACT`: The file explicitly exists to re-export `decision` symbols under a legacy namespace: `truffles-api/app/routers/webhook/_legacy.py:1`, `truffles-api/app/routers/webhook/_legacy.py:49`, `truffles-api/app/routers/webhook/_legacy.py:82`.
- `FACT`: The manual override block shows that the legacy namespace is also being used to preserve old handover function names while redirecting them to `handover_owner_service`: `truffles-api/app/routers/webhook/_legacy.py:36`, `truffles-api/app/routers/webhook/_legacy.py:87`, `truffles-api/app/routers/webhook/_legacy.py:100`.
- `FACT`: Tests encode that compatibility promise directly; `test_legacy_handover_adapter_exports_owner_surface_symbols` asserts that legacy names still point to the owner-service implementations: `truffles-api/tests/test_message_endpoint.py:5182`, `truffles-api/tests/test_message_endpoint.py:5190`, `truffles-api/tests/test_message_endpoint.py:5191`.
- `INFERENCE`: The current reason this file exists is namespace preservation during partial router decomposition, not target-state runtime ownership.

## 3. Active Callers And Entrypoints
- `FACT`: Active webhook modules call into `_legacy.py` from inside functions, not only at module import time, which keeps the compatibility bus on the live runtime path. Representative examples: `context_manager.py` imports it before syncing expected-reply/canonical dialog state, `pending.py` imports it for continuity hooks and handover operations, `response.py` imports it for quiet-hours and expected-reply handling, and `outbox.py` imports it for trace/message/media helpers: `truffles-api/app/routers/webhook/context_manager.py:139`, `truffles-api/app/routers/webhook/pending.py:28`, `truffles-api/app/routers/webhook/pending.py:334`, `truffles-api/app/routers/webhook/response.py:249`, `truffles-api/app/routers/webhook/response.py:698`, `truffles-api/app/routers/webhook/outbox.py:172`, `truffles-api/app/routers/webhook/outbox.py:207`, `truffles-api/app/routers/webhook/outbox.py:221`.
- `FACT`: `decision.py` itself still lazily imports `_legacy` inside live helper stages, so the compatibility adapter and the frozen decision file form a cyclic dependency surface rather than a clean one-way facade: `truffles-api/app/routers/webhook/decision.py:609`, `truffles-api/app/routers/webhook/decision.py:621`, `truffles-api/app/routers/webhook/decision.py:693`.
- `FACT`: `_legacy.py` is also imported directly by tests and provider-gateway integration tests, not only by the modular webhook package: `truffles-api/tests/test_provider_gateway_integration.py:12`, `truffles-api/tests/test_message_endpoint.py:3039`, `truffles-api/tests/test_demo_salon_eval.py:168`.
- `INFERENCE`: `_legacy.py` sits at the center of a compatibility graph that spans live webhook modules, at least one live service module, `decision.py` itself, and multiple compatibility-preserving tests.

## 4. Control Path Owned By This File
- `FACT`: `_legacy.py` defines no runtime handler functions of its own; its active behavior is import-time namespace assembly through `globals().update(...)` and dynamic `__all__`: `truffles-api/app/routers/webhook/_legacy.py:80`, `truffles-api/app/routers/webhook/_legacy.py:82`, `truffles-api/app/routers/webhook/_legacy.py:87`, `truffles-api/app/routers/webhook/_legacy.py:107`.
- `FACT`: Despite that, live control fragments are mediated through this namespace. `pending.py` injects `_legacy` callbacks for conversation context, state transitions, trace recording, and manager resolution into `PendingContinuityRuntimeHooks`; `guards.py` uses `_legacy` to drive escalation/handover reuse; `response.py` uses `_legacy` for expected-reply state updates; `outbox.py` uses `_legacy` for skip-persist trace and message/media lookup helpers: `truffles-api/app/routers/webhook/pending.py:334`, `truffles-api/app/routers/webhook/pending.py:343`, `truffles-api/app/routers/webhook/guards.py:265`, `truffles-api/app/routers/webhook/guards.py:269`, `truffles-api/app/routers/webhook/response.py:694`, `truffles-api/app/routers/webhook/response.py:698`, `truffles-api/app/routers/webhook/outbox.py:207`, `truffles-api/app/routers/webhook/outbox.py:221`, `truffles-api/app/routers/webhook/outbox.py:237`.
- `FACT`: `tool_registry_service.py` also uses `_legacy` for booking missing-slot prompts and expected-reply constants when tool execution needs more user input: `truffles-api/app/services/tool_registry_service.py:1521`, `truffles-api/app/services/tool_registry_service.py:1523`, `truffles-api/app/services/tool_registry_service.py:1527`, `truffles-api/app/services/tool_registry_service.py:1932`, `truffles-api/app/services/tool_registry_service.py:1935`, `truffles-api/app/services/tool_registry_service.py:2278`, `truffles-api/app/services/tool_registry_service.py:2280`, `truffles-api/app/services/tool_registry_service.py:2283`.
- `INFERENCE`: `_legacy.py` does not own one explicit runtime stage; it owns the compatibility contract layer that many runtime stages still depend on.

## 5. Data Reads
- `FACT`: At import time `_legacy.py` reads symbols from `runtime_primitives`, `context_manager`, `response`, `chatflow_service`, `handover_owner_service`, and `decision.py`: `truffles-api/app/routers/webhook/_legacy.py:5`, `truffles-api/app/routers/webhook/_legacy.py:6`, `truffles-api/app/routers/webhook/_legacy.py:7`, `truffles-api/app/routers/webhook/_legacy.py:35`, `truffles-api/app/routers/webhook/_legacy.py:36`, `truffles-api/app/routers/webhook/_legacy.py:49`.
- `FACT`: Downstream modules use `_legacy` to read conversation/context continuity through helpers such as `_get_conversation_context(...)`, `_get_context_manager(...)`, and legacy keys like `SERVICE_CARRYOVER_KEY` / `CONSULT_CONTEXT_KEY`: `truffles-api/app/routers/webhook/guards.py:51`, `truffles-api/app/routers/webhook/guards.py:227`, `truffles-api/app/routers/webhook/context_manager.py:148`, `truffles-api/app/routers/webhook/context_manager.py:154`.
- `FACT`: Downstream modules also read expected-reply and booking prompt constants through `_legacy`, including `EXPECTED_REPLY_SERVICE`, `EXPECTED_REPLY_TIME`, `MSG_BOOKING_ASK_DATETIME`, and `MSG_BOOKING_ASK_SERVICE`: `truffles-api/app/routers/webhook/response.py:703`, `truffles-api/app/services/tool_registry_service.py:1523`, `truffles-api/app/services/tool_registry_service.py:1527`, `truffles-api/app/services/tool_registry_service.py:1935`, `truffles-api/app/services/tool_registry_service.py:2280`, `truffles-api/app/services/tool_registry_service.py:2283`.
- `INFERENCE`: `_legacy.py` widens the read surface of router-era continuity and prompt constants beyond the webhook package.

## 6. Data Writes And Side Effects
- `FACT`: The file's own side effects are namespace mutations: it populates module globals from `_SHARED_EXPORTS`, then from `_decision.__dict__`, then from manual override bindings, and finally exposes the assembled namespace through dynamic `__all__`: `truffles-api/app/routers/webhook/_legacy.py:80`, `truffles-api/app/routers/webhook/_legacy.py:82`, `truffles-api/app/routers/webhook/_legacy.py:87`, `truffles-api/app/routers/webhook/_legacy.py:107`.
- `FACT`: Because live modules call write-capable helpers through `_legacy`, the adapter keeps conversation-context writes, state transitions, trace/meta recording, and escalation side effects reachable under one ambient namespace: `truffles-api/app/routers/webhook/pending.py:336`, `truffles-api/app/routers/webhook/pending.py:337`, `truffles-api/app/routers/webhook/pending.py:339`, `truffles-api/app/routers/webhook/pending.py:340`, `truffles-api/app/routers/webhook/guards.py:278`, `truffles-api/app/routers/webhook/guards.py:279`, `truffles-api/app/routers/webhook/response.py:696`, `truffles-api/app/routers/webhook/response.py:699`, `truffles-api/app/routers/webhook/outbox.py:207`.
- `INFERENCE`: `_legacy.py` does not author runtime writes directly, but it amplifies the side-effect surface by making those helpers universally reachable as `legacy.*`.

## 7. Semantic Authority / Deterministic Authority
- `FACT`: `_legacy.py` does not call policy-core or any owner interface directly; it is a re-export surface over deterministic helpers, constants, trace/state accessors, and handover operations: `truffles-api/app/routers/webhook/_legacy.py:5`, `truffles-api/app/routers/webhook/_legacy.py:7`, `truffles-api/app/routers/webhook/_legacy.py:35`, `truffles-api/app/routers/webhook/_legacy.py:49`, `truffles-api/app/routers/webhook/_legacy.py:87`.
- `FACT`: Live modules still use `legacy` for deterministic semantic/routing work such as text normalization, lexical checks, expected-reply handling, booking prompts, context access, and class/pending routing support: `truffles-api/app/routers/webhook/pending.py:30`, `truffles-api/app/routers/webhook/policy.py:405`, `truffles-api/app/routers/webhook/booking.py:515`, `truffles-api/app/routers/webhook/response.py:699`, `truffles-api/app/routers/webhook/guards.py:286`.
- `FACT`: The manual owner-surface override block means the legacy namespace now mixes decision-era deterministic semantics with handover-owner service authority under the same compatibility alias: `truffles-api/app/routers/webhook/_legacy.py:36`, `truffles-api/app/routers/webhook/_legacy.py:87`, `truffles-api/app/routers/webhook/_legacy.py:100`.
- `INFERENCE`: `_legacy.py` is not a semantic owner; it is the deterministic authority bus that keeps old router semantics and new handover service entrypoints equally ambient to downstream modules.

## 8. Truth Carriers Touched Here
- `FACT`: `_legacy.py` exports legacy question-state and continuity constants such as `EXPECTED_REPLY_*`, `SERVICE_CARRYOVER_TTL_MESSAGES`, `SESSION_MEMORY_SHORT_TOKENS`, and `ConversationState`: `truffles-api/app/routers/webhook/_legacy.py:7`, `truffles-api/app/routers/webhook/_legacy.py:10`, `truffles-api/app/routers/webhook/_legacy.py:31`, `truffles-api/app/routers/webhook/_legacy.py:32`, `truffles-api/app/routers/webhook/_legacy.py:33`, `truffles-api/app/routers/webhook/_legacy.py:51`.
- `FACT`: Downstream modules use the `_legacy` namespace to touch conversation/context truth carriers and trace/meta carriers, for example `_get_conversation_context(...)`, `_set_conversation_context(...)`, `_set_expected_reply_context(...)`, `_record_decision_trace(...)`, and `_update_message_decision_metadata(...)`: `truffles-api/app/routers/webhook/pending.py:335`, `truffles-api/app/routers/webhook/pending.py:336`, `truffles-api/app/routers/webhook/pending.py:339`, `truffles-api/app/routers/webhook/pending.py:340`, `truffles-api/app/routers/webhook/response.py:694`, `truffles-api/app/routers/webhook/response.py:699`, `truffles-api/app/routers/webhook/trace.py:333`.
- `FACT`: `tool_registry_service.py` also emits user-facing booking continuity via `_legacy` constants, so expected-reply carriers are not confined to the webhook package: `truffles-api/app/services/tool_registry_service.py:1523`, `truffles-api/app/services/tool_registry_service.py:1527`, `truffles-api/app/services/tool_registry_service.py:1935`, `truffles-api/app/services/tool_registry_service.py:2280`, `truffles-api/app/services/tool_registry_service.py:2283`.
- `INFERENCE`: `_legacy.py` is the shared compatibility namespace through which multiple state, expected-reply, and trace/meta carriers stay live across the modular webhook stack.

## 9. Violations Against The Target Canon
- `FACT`: `_legacy.py` re-exports every non-dunder symbol from `decision.py` into one namespace via `globals()`, then dynamically exports the whole namespace through `__all__`: `truffles-api/app/routers/webhook/_legacy.py:82`, `truffles-api/app/routers/webhook/_legacy.py:85`, `truffles-api/app/routers/webhook/_legacy.py:107`.
- `FACT`: Live modules and at least one non-webhook service still depend on that broad namespace rather than on explicit narrow contracts: `truffles-api/app/routers/webhook/context_manager.py:139`, `truffles-api/app/routers/webhook/booking.py:115`, `truffles-api/app/routers/webhook/response.py:249`, `truffles-api/app/routers/webhook/pending.py:28`, `truffles-api/app/routers/webhook/outbox.py:172`, `truffles-api/app/services/tool_registry_service.py:1521`.
- `FACT`: `decision.py` still imports `_legacy` back inside live helper stages, so the compatibility layer is cyclical rather than hierarchical: `truffles-api/app/routers/webhook/decision.py:609`, `truffles-api/app/routers/webhook/decision.py:621`, `truffles-api/app/routers/webhook/decision.py:693`.
- `FACT`: Tests explicitly lock parts of the legacy export contract in place: `truffles-api/tests/test_message_endpoint.py:5190`, `truffles-api/tests/test_provider_gateway_integration.py:12`.
- `INFERENCE`: Strategic point `one control path` remains open here because `_legacy.py` keeps the old router/helper surface ambient across many modules even after ingress moved elsewhere.
- `INFERENCE`: Strategic point `deterministic only at boundaries` also remains open here because the adapter keeps decision-era lexical and continuity helpers available as a general-purpose namespace rather than as narrow boundary contracts.

## 10. Salvageable Parts
- `FACT`: The explicit manual handover-owner overrides are narrow and already point to dedicated owner-service implementations: `truffles-api/app/routers/webhook/_legacy.py:36`, `truffles-api/app/routers/webhook/_legacy.py:87`, `truffles-api/app/routers/webhook/_legacy.py:100`.
- `FACT`: The explicit `_SHARED_EXPORTS` subset is far narrower than the full decision re-export and is the closest thing this file has to a bounded contract: `truffles-api/app/routers/webhook/_legacy.py:51`, `truffles-api/app/routers/webhook/_legacy.py:78`.
- `FACT`: The three non-decision explicit helper imports (`_apply_consult_return`, `_apply_quiet_hours_notice`, `_maybe_append_booking_cta`) are already relocatable bounded leaf helpers: `truffles-api/app/routers/webhook/_legacy.py:5`, `truffles-api/app/routers/webhook/_legacy.py:6`.
- `INFERENCE`: The salvageable part is not the adapter as a whole; it is the small set of explicit narrow exports that could be moved into dedicated modules without keeping the decision namespace alive.

## 11. Demotion / Removal Candidates
- `FACT`: The dynamic `for _name, _value in _decision.__dict__.items()` re-export loop is the primary demotion target, because it is what turns `_legacy.py` into a frozen decision namespace mirror: `truffles-api/app/routers/webhook/_legacy.py:82`, `truffles-api/app/routers/webhook/_legacy.py:85`.
- `FACT`: The dynamic `__all__` export of every assembled global is also a demotion target, because it prevents this module from acting like an explicit contract surface: `truffles-api/app/routers/webhook/_legacy.py:107`.
- `FACT`: Service-layer imports from `tool_registry_service.py` are demotion targets because they leak webhook legacy constants/prompts into non-webhook execution code: `truffles-api/app/services/tool_registry_service.py:1521`, `truffles-api/app/services/tool_registry_service.py:1932`, `truffles-api/app/services/tool_registry_service.py:2278`.
- `FACT`: The cyclic imports from `decision.py` back into `_legacy.py` are removal blockers that must be cut before `decision.py` can stop being a resident compatibility warehouse: `truffles-api/app/routers/webhook/decision.py:609`, `truffles-api/app/routers/webhook/decision.py:621`, `truffles-api/app/routers/webhook/decision.py:693`.
- `INFERENCE`: Real removal requires moving the remaining live leaf helpers and constants into explicit modules first, then rewiring the highest-fanout consumers away from `_legacy.py` before deleting the dynamic re-export loop.

## 12. What This Analysis Changes In System Understanding
- `FACT`: The live blocker is no longer just `decision.py` itself; `_legacy.py` is the import bus that keeps the frozen decision/helper surface ambient across modular webhook code and a live service module.
- `FACT`: `_legacy.py` is not an adapter-only file in practice, even though the file body is small, because its dynamic namespace assembly keeps broad deterministic/state/handover helper authority available everywhere as `legacy.*`.
- `FACT`: Tests preserve parts of that legacy namespace explicitly, so cutover cannot be treated as a pure internal cleanup.
- `INFERENCE`: After this hotspot, the next honest hotspot is `truffles-api/app/routers/webhook/context_manager.py`, because it is the densest visible `_legacy` consumer and one of the main bridges between compatibility state carriers and canonical-dialog-state machinery.

## 13. Open Questions
- `UNKNOWN`: Which `_legacy` exports used by `context_manager.py`, `booking.py`, `guards.py`, `response.py`, and `pending.py` are still truly required versus already replaceable with narrow direct imports.
- `UNKNOWN`: Which tests intentionally guarantee long-term `_legacy` compatibility and which tests simply preserve obsolete surfaces accidentally.
- `UNKNOWN`: Whether `tool_registry_service.py` can stop importing `_legacy` immediately or whether some execution-time prompt/expected-reply coupling still blocks that cut.

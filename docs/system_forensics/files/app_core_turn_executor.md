# File Analysis: `truffles-api/app/core/turn_executor.py`

Status: `completed`
Baseline snapshot: `8319d9e1`
Related ledgers:
- `docs/system_forensics/ledgers/CONTROL_PATHS.md`
- `docs/system_forensics/ledgers/SEMANTIC_OWNER_MAP.md`
- `docs/system_forensics/ledgers/TRUTH_CARRIER_MATRIX.md`
- `docs/system_forensics/ledgers/STATE_SURFACE_INVENTORY.md`
- `docs/system_forensics/ledgers/DETERMINISTIC_REWRITE_LEDGER.md`
- `docs/system_forensics/ledgers/CUTOVER_DEPENDENCY_GRAPH.md`
- `docs/system_forensics/ledgers/SALVAGEABLE_COMPONENTS.md`
- `docs/system_forensics/ledgers/DO_NOT_REPEAT.md`
- `docs/system_forensics/final/SYSTEM_FINAL_ANALYSIS.md`

## 1. Role Summary
- `FACT`: `turn_executor.py` is the downstream execution hotspot for the active runtime. `ConsultantRuntime._execute_turn(...)` delegates to `TurnExecutor.execute(...)`, while `reasoning_core.py` also uses the same file to manufacture block/degrade artifacts from explicit boundary requests: `truffles-api/app/core/consultant_runtime.py:766`, `truffles-api/app/core/consultant_runtime.py:780`, `truffles-api/app/core/turn_executor.py:247`, `truffles-api/app/core/turn_executor.py:1437`, `truffles-api/app/core/turn_executor.py:1473`, `truffles-api/app/services/reasoning_core.py:121`, `truffles-api/app/services/reasoning_core.py:142`.
- `FACT`: The class docstring explicitly describes this file as a temporary typed execution seam: `TurnExecutor` “assembles the typed turn result while runtime cutover is still pending”: `truffles-api/app/core/turn_executor.py:118`.
- `INFERENCE`: This file is not just a tool executor. It is the active downstream semantic/boundary assembler for runtime execution, boundary degrade/block handling, and owner-cutover artifact construction.

## 2. Why This File Exists
- `FACT`: `execute(...)` translates a `PolicyDecision` into runtime behavior across four branches: immediate `HANDOFF`, booking confirmation, `FACT`, and `COLLECT`: `truffles-api/app/core/turn_executor.py:247`, `truffles-api/app/core/turn_executor.py:261`, `truffles-api/app/core/turn_executor.py:265`, `truffles-api/app/core/turn_executor.py:279`, `truffles-api/app/core/turn_executor.py:287`.
- `FACT`: The file also constructs typed runtime/boundary artifacts: `TurnResult`, `RuntimeExecutionResult`, `BoundaryExecutionArtifact`, `OwnerExecutionArtifact`, and `TurnOutcome`: `truffles-api/app/core/turn_executor.py:42`, `truffles-api/app/core/turn_executor.py:59`, `truffles-api/app/core/turn_executor.py:67`, `truffles-api/app/core/turn_executor.py:110`, `truffles-api/app/core/turn_executor.py:1510`, `truffles-api/app/core/turn_executor.py:1552`.
- `FACT`: It owns block/degrade request materialization by calling `TurnPlanner().build_preflight_reject(...)` / `build_controlled_degrade(...)`, `BoundaryValidator().build_*_override(...)`, and `DialogStateService().build_*_state(...)`: `truffles-api/app/core/turn_executor.py:1437`, `truffles-api/app/core/turn_executor.py:1473`.
- `INFERENCE`: Historically this file became the typed convergence point for “whatever happens after planning,” which is why it now mixes execution, boundary artifact assembly, and semantic projection.

## 3. Active Callers And Entrypoints
- `FACT`: The active runtime path calls `TurnExecutor.execute(...)` from `ConsultantRuntime._execute_turn(...)`: `truffles-api/app/core/consultant_runtime.py:766`, `truffles-api/app/core/consultant_runtime.py:780`, `truffles-api/app/core/turn_executor.py:247`.
- `FACT`: `reasoning_core.py` bypasses the active runtime and uses `TurnExecutor().build_block_boundary_artifact_from_request(...)` and `build_degrade_boundary_artifact_from_request(...)` as typed boundary factories: `truffles-api/app/services/reasoning_core.py:121`, `truffles-api/app/services/reasoning_core.py:142`, `truffles-api/app/core/turn_executor.py:1437`, `truffles-api/app/core/turn_executor.py:1473`.
- `FACT`: `build_owner_cutover_artifact(...)` exists as an exported artifact constructor in this file, while the active callers confirmed so far for the file itself are `ConsultantRuntime.execute(...)` and `reasoning_core.py` boundary requests: `truffles-api/app/core/turn_executor.py:1552`, `truffles-api/app/core/consultant_runtime.py:780`, `truffles-api/app/services/reasoning_core.py:121`.
- `UNKNOWN`: Whether `build_owner_cutover_artifact(...)` is still required on any live path or is now dead compatibility surface.

## 4. Control Path Owned By This File
- `FACT`: `execute(...)` branches by `decision.outcome` and `decision.tool_action`, then routes into `_execute_collect(...)`, `_execute_fact(...)`, or `_execute_booking_confirmation(...)`: `truffles-api/app/core/turn_executor.py:247`, `truffles-api/app/core/turn_executor.py:261`, `truffles-api/app/core/turn_executor.py:265`, `truffles-api/app/core/turn_executor.py:279`, `truffles-api/app/core/turn_executor.py:287`.
- `FACT`: `_execute_collect(...)` builds execution-time semantic/pending contracts, derives the next booking slot, and shapes a follow-up prompt plus execution meta: `truffles-api/app/core/turn_executor.py:291`, `truffles-api/app/core/turn_executor.py:298`, `truffles-api/app/core/turn_executor.py:302`, `truffles-api/app/core/turn_executor.py:355`, `truffles-api/app/core/turn_executor.py:358`.
- `FACT`: `_execute_fact(...)` merges booking slots, builds execution contracts, resolves info refs, can call pack-runtime master logic, can call the tool registry, can fall back to direct pack truth, and can merge pack semantic grounding back into the contract: `truffles-api/app/core/turn_executor.py:402`, `truffles-api/app/core/turn_executor.py:424`, `truffles-api/app/core/turn_executor.py:431`, `truffles-api/app/core/turn_executor.py:465`, `truffles-api/app/core/turn_executor.py:517`, `truffles-api/app/core/turn_executor.py:576`, `truffles-api/app/core/turn_executor.py:600`.
- `FACT`: `_execute_booking_confirmation(...)` reuses the same execution contracts, checks booking completeness, parses datetime, queries service duration, and may create an appointment via `SchedulingService`: `truffles-api/app/core/turn_executor.py:1123`, `truffles-api/app/core/turn_executor.py:1131`, `truffles-api/app/core/turn_executor.py:1136`, `truffles-api/app/core/turn_executor.py:1179`, `truffles-api/app/core/turn_executor.py:1188`.
- `FACT`: The active downstream chain is `ConsultantRuntime._execute_turn(...) -> TurnExecutor.execute(...) -> RuntimeExecutionResult.meta -> ConsultantRuntime._write_runtime_state(...) -> DialogStateService.write_runtime_payload(...)`: `truffles-api/app/core/consultant_runtime.py:766`, `truffles-api/app/core/consultant_runtime.py:780`, `truffles-api/app/core/consultant_runtime.py:828`, `truffles-api/app/core/consultant_runtime.py:837`, `truffles-api/app/core/consultant_runtime.py:842`.
- `INFERENCE`: The execution seam is still architecturally heavy because it owns both effect execution and the downstream semantic envelope that feeds state persistence.

## 5. Data Reads
- `FACT`: The executor reads planner output from `PolicyDecision`: `outcome`, `tool_action`, `intent`, `slots`, `semantic_frame`, `pending_question_contract`, `pack_refs`, `fact_refs`, `capability_refs`, and `meta.semantic_contract`: `truffles-api/app/core/turn_executor.py:247`, `truffles-api/app/core/turn_executor.py:298`, `truffles-api/app/core/turn_executor.py:424`, `truffles-api/app/core/turn_executor.py:922`, `truffles-api/app/core/turn_executor.py:947`, `truffles-api/app/core/turn_executor.py:1054`.
- `FACT`: The executor also reads runtime inputs supplied by `ConsultantRuntime`: `booking_state`, `message_text`, `client_slug`, `branch_id`, `user_name`, `user_phone`, and `now`: `truffles-api/app/core/consultant_runtime.py:780`, `truffles-api/app/core/turn_executor.py:247`, `truffles-api/app/core/turn_executor.py:402`, `truffles-api/app/core/turn_executor.py:1123`.
- `FACT`: It reads canonical and compatibility state via private/projector helpers from `DialogStateService`, including `_pending_question_from_frame(...)`, `project_pending_question_contract(...)`, and `_semantic_contract_from_frame(...)`: `truffles-api/app/core/turn_executor.py:922`, `truffles-api/app/core/turn_executor.py:930`, `truffles-api/app/core/turn_executor.py:947`.
- `FACT`: It reads external fact/tool/domain systems through `pack_runtime_service`, `tool_registry_service`, `appointment_service`, service DB lookups, and `dateparser`: `truffles-api/app/core/turn_executor.py:413`, `truffles-api/app/core/turn_executor.py:419`, `truffles-api/app/core/turn_executor.py:517`, `truffles-api/app/core/turn_executor.py:1134`, `truffles-api/app/core/turn_executor.py:1188`, `truffles-api/app/core/turn_executor.py:1293`.
- `INFERENCE`: This file sits on top of almost every downstream boundary: runtime input, state projection, pack truth, tool registry, and booking side effects.

## 6. Data Writes And Side Effects
- `FACT`: The executor emits `RuntimeExecutionResult` with `meta` payloads that carry `semantic_contract` and `pending_question_contract` through `_attach_semantic_contract_meta(...)`: `truffles-api/app/core/turn_executor.py:59`, `truffles-api/app/core/turn_executor.py:358`, `truffles-api/app/core/turn_executor.py:535`, `truffles-api/app/core/turn_executor.py:908`.
- `FACT`: `ConsultantRuntime._write_runtime_state(...)` then copies `execution.meta` into `DialogStateService.write_runtime_payload(...)`, so execution-time semantic meta becomes state input: `truffles-api/app/core/consultant_runtime.py:828`, `truffles-api/app/core/consultant_runtime.py:837`, `truffles-api/app/core/consultant_runtime.py:842`.
- `FACT`: `_execute_fact(...)` can execute real tool actions via `execute_tool_action(...)`, and `_execute_booking_confirmation(...)` can create appointments via `SchedulingService(db).create_appointment(...)`: `truffles-api/app/core/turn_executor.py:517`, `truffles-api/app/core/turn_executor.py:1188`.
- `FACT`: The file also builds typed boundary outcomes and owner-cutover outcomes via `build_block_boundary_artifact(...)`, `build_degrade_boundary_artifact(...)`, and `build_owner_cutover_artifact(...)`: `truffles-api/app/core/turn_executor.py:1365`, `truffles-api/app/core/turn_executor.py:1399`, `truffles-api/app/core/turn_executor.py:1552`.
- `INFERENCE`: The executor is not state-pure. It performs provider/tool side effects and also emits semantic meta that becomes the next state writer input.

## 7. Semantic Authority / Deterministic Authority
- `FACT`: `_build_execution_semantic_contract(...)` reconstructs execution semantics from `decision.semantic_frame` or fallback `decision.meta["semantic_contract"]`, then augments referents from entity refs, booking state, and `service_name`: `truffles-api/app/core/turn_executor.py:947`, `truffles-api/app/core/turn_executor.py:954`, `truffles-api/app/core/turn_executor.py:957`, `truffles-api/app/core/turn_executor.py:995`, `truffles-api/app/core/turn_executor.py:1017`, `truffles-api/app/core/turn_executor.py:1030`.
- `FACT`: `_build_execution_pending_question_contract(...)` rebuilds pending-question semantics from the frame and falls back to `decision.pending_question_contract`: `truffles-api/app/core/turn_executor.py:922`, `truffles-api/app/core/turn_executor.py:926`, `truffles-api/app/core/turn_executor.py:943`.
- `FACT`: `_build_tool_execution_projection(...)` deterministically reprojects concrete tool args (`service_query`, `specialist_name`, `specialist_id`, `appointment_id`) from `semantic_contract.referents` and then sanitizes them with `validate_tool_args_shape(...)`: `truffles-api/app/core/turn_executor.py:667`, `truffles-api/app/core/turn_executor.py:681`, `truffles-api/app/core/turn_executor.py:699`, `truffles-api/app/core/turn_executor.py:725`, `truffles-api/app/core/turn_executor.py:735`, `truffles-api/app/core/turn_executor.py:758`.
- `FACT`: `_resolve_fact_tool_action(...)` deterministically remaps policy-core `info` into concrete tool actions via `_POLICY_INFO_TOOL_ACTION_MAP`, while `_build_direct_policy_info_reply(...)` answers some refs directly from truth packs via `_DIRECT_INFO_TRUTH_REFS`: `truffles-api/app/core/turn_executor.py:121`, `truffles-api/app/core/turn_executor.py:130`, `truffles-api/app/core/turn_executor.py:1078`, `truffles-api/app/core/turn_executor.py:1092`.
- `FACT`: `_build_collect_prompt(...)` and booking prompt maps add deterministic wording based on `pending_question_target`, `active_question_relation`, and specialist referents: `truffles-api/app/core/turn_executor.py:137`, `truffles-api/app/core/turn_executor.py:143`, `truffles-api/app/core/turn_executor.py:372`.
- `INFERENCE`: The deterministic layer downstream of the owner is still semantically active here. It does not only validate or execute; it reconstructs contracts, reprojects arguments, chooses concrete tool dialects, and shapes follow-up meaning.

## 8. Truth Carriers Touched Here
- `FACT`: The executor reads `decision.semantic_frame`, `decision.meta.semantic_contract`, and `decision.pending_question_contract` as three semantic carriers entering downstream execution: `truffles-api/app/core/turn_executor.py:922`, `truffles-api/app/core/turn_executor.py:947`, `truffles-api/app/core/turn_executor.py:957`.
- `FACT`: It also merges `booking_state` into execution semantics, meaning booking payload remains a live continuity carrier during execution-time contract construction: `truffles-api/app/core/turn_executor.py:298`, `truffles-api/app/core/turn_executor.py:424`, `truffles-api/app/core/turn_executor.py:995`, `truffles-api/app/core/turn_executor.py:1017`.
- `FACT`: The executor emits `semantic_contract` and `pending_question_contract` again inside `RuntimeExecutionResult.meta`, which `ConsultantRuntime` then forwards into state persistence: `truffles-api/app/core/turn_executor.py:908`, `truffles-api/app/core/turn_executor.py:1150`, `truffles-api/app/core/consultant_runtime.py:837`, `truffles-api/app/core/consultant_runtime.py:842`.
- `FACT`: Pack-runtime grounding is merged into execution-time `semantic_contract` through `_merge_pack_grounding_semantic_contract(...)`: `truffles-api/app/core/turn_executor.py:866`, `truffles-api/app/core/turn_executor.py:487`, `truffles-api/app/core/turn_executor.py:600`.
- `INFERENCE`: This file proves that semantic continuity still survives after planning in multiple downstream carriers, not only in the canonical owner frame.

## 9. Violations Against The Target Canon
- `FACT`: The executor still reconstructs execution semantics after owner output via `_build_execution_semantic_contract(...)` and `_build_execution_pending_question_contract(...)`: `truffles-api/app/core/turn_executor.py:922`, `truffles-api/app/core/turn_executor.py:947`.
- `FACT`: It also still projects concrete tool bindings from semantic referents inside the executor, even though tool binding was already split into `policy_tool_projector` upstream: `truffles-api/app/core/turn_executor.py:667`, `truffles-api/app/core/policy_tool_projector.py:117`.
- `FACT`: Growth vocabulary is still partially hardcoded here through `_POLICY_INFO_TOOL_ACTION_MAP`, `_DIRECT_INFO_TRUTH_REFS`, and booking prompt maps: `truffles-api/app/core/turn_executor.py:121`, `truffles-api/app/core/turn_executor.py:130`, `truffles-api/app/core/turn_executor.py:137`, `truffles-api/app/core/turn_executor.py:143`.
- `FACT`: Boundary artifact factories in this file instantiate planner and state services directly from execution code, which keeps boundary/runtime ownership mixed in one hotspot: `truffles-api/app/core/turn_executor.py:1437`, `truffles-api/app/core/turn_executor.py:1473`.
- `INFERENCE`: Strategic point `pure boundary runtime` remains `open` because this file still owns post-owner semantic reconstruction, tool-binding dialect decisions, and prompt/action rewrites.
- `INFERENCE`: Strategic point `capability-manifest-centered growth` remains `open` because executor-level tool/action maps and prompt vocabularies still live in core code.

## 10. Salvageable Parts
- `FACT`: The typed result/outcome surfaces are reusable: `TurnResult`, `RuntimeExecutionResult`, `BoundaryExecutionArtifact`, `OwnerExecutionArtifact`, and `TurnOutcome` assembly helpers: `truffles-api/app/core/turn_executor.py:42`, `truffles-api/app/core/turn_executor.py:59`, `truffles-api/app/core/turn_executor.py:67`, `truffles-api/app/core/turn_executor.py:110`, `truffles-api/app/core/turn_executor.py:223`, `truffles-api/app/core/turn_executor.py:1510`, `truffles-api/app/core/turn_executor.py:1552`.
- `FACT`: Schema-driven tool-arg sanitation is reusable, even if projection location is not: `_sanitize_projected_tool_args(...)` uses `validate_tool_args_shape(...)` rather than ad-hoc field dropping: `truffles-api/app/core/turn_executor.py:749`, `truffles-api/app/core/turn_executor.py:758`.
- `FACT`: Pack-grounding normalization/provenance shaping is reusable if moved into a dedicated projector/boundary layer: `_normalize_semantic_entity_refs(...)`, `_normalize_semantic_referents(...)`, `_normalize_grounding_provenance(...)`, `_pack_semantic_grounding(...)`, `_merge_pack_grounding_semantic_contract(...)`: `truffles-api/app/core/turn_executor.py:772`, `truffles-api/app/core/turn_executor.py:811`, `truffles-api/app/core/turn_executor.py:829`, `truffles-api/app/core/turn_executor.py:846`, `truffles-api/app/core/turn_executor.py:866`.
- `INFERENCE`: The salvageable core is the typed execution/outcome contract and explicit side-effect seams, not the semantic reconstruction logic currently embedded around them.

## 11. Demotion / Removal Candidates
- `FACT`: `_build_execution_semantic_contract(...)` and `_build_execution_pending_question_contract(...)` are extraction candidates because they rebuild semantic meaning downstream: `truffles-api/app/core/turn_executor.py:922`, `truffles-api/app/core/turn_executor.py:947`.
- `FACT`: `_build_tool_execution_projection(...)` and `_resolve_fact_tool_action(...)` are extraction candidates because they translate semantic meaning into concrete runtime/tool dialect inside the executor: `truffles-api/app/core/turn_executor.py:667`, `truffles-api/app/core/turn_executor.py:1078`.
- `FACT`: Hardcoded `_POLICY_INFO_TOOL_ACTION_MAP`, `_DIRECT_INFO_TRUTH_REFS`, `_BOOKING_PROMPTS`, and `_BOOKING_VERIFICATION_PROMPTS` are demotion candidates if growth is to become manifest/data-driven: `truffles-api/app/core/turn_executor.py:121`, `truffles-api/app/core/turn_executor.py:130`, `truffles-api/app/core/turn_executor.py:137`, `truffles-api/app/core/turn_executor.py:143`.
- `FACT`: The boundary request builders are useful compatibility seams today, but they should not continue to live beside semantic projection logic in the long-term target architecture: `truffles-api/app/core/turn_executor.py:1437`, `truffles-api/app/core/turn_executor.py:1473`.
- `INFERENCE`: A real extraction likely separates typed execution/boundary artifacts from semantic contract projectors and core prompt/action maps.

## 12. What This Analysis Changes In System Understanding
- `FACT`: The downstream runtime after policy-core ownership is not a pure executor. It still reconstructs semantic contracts, reprojects tool arguments, merges grounding, chooses concrete tool dialects, and writes semantic meta back into state persistence.
- `FACT`: `TurnExecutor` is also a shared compatibility surface for reasoning-core block/degrade artifacts, not just the active runtime executor.
- `INFERENCE`: After the first four hotspot analyses, `one semantic owner`, `one canonical semantic state`, and `pure boundary runtime` are all still blocked by downstream reconstruction stages, not only by upstream owner or state code.
- `INFERENCE`: The next honest hotspot is `truffles-api/app/core/turn_planner.py`, because planner-level decision shaping is now the remaining unanalysed core seam between runtime orchestration and the owner gateway.

## 13. Open Questions
- `UNKNOWN`: Which parts of execution-time semantic contract augmentation are product-essential and which are only compatibility scaffolding for the current runtime/state writer.
- `UNKNOWN`: Whether `build_owner_cutover_artifact(...)` is still needed or can be removed after planner/runtime cutover work.
- `UNKNOWN`: How much of the executor’s hardcoded tool/prompt vocabulary can move into manifests or dedicated projection modules without breaking current pack behavior.

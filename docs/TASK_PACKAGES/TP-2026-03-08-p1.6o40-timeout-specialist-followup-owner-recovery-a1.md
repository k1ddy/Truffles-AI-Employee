# TP-2026-03-08-p1.6o40-timeout-specialist-followup-owner-recovery-a1.md

- Название/цель: закрыть surfaced `r41` runtime blocker, где booking specialist follow-up под активным `expected_reply_type=time` корректно проходил в non-timeout path после `P1.6o39`, но на policy-core timeout-degrade всё ещё схлопывался в `handoff`/generic retry path вместо bounded specialist-followup owner recovery.
- Parent TP: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Canon refs: `AGENTS.md`, `STATE.md`, `STRUCTURE.md`, `STRATEGY/REQUIREMENTS.md`, `docs/runbooks/BOOKING_CONFIRM_VERIFY.md`, `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o39-specialist-followup-hint-owner-recovery-a1.md`
- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect remove worktree + branch after merge`

## One web search (mandatory before implementation)

- Query: `site:rasa.com/docs/rasa/forms fallback interruption requested slot preserve context`
- Time (UTC): `2026-03-08T14:42:58Z`
- Sources:
  - `https://rasa.com/docs/rasa/forms/`
- Ready solutions found:
  - active requested-slot state should survive unhappy-path interruptions/fallbacks as long as the assistant can recover the user operation and resume the requested slot;
  - fallback/rejection should not force loop exit when the turn can be mapped back to the active slot contract.
- Decision (`reuse/integrate/build`): `reuse + integrate`
  - reuse existing timeout-degrade booking collect guard;
  - reuse existing `extract_specialist_hint_llm` bounded hint stage;
  - integrate a specialist-followup recovery branch before timeout retry exhaustion escalates the booking collect.
- Rejected options:
  - escalating to manager on every second timeout even when the user provided recoverable specialist preference;
  - phrase/regex matching for `к <name>` in runtime core;
  - weakening scenario expectations to accept timeout handoff for recoverable specialist follow-up.

## Root cause (mandatory)

- Symptom:
  - guarded `dev L2` run `p1.6o6-l2-dev-20260308-a1-r41` no longer fails on `P1.6o39`, but stops on dialog `1`, turn `9`, text `Я хочу записаться к Айгерим.` with `expected_meta_mismatch` and `expected_trace_miss`.

- Minimal reproduction:
  1. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r41/scenarios.json` for dialog `1`, turn `9`
  2. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r41/responses.jsonl` and `trace_bundle.jsonl` for the same turn
  3. note that prior turns preserved active `expected_reply_type=time`, but final `decision_meta` becomes `action=escalate`, `intent=policy_timeout_degrade`
  4. inspect timeout degrade branch in `truffles-api/app/routers/webhook/decision.py` and confirm that retry-exhausted booking collect escalates before any specialist-followup recovery path exists

- Evidence:
  - scenario expects:
    - `pending_question_target=specialist`
    - `pending_question_interaction=booking_specialist_followup`
    - resume contract `expected_reply_type=time`
  - `r41` final meta shows:
    - `action=escalate`
    - `intent=policy_timeout_degrade`
    - `policy_core_timeout_retry_exhausted=true`
    - missing specialist-followup trace/meta
  - `P1.6o39` deterministic regression proves the same family works when policy-core returns a collect payload; therefore the remaining gap is timeout-degrade specific

- Five Whys:
  1. Why did `r41` fail: timeout-degrade escalated instead of preserving specialist-followup collect.
  2. Why did timeout-degrade escalate: retry exhaustion in booking collect executes before any specialist-followup recovery.
  3. Why is recovery missing there: current timeout-special handling exists for `ask_about_requested_slot(time)` but not for named specialist follow-up under active `time` collect.
  4. Why did `P1.6o39` not cover it: that fix only handled the normal collect path with missing referent, not policy-core timeout fallback.
  5. Why is this not phrase-fitting: the missing axis is runtime owner recovery under timeout-degrade for a bounded semantic family, not one literal surface form.

- Root cause statement:
  - timeout-degraded booking collect has a recovery branch for time-slot questions but no equivalent recovery for named specialist follow-up while the active requested slot remains `time`; once timeout retry budget is exhausted, the runtime escalates instead of preserving the booking collect contract.

- Fix mechanism:
  - in timeout-degrade booking collect, detect recoverable named specialist follow-up using the existing bounded specialist hint stage;
  - if recovered, persist specialist preference into booking state, keep `expected_reply_type=time`, and emit canonical `booking_specialist_followup` evidence from `policy_core_guard`;
  - bypass timeout escalation for that bounded recovery path only;
  - cover it with deterministic timeout regression, then rerun guarded `dev L2`.

## Release safety

- Strategy: bounded timeout recovery before retry-limit escalation, then guarded rerun
- Go signals:
  - timeout specialist follow-up preserves active `time` collect
  - existing timeout escalation for generic degraded collect still works
- No-go signals:
  - new generic timeout waiver for unrelated booking turns
  - loss of `P1.6o39` non-timeout specialist-followup behavior
- Rollback verification:
  - revert only the timeout specialist-followup recovery block if it creates false positives in other timeout degraded turns

## Invariant

1. `P1.6o39` non-timeout specialist-followup path stays closed.
2. Generic timeout-degraded booking collect still escalates on exhausted retry budget when no bounded recovery exists.
3. Active requested slot stays `time`; specialist follow-up must not rewrite collect order.
4. No phrase/regex routing is added to runtime core.

## Scope

- `truffles-api/app/routers/webhook/decision.py`
- `truffles-api/tests/test_message_endpoint.py`
- `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o40-timeout-specialist-followup-owner-recovery-a1.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`

## Out of scope

- generic timeout policy redesign
- scenario taxonomy changes
- baseline/lock promotion
- infra tool-evidence remediation

## Touch-list

- `truffles-api/app/routers/webhook/decision.py`
- `truffles-api/tests/test_message_endpoint.py`
- `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o40-timeout-specialist-followup-owner-recovery-a1.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`

## Plan (1..N)

1. Freeze `r41` as bounded timeout runtime evidence.
2. Reuse existing specialist hint extraction inside timeout-degraded booking collect before retry-limit escalation.
3. Persist specialist preference and emit canonical `booking_specialist_followup` timeout-recovery evidence.
4. Add deterministic regression covering exhausted-retry specialist follow-up.
5. Run fresh guarded `dev L2` rerun to verify closure and expose the next honest blocker.

## DoD

1. `r41` blocker is documented as timeout owner recovery gap, not scenario drift.
2. Timeout-degraded named specialist follow-up under active `time` collect emits `booking_specialist_followup` evidence and keeps `expected_reply_type=time`.
3. Existing `P1.6o39` regression remains green.
4. Existing generic timeout escalation regression remains green.
5. Targeted deterministic runtime tests pass.
6. `git diff --check` stays clean.
7. Master/program/proof-bundle docs point to the fresh guarded rerun as the next expensive step.

## Checks

- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r41 --status done --strict-artifacts`
- `pytest -q truffles-api/tests/test_message_endpoint.py -k "booking_specialist_followup or timeout_specialist_followup or timeout_pending_slot_question"`
- `pytest -q truffles-api/tests/test_message_endpoint.py::test_llm_policy_core_booking_specialist_followup_persists_preference_and_resumes_name_collect truffles-api/tests/test_message_endpoint.py::test_llm_policy_core_booking_specialist_followup_recovers_missing_specialist_preference_via_hint truffles-api/tests/test_message_endpoint.py::test_llm_policy_core_timeout_specialist_followup_keeps_time_collect --junitxml=/tmp/booking_quality/p1.6o40-l1-junit.xml`
- `git diff --check`

## Execution status

- `in_progress`

## Evidence

1. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r41/summary.json`
2. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r41/brief.md`
3. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r41/scenarios.json`
4. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r41/responses.jsonl`
5. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r41/trace_bundle.jsonl`
6. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r41/manual_audit.md`
7. `/tmp/booking_quality/p1.6o40-l1-junit.xml`

## Rollback

1. Revert only the timeout specialist-followup recovery block and its tests if it creates false recoveries.
2. Keep `P1.6o6` blocked; do not reinterpret `r41` as proof closure.

## No-go

- no phrase-fitting in runtime core
- no oracle weakening for timeout specialist follow-up
- no generic suppression of timeout escalation for unrelated turns
- no guarded rerun before targeted regressions are green

## Risks/Blockers

1. Timeout specialist recovery still depends on bounded hint extraction budget; if budget is exhausted, the path must remain observable and fail closed.
2. Acceptance remains infra-invalid for unrelated tool-evidence reasons; this block only closes the surfaced semantic family.

## Residual architecture debt (mandatory)

- Current residuals accepted in this block:
  - timeout-degrade recovery remains family-specific rather than a fully generalized pending-question timeout framework
- Why not in this block:
  - this block closes the surfaced named specialist follow-up timeout family only
- Risk if deferred:
  - adjacent timeout families can continue surfacing one by one under the same retry-limit boundary
- Linked follow-up Task Package(s):
  - `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`
- Expiry/trigger to stop deferral:
  - next timeout-degrade family after `r42`

## Next-block contract (mandatory)

- Next block objective:
  - rerun guarded `dev L2` to verify that timeout specialist follow-up no longer surfaces and expose the next honest blocker family
- First deterministic check command:
  - `pytest -q truffles-api/tests/test_message_endpoint.py::test_llm_policy_core_booking_specialist_followup_persists_preference_and_resumes_name_collect truffles-api/tests/test_message_endpoint.py::test_llm_policy_core_booking_specialist_followup_recovers_missing_specialist_preference_via_hint truffles-api/tests/test_message_endpoint.py::test_llm_policy_core_timeout_specialist_followup_keeps_time_collect --junitxml=/tmp/booking_quality/p1.6o40-l1-junit.xml && git diff --check`
- Blocked-by conditions:
  - targeted specialist-followup regressions are not green
  - guarded runtime preflight for the fresh rerun is invalid
  - `r41` audit/docs are not synced
- Owner role for closure:
  - `Hands` for bounded runtime closure + rerun evidence, `Top Architect + Brain` for proof-bundle promotion

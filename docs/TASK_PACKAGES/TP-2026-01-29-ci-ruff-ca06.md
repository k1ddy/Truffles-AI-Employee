# TP-2026-01-29-ci-ruff-ca06

## Название/цель
Устранить красный CI в run `21468288249` (ruff import order + CA06 consult mismatch) без изменения канонов.

## Canon refs
- `AGENTS.md` (Stop-the-line, Task Package, Fitness Functions)
- `STATE.md` (STOP-LINE: CI красный блокирует прогресс)
- `SPECS/CONSULTANT.md`
- `SPECS/SYSTEM_REFERENCE.md` (CA-Matrix / livecheck SOP)
- CA_ID: CA06

## Invariant
- Consult-first + pack-first без генерации новых фактов.
- decision_meta и decision_trace пишутся на ранних возвратах.
- Outbox idempotency и отсутствие оркестрации в entrypoints.

## Scope
- Починить ruff I001 в `app/routers/webhook/decision.py`.
- Разобрать и устранить причину CA06 consult short_circuit mismatch.

## Out of scope
- Новые фичи/поведенческие изменения вне CA06.
- Миграции БД, изменения канонов/политик без DEC.

## Touch-list
- `truffles-api/app/routers/webhook/decision.py`
- `truffles-api/app/routers/webhook/*` (если нужно для CA06)
- `truffles-api/app/consult/*`
- `truffles-api/tests/*`
- `docs/SESSIONS/*` (evidence)

## Plan
1) Создать сессию + worktree.
2) Скачать артефакты livecheck из CI run `21468288249`, локализовать точную причину mismatch.
3) Исправить ruff import order в `decision.py`.
4) Устранить CA06 mismatch (минимальный фикс в коде/тестах/конфиге).
5) Прогнать checks и зафиксировать evidence.

## DoD
- `ruff check app tests` зелёный.
- CA06 mismatch устранён (CI/livecheck подтверждён).
- Evidence записан в session log; `STATE.md` обновит Brain/Top Architect.

## Checks
- `cd truffles-api && ruff check app tests`
- `pytest -q truffles-api/tests/test_message_endpoint.py -k consult`
- (если allowlist доступен) `python3 ops/diagnose.py livecheck-auto --suite ca06-consult --client-slug demo_salon`

## Evidence
- CI run URL (новый зелёный run).
- Логи ruff + ссылка на livecheck artifact/trace bundle.
- Session log `docs/SESSIONS/SESSION-2026-01-29-ci-ruff-ca06-a2.md`.

## Rollback
- `git revert COMMIT_SHA` или откат ветки.

## No-go
- Изменения канонов/политик без DEC.
- Любые правки `_legacy.py`.
- Очистка БД/trace ради evidence.

## Риски/блокеры
- Livecheck зависит от allowlist и доступности провайдера; при блоке использовать CI livecheck artifact.

## Branch / Worktree
- Branch: `feat/2026-01-29-ci-ruff-ca06-a2`
- Worktree: `/home/zhan/worktrees/2026-01-29-ci-ruff-ca06-a2`
- Base ref: `origin/main`
- Merge policy: merge from base (no rebase)
- Cleanup: Brain/Top Architect после merge

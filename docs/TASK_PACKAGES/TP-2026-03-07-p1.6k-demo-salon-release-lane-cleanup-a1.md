# TP-2026-03-07-p1.6k-demo-salon-release-lane-cleanup-a1

- Название/цель: после `budget-stop` очистить `demo_salon` release lane от patch-fitting diff и вернуть критический путь к одному честному `dev L2`, не трогая уже закрытые boundary/owner fixes.
- Parent TP: `docs/TASK_PACKAGES/TP-2026-03-06-p1.6-local-realism-canonical-lock-a1.md`
- Canon refs: `AGENTS.md`, `STATE.md`, `STRATEGY/REQUIREMENTS.md`, `TECH.md`, `docs/runbooks/BOOKING_CONFIRM_VERIFY.md`, `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`

## One web search (mandatory before implementation)

- Query: `NIST SP 800-207 policy decision point policy enforcement point`
- Time (UTC): `2026-03-07T02:48:57Z`
- Sources:
  - `https://csrc.nist.gov/pubs/sp/800/207/final`
  - `https://csrc.nist.gov/glossary/term/policy_decision_point`
  - `https://csrc.nist.gov/glossary/term/policy_enforcement_point`
- Ready solutions found:
  - decision owner and enforcement boundary must stay separate
  - enforcement validates or blocks contract execution, but must not become an alternate semantic decider
- Decision (`reuse/integrate/build`): `integrate + build`
  - integrate the PDP/PEP separation as a release-lane cleanup criterion
  - build a local cleanup patch because there is no reusable component that can automatically separate systemic fixes from patch-fitting in this diff
- Rejected options:
  - keep phrase/lexicon routing in release lane as a temporary acceptance shortcut
  - continue expensive `dev L2` reruns before diff cleanup
  - mark demo-specific heuristic additions as acceptable just because they live in data files

## Root cause (mandatory)

- Symptom:
  - `P1.6` hit `budget-stop` without producing a fresh canonical `demo_salon` readiness verdict
  - the diff accumulated lexical/prompt/demo-specific heuristic additions that fit observed failures but did not improve the release lane systemically
- Minimal reproduction:
  1. inspect `docs/TASK_PACKAGES/TP-2026-03-06-p1.6-local-realism-canonical-lock-a1.md`
  2. inspect the current diff in:
     - `truffles-api/app/knowledge/generic/SYSTEM_LEXICONS.yaml`
     - `truffles-api/app/knowledge/demo_salon/SALON_TRUTH.yaml`
     - `truffles-api/app/services/demo_salon_knowledge.py`
     - `truffles-api/app/services/pack_runtime_service.py`
     - `truffles-api/app/services/pack_runtime_neutral_adapter.py`
     - `truffles-api/app/routers/webhook/info.py`
     - `prompts/llm_policy_core.md`
  3. compare those changes to boundary/owner fixes in:
     - `truffles-api/app/routers/webhook/decision.py`
     - `truffles-api/app/services/tool_registry_service.py`
     - `truffles-api/app/services/booking_transition_owner.py`
- Evidence:
  - `docs/TASK_PACKAGES/TP-2026-03-06-p1.6-local-realism-canonical-lock-a1.md`
    - latest expensive loop ended as partial/non-canonical evidence after `budget-stop`
  - diff audit on `2026-03-07` split the worktree into:
    - `keep`: fact guard, slot completeness/order, pending snapshot hygiene, contact minimum, booking write boundary, phone continuity
    - `quarantine`: phrase-driven `master/price/parking/hours` additions in lexicons, prompt examples, and demo/runtime heuristics
- Five Whys:
  1. Why did budget burn without a clean verdict: expensive runs resumed while the release lane still carried patch-fitting drift.
  2. Why was that drift harmful: it made each rerun partly validate the new heuristic instead of the architectural fix.
  3. Why could that happen: there was no explicit cleanup gate between blocker-fix extraction and the next expensive run.
  4. Why is that a contract problem: acceptance started measuring a moving heuristic target instead of the intended boundary/owner invariants.
  5. Why must cleanup happen now: `demo_salon` is the only active release lane, so its runtime must prove readiness without temporary lexical scaffolding.
- Root cause statement:
  - after the systemic fixes landed, the `demo_salon` lane still contained a second layer of patch-fitting heuristics and prompt examples; without removing them, the next expensive run would not provide a trustworthy readiness signal.
- Fix mechanism:
  - quarantine/remove patch-fitting changes from runtime, knowledge, and prompt layers
  - keep only contract-boundary and single-owner fixes
  - update docs so the next allowed expensive action is exactly one fresh `demo_salon` `dev L2`

## Invariant

1. `demo_salon` remains the only heavy acceptance lane.
2. Deterministic boundaries keep validating/blocking contracts, not deciding semantics through phrase routing.
3. Already-closed fail-closed fixes stay intact: fact evidence linkage, booking contact minimum, transactional write boundary, pending snapshot hygiene, slot completeness/order.
4. No new expensive `dev/acceptance` run starts before cleanup is complete and targeted deterministic checks are green.

## Scope

- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-06-p1.6-local-realism-canonical-lock-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6k-demo-salon-release-lane-cleanup-a1.md`
- `docs/runbooks/BOOKING_CONFIRM_VERIFY.md`
- `prompts/llm_policy_core.md`
- `truffles-api/app/knowledge/generic/SYSTEM_LEXICONS.yaml`
- `truffles-api/app/knowledge/demo_salon/SALON_TRUTH.yaml`
- `truffles-api/app/services/demo_salon_knowledge.py`
- `truffles-api/app/services/pack_runtime_service.py`
- `truffles-api/app/services/pack_runtime_neutral_adapter.py`
- `truffles-api/app/services/info_signal_service.py`
- `truffles-api/app/routers/webhook/info.py`
- tests coupled to the removed heuristics

## Out of scope

- fresh `lock/replay/canary/full`
- any new `P6` or multi-pack work
- new heuristics, lexicons, hardcodes, regex routing, or observed-case examples as a substitute for boundary fixes

## Plan (1..N)

1. Update master TP and runbook to codify the cleanup gate after `budget-stop`.
2. Remove patch-fitting runtime/knowledge/prompt deltas from the `demo_salon` release lane.
3. Realign or delete tests that only prove those heuristics.
4. Run targeted deterministic checks on the cleaned lane.
5. Hand off to one fresh `demo_salon` `dev L2` as the next block.

## DoD

1. Quarantine-set lexical/prompt/demo-specific heuristic changes are removed from the active release lane.
2. Boundary/owner fixes remain in place and targeted tests stay green.
3. Docs clearly state that the next expensive action is one fresh `demo_salon` `dev L2`, not another broad debug loop.

## Checks

- `ruff check truffles-api/app/services/demo_salon_knowledge.py truffles-api/app/services/pack_runtime_service.py truffles-api/app/services/pack_runtime_neutral_adapter.py truffles-api/app/services/info_signal_service.py truffles-api/app/routers/webhook/info.py`
- `pytest -q truffles-api/tests/test_pack_runtime_service.py`
- `pytest -q truffles-api/tests/test_demo_salon_eval.py`
- `pytest -q truffles-api/tests/test_message_endpoint.py -k "fact_guard or collect_slot_order or booking_confirmation_pending_skips_policy_core_and_stays_bot_active"`
- `git diff --check`

## Execution status

- `done`
- Cleanup patch itself is applied and `demo_salon` parking follow-up regression (`E508`) is closed on local deterministic evidence.
- Follow-up `P1.6l` closed the remaining red `LLM policy core` family, so the lane is now restored to green deterministic readiness before the next fresh guarded `dev L2`.

## Evidence

1. diff audit from `2026-03-07`
2. updated TP and runbook references
3. targeted deterministic test outputs from this block
4. local deterministic evidence after cleanup patch:
   - `pytest -q truffles-api/tests/test_pack_runtime_service.py` -> `17 passed`
   - `pytest -q truffles-api/tests/test_cross_domain_signal_contract_suite.py` -> `2 passed`
   - `pytest -q truffles-api/tests/test_booking_quality_response_guard.py` -> `59 passed`
   - `pytest -q truffles-api/tests/test_booking_chaos_dialogs.py` -> `1 passed`
   - `pytest -q truffles-api/tests/test_demo_salon_eval.py` -> `17 passed`
   - targeted `pytest -q truffles-api/tests/test_message_endpoint.py -k "fact_guard_reason_requires_evidence_refs_for_fact_source or policy_collect_interrupt_arbitration_rewrites_master_query_to_info or policy_collect_interrupt_arbitration_rewrites_price_question_to_info or intent_queue_choice_pricing_replies_and_updates_queue"` -> `4 passed`
   - full `pytest -q truffles-api/tests/test_message_endpoint.py` -> `11 failed, 287 passed`
5. follow-up closure evidence from `P1.6l`:
   - `pytest -q truffles-api/tests/test_pack_runtime_service.py` -> `19 passed`
   - `pytest -q truffles-api/tests/test_message_endpoint.py` -> `299 passed, 2 warnings`
   - `pytest -q truffles-api/tests/test_demo_salon_eval.py` -> `17 passed`
   - `ruff check truffles-api/app/services/pack_runtime_service.py truffles-api/tests/test_pack_runtime_service.py truffles-api/tests/test_message_endpoint.py` -> `All checks passed!`
   - `git diff --check` -> clean
6. E508 forensic reproduction after fix:
   - final response includes `Парковка: Бесплатная парковка во дворе, обычно 5–6 мест.`
   - `decision_meta.info_sections=["address","hours","parking"]`
   - stored `class_carryover.intents=["location","hours","parking"]`

## Rollback

1. revert the cleanup patch for the specific runtime/knowledge/prompt files
2. keep the documentation note that expensive reruns remain blocked until cleanup is either restored or explicitly waived by `Top Architect + Brain`

## No-go

- no new expensive rerun before cleanup checks are green
- no phrase/regex/lexicon additions as the primary fix mechanism
- no acceptance decision from partial or invalid runs
- no claim that demo-only heuristic gains prove system readiness

## Risks/Blockers

1. removing heuristic drift may uncover a real remaining semantic-owner bug on the next fresh `dev L2`
2. some existing tests may be over-coupled to the heuristic layer and will need contract realignment, not blanket deletion
3. next expensive step is still blocked by `P1.6` budget/open-window constraints, but no longer by deterministic `L1` red

## Residual architecture debt (mandatory)

- Current residuals accepted in this block:
  - no fresh post-cleanup `dev L2` evidence yet
  - `P1.7` still not created
  - `P6` remains deferred and blocked by evidence
- Why not in this block:
  - this block is only for release-lane cleanup and deterministic proof that the lane is ready for one new expensive run
- Risk if deferred:
  - the next expensive run would still be polluted by patch-fitting drift and would not give a trustworthy readiness answer
- Linked follow-up Task Package(s):
  - `docs/TASK_PACKAGES/TP-2026-03-06-p1.6-local-realism-canonical-lock-a1.md`
  - `docs/TASK_PACKAGES/TP-2026-03-07-p1.6l-llm-policy-core-message-endpoint-family-a1.md`
  - `TBD` for `P1.7 Acceptance replay + regression gate`
- Expiry/trigger to stop deferral:
  - before any fresh `demo_salon` `dev L2`

## Next-block contract (mandatory)

- Next block objective:
  - run one fresh guarded `demo_salon` `dev L2` on the cleaned lane and decide whether `P1.6` can move from `budget-blocked` to `lock-ready`
- First deterministic check command:
  - `pytest -q truffles-api/tests/test_message_endpoint.py && pytest -q truffles-api/tests/test_demo_salon_eval.py`
- Blocked-by conditions:
  - missing guarded `dev L2` budget window / run-id / `PG checklist`
  - missing `OPENAI_API_KEY` / judge access for the guarded run
  - runtime `http://127.0.0.1:18184` not refreshed to the current worktree state
- Owner role for closure:
  - `Top Architect + Brain`

# TP-2026-03-14-p1.6o194-post-p1.6o193-proof-lane-a1

- Название/цель: выполнить один свежий guarded `dev L2` rerun после детерминированного закрытия `P1.6o193`, чтобы доказать, что evidenceful scenario-generation timeout blocker теперь несёт concrete inner error detail и truthfully surfaced следующий admissible result.

## Canon refs

- Binding roadmap: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o135-beauty-salon-canonical-control-plane-roadmap-a1.md`
- Prior deterministic closure: `docs/TASK_PACKAGES/TP-2026-03-14-p1.6o193-scenario-generation-progress-error-detail-a1.md`
- Blocked proof lane: `docs/TASK_PACKAGES/TP-2026-03-14-p1.6o192-post-p1.6o191-proof-lane-a1.md`
- Parent closure program: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Main remediation: `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- State baseline: `STATE.md`

## Git context

- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `origin/main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect` remove branch + worktree after merge

## Root cause (mandatory)

- Symptom:
  - `P1.6o193` is deterministically green, but there is still no fresh strict-audited proof showing whether scenario-generation timeout can now surface the concrete inner error detail needed for the next deterministic slice.
- Minimal reproduction:
  1. finish deterministic closure of `P1.6o193`;
  2. verify runtime parity on `:18184` against current `HEAD`;
  3. run exactly one fresh guarded `dev L2` rerun with strict artifact audit;
  4. classify whether proof is now admissible or another blocker/class surfaces with bounded inner error detail.
- Evidence:
  - `docs/TASK_PACKAGES/TP-2026-03-14-p1.6o192-post-p1.6o191-proof-lane-a1.md`
  - `docs/TASK_PACKAGES/TP-2026-03-14-p1.6o193-scenario-generation-progress-error-detail-a1.md`
  - `/tmp/booking_quality/p1.6o192-l2-dev-20260314-a1-r1/summary.json`
  - `/tmp/booking_quality/p1.6o192-l2-dev-20260314-a1-r1/manual_audit.json`
- Five Whys:
  1. Why run a rerun now? Because deterministic observability closure alone does not prove the blocker is now precise enough to route the next slice.
  2. Why only one rerun? Because the canonical process requires one fresh strict-audited rerun before routing the next class.
  3. Why guarded? Because proof validity still depends on preflight, parity, and artifact integrity gates.
  4. Why strict audit? Because `dev` proof still needs truthful `infra_valid` / `semantic_valid` classification before any next block is admitted.
  5. Why not route a new class from `P1.6o193` without rerun? Because `P1.6o193` only upgrades timeout observability; it does not itself generate semantic runtime evidence.
- Root cause statement:
  - after `P1.6o193`, the missing evidence is one fresh strict-audited rerun showing whether the scenario-generation timeout blocker now carries the concrete inner error detail needed for the next deterministic slice or semantic family.
- Fix mechanism:
  - run one guarded rerun, strict-audit artifacts, then truthfully route the next admissible block from the surfaced result.

## Reuse-first decision (mandatory)

- Reuse:
  - existing guarded `dev L2` proof contour
  - strict artifact audit path
  - runtime parity check against `/admin/version.git_commit`
  - progress/error-detail instrumentation from `P1.6o191` and `P1.6o193`
- Integrate:
  - no code changes in this TP unless parity/preflight exposes a new infra-only blocker
- Build:
  - no new proof harness or scenario set

## Release safety

- Rollout: proof-only; no runtime changes in this TP
- Go signal:
  - fresh rerun exists, strict audit is complete, and surfaced result is classified truthfully
- No-go signal:
  - parity mismatch on `:18184`
  - invalid/incomplete artifacts
  - any attempt to route a new class from a non-audited run
- Rollback:
  - discard the run if preflight/parity/audit invalidate it; keep `P1.6o193` as the active deterministic closure

## Invariant

1. No runtime or oracle weakening in this TP.
2. Exactly one fresh guarded rerun.
3. No class routing from invalid or incomplete runs.
4. Keep `P1.6o193` deterministic closure intact.

## Scope

- parity-check current runtime `:18184`
- run one guarded `dev L2` rerun
- strict-audit artifacts
- classify the surfaced result
- sync canon and publish the next admissible block

## Out of scope

- deterministic implementation changes
- acceptance lock/replay/full
- scenario/oracle edits
- broader architecture refactors

## Touch-list

- `docs/TASK_PACKAGES/TP-2026-03-14-p1.6o192-post-p1.6o191-proof-lane-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-14-p1.6o193-scenario-generation-progress-error-detail-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-14-p1.6o194-post-p1.6o193-proof-lane-a1.md`
- `STATE.md`
- `docs/SESSIONS/SESSION-2026-02-19-llm-first-firebreak-a1.md`
- `docs/SESSION_INDEX.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`

## Plan

1. Verify runtime parity against current `HEAD`.
2. Run one guarded `dev L2` rerun with fresh `run_id`.
3. Strict-audit artifacts.
4. Classify whether scenario-generation timeout now carries concrete inner error detail after `P1.6o193`.
5. Sync canon and publish the next admissible block.

## DoD

1. A fresh guarded rerun exists under `/tmp/booking_quality/p1.6o194-l2-dev-20260314-a1-r1`.
2. Strict artifact audit is complete.
3. Proof is truthfully classified as admissible or blocked, with the next admissible block documented.
4. Active docs/state/session show `P1.6o194` as the proof checkpoint after `P1.6o193`.

## Checks

- `python3 -m py_compile ops/diagnose.py truffles-api/tests/test_diagnose_run_command.py`
- `pytest -q truffles-api/tests/test_diagnose_run_command.py -k "scenario_timeout or generate_batch"`
- `python3 - <<'PY'
import json
import subprocess
head = subprocess.check_output(['git', 'rev-parse', 'HEAD'], text=True).strip()
runtime = json.loads(subprocess.check_output(['curl', '-s', 'http://127.0.0.1:18184/admin/version'], text=True))
print({'head': head, 'runtime': runtime.get('git_commit'), 'match': head == runtime.get('git_commit')})
PY`
- `scripts/llm_quality_guarded.sh --mode lock --run-id p1.6o194-l2-dev-20260314-a1-r1 --allow-pending-previous -- --base-url http://127.0.0.1:18184 --client-slug demo_salon ... --quality-lane dev`
- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o194-l2-dev-20260314-a1-r1 --status done --strict-artifacts`

## Evidence

1. `ops/diagnose.py`
2. `truffles-api/tests/test_diagnose_run_command.py`
3. `/tmp/booking_quality/p1.6o192-l2-dev-20260314-a1-r1/summary.json`
4. `/tmp/booking_quality/p1.6o192-l2-dev-20260314-a1-r1/manual_audit.json`
5. updated `STATE.md` / session docs

## Rollback

1. Keep `P1.6o193` deterministic closure intact.
2. If parity or audit is invalid, stop and keep proof blocked with truthful classification.
3. Do not mutate proof artifacts or gates.

## No-go

- no extra reruns beyond the single guarded proof lane
- no class routing from non-audited artifacts
- no runtime edits in this TP unless an infra-only blocker prevents a valid rerun

## Risks / blockers

- scenario generation may still time out
- the next surfaced result may still be another proof-validity blocker before semantic evidence resumes
- inner error detail may point to timeout-budget debt or generator-internal parse debt as the next true blocker

## Residual architecture debt (mandatory)

### Current residuals accepted in this block

- proof still depends on synchronous OpenAI transport probing and scenario-generation subprocess execution
- booking continuity still spans multiple carriers (`booking_state`, `session_memory`, `expected_reply_type`, policy-core memory)

### Why not in this block

- this TP is proof-only and exists to classify the next admissible result after `P1.6o193`

### Risk if deferred

- the worktree could drift into unsanctioned new fixes without evidenceful classification of the current scenario-generation blocker

### Linked follow-up Task Package(s)

- next TP selected only after `P1.6o194` strict audit

### Expiry/trigger to stop deferral

- immediately after `P1.6o194` finishes and the next blocker/family is known

## Next-block contract (mandatory)

### Next block objective

- publish the next bounded structural/proof-validity block from the surfaced `P1.6o194` admissible result, or truthfully record that proof remains blocked.

### First deterministic check command

- `python3 -m py_compile ops/diagnose.py truffles-api/tests/test_diagnose_run_command.py && pytest -q truffles-api/tests/test_diagnose_run_command.py -k "scenario_timeout or generate_batch"`

### Blocked-by conditions

- `P1.6o194` has not finished with strict audit
- parity or preflight are invalid
- surfaced result is not yet classified from artifacts
- docs/state/session are not synced with truthful run status

### Owner role for closure

- `Top Architect + Brain`

## Execution status

- `todo`

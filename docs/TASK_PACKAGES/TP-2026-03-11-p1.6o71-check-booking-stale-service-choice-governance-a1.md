# TP-2026-03-11-p1.6o71-check-booking-stale-service-choice-governance-a1

- Название/цель: закрыть surfaced `r96` blocker family, где после bounded `M19` closure свежий guarded `dev L2` больше не падает на invalid-schema named-specialist booking-request follow-up, но generated scenario driftит на dialog `2`, turn `4`, `Когда у меня назначена встреча?`: turn tagged как generic `booking` и наследует stale `reply_type=service_choice`, хотя runtime корректно ведет ход как `booking_manage/check_booking` и отвечает через `calendar.get_booking` без активного service-choice question contract.

## Canon refs

- Parent verdict: `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o60a-remaining-closure-architecture-verdict-a1.md`
- Owner matrix: `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o60b-remaining-closure-owner-matrix-a1.md`
- Program TP: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Proof bundle: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`
- Prior child: `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o70-invalid-schema-booking-request-specialist-followup-a1.md`
- STATE: `STATE.md`
- CA: remaining closure row after `M19`

## One web search (mandatory before implementation)

- Query: `Rasa forms unhappy path interruption keep requested slot official docs`
- Date/time: `2026-03-11T16:07:12+05:00`
- Sources opened:
  - official Rasa docs: `https://rasa.com/docs/rasa/forms/`
- Source quality:
  - official product documentation
- Found reusable ideas:
  - unhappy-path turns that temporarily leave a form should not silently mutate the active requested slot contract;
  - after an interruption, the next turn must either resume the previous slot explicitly or start a new management flow with its own state, but not inherit a stale form expectation.
- Decision:
  - `reuse/integrate`
  - next block should inspect scenario-governance/state-normalization only; current runtime behavior already looks contractually stronger than the generated expectation.
- Rejected options:
  - weakening runtime to force `service_choice` after a booking-management query;
  - accepting stale `service_choice` expectation as closure;
  - runtime phrase-hardcode for `Когда у меня назначена встреча?`.

## Root cause (mandatory)

- Symptom:
  - fresh guarded `dev L2` run `p1.6o6-l2-dev-20260311-a1-r96` stops at dialog `2`, turn `4`, user text `Когда у меня назначена встреча?`
- Minimal reproduction:
  1. Inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r96/scenarios.json` dialog `2`, turns `1-4`.
  2. Confirm turn `4` is tagged `booking` and expects `reply_type=service_choice` with `question_contract expected_reply_type=service_choice`.
  3. Inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r96/responses.jsonl` and `trace_bundle.jsonl` for dialog `2`, turn `4`.
  4. Confirm runtime validly routes the turn as `booking_manage` / `calendar.get_booking`, with no active `service_choice` question contract in final meta/trace.
  5. Compare the scenario turn sequence (`booking` -> `photo` -> `check_booking` -> `Когда у меня назначена встреча?`) and confirm the generated expectation is stale governance/state carryover, not a reopen of the runtime owner rows just closed.
- Evidence:
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r96/summary.json`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r96/brief.md`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r96/scenarios.json`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r96/responses.jsonl`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r96/trace_bundle.jsonl`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r96/manual_audit.md`
- Root cause statement:
  - generated scenario governance appears to carry a stale `service_choice` pending-question expectation into a booking-management follow-up after the `photo/check_booking` sequence, while runtime correctly starts a `booking_manage` flow instead of resuming service-choice collect.
- Fix mechanism:
  - freeze matrix row `M20` as a scenario-governance family;
  - bound the next block to scenario normalization / state carryover only;
  - keep runtime code untouched until governance RCA proves otherwise.

## Touch-list

- `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o71-check-booking-stale-service-choice-governance-a1.md`
- `scripts/booking_dialog_scenarios.py`
- `truffles-api/tests/test_booking_dialog_scenarios_script.py`
- active canon docs listed above

## Execution status

- `closed_bounded`
- deterministic scenario-governance implementation is complete
- fresh guarded `dev L2` `p1.6o6-l2-dev-20260311-a1-r101` no longer stopped on `r96/M20` and instead surfaced `r101/M21`


## Closure update

- Deterministic closure completed in `scripts/booking_dialog_scenarios.py` and `truffles-api/tests/test_booking_dialog_scenarios_script.py`.
- Fresh guarded `dev L2` `p1.6o6-l2-dev-20260311-a1-r101` no longer first-fails on `Когда у меня назначена встреча?`; `M20` is therefore bounded closed and the next surfaced row is `M21` / `P1.6o72`.
- `r101` is audited but remains non-canonical proof evidence (`infra_valid=false`, `semantic_valid=false`, `stop_reason=max_failures_reached:1`).

# TP-2026-03-11-p1.6o68-invalid-schema-service-grounded-booking-owner-a1

- Название/цель: закрыть surfaced `r93` blocker family, где после bounded `M16` closure свежий guarded `dev L2` больше не падает на ambiguous-time governance, но service-grounded booking intake `Я хочу записаться на маникюр.` при `llm_policy_core invalid_schema` деградирует в generic `booking_prompt` с `expected_reply_type=service_choice`, теряет grounded service, а следующий time/availability follow-up (`А есть ли свободные слоты на субботу?`) уже падает как stale service-collect drift.

## Canon refs

- Parent verdict: `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o60a-remaining-closure-architecture-verdict-a1.md`
- Owner matrix: `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o60b-remaining-closure-owner-matrix-a1.md`
- Program TP: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Proof bundle: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`
- Prior child: `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o67-ambiguous-time-fill-scenario-governance-a1.md`
- Related bounded closure: `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o18-timeout-booking-intake-degrade-guard-a1.md`
- STATE: `STATE.md`
- CA: remaining closure row after `M16`

## One web search (mandatory before implementation)

- Query: `Rasa forms prefilled slot continue next slot official docs`
- Date/time: `2026-03-11T15:07:51+05:00`
- Sources opened:
  - official Rasa docs: `https://rasa.com/docs/rasa/forms/`
- Source quality:
  - official product documentation
- Found reusable ideas:
  - once a required slot is already grounded from the user message, the form should request the next slot instead of re-asking the filled one;
  - unhappy-path/validation fallback must preserve the already grounded slot rather than restarting the form from an earlier missing slot.
- Decision:
  - `reuse/integrate`
  - reuse the existing timeout-safe booking fallback pattern and integrate an `invalid_schema` analogue that preserves grounded service and resumes `time` collect.
- Rejected options:
  - oracle weakening to accept `service_choice` after explicit service grounding, because the stale slot owner is the defect;
  - treating judge pass as closure, because contract evidence still shows a stale `service_choice` drift;
  - broad phrase-hardcode routing outside the degraded boundary.

## Root cause (mandatory)

- Symptom:
  - fresh guarded `dev L2` run `p1.6o6-l2-dev-20260311-a1-r93` stops at dialog `1`, turn `3`, user text `А есть ли свободные слоты на субботу?`
- Minimal reproduction:
  1. Inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r93/scenarios.json` dialog `1`, turns `1-3`.
  2. Confirm turn `1` text `Я хочу записаться на маникюр.` is service-grounded and keeps canonical scenario `reply_type=time`.
  3. Inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r93/responses.jsonl` and `trace_bundle.jsonl` for turn `1` and confirm `llm_policy_core` ends in `policy_error:invalid_schema`, degraded collect asks service, and context is reset to `expected_reply_type=service_choice`.
  4. Inspect turn `3` and confirm runtime still responds with generic `booking_prompt` asking for service while scenario requires `expected_reply_type=time` and `question_contract` evidence for the active slot question.
  5. Confirm judge passes the visible response, so contract-first evidence is the blocker signal and this is not an oracle-weakening candidate.
- Evidence:
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r93/summary.json`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r93/brief.md`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r93/scenarios.json`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r93/responses.jsonl`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r93/trace_bundle.jsonl`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r93/manual_audit.md`
- Five Whys:
  1. Why did turn `3` fail? Scenario expects the booking flow to already be at `time`, but runtime still asks for `service`.
  2. Why is runtime still asking for `service`? The earlier service-grounded booking turn degraded to generic `service_choice` under `policy_error:invalid_schema`.
  3. Why did degraded collect choose `service_choice`? The `invalid_schema` guard has no service-hint-safe fallback equivalent to the timeout path.
  4. Why is that wrong? The canonical booking contract treats an explicit service-grounded booking request as opening `time` collect even when semantic owner fails; the bounded degrade path must preserve the grounded service instead of discarding it.
  5. Why is this a new bounded family? `P1.6o18` closed timeout-degrade booking intake with service hint, but `invalid_schema` degraded fallback still lacks the analogous service-grounded recovery row.
- Root cause statement:
  - `truffles-api/app/routers/webhook/decision.py` has no bounded `invalid_schema` service-grounded booking recovery, so explicit service hints are discarded on degraded collect and the runtime reopens `service_choice` instead of preserving `time` resume.
- Fix mechanism:
  - freeze matrix row `M17` for invalid-schema service-grounded booking intake;
  - add a bounded `invalid_schema` service-hint-safe fallback that persists grounded `booking.service`, sets `expected_reply_type=time`, and emits explicit policy-failure provenance;
  - add deterministic regressions proving service-grounded invalid-schema booking now resumes `time`, while generic service-missing invalid-schema collect still asks for service.

## Reuse-first decision (mandatory)

- Reuse:
  - existing timeout booking safe fallback pattern from `P1.6o18`
  - existing booking context / expected-reply setters
  - existing booking prompt formatting and degraded-fallback observability
- Integrate:
  - `invalid_schema` degraded collect path in `truffles-api/app/routers/webhook/decision.py`
- Do not build:
  - new phrase router
  - new oracle waiver
  - new scenario family

## Execution profile

- TP mode: `implementation`
- Doc touch budget (files): `9`
- Code dominance: `required`
- Override token: `none`
- Why this profile fits: surfaced family is a bounded invalid-schema degraded-owner gap with an existing timeout analogue and a frozen post-`M16` row.

## Release safety

- Strategy: bounded degraded-owner repair only
- Go signals:
  - service-grounded invalid-schema booking request preserves `booking.service` and resumes `expected_reply_type=time`
  - meta/trace show explicit `policy_error:invalid_schema` provenance and bounded guard recovery
  - generic service-missing invalid-schema collect still asks for service
- No-go signals:
  - widening the fix outside the degraded boundary
  - accepting generic `service_choice` after explicit service grounding
  - weakening contract/judge/oracle boundaries
- Rollback verification:
  - revert only the bounded `M17` runtime/test delta if generic invalid-schema collect regresses

## Invariant

1. `P1.6o67/M16` remains closed.
2. `P1.6o18` timeout service-hint-safe fallback remains behaviorally unchanged.
3. Generic invalid-schema booking collect without a grounded service hint still asks for service.
4. Degraded fallback remains explicit in `decision_meta` / `decision_trace`.

## Scope

- publish surfaced `r93` family as matrix row `M17`
- implement bounded invalid-schema service-grounded booking recovery only

## Out of scope

- acceptance `lock/replay`
- global model-routing changes
- scenario/oracle weakening for judge-pass turns
- timeout-specific degrade logic already closed by `P1.6o18`

## Touch-list

- `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o68-invalid-schema-service-grounded-booking-owner-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o60b-remaining-closure-owner-matrix-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o60a-remaining-closure-architecture-verdict-a1.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`
- `truffles-api/app/routers/webhook/decision.py`
- `truffles-api/tests/test_message_endpoint.py`
- `truffles-api/tests/test_booking_quality_response_guard.py`

## Plan (1..N)

1. Freeze `r93` as audited non-closure evidence and publish `M17`.
2. Add bounded `invalid_schema` service-grounded booking recovery before generic degraded collect.
3. Persist grounded service and resume `time` collect with explicit policy-failure provenance.
4. Add targeted runtime/evidence regressions and one adjacent negative case for service-missing invalid-schema collect.
5. Run focused checks and `git diff --check`.
6. Run exactly one fresh guarded `dev L2` only after deterministic green.

## DoD

1. `r93` is recorded in active docs as audited non-closure evidence.
2. Service-grounded invalid-schema booking request no longer reopens `service_choice`.
3. Final meta/trace preserve `expected_reply_type=time`, bounded guard recovery, and grounded service persistence.
4. Generic service-missing invalid-schema collect still asks for service.
5. Focused deterministic runtime/evidence regressions are green.
6. Fresh guarded `dev L2` no longer stops on the `r93` family.

## Expensive run control (mandatory)

- Hypothesis:
  - after the bounded `M17` invalid-schema service-grounded recovery, fresh guarded `dev L2` will no longer stop on dialog `1`, turn `3`, `А есть ли свободные слоты на субботу?`
- Expected measurable effect:
  - the next guarded run preserves service grounding on the first turn and either proceeds beyond the stale `service_choice` family or surfaces a different first blocker
- Stop condition:
  - run exactly one fresh guarded `dev L2` plus strict audit; if the same stale service-collect family persists, stop the expensive loop and reopen RCA instead of chaining another rerun

## Checks

- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r93 --status done --strict-artifacts`
- `pytest -q truffles-api/tests/test_message_endpoint.py -k "invalid_schema and service_hint and booking_prompt"`
- `pytest -q truffles-api/tests/test_booking_quality_response_guard.py -k "invalid_schema or service_choice or booking_prompt"`
- `git diff --check`

## Execution status

- `open`
- fresh guarded `dev L2` `p1.6o6-l2-dev-20260311-a1-r93` is audited and frozen as the next blocker
- deterministic implementation has not started yet

## Evidence

1. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r93/summary.json`
2. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r93/brief.md`
3. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r93/scenarios.json`
4. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r93/responses.jsonl`
5. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r93/trace_bundle.jsonl`
6. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r93/manual_audit.md`
7. `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o67-ambiguous-time-fill-scenario-governance-a1.md`
8. `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o68-invalid-schema-service-grounded-booking-owner-a1.md`

## Rollback

1. Keep `P1.6o6` blocked.
2. Revert only the future bounded `M17` runtime/test delta if it regresses adjacent degraded collect rows.

## No-go

- no phrase fitting
- no oracle weakening
- no reinterpretation of `r93` as proof that `M16` is still open
- no generic `service_choice` accepted as closure for explicit service-grounded invalid-schema booking intake

## Risks/Blockers

1. The fix may need to persist service hint provenance in addition to `booking.service` if adjacent owner rows depend on recent-service carryover.
2. Judge currently passes the visible reply, so contract evidence must stay strict to avoid a false closure.
3. `r93` remains non-canonical proof evidence (`infra_valid=false`, `semantic_valid=false`), so this block can only claim bounded closure plus rerun rollover.

## Residual architecture debt (mandatory)

- Current residuals accepted in this block:
  - `M17` remains open after bounded `M16` closure.
- Why not in this block:
  - `r93` surfaced only after the next fresh guarded `dev L2` beyond `M16`.
- Risk if deferred:
  - service-grounded booking requests can still collapse back to `service_choice` on invalid-schema degrade and hide later slot/availability rows.
- Linked follow-up Task Package(s):
  - `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`
- Expiry/trigger to stop deferral:
  - before the next guarded rerun beyond `r93`.

## Next-block contract (mandatory)

- Next block objective:
  - implement `M17`, then rerun focused deterministic runtime checks before any new guarded `dev L2`.
- First deterministic check command:
  - `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r93 --status done --strict-artifacts && git diff --check`
- Blocked-by conditions:
  - `M17` is not published in the owner matrix
  - invalid-schema service-grounded regressions are not green
  - acceptance tool-evidence infra remains red
- Owner role for closure:
  - `Hands` for bounded implementation, `Top Architect + Brain` for matrix/program/proof sync


## Closure update

- Deterministic implementation for `M17` is complete.
- Fresh guarded `dev L2` `r94` no longer first-failed on this row and rolled to `P1.6o69`.
- This block is now `closed_bounded`; proof-chain closure remains blocked on later surfaced families and infra-valid canonical evidence.

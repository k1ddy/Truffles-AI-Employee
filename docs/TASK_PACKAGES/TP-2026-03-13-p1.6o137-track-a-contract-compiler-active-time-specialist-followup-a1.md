# TP-2026-03-13-p1.6o137-track-a-contract-compiler-active-time-specialist-followup-a1

- Название/цель: после surfaced `P1.6o136` first fail начать первый structural block в frozen order `contract compiler -> executable owner coverage -> boundary statechart -> proof bundle`: скомпилировать active-time specialist-followup scenario/audit expectations из runtime contract, чтобы covered `Track A` больше не зависел от handwritten `pending_question_interaction` trace drift.

## Canon refs

- Binding roadmap: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o135-beauty-salon-canonical-control-plane-roadmap-a1.md`
- Fresh surfaced proof: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o136-post-p1.6o134-fresh-proof-rerun-a1.md`
- Class-level routing: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o118-class-level-closure-process-reset-a1.md`
- Parent closure program: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Main remediation: `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- State baseline: `STATE.md`

## Git context

- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `origin/main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect` remove branch + worktree after merge

## One web search (mandatory before implementation)

- Query: `OpenAI trace grading intermediate decisions official docs`
- Date/time: `2026-03-13 14:50 +05:00`
- Sources opened:
  - official OpenAI docs: `Trace grading - OpenAI API`
- Source quality:
  - official vendor documentation
- Found reusable ideas:
  - grader rules should evaluate intermediate trace evidence, not just final response text;
  - the right seam is a contract over decision trace / intermediate decisions, not a text-match oracle.
- Decision:
  - `integrate`
  - keep the bounded structural fix on scenario/audit contract compilation from runtime evidence instead of another text/tag rewrite.
- Rejected options:
  - broaden `must_include` / text-match oracle;
  - new runtime owner patch before compiler convergence;
  - statechart-first implementation.

## Root cause (mandatory)

- Symptom:
  - strict-audited fresh rerun `p1.6o136-l2-dev-20260313-a1-r3` surfaced `expected_trace_miss` on `dialog 1 / turn 6 / "Я хочу записаться к Айгерим."`.
- Minimal reproduction:
  1. Run `P1.6o136` on fingerprint-verified runtime `:18184`.
  2. Inspect `/tmp/booking_quality/p1.6o136-l2-dev-20260313-a1-r3/responses.jsonl` and `trace_bundle.jsonl` for `LLM-QUAL-p1.6o136-l2-dev-20260313-a1-r3-001-06-0414c3`.
  3. Compare generated expectation with runtime `decision_meta` and `decision_trace`.
- Evidence:
  - `/tmp/booking_quality/p1.6o136-l2-dev-20260313-a1-r3/summary.json`
  - `/tmp/booking_quality/p1.6o136-l2-dev-20260313-a1-r3/brief.md`
  - `/tmp/booking_quality/p1.6o136-l2-dev-20260313-a1-r3/manual_audit.json`
  - `/tmp/booking_quality/p1.6o136-l2-dev-20260313-a1-r3/scenarios.json`
  - `/tmp/booking_quality/p1.6o136-l2-dev-20260313-a1-r3/responses.jsonl`
- Five Whys:
  1. Why did the rerun stop on `Track A` again? Because the failing turn expected a handwritten `pending_question_interaction` trace entry that runtime no longer emits on this path.
  2. Why was runtime still otherwise coherent? Because `decision_meta` kept canonical contract evidence: `expected_reply_type=time`, `pending_question_target=specialist`, `active_question_relation=referent_followup`, and specialist-followup continuity.
  3. Why did the oracle still fail? Because `scripts/booking_dialog_scenarios.py` hand-built `trace_contains` for this family instead of compiling it from machine-readable runtime contract.
  4. Why is that structural, not another row child? Because the drift is between semantic truth systems (runtime contract vs scenario/oracle contract), not within policy-core intent/owner semantics.
  5. Why is the next block a compiler slice? Because frozen roadmap `P1.6o135` explicitly requires `contract compiler` before new owner/statechart work.
- Root cause statement:
  - the current first fail is a `Track A` contract-drift family: scenario/oracle still hand-assembles specialist-followup expectation meta/trace, so it can require stale trace evidence even when runtime meta/trace remain contractually coherent.
- Fix mechanism:
  - introduce one shared scenario-contract compiler slice for the surfaced active-time specialist-followup family;
  - compile expectation `reply_type/meta_any/trace_contains` from machine-readable runtime contract inputs (owner matrix + expected-reply contract), not handwritten trace literals;
  - route both scenario generation and audit expectation normalization through that compiler for the covered family.

## Reuse-first decision (mandatory)

- Reuse:
  - `truffles-api/app/services/interaction_owner_matrix_service.py`
  - `truffles-api/app/services/expected_reply_contract.py`
  - existing scenario generator and audit evaluation pipeline
- Integrate:
  - add one shared compiler module consumed by `scripts/booking_dialog_scenarios.py` and `ops/diagnose.py`
- Build:
  - only the bounded compiler slice for surfaced active-time specialist-followup contract

## Release safety

- Rollout: none; local deterministic/compiler-only block
- Go signal:
  - covered expectations no longer require handwritten `pending_question_interaction` trace for active-time specialist followup
  - targeted compiler/script/audit tests are green
- No-go signal:
  - compiler introduces new text heuristics as primary path
  - runtime owner code changes become necessary to satisfy the block
- Rollback:
  - revert only the bounded compiler slice and its tests; keep `P1.6o136` surfaced truth intact

## Invariant

1. `llm_policy_core` remains the single semantic owner.
2. No runtime owner/statechart changes in this block.
3. No new phrase/regex hardcode in runtime core.
4. Scenario/audit convergence must come from machine-readable contract inputs, not from looser text matching.

## Scope

- add one shared contract-compiler module for surfaced active-time specialist-followup expectations
- integrate it into `scripts/booking_dialog_scenarios.py`
- integrate the same normalization into `ops/diagnose.py` for the covered family
- add targeted deterministic tests for compiler/script/audit behavior

## Out of scope

- broader compiler coverage beyond surfaced family
- executable owner coverage expansion
- boundary statechart work
- fresh proof rerun after this deterministic slice

## Touch-list

- `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o137-track-a-contract-compiler-active-time-specialist-followup-a1.md`
- `truffles-api/app/services/scenario_contract_compiler.py`
- `scripts/booking_dialog_scenarios.py`
- `ops/diagnose.py`
- `truffles-api/tests/test_booking_dialog_scenarios_script.py`
- `truffles-api/tests/test_booking_quality_scenario_contract_gate.py`
- `STATE.md`
- `docs/SESSIONS/SESSION-2026-02-19-llm-first-firebreak-a1.md`
- `docs/SESSION_INDEX.md`

## Plan (1..N)

1. Freeze `P1.6o136` surfaced evidence and publish this bounded compiler TP.
2. Add a shared compiler helper for the surfaced family using machine-readable contract inputs.
3. Replace handwritten specialist-followup expectation assembly in scenario generation with the compiler output for the covered family.
4. Normalize the same covered expectation family inside `ops/diagnose.py` before strict comparison.
5. Add targeted deterministic tests and sync docs/state/session.

## DoD

1. A shared compiler module exists for the surfaced active-time specialist-followup contract family.
2. `scripts/booking_dialog_scenarios.py` no longer hand-builds primary `trace_contains` for that covered family.
3. `ops/diagnose.py` consumes the same compiled expectation normalization for the covered family.
4. Targeted deterministic tests are green.
5. Active docs/state/session point to the next admissible post-slice proof block instead of another local child patch.

## Checks

- `python3 -m py_compile truffles-api/app/services/scenario_contract_compiler.py scripts/booking_dialog_scenarios.py ops/diagnose.py`
- `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "active_time_specialist_followup or scenario_contract_compiler"`
- `pytest -q truffles-api/tests/test_booking_quality_scenario_contract_gate.py -k "specialist_followup or scenario_contract_compiler"`
- `git diff --check`
- `SESSION_AGENT=a1 scripts/session_check.sh`

## Execution status

- `done`

## Execution result

- Added shared compiler module `truffles-api/app/services/scenario_contract_compiler.py` for the surfaced active-time specialist-followup family.
- `scripts/booking_dialog_scenarios.py` now compiles covered follow-up expectations from machine-readable contract inputs instead of hand-assembling stale `pending_question_interaction` trace literals on the surfaced path.
- `ops/diagnose.py` now normalizes the same covered expectation family through the shared compiler before strict comparison, so generated/audited expectations use one bounded contract path.
- Targeted checks green:
  - `python3 -m py_compile truffles-api/app/services/scenario_contract_compiler.py scripts/booking_dialog_scenarios.py ops/diagnose.py`
  - `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "active_time_specialist_followup or scenario_contract_compiler or named_specialist_preference_availability_keeps_generalized_followup"` -> `2 passed, 110 deselected`
  - `pytest -q truffles-api/tests/test_booking_quality_scenario_contract_gate.py -k "specialist_followup or scenario_contract_compiler or extract_expectations_compiles_active_time_specialist_followup_contract"` -> `1 passed, 8 deselected`
  - `git diff --check`
  - `SESSION_AGENT=a1 scripts/session_check.sh` -> `Session OK`

## Evidence

1. `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o137-track-a-contract-compiler-active-time-specialist-followup-a1.md`
2. `truffles-api/app/services/scenario_contract_compiler.py`
3. `scripts/booking_dialog_scenarios.py`
4. `ops/diagnose.py`
5. `truffles-api/tests/test_booking_dialog_scenarios_script.py`
6. `truffles-api/tests/test_booking_quality_scenario_contract_gate.py`
7. `/tmp/booking_quality/p1.6o136-l2-dev-20260313-a1-r3/summary.json`
8. `/tmp/booking_quality/p1.6o136-l2-dev-20260313-a1-r3/manual_audit.json`
9. `/tmp/booking_quality/p1.6o136-l2-dev-20260313-a1-r3/responses.jsonl`
10. `/tmp/booking_quality/p1.6o136-l2-dev-20260313-a1-r3/trace_bundle.jsonl`

## Rollback

1. Revert only the bounded compiler/helper integration and its tests.
2. Keep `P1.6o136` surfaced evidence intact in docs/state.
3. Do not reopen runtime-owner/statechart work from this block.

## No-go

- no runtime owner/degrade patch in this block
- no statechart work before compiler + executable owner coverage
- no global weakening of `expected_trace_miss`
- no new scenario text heuristics as primary truth source

## Risks / blockers

- matrix coverage is still partial, so the compiler must stay bounded to surfaced family
- `scripts/` import path must stay lightweight and not depend on runtime-only env
- broad trace relaxation would hide real runtime regressions if not scoped precisely

## Residual architecture debt (mandatory)

### Current residuals accepted in this block

- compiler coverage is bounded to surfaced active-time specialist-followup family
- family detection in scenario generation remains local until broader contract compiler lands
- fresh post-slice proof evidence does not exist yet

### Why not in this block

- this is the first structural compiler slice selected by `P1.6o136`; broader family coverage would mix multiple remaining classes without new proof

### Risk if deferred

- adjacent `Track A` families may still require more compiler slices after the next proof-lane

### Linked follow-up Task Package(s)

- `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o138-post-track-a-contract-compiler-proof-lane-a1.md`
- later broader contract-compiler and executable-owner slices from the frozen roadmap

### Expiry/trigger to stop deferral

- immediately after the first post-`P1.6o137` proof rerun truthfully classifies whether `Track A` moved or another class surfaced

## Next-block contract (mandatory)

### Next block objective

- after deterministic closure of `P1.6o137`, publish and run `P1.6o138`, the fresh proof block that verifies the covered compiler family is no longer the first fail and truthfully classifies the next remaining class.

### First deterministic check command

- `python3 -m py_compile truffles-api/app/services/scenario_contract_compiler.py scripts/booking_dialog_scenarios.py ops/diagnose.py && pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "active_time_specialist_followup or scenario_contract_compiler"`

### Blocked-by conditions

- `P1.6o138` has not finished with strict audit
- surfaced post-`P1.6o137` class has not been routed truthfully under `P1.6o118`

### Owner role for closure

- `Top Architect + Brain`

# TP-2026-03-14-p1.6o168-post-p1.6o167-proof-lane-a1

- Название/цель: выполнить один свежий guarded `dev L2` rerun после детерминированного закрытия `P1.6o167`, чтобы доказать, что surfaced choose-specialist runtime owner/response drift ушёл с first fail и truthfully surfaced следующий admissible class.

## Canon refs

- Binding roadmap: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o135-beauty-salon-canonical-control-plane-roadmap-a1.md`
- Prior deterministic closure: `docs/TASK_PACKAGES/TP-2026-03-14-p1.6o167-active-time-choose-specialist-master-interrupt-owner-a1.md`
- Prior proof lane: `docs/TASK_PACKAGES/TP-2026-03-14-p1.6o166-post-p1.6o165-proof-lane-a1.md`
- Class-level routing: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o118-class-level-closure-process-reset-a1.md`
- Parent closure program: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Main remediation: `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- State baseline: `STATE.md`

## Git context

- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `origin/main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect` remove branch + worktree after merge

## One web search (mandatory before implementation)

- Not applicable in this block: proof-only continuation, no implementation.
- Decision:
  - `reuse`
  - reuse the existing guarded `dev L2` proof contour and strict artifact audit to determine whether the covered `P1.6o167` runtime slice moved off first fail.

## Root cause (mandatory)

- Symptom:
  - `P1.6o167` can close deterministically, but there is still no fresh proof showing the covered choose-specialist runtime owner/response family moved off first fail.
- Minimal reproduction:
  1. finish `P1.6o167` deterministic runtime closure;
  2. verify `/admin/version` on `:18184` matches current `HEAD`;
  3. execute one fresh guarded `dev L2` rerun with explicit `--quality-lane dev`;
  4. strict-audit the run and classify the next first fail.
- Evidence:
  - `docs/TASK_PACKAGES/TP-2026-03-14-p1.6o167-active-time-choose-specialist-master-interrupt-owner-a1.md`
  - `/tmp/booking_quality/p1.6o166-l2-dev-20260314-a1-r1/summary.json`
  - `/tmp/booking_quality/p1.6o166-l2-dev-20260314-a1-r1/manual_audit.json`
- Five Whys:
  1. Why is another proof block needed? Because deterministic closure alone does not prove the expensive lane moved past the covered runtime family.
  2. Why not open another structural slice immediately? Because frozen routing requires one proof checkpoint after each bounded fix.
  3. Why reuse the same live proof contour? Because parity drift would make class routing non-admissible.
  4. Why explicit `--quality-lane dev`? Because proof continuation must avoid accidental acceptance-chain preflight.
  5. Why one run only? Because this block is classification/proof, not another expensive loop.
- Root cause statement:
  - after `P1.6o167`, the only missing evidence is one fresh strict-audited proof rerun that shows whether the covered choose-specialist runtime family is gone from first fail.
- Fix mechanism:
  - verify runtime parity, run one guarded `dev L2`, strict-audit it, and select the next structural continuation only from the surfaced class.

## Reuse-first decision (mandatory)

- Reuse:
  - existing `ops/diagnose.py llm-quality` proof-lane contour
  - existing strict audit command
  - live runtime on `:18184`, relaunched from current worktree if parity is missing
- Integrate:
  - no code changes in this block
- Build:
  - none

## Release safety

- Rollout: none; proof-only block
- Go signal:
  - `/admin/version.git_commit == HEAD`
  - strict audit completes for the fresh rerun
- No-go signal:
  - runtime parity mismatch
  - preflight invalid
  - strict audit missing or artifact bundle incomplete
- Rollback:
  - no runtime/code rollback in this block; only stop after audit and publish truthful class routing

## Invariant

1. `P1.6o167` deterministic closure remains intact.
2. No new runtime/oracle patch is allowed before the rerun result is audited.
3. Proof stays on a fingerprint-verified runtime on `:18184`.
4. Evidence remains contract-first and class-routed.

## Scope

- runtime parity check on `:18184`
- one fresh guarded `dev L2`
- strict audit of the run
- class routing from the surfaced first fail
- truthful docs/state/session sync

## Out of scope

- new structural runtime/oracle changes before proof result
- lock/replay/full acceptance bundle
- baseline updates
- scenario regeneration changes beyond the single run

## Touch-list

- `docs/TASK_PACKAGES/TP-2026-03-14-p1.6o167-active-time-choose-specialist-master-interrupt-owner-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-14-p1.6o168-post-p1.6o167-proof-lane-a1.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `STATE.md`
- `docs/SESSIONS/SESSION-2026-02-19-llm-first-firebreak-a1.md`
- `docs/SESSION_INDEX.md`
- `/tmp/booking_quality/p1.6o168-l2-dev-20260314-a1-r1/**`

## Plan

1. Verify runtime parity on `:18184`; relaunch a fresh runtime from current worktree if needed.
2. Execute exactly one fresh guarded `dev L2` rerun.
3. Strict-audit the run.
4. Route the surfaced first fail under `P1.6o118`.
5. Sync truth and publish the next admissible structural block.

## DoD

1. One fresh guarded rerun completed on parity-verified runtime `:18184`.
2. Strict audit is done and artifacts are complete.
3. Covered `P1.6o167` family is no longer first fail, or it is truthfully reopened with evidence.
4. The next admissible block is selected from the surfaced class only.

## Checks

- `python3 - <<'PY'` parity check on `http://127.0.0.1:18184/admin/version`
- `python3 ops/diagnose.py llm-quality ... --output-dir /tmp/booking_quality/p1.6o168-l2-dev-20260314-a1-r1 --run-id p1.6o168-l2-dev-20260314-a1-r1 ... --quality-lane dev`
- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o168-l2-dev-20260314-a1-r1 --status done --strict-artifacts`
- `git diff --check`

## Evidence

1. `docs/TASK_PACKAGES/TP-2026-03-14-p1.6o168-post-p1.6o167-proof-lane-a1.md`
2. `/tmp/booking_quality/p1.6o168-l2-dev-20260314-a1-r1/summary.json`
3. `/tmp/booking_quality/p1.6o168-l2-dev-20260314-a1-r1/brief.md`
4. `/tmp/booking_quality/p1.6o168-l2-dev-20260314-a1-r1/failure_families.json`
5. `/tmp/booking_quality/p1.6o168-l2-dev-20260314-a1-r1/responses.jsonl`
6. `/tmp/booking_quality/p1.6o168-l2-dev-20260314-a1-r1/trace_bundle.jsonl`
7. `/tmp/booking_quality/p1.6o168-l2-dev-20260314-a1-r1/manual_audit.json`

## Rollback

1. No code rollback in this proof-only block.
2. If preflight or audit is invalid, stop and publish the invalid run truthfully.
3. Do not open a new structural TP from unaudited or non-admissible evidence.

## No-go

- no new runtime/oracle patch before the rerun result
- no baseline update from this run
- no silent retry loop beyond the single admissible run
- no stale class routing from pre-`P1.6o167` artifacts

## Risks / blockers

- runtime parity may drift after deterministic closure
- run may still stay non-canonical if infrastructure noise reappears
- surfaced class may move into another runtime or scenario family and require careful routing discipline

## Residual architecture debt (mandatory)

### Current residuals accepted in this block

- runtime success-path owner logic still lives in `decision.py`
- proof bundle closure is still not complete
- full executable owner coverage remains incomplete outside covered slices

### Why not in this block

- this block is proof-only

### Risk if deferred

- the worktree would reopen structural coding without one fresh proof checkpoint after `P1.6o167`

### Linked follow-up Task Package(s)

- next structural TP selected only after `P1.6o168` strict audit

### Expiry/trigger to stop deferral

- immediately after `P1.6o168` finishes and the next surfaced class is known

## Next-block contract (mandatory)

### Next block objective

- publish the next bounded structural block from the surfaced `P1.6o168` first fail family.

### First deterministic check command

- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o168-l2-dev-20260314-a1-r1 --status done --strict-artifacts && git diff --check`

### Blocked-by conditions

- `P1.6o168` has not finished with strict audit
- surfaced class has not been routed truthfully under `P1.6o118`

### Owner role for closure

- `Top Architect + Brain`

## Execution status

- `done`

## Execution result

- Fresh guarded `dev L2` rerun `p1.6o168-l2-dev-20260314-a1-r1` completed on parity-verified runtime `:18184` and moved the covered `P1.6o167` choose-specialist runtime family off first fail.
- Strict audit recorded `artifact_integrity.valid=true`, `snapshot.infra_valid=true`, `snapshot.semantic_valid=false`, `snapshot.stop_reason=max_failures_reached:1`, `snapshot.run_integrity_reasons=[run_completion_gap]`, and `oracle_arbitration.winner=contract`.
- Surfaced first fail is bounded scenario-governance drift on `dialog 1 / turn 10 / "Могу я записаться к ней на 14:00?"`: scenario still requires orphan `service_choice`, while runtime canonically preserves `booking_time_availability_followup` with `pending_question_act=ask_about_requested_slot`, `pending_question_target=time`, and `expected_reply_type=name`.
- The bounded next structural block is `P1.6o169`: active-name specialist-referent explicit-time followup after master-info interrupt must reuse the existing active-name requested-slot compiler contract instead of orphan fallback.

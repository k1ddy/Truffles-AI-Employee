# TP-2026-03-12-p1.6o101-slot-compare-generic-free-slot-question-governance-a1

- Название/цель: закрыть surfaced `M35` scenario-governance drift, где после proof lane `P1.6o100` generic free-slot question `Какое время доступно?` всё ещё генерируется как `slot_compare(time)`, хотя runtime канонически держит `ask_about_requested_slot(time)` / `booking_slot_guidance` под активным `time` collect.

## Canon refs

- Parent closure program: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Main remediation: `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- Historical requested-slot owner closure: `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o16-booking-time-guidance-question-owner-a1.md`
- Historical slot-constraint normalization: `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o89-slot-constraint-generic-free-slot-question-governance-a1.md`
- Proof lane that surfaced the row: `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o100-post-m34-proof-lane-a1.md`
- State + matrix baseline: `STATE.md`, `truffles-api/app/knowledge/generic/INTERACTION_OWNER_MATRIX.yaml`

## Git context

- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect` remove branch + worktree after merge

## One web search (mandatory before implementation)

- Query: `site:rasa.com/docs/rasa/forms requested slot follow-up question while slot is active`
- Date/time: `2026-03-12T16:58:00+05:00`
- Sources opened:
  - official Rasa forms docs: `https://rasa.com/docs/rasa/forms/`
- Source quality:
  - official product documentation
- Found reusable ideas:
  - while a slot is active, follow-up user questions should be interpreted relative to the requested slot rather than collapsed into a different slot family;
  - compare/constraint semantics must stay tied to actual temporal alternatives, not generic availability questions.
- Decision:
  - `reuse + integrate`
  - reuse existing requested-slot normalization helpers in `scripts/booking_dialog_scenarios.py` and integrate one bounded rule that rewrites generic free-slot questions out of `slot_compare` under active `time` collect.
- Rejected options:
  - weakening scenario expectations to accept stale `slot_compare`;
  - runtime patching to imitate stale generator governance;
  - broad redesign of the whole pending-question ontology in this block.

## Root cause (mandatory)

- Symptom:
  - fresh guarded `dev L2` `p1.6o100-l2-dev-20260312-a1-r1` no longer stops on `M34`, but now fails on dialog `1`, turn `2`, text `Какое время доступно?` with `expected_meta_mismatch` / `expected_trace_miss` because generated expectations require `slot_compare(time)` while runtime canonically emits `ask_about_requested_slot(time)`.
- Minimal reproduction:
  1. Inspect `/tmp/booking_quality/p1.6o100-l2-dev-20260312-a1-r1/scenarios.json` dialog `1`, turn `2`.
  2. Confirm the generated turn is tagged `slot_compare` and expects `pending_question_act=slot_compare` plus `pending_question_interaction slot_compare/time`.
  3. Inspect `/tmp/booking_quality/p1.6o100-l2-dev-20260312-a1-r1/responses.jsonl` dialog `1`, turn `2`.
  4. Confirm runtime produces `source=booking_slot_guidance`, `pending_question_act=ask_about_requested_slot`, `pending_question_target=time`, and `expected_reply_type=time`.
  5. Compare with existing generator rules and confirm there is no bounded normalization for generic free-slot questions that were mis-tagged as `slot_compare` without an actual comparison or temporal alternative.
- Evidence:
  - `/tmp/booking_quality/p1.6o100-l2-dev-20260312-a1-r1/scenarios.json`
  - `/tmp/booking_quality/p1.6o100-l2-dev-20260312-a1-r1/responses.jsonl`
  - `/tmp/booking_quality/p1.6o100-l2-dev-20260312-a1-r1/trace_bundle.jsonl`
  - `scripts/booking_dialog_scenarios.py`
  - `truffles-api/tests/test_booking_dialog_scenarios_script.py`
- Five Whys:
  1. Why did the run fail? Because scenario meta/trace still expected `slot_compare(time)` instead of requested-slot guidance.
  2. Why did scenario expect that? Because the generated turn stayed tagged as `slot_compare` even though the text only asks what times are available.
  3. Why was it not normalized away? Because current normalization handles explicit exact-time `slot_compare`, booking-tag contamination, and real temporal constraints, but not generic availability questions mis-tagged as `slot_compare`.
  4. Why is runtime already ahead? Because under active `time` collect, a generic free-slot question is canonically `ask_about_requested_slot(time)`.
  5. Why is this a bounded scenario-governance row? Because runtime meta/trace already preserve the requested-slot contract; the remaining defect is stale generated expectation/trace state.
- Root cause statement:
  - the scenario generator lacks a bounded normalization that converts generic free-slot questions mis-tagged as `slot_compare` back to canonical `ask_about_requested_slot(time)` under active `time` collect, so stale compare expectations survive after runtime has already kept the requested-slot contract.
- Fix mechanism:
  - add bounded requested-slot normalization for generic free-slot questions that were mis-tagged as `slot_compare`;
  - add deterministic sanitize/repair regressions;
  - sync matrix/docs/state truthfully and keep the next expensive proof step blocked until deterministic closure is green.

## Reuse-first decision (mandatory)

- Reuse:
  - existing requested-slot normalization helpers in `scripts/booking_dialog_scenarios.py`
  - existing deterministic generator test harness in `truffles-api/tests/test_booking_dialog_scenarios_script.py`
- Integrate:
  - one bounded normalization for generic free-slot `slot_compare` overclaim
  - targeted sanitize/repair regressions
- Build:
  - no runtime changes planned in this block

## Release safety

- Rollout: deterministic scenario-governance only; no production runtime change
- Go signal:
  - generic free-slot questions normalize to canonical `ask_about_requested_slot(time)`
  - genuine compare turns such as `утром или вечером` stay in `slot_compare`
  - `git diff --check` and `session_check` remain green
- No-go signal:
  - generator rewrites legitimate `slot_compare` turns into requested-slot guidance
  - runtime-related files become necessary in this block
- Rollback:
  - revert only the new generator normalization and matching tests

## Invariant

1. `M34` stays cleared by `P1.6o99` + `P1.6o100`.
2. Genuine temporal comparison turns remain `slot_compare`.
3. Runtime behavior is untouched in this block.
4. No oracle weakening.

## Scope

- bounded scenario-governance closure for generic free-slot questions mis-tagged as `slot_compare`
- deterministic tests
- matrix/docs/state/session sync

## Out of scope

- new guarded `dev L2`
- acceptance `lock/replay`
- runtime semantic changes

## Touch-list

- `scripts/booking_dialog_scenarios.py`
- `truffles-api/tests/test_booking_dialog_scenarios_script.py`
- `truffles-api/app/knowledge/generic/INTERACTION_OWNER_MATRIX.yaml`
- `truffles-api/tests/test_interaction_owner_matrix_contract.py`
- `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o101-slot-compare-generic-free-slot-question-governance-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o100-post-m34-proof-lane-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o102-post-m35-proof-lane-a1.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `STATE.md`
- `STRUCTURE.md`
- `docs/SESSIONS/SESSION-2026-02-19-llm-first-firebreak-a1.md`

## Plan (1..N)

1. Publish `P1.6o101` with RCA + one web search + bounded touch-list.
2. Add bounded normalization for generic free-slot questions mis-tagged as `slot_compare` under active `time` collect.
3. Add deterministic sanitize/repair regressions plus matrix contract update.
4. Run targeted deterministic checks and hygiene gates.
5. Sync active docs/state/session with factual deterministic closure and next admissible proof block.

## DoD

1. Generic free-slot question surfaces like `Какое время доступно?` canonicalize to requested-slot `time` guidance under active `time` collect.
2. Generated expectations preserve `pending_question_act=ask_about_requested_slot`, `pending_question_target=time`, `expected_reply_type=time`.
3. Genuine compare turns stay `slot_compare`.
4. Deterministic tests are green.
5. `M35` is truthfully synced in matrix/docs/state with the next admissible proof block identified.

## Checks

- `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "slot_compare and requested_slot"`
- `pytest -q truffles-api/tests/test_interaction_owner_matrix_contract.py`
- `python3 -m py_compile scripts/booking_dialog_scenarios.py truffles-api/tests/test_booking_dialog_scenarios_script.py truffles-api/tests/test_interaction_owner_matrix_contract.py`
- `git diff --check`
- `SESSION_AGENT=a1 scripts/session_check.sh`

## Execution status

- `done`

## Evidence

1. `/tmp/booking_quality/p1.6o100-l2-dev-20260312-a1-r1/scenarios.json`
2. `/tmp/booking_quality/p1.6o100-l2-dev-20260312-a1-r1/responses.jsonl`
3. `/tmp/booking_quality/p1.6o100-l2-dev-20260312-a1-r1/trace_bundle.jsonl`
4. `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "slot_compare and requested_slot"` -> `2 passed, 98 deselected`
5. `pytest -q truffles-api/tests/test_interaction_owner_matrix_contract.py`
6. `python3 -m py_compile scripts/booking_dialog_scenarios.py truffles-api/tests/test_booking_dialog_scenarios_script.py truffles-api/tests/test_interaction_owner_matrix_contract.py`
7. `git diff --check`
8. `SESSION_AGENT=a1 scripts/session_check.sh`
9. updated matrix/docs/state/session references

## Rollback

1. Revert only the new generator normalization and matching deterministic docs/tests.
2. Keep `P1.6o100` proof evidence and `M34` closure intact.
3. Do not reinterpret `M35` as runtime defect if deterministic closure is rolled back.

## No-go

- no runtime edits
- no new expensive run in this block
- no weakening requested-slot contract globally
- no phrase hardcode for only one exact string

## Risks / blockers

- generic free-slot questions sit close to real compare surfaces and the bounded rule must avoid collapsing legitimate `slot_compare` turns.
- timeout-degraded requested-slot guidance can make the row look like a runtime gap if meta/trace are not reviewed carefully.

## Residual architecture debt (mandatory)

### Current residuals accepted in this block

- Scenario ontology still relies on localized normalization heuristics for some pending-question cross-products.
- Acceptance/proof lane remains blocked until a fresh rerun confirms `M35` is gone.

### Why not in this block

- This block is a bounded scenario-governance repair, not a broader ontology redesign.

### Risk if deferred

- New expensive runs will keep stopping on false-red generic slot-question governance and hide later remaining families.

### Linked follow-up Task Package(s)

- `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o102-post-m35-proof-lane-a1.md`

### Expiry/trigger to stop deferral

- before any next guarded `dev L2` / `lock` / `replay`

## Next-block contract (mandatory)

### Next block objective

- publish and run `P1.6o102`: execute one fresh guarded `dev L2` after `P1.6o101` deterministic closure to prove generic free-slot `slot_compare` row `M35` is no longer first fail and truthfully surface the next family, if any.

### First deterministic check command

- `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "slot_compare and requested_slot" && pytest -q truffles-api/tests/test_interaction_owner_matrix_contract.py && git diff --check`

### Blocked-by conditions

- `P1.6o101` deterministic closure is not synced truthfully into active docs and matrix state
- generic free-slot `slot_compare` deterministic reproduction is not green
- live runtime fingerprint for the proof lane is not proven against current `HEAD`

### Owner role for closure

- `Top Architect + Brain`

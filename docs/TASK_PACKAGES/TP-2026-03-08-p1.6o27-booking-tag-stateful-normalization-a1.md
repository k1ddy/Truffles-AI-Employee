# TP-2026-03-08-p1.6o27-booking-tag-stateful-normalization-a1

- Название/цель: закрыть новый `r26` blocker family, где scenario sanitizer слишком буквально сохраняет LLM-provided `booking` tag/`service_choice` reply contract на service-grounded booking turns и не нормализует active-slot follow-ups, из-за чего valid slot-question surfaces снова выпадают из контракта ещё до runtime.
- Parent TP: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Canon refs: `AGENTS.md`, `STATE.md`, `STRUCTURE.md`, `STRATEGY/REQUIREMENTS.md`, `docs/runbooks/BOOKING_CONFIRM_VERIFY.md`, `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`, `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o26-orphan-pending-question-scenario-state-contract-a1.md`
- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect remove worktree + branch after merge`

## One web search (mandatory before implementation)

- Query: `site:rasa.com/docs forms requested_slot next empty slot service form`
- Time (UTC): `2026-03-08T10:32:41Z`
- Sources:
  - `https://rasa.com/docs/rasa/forms/`
- Ready solutions found:
  - once a required slot is already grounded, the next dialog act should be normalized against that active slot context instead of restarting the form with an earlier missing slot; conversely, service-grounded booking turns should not keep asking for service.
- Decision (`reuse/integrate/build`): `reuse + integrate`
  - reuse existing booking / mixed-slot / pending-question family vocabulary;
  - integrate stateful normalization in the scenario sanitizer instead of inventing a new runtime family;
  - build only the missing deterministic rewrite that maps mis-tagged booking turns back onto the existing active-slot contract.
- Rejected options:
  - runtime fixes for `calendar.list_slots` / `booking_prompt` in this block;
  - weakening oracle to accept `service_choice` on service-grounded booking turns;
  - adding new scenario families instead of normalizing into existing `booking` / `mixed_fill_plus_question` contracts.

## Root cause (mandatory)

- Symptom:
  - fresh guarded `dev L2` rerun `p1.6o6-l2-dev-20260308-a1-r26` stopped on `dialog_id=1`, `turn_index=2`, message `Есть ли свободные слоты на завтра?`

- Minimal reproduction:
  1. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r26/brief.md`
  2. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r26/scenarios.json` for dialog `1`, turns `1-2`
  3. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r26/responses.jsonl` for message id `LLM-QUAL-p1.6o6-l2-dev-20260308-a1-r26-001-02-85f8af`
  4. inspect `scripts/booking_dialog_scenarios.py` `_sanitize_llm_turns()` and booking/pending-question state normalization

- Evidence:
  - Scenario turn `1` is service-grounded (`Я хочу записаться на маникюр.`) but still carries `reply_type=service_choice`, so active `time` collect never opens in scenario state.
  - Scenario turn `2` then arrives with `booking/service_choice` expectations instead of the existing active-slot family, while runtime coherently lists slots and keeps `expected_reply_type=time`.
  - The failure is `expected_meta_mismatch` / `expected_trace_miss`; judge passes the runtime answer.
  - Current sanitizer trusts LLM-provided booking-tag expectations too much and lacks a stateful rewrite from mis-tagged active-slot turns back into existing families.

- Five Whys:
  1. Why did `r26` fail: scenario expected `service_choice` where runtime already had enough grounding to answer with slot availability and keep `expected_reply_type=time`.
  2. Why did scenario expect `service_choice`: service-grounded booking turn stayed under LLM-provided `service_choice` override instead of being normalized back to `time`.
  3. Why did the next turn also stay wrong: once scenario state never opened `time`, the follow-up slot-question turn could not be interpreted as active-slot continuation.
  4. Why did process not catch this earlier: generator sanitation validates tags turn-by-turn, but not whether booking-tag reply contract matches the grounded service state.
  5. Why did this surface now: `P1.6o26` removed the orphan pending-question family, so the next booking-tag state normalization gap became first blocker.

- Root cause statement:
  - scenario sanitizer lacks stateful normalization for booking-tag turns: it preserves `service_choice` on service-grounded booking requests and fails to reclassify booking-tag active-slot follow-ups into existing pending-question families.

- Fix mechanism:
  - force service-grounded `booking` turns to keep `reply_type=time` and scrub stale `service_choice` meta/trace overrides;
  - when active `reply_type=time` is already open, retag booking-tag mixed slot questions into the existing `mixed_fill_plus_question` family.

## Release safety

- Strategy: generator-only closure; no runtime-core changes
- Go signals:
  - service-grounded booking turns normalize to `reply_type=time`
  - active-slot follow-up surfaces under booking tag are rewritten into existing mixed-slot family
  - generic service-missing booking turns can still use `service_choice`
- No-go signals:
  - runtime webhook/core diffs in this block
  - loss of generic `service_choice` clarify coverage when service is genuinely missing
  - new scenario family creation instead of normalization into existing canon
- Rollback verification:
  - revert only the bounded booking-tag normalization helpers if generic booking coverage regresses

## Invariant

1. No runtime-core semantic changes in this block.
2. Service-grounded booking turns must open `time` collect, not `service_choice`.
3. Existing active-slot families remain `ask_about_requested_slot` / `mixed_fill_plus_question`; no new ontology in this block.
4. Generic service-missing booking turns remain allowed to request `service_choice`.

## Scope

- `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o27-booking-tag-stateful-normalization-a1.md`
- `scripts/booking_dialog_scenarios.py`
- `truffles-api/tests/test_booking_dialog_scenarios_script.py`

## Out of scope

- `ops/diagnose.py` oracle changes unless needed for deterministic guard coverage
- runtime owner changes
- lock/replay acceptance evidence

## Touch-list

- `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o27-booking-tag-stateful-normalization-a1.md`
- `scripts/booking_dialog_scenarios.py`
- `truffles-api/tests/test_booking_dialog_scenarios_script.py`

## Plan (1..N)

1. Record `r26` as audited non-closure evidence and freeze the booking-tag normalization RCA.
2. Add service-grounded booking expectation normalization.
3. Retag booking-tag mixed slot follow-ups into the existing mixed-slot family when active `time` context exists.
4. Add deterministic regressions for service-grounded booking, generic service-missing booking, and active mixed follow-up rewrite.
5. Sync active TPs so the next expensive step becomes fresh guarded `r27` only after deterministic closure.

## DoD

1. `r26` is recorded as non-closure evidence.
2. Service-grounded booking turns normalize to `reply_type=time`.
3. Generic service-missing booking turns can still keep `service_choice`.
4. Booking-tag active mixed slot follow-ups rewrite to `mixed_fill_plus_question` under active `time` context.
5. Focused generator tests are green.
6. The next admissible expensive step is one fresh guarded rerun on the updated booking-tag normalization contour.

## Expensive run control (mandatory)

- Hypothesis:
  - after `P1.6o27`, fresh guarded rerun will no longer stop on booking-tag `service_choice` / mixed-slot expectation drift from `r26`
- Expected measurable effect:
  - the next run proceeds beyond dialog `1`, turn `2`; if a new blocker appears, it is a different family than `expected_meta_mismatch` / `expected_trace_miss` on this booking-tag normalization gap
- Stop condition:
  - run exactly one fresh guarded rerun plus strict audit; if the same family persists, stop the loop and reopen RCA instead of chaining more reruns

## Checks

- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r26 --status done --strict-artifacts`
- `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "service_grounded_booking or mixed_slot_question or orphan_pending_question"`
- `git diff --check`

## Execution status

- `done`

## Evidence

1. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r26/summary.json`
2. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r26/brief.md`
3. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r26/failure_families.json`
4. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r26/scenarios.json`
5. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r26/responses.jsonl`
6. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r26/trace_bundle.jsonl`
7. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r26/manual_audit.md`

## Rollback

1. Revert only the bounded booking-tag normalization helpers if generic booking coverage regresses.
2. Keep `P1.6o6` blocked; do not reinterpret `r26` as closure evidence.

## No-go

- no runtime behavior changes
- no weak-oracle waiver for service-grounded booking turns
- no fresh guarded rerun before deterministic closure is green

## Risks/Blockers

1. Another adjacent scenario-state family may surface once booking-tag normalization is corrected.
2. Transport/tool-hook infra gaps remain orthogonal and do not close `P1.6o6`.

## Residual architecture debt (mandatory)

- Current residuals accepted in this block:
  - fresh guarded proof remains open via `P1.6o6` after this deterministic booking-tag normalization closure
- Why not in this block:
  - this block only realigns booking-tag state normalization and cannot itself provide guarded proof
- Risk if deferred:
  - valid runtime answers continue to fail on scenario-side booking/service-choice drift before the next real family surfaces
- Linked follow-up Task Package(s):
  - `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`
- Expiry/trigger to stop deferral:
  - before the next fresh guarded rerun

## Next-block contract (mandatory)

- Next block objective:
  - keep `P1.6o27` closed via `r27` rollover evidence and continue with bounded `P1.6o28` before fresh guarded `r28`
- First deterministic check command:
  - `pytest -q truffles-api/tests/test_message_endpoint.py -k "pending_question_act_time_guidance or semantic_temporal_scope_missing" && pytest -q truffles-api/tests/test_booking_quality_response_guard.py -k "pending_question_interaction or booking_slot_stall" && git diff --check`
- Blocked-by conditions:
  - `P1.6o28` is not deterministically green
  - runtime `http://127.0.0.1:18184` is not refreshed after the next code delta
  - guarded preflight is not green
- Owner role for closure:
  - `Hands` for doc/code/evidence, `Top Architect + Brain` for closure gating

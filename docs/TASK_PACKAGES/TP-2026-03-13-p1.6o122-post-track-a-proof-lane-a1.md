# TP-2026-03-13-p1.6o122-post-track-a-proof-lane-a1

- Название/цель: после deterministic closure `P1.6o121` выполнить один fresh guarded `dev L2`, чтобы доказать, что surfaced `Track A` slot-guidance oracle drift больше не является first fail, и truthfully surface-ить следующий remaining class, если он есть.

## Canon refs

- Parent closure program: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Main remediation: `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- Process reset / class triage: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o118-class-level-closure-process-reset-a1.md`
- Prior structural slices: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o119-timeout-degrade-owner-boundary-first-slice-a1.md`, `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o121-booking-slot-guidance-scenario-convergence-first-slice-a1.md`
- Prior proof lane: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o120-post-track-c-proof-lane-a1.md`
- Machine-readable owner matrix: `truffles-api/app/knowledge/generic/INTERACTION_OWNER_MATRIX.yaml`
- State baseline: `STATE.md`

## Git context

- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `origin/main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect` remove branch + worktree after merge

## One web search (mandatory before implementation)

- Query: `site:www.uvicorn.org/settings uvicorn host port admin version runtime fingerprint`
- Date/time: `reused official proof-lane reference from P1.6o120 (2026-03-13T10:20:00+05:00)`
- Sources opened:
  - official Uvicorn settings docs: `https://www.uvicorn.org/settings/`
- Source quality:
  - official product documentation
- Found reusable ideas:
  - proof runtime startup should stay explicit and reproducible via CLI flags;
  - fingerprint validation must stay external to the app process and be recorded before the guarded run starts.
- Decision:
  - `reuse`
  - reuse the established live proof-run startup pattern on `:18184` and record `/admin/version == HEAD` before the single guarded run.
- Rejected options:
  - detached startup patterns that hide runtime parity;
  - additional expensive runs in the same block.

## Root cause (mandatory)

- Symptom:
  - `P1.6o121` is green on deterministic contour, but there is still no fresh proof run showing that the surfaced `Track A` slot-guidance oracle drift is no longer the first fail family.
- Minimal reproduction:
  1. Use `/tmp/booking_quality/p1.6o120-l2-dev-20260313-a1-r1` as the pre-fix proof baseline.
  2. Confirm it surfaced `dialog 1 / turn 2 / "Какие дни у вас доступны?"` with generated `slot_compare` oracle expectation while runtime canonically preserved `booking_slot_guidance` / `ask_about_requested_slot(time)`.
  3. Confirm `P1.6o121` now deterministically canonicalizes this bounded scenario family on the generator/oracle path.
  4. Run one fresh guarded `dev L2` on a live runtime whose `/admin/version` matches current `HEAD`.
  5. Audit the resulting run strictly and classify the new first fail through `P1.6o118` rules.
- Evidence:
  - `/tmp/booking_quality/p1.6o120-l2-dev-20260313-a1-r1/summary.json`
  - `/tmp/booking_quality/p1.6o120-l2-dev-20260313-a1-r1/brief.md`
  - `/tmp/booking_quality/p1.6o120-l2-dev-20260313-a1-r1/scenarios.json`
  - `/tmp/booking_quality/p1.6o120-l2-dev-20260313-a1-r1/responses.jsonl`
  - `/tmp/booking_quality/p1.6o120-l2-dev-20260313-a1-r1/trace_bundle.jsonl`
  - `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o121-booking-slot-guidance-scenario-convergence-first-slice-a1.md`
- Five Whys:
  1. Why is closure still incomplete? Because deterministic green alone does not prove the expensive lane rolled past the covered `Track A` family.
  2. Why is a proof lane needed now? Because the process accepts progress only when a fresh guarded run confirms the previous first-fail family is no longer first.
  3. Why can we run it now? Because `P1.6o121` landed the first bounded scenario-governance convergence slice and its targeted deterministic checks are green.
  4. Why must runtime be fingerprinted first? Because runtime/preflight mismatch makes the run non-canonical.
  5. Why only one run? Because the goal is truthful admission/classification of the next surfaced result, not another expensive rerun loop.
- Root cause statement:
  - the remaining gap after `P1.6o121` is proof, not local implementation: without one fresh guarded `dev L2` on a verified live runtime, `Track A` remains only deterministically closed and the next remaining class stays unproven.
- Fix mechanism:
  - start a live worktree runtime on `:18184` and prove `/admin/version == HEAD`;
  - execute exactly one guarded `dev L2` run and strict audit;
  - sync docs/state/session truthfully based on the new first-fail outcome, applying `P1.6o118` class triage instead of automatic row admission.

## Reuse-first decision (mandatory)

- Reuse:
  - existing guarded entrypoint `scripts/llm_quality_guarded.sh`
  - existing audit command `python3 ops/diagnose.py llm-quality-audit`
  - existing live runtime startup pattern used in prior proof-lane blocks
- Integrate:
  - no runtime/generator code changes unless the proof run surfaces a new deterministic or bounded family
- Build:
  - none planned in this block

## Release safety

- Rollout: none; local proof-only block
- Go signal:
  - runtime fingerprint matches `HEAD`
  - guarded run artifacts are valid enough to audit (`artifact_integrity.valid=true`)
  - first fail is no longer the covered `Track A` slot-guidance oracle family
- No-go signal:
  - invalid runtime fingerprint preflight
  - incomplete/invalid guarded artifacts
  - fresh first fail remains the same covered `Track A` family
- Rollback:
  - stop the proof lane, record evidence truthfully, do not reinterpret invalid/incomplete runs as closure evidence

## Invariant

1. `P1.6o119` and `P1.6o121` deterministic closure stay intact.
2. Targeted `Track A` deterministic regressions remain green.
3. No runtime/product contract weakening.
4. Exactly one fresh guarded `dev L2` run in this block.

## Scope

- proof-lane runtime startup + fingerprint validation
- one guarded `dev L2`
- strict audit
- doc/state/session sync for the resulting admission

## Out of scope

- new implementation before the proof run surfaces a new class or proves broader residual
- acceptance `lock/replay/full`
- broad scenario-generator convergence beyond `P1.6o121`

## Touch-list

- `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o122-post-track-a-proof-lane-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o121-booking-slot-guidance-scenario-convergence-first-slice-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o120-post-track-c-proof-lane-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o118-class-level-closure-process-reset-a1.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `STATE.md`
- `STRUCTURE.md`
- `docs/SESSIONS/SESSION-2026-02-19-llm-first-firebreak-a1.md`

## Plan (1..N)

1. Publish `P1.6o122` with proof-lane scope and runtime-fingerprint requirements.
2. Re-run the deterministic prerequisites from `P1.6o121`.
3. Start live worktree runtime on `:18184` and prove `/admin/version == HEAD`.
4. Execute exactly one fresh guarded `dev L2` and strict audit.
5. Sync docs/state/session with truthful post-run admission/classification.

## DoD

1. Fresh guarded `dev L2` is executed on a fingerprint-verified live runtime.
2. Covered `Track A` slot-guidance oracle family is no longer the first fail.
3. The next first fail, if any, is captured truthfully and classified by class, not auto-admitted as a new row.
4. Docs/state/session are synced to the new admission truth.

## Checks

- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o120-l2-dev-20260313-a1-r1 --status done --strict-artifacts`
- `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "slot_compare_requested_slot_overclaim or slot_compare_days_availability or preserves_generic_slot_compare_without_exact_time or rewrites_check_booking_like_query_without_prior_management_tag"`
- `pytest -q truffles-api/tests/test_booking_quality_response_guard.py -k "booking_slot_stall_not_reported_for_ask_about_requested_slot_contract"`
- `TEST_MODE=1 scripts/llm_quality_guarded.sh --mode lock --run-id p1.6o122-l2-dev-20260313-a1-r1 --allow-pending-previous --allow-repeat-fingerprint -- --base-url http://127.0.0.1:18184 --client-slug demo_salon --mode llm --count 10 --min-turns 10 --max-turns 15 --include-media --scenario-coverage booking,info,interrupt,handoff --tool-hooks auto --jid-mode unique --allow-non-allowlist --judge-mode all --quality-lane dev --run-economy-gate block --fail-on-thresholds --max-failures 1 --reset-before-dialog`
- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o122-l2-dev-20260313-a1-r1 --status done --strict-artifacts`
- `git diff --check`
- `SESSION_AGENT=a1 scripts/session_check.sh`

## Execution status

- `done`
- `P1.6o122` completed on a fingerprint-verified live runtime after deterministic closure of `P1.6o121`
- audited run truth:
  - `run_id=p1.6o122-l2-dev-20260313-a1-r1`
  - `artifact_integrity.valid=true`
  - `infra_valid=true`
  - `semantic_valid=false`
  - `stop_reason=max_failures_reached:1`
- the covered slot-guidance `Track A` family from `P1.6o120`/`P1.6o121` is no longer the first fail
- the new surfaced first fail is `dialog 1 / turn 9 / "Сколько стоит маникюр?"` after turn 8 timeout-degraded collect on `Когда у вас есть свободные слоты?`
- surfaced class triage:
  - runtime answers the pricing turn truthfully through `catalog.service_query`
  - runtime continuity should resume on explicit `catalog_service_booking_progress`, but the surfaced turn still falls back to stale continuity state/reason instead of stamping fresh contract evidence
  - generated expectations still carry stale `service_choice` into the factual interrupt family because the runtime contract is not explicit enough
  - routed as a bounded runtime continuity continuation with required oracle sync, not as a new row-local child
- no new matrix row is admitted from this run; the next structural block is `P1.6o123`

## Evidence

1. `/tmp/booking_quality/p1.6o122-l2-dev-20260313-a1-r1/summary.json`
2. `/tmp/booking_quality/p1.6o122-l2-dev-20260313-a1-r1/brief.md`
3. `/tmp/booking_quality/p1.6o122-l2-dev-20260313-a1-r1/scenarios.json`
4. `/tmp/booking_quality/p1.6o122-l2-dev-20260313-a1-r1/responses.jsonl`
5. `/tmp/booking_quality/p1.6o122-l2-dev-20260313-a1-r1/trace_bundle.jsonl`
6. `/tmp/booking_quality/p1.6o122-l2-dev-20260313-a1-r1/manual_audit.md`
7. `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o121-booking-slot-guidance-scenario-convergence-first-slice-a1.md`

## Rollback

1. Stop the local runtime and keep `P1.6o121` deterministic closure intact.
2. If the run is invalid/incomplete, record it truthfully and keep `P1.6o122` open.
3. Do not mutate the matrix or docs to hide surfaced evidence.

## No-go

- no second expensive run in this block
- no new implementation before the proof run surfaces the next class
- no reuse of invalid preflight evidence as closure proof
- no automatic `Mxx -> child TP` admission without `P1.6o118` class triage

## Risks / blockers

- local runtime may fail fingerprint validation or die before the guarded run starts
- the next surfaced result may still look like oracle drift first, but the actual blocker can still be a runtime continuity gap that oracle merely exposes
- the run may remain infra-invalid, which blocks closure admission even if the first fail moves

## Residual architecture debt (mandatory)

### Current residuals accepted in this block

- runtime continuity around factual service interrupts remains incomplete; oracle drift on the surfaced family is secondary to that missing explicit contract evidence
- broader timeout/degrade migration beyond `P1.6o119` remains open
- acceptance proof (`lock/replay/full`) remains blocked

### Why not in this block

- this block is proof-only; it verifies whether the first structural `Track A` slice moved the expensive lane and truthfully classifies the next remaining class

### Risk if deferred

- the team can misread deterministic green as closure and skip the only admissible proof step

### Linked follow-up Task Package(s)

- `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o123-active-service-choice-service-info-progress-scenario-convergence-a1.md`
- `P1.7` acceptance replay gate after closure-program proof is admissible

### Expiry/trigger to stop deferral

- before any further guarded `dev/acceptance` rerun beyond `P1.6o122`

## Next-block contract (mandatory)

### Next block objective

- deterministically close `P1.6o123`, the next bounded runtime continuity continuation for service-grounded factual info interrupts after degraded collect, and sync oracle expectations only to the explicit runtime contract emitted on that contour.

### First deterministic check command

- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o122-l2-dev-20260313-a1-r1 --status done --strict-artifacts && pytest -q truffles-api/tests/test_expected_reply_contract.py -k "catalog_service_booking_progress" && pytest -q truffles-api/tests/test_message_endpoint.py -k "service_info_interrupt_under_degraded_collect" && git diff --check`

### Blocked-by conditions

- `P1.6o122` proof truth is not synced truthfully in active docs/session
- `P1.6o123` is not published as the selected structural continuation
- targeted `P1.6o123` deterministic runtime/scenario/oracle checks are not green

### Owner role for closure

- `Top Architect + Brain`

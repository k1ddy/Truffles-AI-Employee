# TP-2026-03-11-p1.6o75-active-time-consult-topic-shift-service-choice-owner-a1

- Название/цель: закрыть surfaced `r104` blocker family, где после bounded `M23` closure fresh guarded `dev L2` больше не падает на declarative named-specialist follow-up, но under active `time` collect + post-booking handoff standalone consult topic shift (`Я интересуюсь дизайном ногтей.`) still leaves stale booking resume `time`; the next follow-up `Какие у вас есть варианты?` is then routed to `calendar.list_slots` instead of the consult/service-choice owner.

## Canon refs

- Parent verdict: `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o60a-remaining-closure-architecture-verdict-a1.md`
- Owner matrix: `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o60b-remaining-closure-owner-matrix-a1.md`
- Program TP: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Proof bundle: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`
- Prior child: `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o74-active-name-named-specialist-followup-owner-a1.md`
- Related bounded closures: `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o22-consult-cta-check-booking-owner-boundary-a1.md`, `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o71-check-booking-stale-service-choice-governance-a1.md`
- STATE: `STATE.md`
- CA: remaining closure row after `M23`

## One web search (mandatory before implementation)

- Query: `site:rasa.com/docs/rasa/forms unhappy path requested_slot change topic`
- Date/time: `2026-03-11T18:53:54+05:00`
- Sources opened:
  - official Rasa docs: `https://rasa.com/docs/rasa/forms/`
- Source quality:
  - official product documentation
- Found reusable ideas:
  - active slot collection must make topic shifts explicit; when the user enters an unhappy-path or changes the subject, the old requested-slot contract cannot silently remain active;
  - the flow should either deactivate the current requested slot or replace it with the new collection contract before the next follow-up turn is interpreted.
- Decision:
  - `reuse/integrate`
  - reuse the existing expected-reply / question-contract boundary and integrate a bounded consult topic-shift row that clears stale booking `time` resume and moves the next turn into consult/service-choice ownership.
- Rejected options:
  - weakening oracle to accept stale `expected_reply_type=time` after a standalone consult shift;
  - accepting `calendar.list_slots` as a valid answer to `Какие у вас есть варианты?` once the previous turn already switched to nail-design consult;
  - broad phrase-hardcode branching outside the bounded active-time consult topic-shift row.

## Root cause (mandatory)

- Symptom:
  - fresh guarded `dev L2` run `p1.6o6-l2-dev-20260311-a1-r104` no longer stops on `M23`, but the next first strict fail appears in dialog `2`, turn `7`, user text `Какие у вас есть варианты?`
- Minimal reproduction:
  1. Inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r104/scenarios.json` dialog `2`, turns `1-7`.
  2. Confirm turns `1-4` ground booking for `Стрижка`, explicit `20:00`, and customer name `Марина`.
  3. Confirm turn `5` is handoff (`Можно связаться с менеджером?`) and turn `6` switches topic to standalone consult (`Я интересуюсь дизайном ногтей.`), with generated scenario expecting `reply_type=service_choice` for the new consult follow-up lane.
  4. Inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r104/trace_bundle.jsonl` dialog `2`, turn `6` and confirm runtime returns `action=reply`, `intent=consult_reply`, `source=pack`, but `question_contract` still carries `expected_reply_type=time` from the earlier booking flow.
  5. Inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r104/scenarios.json` dialog `2`, turn `7` and confirm the follow-up `Какие у вас есть варианты?` expects `service_choice` meta/trace evidence.
  6. Inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r104/responses.jsonl` and `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r104/trace_bundle.jsonl` dialog `2`, turn `7` and confirm runtime executes `calendar.list_slots`, preserves `expected_reply_type=time`, and replies with slot availability (`На 20:00 свободного окна нет...`) instead of consult/service options.
- Evidence:
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r104/summary.json`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r104/brief.md`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r104/scenarios.json`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r104/responses.jsonl`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r104/trace_bundle.jsonl`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r104/manual_audit.md`
- Five Whys:
  1. Why did the chain fail? The follow-up `Какие у вас есть варианты?` answered booking slot availability instead of the newly introduced consult topic.
  2. Why did runtime list slots? `question_contract` still treated the conversation as active booking `time` collect.
  3. Why was booking `time` still active? The standalone consult shift on turn `6` (`Я интересуюсь дизайном ногтей.`) returned `consult_reply`, but the runtime did not clear or replace the earlier booking resume contract.
  4. Why did that matter on the next turn? The vague follow-up `Какие у вас есть варианты?` was interpreted through stale `expected_reply_type=time`, so policy/runtime chose `calendar.list_slots`.
  5. Why is this a new bounded family? Prior children closed consult-CTA bleed (`P1.6o22`), stale `service_choice` governance (`P1.6o71`), exact-time grounding (`P1.6o73`), and active-name specialist follow-up (`P1.6o74`), but the cross-product `active time booking resume -> standalone consult topic shift -> generic follow-up` was never frozen.
- Root cause statement:
  - the remaining closure lane lacks a bounded owner boundary for standalone consult/service-topic shifts under active booking `time` collect: consult replies do not clear or replace the stale booking resume contract, so the next generic follow-up is still interpreted as a booking slot question.
- Fix mechanism:
  - freeze matrix row `M24` for active-time consult topic-shift ownership;
  - ensure scenario/runtime contract explicitly clears or replaces stale booking `expected_reply_type=time` when the user switches into a standalone consult/service-choice lane;
  - add a bounded runtime guard so consult/service-choice follow-ups cannot fall through `calendar.list_slots` while the new consult topic is active.

## Reuse-first decision (mandatory)

- Reuse:
  - existing `question_contract` / expected-reply boundary in `decision.py`
  - existing scenario-governance service-choice normalization seams in `scripts/booking_dialog_scenarios.py`
  - existing quality guard patterns for `expected_reply_type` / trace evidence
- Integrate:
  - bounded active-time consult topic-shift row over the existing consult + service-choice contract
  - bounded stale-booking-resume clear boundary when consult becomes the semantic owner
- Do not build:
  - new consult recommender flow
  - new handoff state machine
  - oracle waiver for stale `time`

## Execution profile

- TP mode: `implementation`
- Doc touch budget (files): `11`
- Code dominance: `required`
- Override token: `none`
- Why this profile fits: surfaced family is a bounded owner-boundary row where the semantic shift already exists (`consult_reply`), but runtime/scenario still preserve the wrong active resume axis.

## Invariant

1. `P1.6o74/M23` remains closed.
2. Existing active-time requested-slot families (`M7`, `M11`, `M22`) remain behaviorally consistent.
3. Existing consult/service-choice and booking-management rows remain behaviorally consistent.
4. Standalone consult topic shifts must not silently inherit stale booking `expected_reply_type=time` once consult becomes the semantic owner.
5. Any bounded fix must preserve explicit `decision_meta` / `decision_trace` evidence.

## Scope

- publish surfaced `r104` family as matrix row `M24`
- implement bounded active-time consult topic-shift owner only

## Out of scope

- acceptance lock/replay
- generic consult recommendation redesign
- handoff state-machine redesign
- reopening `M23` after it already rolled to a different family

## Touch-list

- `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o75-active-time-consult-topic-shift-service-choice-owner-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o60b-remaining-closure-owner-matrix-a1.md`
- `truffles-api/app/routers/webhook/response.py`
- `truffles-api/tests/test_consult_followup_guard.py`
- `truffles-api/tests/test_message_endpoint.py`
- `truffles-api/tests/test_booking_quality_response_guard.py`

## Plan (1..N)

1. Freeze `r104` as audited non-closure evidence and publish `M24`.
2. Align scenario/runtime contract so standalone consult/service-topic shifts under active `time` collect replace stale booking resume with consult/service-choice ownership.
3. Add a bounded runtime guard so follow-up generic `варианты` questions cannot fall through `calendar.list_slots` after a consult topic shift.
4. Add targeted deterministic regressions plus one adjacent negative case where booking time follow-up is still genuinely intended.
5. Run focused checks and `git diff --check`.
6. Authorize a fresh guarded `dev L2` only after deterministic green.

## DoD

1. `r104` is recorded in active docs as audited non-closure evidence.
2. Standalone consult/service-topic shifts under active booking `time` collect no longer preserve stale `expected_reply_type=time` into the next turn.
3. Final meta/trace preserve consult/service-choice ownership instead of `calendar.list_slots` for the surfaced family.
4. Adjacent real booking time follow-ups remain unchanged.
5. Focused deterministic scenario/runtime/evidence regressions are green.
6. Fresh guarded `dev L2` no longer stops on the `r104` family.

## Checks

- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r104 --status done --strict-artifacts`
- `pytest -q truffles-api/tests/test_consult_followup_guard.py -k "consult_followup or locked_consult_topic_shift"`
- `pytest -q truffles-api/tests/test_message_endpoint.py -k "locked_time_consult_topic_shift_sets_service_choice_owner or consult_reply_with_service_hint_sets_service_expected_reply_for_booking_cta"`
- `pytest -q truffles-api/tests/test_booking_quality_response_guard.py -k "consult_topic_shift_service_choice_gap or invalid_schema_service_grounded_booking_service_choice_gap"`
- `pytest -q truffles-api/tests/test_message_endpoint.py -k "expected_service_turn_direct_booking_request_keeps_booking_prompt or stale_service_choice_bypasses_to_check_booking_prompt or llm_policy_core_list_slots_keeps_context_datetime_when_expected_service_choice"`
- `git diff --check`

## Execution status

- `closed_bounded`
- fresh guarded `dev L2` `p1.6o6-l2-dev-20260311-a1-r104` is audited and frozen as the next blocker
- bounded runtime fix is implemented in `truffles-api/app/routers/webhook/response.py`
- deterministic targeted regressions are green:
  - `pytest -q truffles-api/tests/test_consult_followup_guard.py -k "consult_followup or locked_consult_topic_shift"` -> `7 passed`
  - `pytest -q truffles-api/tests/test_message_endpoint.py -k "locked_time_consult_topic_shift_sets_service_choice_owner or consult_reply_with_service_hint_sets_service_expected_reply_for_booking_cta"` -> `2 passed`
  - `pytest -q truffles-api/tests/test_booking_quality_response_guard.py -k "consult_topic_shift_service_choice_gap or invalid_schema_service_grounded_booking_service_choice_gap"` -> `2 passed`
  - `pytest -q truffles-api/tests/test_message_endpoint.py -k "expected_service_turn_direct_booking_request_keeps_booking_prompt or stale_service_choice_bypasses_to_check_booking_prompt or llm_policy_core_list_slots_keeps_context_datetime_when_expected_service_choice"` -> `3 passed`
- fresh guarded `dev L2` `p1.6o6-l2-dev-20260311-a1-r105` no longer stops on `M24` and instead surfaces `M25`

## Evidence

1. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r104/summary.json`
2. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r104/brief.md`
3. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r104/scenarios.json`
4. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r104/responses.jsonl`
5. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r104/trace_bundle.jsonl`
6. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r104/manual_audit.md`
7. `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o74-active-name-named-specialist-followup-owner-a1.md`
8. `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o75-active-time-consult-topic-shift-service-choice-owner-a1.md`

## Rollback

1. Keep `P1.6o6` blocked.
2. Revert only the future bounded `M24` delta if it regresses adjacent consult or booking-time rows.
3. Do not roll back prior bounded rows (`M23`, `M22`, `M20`).

## No-go

- no oracle weakening to accept stale `expected_reply_type=time` after standalone consult shift
- no treating `calendar.list_slots` as valid closure for the surfaced consult follow-up family
- no phrase-hardcode branch outside the bounded active-time consult topic-shift row
- no changes to unrelated booking-management or handoff families

## Risks / blockers

- `judge` already conflicts with contract on `r104`, so contract-first trace/meta invariants must remain the winner.
- The live row is a runtime owner-boundary gap, not just scenario drift: turn `6` already replies as consult, but stale booking resume remains active.
- `r104` is still non-canonical proof evidence (`infra_valid=false`, `semantic_valid=false`, `stop_reason=max_failures_reached:1`). This block can only claim bounded closure plus rerun rollover.

## Residual architecture debt (mandatory)

### Current residuals accepted in this block

- Active-time consult topic-shift ownership remains separate from generic consult-CTA bleed and booking-management rows.
- Runtime still relies on shared `expected_reply_type` + side-channel intent ownership instead of a fuller owner object.

### Why not in this block

- `M24` is specifically about standalone consult shift under active booking `time` collect. Broadening into all consult/topic-change families would violate bounded-row closure.

### Risk if deferred

- Later guarded reruns can continue to hide true consult follow-up behavior behind stale booking slot responses.

### Linked follow-up Task Package(s)

- next surfaced row after fresh guarded rerun, if any

### Expiry/trigger to stop deferral

- before the next guarded rerun after `M24`, if standalone consult topic shifts still do not have deterministic green evidence.

## Next-block contract (mandatory)

### Next block objective

- implement `M24`, then rerun focused deterministic scenario/runtime checks before any new guarded `dev L2`.

### First deterministic check command

- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r104 --status done --strict-artifacts && git diff --check`

### Blocked-by conditions

- `M24` is not published in the owner matrix
- active-time consult topic-shift scenario/runtime regressions are not green
- bounded runtime guard still allows stale booking `time` resume to drive consult follow-up turns into `calendar.list_slots`

### Owner role for closure

- `Hands` for deterministic implementation/evidence, `Top Architect + Brain` for guarded-rerun authorization and closure

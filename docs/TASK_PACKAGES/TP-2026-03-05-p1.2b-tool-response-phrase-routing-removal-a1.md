# TP-2026-03-05-p1.2b-tool-response-phrase-routing-removal-a1

- Название/цель: P1.2b убрать оставшийся keyword/phrase routing из tool-response arbitration path (`catalog.location` info_sections enrichment) в `decision.py`.
- Parent TP: `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`.
- Canon refs: `AGENTS.md`, `STATE.md`, `STRATEGY/REQUIREMENTS.md`, `SPECS/SYSTEM_REFERENCE.md`.
- Branch: `fix/llm-first-firebreak-2026-02-19`.
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`.

## One web search (mandatory before implementation)

- Query: `site:csrc.nist.gov "policy enforcement point" "policy decision point"`
- Time (UTC): `2026-03-05T13:43:58Z`
- Sources:
  - `https://csrc.nist.gov/glossary/term/policy_enforcement_point`
  - `https://csrc.nist.gov/glossary/term/policy_decision_point`
  - `https://csrc.nist.gov/pubs/sp/800/207/final`
- Ready solutions found:
  - PDP/PEP separation: enforcement validates and applies policy, but does not invent semantic policy decisions.
- Decision (`reuse/integrate/build`): `integrate`
  - Keep owner/resolver semantics; remove core keyword enrichment in runtime arbitration.
- Rejected options:
  - Keep keyword enrichment behind feature flag.
  - Add new lexicon exceptions in core.

## Root cause (mandatory)

- Symptom:
  - Tool-response arbitration still enriches `info_sections_hint` using core keyword/phrase checks.
- Minimal reproduction:
  - `rg -n 'policy_intent_hint|"мест" in normalize_for_matching|"парков"|"паркинг"|"стоян"|"тұрақ"' truffles-api/app/routers/webhook/decision.py`
- Evidence:
  - `catalog.location` path adds `parking` from:
    - `policy_payload.intent` substring,
    - carryover phrase `мест`,
    - hardcoded marker tuple `("парков", "паркинг", "стоян", "тұрақ")`.
- Five Whys:
  1. Why present: historical recall fallback for parking/location followups.
  2. Why in core: monolithic orchestration mixed semantic and enforcement concerns.
  3. Why risky: language/business coupling in runtime core.
  4. Why architecture violation: deterministic layer infers semantics outside owner/resolver contract.
  5. Why must fix now: P1 single-owner closure requires core arbitration to be phrase-agnostic.
- Root cause statement:
  - Tool-response arbitration still contains inline phrase heuristics, creating a latent secondary semantic path.
- Fix mechanism:
  - Remove keyword-based enrichment; preserve only owner/resolver-derived sections and deterministic defaults.

## Invariant

1. Semantic ownership stays in policy-core + resolver.
2. Deterministic layer performs protocol/boundary logic only.
3. No direct phrase/keyword semantic inference added in core tool-response path.

## Scope

- `truffles-api/app/routers/webhook/decision.py`
- `truffles-api/tests/test_message_endpoint.py`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-05-p1.2b-tool-response-phrase-routing-removal-a1.md`

## Out of scope

- P1.3 boundary hard lock.
- P3 booking transactional protocol.
- P4 fact evidence linkage.
- P6 open-world generator.

## Plan (1..N)

1. Remove keyword/phrase parking enrichment branch in `policy_tool_action == "catalog.location"`.
2. Keep only owner/resolver derived `info_sections_hint` + deterministic `TOOL_INFO_SECTION_MAP` default.
3. Update tests that asserted removed keyword behavior.
4. Run deterministic checks.

## DoD

1. No keyword-based `parking` enrichment from `policy_payload.intent`, `policy_payload.reason`, `"мест"`, or hardcoded parking marker tuple in this tool-response path.
2. Contract tests updated to owner/resolver behavior.
3. Deterministic test pack green.

## Checks

- Static:
  - `rg -n 'policy_intent_hint|"мест" in normalize_for_matching|"парков"|"паркинг"|"стоян"|"тұрақ"' truffles-api/app/routers/webhook/decision.py`
- Deterministic:
  - `pytest -q truffles-api/tests/test_message_endpoint.py`
  - `pytest -q truffles-api/tests/test_booking_chaos_dialogs.py`
  - `pytest -q truffles-api/tests/test_booking_quality_response_guard.py`
  - `pytest -q truffles-api/tests/test_demo_salon_eval.py`

## Execution status

- `done`
- Static check results:
  - `policy_intent_hint`, parking marker tuple, and `"мест"` carryover enrichment pattern are not found in `decision.py`.
- Deterministic check results:
  - `test_message_endpoint.py`: `282 passed`.
  - `test_booking_chaos_dialogs.py`: `1 passed`.
  - `test_booking_quality_response_guard.py`: `57 passed`.
  - `test_demo_salon_eval.py`: `17 passed`.

## Evidence

1. Code diff (P1.2b only).
2. Static check output.
3. Deterministic test output.

## Rollback

1. `git revert <commit_with_p1.2b_changes>`
2. Re-run deterministic checks.

## No-go

- No reintroduction of phrase routing via aliases/hidden helpers.
- No new semantic fallback in core runtime path.

## Risks/Blockers

1. Legacy tests asserting reason/intent-based parking hint must be rewritten.
2. Monolith complexity can hide coupled side effects.

## Residual architecture debt (mandatory)

- Current residuals accepted in this block:
  - Potential phrase-coupled paths outside this exact slice.
  - `P1.3`, `P1.4`, `P1.5` pending.
- Why not in this block:
  - Atomic block closure discipline.
- Risk if deferred:
  - P1 remains incomplete.
- Linked follow-up Task Package(s):
  - `TP-2026-03-05-p1.3-...`.
- Expiry/trigger to stop deferral:
  - Before P1.3 starts.

## Next-block contract (mandatory)

- Next block objective:
  - `P1.3` deterministic boundary hard lock.
- First deterministic check command:
  - `rg -n '_register_policy_override\(|policy_semantic_override' truffles-api/app/routers/webhook/decision.py`
- Blocked-by conditions:
  - Any regression in deterministic pack.
- Owner role for closure:
  - `Top Architect + Brain`

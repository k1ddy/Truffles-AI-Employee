# TP-2026-03-11-p1.6o72-active-name-deictic-time-availability-owner-a1

- Название/цель: закрыть surfaced `r101` blocker family, где после bounded `M20` closure fresh guarded `dev L2` больше не падает на stale `check_booking/service_choice` governance, но under active `name` collect deictic time-availability follow-up `А есть ли у вас места в это время?` still collapses into generic name collect: generated scenario keeps stale `service_choice`, `llm_policy_core` keeps `subject_kind=booking`, `capability=live_availability`, `temporal_scope=specific_time`, but leaves `pending_question_target` / `active_question_relation` blank and asks for name instead of answering or clarifying the requested slot.

## Canon refs

- Parent verdict: `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o60a-remaining-closure-architecture-verdict-a1.md`
- Owner matrix: `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o60b-remaining-closure-owner-matrix-a1.md`
- Program TP: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Proof bundle: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`
- Prior child: `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o71-check-booking-stale-service-choice-governance-a1.md`
- Related bounded closure: `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o63-active-name-time-availability-followup-owner-a1.md`
- STATE: `STATE.md`
- CA: remaining closure row after `M20`

## One web search (mandatory before implementation)

- Query: `site:rasa.com/docs/rasa/forms requested slot follow-up explicit time reference`
- Date/time: `2026-03-11T16:44:00+05:00`
- Sources opened:
  - official Rasa docs: `https://rasa.com/docs/rasa/forms/`
- Source quality:
  - official product documentation
- Found reusable ideas:
  - after a slot is already grounded, a user can still ask a side-question about that same slot and the form should keep the current requested slot contract explicit instead of blindly advancing the next slot;
  - interruption/follow-up handling must preserve the relation to the active requested slot, including referential questions about an already grounded value.
- Decision:
  - `reuse/integrate`
  - reuse the existing active-`name` follow-up/runtime contract and integrate a bounded deictic requested-slot owner for grounded specific time references.
- Rejected options:
  - weakening scenario/oracle to accept stale `service_choice`;
  - treating generic `name` collect as closure just because `datetime` is already grounded;
  - phrase-hardcode branching in runtime outside the active `name` follow-up boundary.

## Root cause (mandatory)

- Symptom:
  - fresh guarded `dev L2` run `p1.6o6-l2-dev-20260311-a1-r101` stops at dialog `1`, turn `5`, user text `А есть ли у вас места в это время?`
- Minimal reproduction:
  1. Inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r101/scenarios.json` dialog `1`, turns `4-5`.
  2. Confirm turn `4` explicitly grounds `datetime` (`Я хочу записаться на завтра в 15:00.`) and advances canonical resume to `reply_type=name`.
  3. Inspect turn `5` in `scenarios.json` and confirm generated scenario still tags the turn as generic `booking` with stale `reply_type=service_choice`.
  4. Inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r101/trace_bundle.jsonl` for dialog `1`, turn `5` and confirm `llm_policy_core` keeps `capability=live_availability`, `temporal_scope=specific_time`, `resolution_mode=referent_followup`, but leaves `pending_question_target` / `active_question_relation` blank and sets `next_question=name`.
  5. Confirm judge fails the visible answer as `missed_question`, so this is not only a governance drift: runtime semantics also miss the active-name requested-slot follow-up owner.
- Evidence:
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r101/summary.json`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r101/brief.md`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r101/scenarios.json`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r101/responses.jsonl`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r101/trace_bundle.jsonl`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r101/manual_audit.md`
- Five Whys:
  1. Why did turn `5` fail? The bot asked for the user's name instead of addressing availability at the grounded time.
  2. Why did the bot ask for name? `llm_policy_core` treated the turn as ordinary collect continuation with `next_question=name`.
  3. Why did policy-core do that? The deictic time follow-up over an already grounded `datetime` was not expressed as its own active-question relation.
  4. Why is that wrong? Under active `name` collect, `в это время` is still a requested-slot availability probe about the grounded time, not a plain slot-fill continuation.
  5. Why is this a new bounded family? `P1.6o63` closed explicit alternate-time probes under active `name` collect, and `P1.6o71` closed stale `check_booking/service_choice` governance; neither closed deictic requested-slot follow-ups over the grounded current time.
- Root cause statement:
  - the remaining closure lane still lacks a bounded owner contract for active-`name` deictic time-availability follow-ups: scenario governance leaves stale `service_choice`, and `llm_policy_core` / runtime leave `pending_question_target` / `active_question_relation` blank and collapse the turn into generic name collect.
- Fix mechanism:
  - freeze matrix row `M21` for active-name deictic time-availability follow-up;
  - align scenario normalization so grounded-time deictic availability questions do not inherit stale `service_choice`;
  - add prompt/runtime owner contract so deictic requested-slot follow-ups preserve `pending_question_target=time`, `active_question_relation=ask_about_requested_slot`, and resume `name`.

## Reuse-first decision (mandatory)

- Reuse:
  - existing active-name alternate-time follow-up contract from `P1.6o63`
  - existing pending-question / requested-slot governance helpers in `scripts/booking_dialog_scenarios.py`
  - existing runtime owner/arbitration seams around active `name` collect
- Integrate:
  - bounded deictic-time follow-up normalization in scenario governance
  - bounded semantic owner/runtime fix for active-name requested-slot follow-up
- Do not build:
  - new generic booking loop
  - new oracle waiver
  - phrase-hardcode fallback outside the bounded owner path

## Execution profile

- TP mode: `implementation`
- Doc touch budget (files): `10`
- Code dominance: `required`
- Override token: `none`
- Why this profile fits: surfaced family is a new bounded row with explicit runtime/judge failure and a frozen scenario-governance sub-drift.

## Invariant

1. `P1.6o71/M20` remains closed.
2. `P1.6o63/M12` active-name explicit alternate-time follow-up remains behaviorally unchanged.
3. Grounded explicit time fills still resume `name` when no requested-slot follow-up is asked.
4. Any bounded fix must keep observability explicit in `decision_meta` / `decision_trace`.

## Scope

- publish surfaced `r101` family as matrix row `M21`
- implement bounded active-name deictic requested-slot follow-up owner only

## Out of scope

- acceptance lock/replay
- global model-routing changes
- generic deictic resolver redesign outside this row
- re-opening `M20` after it already rolled to a different family

## Touch-list

- `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o72-active-name-deictic-time-availability-owner-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o60b-remaining-closure-owner-matrix-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o60a-remaining-closure-architecture-verdict-a1.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`
- `prompts/llm_policy_core.md`
- `scripts/booking_dialog_scenarios.py`
- `truffles-api/app/routers/webhook/decision.py`
- `truffles-api/tests/test_booking_dialog_scenarios_script.py`
- `truffles-api/tests/test_message_endpoint.py`
- `truffles-api/tests/test_booking_quality_response_guard.py`

## Plan (1..N)

1. Freeze `r101` as audited non-closure evidence and publish `M21`.
2. Normalize generated scenario contract for grounded-time deictic follow-ups under active `name` collect.
3. Add bounded semantic/runtime owner so `в это время` preserves requested-slot follow-up instead of generic name collect.
4. Add targeted deterministic regressions plus one adjacent negative active-name case.
5. Run focused checks and `git diff --check`.
6. Run exactly one fresh guarded `dev L2` only after deterministic green.

## DoD

1. `r101` is recorded in active docs as audited non-closure evidence.
2. Grounded-time deictic availability follow-ups under active `name` collect no longer inherit stale `service_choice`.
3. Final meta/trace preserve `pending_question_target=time`, `active_question_relation=ask_about_requested_slot`, and resume `name`.
4. Adjacent explicit alternate-time follow-up rows remain unchanged.
5. Focused deterministic scenario/runtime/evidence regressions are green.
6. Fresh guarded `dev L2` no longer stops on the `r101` family.

## Checks

- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r101 --status done --strict-artifacts`
- `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "check_booking_followup or active_name_time_availability_followup or this_time"`
- `pytest -q truffles-api/tests/test_message_endpoint.py -k "active_name or requested_slot or deictic_time_followup"`
- `pytest -q truffles-api/tests/test_booking_quality_response_guard.py -k "active_name or deictic_time_followup or requested_slot"`
- `git diff --check`

## Execution status

- `closed_bounded`
- deterministic implementation is complete and targeted scenario/runtime/response-guard checks are green
- fresh guarded `dev L2` `p1.6o6-l2-dev-20260311-a1-r102` no longer stops on the `r101/M21` family and instead surfaces the next blocker `r102/M22`
- `r102` remains non-canonical proof evidence (`infra_valid=false`, `semantic_valid=false`, `stop_reason=max_failures_reached:1`)

## Evidence

1. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r101/summary.json`
2. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r101/brief.md`
3. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r101/scenarios.json`
4. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r101/responses.jsonl`
5. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r101/trace_bundle.jsonl`
6. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r101/manual_audit.md`
7. `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o71-check-booking-stale-service-choice-governance-a1.md`
8. `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o72-active-name-deictic-time-availability-owner-a1.md`
9. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r102/summary.json`
10. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r102/brief.md`
11. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r102/scenarios.json`
12. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r102/responses.jsonl`
13. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r102/trace_bundle.jsonl`
14. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r102/manual_audit.md`

## Rollback

1. Keep `P1.6o6` blocked.
2. Revert only the future bounded `M21` delta if it regresses adjacent active-name follow-up rows.

## No-go

- no phrase fitting
- no oracle weakening
- no reinterpretation of `r101` as proof that `M20` is still open
- no generic `name` collect accepted as closure for grounded-time deictic availability follow-up
- no stale `service_choice` accepted as closure after explicit time grounding advanced resume to `name`

## Risks/Blockers

1. Scenario and runtime may each require a bounded change; fixing only one layer can leave the other as the first blocker.
2. The deictic reference (`в это время`) may overmatch unrelated booking follow-ups if the detector is too broad.
3. `r101` remains non-canonical proof evidence (`infra_valid=false`, `semantic_valid=false`), so this block can only claim bounded closure plus rerun rollover.

## Residual architecture debt (mandatory)

- Current residuals accepted in this block:
  - `M22` remains open after bounded `M21` closure.
- Why not in this block:
  - `r102` surfaced only after the next fresh guarded `dev L2` beyond `M21`.
- Risk if deferred:
  - question-like daypart exact-time fills can keep the booking flow on generic time guidance and hide later remaining rows behind stale `time` resume.
- Linked follow-up Task Package(s):
  - `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o73-question-like-daypart-exact-time-fill-owner-a1.md`
- Expiry/trigger to stop deferral:
  - before the next guarded rerun beyond `r102`.

## Next-block contract (mandatory)

- Next block objective:
  - implement `M22`, then rerun focused deterministic semantic/scenario/runtime checks before any new guarded `dev L2`.
- First deterministic check command:
  - `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r102 --status done --strict-artifacts && git diff --check`
- Blocked-by conditions:
  - `M22` is not published in the owner matrix
  - question-like daypart exact-time-fill regressions are not green
  - acceptance tool-evidence infra remains red
- Owner role for closure:
  - `Hands` for bounded implementation, `Top Architect + Brain` for matrix/program/proof sync

# TP-2026-03-06-p4.1-fact-evidence-linkage-fail-closed-a1

- Название/цель: закрыть первый обязательный срез `P4 FACT evidence linkage`: `action=fact` не должен проходить runtime-core без валидной evidence linkage; при пустом/невалидном evidence runtime должен fail-closed уходить в `clarify` или `handoff`.
- Parent TP: `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- Canon refs: `AGENTS.md`, `STATE.md`, `STRATEGY/REQUIREMENTS.md`, `TECH.md`
- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`

## One web search (mandatory before implementation)

- Query: `attributed question answering evidence citation grounded generation arxiv`
- Time (UTC): `2026-03-06T12:39:00+00:00`
- Sources:
  - `arXiv: EvidenceBench: An Evidence-Based Large Language Model Benchmark`
  - `arXiv: Attributable and Scalable Open-Book Question Answering via Retrieval-Augmented Generation`
- Ready solutions found:
  - grounded answer quality must be evaluated and enforced with explicit evidence attribution, not only answer plausibility
  - retrieval-backed answers should degrade when attribution is missing instead of silently producing fluent unsupported output
- Decision (`reuse/integrate/build`): `reuse + integrate`
  - reuse existing `pack_refs/fact_refs` traces and policy validation path
  - integrate a hard fail-closed validator before FACT rendering
- Rejected options:
  - keep `FACT_GUARD_ENABLED` disabled and rely on judge/oracle to catch misses later
  - infer evidence implicitly from tool/action without explicit linkage

## Root cause (mandatory)

- Symptom:
  - runtime still permits FACT-like behavior without universal hard evidence linkage.

- Minimal reproduction:
  1. inspect `FACT_GUARD_ENABLED = False` in `truffles-api/app/routers/webhook/decision.py`
  2. inspect `policy_action == "fact"` path and `pack_refs` validation in `truffles-api/app/routers/webhook/decision.py`
  3. inspect policy-core schema fields for `pack_refs` in `truffles-api/app/services/intent_service.py`

- Evidence:
  - `truffles-api/app/routers/webhook/decision.py`
    - `FACT_GUARD_ENABLED = False`
    - FACT flow still depends on `pack_refs`/fact scopes, but guard is not globally fail-closed
  - `truffles-api/app/services/intent_service.py`
    - policy-core plan structure already carries `pack_refs`, so the missing piece is enforcement, not a new semantic path

- Five Whys:
  1. Why can unsupported FACT survive: runtime guard is disabled or incomplete.
  2. Why is that unsafe: fluent unsupported answers look valid but break truth-first contract.
  3. Why does current architecture allow it: evidence linkage is partially traced but not universally required.
  4. Why must this be fixed now: after semantic-owner and booking commit cleanup, unsupported FACT is the next high-risk systemic defect.
  5. Why is this a separate block: it changes FACT acceptance rules and needs its own deterministic regressions.

- Root cause statement:
  - evidence linkage exists structurally in the plan, but runtime-core does not yet enforce it as a hard precondition for FACT output.

- Fix mechanism:
  - convert FACT evidence linkage from advisory/trace-only to fail-closed validator
  - block FACT when refs are empty, dropped, or invalid for the selected fact scope
  - degrade transparently to `clarify`/`handoff` with reason-code and trace/meta evidence

## Invariant

1. No FACT without valid evidence linkage.
2. Deterministic layer validates evidence boundaries; it does not invent semantic meaning.
3. Failure path stays observable through `reason_code`, `decision_meta`, `decision_trace`.

## Scope

- `truffles-api/app/routers/webhook/decision.py`
- `truffles-api/app/services/intent_service.py`
- `truffles-api/tests/test_message_endpoint.py`
- `docs/TASK_PACKAGES/TP-2026-03-06-p4.1-fact-evidence-linkage-fail-closed-a1.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`

## Out of scope

- open-world multilingual proof
- acceptance promotion chain
- invariant quality gates (`P5`)

## Plan (1..N)

1. Inventory current FACT guard gaps and normalize the target evidence contract.
2. Enforce fail-closed FACT validation on empty/invalid evidence linkage.
3. Add deterministic regressions for clarify/handoff fallback on missing evidence.
4. Re-run targeted checks and sync TP evidence.

## DoD

1. FACT path is blocked when evidence linkage is empty or invalid.
2. Failure path is explicit and observable in trace/meta.
3. Targeted deterministic tests are green.

## Checks

- `rg -n "FACT_GUARD_ENABLED|pack_refs|fact_scope|policy_action == \\\"fact\\\"" truffles-api/app/routers/webhook/decision.py truffles-api/app/services/intent_service.py`
- `ruff check truffles-api/app/routers/webhook/decision.py truffles-api/app/services/intent_service.py truffles-api/tests/test_message_endpoint.py`
- `pytest -q truffles-api/tests/test_message_endpoint.py -k "fact and evidence"`
- `git diff --check`

## Execution status

- `done`

## Evidence

1. Code:
   - `truffles-api/app/routers/webhook/decision.py`
   - `truffles-api/tests/test_message_endpoint.py`
2. Commands:
   - `ruff check truffles-api/app/routers/webhook/decision.py truffles-api/tests/test_message_endpoint.py` -> `passed`
   - `pytest -q truffles-api/tests/test_message_endpoint.py -k "direct_info_reply_without_evidence_clarifies or tool_reply_without_evidence_clarifies or extract_fact_evidence_refs_uses_pack_refs_tool_args_and_payload or fact_guard_reason_requires_evidence_refs_for_fact_source or fact_guard_reason_ignores_missing_slot_tool_decisions"` -> `5 passed`
   - `pytest -q truffles-api/tests/test_message_endpoint.py -k "llm_policy_core_catalog_service_reply_keeps_info_answer_for_info_query or llm_policy_core_info_tool_master_reply_sent_without_clarify or llm_policy_core_master_query_normalizes_to_master_info_under_booking or direct_info_reply_without_evidence_clarifies or tool_reply_without_evidence_clarifies"` -> `5 passed`
   - `git diff --check` -> `clean`

## Rollback

1. revert FACT guard enforcement changes
2. rerun targeted deterministic checks

## No-go

- no silent fallback to unsupported FACT
- no keyword-based fake evidence inference
- no acceptance threshold downgrade

## Risks/Blockers

1. FACT and tool-backed info flows overlap in current runtime, so guard must not break valid pack-backed info answers

## Residual architecture debt (mandatory)

- Current residuals accepted in this block:
  - invariant-quality gates and open-world proof remain separate follow-ups
- Why not in this block:
  - this TP scopes only the FACT evidence hard precondition
- Risk if deferred:
  - quality lane still cannot block branch progression on aggregate invariant drift automatically
- Linked follow-up Task Package(s):
  - `docs/TASK_PACKAGES/TP-2026-03-06-p5.1-invariant-quality-gates-blocking-a1.md`
- Expiry/trigger to stop deferral:
  - before any acceptance replay/canary reopening

## Next-block contract (mandatory)

- Next block objective:
  - convert invariant metrics into blocking quality gates for branch progression and acceptance chain admission
- First deterministic check command:
  - `rg -n "fact_without_evidence_rate|irrelevant_fact_rate|booking_commit_without_required_contact|semantic_override_rate|stale_state_leak_rate" ops/diagnose.py scripts/llm_quality_guarded.sh scripts/quality_chain_controller.sh`
- Blocked-by conditions:
  - invariants still advisory-only
  - no blocking fail-closed gate for quality admission/promotion
- Owner role for closure:
  - `Top Architect + Brain`

# TP-2026-03-08-p1.6o16d-deterministic-family-matrix-time-guidance-a1

- Название/цель: закрыть `P1.6o16d` как deterministic proof slice — показать, что bounded owner `ask_about_requested_slot(time)` покрывает family surfaces, а не одну фразу, до нового guarded `dev L2`.
- Parent TP: `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o16-booking-time-guidance-question-owner-a1.md`
- Canon refs: `AGENTS.md`, `STATE.md`, `STRUCTURE.md`, `STRATEGY/REQUIREMENTS.md`, `docs/runbooks/BOOKING_CONFIRM_VERIFY.md`, `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o16-booking-time-guidance-question-owner-a1.md`, `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o16c-runtime-owner-ask-about-requested-slot-time-a1.md`
- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect remove worktree + branch after merge`

## One web search (mandatory before implementation)

- Query: `site:rasa.com/docs testing forms unhappy path stories paraphrases`
- Time (UTC): `2026-03-08T05:55:00Z`
- Sources:
  - `https://rasa.com/docs/rasa/forms/`
- Ready solutions found:
  - unhappy-path handling for active slot collection should be tested at the story/family level, not only with one literal sample;
  - once the owner path exists, multiple paraphrases of the same interaction act should prove the same resume contract.
- Decision (`reuse/integrate/build`): `reuse + integrate`
  - reuse the bounded runtime owner from `P1.6o16c`;
  - integrate deterministic family-matrix tests around that owner;
  - do not widen runtime scope to `slot_compare` / `slot_constraint` in this block.
- Rejected options:
  - treating one passing phrase as closure evidence for the family;
  - moving directly to guarded `dev L2` without deterministic family proof;
  - widening runtime ownership to adjacent families in the same block.

## Root cause (mandatory)

- Symptom:
  - `P1.6o16c` introduced the bounded owner and one positive deterministic test, but a single sample can still hide phrase-fitting.

- Minimal reproduction:
  1. inspect `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o16c-runtime-owner-ask-about-requested-slot-time-a1.md`
  2. inspect `truffles-api/tests/test_message_endpoint.py` around the bounded owner test
  3. inspect `truffles-api/tests/test_booking_quality_response_guard.py` around pending-question interaction guard checks

- Evidence:
  - the runtime owner now keys off `pending_question_act`, not raw wording
  - however the deterministic contour still had only one explicit surface for this owner path
  - without a small family matrix, the next guarded run would still be the first completeness gate against same-family paraphrases

- Five Whys:
  1. Why is one positive test insufficient: it proves the branch exists, but not that the family is framed contractually rather than by one example.
  2. Why can the family still drift: surface forms differ while preserving the same interaction act.
  3. Why is this dangerous before guarded `dev L2`: expensive reruns become the first detector of same-family gaps again.
  4. Why not solve this by adding more runtime logic: the runtime is already intentionally bounded in `P1.6o16c`.
  5. Why does the deterministic matrix help: it proves the existing owner contract is reused across multiple surfaces without widening scope.

- Root cause statement:
  - after `P1.6o16c`, the remaining risk is not missing runtime ownership but insufficient deterministic evidence that the bounded owner covers a small same-act surface family.

- Fix mechanism:
  - add a focused parameterized family matrix around `ask_about_requested_slot(time)` in deterministic tests
  - keep oracle checks evidence-based
  - update TPs so the next step is guarded `r12`, not more same-family coding

## Release safety

- Strategy: deterministic-only closure in this block; no prod promotion
- Go signals:
  - family-matrix tests are green
  - guard tests stay green
  - `git diff --check` is clean
- No-go signals:
  - any family surface falls back to generic `booking_prompt`
  - resume contract is lost for any family surface
  - a neighboring act (`slot_compare` / `slot_constraint`) is silently absorbed into this matrix without explicit scope change
- Rollback verification:
  - revert only the deterministic-family-matrix delta and confirm prior focused checks return

## Invariant

1. No runtime scope widening beyond `ask_about_requested_slot(time)`.
2. No phrase-hardcoded assertions; tests prove owner reuse through shared contract output.
3. Resume contract remains explicit for every matrix surface.
4. No fake slot fill for any matrix surface.

## Scope

- `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o16d-deterministic-family-matrix-time-guidance-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o16-booking-time-guidance-question-owner-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o16c-runtime-owner-ask-about-requested-slot-time-a1.md`
- `truffles-api/tests/test_message_endpoint.py`
- `truffles-api/tests/test_booking_quality_response_guard.py`

## Out of scope

- new runtime behavior
- guarded `dev L2`, `lock`, or `replay`
- adjacent acts `slot_compare`, `slot_constraint`, `mixed_fill_plus_question`

## Touch-list

- `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o16d-deterministic-family-matrix-time-guidance-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o16-booking-time-guidance-question-owner-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o16c-runtime-owner-ask-about-requested-slot-time-a1.md`
- `truffles-api/tests/test_message_endpoint.py`
- `truffles-api/tests/test_booking_quality_response_guard.py`

## Plan (1..N)

1. Freeze `P1.6o16d` as the deterministic family-matrix slice.
2. Expand the bounded owner test into a small same-act parameterized family matrix.
3. Re-run focused guard checks to prove the oracle still accepts only explicit contract evidence.
4. Sync parent/child TP status so the next block is the guarded rerun, not more same-family coding.

## DoD

1. The bounded owner is proven across multiple `ask_about_requested_slot(time)` surfaces.
2. Every matrix case preserves `expected_reply_type=time`.
3. No matrix case writes `booking.datetime` without grounded slot evidence.
4. Guard checks remain green.
5. Parent/child TPs point to guarded `r12` as the next block.

## Checks

- `pytest -q truffles-api/tests/test_message_endpoint.py -k "pending_question_act_time_guidance or question_like"`
- `pytest -q truffles-api/tests/test_booking_quality_response_guard.py -k "pending_question_interaction or booking_slot_stall"`
- `git diff --check`

## Execution status

- `done`
- focused deterministic family-matrix checks are green in the current worktree:
  - `pytest -q truffles-api/tests/test_message_endpoint.py -k "pending_question_act_time_guidance or question_like"` -> `8 passed, 320 deselected`
  - `pytest -q truffles-api/tests/test_booking_quality_response_guard.py -k "pending_question_interaction or booking_slot_stall"` -> `8 passed, 63 deselected`
  - `git diff --check` -> `clean`

## Evidence

1. `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o16d-deterministic-family-matrix-time-guidance-a1.md`
2. `truffles-api/tests/test_message_endpoint.py`
3. `truffles-api/tests/test_booking_quality_response_guard.py`

## Rollback

1. Revert only the `P1.6o16d` deterministic matrix/doc sync.
2. Keep guarded `r12` blocked until the deterministic family matrix is restored.

## No-go

- no new runtime owner branches
- no same-block expansion to `slot_compare` / `slot_constraint`
- no guarded rerun before deterministic family matrix is green

## Risks/Blockers

1. The matrix may accidentally include neighboring acts and create false confidence.
2. Guarded rerun remains blocked by transport/runtime readiness even after this slice is green.

## Residual architecture debt (mandatory)

- Current residuals accepted in this block:
  - guarded `dev L2` rerun (`r12`) remains open
  - adjacent pending-question families remain open
- Why not in this block:
  - this slice is only deterministic proof for the bounded owner already implemented
- Risk if deferred:
  - the next expensive rerun becomes the first validator of same-family coverage again
- Linked follow-up Task Package(s):
  - `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`
- Expiry/trigger to stop deferral:
  - before the next guarded `dev L2`

## Next-block contract (mandatory)

- Next block objective:
  - restart the runtime from this worktree state, then run one fresh guarded `dev L2` rerun (`r12`) as the first post-matrix semantic proof
- First deterministic check command:
  - `pytest -q truffles-api/tests/test_message_endpoint.py -k "pending_question_act_time_guidance or question_like" && pytest -q truffles-api/tests/test_booking_quality_response_guard.py -k "pending_question_interaction or booking_slot_stall" && git diff --check`
- Blocked-by conditions:
  - deterministic family matrix is not green
  - local runtime is not restarted from the post-`P1.6o16d` worktree state
  - guarded transport/preflight remains invalid
- Owner role for closure:
  - `Hands` for evidence and rerun execution, `Top Architect + Brain` for closure gating

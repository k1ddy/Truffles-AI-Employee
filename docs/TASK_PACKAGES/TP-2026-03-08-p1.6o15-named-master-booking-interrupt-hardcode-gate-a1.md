# TP-2026-03-08-p1.6o15-named-master-booking-interrupt-hardcode-gate-a1

- Название/цель: закрыть новый `P1.6o6` blocker, где booking-active вопрос про конкретного мастера (`Можно к мастеру Айгерим?`) не поднимается в `master/specialist` info interrupt, а также снять связанный `hardcode_core_violation` в `pack_runtime_service`.
- Parent TP: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Canon refs: `AGENTS.md`, `STATE.md`, `STRUCTURE.md`, `STRATEGY/REQUIREMENTS.md`, `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`, `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o9-specialist-choice-master-interrupt-a1.md`
- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`

## One web search (mandatory before implementation)

- Query: `site:rasa.com/docs lookup tables entities roles intent classification`
- Time (UTC): `2026-03-08T03:18:00Z`
- Sources:
  - `https://rasa.com/docs/reference/primitives/intents-and-entities/`
- Ready solutions found:
  - lexical/entity surfaces should stay in data-driven intent/entity features instead of ad hoc code branches;
  - entity roles/groups and lookup-style signals are the right place to carry name-specific semantics that reuse one resolver contract.
- Decision (`reuse/integrate/build`): `reuse + integrate`
  - reuse the existing lexicon-backed `resolve_master_intent()` and booking collect interrupt owner;
  - integrate specialist-name evidence from pack data / semantic envelope into the same resolver instead of adding another `decision.py` phrase branch.
- Rejected options:
  - weakening `expected_info_section_miss` / `info_section_miss`;
  - adding a direct `"к мастеру"` branch in `decision.py`;
  - leaving the existing hardcode gate violation in `pack_runtime_service.py` and treating it as “known red”.

## Root cause (mandatory)

- Symptom:
  - fresh guarded `dev L2` rerun `p1.6o6-l2-dev-20260308-a1-r10` stopped with `semantic_valid=false`, `stop_reason=max_failures_reached:1`
  - failing turn: `dialog_id=1`, `turn_index=4`, message `Можно к мастеру Айгерим?`
  - family: `expected_info_section_miss + info_section_miss`
  - same run remains blocked by `hardcode_core_violation` in `truffles-api/app/services/pack_runtime_service.py`

- Minimal reproduction:
  1. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r10/summary.json`
  2. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r10/responses.jsonl` for `message_id=LLM-QUAL-p1.6o6-l2-dev-20260308-a1-r10-001-04-d8df14`
  3. inspect `resolve_master_intent()` and `_has_master_person_service_relation()` in `truffles-api/app/services/pack_runtime_service.py`
  4. inspect existing `P1.6o9` resolver tests for `choose_specialist`

- Evidence:
  - policy-core raw payload already carries `entity_refs=[master:Айгерим]`, `subject_kind=specialist`, `capability=bookability`
  - runtime still ends with `action=booking_prompt`, `intent=booking`, empty `info_sections`
  - `signal_snapshot.info_class.master=false`, so collect interrupt arbitration never upgrades the turn into `master`
  - hardcode gate reports `core_phrase_branching_detected` on `_has_master_person_service_relation()` line `if f"{candidate} {connector} " in normalized_message:`

- Five Whys:
  1. Why did guarded quality fail: the bot answered a specialist-info turn with another booking datetime prompt.
  2. Why did booking prompt survive: `_resolve_policy_collect_interrupt_arbitration()` never received `master` in derived info refs.
  3. Why were `master` refs missing: current resolver covers direct master-query phrases and choose/select-specialist phrases, but not named-master booking questions like `Можно к мастеру Айгерим?`.
  4. Why is that still red architecturally: policy-core semantic envelope already exposes `subject_kind=specialist` and the pack knows specialist names, but the resolver contract does not consume that evidence.
  5. Why is proof bundle also blocked regardless of turn result: the same resolver area still contains hardcoded phrase branching that fails `hardcode_core_gate`.

- Root cause statement:
  - master interrupt resolution is incomplete for named-specialist booking questions, and the remaining person/service relation helper still hardcodes phrase connectors in core code; together they leave booking-active specialist questions outside the canonical resolver contract and keep the hardcode gate red.

- Fix mechanism:
  - move master relation connectors into lexicon data so `pack_runtime_service` no longer hardcodes raw phrase branching;
  - extend `resolve_master_intent()` to recognize named-specialist question turns from pack data / resolver evidence under booking service context;
  - prove it with resolver + message-endpoint regressions, then rerun guarded `dev L2`.

## Invariant

1. No new phrase hardcode in `decision.py` or `pack_runtime_service.py`.
2. Specialist-name booking commands must remain booking when they are actual booking commands, not info interruptions.
3. Booking-active named-master questions must answer with `master/specialist` info or fail-closed clarify, never with blind booking prompt.
4. `hardcode_core_gate` must become green for this resolver area.

## Scope

- `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o15-named-master-booking-interrupt-hardcode-gate-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `truffles-api/app/knowledge/generic/SYSTEM_LEXICONS.yaml`
- `truffles-api/app/services/pack_runtime_service.py`
- `truffles-api/tests/test_pack_runtime_service.py`
- `truffles-api/tests/test_message_endpoint.py`

## Out of scope

- new acceptance `lock/replay`
- generic master-query redesign outside the named-specialist question gap
- unrelated quality families outside `r10`

## Touch-list

- `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o15-named-master-booking-interrupt-hardcode-gate-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `truffles-api/app/knowledge/generic/SYSTEM_LEXICONS.yaml`
- `truffles-api/app/services/pack_runtime_service.py`
- `truffles-api/tests/test_pack_runtime_service.py`
- `truffles-api/tests/test_message_endpoint.py`

## Plan (1..N)

1. Prove the resolver gap on named-master question and the exact hardcode gate hit in `pack_runtime_service.py`.
2. Move relation connectors into lexicon data and remove raw phrase branching from core code.
3. Extend the master resolver with named-specialist question detection using pack data + service context.
4. Add exact resolver and booking-active message-endpoint regressions.
5. Re-run deterministic contour, then return to one fresh guarded `dev L2`.

## DoD

1. `resolve_master_intent()` marks `Можно к мастеру Айгерим?` as explicit `master` interrupt when booking service context is present.
2. Booking-active message-endpoint rewrites that turn into `master/specialist` info reply with `info_sections` evidence.
3. `hardcode_core_gate` no longer reports `core_phrase_branching_detected` for `pack_runtime_service.py`.
4. Exact deterministic regressions are green.
5. Fresh guarded `dev L2` removes the `r10` failing family and no longer carries this hardcode gate violation.

## Checks

- `pytest -q truffles-api/tests/test_pack_runtime_service.py -k "named_master or choose_specialist"`
- `pytest -q truffles-api/tests/test_message_endpoint.py -k "named_master or choose_specialist"`
- `pytest -q truffles-api/tests/test_booking_quality_status_gate.py -k "hardcode_core_gate"`
- `pytest -q truffles-api/tests/test_message_endpoint.py`
- `pytest -q truffles-api/tests/test_pack_runtime_service.py`
- `git diff --check`

## Execution status

- `done` on deterministic contour
- guarded `dev L2` proof rerun remains pending under `P1.6o6`

## Evidence

1. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r10/summary.json`
2. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r10/brief.md`
3. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r10/responses.jsonl`
4. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r10/trace_bundle.jsonl`
5. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r10/manual_audit.md`
6. `pytest -q truffles-api/tests/test_pack_runtime_service.py -k "named_master or choose_specialist"` -> `4 passed, 20 deselected`
7. `pytest -q truffles-api/tests/test_message_endpoint.py -k "named_master or choose_specialist"` -> `3 passed, 322 deselected`
8. `pytest -q truffles-api/tests/test_booking_quality_status_gate.py -k "hardcode_core_gate or phrase_branching"` -> `7 passed, 89 deselected`
9. `pytest -q truffles-api/tests/test_booking_quality_response_guard.py` -> `68 passed`
10. `pytest -q truffles-api/tests/test_message_endpoint.py` -> `325 passed, 2 warnings`
11. `python3 - <<..._llm_quality_collect_hardcode_core_violations...` -> `violations=[]`
12. `git diff --check` -> `clean`

## Rollback

1. Revert only the resolver/data changes if they steal true booking turns or destabilize master-query resolution.
2. Keep `P1.6o6` blocked until a fresh guarded rerun is green and audited.

## No-go

- no oracle weakening
- no new phrase-hardcode branch in runtime core
- no ignoring `hardcode_core_gate` as “non-blocking”

## Risks/Blockers

1. A too-broad named-master signal may reclassify real booking commands as info turns.
2. If pack-data name matching is too weak, the same family may reappear under paraphrase.

## Residual architecture debt (mandatory)

- Current residuals accepted in this block:
  - `P1.6o6` proof bundle still needs a fresh rerun after the resolver/gate fix
  - fresh rerun `r11` exposed the next semantic family (`P1.6o16`) outside this block
- Why not in this block:
  - this block only closes one newly surfaced resolver family plus the coupled hardcode gate red
- Risk if deferred:
  - guarded proof remains blocked both on runtime behavior and on hardcode governance
- Linked follow-up Task Package(s):
  - `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`
  - `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o16-booking-time-guidance-question-owner-a1.md`
- Expiry/trigger to stop deferral:
  - before any new acceptance `lock/replay` or closure claim

## Next-block contract (mandatory)

- Next block objective:
  - hand off to `P1.6o16`, which now owns the next semantic family from guarded rerun `r11`; only after `P1.6o16` is green may `P1.6o6` return to a fresh guarded `dev L2`
- First deterministic check command:
  - `pytest -q truffles-api/tests/test_message_endpoint.py::test_llm_policy_core_time_guidance_question_under_expected_time_keeps_progress_contract && pytest -q truffles-api/tests/test_booking_quality_response_guard.py::test_evaluate_turn_allows_booking_time_guidance_reply_without_slot_progress_stall`
- Blocked-by conditions:
  - `P1.6o16` exact regressions are not green
  - firebreak runtime on `:18184` is not restarted from current worktree
  - guarded runtime transport remains infra-invalid
  - guarded runtime/judge prerequisites are not satisfied
- Owner role for closure:
  - `Hands` for code/tests/evidence, `Top Architect + Brain` for proof-bundle closure decision

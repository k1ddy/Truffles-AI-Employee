# TP-2026-03-09-p1.6o43-booking-master-info-interrupt-name-collect-a1.md

- Название/цель: закрыть surfaced `r47` blocker, где generic specialist/master question inside active booking `name` collect (`Есть ли доступные специалисты?`) не получает info-reply owner и схлопывается в generic `booking_prompt`.
- Parent TP: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Canon refs: `AGENTS.md`, `STATE.md`, `STRUCTURE.md`, `STRATEGY/REQUIREMENTS.md`, `docs/runbooks/BOOKING_CONFIRM_VERIFY.md`, `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o42-generic-info-interrupt-pending-question-context-a1.md`, `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o37-booking-specialist-followup-owner-contract-a1.md`, `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o24-generic-master-info-interrupt-taxonomy-resume-contract-a1.md`
- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect remove worktree + branch after merge`

## One web search (mandatory before implementation)

- Query: `Rasa forms requested slot side question specialist availability official docs`
- Time (UTC): `2026-03-09T05:08:28Z`
- Sources:
  - `https://rasa.com/docs/rasa/forms/`
- Ready solutions found:
  - slot-filling flows must answer side questions and then resume the active requested slot instead of ignoring the side question;
  - the side question should reuse the existing domain info owner instead of inventing a separate ad-hoc prompt path.
- Decision (`reuse/integrate/build`): `reuse + integrate`
  - reuse existing `resolve_master_intent` and booking-interrupt info reply machinery;
  - integrate it into the bounded active-collect owner for the surfaced `name`-collect specialist question family;
  - build no new phrase router in runtime core.
- Rejected options:
  - hardcoding `доступные специалисты` in decision core;
  - keeping scenario as plain `booking` while waiving missing `master` info evidence;
  - teaching runtime to answer this family via generic prompt only.

## Root cause (mandatory)

- Symptom:
  - guarded `dev L2` run `p1.6o6-l2-dev-20260309-a1-r47` fails on dialog 1 turn 5 (`Есть ли доступные специалисты?`) with `expected_meta_mismatch`, `expected_trace_miss`, `info_section_miss`, `judge_fail`.

- Minimal reproduction:
  1. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r47/scenarios.json`
  2. note the chain: `booking -> ask_about_requested_slot(time) -> price -> explicit time fill -> specialist/master question while expected_reply_type=name`
  3. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r47/responses.jsonl` and confirm bot replies only `Как вас зовут?`
  4. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r47/trace_bundle.jsonl` and confirm `llm_policy_core` already sets `subject_kind=specialist`, `pending_question_target=specialist`, but `question_contract` is `missed` with `answer_error=blocked_by_info`
  5. inspect `decision.py` collect branch and confirm specialist follow-up owner is only activated when a concrete specialist referent/hint is recovered; generic specialist info questions fall through to prompt-only collect

- Evidence:
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r47/scenarios.json`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r47/responses.jsonl`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r47/trace_bundle.jsonl`
  - `truffles-api/app/routers/webhook/decision.py`
  - `truffles-api/app/routers/webhook/booking.py`
  - `scripts/booking_dialog_scenarios.py`

- Five Whys:
  1. Why did the turn fail: runtime asked for `name` again and did not answer the specialist question.
  2. Why did runtime skip the specialist question: collect branch only upgrades to specialist follow-up owner when a concrete specialist referent/hint is present.
  3. Why was there no bounded fallback for the generic question: generic master-info interrupt handling is modeled for earlier time-collect families, not for active `name` collect after time is already filled.
  4. Why is scenario also ambiguous: current taxonomy leaves the turn as `booking` even though the user is asking a bounded master-info side question inside booking.
  5. Why is this a contract gap rather than one phrase: the missing owner is the general class `master/specialist info interrupt during active name collect`, not the single surface `Есть ли доступные специалисты?`.

- Root cause statement:
  - runtime and taxonomy already distinguish specialist-target follow-ups with explicit referents, but they do not contractually model generic master/specialist info questions inside active booking `name` collect, so the same turn is compressed into prompt-only collect instead of `info reply + resume name`.

- Fix mechanism:
  - normalize this family in taxonomy as booking master-info interrupt with resume-to-name contract;
  - reuse existing `resolve_master_intent` + `_build_info_intent_reply("master", ...)` inside the collect owner when active slot is `name`, the semantic payload targets `specialist`, and the turn is a generic master-info question without a concrete specialist referent;
  - preserve `expected_reply_type=name` after the info reply;
  - add deterministic taxonomy and runtime regressions before the next guarded rerun.

## Release safety

- Strategy: bounded taxonomy/runtime fix using existing master-info owner contracts
- Go signals:
  - generic specialist/master questions under active `name` collect produce master-info reply and then resume `name`
  - explicit named-specialist follow-up behavior from `P1.6o37` / `P1.6o40` remains unchanged
- No-go signals:
  - new phrase-hardcode in runtime core
  - weakening info/data oracle instead of answering the specialist question
- Rollback verification:
  - revert only the new bounded collect-owner branch and matching taxonomy regression if it disturbs existing named-specialist follow-up behavior

## Invariant

1. `P1.6o42` generic `info` context closure remains closed.
2. `P1.6o37` / `P1.6o39` / `P1.6o40` explicit specialist follow-up contracts remain closed.
3. Existing generic master-info interrupt contract from `P1.6o24` remains the reusable source of truth.
4. No semantic phrase-routing is added in runtime core.

## Scope

- `scripts/booking_dialog_scenarios.py`
- `truffles-api/app/routers/webhook/decision.py`
- `truffles-api/tests/test_booking_dialog_scenarios_script.py`
- `truffles-api/tests/test_message_endpoint.py`
- `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o43-booking-master-info-interrupt-name-collect-a1.md`
- tracker docs after deterministic/runtime closure and guarded evidence:
  - `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
  - `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
  - `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`

## Out of scope

- broader pending-question-act ontology redesign
- lock/replay promotion
- transport/tool-evidence infra remediation

## Touch-list

- `scripts/booking_dialog_scenarios.py`
- `truffles-api/app/routers/webhook/decision.py`
- `truffles-api/tests/test_booking_dialog_scenarios_script.py`
- `truffles-api/tests/test_message_endpoint.py`
- `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o43-booking-master-info-interrupt-name-collect-a1.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`

## Plan (1..N)

1. Freeze `r47` as bounded master-info interrupt evidence.
2. Normalize taxonomy for generic specialist/master questions under active `name` collect.
3. Reuse existing master-info reply builder inside the collect owner for this bounded family.
4. Add deterministic taxonomy/runtime regressions.
5. Run targeted checks and `git diff --check`.
6. If green, run fresh guarded `dev L2` `r48`.

## DoD

1. Generic specialist/master question under active `name` collect answers the info question and resumes `name`.
2. Taxonomy/runtime deterministic regressions pass.
3. `git diff --check` stays clean.
4. Tracker docs point to `r48` as the next expensive step, then sync to `r48` evidence after the run.

## Checks

- `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "master_info or specialist_followup or pending_question"`
- `pytest -q truffles-api/tests/test_message_endpoint.py -k "master_info_name_collect or booking_specialist_followup"`
- `git diff --check`

## Execution status

- `in_progress`

## Evidence

1. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r47/scenarios.json`
2. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r47/responses.jsonl`
3. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r47/trace_bundle.jsonl`
4. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r47/manual_audit.md`

## Rollback

1. Revert only the bounded master-info interrupt owner changes and their regressions if they affect other booking collect families.
2. Keep `P1.6o6` blocked; do not reinterpret `r47` as proof-bundle closure evidence.

## No-go

- no phrase-specific branch for `Есть ли доступные специалисты?`
- no oracle waiver for missing master info answer
- no generic collect prompt accepted as success for this family
- no new guarded rerun before deterministic checks are green

## Risks/Blockers

1. The surfaced family may reveal a wider missing ontology for `specialist question during later collect slots`; if so, `P1.6o43` must stay bounded and not overclaim total closure.
2. Acceptance remains infra-invalid for unrelated tool-evidence gaps; this block only closes the surfaced semantic/runtime family from `r47`.

## Residual architecture debt (mandatory)

- Current residuals accepted in this block:
  - pending-question interaction ontology still compresses several specialist-question acts into bounded runtime reuse paths
- Why not in this block:
  - this block closes only the surfaced generic specialist/master info interrupt under active `name` collect
- Risk if deferred:
  - live booking collect will keep ignoring specialist side questions once time is already filled, producing wrong action and poor UX
- Linked follow-up Task Package(s):
  - `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`
- Expiry/trigger to stop deferral:
  - next surfaced specialist-question family that cannot be expressed as bounded reuse of existing master-info owner

## Next-block contract (mandatory)

- Next block objective:
  - run guarded `r48` after deterministic/runtime closure of `P1.6o43` so the next surfaced blocker is no longer the missing master-info interrupt owner under active `name` collect
- First deterministic check command:
  - `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "master_info_name_collect" && pytest -q truffles-api/tests/test_message_endpoint.py -k "master_info_name_collect" && git diff --check`
- Blocked-by conditions:
  - deterministic taxonomy/runtime checks are not green
  - runtime `http://127.0.0.1:18184` is not refreshed from current worktree state after the code delta
  - guarded `r48` preflight is invalid
- Owner role for closure:
  - `Hands` for deterministic/runtime closure + rerun evidence, `Top Architect + Brain` for proof-bundle promotion

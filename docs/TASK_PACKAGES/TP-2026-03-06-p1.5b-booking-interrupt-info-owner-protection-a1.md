# TP-2026-03-06-p1.5b-booking-interrupt-info-owner-protection-a1

- Название/цель: P1.5b выровнять deterministic contract suite под текущий single-owner runtime, чтобы `P1.6` блокировался только реальными runtime-дефектами, а не устаревшими ожиданиями или неверными patch-target в тестах.
- Parent TP: `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`.
- Canon refs: `AGENTS.md`, `STATE.md`, `STRATEGY/REQUIREMENTS.md`, `SPECS/SYSTEM_REFERENCE.md`.
- Branch: `fix/llm-first-firebreak-2026-02-19`.
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`.

## One web search (mandatory before implementation)

- Query: `site:csrc.nist.gov "policy decision point" "policy enforcement point"`
- Time (UTC): `2026-03-06T02:15:32Z`
- Sources:
  - `https://csrc.nist.gov/glossary/term/policy_decision_point`
  - `https://csrc.nist.gov/glossary/term/policy_enforcement_point`
- Ready solutions found:
  - Policy decision ownership and enforcement boundaries must stay separated; tests should verify that contract instead of encoding a second semantic decision layer.
- Decision (`reuse/integrate/build`): `integrate`
  - Integrate PDP/PEP separation into test expectations: decision-owner semantics are asserted by plan/trace/meta, while deterministic enforcement is checked only at allowed boundary outcomes.
- Rejected options:
  - Reintroduce removed runtime semantics to satisfy stale tests.
  - Keep wrong patch targets that don’t affect the code under test.

## Root cause (mandatory)

- Symptom:
  - `P1.6` deterministic suite is red, but failures are not proving a new runtime regression consistently.

- Minimal reproduction:
  1. `pytest -q truffles-api/tests/test_message_endpoint.py`
  2. `python3 - <<'PY' ... patch('app.routers.webhook.info._build_info_intent_reply') ... PY`
  3. Inspect failing tests around:
     - `truffles-api/tests/test_message_endpoint.py:10538`
     - `truffles-api/tests/test_message_endpoint.py:19639`
     - `truffles-api/tests/test_message_endpoint.py:20879`
     - `truffles-api/tests/test_message_endpoint.py:21068`
     - `truffles-api/tests/test_message_endpoint.py:21414`

- Evidence:
  - `truffles-api/tests/test_message_endpoint.py:10538`
    - patches `app.routers.webhook.info._build_info_intent_reply`, but `decision.py` imports `_build_info_intent_reply` directly, so the patch does not affect the symbol under test.
  - `truffles-api/tests/test_message_endpoint.py:20879`
  - `truffles-api/tests/test_message_endpoint.py:21068`
  - `truffles-api/tests/test_message_endpoint.py:21414`
    - still assert `master_signal_override_blocked` behavior that P1 intentionally removed from runtime semantics.
  - `truffles-api/tests/test_message_endpoint.py:19639`
    - still asserts a pre-P1 contract around `catalog.service_query` + location/parking semantics that no longer matches the updated pack-ref-driven owner contract.

- Five Whys:
  1. Why is the deterministic suite red: several tests still encode pre-P1 semantic behavior.
  2. Why is one failure misleading: the patch target does not touch the imported symbol used by `decision.py`.
  3. Why is that dangerous: it makes `P1.6` look blocked by runtime when it is partly blocked by stale or miswired tests.
  4. Why can’t we ignore this: `P1.6` lock must be gated by trustworthy tests, not by obsolete expectations.
  5. Why fix now: otherwise future sessions and agents will keep reading the wrong blocker signal and may add unnecessary runtime code.

- Root cause statement:
  - The contract suite drifted behind the new single-owner architecture: some tests assert removed semantic normalization, and one test patches the wrong symbol, so the suite no longer cleanly distinguishes real runtime defects from stale expectations.

- Fix mechanism:
  - Correct patch targets to the actual symbols used by `decision.py`.
  - Rewrite stale tests to assert current single-owner contract via `action/tool_action/trace/meta`, not removed `master_signal` behavior.
  - Keep one focused booking-interrupt test that proves hours/info ownership without depending on obsolete patching or semantic rescue.

## Invariant

1. Tests must validate the current single-owner runtime contract, not historical semantic rescue paths.
2. Patch targets in tests must affect the actual symbol under test.
3. `P1.6` remains blocked only by real runtime defects after this block.

## Scope

- `truffles-api/tests/test_message_endpoint.py`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-06-p1.5-post-plan-semantic-rescue-removal-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-06-p1.5b-booking-interrupt-info-owner-protection-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-06-p1.6-local-realism-canonical-lock-a1.md`

## Out of scope

- New runtime semantic changes unless a real defect remains after test realignment.
- `P1.6` realism lock.
- `P1.7` replay gate.

## Plan (1..N)

1. Rewrite the mispatched booking-interrupt test to patch the correct decision-level symbol and assert the current contract.
2. Rewrite/remove stale `master_signal`/pre-P1 normalization assertions so they match the current single-owner semantics.
3. Run the focused failing subset, then rerun full `test_message_endpoint.py`.
4. Reclassify `P1.6` blocker based on the new suite result.

## DoD

1. The five current failing tests are either green or replaced by updated contract tests covering the same invariant.
2. `pytest -q truffles-api/tests/test_message_endpoint.py` no longer fails because of stale pre-P1 expectations or wrong patch targets.
3. If red remains, the remainder is a proven runtime blocker and is documented as such.

## Checks

- `pytest -q truffles-api/tests/test_message_endpoint.py -k "booking_interrupt_hours_contract_blocks_price_takeover or llm_policy_core_service_query_does_not_rewrite_under_single_owner_lock or llm_policy_core_catalog_service_reply_normalized_to_master_info_by_signal or llm_policy_core_calendar_get_booking_not_found_normalized_to_master_info or llm_policy_core_semantic_intent_override_without_whitelist_reason_code_blocks_with_clarify"`
- `pytest -q truffles-api/tests/test_message_endpoint.py`
- `ruff check truffles-api/tests/test_message_endpoint.py`

## Execution status

- `done`

## Evidence

1. Test diff removing stale master-signal expectations and fixing patch-targets.
2. Focused subset:
   - `pytest -q truffles-api/tests/test_message_endpoint.py -k "booking_interrupt_hours_contract_blocks_price_takeover or service_query_keeps_plan_tool_action_despite_location_signal_block or catalog_service_reply_does_not_apply_master_signal_override or calendar_get_booking_not_found_keeps_booking_reply_without_master_override or service_query_without_master_override_reason_keeps_plan_reply"`
   - result: `1 failed, 4 passed, 278 deselected`
3. Full deterministic file suite after realignment:
   - `pytest -q truffles-api/tests/test_message_endpoint.py`
   - result: `1 failed, 282 passed, 2 warnings`
4. Remaining blocker is now isolated as a real runtime path and moved to:
   - `docs/TASK_PACKAGES/TP-2026-03-06-p1.5c-booking-interrupt-plan-conflict-fail-closed-a1.md`

## Rollback

1. `git revert <commit_with_p1.5b_changes>`
2. Re-run the focused failing subset.

## No-go

- No runtime code changes just to satisfy stale tests.
- No restoration of removed `master_signal` semantics.
- No `P1.6` lock while the contract suite is still ambiguous.

## Risks/Blockers

1. After test realignment, a smaller set of real runtime blockers may still remain.
2. Other tests outside the current failing five may encode similar pre-P1 assumptions and surface on broader suites later.

## Residual architecture debt (mandatory)

- Current residuals accepted in this block:
  - isolated runtime blocker: booking-interrupt plan/conflict fallback leak.
  - `P1.6` local realism + canonical acceptance lock.
  - `P1.7` replay regression gate.
- Why not in this block:
  - First remove false blocker signals from the suite.
- Risk if deferred:
  - Future sessions may keep introducing unnecessary runtime fixes to satisfy obsolete tests.
- Linked follow-up Task Package(s):
  - `docs/TASK_PACKAGES/TP-2026-03-06-p1.5c-booking-interrupt-plan-conflict-fail-closed-a1.md`
  - `docs/TASK_PACKAGES/TP-2026-03-06-p1.6-local-realism-canonical-lock-a1.md`
- Expiry/trigger to stop deferral:
  - Before any lock/canary/full acceptance evidence.

## Next-block contract (mandatory)

- Next block objective:
  - Close the isolated runtime blocker in `P1.5c`, then resume `P1.6`.
- First deterministic check command:
  - `pytest -q truffles-api/tests/test_message_endpoint.py -k booking_interrupt_hours_contract_blocks_price_takeover -vv`
- Blocked-by conditions:
  - The booking-interrupt runtime path still degrades into unrelated FACT instead of fail-closed clarify.
- Owner role for closure:
  - `Top Architect + Brain`

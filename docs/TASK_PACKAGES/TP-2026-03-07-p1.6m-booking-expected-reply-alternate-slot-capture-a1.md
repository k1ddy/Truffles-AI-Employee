# TP-2026-03-07-p1.6m-booking-expected-reply-alternate-slot-capture-a1

- Название/цель: закрыть свежий `demo_salon` `dev L2` blocker `booking_slot_stall`, в котором stale `expected_reply_type=time` выбрасывает валидный ответ на другой booking slot (`name`) и повторно спрашивает уже предоставленный слот.
- Parent TP: `docs/TASK_PACKAGES/TP-2026-03-06-p1.6-local-realism-canonical-lock-a1.md`
- Canon refs: `AGENTS.md`, `STATE.md`, `STRATEGY/REQUIREMENTS.md`, `TECH.md`, `docs/runbooks/BOOKING_CONFIRM_VERIFY.md`, `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`

## One web search (mandatory before implementation)

- Query: `Rasa forms fill other slots while requested_slot active`
- Time (UTC): `2026-03-07T05:02:07Z`
- Sources:
  - `https://rasa.com/docs/reference/primitives/slots/`
  - `https://legacy-docs-oss.rasa.com/docs/rasa/forms/`
- Ready solutions found:
  - requested-slot handling must keep one slot actively pending instead of greedily consuming everything from one utterance
  - slot extraction may still validate and retain other user-provided slot values explicitly, as long as the requested slot state remains observable and validation is explicit
- Decision (`reuse/integrate/build`): `integrate + build`
  - integrate the requested-slot discipline: keep the true missing booking slot pending
  - build local alternate-slot capture for explicit valid booking slot answers so they are not lost on stale `expected_reply_type`
- Rejected options:
  - keep current `slot_mismatch -> drop answer` behavior
  - greedily auto-fill every alternate slot without validation
  - rerun expensive `dev L2` before code delta

## Root cause (mandatory)

- Symptom:
  - fresh guarded `demo_salon` `dev L2` run `p1.6-l2-dev-seed42-r21` stopped on the first new strict failure family:
    - `booking_slot_stall`
    - `judge_fail`
  - failing turn:
    - `dialog_index=5`
    - `turn_index=13`
    - user text: `Меня зовут Лена.`
    - bot reply: `Отлично, время подходит. Как вас зовут?`
- Minimal reproduction:
  1. inspect `/tmp/booking_quality/p1.6-l2-dev-seed42-r21/brief.md`
  2. inspect `/tmp/booking_quality/p1.6-l2-dev-seed42-r21/responses.jsonl` for `dialog_index=5`, `turn_index=13`
  3. inspect `/tmp/booking_quality/p1.6-l2-dev-seed42-r21/trace_bundle.jsonl` for `question_contract` / `llm_policy_core`
  4. inspect `truffles-api/app/services/intent_service.py::interpret_expected_reply`
  5. inspect `truffles-api/app/routers/webhook/decision.py` expected-reply question-contract around `answer_slot != expected_slot_key`
- Evidence:
  - `/tmp/booking_quality/p1.6-l2-dev-seed42-r21/summary.json`
  - `/tmp/booking_quality/p1.6-l2-dev-seed42-r21/brief.md`
  - `/tmp/booking_quality/p1.6-l2-dev-seed42-r21/responses.jsonl`
  - `/tmp/booking_quality/p1.6-l2-dev-seed42-r21/trace_bundle.jsonl`
  - `/tmp/booking_quality/p1.6-l2-dev-seed42-r21/manual_audit.json`
  - `/tmp/booking_quality/p1.6-l2-dev-seed42-r21/manual_audit.md`
  - turn forensic facts:
    - `question_contract` processed `expected_reply_type=time`
    - answer interpreter path produced `answer_error=slot_mismatch`
    - final turn still asked `name`, so the runtime lost the valid `name` answer instead of preserving it and continuing booking
- Five Whys:
  1. Why did booking stall: the user provided a valid booking slot answer, but the runtime treated it as unusable.
  2. Why was it treated as unusable: question-contract only accepts the currently expected slot and zeroes the answer on `slot_mismatch`.
  3. Why could it not recover later in the same turn: the alternate slot value was not persisted into booking state/session memory before policy-core continued.
  4. Why did this produce a repeated name question: downstream booking prompt still saw `name` as missing.
  5. Why is this not a prompt/lexicon problem: the failure is boundary logic in expected-reply arbitration, not semantic ownership or pack data.
- Root cause statement:
  - stale or lagging booking `expected_reply_type` causes question-contract to discard explicit valid answers for a different booking slot, because `interpret_expected_reply(...)` normalizes mismatch back to the expected slot and `decision.py` then fail-drops the answer instead of capturing the alternate validated slot while keeping the true missing slot pending.
- Fix mechanism:
  - preserve alternate-slot evidence from answer interpreter / local validation
  - when a valid explicit alternate booking slot answer is detected, write it into booking state/session memory without clearing the still-missing requested slot
  - add focused regressions proving that stale `time` does not cause repeated `name` prompts
  - no expensive rerun in this block

## Invariant

1. The true missing booking slot must remain pending; this block must not greedily clear `expected_reply_type`.
2. Collect slot-order invariant stays intact; prompts still ask for the earliest missing slot.
3. No prompt/lexicon/hardcode patch-fitting is allowed.
4. No new expensive `dev L2 / lock / replay` run starts before deterministic closure of this block.

## Scope

- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-06-p1.6-local-realism-canonical-lock-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6m-booking-expected-reply-alternate-slot-capture-a1.md`
- `truffles-api/app/routers/webhook/decision.py`
- `truffles-api/app/services/intent_service.py`
- `truffles-api/tests/test_message_endpoint.py`
- `truffles-api/tests/test_intent.py`

## Out of scope

- new expensive `demo_salon` quality run in this block
- `P1.7` replay
- `canary/full`
- unrelated booking/info families

## Plan (1..N)

1. Record the `r21` blocker and RCA in TP/master docs.
2. Preserve alternate-slot evidence in the answer-interpreter path instead of collapsing it away.
3. Capture validated alternate booking slot answers into booking state/session memory while keeping the still-missing requested slot pending.
4. Add focused regressions for stale `time -> name` mismatch handling.
5. Rerun deterministic checks only.

## DoD

1. A valid explicit alternate booking slot answer is no longer dropped on `slot_mismatch`.
2. The still-missing requested slot remains active after alternate-slot capture.
3. `r21` family is closed on deterministic evidence without a new expensive rerun.
4. Docs clearly state that the next allowed expensive action remains exactly one fresh guarded `demo_salon` `dev L2` only after this block is green.

## Checks

- `pytest -q truffles-api/tests/test_intent.py -k "AnswerInterpreterSchema or alternate_slot"`
- `pytest -q truffles-api/tests/test_message_endpoint.py -k "collect_slot_order or alternate_slot or expected_reply"`
- `pytest -q truffles-api/tests/test_message_endpoint.py`
- `pytest -q truffles-api/tests/test_booking_quality_response_guard.py`
- `pytest -q truffles-api/tests/test_demo_salon_eval.py`
- `pytest -q truffles-api/tests/test_booking_chaos_dialogs.py`
- `ruff check truffles-api/app/routers/webhook/decision.py truffles-api/app/services/intent_service.py truffles-api/tests/test_intent.py truffles-api/tests/test_message_endpoint.py`
- `git diff --check`

## Execution status

- `done`
- stale `expected_reply_type -> alternate booking slot` capture is now closed on deterministic contour
- stale `expected_reply_type -> alternate booking slot` capture now also seeds minimal booking context when the stale expected reply survives without a live `booking` object
- `r21` remains partial forensic evidence only; it is not lock evidence and not replay evidence

## Evidence

1. `/tmp/booking_quality/p1.6-l2-dev-seed42-r21/summary.json`
2. `/tmp/booking_quality/p1.6-l2-dev-seed42-r21/brief.md`
3. `/tmp/booking_quality/p1.6-l2-dev-seed42-r21/responses.jsonl`
4. `/tmp/booking_quality/p1.6-l2-dev-seed42-r21/trace_bundle.jsonl`
5. `/tmp/booking_quality/p1.6-l2-dev-seed42-r21/manual_audit.json`
6. `/tmp/booking_quality/p1.6-l2-dev-seed42-r21/manual_audit.md`
7. deterministic closure on `2026-03-07`:
   - `pytest -q truffles-api/tests/test_intent.py -k "AnswerInterpreterSchema or slot_mismatch_preserves_detected_slot"` -> `3 passed, 44 deselected`
   - `pytest -q truffles-api/tests/test_message_endpoint.py -k "captures_alternate_name_without_clearing_time or expected_reply_time_merges_datetime_and_clears_stale_intent_queue or question_like_info_does_not_match_deterministic"` -> `3 passed, 298 deselected`
   - `pytest -q truffles-api/tests/test_message_endpoint.py -k "without_booking_context or captures_alternate_name_without_clearing_time"` -> `2 passed, 300 deselected`
   - `pytest -q truffles-api/tests/test_message_endpoint.py` -> `302 passed, 2 warnings`
   - `pytest -q truffles-api/tests/test_booking_quality_response_guard.py` -> `60 passed`
   - `pytest -q truffles-api/tests/test_demo_salon_eval.py` -> `17 passed`
   - `pytest -q truffles-api/tests/test_booking_chaos_dialogs.py` -> `1 passed`
   - `ruff check truffles-api/app/routers/webhook/decision.py truffles-api/app/services/intent_service.py truffles-api/tests/test_intent.py truffles-api/tests/test_message_endpoint.py` -> `All checks passed!`
   - `git diff --check` -> `clean`

## Rollback

1. revert only the alternate-slot evidence/capture patch
2. keep expensive `demo_salon` runs blocked until deterministic contour is green again

## No-go

- no expensive rerun before code delta and deterministic closure
- no threshold/oracle weakening
- no semantic shortcut in runtime core
- no silent clearing of the requested slot when only an alternate slot was captured

## Risks/Blockers

1. Over-eager alternate capture could accidentally bypass slot-order discipline if validation is too broad.
2. Answer-interpreter payload changes can break stale tests asserting the previous minimal payload shape.

## Residual architecture debt (mandatory)

- Current residuals accepted in this block:
  - no fresh post-fix `demo_salon` `dev L2` evidence yet
  - `P1.7` still not created
  - `P6` remains deferred
- Why not in this block:
  - this block only restores correct boundary behavior for booking expected-reply arbitration on the `demo_salon` lane
- Risk if deferred:
  - every next expensive `demo_salon` run can stop on the same booking-stall family
- Linked follow-up Task Package(s):
  - `docs/TASK_PACKAGES/TP-2026-03-06-p1.6-local-realism-canonical-lock-a1.md`
  - `TBD` for `P1.7 Acceptance replay + regression gate`
- Expiry/trigger to stop deferral:
  - before the next guarded `demo_salon` `dev L2`

## Next-block contract (mandatory)

- Next block objective:
  - run exactly one fresh guarded `demo_salon` `dev L2` on `http://127.0.0.1:18184` and confirm whether the `r21` family is gone before any `lock/replay`
- First deterministic check command:
  - `pytest -q truffles-api/tests/test_message_endpoint.py && pytest -q truffles-api/tests/test_booking_quality_response_guard.py && pytest -q truffles-api/tests/test_demo_salon_eval.py && pytest -q truffles-api/tests/test_booking_chaos_dialogs.py`
- Blocked-by conditions:
  - runtime `http://127.0.0.1:18184` not refreshed to the current worktree state after the code delta
  - guarded `dev L2` run-id / budget window / `PG checklist` not prepared
  - `OPENAI_API_KEY` / judge access missing for the guarded run
- Owner role for closure:
  - `Top Architect + Brain`

# TP-2026-03-12-p1.6o89-slot-constraint-generic-free-slot-question-governance-a1

- Название/цель: закрыть surfaced `M30` scenario-governance drift, где после bounded `M29` closure fresh guarded `dev L2` больше не падает на booking-tag requested-slot contamination, но generic free-slot question `Какое время доступно?` всё ещё генерируется как `slot_constraint(time)`, хотя runtime канонически держит `ask_about_requested_slot(time)` / `booking_slot_guidance` даже на timeout-degraded path.

## Canon refs

- Parent closure program: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Main remediation: `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- Historical requested-slot owner closure: `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o16-booking-time-guidance-question-owner-a1.md`
- Historical free-slot owner closure: `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o59-live-availability-slot-question-policy-core-contract-a1.md`
- Proof lane that surfaced the row: `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o88-post-m29-proof-lane-a1.md`
- State + matrix baseline: `STATE.md`, `truffles-api/app/knowledge/generic/INTERACTION_OWNER_MATRIX.yaml`

## Git context

- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect` remove branch + worktree after merge

## One web search (mandatory before implementation)

- Query: `site:rasa.com/docs/rasa/forms requested_slot user asks question while slot active`
- Date/time: `2026-03-12T12:05:41+05:00`
- Sources opened:
  - official Rasa forms docs: `https://rasa.com/docs/rasa/forms/`
- Source quality:
  - official product documentation
- Found reusable ideas:
  - while a slot is active, follow-up user questions should be interpreted relative to the requested slot rather than collapsed into a different slot state;
  - constraint-like handling must stay tied to actual slot constraints, not generic availability questions.
- Decision:
  - `reuse + integrate`
  - reuse existing requested-slot normalization helpers in `scripts/booking_dialog_scenarios.py` and integrate one bounded rule that rewrites generic free-slot questions out of `slot_constraint` under active `time` collect.
- Rejected options:
  - weakening scenario expectations to accept stale `slot_constraint`;
  - runtime patching to imitate stale generator governance;
  - broad redesign of the whole pending-question ontology in this block.

## Root cause (mandatory)

- Symptom:
  - fresh guarded `dev L2` `p1.6o88-l2-dev-20260312-a1-r1` no longer stops on `M29`, but now fails on dialog `1`, turn `2`, text `Какое время доступно?` with `expected_meta_mismatch` / `expected_trace_miss` because generated expectations require `slot_constraint(time)` while runtime canonically emits `ask_about_requested_slot(time)`.
- Minimal reproduction:
  1. Inspect `/tmp/booking_quality/p1.6o88-l2-dev-20260312-a1-r1/scenarios.json` dialog `1`, turn `2`.
  2. Confirm the generated turn is tagged `slot_constraint` and expects `pending_question_act=slot_constraint` plus `pending_question_interaction slot_constraint/time`.
  3. Inspect `/tmp/booking_quality/p1.6o88-l2-dev-20260312-a1-r1/responses.jsonl` dialog `1`, turn `2`.
  4. Confirm runtime produces `source=booking_slot_guidance`, `pending_question_act=ask_about_requested_slot`, `pending_question_target=time`, and `expected_reply_type=time`.
  5. Compare with existing generator rules and confirm there is no bounded normalization for generic free-slot questions that were mis-tagged as `slot_constraint` without an actual temporal constraint.
- Evidence:
  - `/tmp/booking_quality/p1.6o88-l2-dev-20260312-a1-r1/scenarios.json`
  - `/tmp/booking_quality/p1.6o88-l2-dev-20260312-a1-r1/responses.jsonl`
  - `/tmp/booking_quality/p1.6o88-l2-dev-20260312-a1-r1/trace_bundle.jsonl`
  - `scripts/booking_dialog_scenarios.py`
  - `truffles-api/tests/test_booking_dialog_scenarios_script.py`
- Five Whys:
  1. Why did the run fail? Because scenario meta/trace still expected `slot_constraint(time)` instead of requested-slot guidance.
  2. Why did scenario expect that? Because the generated turn stayed tagged as `slot_constraint` even though the text only asks what times are available.
  3. Why was it not normalized away? Because current normalization handles booking-tag contamination and real question-like temporal constraints, but not generic availability questions mis-tagged as `slot_constraint`.
  4. Why is runtime already ahead? Because under active `time` collect, a generic free-slot question is canonically `ask_about_requested_slot(time)` even when the policy-core path degrades on timeout.
  5. Why is this a bounded scenario-governance row? Because runtime meta/trace already preserve the requested-slot contract; the remaining defect is stale generated expectation/trace state.
- Root cause statement:
  - the scenario generator lacks a bounded normalization that converts generic free-slot questions mis-tagged as `slot_constraint` back to canonical `ask_about_requested_slot(time)` under active `time` collect, so stale constraint expectations survive after runtime has already kept the requested-slot contract.
- Fix mechanism:
  - add bounded requested-slot normalization for generic free-slot questions that were mis-tagged as `slot_constraint`;
  - add deterministic sanitize/repair regressions;
  - sync matrix/docs/state truthfully and keep the next expensive proof step blocked until deterministic closure is green.

## Reuse-first decision (mandatory)

- Reuse:
  - existing requested-slot normalization helpers in `scripts/booking_dialog_scenarios.py`
  - existing deterministic generator test harness in `truffles-api/tests/test_booking_dialog_scenarios_script.py`
- Integrate:
  - one bounded normalization for generic free-slot `slot_constraint` overclaim
  - targeted sanitize/repair regressions
- Build:
  - no runtime changes planned in this block

## Release safety

- Rollout: deterministic scenario-governance only; no production runtime change
- Go signal:
  - generic free-slot questions normalize to canonical `ask_about_requested_slot(time)`
  - genuine temporal constraints such as `после обеда` or `до шести` stay in `slot_constraint`
  - `git diff --check` and `session_check` remain green
- No-go signal:
  - generator rewrites legitimate `slot_constraint` turns into requested-slot guidance
  - runtime-related files become necessary in this block
- Rollback:
  - revert only the new generator normalization and matching tests

## Invariant

1. `M29` stays boundedly cleared by `P1.6o87` + `P1.6o88`.
2. Genuine temporal constraint turns remain `slot_constraint`.
3. Runtime behavior is untouched in this block.
4. No oracle weakening.

## Scope

- bounded scenario-governance closure for generic free-slot questions mis-tagged as `slot_constraint`
- deterministic tests
- matrix/docs/state/session sync

## Out of scope

- new guarded `dev L2`
- acceptance `lock/replay`
- runtime semantic changes

## Touch-list

- `scripts/booking_dialog_scenarios.py`
- `truffles-api/tests/test_booking_dialog_scenarios_script.py`
- `truffles-api/app/knowledge/generic/INTERACTION_OWNER_MATRIX.yaml`
- `truffles-api/tests/test_interaction_owner_matrix_contract.py`
- `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o89-slot-constraint-generic-free-slot-question-governance-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o90-post-m30-proof-lane-a1.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `STATE.md`
- `STRUCTURE.md`
- `docs/SESSIONS/SESSION-2026-02-19-llm-first-firebreak-a1.md`

## Plan (1..N)

1. Publish `P1.6o89` with RCA + one web search + bounded touch-list.
2. Add bounded normalization for generic free-slot questions mis-tagged as `slot_constraint` under active `time` collect.
3. Add deterministic sanitize/repair regressions plus matrix contract update.
4. Run targeted deterministic checks and hygiene gates.
5. Sync active docs/state/session with factual deterministic closure and next admissible proof block.

## DoD

1. Generic free-slot question surfaces like `Какое время доступно?` canonicalize to requested-slot `time` guidance under active `time` collect.
2. Generated expectations preserve `pending_question_act=ask_about_requested_slot`, `pending_question_target=time`, `expected_reply_type=time`.
3. Genuine temporal constraints stay `slot_constraint`.
4. Deterministic tests are green.
5. `M30` is truthfully synced in matrix/docs/state with the next admissible proof block identified.

## Checks

- `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "slot_constraint and requested_slot"`
- `pytest -q truffles-api/tests/test_interaction_owner_matrix_contract.py`
- `git diff --check`
- `SESSION_AGENT=a1 scripts/session_check.sh`

## Execution status

- `done`

## Evidence

1. `/tmp/booking_quality/p1.6o88-l2-dev-20260312-a1-r1/scenarios.json`
2. `/tmp/booking_quality/p1.6o88-l2-dev-20260312-a1-r1/responses.jsonl`
3. `/tmp/booking_quality/p1.6o88-l2-dev-20260312-a1-r1/trace_bundle.jsonl`
4. `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "slot_constraint and requested_slot"` -> `3 passed, 93 deselected`
5. `pytest -q truffles-api/tests/test_interaction_owner_matrix_contract.py` -> `2 passed`
6. `git diff --check`
7. `SESSION_AGENT=a1 scripts/session_check.sh`
8. updated matrix/docs/state/session references

## Rollback

1. Revert only the new generator normalization and matching deterministic docs/tests.
2. Keep `P1.6o88` proof evidence and `M29` closure intact.
3. Do not reinterpret `M30` as runtime defect if deterministic closure is rolled back.

## No-go

- no runtime edits
- no new expensive run in this block
- no weakening requested-slot contract globally
- no phrase hardcode for only one exact string

## Risks / blockers

- generic free-slot questions sit close to real temporal-constraint surfaces and the bounded rule must avoid collapsing those genuine constraints.
- timeout-degraded requested-slot guidance can make the row look like a runtime gap if meta/trace are not reviewed carefully.

## Residual architecture debt (mandatory)

### Current residuals accepted in this block

- Scenario ontology still relies on localized normalization heuristics for some pending-question cross-products.
- Acceptance/proof lane remains blocked until a fresh rerun confirms `M30` is gone.

### Why not in this block

- This block is a bounded scenario-governance repair, not a broader ontology redesign.

### Risk if deferred

- New expensive runs will keep stopping on false-red generic slot-question governance and hide later remaining families.

### Linked follow-up Task Package(s)

- `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o90-post-m30-proof-lane-a1.md`

### Expiry/trigger to stop deferral

- before any next guarded `dev L2` / `lock` / `replay`

## Next-block contract (mandatory)

### Next block objective

- publish and run `P1.6o90`: execute one fresh guarded `dev L2` after `P1.6o89` deterministic closure to prove generic free-slot `slot_constraint` row `M30` is no longer first fail and truthfully surface the next family, if any.

### First deterministic check command

- `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "slot_constraint and requested_slot" && pytest -q truffles-api/tests/test_interaction_owner_matrix_contract.py && git diff --check`

### Blocked-by conditions

- `P1.6o89` deterministic closure is not synced truthfully into active docs and matrix state
- generic free-slot `slot_constraint` deterministic reproduction is not green
- live runtime fingerprint for the proof lane is not proven against current `HEAD`

### Owner role for closure

- `Top Architect + Brain`

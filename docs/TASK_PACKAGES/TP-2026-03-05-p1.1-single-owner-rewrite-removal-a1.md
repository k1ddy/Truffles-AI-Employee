# TP-2026-03-05-p1.1-single-owner-rewrite-removal-a1

- Название/цель: P1.1 remove latent semantic rewrite runtime paths; оставить только boundary validation/degrade events.
- Parent TP: `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`.
- Canon refs: `AGENTS.md`, `STATE.md`, `STRATEGY/REQUIREMENTS.md`, `SPECS/SYSTEM_REFERENCE.md`.
- Branch: `fix/llm-first-firebreak-2026-02-19`.
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`.

## One web search (mandatory before implementation)

- Reused from parent TP (same block, no additional queries):
  - Query: `NIST SP 800-207 policy decision point policy enforcement point`
  - Time (UTC): `2026-03-05T12:44:50Z`
  - Sources:
    - `https://csrc.nist.gov/pubs/sp/800/207/final`
    - `https://csrc.nist.gov/glossary/term/policy_decision_point`
    - `https://csrc.nist.gov/glossary/term/policy_enforcement_point`

## Root cause (mandatory)

- Symptom:
  - В `decision.py` присутствовали latent `else` ветки post-hoc semantic rewrites (`policy_action/policy_tool_action/policy_intent` mutations), даже при hard lock.
- Minimal reproduction:
  - `rg -n "_register_policy_override\(" truffles-api/app/routers/webhook/decision.py`
  - `rg -n "_register_policy_intent_override\(" truffles-api/app/routers/webhook/decision.py`
- Evidence:
  - До фикса: множество semantic override callsites для `style_reference_without_media`, `collect_slot_recovery`, `collect_complete_slots_promoted_to_booking`, `collect_plan_replay_for_list_slots`, `name_stage_commit_promoted_to_booking`, `booking_request_blocks_service_query_fact`, `explicit_location_or_hours_signal`, `master_signal_override`.
- Root cause statement:
  - Semantic rewrite capability оставалась в runtime коде как fallback path, создавая риск вторичного semantic owner.
- Fix mechanism:
  - Удалить rewrite branches физически, сохранить только `semantic_override_blocked` evidence.

## Invariant

1. Semantic owner только policy-core LLM.
2. Deterministic слой не меняет semantic решение post-hoc.
3. Boundary overrides допускаются только для `policy_guard` и `tool_unavailable` degrade-contract.

## Scope

- Изменить только `truffles-api/app/routers/webhook/decision.py`.
- Удалить latent semantic rewrites и intent override helper.

## Out of scope

- FACT evidence linkage (`P4`).
- Booking atomic commit protocol (`P3`).
- Phrase-routing removal (`P1.2`).

## Touch-list

- `truffles-api/app/routers/webhook/decision.py`
- `docs/TASK_PACKAGES/TP-2026-03-05-p1.1-single-owner-rewrite-removal-a1.md`

## Implementation

1. Removed `_register_policy_intent_override` helper and its only runtime usage path.
2. Removed post-hoc rewrite `else` branches and kept fail-closed block events for:
  - `action_invalid` normalization fallback
  - `action_tool_mismatch` normalization fallback
  - `style_reference_without_media`
  - `collect_slot_recovery`
  - `service_query_non_service_refs`
  - `collect_complete_slots_promoted_to_booking`
  - `collect_plan_replay_for_list_slots`
  - `name_stage_commit_promoted_to_booking`
  - `booking_request_blocks_service_query_fact`
  - `explicit_location_or_hours_signal`
  - `master_signal_override`
3. Preserved boundary override events only for:
  - `_apply_policy_guard_override(...)`
  - `calendar_capability_blocked_handoff` (`reason_code=tool_unavailable`)
4. Extended `_record_semantic_override_block(...)` with optional `source` to preserve deterministic meta evidence.

## DoD

1. Нет latent semantic rewrite branches в runtime для указанных сценариев.
2. `_register_policy_intent_override` отсутствует в файле.
3. `_register_policy_override` используется только для boundary contracts (guard/tool_unavailable).
4. Обязательные deterministic тесты по затронутым областям green.

## Checks (executed)

- Static checks:
  - `rg -n "_register_policy_intent_override\(" truffles-api/app/routers/webhook/decision.py` -> no matches
  - `rg -n "_register_policy_override\(" truffles-api/app/routers/webhook/decision.py` -> definition + 2 callsites (guard/tool_unavailable)
  - `ruff check truffles-api/app/routers/webhook/decision.py` -> `All checks passed`
- Tests:
  - `pytest -q truffles-api/tests/test_message_endpoint.py` -> `282 passed, 2 warnings`
  - `pytest -q truffles-api/tests/test_booking_chaos_dialogs.py` -> `1 passed`
  - `pytest -q truffles-api/tests/test_booking_quality_response_guard.py` -> `57 passed`
  - `pytest -q truffles-api/tests/test_demo_salon_eval.py --maxfail=1 -vv` -> `17 passed`

## Evidence

- Code diff: `truffles-api/app/routers/webhook/decision.py`
- Test outputs captured in session log/terminal for above commands.

## Rollback

1. `git revert <commit_with_p1.1_changes>`
2. Re-run:
  - `pytest -q truffles-api/tests/test_message_endpoint.py`
  - `pytest -q truffles-api/tests/test_booking_quality_response_guard.py`

## No-go

- Не возвращать rewrite branches behind feature flags.
- Не чинить semantic regressions через keyword hardcode.

## Residual architecture debt (mandatory)

- Current residuals accepted in this block:
  - `P1.2` core phrase routing still exists.
  - `P1.3/P1.4/P1.5` pending.
- Why not in this block:
  - strict one-point closure (`P1.1` only).
- Risk if deferred:
  - phrase coupling может продолжать бизнес-coupling даже без rewrite.
- Linked follow-up Task Package(s):
  - `P1.2` (next).
- Expiry/trigger to stop deferral:
  - before any canary/full rollout.

## Next-block contract (mandatory)

- Next block objective:
  - `P1.2` remove core phrase routing from semantic arbitration path.
- First deterministic check command:
  - `rg -n "адрес|где|парков|до скольки|во сколько|мест" truffles-api/app/routers/webhook/decision.py`
- Blocked-by conditions:
  - regression in `test_message_endpoint.py` or reintroduced semantic rewrite path.
- Owner role for closure:
  - `Top Architect + Brain`

# TP-2026-03-07-p1.6o11-master-query-clarify-info-oracle-boundary-a1

- Название/цель: закрыть новый `P1.6o6` blocker, где guarded `dev L2` ошибочно считает `master_query -> clarify_missing_subject -> service_clarify` ответ `info_section_miss`, хотя runtime корректно fail-closed запрашивает услугу.
- Parent TP: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Canon refs: `AGENTS.md`, `STATE.md`, `STRUCTURE.md`, `STRATEGY/REQUIREMENTS.md`, `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o3-referent-resolver-capability-arbitration-a1.md`, `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o5-oracle-acceptance-contract-realignment-a1.md`, `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`
- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`

## One web search (mandatory before implementation)

- Query: `site:rasa.com/docs forms required slots ask for missing information`
- Time (UTC): `2026-03-07T16:28:13Z`
- Sources:
  - `https://rasa.com/docs/rasa/forms/`
- Ready solutions found:
  - when required information is missing, the assistant should stay in slot-filling mode and ask for the missing slot instead of pretending it answered the original info request;
  - acceptance for such turns should follow the missing-slot contract, not a knowledge-answer oracle.
- Decision (`reuse/integrate/build`): `reuse + integrate`
  - reuse the existing guarded oracle and current `clarify_missing_subject` runtime contract;
  - integrate a narrow fallback in `ops/diagnose.py` for `master_query` turns that legitimately degrade into `service_clarify`.
- Rejected options:
  - forcing runtime to emit fake `master` info sections for incomplete requests;
  - weakening `info_section_miss` globally for all `master` turns;
  - changing `P1.6o3` runtime contract that already fail-closes correctly.

## Root cause (mandatory)

- Symptom:
  - guarded `dev L2` rerun `p1.6o6-l2-dev-20260307-a1-r5` stopped at `dialog_index=2`, `turn_index=3`
  - user turn: `Есть ли у вас мастера на завтра?`
  - family: `info_section_miss`

- Minimal reproduction:
  1. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260307-a1-r5/brief.md`
  2. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260307-a1-r5/failure_families.json`
  3. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260307-a1-r5/responses.jsonl` for `message_id=LLM-QUAL-p1.6o6-l2-dev-20260307-a1-r5-002-03-af2696`
  4. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260307-a1-r5/trace_bundle.jsonl` for the same `message_id`
  5. inspect `_llm_quality_evaluate_turn()` and info fallback helpers in `ops/diagnose.py`

- Evidence:
  - runtime meta on the failing turn is `action=reply`, `intent=service_clarify`, `expected_reply_type=service_choice`, `source=llm_policy_core`
  - embedded policy-core payload is `intent=master_query`, `action=collect`, `subject_kind=specialist`, `temporal_scope=specific_time`, `resolution_mode=clarify_missing_subject`, `next_question=service`
  - trace contains `question_contract` with `missing_slot=service` and no `info_sections`, which matches the `P1.6o3` fail-closed contract
  - diagnose currently infers `master` from user text (`мастера`) and then requires an answered info section unless one of the existing generic/catalog fallbacks matches

- Five Whys:
  1. Why did guarded quality fail: the turn was classified as an unanswered master-info request.
  2. Why was it classified that way: diagnose inferred `master` from the user text and saw no `info_sections` in meta/trace.
  3. Why were there no `info_sections`: runtime correctly chose `clarify_missing_subject` because service referent was absent.
  4. Why did the oracle not accept that: current info fallback logic covers generic consult and catalog service-choice fallbacks, but not `master_query` missing-subject clarifies.
  5. Why is this an oracle boundary issue: changing runtime would violate the `P1.6o3` contract; the mismatch is between valid runtime evidence and acceptance classification.

- Root cause statement:
  - the guarded oracle treats `master`-tagged turns as `info_section_miss` even when policy-core explicitly emits `master_query + clarify_missing_subject` and the runtime correctly responds with `service_clarify`.

- Fix mechanism:
  - add a narrow oracle fallback for `master_query` turns that end in `service_clarify` with `resolution_mode=clarify_missing_subject` and `expected_reply_type=service_choice`
  - prove it with exact tests around turn evaluation and diagnose helper behavior
  - rerun one fresh guarded `dev L2` after the oracle fix

## Invariant

1. `P1.6o3` runtime contract stays unchanged: missing service referent for master-availability questions must fail closed into explicit service clarify.
2. Real answered `master/specialist` info turns still require `info_sections`/intent evidence.
3. The new fallback is narrow and trace-backed; it must not hide unrelated `info_section_miss` regressions.

## Scope

- `ops/diagnose.py`
- `truffles-api/tests/test_booking_quality_response_guard.py`
- `truffles-api/tests/test_diagnose_run_command.py`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o11-master-query-clarify-info-oracle-boundary-a1.md`

## Out of scope

- changing runtime booking/master arbitration
- changing judge thresholds or acceptance thresholds
- acceptance `lock/replay`

## Touch-list

- `ops/diagnose.py`
- `truffles-api/tests/test_booking_quality_response_guard.py`
- `truffles-api/tests/test_diagnose_run_command.py`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o11-master-query-clarify-info-oracle-boundary-a1.md`

## Plan (1..N)

1. Reproduce the `r5` blocker and confirm runtime evidence already satisfies the `clarify_missing_subject` contract.
2. Add a narrow diagnose fallback for `master_query` missing-subject clarifies.
3. Add exact tests for both turn evaluation and direct diagnose helper behavior.
4. Run targeted deterministic suites for diagnose/oracle.
5. Return to `P1.6o6` for exactly one fresh guarded `dev L2`.

## DoD

1. `master_query` turns that end in `service_clarify` with `resolution_mode=clarify_missing_subject` no longer trigger `expected_info_section_miss` or `info_section_miss`.
2. True answered master-info turns still require `master/specialist` evidence.
3. Targeted deterministic suites are green.
4. `P1.6o6` can truthfully rerun one fresh guarded `dev L2` from this new baseline.

## Checks

- `pytest -q truffles-api/tests/test_booking_quality_response_guard.py -k "master_query or clarify_missing_subject or info_section_miss"`
- `pytest -q truffles-api/tests/test_diagnose_run_command.py -k "master_query or clarify_missing_subject or info_section_miss"`
- `pytest -q truffles-api/tests/test_booking_quality_response_guard.py`
- `pytest -q truffles-api/tests/test_diagnose_run_command.py`
- `ruff check ops/diagnose.py truffles-api/tests/test_booking_quality_response_guard.py truffles-api/tests/test_diagnose_run_command.py`
- `git diff --check`

## Execution status

- `done`
- exact oracle checks:
  - `pytest -q truffles-api/tests/test_booking_quality_response_guard.py -k "master_query or clarify_missing_subject or info_section_miss"` -> `2 passed, 65 deselected`
  - `pytest -q truffles-api/tests/test_diagnose_run_command.py -k "master_query or clarify_missing_subject or info_section_miss"` -> `1 passed, 17 deselected`
- full targeted suites:
  - `pytest -q truffles-api/tests/test_booking_quality_response_guard.py` -> `67 passed`
  - `pytest -q truffles-api/tests/test_diagnose_run_command.py` -> `18 passed`
  - `ruff check ops/diagnose.py truffles-api/tests/test_booking_quality_response_guard.py truffles-api/tests/test_diagnose_run_command.py` -> `pass`
  - `git diff --check` -> `clean`
- guarded follow-up:
  - fresh `dev L2` rerun `p1.6o6-l2-dev-20260307-a1-r6` finished and was audited
  - the prior `info_section_miss` / `master_query + clarify_missing_subject` blocker did not recur
  - `r6` surfaced a new blocker family `booking_slot_stall` at `dialog_index=4`, `turn_index=4`, message `Может быть, на утро?`
  - follow-up opened: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o12-daypart-partial-datetime-parity-a1.md`

## Evidence

1. `/tmp/booking_quality/p1.6o6-l2-dev-20260307-a1-r5/summary.json`
2. `/tmp/booking_quality/p1.6o6-l2-dev-20260307-a1-r5/brief.md`
3. `/tmp/booking_quality/p1.6o6-l2-dev-20260307-a1-r5/failure_families.json`
4. `/tmp/booking_quality/p1.6o6-l2-dev-20260307-a1-r5/manual_audit.md`
5. exact deterministic green outputs from this TP
6. `/tmp/booking_quality/p1.6o6-l2-dev-20260307-a1-r6/summary.json`
7. `/tmp/booking_quality/p1.6o6-l2-dev-20260307-a1-r6/brief.md`
8. `/tmp/booking_quality/p1.6o6-l2-dev-20260307-a1-r6/failure_families.json`
9. `/tmp/booking_quality/p1.6o6-l2-dev-20260307-a1-r6/manual_audit.md`

## Rollback

1. Revert only the diagnose fallback and its tests if it hides real master-info misses.
2. Do not treat `r6` as closure evidence for `P1.6o6`; resume the proof bundle only from the post-`P1.6o12` deterministic baseline.

## No-go

- no fake `master` info sections or synthetic evidence in runtime meta/trace
- no broad weakening of `info_section_miss`
- no runtime semantic rewrite to satisfy the oracle

## Risks/Blockers

1. A too-broad fallback could suppress real specialist-info regressions.
2. Any recurrence after `P1.6o12` should reopen the runtime/oracle boundary audit instead of broadening this fallback.

## Residual architecture debt (mandatory)

- Current residuals accepted in this block:
  - `P1.6o6` proof bundle still needs a fresh guarded `dev L2` from the post-`P1.6o12` deterministic baseline
- Why not in this block:
  - this block closed only the `master_query` oracle mismatch, while `r6` surfaced a separate runtime family
- Risk if deferred:
  - proof-bundle resume stalls and future reruns may blur resolved oracle debt with unresolved runtime debt
- Linked follow-up Task Package(s):
  - `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o12-daypart-partial-datetime-parity-a1.md`
  - `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`
- Expiry/trigger to stop deferral:
  - before any new guarded `lock/replay` or baseline update

## Next-block contract (mandatory)

- Next block objective:
  - resume `P1.6o6` preflight and run one fresh guarded `dev L2` (`r7`) from the post-`P1.6o12` deterministic baseline
- First deterministic check command:
  - `ruff check truffles-api/app/routers/webhook/booking.py truffles-api/app/routers/webhook/decision.py truffles-api/tests/test_message_endpoint.py && pytest -q truffles-api/tests/test_message_endpoint.py -k "daypart or same_day_info or question_without_datetime or question_like_info_does_not_match_deterministic or llm_policy_core_list_slots_keeps_context_datetime_when_expected_service_choice" && pytest -q truffles-api/tests/test_message_endpoint.py && pytest -q truffles-api/tests/test_booking_quality_response_guard.py`
- Blocked-by conditions:
  - `P1.6o12` deterministic contour is not green
  - `r6` audit is not recorded in docs
  - guarded runtime/judge prerequisites are not satisfied
- Owner role for closure:
  - `Hands` for rerun/evidence, `Brain + Top Architect` for proof-bundle resume decision

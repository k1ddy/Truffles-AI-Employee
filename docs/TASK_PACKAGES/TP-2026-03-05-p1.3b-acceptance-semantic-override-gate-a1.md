# TP-2026-03-05-p1.3b-acceptance-semantic-override-gate-a1

- Название/цель: P1.3b сделать semantic override budget fail-closed и blocking в acceptance lane.
- Parent TP: `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`.
- Canon refs: `AGENTS.md`, `STATE.md`, `STRATEGY/REQUIREMENTS.md`, `SPECS/SYSTEM_REFERENCE.md`.
- Branch: `fix/llm-first-firebreak-2026-02-19`.
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`.

## One web search (mandatory before implementation)

- Query: `site:csrc.nist.gov "deny by default"`
- Time (UTC): `2026-03-05T14:18:22Z`
- Sources:
  - `https://csrc.nist.gov/glossary/term/deny_by_default`
  - `https://csrc.nist.gov/glossary/term/fail_secure`
  - `https://csrc.nist.gov/pubs/sp/800/53/r5/upd1/final`
- Ready solutions found:
  - Deny-by-default/fail-secure: acceptance path must fail closed when safety budget is not strict.
- Decision (`reuse/integrate/build`): `integrate`
  - Add acceptance-lane preflight gate for zero semantic rewrite budgets and enforce defaults in guarded entrypoint.
- Rejected options:
  - Keep acceptance default with non-zero semantic rewrite budget.

## Root cause (mandatory)

- Symptom:
  - Acceptance lane did not explicitly require `max_post_llm_semantic_rewrite_rate=0.0`; parser default allowed `0.02`.
- Minimal reproduction:
  - `rg -n "max-post-llm-semantic-rewrite-rate|_llm_quality_build_quality_constant_status" ops/diagnose.py`
- Evidence:
  - `ops/diagnose.py` parser default for `--max-post-llm-semantic-rewrite-rate` was non-zero.
  - `quality_constant` acceptance checks did not enforce zero rewrite budgets.
- Five Whys:
  1. Why possible: gate focused on envelope flags (judge/media/coverage) but not rewrite budget strictness.
  2. Why risky: acceptance could pass while semantic rewrite budget remains non-zero.
  3. Why architecture issue: single-owner hard lock requires no semantic rewrite allowance in acceptance.
  4. Why scripts insufficient alone: direct `ops/diagnose.py llm-quality` bypasses wrapper defaults.
  5. Why fix now: P1.3 requires deterministic boundary hard lock end-to-end (runtime + acceptance gate).
- Root cause statement:
  - Acceptance preflight lacked explicit zero-budget contract for semantic rewrite metrics.
- Fix mechanism:
  - Enforce zero budgets in quality-constant acceptance gate and auto-inject zeros in guarded wrapper when absent.

## Invariant

1. Acceptance lane is fail-closed for semantic rewrite budgets.
2. Default acceptance execution keeps zero budget without relying on user memory.
3. Dev/forensic flexibility remains outside acceptance lane.

## Scope

- `ops/diagnose.py`
- `scripts/llm_quality_guarded.sh`
- `truffles-api/tests/test_booking_quality_status_gate.py`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-05-p1.3b-acceptance-semantic-override-gate-a1.md`

## Out of scope

- P1.4 contract shield.
- P1.5 lock/replay/full realism closure.

## Plan (1..N)

1. Add acceptance checks in `_llm_quality_build_quality_constant_status` for zero rewrite budgets.
2. Update llm-quality parser default for post-LLM semantic rewrite budget to zero.
3. Ensure guarded entrypoint appends zero budget flags for acceptance lane when omitted.
4. Add/adjust quality gate tests.
5. Run quality status deterministic tests.

## DoD

1. Acceptance lane rejects non-zero semantic rewrite budgets.
2. Guarded acceptance runs include zero-budget flags by default.
3. Quality status tests pass with new gate behavior.

## Checks

- Static:
  - `ruff check ops/diagnose.py truffles-api/tests/test_booking_quality_status_gate.py`
  - `bash -n scripts/llm_quality_guarded.sh`
- Deterministic:
  - `pytest -q truffles-api/tests/test_booking_quality_status_gate.py`

## Execution status

- `done`
- Static check results:
  - `ruff`: passed for modified files.
  - `bash -n`: passed for `llm_quality_guarded.sh`.
- Deterministic check results:
  - `test_booking_quality_status_gate.py`: `91 passed`.

## Evidence

1. Code diff in `ops/diagnose.py`, `scripts/llm_quality_guarded.sh`, tests.
2. Static and test command outputs.

## Rollback

1. `git revert <commit_with_p1.3b_changes>`
2. Re-run `pytest -q truffles-api/tests/test_booking_quality_status_gate.py`.

## No-go

- No acceptance bypass for non-zero rewrite budgets.
- No hidden fallback that silently relaxes quality-constant gate.

## Risks/Blockers

1. Existing external scripts invoking acceptance lane with custom non-zero budgets will now fail preflight.
2. Additional docs/runbook updates may be needed for explicit budget knobs.

## Residual architecture debt (mandatory)

- Current residuals accepted in this block:
  - `P1.4`, `P1.5` pending.
- Why not in this block:
  - Atomic closure discipline.
- Risk if deferred:
  - P1 not fully closed yet.
- Linked follow-up Task Package(s):
  - `TP-2026-03-05-p1.4-...`.
- Expiry/trigger to stop deferral:
  - Before any canary/full promotion.

## Next-block contract (mandatory)

- Next block objective:
  - `P1.4` contract tests/refactor shield for anti-regression.
- First deterministic check command:
  - `pytest -q truffles-api/tests/test_message_endpoint.py -k semantic_override`
- Blocked-by conditions:
  - Red deterministic suite on quality/status gates.
- Owner role for closure:
  - `Top Architect + Brain`

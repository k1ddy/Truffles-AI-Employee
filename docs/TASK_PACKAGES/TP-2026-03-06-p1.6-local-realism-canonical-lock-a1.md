# TP-2026-03-06-p1.6-local-realism-canonical-lock-a1

- Название/цель: P1.6 подтвердить single-owner runtime на обязательном local-first realism контуре и зафиксировать новый canonical acceptance lock после закрытия pre-realism semantic blocker-ов.
- Parent TP: `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`.
- Canon refs: `AGENTS.md`, `STATE.md`, `STRATEGY/REQUIREMENTS.md`, `TECH.md`, `docs/runbooks/BOOKING_CONFIRM_VERIFY.md`.
- Branch: `fix/llm-first-firebreak-2026-02-19`.
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`.

## One web search (mandatory before implementation)

- Query: `site:sre.google/sre-book canarying releases reliable product launches`
- Time (UTC): `2026-03-06T02:04:19Z`
- Sources:
  - `https://sre.google/sre-book/reliable-product-launches/`
  - `https://sre.google/workbook/canarying-releases/`
- Ready solutions found:
  - Reliable launch decisions need reproducible checklists/baselines before staged rollout.
  - Canary/release safety works only after appropriate validation steps and a trustworthy baseline exist.
- Decision (`reuse/integrate/build`): `reuse + integrate`
  - Reuse existing guarded quality entrypoints and audit flow.
  - Integrate them in the corrected order: local realism -> canonical lock -> later replay/canary.
- Rejected options:
  - Replay against a pre-P1 mixed-owner baseline.
  - Canary/full before a fresh post-P1 valid lock.

## Root cause (mandatory)

- Symptom:
  - После P1.5 старый acceptance baseline больше не является архитектурно сопоставимым: он был снят на mixed-owner runtime.
  - Запуск replay поверх него исказит результат, потому что будет смешивать реальные исправления semantic ownership с обычной регрессией.

- Minimal reproduction:
  1. `sed -n '24,35p' docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
  2. `sed -n '1,220p' docs/TASK_PACKAGES/TP-2026-03-06-p1.5-post-plan-semantic-rescue-removal-a1.md`
  3. `sed -n '1,220p' docs/runbooks/BOOKING_CONFIRM_VERIFY.md`

- Evidence:
  - `P1.4` and `P1.5` removed runtime semantic rescue/hint hardcode from the LLM path.
  - Acceptance anti-drift rules require replay only against a valid comparable lock.
  - Earlier chain state already showed rollout blocked and pre-existing baseline trust issues.

- Five Whys:
  1. Why can’t we jump directly to replay: baseline semantics changed materially after P1 closure.
  2. Why is that a problem: replay would compare different runtimes, not the same runtime before/after a bounded fix.
  3. Why does that weaken trust: false regressions and false passes become indistinguishable.
  4. Why must lock come first: lock is the canonical reference for the new single-owner runtime.
  5. Why do realism and lock stay in one block: realism is the precondition for a trustworthy lock artifact.

- Root cause statement:
  - There is no valid post-P1 canonical baseline yet, so replay/canary decisions remain underdetermined until we create one on the corrected runtime.

- Fix mechanism:
  - Run the required local deterministic contour.
  - Run guarded local realism / acceptance lock with strict preflight and strict post-run audit.
  - Freeze resulting artifacts as the new single-owner baseline for the following replay block.

## Invariant

1. No rollout decision is made from an invalid or pre-P1 baseline.
2. Quality evidence is accepted only when `infra_valid=true`, `semantic_valid=true`, `judge.enabled=true`, `manual_audit=done`.
3. P1 runtime is evaluated on the corrected single-owner path, not on a mixed-owner fallback path.

## Scope

- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-06-p1.6-local-realism-canonical-lock-a1.md`
- `docs/runbooks/BOOKING_CONFIRM_VERIFY.md`
- `/tmp/booking_quality/*` artifacts produced by this block

## Out of scope

- P1.7 replay regression gate.
- Canary/full rollout.
- New core-code changes unless preflight or lock exposes a concrete blocker that must be fixed in a separate TP.

## Plan (1..N)

1. Verify local preflight and runtime prerequisites for guarded quality.
2. Run mandatory deterministic suites required for core behavior confidence.
3. Close pre-lock readiness envelope (`P1.6c`): audit debt, `L1/L2 evidence`, `PG checklist`.
4. Run guarded acceptance lock on the corrected runtime with a fresh run id.
5. Run strict post-run audit and capture the evidence packet.
6. Update TP execution status with exact artifact paths and commands for the next replay block.

## DoD

1. Mandatory deterministic suites are green.
2. Guarded lock run finishes as valid: `infra_valid=true`, `semantic_valid=true`, `judge.enabled=true`.
3. Post-run audit is `done` with strict artifacts.
4. Evidence packet exists and is suitable to become the replay baseline for P1.7.

## Checks

- `pytest -q truffles-api/tests/test_message_endpoint.py`
- `pytest -q truffles-api/tests/test_booking_chaos_dialogs.py`
- `pytest -q truffles-api/tests/test_booking_quality_response_guard.py`
- `pytest -q truffles-api/tests/test_demo_salon_eval.py`
- `scripts/llm_quality_guarded.sh --mode lock --run-id booking-lock-p1.6-<id> --pg-checklist /tmp/booking_quality/pg_checklist-p1.6-<id>.json -- --base-url <url> --client-slug demo_salon --mode llm --count 10 --min-turns 10 --max-turns 15 --include-media --scenario-coverage booking,info,interrupt,handoff --tool-hooks auto --jid-mode unique --judge-mode all --quality-lane acceptance --run-economy-gate block --fail-on-thresholds`
- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/booking-lock-p1.6-<id> --status done --strict-artifacts`

## Execution status

- `budget-blocked`
- Acceptance strategy clarified:
  - `lock/replay/canary/full` is the promotion chain, not the primary debugging loop.
  - before guarded `lock`, this block now requires readiness closure through `P1.6c`.
  - after the latest `budget-stop`, the mandatory next block is `P1.6k demo_salon release-lane cleanup`; no fresh expensive `dev L2/lock/replay` is allowed until that cleanup is complete.
- Deterministic prerequisite revalidated on current runtime code:
  - `pytest -q tests/test_message_endpoint.py` -> `283 passed, 2 warnings`
  - `pytest -q tests/test_booking_chaos_dialogs.py` -> `1 passed`
  - `pytest -q tests/test_booking_quality_response_guard.py` -> `57 passed`
  - `pytest -q tests/test_demo_salon_eval.py` -> `17 passed`
- L1 pre-lock evidence prepared:
  - `pytest -q tests/test_intent.py::TestPolicyCoreTimeoutRetry::test_retries_without_response_format_when_provider_rejects_it tests/test_intent.py::TestPolicyCoreTimeoutRetry::test_policy_core_response_format_is_provider_compatible_and_keeps_dynamic_objects tests/test_cross_domain_signal_contract_suite.py::test_signal_token_prefix_match_uses_data_driven_prefixes_for_parking_capacity tests/test_demo_salon_eval.py::test_info_bundle_parking_capacity_followup_keeps_parking_section --junitxml /tmp/booking_quality/l1-p1.6-preflight-20260306-a1-r1/pytest-junit.xml` -> `4 passed`
- Runtime preflight corrected:
  - branch runtime clone `truffles-api-p1.6-cdc8c2a` on `http://127.0.0.1:18184`
  - `/admin/version` commit matches worktree `cdc8c2a82ac7db8308ab283be5691dc1a9f57b66`
- Local realism evidence runs:
  - `booking-dev-p1.6-20260306-a1-r4` -> incomplete, manual audit done
  - `booking-dev-p1.6-20260306-a1-r5` -> invalid `run_economy`, manual audit done
  - `booking-dev-p1.6-20260306-a1-r6` -> stopped after runtime blocker confirmation, manual audit done
- Closed during follow-up blocker TP:
  - `P1.6a` removed provider-incompatible policy-core structured-output schema and restored provider-compatible `response_format` probe with `INVALID_SCHEMA_COUNT=0`
  - `P1.6b` removed the remaining `hardcode_core_violation` from `truffles-api/app/routers/webhook/info.py`; direct hardcode gate recheck is now `valid=True`
- PG checklist preparation status:
  - historical pending manual audits that blocked new runs were explicitly closed for:
    - `booking-dev-p1.6-20260306-a1-r3`
    - `booking-lock-p11-20260305-a4`
    - `p1.6-l2-dev-seed42-r1`
    - `p1.6-l2-dev-seed42-r2`
    - `p1.6-l2-dev-seed42-r3`
    - `p1.6-l2-dev-seed42-r4`
    - `p1.6-l2-dev-seed42-r5`
  - direct dev L2 preflight also exposed a runtime contract requirement: `--jid-mode unique` needs `--allow-non-allowlist` on this local runtime
  - completed non-acceptance L2 evidence run:
    - `p1.6-l2-dev-seed42-r6` -> `infra_valid=true`, `semantic_valid=false`, `run_integrity_valid=true`
    - audit: `/tmp/booking_quality/p1.6-l2-dev-seed42-r6/manual_audit.json`
- New confirmed blocker from fresh L2 evidence:
  - `P1.6d` opened for timeout-degrade fallback hardening
  - root cause family:
    - fresh booking-start timeout degrade falls back to generic clarify instead of safe `datetime` collect when service is already known
    - ambiguous `hours + master` phrase (`Когда у вас работают мастера?`) degrades into `master` fallback due `person_action_signal`
- `P1.6d` implementation progress:
  - targeted deterministic fixes landed in `decision.py`, `pack_runtime_service.py`, and `demo_salon_knowledge.py`
  - targeted contracts green
  - invalid replay-like reruns audited and discarded:
    - `/tmp/booking_quality/p1.6-l2-dev-seed42-r7`
    - `/tmp/booking_quality/p1.6-l2-dev-seed42-r8`
    - `/tmp/booking_quality/p1.6-l2-dev-seed42-r9`
  - current rerun switched to the correct readiness path:
    - guarded fresh `dev L2` run `p1.6-l2-dev-seed42-r10`
    - entrypoint `scripts/llm_quality_guarded.sh --mode lock ... --quality-lane dev --allow-pending-previous`
    - early turns confirm the `P1.6d` target family is improved (`location/hours` strict green in dialogs `1` and `2`)
    - first new strict fail in `r10` is at `dialog_index=2 turn_index=8` for `Какого мастера вы можете предложить?`, where active booking memory still biases the answer into `pricing`
  - `r10` was stopped on explicit stop condition after the first non-`P1.6d` strict fail; audit stored at `/tmp/booking_quality/p1.6-l2-dev-seed42-r10/manual_audit.json`
  - `P1.6d` is now closed
  - new blocker TP opened:
    - `docs/TASK_PACKAGES/TP-2026-03-06-p1.6e-booking-master-query-pack-enforcement-a1.md`
  - `P1.6e` implementation progress:
    - centralized master pack enforcement landed in `decision.py`
    - stale pricing-based contract tests were rewritten
    - targeted checks are green
    - attempted cheap `count=2` forensic slice is invalid by scenario contract, so the next valid runtime step is a full guarded `dev L2 x10` on updated runtime
    - local worktree runtime spawned on `http://127.0.0.1:18190`
    - full guarded rerun started as `p1.6-l2-dev-seed42-r11`
  - `P1.6e` is now closed
  - fresh guarded `dev L2` rerun `p1.6-l2-dev-seed42-r11` exposed the next blocker at `dialog_index=1 turn_index=13`:
    - `У вас есть специалист по окрашиванию?` still returned `pricing`
    - follow-up extracted into `docs/TASK_PACKAGES/TP-2026-03-06-p1.6f-master-availability-query-ownership-a1.md`
- `P1.6f` is now closed:
  - policy-core prompt and fallback now explicitly separate service-present specialist availability from service-missing master collect
  - resolver now marks `person + service + question-like` as explicit master intent
  - targeted contracts are green
  - live policy-core probe now returns `master_query fact` for `У вас есть специалист по окрашиванию?`
- fresh guarded `dev L2` rerun `p1.6-l2-dev-seed42-r13` exposed the next blocker:
  - rejected `datetime` no longer leaks into booking state, but reused active handover still leaves live `expected_reply_type/session_memory` in runtime
  - evidence:
    - `/tmp/booking_quality/p1.6-l2-dev-seed42-r13/summary.json`
    - `/tmp/booking_quality/p1.6-l2-dev-seed42-r13/responses.jsonl`
    - `/tmp/booking_quality/p1.6-l2-dev-seed42-r13/trace_bundle.jsonl`
  - follow-up extracted into:
    - `docs/TASK_PACKAGES/TP-2026-03-06-p1.6g-reused-handover-context-capture-a1.md`
- `P1.6g` is now closed:
  - reused active handover now applies the same pending snapshot/clear contract as `escalate_to_pending`
  - boundary fix landed in `truffles-api/app/routers/webhook/decision.py`
  - targeted contracts are green:
    - `pytest -q truffles-api/tests/test_message_endpoint.py -k "reuse_active_handover_captures_pending_resume_on_reuse or reuse_active_handover_preserves_existing_pending_snapshot"` -> `2 passed`
    - `pytest -q truffles-api/tests/test_state_service.py -k "preserve_context_restores_pending_resume_snapshot"` -> `2 passed`
  - next runtime step returns to `P1.6`:
    - restart local runtime on the new code
    - run fresh guarded `dev L2`
    - if the first strict fail is new, extract the next atomic blocker TP
- fresh guarded `dev L2` rerun `p1.6-l2-dev-seed42-r14` exposed the next blocker:
  - first strict fail at `dialog_index=8 turn_index=13` on `Меня зовут Динара.`
  - failure family:
    - `booking_slot_stall`
    - `judge_fail`
  - root cause:
    - date-only booking `datetime` still counts as active/complete in policy-core memory/profile and deterministic completeness checks
    - booking prompt contract already treats that same value as incomplete, so runtime owners diverge on whether `time` is still missing
  - evidence:
    - `/tmp/booking_quality/p1.6-l2-dev-seed42-r14/summary.json`
    - `/tmp/booking_quality/p1.6-l2-dev-seed42-r14/manual_audit.json`
    - `/tmp/booking_quality/p1.6-l2-dev-seed42-r14/trace_bundle.jsonl`
  - follow-up extracted into:
    - `docs/TASK_PACKAGES/TP-2026-03-06-p1.6h-partial-datetime-slot-completeness-parity-a1.md`
- `P1.6h` is now closed:
  - policy-core prompt now states that date-only `datetime` is still missing `time`
  - policy-core memory/profile no longer marks date-only `datetime` as active
  - deterministic booking completeness checks now share the same grounded-datetime rule
  - targeted contracts are green:
    - `pytest -q truffles-api/tests/test_message_endpoint.py -k "plan_has_complete_booking_slots_requires_grounded_datetime or memory_profile_drops_date_only_datetime_from_active_slots or list_slots_keeps_context_datetime_when_expected_time"` -> `3 passed`
    - `pytest -q truffles-api/tests/test_booking_quality_response_guard.py -k "booking_slot_stall"` -> `5 passed`
    - `pytest -q truffles-api/tests/test_message_endpoint.py -k "book_slot_conflict_forces_datetime_followup or list_slots_provider_unavailable_keeps_booking_question or list_slots_drops_hallucinated_date_without_datetime_signal"` -> `3 passed`
  - next runtime step returns to `P1.6`:
    - restart local runtime on the new code
    - run fresh guarded `dev L2`
    - if the first strict fail is new, extract the next atomic blocker TP
- fresh guarded `dev L2` rerun `p1.6-l2-dev-seed42-r15` exposed the next blocker:
  - first strict fail at `dialog_index=5 turn_index=6`
  - failure family:
    - `booking_slot_stall`
    - `judge_fail`
  - root cause:
    - policy-core emitted `collect/name` while `service` was still the earliest missing booking slot
    - deterministic boundary validated token shape but not booking collect slot order
  - evidence:
    - `/tmp/booking_quality/p1.6-l2-dev-seed42-r15/summary.json`
    - `/tmp/booking_quality/p1.6-l2-dev-seed42-r15/manual_audit.json`
    - `/tmp/booking_quality/p1.6-l2-dev-seed42-r15/trace_bundle.jsonl`
  - follow-up extracted into:
    - `docs/TASK_PACKAGES/TP-2026-03-06-p1.6i-booking-collect-slot-order-invariant-a1.md`
- `P1.6i` is now closed:
  - `truffles-api/app/routers/webhook/decision.py` enforces booking collect slot order and fail-closes to earliest-slot prompt with `validation_error=collect_slot_order_invalid`
  - `pytest -q truffles-api/tests/test_message_endpoint.py -k "collect_slot_order or llm_policy_core_specialist_missing_prefers_tool_info_sections_and_name_followup"` -> `3 passed`
  - `pytest -q truffles-api/tests/test_booking_quality_response_guard.py -k "booking_slot_stall"` -> `5 passed`
- fresh guarded `dev L2` rerun `p1.6-l2-dev-seed42-r16` exposed the next blocker:
  - first strict fail at `dialog_index=8 turn_index=13`
  - failure family:
    - `expected_state_mismatch`
    - `handoff_miss`
  - root cause:
    - unresolved `booking.confirmation` still allowed policy-core runtime/degraded handoff logic to run
    - when policy-core degraded on a confirmation turn, runtime held the conversation in `pending` instead of keeping the turn bot-owned
  - evidence:
    - `/tmp/booking_quality/p1.6-l2-dev-seed42-r16/summary.json`
    - `/tmp/booking_quality/p1.6-l2-dev-seed42-r16/brief.md`
    - `/tmp/booking_quality/p1.6-l2-dev-seed42-r16/responses.jsonl`
    - `/tmp/booking_quality/p1.6-l2-dev-seed42-r16/manual_audit.json`
  - follow-up extracted into:
    - `docs/TASK_PACKAGES/TP-2026-03-06-p1.6j-booking-confirmation-priority-over-policy-core-degrade-a1.md`
- `P1.6j` is now closed:
  - `truffles-api/app/routers/webhook/decision.py` now hard-skips policy-core runtime while `booking.confirmation` is pending
  - trace/meta now record `policy_core_guard -> booking_confirmation_priority_skip`
  - targeted contracts are green:
    - `pytest -q truffles-api/tests/test_message_endpoint.py -k "booking_confirmation_pending_skips_policy_core_and_stays_bot_active or booking_confirm_requires_yes_for_llm_slot"` -> `2 passed`
    - `pytest -q truffles-api/tests/test_booking_quality_response_guard.py -k "handoff_miss or booking_slot_stall"` -> `5 passed`
- partial guarded `dev L2` rerun `p1.6-l2-dev-seed42-r17` was started on the `P1.6j` runtime and then intentionally stopped to preserve remaining LLM quality budget:
  - runtime: `http://127.0.0.1:18196`
  - audit: `/tmp/booking_quality/p1.6-l2-dev-seed42-r17/manual_audit.json`
  - snapshot:
    - `infra_valid=true`
    - `semantic_valid=false`
    - `responses_rows=28`
    - `dialogs_seen=2/10`
    - `run_integrity_reasons=["run_completion_gap"]`
  - dominant findings:
    - `pipeline_budget_exceeded`
    - `judge_oracle_alignment_gap`
    - `non_canonical_partial_dialog_execution`
- Updated execution strategy:
  - stop the expensive `P1.6` discovery loop here
  - keep rollout/promotion frozen
  - run `P1.6k demo_salon release-lane cleanup` before any new expensive lane action
  - `P1.6k` and follow-up `P1.6l` are now green on deterministic contour:
    - `pytest -q truffles-api/tests/test_pack_runtime_service.py` -> `19 passed`
    - `pytest -q truffles-api/tests/test_message_endpoint.py` -> `299 passed`
    - `pytest -q truffles-api/tests/test_demo_salon_eval.py` -> `17 passed`
  - next allowed expensive lane action was exactly one fresh guarded `demo_salon dev L2` on `http://127.0.0.1:18184`
  - first fresh post-cleanup guarded `demo_salon dev L2` (`p1.6-l2-dev-seed42-r20`) reached valid preflight and was intentionally stopped on the first new strict failure family:
    - `booking_commit_without_required_contact` at `dialog_index=1`, `turn_index=4`
    - forensic review plus local DB evidence show the appointment used valid existing profile contact, but success meta/trace did not expose enough machine-readable name/source evidence for the quality guard
    - follow-up extracted into `docs/TASK_PACKAGES/TP-2026-03-07-p3.1r-demo-salon-booking-contact-evidence-contract-a1.md`
  - `P3.1r` is now closed on deterministic contour: booking success meta/trace carries contact sources and the quality guard no longer infers a false missing-contact failure from valid profile evidence
  - exactly one fresh guarded `demo_salon dev L2` was then executed on `http://127.0.0.1:18184` as `p1.6-l2-dev-seed42-r21` with `--max-failures 1`
  - `r21` cleared the previous false-red contact blocker and stopped on the first new real strict failure family:
    - `booking_slot_stall` at `dialog_index=5`, `turn_index=13`
    - user: `Меня зовут Лена.`
    - bot: `Отлично, время подходит. Как вас зовут?`
    - question-contract still processed stale `expected_reply_type=time`, produced `answer_error=slot_mismatch`, and lost the valid `name` answer before policy-core continued
    - follow-up extracted into `docs/TASK_PACKAGES/TP-2026-03-07-p1.6m-booking-expected-reply-alternate-slot-capture-a1.md`
  - `P1.6m` is now closed on deterministic contour:
    - answer interpreter preserves `detected_slot` on `slot_mismatch`
    - question-contract captures validated alternate booking slots without clearing the still-missing requested slot
    - if stale expected-reply survives without a live `booking` object, a minimal booking context is seeded before alternate-slot persistence
    - deterministic closure:
      - `pytest -q truffles-api/tests/test_intent.py -k "AnswerInterpreterSchema or slot_mismatch_preserves_detected_slot"` -> `3 passed, 44 deselected`
      - `pytest -q truffles-api/tests/test_message_endpoint.py` -> `302 passed, 2 warnings`
      - `pytest -q truffles-api/tests/test_booking_quality_response_guard.py` -> `60 passed`
      - `pytest -q truffles-api/tests/test_demo_salon_eval.py` -> `17 passed`
      - `pytest -q truffles-api/tests/test_booking_chaos_dialogs.py` -> `1 passed`
  - `r21` audit is complete:
    - `/tmp/booking_quality/p1.6-l2-dev-seed42-r21/manual_audit.json`
    - `/tmp/booking_quality/p1.6-l2-dev-seed42-r21/manual_audit.md`
  - one fresh guarded `demo_salon dev L2` was then executed on the refreshed `http://127.0.0.1:18184` runtime as `p1.6-l2-dev-seed42-r22`
  - `r22` stopped on the first new real strict failure family:
    - `expected_info_section_miss`
    - `info_section_miss`
    - failing turn: `dialog_index=4`, `turn_index=4`
    - user: `Можно ли записаться на эту услугу на выходные?`
    - runtime lost the current service referent, emitted `booking_prompt`, set `expected_reply_type=service_choice`, and attached `consult_return` instead of answering `hours/schedule`
    - follow-up extracted into `docs/TASK_PACKAGES/TP-2026-03-07-p1.6n-weekend-availability-service-referent-arbitration-a1.md`
  - `r22` audit is complete:
    - `/tmp/booking_quality/p1.6-l2-dev-seed42-r22/manual_audit.json`
    - `/tmp/booking_quality/p1.6-l2-dev-seed42-r22/manual_audit.md`
  - next allowed expensive lane action is now blocked by `P1.6o demo_salon architecture closure program`; `P1.6n` remains the first open child family inside that program and no new expensive rerun is allowed before the active child slice closes on deterministic contour
  - resume canonical `lock/replay` only after that next fresh `dev L2` is green and the next acceptance budget window is explicitly reopened
- Remaining runtime observations to validate on the next fresh lock attempt:
  - repeated multi-intent timeout pressure on default realism budget (`1.2s` -> `2.4s`)
  - repeated `pipeline_budget_exceeded` turns in fresh L2 evidence
  - judge/contract conflict review remains advisory and must not override contract-first arbitration

## Evidence

1. Deterministic suite logs.
   - `pytest -q truffles-api/tests/test_message_endpoint.py`
2. Lock run directory:
   - `summary.json`
   - `brief.md`
   - `responses.jsonl`
   - `trace_bundle.jsonl`
   - `run_manifest.json`
3. Strict audit outputs:
   - `manual_audit.md`
   - `manual_audit.json`
4. Exact replay command for the next block.

## Rollback

1. No runtime rollback required for doc-only planning.
2. If a lock run is invalid, discard it as evidence and do not update baseline/chain.

## No-go

- No replay against pre-P1 lock artifacts.
- No baseline promotion from an invalid or incomplete run.
- No canary/full before P1.7 is closed.

## Risks/Blockers

1. Preflight may fail on env/judge/runtime prerequisites.
2. Old acceptance artifacts may still contain process-trust inconsistencies and must not be reused blindly.
3. `PG checklist` cannot be assembled while `p1.6-l2-dev-seed42-r6` remains semantically invalid.
4. `P1.6` remains budget-blocked after `r17`; no new expensive quality run starts by default until a later acceptance window is explicitly reopened.
5. Multi-intent timeout pressure (`1.2s` -> `2.4s`) is now a live blocker, not just an observation.
6. Booking interrupt/reschedule contract drift remains a separate blocker family after timeout fallback hardening.
7. Even a green `P1.6` lock proves only corrected single-owner baseline on the current acceptance canary; multi-business, branch-capability, multilingual/code-switch proof remains a separate post-P1 obligation.

## Residual architecture debt (mandatory)

- Current residuals accepted in this block:
  - `P1.6c` readiness envelope completion.
  - `P1.7` replay regression gate.
  - Program-level blocks after P1 (`P3`, `P4`, `P6`).
  - fresh post-`P1.6m` guarded `dev L2` evidence `r22` is partial forensic evidence only and exposed the next runtime blocker family (`P1.6n`); it is still not canonical lock evidence.
  - multi-business / multilingual / code-switch proof remains deferred to post-P1 open-world validation.
- Why not in this block:
  - Atomic closure discipline; lock must exist before replay can be meaningful.
- Risk if deferred:
  - Without replay, P1 still lacks anti-drift confirmation against the fresh single-owner baseline.
  - Without a later acceptance window, P1.6 cannot produce a new canonical lock.
- Linked follow-up Task Package(s):
  - `docs/TASK_PACKAGES/TP-2026-03-06-p1.6c-pre-lock-readiness-envelope-a1.md`
  - `docs/TASK_PACKAGES/TP-2026-03-06-p1.6i-booking-collect-slot-order-invariant-a1.md`
  - `docs/TASK_PACKAGES/TP-2026-03-06-p1.6j-booking-confirmation-priority-over-policy-core-degrade-a1.md`
  - `docs/TASK_PACKAGES/TP-2026-03-06-p3.1-book-slot-contact-minimum-fail-closed-a1.md`
  - `docs/TASK_PACKAGES/TP-2026-03-07-p1.6m-booking-expected-reply-alternate-slot-capture-a1.md`
  - `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
  - `docs/TASK_PACKAGES/TP-2026-03-07-p1.6n-weekend-availability-service-referent-arbitration-a1.md`
  - `P1.7` sub-TP.
- Expiry/trigger to stop deferral:
  - Before any `lock/replay/canary/full`.

## Next-block contract (mandatory)

- Next block objective:
  - close `P1.6o16` on deterministic contour, restore guarded transport/runtime stability, then run one fresh post-`P1.6o16` guarded `dev L2` inside `P1.6o6` before any `lock/replay`
- First deterministic check command:
  - `pytest -q truffles-api/tests/test_message_endpoint.py::test_llm_policy_core_time_guidance_question_under_expected_time_keeps_progress_contract && pytest -q truffles-api/tests/test_booking_quality_response_guard.py::test_evaluate_turn_allows_booking_time_guidance_reply_without_slot_progress_stall`
- Blocked-by conditions:
  - refreshed post-`P1.6o16` deterministic contour not green
  - `P1.6o16` opening or audited `r11` blocker not recorded in docs
  - runtime `http://127.0.0.1:18184` not refreshed to the current worktree state after the code delta
  - transport remains infra-invalid for guarded `dev L2`
  - missing guarded `dev L2` budget window / run-id / `PG checklist` for the next post-fix run
- Owner role for closure:
  - `Top Architect + Brain`

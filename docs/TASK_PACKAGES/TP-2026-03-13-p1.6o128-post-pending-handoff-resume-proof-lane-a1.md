# TP-2026-03-13-p1.6o128-post-pending-handoff-resume-proof-lane-a1

- Название/цель: после deterministic closure `P1.6o127` выполнить один fresh guarded `dev L2`, чтобы получить admissible proof evidence и truthfully понять, ушёл ли pending/handoff runtime-owner family из first-fail позиции на каноничном rerun.

## Canon refs

- Parent closure program: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Main remediation: `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- Process reset / class triage: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o118-class-level-closure-process-reset-a1.md`
- Prior proof lane: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o126-post-tool-evidence-proof-lane-a1.md`
- Prior structural slice: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o127-pending-handoff-resume-boundary-first-slice-a1.md`
- State baseline: `STATE.md`

## Git context

- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `origin/main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect` remove branch + worktree after merge

## One web search (mandatory before implementation)

- Query: `site:www.uvicorn.org/settings uvicorn command line host port settings admin version`
- Date/time: `2026-03-13T13:05:00+05:00`
- Sources opened:
  - official Uvicorn settings docs: `https://www.uvicorn.org/settings/`
- Source quality:
  - official product documentation
- Found reusable ideas:
  - proof runtime startup should stay explicit and reproducible through CLI flags;
  - host/port launch parameters should be recorded before the guarded run starts so runtime parity is auditable.
- Decision:
  - `reuse`
  - reuse the established live proof-run startup pattern on `:18184` and record `/admin/version == HEAD` before the single guarded rerun.
- Rejected options:
  - detached startup patterns that hide runtime parity;
  - multiple reruns in the same proof block;
  - routing from deterministic `P1.6o127` alone without fresh proof evidence.

## Root cause (mandatory)

- Symptom:
  - `P1.6o127` is green on deterministic contour, but the worktree still lacks one fresh admissible proof rerun to confirm whether the surfaced pending/handoff runtime-owner family is gone from first fail on canonical evidence.
- Minimal reproduction:
  1. Open `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o127-pending-handoff-resume-boundary-first-slice-a1.md`.
  2. Confirm the bounded runtime-owner slice preserves booking follow-up continuity across pending/handoff soft-pass on deterministic contour.
  3. Start a live runtime whose `/admin/version` matches current `HEAD`.
  4. Run exactly one guarded `dev L2` proof lane and strict audit it.
  5. Truthfully classify the first surfaced result under `P1.6o118` rules.
- Evidence:
  - `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o127-pending-handoff-resume-boundary-first-slice-a1.md`
  - `truffles-api/tests/test_message_endpoint.py`
  - `truffles-api/tests/test_booking_quality_response_guard.py`
- Five Whys:
  1. Why is closure still incomplete after `P1.6o127`? Because deterministic green alone does not prove first-fail displacement on the guarded proof lane.
  2. Why can the worktree rerun now? Because the bounded runtime-owner slice is deterministically closed.
  3. Why must runtime be fingerprinted first? Because proof evidence is admissible only if the live runtime matches current `HEAD`.
  4. Why only one rerun? Because the goal is truthful class admission, not another expensive replay loop.
  5. Why does class triage still matter after a green structural slice? Because the rerun can surface a different already-known class, and continuation must follow class-level routing rather than row-local reflex.
- Root cause statement:
  - after deterministic `P1.6o127` closure, the remaining gap is proof admissibility: one fresh fingerprint-verified guarded rerun is needed to determine whether the pending/handoff runtime-owner family is actually gone from first-fail position.
- Fix mechanism:
  - start a live worktree runtime on `:18184` and prove `/admin/version.git_commit == HEAD`;
  - execute exactly one guarded `dev L2` rerun and strict audit it;
  - route the surfaced result by `P1.6o118` class triage instead of automatic row admission.

## Reuse-first decision (mandatory)

- Reuse:
  - existing guarded entrypoint `scripts/llm_quality_guarded.sh`
  - existing audit command `python3 ops/diagnose.py llm-quality-audit`
  - existing live runtime startup pattern used in prior proof-lane blocks
- Integrate:
  - no runtime/scenario/oracle code changes unless the rerun surfaces a new deterministic or bounded family
- Build:
  - none planned in this block

## Invariant

1. `P1.6o126` remains the canonical class-routing proof artifact for the pending/handoff runtime-owner family.
2. `P1.6o127` deterministic closure truth stays intact.
3. No new row-local `Mxx -> child TP` admission happens without `P1.6o118` class triage.
4. Proof evidence is accepted only from the fresh fingerprint-verified rerun and strict audit artifacts.

## Scope

- Refresh the live worktree runtime on `127.0.0.1:18184`.
- Verify `/admin/version.git_commit == HEAD`.
- Execute exactly one guarded `dev L2` proof rerun after `P1.6o127`.
- Strictly audit the run.
- Truthfully sync docs/state/session and publish the next admissible structural block only if the surfaced class requires it.

## Out of scope

- new runtime/scenario/oracle code changes before the proof rerun surfaces a new class
- acceptance `lock/replay/full`
- new matrix-row admission without class-level routing

## Touch-list

- `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o128-post-pending-handoff-resume-proof-lane-a1.md`
- `STATE.md`
- `STRUCTURE.md`
- `docs/SESSIONS/SESSION-2026-02-19-llm-first-firebreak-a1.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `/tmp/booking_quality/p1.6o128-l2-dev-20260313-a1-r1/**`

## Plan

1. Sync deterministic `P1.6o127` closure truth into the active docs.
2. Start a live worktree runtime on `127.0.0.1:18184` and prove `/admin/version.git_commit == HEAD`.
3. Run exactly one guarded `dev L2` proof lane as `p1.6o128-l2-dev-20260313-a1-r1`.
4. Strictly audit the run and inspect the first fail.
5. Classify the surfaced result using `P1.6o118` rules.
6. Sync truth into docs/state/session and publish the next admissible block only if the surfaced class requires it.

## DoD

1. Exactly one fresh guarded `dev L2` run is executed after deterministic closure of `P1.6o127`.
2. Live runtime fingerprint parity with current `HEAD` is recorded before the run.
3. Strict audit completes for `/tmp/booking_quality/p1.6o128-l2-dev-20260313-a1-r1`.
4. Active docs/state/session record whether the pending/handoff runtime-owner family remains first fail on canonical proof evidence.
5. Any surfaced next step is routed by class-level triage, not by automatic row admission.

## Checks

- `git rev-parse HEAD`
- `curl -sf http://127.0.0.1:18184/admin/version`
- `TEST_MODE=1 scripts/llm_quality_guarded.sh --mode lock --run-id p1.6o128-l2-dev-20260313-a1-r1 --allow-pending-previous --allow-repeat-fingerprint -- --base-url http://127.0.0.1:18184 --client-slug demo_salon --mode llm --count 10 --min-turns 10 --max-turns 15 --include-media --scenario-coverage booking,info,interrupt,handoff --tool-hooks auto --jid-mode unique --allow-non-allowlist --judge-mode all --quality-lane dev --run-economy-gate block --fail-on-thresholds --max-failures 1 --reset-before-dialog`
- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o128-l2-dev-20260313-a1-r1 --status done --strict-artifacts`
- `git diff --check`
- `SESSION_AGENT=a1 scripts/session_check.sh`

## Execution status

- `done`
- `P1.6o128` completed with one fresh guarded `dev L2` `p1.6o128-l2-dev-20260313-a1-r1` on fingerprint-verified live runtime, proved the covered pending/handoff runtime-owner family is no longer first fail, and truthfully surfaced `Track A` scenario-governance/oracle drift on `dialog 1 / turn 5 / "Я предпочитаю Айгерим, она свободна?"`, where runtime emits generalized specialist followup but scenario expectations stayed stale on requested-slot meta/trace.

## Evidence

1. `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o127-pending-handoff-resume-boundary-first-slice-a1.md`
2. `truffles-api/tests/test_message_endpoint.py`
3. `truffles-api/tests/test_booking_quality_response_guard.py`

## Rollback

1. Stop the local runtime and keep `P1.6o127` deterministic closure intact.
2. If the rerun is invalid/incomplete, record it truthfully and do not reinterpret `P1.6o127`.
3. Do not mutate the matrix or docs to hide surfaced evidence.

## No-go

- no second expensive run in this block
- no new implementation before the proof rerun surfaces the next class
- no reuse of deterministic `P1.6o127` alone as closure proof
- no automatic `Mxx -> child TP` admission without `P1.6o118` class triage

## Risks / blockers

- local runtime may fail fingerprint validation or die before the guarded rerun starts
- the rerun may still stay semantic-red because of inherited `hardcode_core_violation` even if the pending/handoff family is gone
- the next surfaced result may expose broader owner-resolver or scenario-governance debt, which must open a structural continuation rather than a local patch

## Residual architecture debt (mandatory)

### Current residuals accepted in this block

- surfaced semantic family after `P1.6o127` remains unknown until a fresh proof rerun confirms or displaces it
- inherited `hardcode_core_violation` remains outside this proof-only block
- acceptance proof (`lock/replay/full`) remains blocked

### Why not in this block

- this block is proof-only; it determines the next remaining class on admissible evidence

### Risk if deferred

- the team can keep routing from deterministic closure alone instead of fresh proof evidence

### Linked follow-up Task Package(s)

- one structural continuation selected by `P1.6o118` after truthful `P1.6o128` class triage
- later hardcode-core cleanup block if the inherited gate remains active on the selected lane

### Expiry/trigger to stop deferral

- before any further guarded `dev/acceptance` rerun beyond `P1.6o128`

## Next-block contract (mandatory)

### Next block objective

- truthfully sync the surfaced `P1.6o128` result and land bounded `Track A` continuation `P1.6o129` so active-time named specialist preference availability turns converge to canonical specialist-followup expectations before any further expensive rerun.

### First deterministic check command

- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o128-l2-dev-20260313-a1-r1 --status done --strict-artifacts && python3 -m py_compile scripts/booking_dialog_scenarios.py truffles-api/tests/test_booking_dialog_scenarios_script.py truffles-api/tests/test_booking_quality_response_guard.py && git diff --check`

### Blocked-by conditions

- deterministic `P1.6o127` closure is not reflected truthfully in active docs/state/session
- the surfaced runtime result is misread as runtime-owner reopen instead of `Track A` scenario-governance drift
- the team attempts another expensive rerun before processing the surfaced `P1.6o128` result through bounded `P1.6o129`

### Owner role for closure

- `Top Architect + Brain`

# TP-2026-03-12-p1.6o111-active-time-partial-date-fill-timeout-degraded-collect-governance-a1

- Название/цель: закрыть surfaced `M40`, где fresh proof lane `P1.6o110` показал scenario-governance drift на фразе `Могу прийти в пятницу.` под активным `expected_reply_type=time`: runtime канонически groundит `datetime`, затем при `policy_core` timeout легально резюмирует missing slot `name`, но generated scenario всё ещё держит stale `reply_type=time` / `expected_reply_type=time`.

## Canon refs

- Parent closure program: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Main remediation: `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- Prior proof lane: `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o110-post-m39-proof-lane-a1.md`
- Related bounded rows: `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o64-timeout-mixed-date-availability-governance-a1.md`, `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o103-grounded-partial-date-daypart-fill-governance-a1.md`
- Owner matrix artifact: `truffles-api/app/knowledge/generic/INTERACTION_OWNER_MATRIX.yaml`
- State baseline: `STATE.md`

## Git context

- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect` remove branch + worktree after merge

## One web search (mandatory before implementation)

- Query: `Rasa forms requested slot next slot official docs`
- Date/time: `2026-03-12T20:31:00+05:00`
- Sources opened:
  - official Rasa forms docs: `https://rasa.com/docs/rasa/forms/`
- Source quality:
  - official product documentation
- Found reusable ideas:
  - once a requested slot is filled, the dialog should move to the next missing slot instead of keeping the previous requested slot active;
  - slot-filling governance should be expressed as contract/state progression, not surface-text matching.
- Decision:
  - `reuse + integrate`
  - reuse the existing active-`time` fill normalization pattern and integrate one bounded scenario-governance rule for pure partial-date fills under active `time` collect.
- Rejected options:
  - weakening the oracle to accept stale `time` expectations after grounded partial-date fill;
  - runtime changes for a row that is already canonical in `responses.jsonl` / `trace_bundle.jsonl`.

## Root cause (mandatory)

- Symptom:
  - fresh guarded `dev L2` `p1.6o110-l2-dev-20260312-a1-r1` stops at dialog `1`, turn `3`, user text `Могу прийти в пятницу.`.
- Minimal reproduction:
  1. Inspect `/tmp/booking_quality/p1.6o110-l2-dev-20260312-a1-r1/scenarios.json` dialog `1`, turn `3` and confirm the turn expects `reply_type=time` and `meta_any.expected_reply_type=time`.
  2. Inspect `/tmp/booking_quality/p1.6o110-l2-dev-20260312-a1-r1/responses.jsonl` for `message_id=LLM-QUAL-p1.6o110-l2-dev-20260312-a1-r1-001-03-cfc414`.
  3. Confirm runtime deterministically matches `answer_slot=datetime`, `answer_value=в пятницу`, then `policy_core_guard` emits `timeout_booking_collect` / `degraded_collect` and final `question_contract` resumes `expected_reply_type=name`.
  4. Compare with existing exact-time and grounded-partial-date normalization helpers in `scripts/booking_dialog_scenarios.py`.
  5. Confirm there is no bounded normalization for pure partial-date fills already tagged as `time` under active `time` collect.
- Evidence:
  - `/tmp/booking_quality/p1.6o110-l2-dev-20260312-a1-r1/summary.json`
  - `/tmp/booking_quality/p1.6o110-l2-dev-20260312-a1-r1/brief.md`
  - `/tmp/booking_quality/p1.6o110-l2-dev-20260312-a1-r1/scenarios.json`
  - `/tmp/booking_quality/p1.6o110-l2-dev-20260312-a1-r1/responses.jsonl`
  - `/tmp/booking_quality/p1.6o110-l2-dev-20260312-a1-r1/trace_bundle.jsonl`
  - `/tmp/booking_quality/p1.6o110-l2-dev-20260312-a1-r1/manual_audit.md`
- Five Whys:
  1. Why did the proof lane fail? Because scenario meta expected `time`, while runtime truthfully ended on `name`.
  2. Why did scenario expect `time`? Because pure partial-date fills under active `time` collect are not normalized the same way as exact-time fills.
  3. Why is runtime already canonical? Because the turn grounds `datetime` and timeout-degraded collect legitimately resumes the next missing booking slot `name`.
  4. Why doesn’t existing bounded normalization cover it? Because current helpers cover `slot_constraint`, `mixed_fill_plus_question`, exact-time fills, and grounded partial-date daypart fills, but not direct `time`-tagged partial-date answers.
  5. Why is this scenario-governance, not runtime? Because `responses.jsonl` and `trace_bundle.jsonl` already show canonical `time -> name` transition with explicit timeout provenance.
- Root cause statement:
  - scenario generation lacks a bounded rule that converts pure partial-date `time` answers under active `time` collect into the canonical post-grounding `name` collect, so the proof lane keeps a stale `time` expectation even though runtime has already advanced correctly.
- Fix mechanism:
  - freeze surfaced row `M40` in the machine-readable owner matrix;
  - add one bounded active-`time` partial-date-fill expectation rewrite in sanitize and post-coverage repair flows;
  - add deterministic positive regressions for sanitize and repair;
  - publish the next proof-lane block only after deterministic closure is green.

## Reuse-first decision (mandatory)

- Reuse:
  - existing `_looks_like_partial_date_fill_without_availability_query(...)`
  - existing exact-time `time -> name` expectation rewrite
  - existing post-coverage orphan repair flow
- Integrate:
  - one bounded expectation rewrite for pure partial-date `time` fills under active `time` collect
  - deterministic scenario + matrix regressions
- Do not build:
  - new runtime owner branch
  - new generator taxonomy family outside the bounded `M40` row

## Execution profile

- TP mode: `implementation`
- Doc touch budget (files): `11`
- Code dominance: `required`
- Override token: `none`
- Why this profile fits: `r126` surfaced a bounded scenario-governance drift adjacent to already-closed time-fill rows, so reuse exists and only the generated contract needs alignment.

## Release safety

- Strategy: bounded scenario-governance normalization only
- Go signals:
  - pure partial-date fills such as `Могу прийти в пятницу.` under active `time` collect now canonicalize to `reply_type=name`
  - stale `pending_question_act/target` is scrubbed from scenario expectations after grounded fill
  - adjacent exact-time and grounded partial-date daypart rows remain green
- No-go signals:
  - ungrounded partial-date questions lose truthful `time` expectations
  - active `time` turns without partial-date fill start advancing to `name`
  - deterministic matrix or scenario regressions fail
- Rollback verification:
  - revert only the bounded `M40` scenario-governance delta if adjacent time-fill families regress

## Invariant

1. Runtime behavior for `M39` and all prior rows remains unchanged.
2. Exact-time fill normalization remains green.
3. Grounded partial-date daypart normalization remains green.
4. No guarded rerun in this block.

## Scope

- publish surfaced row `M40`
- add bounded active-time partial-date-fill expectation rewrite in sanitize flow
- add the same bounded rewrite in post-coverage repair flow
- add targeted scenario + matrix regressions
- sync active docs/state/session and publish the next proof-lane block ID

## Out of scope

- runtime code changes
- fresh guarded `dev L2`
- acceptance `lock/replay/full`

## Touch-list

- `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o110-post-m39-proof-lane-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o111-active-time-partial-date-fill-timeout-degraded-collect-governance-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o112-post-m40-proof-lane-a1.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `STATE.md`
- `STRUCTURE.md`
- `docs/SESSIONS/SESSION-2026-02-19-llm-first-firebreak-a1.md`
- `truffles-api/app/knowledge/generic/INTERACTION_OWNER_MATRIX.yaml`
- `scripts/booking_dialog_scenarios.py`
- `truffles-api/tests/test_booking_dialog_scenarios_script.py`
- `truffles-api/tests/test_interaction_owner_matrix_contract.py`

## Plan (1..N)

1. Freeze `r126` as audited proof evidence and publish `M40`.
2. Add bounded `time -> name` expectation rewrite for pure partial-date fills under active `time` collect.
3. Add targeted sanitize/repair + matrix regressions.
4. Run focused checks and `git diff --check`.
5. Publish the next proof-lane block ID only after deterministic closure is green.

## DoD

1. `r126` is recorded truthfully as surfaced `M40` in active docs and the owner matrix.
2. `Могу прийти в пятницу.` under active `time` collect no longer keeps stale `reply_type=time` in sanitize or post-coverage repair flows.
3. Targeted deterministic scenario + matrix regressions are green.
4. The next admissible block is the fresh proof lane `P1.6o112`.

## Expensive run control (mandatory)

- Hypothesis:
  - after bounded `M40` scenario-governance closure, a fresh guarded `dev L2` will no longer stop on dialog `1`, turn `3`, `Могу прийти в пятницу.`
- Expected measurable effect:
  - targeted deterministic checks turn green, and the next guarded proof lane either moves past this row or surfaces a different first-fail family
- Stop condition:
  - no guarded run inside this block; stop after deterministic closure and publish the next proof TP ID

## Checks

- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o110-l2-dev-20260312-a1-r1 --status done --strict-artifacts`
- `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "partial_date_fill or grounded_partial_date_daypart_fill or exact_time_fill_collect"` -> `4 passed, 101 deselected`
- `pytest -q truffles-api/tests/test_interaction_owner_matrix_contract.py` -> `2 passed`
- `python3 -m py_compile scripts/booking_dialog_scenarios.py truffles-api/tests/test_booking_dialog_scenarios_script.py truffles-api/tests/test_interaction_owner_matrix_contract.py` -> `pass`
- `git diff --check` -> `pass`
- `SESSION_AGENT=a1 scripts/session_check.sh` -> `Session OK`

## Execution status

- `done`
- active-time pure partial-date fills now canonicalize to `name` collect in both sanitize and post-coverage repair flows
- `M40` is closed on deterministic contour and the next admissible block is the fresh proof lane `P1.6o112`

## Evidence

1. `/tmp/booking_quality/p1.6o110-l2-dev-20260312-a1-r1/summary.json`
2. `/tmp/booking_quality/p1.6o110-l2-dev-20260312-a1-r1/brief.md`
3. `/tmp/booking_quality/p1.6o110-l2-dev-20260312-a1-r1/scenarios.json`
4. `/tmp/booking_quality/p1.6o110-l2-dev-20260312-a1-r1/responses.jsonl`
5. `/tmp/booking_quality/p1.6o110-l2-dev-20260312-a1-r1/trace_bundle.jsonl`
6. `/tmp/booking_quality/p1.6o110-l2-dev-20260312-a1-r1/manual_audit.md`
7. `scripts/booking_dialog_scenarios.py`
8. `truffles-api/tests/test_booking_dialog_scenarios_script.py`
9. `truffles-api/app/knowledge/generic/INTERACTION_OWNER_MATRIX.yaml`
10. `truffles-api/tests/test_interaction_owner_matrix_contract.py`
11. `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o112-post-m40-proof-lane-a1.md`

## Rollback

1. Keep `P1.6o110` proof truth intact.
2. Revert only the bounded `M40` scenario-governance delta and targeted regressions if adjacent time-fill families regress.
3. Do not mutate the matrix to hide surfaced evidence.

## No-go

- no runtime owner patch for a row already canonical in runtime evidence
- no weakening of oracle to accept stale `time` after grounded partial-date fill
- no second expensive run before deterministic closure is green

## Risks / blockers

- partial-date normalization can overmatch question-like availability turns if the predicate becomes too broad
- post-coverage repair can silently rewrite unrelated `time` turns if active context handling regresses
- proof remains non-canonical until unrelated infra/tool-evidence red is separately closed

## Residual architecture debt (mandatory)

### Current residuals accepted in this block

- scenario-governance still duplicates part of the runtime transition truth instead of being generated directly from the executable owner matrix
- closure proof remains on `dev L2`; `lock/replay/full` stay blocked

### Why not in this block

- this block only closes surfaced row `M40`

### Risk if deferred

- active-time partial-date fills keep stopping the proof lane on stale scenario expectations and mask later remaining families

### Linked follow-up Task Package(s)

- `P1.6o112` post-`M40` proof lane
- `P1.7` acceptance replay gate after the closure lane is green enough to advance

### Expiry/trigger to stop deferral

- before any further guarded `dev/acceptance` rerun beyond `P1.6o112`

## Next-block contract (mandatory)

### Next block objective

- publish and run `P1.6o112`: prove on one fresh guarded `dev L2` that `M40` is gone from the first-fail position and truthfully admit the next family, if any.

### First deterministic check command

- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o110-l2-dev-20260312-a1-r1 --status done --strict-artifacts && pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "partial_date_fill or grounded_partial_date_daypart_fill or exact_time_fill_collect" && pytest -q truffles-api/tests/test_interaction_owner_matrix_contract.py && git diff --check`

### Blocked-by conditions

- `P1.6o110` proof truth and `M40` matrix admission are not synced into active docs/state
- bounded partial-date deterministic evidence is not green
- `P1.6o112` is not published as the next proof block

### Owner role for closure

- `Top Architect + Brain`

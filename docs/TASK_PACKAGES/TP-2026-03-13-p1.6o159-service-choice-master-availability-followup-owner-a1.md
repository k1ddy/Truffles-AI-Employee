# TP-2026-03-13-p1.6o159-service-choice-master-availability-followup-owner-a1

- Название/цель: закрыть surfaced `P1.6o158` runtime owner miss, где service-choice continuity + live specialist availability question с partial date (`"Какой мастер будет делать маникюр в субботу?"`) схлопывается в generic `booking_prompt/time` без `master` followup evidence, вместо использования существующего specialist-availability followup path.

## Canon refs

- Parent closure program: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Main remediation: `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- Process reset / class triage: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o118-class-level-closure-process-reset-a1.md`
- Prior proof lane: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o158-post-p1.6o157-proof-lane-a1.md`
- Prior proof-validity slice: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o157-provider-unavailable-calendar-hook-proof-validity-a1.md`
- State baseline: `STATE.md`

## Git context

- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `origin/main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect` remove branch + worktree after merge

## One web search (mandatory before implementation)

- Query: `site:stately.ai/docs guards context state machines`
- Date/time: `2026-03-13T19:50:47+05:00`
- Sources opened:
  - Stately docs — Guards: `https://stately.ai/docs/guards`
- Source quality:
  - official product documentation
- Found reusable ideas:
  - guarded transitions should be pure, explicit conditions over state/context;
  - context data should drive transitions rather than local branch sprawl.
- Decision:
  - `reuse`
  - reuse the guard/context principle for one bounded owner-preservation helper that selects the existing specialist-availability followup path from policy contract state, instead of introducing a new branchy semantic rewrite.
- Rejected options:
  - loosening scenario/oracle to accept the surfaced generic `booking_prompt` as success;
  - hardcoding phrase rules in `llm_policy_core`;
  - opening another proof-only block while the surfaced runtime owner miss is already on admissible evidence.

## Root cause (mandatory)

- Symptom:
  - `P1.6o158` is infra-valid and surfaces `dialog 2 / turn 8 / "Какой мастер будет делать маникюр в субботу?"`, where runtime ends with generic `booking_prompt/time` and no `master/specialist` followup evidence, while scenario and judge both expect a specialist-availability answer/followup contour.
- Minimal reproduction:
  1. Open `/tmp/booking_quality/p1.6o158-l2-dev-20260313-a1-r1/responses.jsonl` and inspect message `LLM-QUAL-p1.6o158-l2-dev-20260313-a1-r1-002-08-74566e`.
  2. Confirm `llm_policy_core` emitted `goal=info`, `action=collect`, `subject_kind=specialist`, `capability=live_availability`, `resolution_mode=clarify_missing_time`, `pending_question_target=specialist`, `active_question_relation=ask_about_requested_slot`, and `next_question=datetime`.
  3. Confirm runtime kept `source=llm_policy_core`, `action=booking_prompt`, `expected_reply_type=time`, but did not stamp `pending_question_interaction=specialist_availability_followup` nor `info_sections=master`.
  4. Inspect `_should_preserve_specialist_availability_followup_owner()` and confirm it only admits the existing followup path when `policy_goal == booking` and `active_question_relation == specialist_availability_followup`, so the surfaced `goal=info + clarify_missing_time + ask_about_requested_slot` envelope falls through to generic prompt handling.
- Evidence:
  - `/tmp/booking_quality/p1.6o158-l2-dev-20260313-a1-r1/summary.json`
  - `/tmp/booking_quality/p1.6o158-l2-dev-20260313-a1-r1/responses.jsonl`
  - `/tmp/booking_quality/p1.6o158-l2-dev-20260313-a1-r1/trace_bundle.jsonl`
  - `truffles-api/app/routers/webhook/decision.py`
  - `truffles-api/tests/test_message_endpoint.py`
- Five Whys:
  1. Why did the turn fail? Because runtime did not answer/acknowledge the specialist availability question.
  2. Why did runtime miss it? Because it fell through to generic `booking_prompt/time`.
  3. Why did it fall through? Because the existing specialist-availability owner helper rejected the policy envelope.
  4. Why did the helper reject it? Because it only preserves `booking + specialist_availability_followup`, not the surfaced `info + live_availability + clarify_missing_time + ask_about_requested_slot` envelope that still semantically targets specialist availability.
  5. Why is this architectural debt? Because the reusable followup path already exists, but owner coverage is too narrow and still keyed to one local subset of the policy contract.
- Root cause statement:
  - runtime owner coverage for specialist availability is narrower than the policy contract: the existing followup path does not admit the surfaced service-choice/live-availability `clarify_missing_time` envelope, so the turn degrades to generic booking prompt and loses `master` followup evidence.
- Fix mechanism:
  - add one bounded owner-preservation helper for the surfaced service-choice/live-availability envelope and route it into the existing specialist-availability followup response/meta/trace path;
  - add a regression test for the surfaced turn and keep adjacent specialist-followup families green.

## Reuse-first decision (mandatory)

- Reuse:
  - existing `_build_specialist_availability_followup_response()`
  - existing specialist-availability followup trace/meta path in `decision.py`
  - existing message-endpoint tests for specialist availability followup and grounded transition
- Integrate:
  - extend owner-preservation coverage with one bounded helper; do not invent a new response family
- Build:
  - no new framework or dependency

## Invariant

1. No phrase-hardcode is introduced into `llm_policy_core`.
2. Existing grounded specialist-availability and active-time followup paths remain unchanged outside the surfaced envelope.
3. Scenario/oracle are not weakened to accept the current generic `booking_prompt` as success.
4. Deterministic boundaries still preserve contract-first trace/meta evidence.

## Scope

- `truffles-api/app/routers/webhook/decision.py`
- `truffles-api/tests/test_message_endpoint.py`
- truth sync docs for `P1.6o158` / `P1.6o159`

## Out of scope

- scenario/oracle compiler changes
- proof-lane rerun itself
- broad specialist-availability redesign outside the surfaced envelope

## Touch-list

- `truffles-api/app/routers/webhook/decision.py`
- `truffles-api/tests/test_message_endpoint.py`
- `STATE.md`
- `docs/SESSIONS/SESSION-2026-02-19-llm-first-firebreak-a1.md`
- `docs/SESSION_INDEX.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o158-post-p1.6o157-proof-lane-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o159-service-choice-master-availability-followup-owner-a1.md`

## Plan

1. Freeze `P1.6o158` truth and publish `P1.6o159`.
2. Add one bounded owner helper for the surfaced service-choice/live-availability clarify-missing-time envelope.
3. Route that envelope into the existing specialist-availability followup response/meta/trace path.
4. Add a regression test for the surfaced turn plus adjacent specialist-availability checks.
5. Run targeted deterministic checks and sync truth.
6. Publish the next proof lane after deterministic closure.

## DoD

1. The surfaced `dialog 2 / turn 8` envelope no longer falls through to generic `booking_prompt/time` without master followup evidence.
2. Runtime reuses the existing specialist-availability followup response/meta/trace path on the bounded envelope.
3. Adjacent specialist-availability regressions remain green.
4. Active docs/state/session reflect that `P1.6o158` surfaced a runtime owner miss and `P1.6o159` is the selected continuation.

## Checks

- `python3 -m py_compile truffles-api/app/routers/webhook/decision.py truffles-api/tests/test_message_endpoint.py`
- `pytest -q truffles-api/tests/test_message_endpoint.py -k "specialist_availability_followup_keeps_time_collect or grounded_specialist_availability_transitions_to_name_collect or service_choice_master_availability_followup_keeps_time_collect"`
- `git diff --check`
- `SESSION_AGENT=a1 scripts/session_check.sh`

## Evidence

1. `/tmp/booking_quality/p1.6o158-l2-dev-20260313-a1-r1/summary.json`
2. `/tmp/booking_quality/p1.6o158-l2-dev-20260313-a1-r1/responses.jsonl`
3. `/tmp/booking_quality/p1.6o158-l2-dev-20260313-a1-r1/trace_bundle.jsonl`
4. `truffles-api/app/routers/webhook/decision.py`
5. `truffles-api/tests/test_message_endpoint.py`

## Rollback

1. Revert the bounded owner helper and its regression test.
2. Keep `P1.6o158` truth frozen as the surfaced runtime owner miss.
3. Do not reinterpret `P1.6o158` as scenario-only drift without new evidence.

## No-go

- no scenario/oracle weakening to green the run
- no hardcoded phrase routing inside `llm_policy_core`
- no broad refactor beyond the surfaced owner envelope
- no proof rerun before deterministic closure

## Risks / blockers

- over-matching the new helper could absorb unrelated `info` turns that should stay generic
- under-scoping the helper could leave the surfaced turn on the same generic prompt path
- additional latent runtime families may appear once this owner miss moves off first fail

## Residual architecture debt (mandatory)

### Current residuals accepted in this block

- scenario/oracle duplication still exists outside the surfaced envelope
- proof bundle closure remains incomplete
- specialist-availability owner coverage is still not globally matrix-driven

### Why not in this block

- this block is a bounded runtime-owner continuation only

### Risk if deferred

- the worktree will keep treating a real admissible runtime owner miss as unresolved while bouncing between proof lanes

### Linked follow-up Task Package(s)

- next proof-lane TP after deterministic closure of `P1.6o159`

### Expiry/trigger to stop deferral

- before any new expensive rerun after `P1.6o158`

## Next-block contract (mandatory)

### Next block objective

- publish and run one fresh proof-lane rerun after deterministic closure of `P1.6o159`, then truthfully decide whether the surfaced service-choice master-availability family moved off first fail.

### First deterministic check command

- `python3 -m py_compile truffles-api/app/routers/webhook/decision.py truffles-api/tests/test_message_endpoint.py && pytest -q truffles-api/tests/test_message_endpoint.py -k "specialist_availability_followup_keeps_time_collect or grounded_specialist_availability_transitions_to_name_collect or service_choice_master_availability_followup_keeps_time_collect" && git diff --check`

### Blocked-by conditions

- `P1.6o158` truth is not synced in active docs/state/session
- targeted `P1.6o159` runtime-owner checks are not green
- the team attempts another expensive rerun before deterministic closure of this block

### Owner role for closure

- `Top Architect + Brain`

## Execution status

- `done`

## Execution result

- Added one bounded owner-preservation helper in `truffles-api/app/routers/webhook/decision.py` for the surfaced `service_choice + live_availability + clarify_missing_time + ask_about_requested_slot` envelope.
- Routed that envelope into the existing specialist-availability followup response/meta/trace path instead of generic `booking_prompt/time`.
- Added `test_llm_policy_core_service_choice_master_availability_followup_keeps_time_collect` in `truffles-api/tests/test_message_endpoint.py` and kept adjacent specialist-availability regressions green.
- Deterministic checks completed:
  - `python3 -m py_compile truffles-api/app/routers/webhook/decision.py truffles-api/tests/test_message_endpoint.py`
  - `pytest -q truffles-api/tests/test_message_endpoint.py -k "specialist_availability_followup_keeps_time_collect or grounded_specialist_availability_transitions_to_name_collect or service_choice_master_availability_followup_keeps_time_collect"`
  - `git diff --check`
  - `SESSION_AGENT=a1 scripts/session_check.sh`

# TP-2026-03-14-p1.6o174-post-p1.6o173-proof-lane-a1

- Название/цель: выполнить один свежий guarded `dev L2` rerun после детерминированного закрытия `P1.6o173`, чтобы доказать, что surfaced invalid-schema named-specialist runtime drift ушёл с first fail и truthfully surfaced следующий admissible class.

## Canon refs

- Binding roadmap: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o135-beauty-salon-canonical-control-plane-roadmap-a1.md`
- Prior deterministic closure: `docs/TASK_PACKAGES/TP-2026-03-14-p1.6o173-invalid-schema-named-specialist-surface-hint-followup-a1.md`
- Prior proof lane: `docs/TASK_PACKAGES/TP-2026-03-14-p1.6o172-post-p1.6o171-proof-lane-a1.md`
- Class-level routing: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o118-class-level-closure-process-reset-a1.md`
- Parent closure program: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Main remediation: `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- State baseline: `STATE.md`

## Git context

- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `origin/main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect` remove branch + worktree after merge

## One web search (mandatory before implementation)

- Not applicable in this block: proof-only continuation, no implementation.
- Decision:
  - `reuse`
  - reuse the existing guarded `dev L2` proof contour and strict artifact audit to determine whether the covered `P1.6o173` runtime slice moved off first fail.

## Root cause (mandatory)

- Symptom:
  - `P1.6o173` can close deterministically, but there is still no fresh proof showing the covered invalid-schema named-specialist runtime family moved off first fail.
- Minimal reproduction:
  1. finish `P1.6o173` deterministic closure;
  2. verify `/admin/version` on `:18184` matches current `HEAD`;
  3. execute one fresh guarded `dev L2` rerun with explicit `--quality-lane dev`;
  4. strict-audit the run and classify the next first fail.
- Evidence:
  - `docs/TASK_PACKAGES/TP-2026-03-14-p1.6o173-invalid-schema-named-specialist-surface-hint-followup-a1.md`
  - `/tmp/booking_quality/p1.6o172-l2-dev-20260314-a1-r2/summary.json`
  - `/tmp/booking_quality/p1.6o172-l2-dev-20260314-a1-r2/manual_audit.json`
- Five Whys:
  1. Why is another proof block needed? Because deterministic closure alone does not prove the expensive lane moved past the covered runtime family.
  2. Why not open another structural slice immediately? Because frozen routing requires one proof checkpoint after each bounded fix.
  3. Why reuse the same live proof contour? Because parity drift would make class routing non-admissible.
  4. Why explicit `--quality-lane dev`? Because proof continuation must avoid accidental acceptance-chain preflight.
  5. Why one run only? Because this block is classification/proof, not another expensive loop.
- Root cause statement:
  - after `P1.6o173`, the only missing evidence is one fresh strict-audited proof rerun that shows whether the covered invalid-schema named-specialist runtime family is gone from first fail.
- Fix mechanism:
  - verify runtime parity, run one guarded `dev L2`, strict-audit it, and select the next structural continuation only from the surfaced class.

## Reuse-first decision (mandatory)

- Reuse:
  - existing `ops/diagnose.py llm-quality` proof-lane contour
  - existing strict audit command
  - live runtime on `:18184`, relaunched from current worktree if parity is missing
- Integrate:
  - no code changes in this block
- Build:
  - none

## Release safety

- Rollout: none; proof-only block
- Go signal:
  - `/admin/version.git_commit == HEAD`
  - strict audit completes for the fresh rerun
- No-go signal:
  - runtime parity mismatch
  - preflight invalid
  - strict audit missing or artifact bundle incomplete
- Rollback:
  - no runtime/code rollback in this block; only stop after audit and publish truthful class routing

## Invariant

1. `P1.6o173` deterministic closure remains intact.
2. No new runtime/oracle patch is allowed before the rerun result is audited.
3. Proof stays on a fingerprint-verified runtime on `:18184`.
4. Evidence remains contract-first and class-routed.

## Scope

- runtime parity check on `:18184`
- one fresh guarded `dev L2`
- strict audit of the run
- class routing from the surfaced first fail
- truthful docs/state/session sync

## Out of scope

- new structural runtime/oracle changes before proof result
- lock/replay/full acceptance bundle
- baseline updates
- scenario regeneration changes beyond the single run

## Touch-list

- `docs/TASK_PACKAGES/TP-2026-03-14-p1.6o173-invalid-schema-named-specialist-surface-hint-followup-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-14-p1.6o174-post-p1.6o173-proof-lane-a1.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `STATE.md`
- `docs/SESSIONS/SESSION-2026-02-19-llm-first-firebreak-a1.md`
- `docs/SESSION_INDEX.md`
- `/tmp/booking_quality/p1.6o174-l2-dev-20260314-a1-r1/**`

## Plan

1. Verify runtime parity on `:18184`; relaunch a fresh runtime from current worktree if needed.
2. Execute exactly one fresh guarded `dev L2` rerun.
3. Strict-audit the run.
4. Route the surfaced first fail under `P1.6o118`.
5. Sync truth and publish the next admissible structural block.

## DoD

1. A fresh guarded rerun exists under `/tmp/booking_quality/p1.6o174-l2-dev-20260314-a1-r1`.
2. Strict audit records `artifact_integrity.valid=true`.
3. `P1.6o173` is proven off first fail or a new surfaced class is truthfully documented.
4. Active docs/state/session show `P1.6o174` as the proof checkpoint after `P1.6o173`.

## Checks

- `python3 - <<'PY'\nimport json, subprocess, urllib.request\nhead = subprocess.check_output(['git', 'rev-parse', 'HEAD'], text=True).strip()\nwith urllib.request.urlopen('http://127.0.0.1:18184/admin/version', timeout=10) as r:\n    runtime = json.load(r).get('git_commit')\nprint({'head': head, 'runtime': runtime, 'match': head == runtime})\nPY`
- `python3 ops/diagnose.py llm-quality ... --output-dir /tmp/booking_quality/p1.6o174-l2-dev-20260314-a1-r1 --run-id p1.6o174-l2-dev-20260314-a1-r1 ... --quality-lane dev`
- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o174-l2-dev-20260314-a1-r1 --status done --strict-artifacts`
- `git diff --check`

## Evidence

1. `/tmp/booking_quality/p1.6o174-l2-dev-20260314-a1-r1/summary.json`
2. `/tmp/booking_quality/p1.6o174-l2-dev-20260314-a1-r1/brief.md`
3. `/tmp/booking_quality/p1.6o174-l2-dev-20260314-a1-r1/scenarios.json`
4. `/tmp/booking_quality/p1.6o174-l2-dev-20260314-a1-r1/responses.jsonl`
5. `/tmp/booking_quality/p1.6o174-l2-dev-20260314-a1-r1/trace_bundle.jsonl`
6. `/tmp/booking_quality/p1.6o174-l2-dev-20260314-a1-r1/manual_audit.json`

## Rollback

1. No code rollback in this proof-only block.
2. If parity or audit is invalid, stop and keep `P1.6o173` as the active deterministic closure with no new class routing.

## No-go

- no structural patching before strict audit
- no baseline update from this run
- no acceptance claims from a `dev` rerun

## Risks / blockers

- runtime parity mismatch would invalidate class routing
- preflight or artifact integrity failure would make the run inadmissible
- next surfaced class may move into a different family and require a different bounded slice

## Residual architecture debt (mandatory)

### Current residuals accepted in this block

- proof remains non-canonical until full closure bundle exists
- runtime and scenario compiler still converge slice by slice, not from a single generated contract

### Why not in this block

- this block is proof-only and exists to classify the next admissible family

### Risk if deferred

- the worktree could drift into unsanctioned new fixes without proof that `P1.6o173` actually moved off first fail

### Linked follow-up Task Package(s)

- next TP selected only after `P1.6o174` strict audit

### Expiry/trigger to stop deferral

- immediately after `P1.6o174` finishes and the next surfaced class is known

## Next-block contract (mandatory)

### Next block objective

- publish the next bounded structural block from the surfaced `P1.6o174` first fail family.

### First deterministic check command

- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o174-l2-dev-20260314-a1-r1 --status done --strict-artifacts && git diff --check`

### Blocked-by conditions

- `P1.6o174` has not finished with strict audit
- surfaced first fail is not yet classified from artifacts
- docs/state/session are not synced with truthful run status

### Owner role for closure

- `Top Architect + Brain`

## Execution status

- `done`


## Execution result

- Fresh guarded `dev L2` rerun `p1.6o174-l2-dev-20260314-a1-r1` completed with `artifact_integrity.valid=true`, `infra_valid=true`, `semantic_valid=false`, `stop_reason=max_failures_reached:1`, and `oracle_arbitration.winner=contract`.
- The covered invalid-schema named-specialist family moved off first fail; the next surfaced class is a bounded runtime-owner drift at `dialog 1 / turn 3 / "Что насчет времени выполнения?"`.
- Runtime stayed on `action=reply`, `intent=booking`, `source=booking_slot_guidance`, with missing duration info evidence (`expected_info_section_miss`, `info_section_miss`) even though scenario contract still required `info_sections=[duration,service_duration]` plus `expected_reply_type=time`.
- `P1.6o175` is therefore the admissible next structural block, and no new proof rerun is allowed before that bounded runtime slice closes deterministically.

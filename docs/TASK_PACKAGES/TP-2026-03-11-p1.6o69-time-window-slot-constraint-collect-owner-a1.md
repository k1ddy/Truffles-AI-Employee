# TP-2026-03-11-p1.6o69-time-window-slot-constraint-collect-owner-a1

- Название/цель: закрыть surfaced `r94` blocker family, где после bounded `M17` closure свежий guarded `dev L2` больше не падает на invalid-schema service-grounded intake, но declarative time-window constraint `Мне нужно с 10 до 12.` под active `time` collect не получает bounded `slot_constraint(time)` owner: `llm_policy_core` валится в `collect_slot_order_invalid`, runtime отвечает generic `booking_prompt`, а финальные `decision_meta` / `decision_trace` теряют `pending_question_interaction=slot_constraint` и canonical pending-question guidance contract.

## Canon refs

- Parent verdict: `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o60a-remaining-closure-architecture-verdict-a1.md`
- Owner matrix: `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o60b-remaining-closure-owner-matrix-a1.md`
- Program TP: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Proof bundle: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`
- Prior child: `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o68-invalid-schema-service-grounded-booking-owner-a1.md`
- Related bounded closures:
  - `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o46-question-like-daypart-slot-constraint-contract-a1.md`
  - `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o64-timeout-mixed-date-availability-governance-a1.md`
- STATE: `STATE.md`
- CA: remaining closure row after `M17`

## One web search (mandatory before implementation)

- Query: `Rasa forms custom slot validation keep requested slot official docs`
- Date/time: `2026-03-11T15:30:14+05:00`
- Sources opened:
  - official Rasa docs: `https://rasa.com/docs/rasa/forms/`
- Source quality:
  - official product documentation
- Found reusable ideas:
  - if a user provides a partial or constrained answer for the currently requested slot, the form should keep the same requested slot active and emit an explicit slot-validation/clarification step instead of silently advancing to the next slot;
  - validation fallback should preserve the active slot contract and observability, not collapse into a generic prompt that hides the unresolved relation.
- Decision:
  - `reuse/integrate`
  - reuse the existing bounded `booking_slot_guidance` runtime contract and integrate a narrow time-window constraint detector so `collect_slot_order_invalid` on active `time` collect preserves `slot_constraint(time)` evidence.
- Rejected options:
  - weakening scenario/oracle to accept missing `slot_constraint` evidence, because `r94` exposes a real runtime contract gap;
  - broad prompt-only patch without runtime guard, because the failure is currently contained by deterministic validation and still needs bounded trace/meta recovery;
  - generic phrase-hardcode branching in booking core outside the active `time` collect boundary.

## Root cause (mandatory)

- Symptom:
  - fresh guarded `dev L2` run `p1.6o6-l2-dev-20260311-a1-r94` stops at dialog `1`, turn `3`, user text `Мне нужно с 10 до 12.`
- Minimal reproduction:
  1. Inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r94/scenarios.json` dialog `1`, turns `1-3`.
  2. Confirm turn `3` is tagged `slot_constraint` with canonical expectation `pending_question_act=slot_constraint`, `pending_question_target=time`, `expected_reply_type=time`.
  3. Inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r94/responses.jsonl` and `trace_bundle.jsonl` for turn `3` and confirm `llm_policy_core` returns `next_question=name`, `validation_error=collect_slot_order_invalid`, and final runtime falls back to generic `booking_prompt`.
  4. Confirm final `decision_meta` keeps only `expected_reply_type=time` and drops `pending_question_act/pending_question_target`, while `decision_trace` also lacks `pending_question_interaction`.
  5. Compare with existing bounded daypart/question-like slot-constraint rows and confirm this is a new declarative time-window family, not a reopen of `M8/M13`.
- Evidence:
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r94/summary.json`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r94/brief.md`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r94/scenarios.json`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r94/responses.jsonl`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r94/trace_bundle.jsonl`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r94/manual_audit.md`
- Five Whys:
  1. Why did turn `3` fail? The scenario expects `slot_constraint(time)` evidence, but final meta/trace only show a generic `booking_prompt` with `expected_reply_type=time`.
  2. Why is the final response generic? `llm_policy_core` validated to `collect_slot_order_invalid` and the runtime used the default `collect_prompt` degrade path.
  3. Why did the runtime use the default path? The active-time guard has no bounded detector for declarative time-window constraints like `с 10 до 12`, so it cannot switch to `booking_slot_guidance`.
  4. Why is that wrong? Under an active `datetime` question, a time-window constraint is not a filled slot and not a generic interrupt; the runtime must preserve `slot_constraint(time)` ownership and keep the same resume axis observable.
  5. Why is this a new bounded family? Earlier rows closed question-like/daypart and timeout-grounded slot-constraint variants, but not declarative clock-time window constraints under `collect_slot_order_invalid`.
- Root cause statement:
  - `truffles-api/app/routers/webhook/decision.py` lacks a bounded declarative time-window constraint detector and corresponding `booking_slot_guidance` guard for `collect_slot_order_invalid`, so active-time window constraints degrade to a generic prompt without `slot_constraint(time)` contract evidence.
- Fix mechanism:
  - freeze matrix row `M18` for active-time declarative window constraints;
  - add a bounded time-window constraint detector for `collect_slot_order_invalid` under active `time` collect;
  - route that row to existing `booking_slot_guidance` so final `decision_meta` / `decision_trace` preserve `slot_constraint(time)` while keeping `expected_reply_type=time`.

## Reuse-first decision (mandatory)

- Reuse:
  - existing `booking_slot_guidance` degrade contract in `truffles-api/app/routers/webhook/decision.py`
  - existing `slot_constraint` response guard expectations
  - existing collect-slot-order validation boundary
- Integrate:
  - bounded `collect_slot_order_invalid` branch for active `time` collect
- Do not build:
  - new generic booking flow
  - new prompt-only phrase router
  - new oracle waiver

## Execution profile

- TP mode: `implementation`
- Doc touch budget (files): `9`
- Code dominance: `required`
- Override token: `none`
- Why this profile fits: `r94` surfaced a single runtime owner row with an existing bounded guidance helper and explicit deterministic evidence.

## Release safety

- Strategy: bounded active-time guard repair only
- Go signals:
  - declarative time-window constraint under active `time` collect emits `slot_constraint(time)` trace/meta and keeps `expected_reply_type=time`
  - final response comes from bounded pending-question guidance, not generic `booking_prompt`
  - adjacent generic `collect_slot_order_invalid` rows remain unchanged
- No-go signals:
  - widening the fix outside active `time` collect
  - accepting missing `slot_constraint` evidence as closure
  - making exact-time fills look like `slot_constraint`
- Rollback verification:
  - revert only the bounded `M18` runtime/test delta if adjacent collect-slot-order rows regress

## Invariant

1. `P1.6o68/M17` remains closed.
2. Existing question-like/daypart `slot_constraint` rows remain behaviorally unchanged.
3. Exact grounded datetime fills must not be reclassified as `slot_constraint`.
4. Degraded fallback remains explicit in `decision_meta` / `decision_trace`.

## Scope

- publish surfaced `r94` family as matrix row `M18`
- implement bounded declarative time-window `slot_constraint(time)` recovery under `collect_slot_order_invalid`

## Out of scope

- acceptance `lock/replay`
- prompt-only semantic redesign
- scenario normalization for `r94` (current scenario expectation is canonical)
- timeout-specific degraded collect rows already closed elsewhere

## Touch-list

- `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o69-time-window-slot-constraint-collect-owner-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o60b-remaining-closure-owner-matrix-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o60a-remaining-closure-architecture-verdict-a1.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`
- `truffles-api/app/routers/webhook/decision.py`
- `truffles-api/tests/test_message_endpoint.py`
- `truffles-api/tests/test_booking_quality_response_guard.py`

## Plan (1..N)

1. Freeze `r94` as audited non-closure evidence and publish `M18`.
2. Add a bounded declarative time-window constraint detector under active `time` collect.
3. Route `collect_slot_order_invalid` for that row to `booking_slot_guidance` and preserve `slot_constraint(time)` meta/trace.
4. Add targeted runtime/response-guard regressions plus one adjacent negative collect-slot-order case.
5. Run focused checks and `git diff --check`.
6. Run exactly one fresh guarded `dev L2` only after deterministic green.

## DoD

1. `r94` is recorded in active docs as audited non-closure evidence.
2. Declarative time-window constraints under active `time` collect no longer end as generic `booking_prompt` without `slot_constraint` evidence.
3. Final meta/trace preserve `pending_question_act=slot_constraint`, `pending_question_target=time`, and `expected_reply_type=time`.
4. Adjacent generic `collect_slot_order_invalid` rows still use the previous bounded behavior.
5. Focused deterministic runtime/evidence regressions are green.
6. Fresh guarded `dev L2` no longer stops on the `r94` family.

## Expensive run control (mandatory)

- Hypothesis:
  - after the bounded `M18` fix, fresh guarded `dev L2` will no longer stop on dialog `1`, turn `3`, `Мне нужно с 10 до 12.`
- Expected measurable effect:
  - the next guarded run preserves `slot_constraint(time)` observability or surfaces a different first blocker
- Stop condition:
  - run exactly one fresh guarded `dev L2` plus strict audit; if the same `r94` family persists, stop the expensive loop and reopen RCA instead of chaining another rerun

## Checks

- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r94 --status done --strict-artifacts`
- `pytest -q truffles-api/tests/test_message_endpoint.py -k "time_window_slot_constraint or collect_slot_order_invalid_degrades_to_datetime_prompt"`
- `pytest -q truffles-api/tests/test_booking_quality_response_guard.py -k "time_window_slot_constraint or collect_slot_order_invalid or slot_constraint"`
- `git diff --check`

## Execution status

- `open`
- fresh guarded `dev L2` `p1.6o6-l2-dev-20260311-a1-r94` is audited and frozen as the next blocker
- deterministic implementation has not started yet

## Evidence

1. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r94/summary.json`
2. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r94/brief.md`
3. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r94/scenarios.json`
4. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r94/responses.jsonl`
5. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r94/trace_bundle.jsonl`
6. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r94/manual_audit.md`
7. `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o68-invalid-schema-service-grounded-booking-owner-a1.md`
8. `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o69-time-window-slot-constraint-collect-owner-a1.md`

## Rollback

1. Keep `P1.6o6` blocked.
2. Revert only the future bounded `M18` runtime/test delta if it regresses adjacent collect-slot-order rows.

## No-go

- no phrase fitting
- no oracle weakening
- no reinterpretation of `r94` as proof that `M17` is still open
- no exact-time fill reclassified as `slot_constraint`
- no generic `booking_prompt` accepted as closure for active-time declarative window constraints

## Risks/Blockers

1. The detector may overmatch exact clock-time fills if the range heuristic is too broad.
2. The bounded guidance path must not reopen scenario-governance rows that already accept `reply_type=time` without `slot_constraint` evidence.
3. `r94` remains non-canonical proof evidence (`infra_valid=false`, `semantic_valid=false`), so this block can only claim bounded closure plus rerun rollover.

## Residual architecture debt (mandatory)

- Current residuals accepted in this block:
  - `M18` remains open after bounded `M17` closure.
- Why not in this block:
  - `r94` surfaced only after the next fresh guarded `dev L2` beyond `M17`.
- Risk if deferred:
  - declarative time-window constraints can still collapse into generic collect prompts and hide later owner rows.
- Linked follow-up Task Package(s):
  - `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`
- Expiry/trigger to stop deferral:
  - before the next guarded rerun beyond `r94`.

## Next-block contract (mandatory)

- Next block objective:
  - implement `M18`, then rerun focused deterministic runtime checks before any new guarded `dev L2`.
- First deterministic check command:
  - `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r94 --status done --strict-artifacts && git diff --check`
- Blocked-by conditions:
  - `M18` is not published in the owner matrix
  - declarative time-window regressions are not green
  - acceptance tool-evidence infra remains red
- Owner role for closure:
  - `Hands` for bounded implementation, `Top Architect + Brain` for matrix/program/proof sync


## Closure update

- Deterministic implementation for `M18` is complete.
- Fresh guarded `dev L2` `r95` no longer first-failed on this row and rolled to `P1.6o70`.
- This block is now `closed_bounded`; proof-chain closure remains blocked on later surfaced families and infra-valid canonical evidence.

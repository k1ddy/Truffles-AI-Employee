# TP-2026-03-12-p1.6o109-active-time-services-overview-interrupt-owner-a1

- Название/цель: закрыть surfaced `M39`, где fresh proof lane `P1.6o108` показал success-path owner gap на фразе `Какие услуги по стрижке волос вы предлагаете?` под активным `expected_reply_type=time`: policy-core already keeps `intent=info`, `subject_kind=service`, `service_query=стрижка волос`, но runtime оставляет `tool_action=collect`, переоткрывает `service_choice`, и не отвечает на factual side-question вместо service-info interrupt с сохранённым `time` resume.

## Canon refs

- Parent verdict: `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o60a-remaining-closure-architecture-verdict-a1.md`
- Closure program: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Main remediation: `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- Prior proof lane: `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o108-post-m38-proof-lane-a1.md`
- Related bounded rows: `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o83-owner-resolver-m27-vertical-slice-a1.md`, `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o97-active-name-service-info-interrupt-owner-a1.md`
- Owner matrix artifact: `truffles-api/app/knowledge/generic/INTERACTION_OWNER_MATRIX.yaml`
- State baseline: `STATE.md`

## Git context

- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect` remove branch + worktree after merge

## One web search (mandatory before implementation)

- Query: `site:stately.ai/docs state machine guards preserve parent state during interrupt side question`
- Date/time: `2026-03-12T19:46:00+05:00`
- Sources opened:
  - official Stately guards docs: `https://stately.ai/docs/guards`
  - official Stately parent states docs: `https://stately.ai/docs/parent-states`
- Source quality:
  - official product documentation
- Found reusable ideas:
  - interruption handling should branch through explicit guards rather than silently collapsing the parent state;
  - parent/active state should keep ownership while the side-question branch resolves and then resume the parent objective.
- Decision:
  - `reuse + integrate`
  - reuse the existing booking interrupt + `catalog.service_query` tool path and integrate one bounded collect-to-tool recovery for the active-time services-overview row before the collect prompt is emitted.
- Rejected options:
  - weakening the scenario to accept stale `service_choice` reopening;
  - broad prompt/schema redesign in this child block;
  - direct phrase-hardcode reply text in core.

## Root cause (mandatory)

- Symptom:
  - fresh guarded `dev L2` `p1.6o108-l2-dev-20260312-a1-r1` stops at dialog `2`, turn `2`, user text `Какие услуги по стрижке волос вы предлагаете?`.
- Minimal reproduction:
  1. Inspect `/tmp/booking_quality/p1.6o108-l2-dev-20260312-a1-r1/scenarios.json` dialog `2`, turn `2` and confirm the turn expects `reply_type=time` plus `question_contract expected_reply_type=time`.
  2. Inspect `/tmp/booking_quality/p1.6o108-l2-dev-20260312-a1-r1/responses.jsonl` for `message_id=LLM-QUAL-p1.6o108-l2-dev-20260312-a1-r1-002-02-1ce3ae`.
  3. Confirm runtime returns `action=booking_prompt`, `intent=booking`, `expected_reply_type=service_choice`, and reply text `На какую услугу хотите записаться? После этого сразу проверю свободное время.` instead of answering the haircut-services side-question.
  4. Inspect `/tmp/booking_quality/p1.6o108-l2-dev-20260312-a1-r1/trace_bundle.jsonl` for the same turn and confirm `question_contract` first sees active `expected_reply_type=time`, but final `question_contract` is reset to `service_choice` after `llm_policy_core` emits `intent=info`, `tool_action=collect`, `subject_kind=service`, `resolution_mode=clarify_missing_subject`, and `service_query=стрижка волос`.
  5. Compare against the already-closed active-name service-info interrupt row (`M33`) to confirm the remaining gap is the active-time collect variant before tool execution, not tool-response ownership.
- Evidence:
  - `/tmp/booking_quality/p1.6o108-l2-dev-20260312-a1-r1/summary.json`
  - `/tmp/booking_quality/p1.6o108-l2-dev-20260312-a1-r1/brief.md`
  - `/tmp/booking_quality/p1.6o108-l2-dev-20260312-a1-r1/scenarios.json`
  - `/tmp/booking_quality/p1.6o108-l2-dev-20260312-a1-r1/responses.jsonl`
  - `/tmp/booking_quality/p1.6o108-l2-dev-20260312-a1-r1/trace_bundle.jsonl`
  - `/tmp/booking_quality/p1.6o108-l2-dev-20260312-a1-r1/manual_audit.md`
- Five Whys:
  1. Why did the run fail? Because strict contract evaluation hit `expected_meta_mismatch`, and judge marked the turn `missed_question`.
  2. Why did meta/judge fail? Because runtime reopened `service_choice` and never answered the haircut-services question.
  3. Why did runtime reopen `service_choice`? Because `llm_policy_core` kept `tool_action=collect` even though the turn is already semantically `intent=info` with a concrete `service_query`.
  4. Why wasn’t the info side-question recovered by existing bounded rows? Because `M27`/`M33` only cover pricing/tool-response service-info interruptions after the tool path is already active; they do not claim the pre-tool collect-collapse variant under active `time` collect.
  5. Why is this a new row instead of scenario drift? Because runtime itself resets `expected_reply_type` from `time` to `service_choice` and emits a collect prompt that misses the user’s question before oracle comparison.
- Root cause statement:
  - active-time service-info interruption is only partially compiled: when policy-core labels the turn `intent=info` but leaves `tool_action=collect`, runtime has no bounded recovery that promotes the turn into the existing `catalog.service_query` interrupt path, so the side-question is missed and the active `time` resume contract is overwritten by stale `service_choice` collect.
- Fix mechanism:
  - freeze surfaced row `M39` in the machine-readable owner matrix;
  - add one bounded collect-to-`catalog.service_query` recovery for active-time services-overview/service-info side-questions before tool verification/execution;
  - preserve the existing booking interrupt follow-up so `expected_reply_type=time` survives the factual reply;
  - add targeted runtime/response-guard/matrix regressions.

## Reuse-first decision (mandatory)

- Reuse:
  - existing `catalog.service_query` tool path
  - existing services-overview tool recovery / contract handling
  - existing booking interrupt follow-up contract that keeps active `time`
  - existing active-name service-info interrupt evidence from `M33`
- Integrate:
  - bounded collect-to-tool recovery for active-time services-overview/service-info interruptions
  - targeted regressions for the surfaced haircut-services phrasing and active-time info-resume contract
- Do not build:
  - new catalog adapter
  - new generic info router
  - new phrase-owned reply path in core

## Execution profile

- TP mode: `implementation`
- Doc touch budget (files): `11`
- Code dominance: `required`
- Override token: `none`
- Why this profile fits: `r125` surfaced a bounded pre-tool owner gap adjacent to already-closed service-info interrupt rows, so reuse exists and the block should compile the existing interrupt path rather than redesign the runtime.

## Release safety

- Strategy: bounded pre-tool collect-to-service-info recovery only
- Go signals:
  - `Какие услуги по стрижке волос вы предлагаете?` under active `time` collect now routes through `catalog.service_query` and replies with factual service info / services overview
  - runtime preserves `expected_reply_type=time` and booking-interrupt evidence after the factual reply
  - adjacent active-name service-info and active-time pricing/master interrupt rows remain green
- No-go signals:
  - the surfaced turn still reopens `service_choice`
  - unrelated collect turns start routing into `catalog.service_query`
  - response text skips the user’s info question and only repeats booking prompt
- Rollback verification:
  - revert only the bounded `M39` collect-to-tool recovery and targeted regressions if adjacent collect/info rows regress

## Invariant

1. Existing active-time pricing/master interrupt rows remain green.
2. Existing active-name service-info interrupt rows remain green.
3. No weakening of collect-slot or clarify-limit gates outside the bounded `M39` family.
4. No guarded rerun in this block.

## Scope

- publish surfaced row `M39`
- implement bounded active-time collect-to-service-info recovery before tool execution
- add targeted runtime + response-guard + matrix regressions
- sync active docs/state/session and publish the next proof-lane block ID

## Out of scope

- fresh guarded `dev L2`
- acceptance `lock/replay/full`
- broad collect/info taxonomy redesign

## Touch-list

- `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o108-post-m38-proof-lane-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o109-active-time-services-overview-interrupt-owner-a1.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `STATE.md`
- `STRUCTURE.md`
- `docs/SESSIONS/SESSION-2026-02-19-llm-first-firebreak-a1.md`
- `truffles-api/app/knowledge/generic/INTERACTION_OWNER_MATRIX.yaml`
- `truffles-api/app/routers/webhook/decision.py`
- `truffles-api/tests/test_message_endpoint.py`
- `truffles-api/tests/test_booking_quality_response_guard.py`
- `truffles-api/tests/test_interaction_owner_matrix_contract.py`

## Plan (1..N)

1. Freeze `r125` as audited proof evidence and publish `M39`.
2. Add bounded pre-tool recovery so active-time collect service-info interruptions route into `catalog.service_query` instead of stale `service_choice` prompt.
3. Add targeted deterministic runtime/response-guard/matrix regressions.
4. Run focused checks and `git diff --check`.
5. Publish the next proof-lane block ID only after deterministic closure is green.

## DoD

1. `r125` is recorded truthfully as surfaced `M39` in active docs and the owner matrix.
2. `Какие услуги по стрижке волос вы предлагаете?` under active `time` collect no longer produces a stale `service_choice` prompt and instead answers through the service-info/tool path.
3. `expected_reply_type=time` remains active after the info reply, with explicit trace/meta evidence.
4. Focused deterministic runtime/response-guard/matrix regressions are green.
5. The next admissible block is the fresh proof lane `P1.6o110`.

## Expensive run control (mandatory)

- Hypothesis:
  - after bounded `M39` runtime closure, a fresh guarded `dev L2` will no longer stop on dialog `2`, turn `2`, `Какие услуги по стрижке волос вы предлагаете?`
- Expected measurable effect:
  - targeted deterministic checks turn green, and the next guarded proof lane either moves past this row or surfaces a different first-fail family
- Stop condition:
  - no guarded run inside this block; stop after deterministic closure and publish the next proof TP ID

## Checks

- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o108-l2-dev-20260312-a1-r1 --status done --strict-artifacts`
- `pytest -q truffles-api/tests/test_message_endpoint.py -k "collect_service_info_interrupt or owner_resolver_m33 or reschedule_pricing_interrupt"` -> `3 passed, 380 deselected`
- `pytest -q truffles-api/tests/test_booking_quality_response_guard.py -k "services_overview or collect_service_info_interrupt"` -> `2 passed, 94 deselected`
- `pytest -q truffles-api/tests/test_interaction_owner_matrix_contract.py` -> `2 passed`
- `python3 -m py_compile truffles-api/app/routers/webhook/decision.py truffles-api/tests/test_message_endpoint.py truffles-api/tests/test_booking_quality_response_guard.py truffles-api/tests/test_interaction_owner_matrix_contract.py` -> `pass`
- `git diff --check` -> `pass`
- `SESSION_AGENT=a1 scripts/session_check.sh` -> `Session OK`

## Execution status

- `done`
- active-time services-overview side questions with grounded service referents now promote stale `tool_action=collect` into the existing `catalog.service_query` interrupt path before collect prompt emission
- the bounded recovery preserves `expected_reply_type=time`, stamps explicit `policy_interrupt_contract` / `booking_interrupt_info` evidence, and answers the factual side-question instead of reopening `service_choice`
- `M39` is closed on deterministic contour and the next admissible block is the fresh proof lane `P1.6o110`

## Evidence

1. `/tmp/booking_quality/p1.6o108-l2-dev-20260312-a1-r1/summary.json`
2. `/tmp/booking_quality/p1.6o108-l2-dev-20260312-a1-r1/brief.md`
3. `/tmp/booking_quality/p1.6o108-l2-dev-20260312-a1-r1/scenarios.json`
4. `/tmp/booking_quality/p1.6o108-l2-dev-20260312-a1-r1/responses.jsonl`
5. `/tmp/booking_quality/p1.6o108-l2-dev-20260312-a1-r1/trace_bundle.jsonl`
6. `/tmp/booking_quality/p1.6o108-l2-dev-20260312-a1-r1/manual_audit.md`
7. `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o108-l2-dev-20260312-a1-r1 --status done --strict-artifacts`
8. `truffles-api/app/routers/webhook/decision.py`
9. `truffles-api/tests/test_message_endpoint.py`
10. `truffles-api/tests/test_booking_quality_response_guard.py`
11. `truffles-api/app/knowledge/generic/INTERACTION_OWNER_MATRIX.yaml`
12. `truffles-api/tests/test_interaction_owner_matrix_contract.py`
13. `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o110-post-m39-proof-lane-a1.md`

## Rollback

1. Keep `P1.6o108` proof truth intact.
2. Revert only the bounded `M39` collect-to-tool recovery and targeted regressions if adjacent service-info rows regress.
3. Do not mutate the matrix to hide surfaced evidence.

## No-go

- no scenario weakening to accept stale `service_choice`
- no global collect/info arbitration rewrite
- no second expensive run before deterministic closure is green

## Risks / blockers

- bounded collect-to-tool recovery can accidentally steal unrelated collect turns if the candidate gate is too broad
- service-overview detection without a grounded `service_query` can overgeneralize the tool path if fallback values are too permissive
- proof remains non-canonical until unrelated infra/tool-evidence red is separately closed

## Residual architecture debt (mandatory)

### Current residuals accepted in this block

- collect-to-info/tool arbitration still lives in `decision.py`, not in the migrated owner resolver/handler layer
- closure proof remains on `dev L2`; `lock/replay/full` stay blocked

### Why not in this block

- this block only closes surfaced row `M39`

### Risk if deferred

- active-time service-info side-questions keep reopening `service_choice` and mask later remaining families

### Linked follow-up Task Package(s)

- `P1.6o110` post-`M39` proof lane
- `P1.7` acceptance replay gate after the closure lane is green enough to advance

### Expiry/trigger to stop deferral

- before any next guarded `dev/acceptance` rerun beyond the `M39` proof admission

## Next-block contract (mandatory)

### Next block objective

- publish and run `P1.6o110`: prove on one fresh guarded `dev L2` that `M39` is gone from the first-fail position and truthfully admit the next family, if any.

### First deterministic check command

- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o108-l2-dev-20260312-a1-r1 --status done --strict-artifacts && pytest -q truffles-api/tests/test_message_endpoint.py -k "collect_service_info_interrupt or owner_resolver_m33 or reschedule_pricing_interrupt" && pytest -q truffles-api/tests/test_booking_quality_response_guard.py -k "services_overview or collect_service_info_interrupt" && pytest -q truffles-api/tests/test_interaction_owner_matrix_contract.py && git diff --check`

### Blocked-by conditions

- `P1.6o108` proof evidence and `M39` admission are not synced truthfully into active docs and the owner matrix
- exact surfaced deterministic reproductions are not green
- `P1.6o109` deterministic closure is not synced truthfully into active docs and the owner matrix
- `P1.6o110` is not published as the next proof block

### Owner role for closure

- `Top Architect + Brain`

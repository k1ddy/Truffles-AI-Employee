# TP-2026-03-14-p1.6o191-scenario-generation-timeout-progress-evidence-a1

- Название/цель: закрыть surfaced proof-lane blocker после `P1.6o190`, где OpenAI preflight уже green, но scenario generation still dies on `scenario_generation_timeout` before `scenarios.json` is emitted, а guarded run не даёт last-progress evidence для точной RCA.

## Canon refs

- Binding roadmap: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o135-beauty-salon-canonical-control-plane-roadmap-a1.md`
- Prior deterministic closures: `docs/TASK_PACKAGES/TP-2026-03-14-p1.6o187-openai-llm-preflight-timeout-retry-a1.md`, `docs/TASK_PACKAGES/TP-2026-03-14-p1.6o189-openai-llm-preflight-retry-timeout-escalation-a1.md`
- Blocked proof lane: `docs/TASK_PACKAGES/TP-2026-03-14-p1.6o190-post-p1.6o189-proof-lane-a1.md`
- Parent closure program: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Main remediation: `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- State baseline: `STATE.md`

## One web search (mandatory before implementation)

- Exact query: `python subprocess.run timeout TimeoutExpired official docs`
- Date/time: `2026-03-14 15:50 +05:00`
- Opened sources:
  - official Python subprocess docs (`https://docs.python.org/3/library/subprocess.html`)
- Found ready solutions:
  - `subprocess.run(timeout=...)` applies to total child-process wall time and timeout diagnosis depends on what the child emitted before exit;
  - stderr progress evidence is the narrowest way to distinguish outer subprocess timeout from inner request timeout without changing proof behavior.
- Decision: `integrate/build`
  - keep the guarded proof contract unchanged;
  - enable scenario-generation progress evidence so timeout normalization can include last `batch/attempt/event` and the next blocker is root-caused from artifacts instead of guesses.
- Rejected variants:
  - rerunning the proof lane again without new timeout evidence;
  - immediately changing timeout math before proving whether timeout is outer subprocess budget or inner request timeout;
  - disabling timeout protection.
- Source quality:
  - primary/official source: `docs.python.org`.

## Git context

- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `origin/main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect` remove branch + worktree after merge

## Root cause (mandatory)

- Symptom:
  - `p1.6o190-l2-dev-20260314-a1-r1` passes runtime parity and both OpenAI preflights, but exits before scenario emission with `llm-quality: scenario generation failed (scenario_generation_timeout)` and audited missing `scenarios.json`, `responses.jsonl`, and `trace_bundle.jsonl`.
- Minimal reproduction:
  1. run guarded proof lane `P1.6o190` on parity-verified runtime `http://127.0.0.1:18184`;
  2. let `ops/diagnose.py` spawn `scripts/booking_dialog_scenarios.py` for LLM generation;
  3. observe `scenario_generation_timeout` before any scenario artifacts exist;
  4. inspect `_normalize_scenario_generation_error(...)` and note that it can emit last `batch/attempt/event` only when `booking_scenario_llm_progress` exists in stderr, but guarded runs currently keep `scenario_progress_stderr=false`.
- Evidence:
  - `/tmp/booking_quality/p1.6o190-l2-dev-20260314-a1-r1/summary.json`
  - `/tmp/booking_quality/p1.6o190-l2-dev-20260314-a1-r1/brief.md`
  - `/tmp/booking_quality/p1.6o190-l2-dev-20260314-a1-r1/manual_audit.json`
  - `ops/diagnose.py`
  - `scripts/booking_dialog_scenarios.py`
  - `truffles-api/tests/test_diagnose_run_command.py`
- Five Whys:
  1. Why did the proof lane still fail before scenarios? Because scenario generation timed out.
  2. Why is the timeout class still not precisely root-caused? Because the guarded run emitted no batch/attempt progress evidence.
  3. Why is no progress evidence emitted? Because `scenario_progress_stderr` stays false in the guarded proof contour.
  4. Why is that a blocker? Because without last-progress evidence we cannot distinguish outer subprocess-budget exhaustion from inner OpenAI request timeout truthfully enough for the next structural slice.
  5. Why is progress evidence the right next slice? Because it is the narrowest deterministic observability change that preserves proof behavior while upgrading RCA quality.
- Root cause statement:
  - the surfaced `scenario_generation_timeout` blocker lacks machine-visible last-progress evidence because guarded LLM scenario generation runs with `scenario_progress_stderr=false`, so timeout normalization collapses several distinct timeout contours into one opaque label.
- Fix mechanism:
  - enable scenario-generation progress evidence in the guarded LLM proof contour, keep timeout behavior unchanged, and add targeted tests that prove progress stderr is passed through and timeout normalization can report last progress.

## Reuse-first decision (mandatory)

- Reuse:
  - existing `_normalize_scenario_generation_error(...)` support for `booking_scenario_llm_progress`
  - existing scenario-generation command assembly in `ops/diagnose.py`
  - existing tests in `truffles-api/tests/test_diagnose_run_command.py`
- Integrate:
  - keep the fix inside proof-tooling observability only; no runtime business logic changes
- Build:
  - add only the bounded observability change and tests for the surfaced timeout contour

## Release safety

- Rollout: local tooling-only change
- Go signal:
  - scenario generation timeouts now carry last-progress evidence in guarded runs
- No-go signal:
  - change alters runtime semantics or timeout decisions instead of only observability
- Rollback:
  - revert only the progress-evidence change and tests; keep `P1.6o190` blocked truth intact

## Invariant

1. No weakening of proof gates.
2. No timeout-budget changes in this block.
3. No runtime/business behavior changes.
4. No extra proof rerun before deterministic closure.

## Scope

- enable scenario-generation progress stderr in the guarded LLM proof contour
- preserve and test last-progress timeout normalization in `ops/diagnose.py`
- sync canon docs/state/session after deterministic closure

## Out of scope

- fresh guarded rerun before deterministic closure
- changing scenario timeout math
- any booking/runtime semantic changes

## Touch-list

- `ops/diagnose.py`
- `truffles-api/tests/test_diagnose_run_command.py`
- `STATE.md`
- `docs/SESSIONS/SESSION-2026-02-19-llm-first-firebreak-a1.md`
- `docs/SESSION_INDEX.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-14-p1.6o190-post-p1.6o189-proof-lane-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-14-p1.6o191-scenario-generation-timeout-progress-evidence-a1.md`

## Plan

1. Add targeted unit coverage that proves last-progress timeout normalization and guarded command pass-through for scenario progress stderr.
2. Enable scenario-generation progress evidence for guarded LLM proof runs.
3. Re-run targeted tooling tests and static checks.
4. Sync `P1.6o190` truth as a scenario-generation proof blocker and mark `P1.6o191` complete if green.
5. Publish the next proof-lane TP.

## DoD

1. Guarded LLM proof runs emit scenario-generation progress evidence to stderr.
2. Timeout normalization can report last `batch/attempt/event` when scenario generation times out.
3. Targeted tooling tests are green.
4. Docs/state/session truthfully record `P1.6o190` as blocked on scenario generation timeout and publish the next admissible rerun TP.

## Checks

- `python3 -m py_compile ops/diagnose.py truffles-api/tests/test_diagnose_run_command.py`
- `pytest -q truffles-api/tests/test_diagnose_run_command.py -k "scenario_timeout or generate_batch"`
- `git diff --check`
- `SESSION_AGENT=a1 scripts/session_check.sh`

## Evidence

1. `/tmp/booking_quality/p1.6o190-l2-dev-20260314-a1-r1/summary.json`
2. `/tmp/booking_quality/p1.6o190-l2-dev-20260314-a1-r1/brief.md`
3. `/tmp/booking_quality/p1.6o190-l2-dev-20260314-a1-r1/manual_audit.json`
4. `ops/diagnose.py`
5. `scripts/booking_dialog_scenarios.py`
6. `truffles-api/tests/test_diagnose_run_command.py`
7. updated docs listed above

## Rollback

1. Revert only the progress-evidence change and tests.
2. Keep `P1.6o190` invalid-run truth intact.
3. Do not mutate proof artifacts.

## No-go

- no silent pass when scenario generation times out
- no extra proof rerun before deterministic closure of this blocker
- no timeout-budget changes in this block
- no runtime or oracle changes in this block

## Risks / blockers

- the next rerun may still time out, but with better blocker evidence
- progress stderr increases log volume slightly
- another proof-validity blocker may surface once timeout evidence is explicit

## Residual architecture debt (mandatory)

### Current residuals accepted in this block

- proof admission still depends on synchronous subprocess timeout budgeting
- guarded rerun remains blocked until post-fix proof evidence exists

### Why not in this block

- this slice is only to upgrade timeout RCA evidence, not to change timeout math before the blocker is proven

### Risk if deferred

- the worktree stays blocked on an opaque proof failure and risks routing the wrong next structural fix

### Linked follow-up Task Package(s)

- next proof-lane or timeout-budget TP after deterministic closure of `P1.6o191`

### Expiry/trigger to stop deferral

- immediately after deterministic closure and one fresh guarded rerun classification

## Next-block contract (mandatory)

### Next block objective

- run exactly one fresh guarded rerun after `P1.6o191` so proof surfaces either precise timeout progress evidence or the next admissible semantic/proof blocker.

### First deterministic check command

- `python3 -m py_compile ops/diagnose.py truffles-api/tests/test_diagnose_run_command.py && pytest -q truffles-api/tests/test_diagnose_run_command.py -k "scenario_timeout or generate_batch"`

### Blocked-by conditions

- `P1.6o191` deterministic closure is not green
- `P1.6o190` invalid-run truth is not synced
- targeted tooling tests are not green

### Owner role for closure

- `Top Architect + Brain`

## Execution result

- `ops/diagnose.py` now enables scenario-generation progress stderr by default for generated LLM proof runs, while keeping timeout behavior unchanged.
- Added targeted tooling coverage in `truffles-api/tests/test_diagnose_run_command.py` for default progress pass-through and last-progress timeout normalization.
- Targeted validation is green:
  - `python3 -m py_compile ops/diagnose.py truffles-api/tests/test_diagnose_run_command.py`
  - `pytest -q truffles-api/tests/test_diagnose_run_command.py -k "scenario_timeout or generate_batch"` -> `3 passed, 17 deselected in 4.19s`
  - `git diff --check`
  - `SESSION_AGENT=a1 scripts/session_check.sh`
- Published `P1.6o192` as the next admissible proof lane after deterministic closure of this observability slice.

## Execution status

- `done`

# TP-2026-03-09-p1.6o51-question-like-specialist-followup-collect-owner-a1.md

- Название/цель: закрыть surfaced `r60` blocker, где question-like named-specialist follow-up `Есть ли возможность сделать это у Айгерим?` под активным `expected_reply_type=time` дрейфует между `master info` и generic collect, хотя контракт уже знает `pending_question_target=specialist`.
- Parent TP: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Canon refs: `AGENTS.md`, `STATE.md`, `STRUCTURE.md`, `STRATEGY/REQUIREMENTS.md`, `docs/runbooks/BOOKING_CONFIRM_VERIFY.md`, `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o50-daypart-constraint-hours-info-block-owner-a1.md`
- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect remove worktree + branch after merge`

## One web search (mandatory before implementation)

- Query: `Rasa forms unhappy path questions during slot filling official docs`
- Time (UTC): `2026-03-09T09:26:00Z`
- Sources:
  - `https://rasa.com/docs/rasa/forms/`
- Ready solutions found:
  - question-like follow-ups during active slot collection should stay inside the slot-collection contract when the form/slot owner already knows what slot is active;
  - interruption routing should consult the active slot owner before handing control to generic FAQ/info handlers.
- Decision (`reuse/integrate/build`): `reuse + integrate`
  - reuse existing `pending_question_target=specialist` semantic contract and existing `booking_specialist_followup` owner;
  - integrate the missing collect-vs-info guard so named specialist follow-ups do not get rewritten to generic `master info` before the collect owner runs;
  - build no new phrase router in runtime core.
- Rejected options:
  - teaching the runtime specific phrases like `есть ли возможность` or `можно к мастеру`;
  - weakening scenario/oracle to accept either `master info` or collect for the same contract;
  - pushing the fix into post-hoc trace/meta rewrite after the info owner already answered.

## Root cause (mandatory)

- Symptom:
  - guarded `dev L2` run `p1.6o6-l2-dev-20260309-a1-r60` fails on dialog 1 turn 4 with `expected_info_section_miss`, `info_section_miss`, `judge_fail` for `Есть ли возможность сделать это у Айгерим?`.

- Minimal reproduction:
  1. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r60/scenarios.json` dialog 1 turn 4 and confirm stale `master` info expectation;
  2. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r60/responses.jsonl` turn 4 and confirm runtime keeps `pending_question_target=specialist` but still answers with a generic booking prompt;
  3. run `pytest -q truffles-api/tests/test_message_endpoint.py -k "question_like_specialist_followup_acknowledges or named_master_question_rewrites_collect_to_master_info or choose_specialist_question_rewrites_collect_to_master_info or bare_named_specialist_question_rewrites_collect_to_master_info" -vv` and confirm the question-like specialist follow-up used to rewrite into `master info` in the mocked path.

- Evidence:
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r60/summary.json`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r60/brief.md`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r60/scenarios.json`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r60/responses.jsonl`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r60/trace_bundle.jsonl`
  - `truffles-api/app/routers/webhook/decision.py`
  - `truffles-api/tests/test_message_endpoint.py`
  - `truffles-api/tests/test_booking_dialog_scenarios_script.py`

- Five Whys:
  1. Why did the turn fail: scenario and one mocked runtime path still treated the follow-up as `master info`, while the active booking contract expected collect continuation.
  2. Why did the mocked path rewrite to info: collect-vs-info arbitration looked only at generic info refs and ignored the already-resolved specialist follow-up contract.
  3. Why was the specialist contract ignored: `_resolve_policy_collect_interrupt_arbitration(...)` had no input for `pending_question_target` or semantic specialist preference.
  4. Why did this become architectural instead of phrase-fitting: the missing condition is not one surface phrase; it is the absent contract boundary between generic info interrupts and named specialist follow-ups inside active collect.
  5. Why is a runtime fix required in addition to taxonomy sync: without the boundary fix, tests and future edge paths can still regress into `master info` before the bounded specialist owner runs.

- Root cause statement:
  - collect-vs-info arbitration ran before the booking specialist-followup owner and did not consider `pending_question_target=specialist` plus resolved specialist preference, so named specialist follow-ups could still be rewritten to generic `master info`.

- Fix mechanism:
  - pass `pending_question_target` and semantic specialist preference into collect-vs-info arbitration;
  - preserve collect ownership when the active turn is a named specialist follow-up;
  - add deterministic regressions for the runtime path and the stateful scenario normalization sequence.

## Release safety

- Strategy: bounded collect-owner guard only
- Go signals:
  - named specialist follow-up under active time collect stays in `booking_specialist_followup`
  - question-like response acknowledges the specialist preference and resumes `time` collect without availability claims
- No-go signals:
  - generic `master info` still wins for the same contract
  - new phrase-branches appear in runtime core
- Rollback verification:
  - revert only the collect-arbitration guard and the targeted regressions if it suppresses legitimate generic master-info turns

## Invariant

1. Generic `choose specialist` info questions without a concrete specialist preference remain eligible for `master info`.
2. Named specialist follow-up stays a booking-state operation, not a post-hoc trace rewrite.
3. No live specialist availability claim is introduced without calendar evidence.

## Scope

- `truffles-api/app/routers/webhook/decision.py`
- `truffles-api/tests/test_message_endpoint.py`
- `truffles-api/tests/test_booking_dialog_scenarios_script.py`
- tracker docs after rerun handoff

## Out of scope

- broad `master info` interrupt redesign
- specialist calendar availability answers without tool evidence
- acceptance/full proof closure beyond the fresh guarded dev rerun

## Touch-list

- `truffles-api/app/routers/webhook/decision.py`
- `truffles-api/tests/test_message_endpoint.py`
- `truffles-api/tests/test_booking_dialog_scenarios_script.py`
- `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o51-question-like-specialist-followup-collect-owner-a1.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`

## Plan (1..N)

1. Freeze `r60` as the bounded named-specialist follow-up blocker.
2. Preserve collect ownership in the existing arbitration boundary when semantic contract already resolved a concrete specialist target.
3. Add deterministic runtime + scenario regressions.
4. Run focused checks and `git diff --check`.
5. Run one fresh guarded dev rerun beyond the `r60` family.

## DoD

1. Question-like named-specialist follow-up under active `time` collect stays in `booking_specialist_followup`.
2. Focused deterministic regressions pass.
3. One fresh guarded rerun no longer stops on the `r60` family.

## Checks

- `pytest -q truffles-api/tests/test_message_endpoint.py -k "question_like_specialist_followup_acknowledges or booking_specialist_followup_persists_preference or choose_specialist_question_rewrites_collect_to_master_info or named_master_question_rewrites_collect_to_master_info or bare_named_specialist_question_rewrites_collect_to_master_info"`
- `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "named_specialist_master or partial_date or grounded_time or pending_question"`
- `pytest -q truffles-api/tests/test_booking_quality_response_guard.py -k "pending_question_interaction or booking_slot_stall or specialist_followup"`
- `git diff --check`

## Execution status

- `done`
- collect-vs-info arbitration now preserves named specialist follow-up collect ownership, deterministic regressions are green, and guarded `r62` no longer stops on the `r60` family.

## Evidence

1. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r60/summary.json`
2. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r60/brief.md`
3. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r60/scenarios.json`
4. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r60/responses.jsonl`
5. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r60/trace_bundle.jsonl`
6. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r62/summary.json`
7. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r62/brief.md`
8. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r62/scenarios.json`
9. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r62/responses.jsonl`
10. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r62/trace_bundle.jsonl`
11. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r62/manual_audit.md`

## Rollback

1. Revert only the collect-arbitration semantic guard and targeted regressions.
2. Keep `P1.6o6` blocked; do not reinterpret `r60` as closure evidence without `r62`.

## No-go

- no phrase-hardcode for `Айгерим`/`есть ли возможность`
- no generic `master info` waiver for named specialist follow-ups
- no availability claim without tool evidence

## Risks/Blockers

1. The next surfaced family can still be scenario-governance drift rather than runtime.
2. Acceptance remains non-canonical until a full guarded proof chain completes.

## Residual architecture debt (mandatory)

- Current residuals accepted in this block:
  - reschedule/governance follow-up state can still drift after a hard handoff pivot
- Why not in this block:
  - this slice only closes named specialist collect ownership, not management-state governance
- Risk if deferred:
  - future guarded runs can still fail on scenario-state drift after `reschedule`
- Linked follow-up Task Package(s):
  - `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o52-reschedule-followup-service-choice-drift-a1.md`
- Expiry/trigger to stop deferral:
  - first fresh guarded rerun after this block that stops on post-reschedule state drift

## Next-block contract (mandatory)

- Next block objective:
  - freeze and close the post-reschedule scenario-governance drift surfaced by `r62`
- First deterministic check command:
  - `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "reschedule or pending_question or named_specialist_master" && git diff --check`
- Blocked-by conditions:
  - `r62` audit is not reflected in tracker docs
  - fresh deterministic governance fix is not green
  - next guarded rerun preflight is invalid
- Owner role for closure:
  - `Hands` for deterministic scenario-governance closure, `Top Architect + Brain` for proof-bundle promotion

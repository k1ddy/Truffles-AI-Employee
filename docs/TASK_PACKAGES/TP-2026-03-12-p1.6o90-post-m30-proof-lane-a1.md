# TP-2026-03-12-p1.6o90-post-m30-proof-lane-a1

- Название/цель: после deterministic closure `P1.6o89` выполнить один fresh guarded `dev L2`, чтобы доказать, что `M30` больше не является first fail, и truthfully surface-ить следующую remaining family, если она есть.

## Canon refs

- Parent closure program: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Main remediation: `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- Prior proof lane: `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o88-post-m29-proof-lane-a1.md`
- Deterministic closure prerequisite: `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o89-slot-constraint-generic-free-slot-question-governance-a1.md`
- Owner matrix baseline: `truffles-api/app/knowledge/generic/INTERACTION_OWNER_MATRIX.yaml`
- State baseline: `STATE.md`

## Git context

- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect` remove branch + worktree after merge

## One web search (mandatory before implementation)

- Query: `site:www.uvicorn.org/settings uvicorn host port app-dir env-file`
- Date/time: `2026-03-12T12:30:05+05:00`
- Sources opened:
  - official Uvicorn settings docs: `https://www.uvicorn.org/settings/`
- Source quality:
  - official product documentation
- Found reusable ideas:
  - CLI flags such as `--host`, `--port`, and `--app-dir` remain the canonical way to bind the server for a local proof run;
  - CLI arguments take precedence over environment variables;
  - `--env-file` configures the ASGI application environment, not `UVICORN_*` server settings.
- Decision:
  - `reuse`
  - keep the proof runtime startup explicit and deterministic with CLI `host/port/app-dir` flags inside the worktree shell session.
- Rejected options:
  - detached launch patterns that make runtime fingerprint validation ambiguous;
  - implicit `UVICORN_*` configuration through env files for the server process.

## Root cause (mandatory)

- Symptom:
  - after `P1.6o89`, deterministic contour is green, but there is still no fresh proof run showing that `M30` disappeared from the first-fail position.
- Minimal reproduction:
  1. Use the last fresh run `p1.6o88-l2-dev-20260312-a1-r1` as the pre-fix baseline.
  2. Confirm it failed first on dialog `1`, turn `2`, text `Какое время доступно?`.
  3. Confirm `P1.6o89` now deterministically rewrites that family to canonical `ask_about_requested_slot(time)`.
  4. Run one fresh guarded `dev L2` on a live runtime whose `/admin/version` matches current `HEAD`.
  5. Audit the resulting run strictly and compare the new first fail, if any, against the pre-fix surfaced row.
- Evidence:
  - `/tmp/booking_quality/p1.6o88-l2-dev-20260312-a1-r1/summary.json`
  - `/tmp/booking_quality/p1.6o88-l2-dev-20260312-a1-r1/scenarios.json`
  - `/tmp/booking_quality/p1.6o88-l2-dev-20260312-a1-r1/responses.jsonl`
  - `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o89-slot-constraint-generic-free-slot-question-governance-a1.md`
- Five Whys:
  1. Why is closure still incomplete? Because bounded deterministic closure alone does not prove the expensive lane rolled past `M30`.
  2. Why is a proof lane needed? Because the quality chain admits closure only when a fresh guarded run confirms the surfaced family is no longer first.
  3. Why can we run it now? Because `P1.6o89` removed the stale scenario-governance contract on deterministic contour.
  4. Why must the runtime be fingerprinted first? Because preflight/runtime mismatch makes the run non-canonical.
  5. Why only one run? Because the goal is admission of the next family, not another reactive multi-run loop.
- Root cause statement:
  - the remaining gap after `P1.6o89` is proof, not implementation: without one fresh guarded `dev L2` on a verified live runtime, `M30` remains only deterministically closed and the next remaining family stays unknown.
- Fix mechanism:
  - publish this proof-lane TP;
  - start a live worktree runtime on `:18184` and prove `/admin/version == HEAD`;
  - execute exactly one guarded `dev L2` run and strict audit;
  - sync docs/state/matrix truthfully based on the new first-fail outcome.

## Reuse-first decision (mandatory)

- Reuse:
  - existing guarded entrypoint `scripts/llm_quality_guarded.sh`
  - existing audit command `python3 ops/diagnose.py llm-quality-audit`
  - existing live runtime startup pattern used in `P1.6o84`, `P1.6o86`, and `P1.6o88`
- Integrate:
  - no runtime code changes unless the proof run surfaces a new deterministic or bounded family that requires a separate TP
- Build:
  - none planned in this block

## Release safety

- Rollout: none; local proof-only block
- Go signal:
  - runtime fingerprint matches `HEAD`
  - guarded run artifacts are valid enough to audit (`artifact_integrity.valid=true`)
  - first fail is no longer the `M30` row
- No-go signal:
  - invalid runtime fingerprint preflight
  - incomplete/invalid guarded artifacts
  - fresh first fail remains the same `M30` family
- Rollback:
  - stop the proof lane, record evidence truthfully, do not reinterpret invalid/incomplete runs as closure evidence

## Invariant

1. `M29` stays boundedly cleared by `P1.6o87` + `P1.6o88`.
2. `M30` stays deterministically closed by `P1.6o89`.
3. No runtime/product contract weakening.
4. Exactly one fresh guarded `dev L2` run in this block.

## Scope

- proof-lane runtime startup + fingerprint validation
- one guarded `dev L2`
- strict audit
- doc/state/session sync for the resulting admission

## Out of scope

- new runtime implementation before the proof run surfaces a new row
- acceptance `lock/replay/full`
- matrix redesign

## Touch-list

- `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o90-post-m30-proof-lane-a1.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `STATE.md`
- `STRUCTURE.md`
- `docs/SESSIONS/SESSION-2026-02-19-llm-first-firebreak-a1.md`
- `truffles-api/app/knowledge/generic/INTERACTION_OWNER_MATRIX.yaml`
- `truffles-api/tests/test_interaction_owner_matrix_contract.py`

## Plan (1..N)

1. Publish `P1.6o90` with proof-lane scope and runtime-fingerprint requirements.
2. Re-run the deterministic prerequisite check from `P1.6o89`.
3. Start live worktree runtime on `:18184` and prove `/admin/version == HEAD`.
4. Execute exactly one fresh guarded `dev L2` and strict audit.
5. Sync matrix/docs/state/session with truthful post-run admission.

## DoD

1. Fresh guarded `dev L2` is executed on a fingerprint-verified live runtime.
2. `M30` is no longer the first fail family.
3. The next first fail, if any, is captured truthfully with artifacts.
4. Docs/state/session are synced to the new admission truth.

## Checks

- `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "slot_constraint and requested_slot"`
- `pytest -q truffles-api/tests/test_interaction_owner_matrix_contract.py`
- `TEST_MODE=1 scripts/llm_quality_guarded.sh --mode lock --run-id p1.6o90-l2-dev-20260312-a1-r1 --allow-pending-previous --allow-repeat-fingerprint -- --base-url http://127.0.0.1:18184 --client-slug demo_salon --mode llm --count 10 --min-turns 10 --max-turns 15 --include-media --scenario-coverage booking,info,interrupt,handoff --tool-hooks auto --jid-mode unique --allow-non-allowlist --judge-mode all --quality-lane dev --run-economy-gate block --fail-on-thresholds --max-failures 1 --reset-before-dialog`
- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o90-l2-dev-20260312-a1-r1 --status done --strict-artifacts`
- `git diff --check`
- `SESSION_AGENT=a1 scripts/session_check.sh`

## Execution status

- `done`

## Evidence

1. `git rev-parse HEAD` -> `21afcead084aa3fdfc5702c5547fb40d74970b5a`
2. `curl -sf http://127.0.0.1:18184/admin/version` -> `{"version":"fix/llm-first-firebreak-2026-02-19","git_commit":"21afcead084aa3fdfc5702c5547fb40d74970b5a",...}`
3. `/tmp/booking_quality/p1.6o90-l2-dev-20260312-a1-r1/summary.json`
4. `/tmp/booking_quality/p1.6o90-l2-dev-20260312-a1-r1/brief.md`
5. `/tmp/booking_quality/p1.6o90-l2-dev-20260312-a1-r1/scenarios.json`
6. `/tmp/booking_quality/p1.6o90-l2-dev-20260312-a1-r1/responses.jsonl`
7. `/tmp/booking_quality/p1.6o90-l2-dev-20260312-a1-r1/trace_bundle.jsonl`
8. `/tmp/booking_quality/p1.6o90-l2-dev-20260312-a1-r1/manual_audit.md`
9. `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o90-l2-dev-20260312-a1-r1 --status done --strict-artifacts`

## Proof result

- `M30` is no longer the first fail family.
- Fresh guarded `dev L2` `p1.6o90-l2-dev-20260312-a1-r1` stopped first at dialog `1`, turn `5`, user text `Можно записаться к Айгерим?`.
- This is not a new matrix family; it truthfully reopens existing `M19`.
- Runtime evidence:
  - `policy_core_mode=degraded_fallback`
  - `policy_core_degrade_reason=policy_error:invalid_schema`
  - `specialist_hint decision=empty`
  - final response falls back to generic `booking_prompt`
  - final meta drops `pending_question_target=specialist` and `active_question_relation=referent_followup`
- Contract-first evaluation fails on `expected_meta_mismatch` + `expected_trace_miss`.
- Strict audit result:
  - `artifact_integrity.valid=true`
  - `infra_valid=false`
  - `semantic_valid=false`
  - `stop_reason=max_failures_reached:1`

## Rollback

1. Stop the local runtime and keep `P1.6o89` deterministic closure intact.
2. Keep `M19` reopened truthfully until deterministic re-closure is synced.
3. Do not reinterpret `p1.6o90-l2-dev-20260312-a1-r1` as evidence for any family other than `M30 cleared / M19 reopened`.

## No-go

- no second expensive run in this block
- no relabeling of reopened `M19` as a brand new family when the matrix already predicted it
- no default-path runtime patch outside a separate bounded child TP

## Risks / blockers

- reopened `M19` is a runtime closure gap, not a scenario-governance issue
- proof lane remains non-canonical for acceptance while `infra_valid=false` and `semantic_valid=false`

## Residual architecture debt (mandatory)

### Current residuals accepted in this block

- closure proof is still on the `dev L2` lane only; `lock/replay/full` remain blocked
- invalid-schema specialist recovery still depends on `decision.py` boundary logic rather than a dedicated owner handler

### Why not in this block

- this block only admits the next remaining family after `M30` proof clearance

### Risk if deferred

- reopened `M19` stays unowned truthfully and the closure lane stops moving

### Linked follow-up Task Package(s)

- `P1.6o91`
- `P1.6o92`
- `P1.7` acceptance replay gate after closure-program proof lane is green enough to advance

### Expiry/trigger to stop deferral

- before any next guarded `lock/replay/full`

## Next-block contract (mandatory)

### Next block objective

- boundedly re-close reopened `M19` by adding deterministic branch-catalog specialist recovery before the next proof lane.

### First deterministic check command

- `pytest -q truffles-api/tests/test_message_endpoint.py -k "branch_catalog_match or invalid_schema_question_like_named_specialist_followup_keeps_time_collect" && pytest -q truffles-api/tests/test_booking_quality_response_guard.py -k "invalid_schema_specialist_followup or specialist_followup_owner_gap" && git diff --check`

### Blocked-by conditions

- `P1.6o90` proof evidence is not synced truthfully into active docs and matrix state
- reopened `M19` is not classified as the same predicted family before implementation
- `P1.6o91` deterministic closure is not green before publishing `P1.6o92`

### Owner role for closure

- `Top Architect + Brain`

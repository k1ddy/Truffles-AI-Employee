# TP-2026-03-06-p1.6b-info-followup-hardcode-removal-a1

- Название/цель: убрать phrase hardcode из info followup path для parking-capacity carryover, сохранив поведение как data-driven signal contract, а не core keyword branch.
- Parent TP: `docs/TASK_PACKAGES/TP-2026-03-06-p1.6-local-realism-canonical-lock-a1.md`
- Canon refs: `AGENTS.md`, `STATE.md`, `STRATEGY/REQUIREMENTS.md`, `TECH.md`
- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`

## One web search (mandatory before implementation)

- Query: `Martin Fowler Table Driven Method replace conditional business rules`
- Time (UTC): `2026-03-06T04:00:00Z`
- Sources:
  - `martinfowler.com` search result on data-driven / adaptive model patterns
- Ready solutions found:
  - Domain-specific branching should move from code conditionals into data/config structures when the domain evolves faster than the runtime shell.
- Decision (`reuse/integrate/build`): `reuse + integrate`
  - Reuse existing signal lexicon/data path instead of keeping a raw phrase literal in core.
  - Integrate a signal-helper that consumes lexicon data from packs/system data.
- Rejected options:
  - Keep literal `"мест"` in core followup branch.
  - Silence the hardcode gate or whitelist this branch.

## Root cause (mandatory)

- Symptom:
  - `P1.6` realism run `booking-dev-p1.6-20260306-a1-r6` is blocked by `hardcode_core_violation`.

- Minimal reproduction:
  1. `python3 - <<'PY'\nimport json\np='/tmp/booking_quality/booking-dev-p1.6-20260306-a1-r6/summary.json'\nprint(json.load(open(p))['hardcode_core_gate'])\nPY`
  2. `nl -ba truffles-api/app/routers/webhook/info.py | sed -n '987,1005p'`
  3. `pytest -q truffles-api/tests/test_demo_salon_eval.py -k parking_capacity_followup`

- Evidence:
  - `/tmp/booking_quality/booking-dev-p1.6-20260306-a1-r6/summary.json`
    - `hardcode_core_gate.reasons=["core_phrase_branching_detected"]`
    - violation path `truffles-api/app/routers/webhook/info.py`
  - `truffles-api/app/routers/webhook/info.py:1000`
    - carryover parking followup still depends on raw phrase literal `"мест"`.
  - `truffles-api/tests/test_demo_salon_eval.py:1413`
    - existing contract proves the business behavior we must preserve: `"Сколько обычно мест?"` should keep `parking` section in followup.

- Five Whys:
  1. Почему gate сработал: core file still contains raw phrase branching.
  2. Почему именно этот branch problematic: it encodes Russian parking-capacity semantics in code.
  3. Почему это против LLM-first/business-agnostic: language/domain tokens must live in data packs/system lexicons, not runtime core.
  4. Почему нельзя просто удалить branch: parking-capacity followup is a real product behavior already covered by acceptance tests.
  5. Почему правильный fix — data-driven signal: it preserves behavior while moving semantics out of code and into pack/system data.

- Root cause statement:
  - Parking-capacity carryover detection remained implemented as a literal Russian substring check inside core followup arbitration, instead of using the existing signal lexicon/data path.

- Fix mechanism:
  - Replace raw substring branching with a token-prefix signal helper fed by signal lexicons.
  - Store the language token in `SYSTEM_LEXICONS.yaml`/signal data, not in core code.
  - Keep existing followup contract green with targeted tests.

## Invariant

1. `parking` carryover followup behavior stays intact for `"Сколько обычно мест?"`.
2. No new phrase/regex hardcode is added to core files.
3. Domain/language markers live in signal data, not in runtime branching code.

## Scope

- `truffles-api/app/routers/webhook/info.py`
- `truffles-api/app/services/info_signal_service.py`
- `truffles-api/app/knowledge/generic/SYSTEM_LEXICONS.yaml`
- `truffles-api/tests/test_demo_salon_eval.py`
- `truffles-api/tests/test_cross_domain_signal_contract_suite.py`
- `docs/TASK_PACKAGES/TP-2026-03-06-p1.6-local-realism-canonical-lock-a1.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`

## Out of scope

- Multi-intent timeout tuning.
- Trace retention limit tuning.
- `P1.6` realism rerun itself.
- `test_derive_policy_info_refs_accepts_slot_style_hours_hint` fix unless this block directly proves it is the same root cause.

## Plan (1..N)

1. Add a data-driven token-prefix signal helper for followup markers.
2. Move parking-capacity followup token into `SYSTEM_LEXICONS.yaml`.
3. Replace raw phrase literal in `info.py` with the helper.
4. Add/adjust targeted tests for helper behavior and existing parking-capacity contract.
5. Re-run targeted tests and document whether hardcode blocker is now cleared.

## DoD

1. `truffles-api/app/routers/webhook/info.py` no longer contains raw parking-capacity phrase literal.
2. Parking-capacity followup still keeps `parking` info section.
3. New helper behavior is covered by deterministic tests, including a non-match case.
4. `git diff --check` is clean for touched files.

## Checks

- `pytest -q truffles-api/tests/test_cross_domain_signal_contract_suite.py -k parking`
- `pytest -q truffles-api/tests/test_demo_salon_eval.py -k parking_capacity_followup`
- `ruff check truffles-api/app/routers/webhook/info.py truffles-api/app/services/info_signal_service.py truffles-api/tests/test_cross_domain_signal_contract_suite.py`

## Execution status

- `done`
- Implemented:
  - `truffles-api/app/services/info_signal_service.py`
    - added `signal_token_prefix_match(...)` for token-aware, data-driven prefix detection
  - `truffles-api/app/routers/webhook/info.py`
    - removed raw phrase branch on `"мест"` and switched parking-capacity followup to the signal helper
  - `truffles-api/app/knowledge/generic/SYSTEM_LEXICONS.yaml`
    - added `parking_capacity_followup_prefixes`
  - `truffles-api/tests/test_cross_domain_signal_contract_suite.py`
    - added positive/negative contract for parking-capacity prefix matching
- Deterministic evidence:
  - `ruff check truffles-api/app/routers/webhook/info.py truffles-api/app/services/info_signal_service.py truffles-api/tests/test_cross_domain_signal_contract_suite.py` -> `All checks passed!`
  - `pytest -q truffles-api/tests/test_cross_domain_signal_contract_suite.py -k parking` -> `1 passed, 2 deselected`
  - `pytest -q truffles-api/tests/test_demo_salon_eval.py -k parking_capacity_followup` -> `1 passed, 16 deselected`
  - `rg -n '"мест" in normalized_followup' truffles-api/app/routers/webhook/info.py` -> no matches
  - `git diff --check -- truffles-api/app/routers/webhook/info.py truffles-api/app/services/info_signal_service.py truffles-api/app/knowledge/generic/SYSTEM_LEXICONS.yaml truffles-api/tests/test_cross_domain_signal_contract_suite.py docs/TASK_PACKAGES/TP-2026-03-06-p1.6b-info-followup-hardcode-removal-a1.md docs/TASK_PACKAGES/TP-2026-03-06-p1.6-local-realism-canonical-lock-a1.md docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md` -> clean
  - direct hardcode gate recheck via `ops.diagnose` -> `valid=True`, `violations=[]`

## Evidence

1. Code diff removing the raw phrase branch.
2. Targeted test output for helper + eval contract.
3. Updated parent TP status showing hardcode blocker progress.

## Rollback

1. Revert helper + lexicon + test changes from this sub-TP.
2. If parking-capacity followup regresses, stop and do not continue to `P1.6` realism rerun.

## No-go

- No whitelist/waiver for `hardcode_core_gate`.
- No reintroducing literal language tokens elsewhere in core as replacement.
- No unrelated timeout/tuning changes in the same block.

## Risks/Blockers

1. A too-broad lexicon token can create false positives, so helper must be token-aware, not naive substring-only.
2. Other latent hardcodes may remain elsewhere, but this block only closes the confirmed `info.py` violation.

## Residual architecture debt (mandatory)

- Current residuals accepted in this block:
  - multi-intent timeout pressure under realism
  - trace retention overflow under long dialogs
  - possible follow-up on `test_derive_policy_info_refs_accepts_slot_style_hours_hint`
- Why not in this block:
  - One runtime blocker at a time.
- Risk if deferred:
  - `P1.6` cannot rerun canonically until the confirmed hardcode blocker is closed.
- Linked follow-up Task Package(s):
  - `docs/TASK_PACKAGES/TP-2026-03-06-p1.6-local-realism-canonical-lock-a1.md`
- Expiry/trigger to stop deferral:
  - Before the next `P1.6` realism rerun.

## Next-block contract (mandatory)

- Next block objective:
  - Return to `P1.6` realism preflight and confirm `hardcode_core_gate` is no longer blocking.
- First deterministic check command:
  - `rg -n '\"мест\" in normalized_followup' truffles-api/app/routers/webhook/info.py`
- Blocked-by conditions:
  - `P1.6b` tests red.
- Owner role for closure:
  - `Top Architect + Brain`

# TP-2026-03-13-p1.6o149-partial-date-availability-slot-constraint-governance-a1

- Название/цель: закрыть surfaced `Track A` scenario-governance drift после proof lane `P1.6o148`, где partial-date availability probe `Есть ли у вас время на завтра?` всё ещё генерируется как `slot_compare(time)`, хотя runtime канонически держит turn как bounded `slot_constraint(time)` под активным `time` collect.

## Canon refs

- Binding roadmap: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o135-beauty-salon-canonical-control-plane-roadmap-a1.md`
- Prior proof lane: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o148-post-p1.6o147-proof-lane-a1.md`
- Historical partial-date normalization: `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o33-partial-date-mixed-question-normalization-a1.md`
- Historical generic slot-compare normalization: `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o101-slot-compare-generic-free-slot-question-governance-a1.md`
- Runtime truth for grounded slot-constraint degrade: `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o69-time-window-slot-constraint-collect-owner-a1.md`
- Class-level routing: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o118-class-level-closure-process-reset-a1.md`
- Parent closure program: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Main remediation: `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- State baseline: `STATE.md`

## Git context

- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `origin/main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect` remove branch + worktree after merge

## One web search (mandatory before implementation)

- Query: `site:developers.openai.com/api/docs/guides trace grading evaluation best practices intermediate decisions`
- Date/time: `2026-03-13T17:31:00+05:00`
- Sources opened:
  - official OpenAI eval docs: `https://developers.openai.com/api/docs/guides/evaluation-best-practices`
  - official OpenAI trace grading docs: `https://developers.openai.com/api/docs/guides/trace-grading`
- Source quality:
  - official vendor documentation
- Found reusable ideas:
  - eval fixes should be judged on comparable runs and traced intermediate decisions, not only on final response text;
  - when judge and runtime disagree, generator/oracle should converge on the trace-backed contract before opening a new runtime patch.
- Decision:
  - `reuse + integrate`
  - reuse the existing trace/meta contract from `P1.6o148` and integrate one bounded generator normalization so partial-date availability probes stop overclaiming `slot_compare` when runtime canonically emits `slot_constraint`.
- Rejected options:
  - patching runtime to imitate stale scenario taxonomy;
  - weakening oracle to accept both `slot_compare` and `slot_constraint` for the same surfaced contour;
  - opening another boundary/runtime slice before closing this generator drift.

## Root cause (mandatory)

- Symptom:
  - fresh guarded `dev L2` `p1.6o148-l2-dev-20260313-a1-r1` no longer stops first on the covered `P1.6o147` timeout slot-question continuity family, but now fails on dialog `1`, turn `3`, text `Есть ли у вас время на завтра?` with `expected_meta_mismatch` / `expected_trace_miss` because generated expectations still require `slot_compare(time)` while runtime canonically emits `slot_constraint(time)`.
- Minimal reproduction:
  1. inspect `/tmp/booking_quality/p1.6o148-l2-dev-20260313-a1-r1/scenarios.json` dialog `1`, turn `3`;
  2. confirm the generated turn is tagged `slot_compare` and expects `pending_question_act=slot_compare` plus `pending_question_interaction slot_compare/time`;
  3. inspect `/tmp/booking_quality/p1.6o148-l2-dev-20260313-a1-r1/responses.jsonl` dialog `1`, turn `3`;
  4. confirm runtime produces `action=booking_prompt`, `source=policy_core_guard`, `expected_reply_type=time`, `pending_question_act=slot_constraint`, `pending_question_target=time`, `pending_question_interaction=slot_constraint`, `answer_slot=datetime`, `answer_value=завтра`, and `policy_core_degrade_reason=policy_validation:collect_slot_order_invalid`;
  5. inspect `/tmp/booking_quality/p1.6o148-l2-dev-20260313-a1-r1/trace_bundle.jsonl` dialog `1`, turn `3` and confirm `question_contract decision=matched` plus `pending_question_interaction decision=slot_constraint` before degraded collect;
  6. inspect `scripts/booking_dialog_scenarios.py` and confirm there is no bounded normalization that converts partial-date availability probes mis-tagged as `slot_compare` into canonical `slot_constraint` under active `time` collect.
- Evidence:
  - `/tmp/booking_quality/p1.6o148-l2-dev-20260313-a1-r1/summary.json`
  - `/tmp/booking_quality/p1.6o148-l2-dev-20260313-a1-r1/manual_audit.json`
  - `/tmp/booking_quality/p1.6o148-l2-dev-20260313-a1-r1/scenarios.json`
  - `/tmp/booking_quality/p1.6o148-l2-dev-20260313-a1-r1/responses.jsonl`
  - `/tmp/booking_quality/p1.6o148-l2-dev-20260313-a1-r1/trace_bundle.jsonl`
  - `scripts/booking_dialog_scenarios.py`
  - `truffles-api/tests/test_booking_dialog_scenarios_script.py`
- Five Whys:
  1. Why did the run fail? Because scenario meta/trace still expected `slot_compare(time)` while runtime emitted `slot_constraint(time)`.
  2. Why did scenario expect that? Because the generated turn stayed tagged `slot_compare` even though the utterance grounded only a partial date and asked availability relative to that same active slot.
  3. Why was it not normalized away? Because current sanitizer rewrites generic free-slot `slot_compare` to `ask_about_requested_slot` and rewrites already-tagged `slot_constraint` partial-date fills, but has no bounded `slot_compare -> slot_constraint` rule for partial-date availability probes.
  4. Why is runtime already ahead? Because question-contract canonically records the turn as a partial datetime constraint (`answer_slot=datetime`, `answer_value=завтра`) under active `time` collect, then degrades only on downstream slot-order validation.
  5. Why is this Track A and not another boundary/runtime slice? Because judge passes the text and runtime meta/trace are internally coherent; the remaining defect is stale generator taxonomy.
- Root cause statement:
  - the scenario generator lacks a bounded normalization that rewrites partial-date availability probes mis-tagged as `slot_compare` into canonical `slot_constraint(time)` under active `time` collect, so stale compare expectations survive after runtime has already preserved the slot-constraint contract.
- Fix mechanism:
  - add one bounded generator normalization for partial-date availability probes under active `time` collect that converts `slot_compare` to `slot_constraint` only when the turn grounds a partial date without opening a genuine temporal comparison family;
  - add deterministic sanitize + post-coverage repair regressions for positive and negative cases;
  - keep runtime untouched and publish the next proof lane only after deterministic closure is green.

## Reuse-first decision (mandatory)

- Reuse:
  - existing partial-date and slot-compare helper patterns in `scripts/booking_dialog_scenarios.py`
  - existing sanitize/repair deterministic harness in `truffles-api/tests/test_booking_dialog_scenarios_script.py`
- Integrate:
  - one bounded `slot_compare -> slot_constraint` normalization for partial-date availability probes
  - targeted sanitize/repair regressions
- Build:
  - no runtime changes planned in this block

## Release safety

- Rollout: deterministic scenario-governance only; no production runtime change
- Go signal:
  - partial-date availability probes like `Есть ли у вас время на завтра?` canonicalize to `slot_constraint(time)` under active `time` collect
  - genuine compare turns with explicit alternatives remain `slot_compare`
  - `git diff --check` and `SESSION_AGENT=a1 scripts/session_check.sh` remain green
- No-go signal:
  - generator rewrites legitimate compare turns into `slot_constraint`
  - runtime files become necessary in this block
- Rollback:
  - revert only the new generator normalization and matching deterministic tests/docs

## Invariant

1. `P1.6o147` stays closed.
2. Runtime behavior is untouched in this block.
3. Genuine temporal comparison turns remain `slot_compare`.
4. No oracle weakening.

## Scope

- bounded scenario-governance closure for partial-date availability probes mis-tagged as `slot_compare`
- deterministic tests
- docs/state/session sync for truthful closure routing

## Out of scope

- new guarded `dev L2` in this block
- acceptance `lock/replay/full`
- runtime semantic changes
- matrix-wide ontology redesign

## Touch-list

- `scripts/booking_dialog_scenarios.py`
- `truffles-api/tests/test_booking_dialog_scenarios_script.py`
- `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o148-post-p1.6o147-proof-lane-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o149-partial-date-availability-slot-constraint-governance-a1.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `STATE.md`
- `docs/SESSIONS/SESSION-2026-02-19-llm-first-firebreak-a1.md`
- `docs/SESSION_INDEX.md`

## Plan

1. Publish `P1.6o149` with RCA + one web search + bounded touch-list.
2. Add a bounded normalization for partial-date availability probes that rewrites `slot_compare` to `slot_constraint` under active `time` collect.
3. Add deterministic sanitize/repair regressions for positive and negative cases.
4. Run targeted deterministic checks and hygiene gates.
5. Sync canon truthfully and publish the next admissible proof lane.

## DoD

1. Partial-date availability probes like `Есть ли у вас время на завтра?` canonicalize to `slot_constraint(time)` under active `time` collect.
2. Generated expectations preserve `pending_question_act=slot_constraint`, `pending_question_target=time`, and `expected_reply_type=time` on that family.
3. Genuine compare turns with explicit alternatives remain `slot_compare`.
4. Deterministic tests are green.
5. `P1.6o149` is synced truthfully in active docs/state/session with the next admissible proof block identified.

## Checks

- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o148-l2-dev-20260313-a1-r1 --status done --strict-artifacts`
- `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "partial_date and slot_compare or slot_constraint"`
- `python3 -m py_compile scripts/booking_dialog_scenarios.py truffles-api/tests/test_booking_dialog_scenarios_script.py`
- `git diff --check`
- `SESSION_AGENT=a1 scripts/session_check.sh`

## Evidence

1. `/tmp/booking_quality/p1.6o148-l2-dev-20260313-a1-r1/summary.json`
2. `/tmp/booking_quality/p1.6o148-l2-dev-20260313-a1-r1/brief.md`
3. `/tmp/booking_quality/p1.6o148-l2-dev-20260313-a1-r1/scenarios.json`
4. `/tmp/booking_quality/p1.6o148-l2-dev-20260313-a1-r1/responses.jsonl`
5. `/tmp/booking_quality/p1.6o148-l2-dev-20260313-a1-r1/trace_bundle.jsonl`
6. `/tmp/booking_quality/p1.6o148-l2-dev-20260313-a1-r1/manual_audit.json`

## Rollback

1. Revert only the new generator normalization and matching deterministic docs/tests.
2. Keep `P1.6o148` proof evidence intact.
3. Do not reinterpret this class as runtime defect if deterministic closure is rolled back.

## No-go

- no runtime edits
- no new expensive run before deterministic scenario regression is green
- no weakening oracle to accept contradictory `slot_compare` / `slot_constraint` contracts for the same turn
- no phrase hardcode for one exact sentence only

## Risks / blockers

- partial-date availability probes sit close to genuine compare surfaces, so the bounded rule must avoid collapsing alternative-bearing turns.
- sanitize and post-coverage repair flows must stay aligned or stale expectations will reappear after generation.
- the next surfaced class after this deterministic closure may still be non-canonical if proof integrity remains partial.

## Residual architecture debt (mandatory)

### Current residuals accepted in this block

- Scenario ontology still relies on localized normalization heuristics for pending-question cross-products.
- Proof lane remains partially non-canonical until a fresh rerun completes beyond the observed prefix.

### Why not in this block

- this block is a bounded scenario-governance repair, not a broader compiler/statechart redesign.

### Risk if deferred

- fresh expensive runs will keep stopping on false-red partial-date availability governance and hide the next real family.

### Linked follow-up Task Package(s)

- `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o150-post-p1.6o149-proof-lane-a1.md`

### Expiry/trigger to stop deferral

- before any next guarded `dev L2` / `lock` / `replay`

## Next-block contract (mandatory)

### Next block objective

- publish and run `P1.6o150`: execute one fresh guarded `dev L2` after `P1.6o149` deterministic closure to prove the surfaced partial-date availability `slot_compare` drift is no longer first fail and truthfully surface the next class.

### First deterministic check command

- `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "partial_date and slot_compare or slot_constraint" && python3 -m py_compile scripts/booking_dialog_scenarios.py truffles-api/tests/test_booking_dialog_scenarios_script.py && git diff --check`

### Blocked-by conditions

- `P1.6o149` deterministic closure is not green
- `P1.6o148` truth is not synced into active docs/state/session
- live runtime fingerprint for the next proof lane is not verified against current `HEAD`

### Owner role for closure

- `Top Architect + Brain`

## Execution status

- `done`

# TP-2026-03-08-p1.6o30-slot-constraint-answer-normalization-a1.md

- Название/цель: закрыть новый `r29` deterministic blocker family, где scenario taxonomy маркирует явный slot-answer turn (`Мне подходит утро, например, в 10:00.`) как `slot_constraint`, хотя runtime корректно трактует его как ответ на `expected_reply_type=time` и продвигает collect к `name`.
- Parent TP: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Canon refs: `AGENTS.md`, `STATE.md`, `STRUCTURE.md`, `STRATEGY/REQUIREMENTS.md`, `docs/runbooks/BOOKING_CONFIRM_VERIFY.md`, `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`, `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o29-mixed-slot-question-stale-expectation-scrub-a1.md`
- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect remove worktree + branch after merge`

## One web search (mandatory before implementation)

- Query: `site:rasa.com/docs forms requested_slot slot answer normalization mixed user utterance`
- Time (UTC): `2026-03-08T11:18:30Z`
- Sources:
  - `https://rasa.com/docs/rasa/forms/`
- Ready solutions found:
  - once the user fills the requested slot with a concrete value, the form should treat the turn as slot progress even if the same utterance also contains soft preference language; taxonomy must not force it into an interruption/constraint family when runtime semantics are clearly slot-fill.
- Decision (`reuse/integrate/build`): `reuse + integrate`
  - reuse the existing `question_contract matched` / booking-progress contract;
  - integrate scenario normalization so explicit datetime answers are not preserved as `slot_constraint` family;
  - build no new runtime behavior until deterministic taxonomy is aligned.
- Rejected options:
  - weakening runtime to emit `slot_constraint` evidence after real slot progress;
  - accepting contradictory scenario contracts (`slot_constraint` + slot-answer progress) in oracle;
  - phrase hardcodes in runtime core.

## Root cause (mandatory)

- Symptom:
  - guarded `dev L2` rerun `p1.6o6-l2-dev-20260308-a1-r29` progressed beyond `r28`, then stopped on dialog `1`, turn `3`, text `Мне подходит утро, например, в 10:00.` with `expected_meta_mismatch` / `expected_trace_miss`.

- Minimal reproduction:
  1. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r29/scenarios.json` for dialog `1`, turn `3`
  2. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r29/responses.jsonl` for message id `LLM-QUAL-p1.6o6-l2-dev-20260308-a1-r29-001-03-473793`
  3. compare scenario tag/expect (`slot_constraint`) against runtime trace (`question_contract matched`, `booking prompt missing_slot=name`)
  4. inspect `scripts/booking_dialog_scenarios.py` slot-constraint normalization around explicit time-answer surfaces

- Evidence:
  - scenario expected `pending_question_act=slot_constraint`, `pending_question_target=time`, and `pending_question_interaction` trace.
  - runtime matched `10:00` as `answer_slot=datetime`, advanced booking slots, and asked for `name`.
  - policy-core payload also carried concrete `slots.datetime=10:00`, `tool_action=calendar.book_slot`, `temporal_scope=specific_time`, `resolution_mode=direct`, with no `pending_question_act`.
  - mismatch is therefore taxonomy/oracle overclaim on an explicit slot-answer surface, not runtime regression.

- Five Whys:
  1. Why did `r29` fail: scenario expected slot-constraint evidence, but runtime emitted slot progress.
  2. Why did scenario expect slot-constraint: generator surface combined soft preference wording with a concrete time and preserved the `slot_constraint` tag.
  3. Why is that wrong: once the utterance supplies a grounded datetime value, the active requested slot is filled and runtime should progress collect.
  4. Why did taxonomy not normalize it: current scenario sanitize distinguishes mixed questions, but not explicit slot-answer surfaces inside `slot_constraint` family.
  5. Why did this surface now: `P1.6o29` removed the earlier stale mixed expectation drift, so the next overclaim in scenario taxonomy became first blocker.

- Root cause statement:
  - scenario taxonomy overlabels explicit datetime-answer turns as `slot_constraint` even when the utterance clearly fills the active requested slot, causing false expectation drift against correct booking progress behavior.

- Fix mechanism:
  - normalize explicit datetime-answer surfaces out of `slot_constraint` into ordinary slot-answer booking expectations;
  - add deterministic regression for the `утро, например, в 10:00` shape;
  - then run one fresh guarded rerun `r30`.

## Release safety

- Strategy: deterministic scenario/oracle normalization only
- Go signals:
  - explicit datetime answers no longer require `pending_question_interaction=slot_constraint`
  - runtime progress-to-name path remains unchanged
- No-go signals:
  - runtime-core changes for this block
  - weakening oracle to accept contradictory slot-fill + slot-constraint contracts simultaneously
- Rollback verification:
  - revert only the scenario normalization if unrelated `slot_constraint` families regress

## Invariant

1. Concrete slot answers must remain valid booking progress.
2. `slot_constraint` family must remain for real constraint turns without grounded slot fill.
3. No runtime-core changes in this block.
4. Expensive rerun remains exactly one fresh guarded iteration after deterministic closure.

## Scope

- `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o30-slot-constraint-answer-normalization-a1.md`
- `scripts/booking_dialog_scenarios.py`
- `truffles-api/tests/test_booking_dialog_scenarios_script.py`

## Out of scope

- runtime decision changes
- guard/oracle evaluator redesign outside scenario taxonomy
- acceptance lock/replay

## Touch-list

- `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o30-slot-constraint-answer-normalization-a1.md`
- `scripts/booking_dialog_scenarios.py`
- `truffles-api/tests/test_booking_dialog_scenarios_script.py`

## Plan (1..N)

1. Freeze `r29` as non-closure evidence and isolate the mismatch as taxonomy-side.
2. Normalize explicit slot-answer surfaces out of `slot_constraint` expectations.
3. Add deterministic regression for the exact `r29` shape.
4. Run one fresh guarded rerun `r30` only after deterministic checks are green.

## DoD

1. Explicit datetime-answer turns no longer retain `slot_constraint` expectation drift.
2. Deterministic scenario-script regression is green.
3. `r29` is recorded as non-closure evidence; `P1.6o29` deterministic closure stands.
4. Next expensive step is exactly one fresh guarded rerun `r30`.

## Expensive run control (mandatory)

- Hypothesis:
  - after slot-answer normalization, the next guarded rerun will not fail first on the `r29` expectation-only family.
- Expected measurable effect:
  - either the run proceeds beyond dialog `1`, turn `3`, or a different blocker family surfaces.
- Stop condition:
  - run exactly one fresh guarded rerun plus strict audit; if the same mismatch persists, stop and reopen RCA instead of chaining more reruns.

## Checks

- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r29 --status done --strict-artifacts`
- `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "slot_constraint or mixed_fill_plus_question or pending_question"`
- `git diff --check`

## Execution status

- `done`
- deterministic normalization is green:
  - `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "slot_constraint or mixed_fill_plus_question or pending_question"`
  - `git diff --check`
- fresh guarded `r30` is audited non-closure evidence and no longer surfaces the explicit slot-answer family
- `r30` instead surfaces a different runtime blocker family on `calendar.list_slots/missing_slot` pending-question evidence, reopened as `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o31-tool-registry-missing-slot-pending-question-evidence-a1.md`

## Evidence

1. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r29/summary.json`
2. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r29/brief.md`
3. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r29/scenarios.json`
4. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r29/responses.jsonl`
5. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r29/trace_bundle.jsonl`
6. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r29/manual_audit.md`
7. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r30/summary.json`
8. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r30/brief.md`
9. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r30/manual_audit.md`

## Rollback

1. Revert only the slot-answer normalization if unrelated `slot_constraint` families regress.
2. Keep `P1.6o6` blocked; do not reinterpret `r29` as closure evidence.

## No-go

- no runtime-core edits
- no oracle weakening to accept contradictory scenario contracts
- no fresh guarded rerun before deterministic scenario regression is green

## Risks/Blockers

1. Another adjacent scenario-state drift may surface once explicit slot answers are normalized out of `slot_constraint`.
2. Infra tool-evidence invalidity remains orthogonal and can still make the guarded run non-canonical.

## Residual architecture debt (mandatory)

- Current residuals accepted in this block:
  - fresh guarded proof remains open via `P1.6o6` after this deterministic cleanup
- Why not in this block:
  - this block only repairs one scenario taxonomy overclaim
- Risk if deferred:
  - expensive runs keep surfacing false slot-constraint mismatches after correct runtime slot progress
- Linked follow-up Task Package(s):
  - `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`
  - `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o31-tool-registry-missing-slot-pending-question-evidence-a1.md`
- Expiry/trigger to stop deferral:
  - before the next fresh guarded rerun after `r30`

## Next-block contract (mandatory)

- Next block objective:
  - keep `P1.6o30` closed and close bounded `P1.6o31`, then run one fresh guarded rerun `r31` to verify that the normal `tool_registry missing_slot` path preserves pending-question evidence
- First deterministic check command:
  - `pytest -q truffles-api/tests/test_message_endpoint.py -k "pending_question_act_time_guidance or list_slots_missing_slot_pending_question" && pytest -q truffles-api/tests/test_booking_quality_response_guard.py -k "pending_question_interaction or booking_slot_stall" && git diff --check`
- Blocked-by conditions:
  - `P1.6o31` is not deterministically green
  - guarded preflight is not green
  - runtime `http://127.0.0.1:18184` is unavailable
- Owner role for closure:
  - `Hands` for doc/code/evidence, `Top Architect + Brain` for closure gating

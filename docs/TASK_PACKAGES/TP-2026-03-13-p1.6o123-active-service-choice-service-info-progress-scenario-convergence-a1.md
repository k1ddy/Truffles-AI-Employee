# TP-2026-03-13-p1.6o123-active-service-choice-service-info-progress-scenario-convergence-a1

- Название/цель: после truthful proof admission `P1.6o122` закрыть bounded continuity gap, в котором `catalog.service_query` отвечает truthfully на service-grounded pricing/duration/promo interrupt под активным booking flow, но не выставляет свежий `catalog_service_booking_progress` contract evidence, из-за чего runtime опирается на stale expected state, а scenario/oracle остаётся на stale `service_choice`.
- Note: filename retained in place for continuity, but the block is truthfully reclassified from the earlier premature pure-`Track A` draft into a runtime-first continuity slice with bounded oracle sync.

## Canon refs

- Parent closure program: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Main remediation: `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- Process reset / class triage: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o118-class-level-closure-process-reset-a1.md`
- Prior bounded slices and proof: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o119-timeout-degrade-owner-boundary-first-slice-a1.md`, `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o121-booking-slot-guidance-scenario-convergence-first-slice-a1.md`, `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o122-post-track-a-proof-lane-a1.md`
- Machine-readable owner matrix: `truffles-api/app/knowledge/generic/INTERACTION_OWNER_MATRIX.yaml`
- State baseline: `STATE.md`

## Git context

- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `origin/main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect` remove branch + worktree after merge

## One web search (mandatory before implementation)

- Query: `site:stately.ai history states preserve previous state after interrupt`
- Date/time: `2026-03-13T13:30:00+05:00`
- Sources opened:
  - official Stately docs: `https://stately.ai/docs/history-states`
- Source quality:
  - official product documentation
- Found reusable ideas:
  - interrupt branches should resume the preserved state explicitly instead of inferring a new default state from local surface conditions;
  - continuity after an interrupt must be emitted as explicit machine state, not guessed later from stale context.
- Decision:
  - `reuse`
  - reuse the state-machine principle for runtime continuity: a service-grounded factual interrupt under active booking must stamp the resumed contract explicitly before oracle/generator can follow it truthfully.
- Rejected options:
  - generator-only normalization without fresh runtime contract evidence;
  - another expensive rerun before deterministic closure;
  - new row-local matrix child TP for what is still a continuation of existing runtime continuity debt.

## Root cause (mandatory)

- Symptom:
  - `P1.6o122` rolled past the covered slot-guidance `Track A` family, but the new first fail is `dialog 1 / turn 9 / "Сколько стоит маникюр?"`: runtime answers pricing truthfully through `catalog.service_query`, yet the surfaced turn still lacks fresh `catalog_service_booking_progress` continuity evidence and the generated oracle remains on stale `service_choice`.
- Minimal reproduction:
  1. Audit `/tmp/booking_quality/p1.6o122-l2-dev-20260313-a1-r1`.
  2. Inspect dialog `1`, turns `8` and `9`.
  3. Turn `8` timeout-degraded collect asks `service_choice`.
  4. Turn `9` is a grounded pricing interrupt (`Маникюр`) under booking goal.
  5. Runtime answers via `catalog.service_query`, but `resolve_tool_expected_reply_contract(...)` returns `None` because slot completeness still looks closed.
  6. Tool-registry follow-up falls back to stale current expected state/reason instead of emitting a fresh continuity contract.
  7. Generator/oracle keeps stale `service_choice`, because the canonical continuity contract is not stamped explicitly.
- Evidence:
  - `/tmp/booking_quality/p1.6o122-l2-dev-20260313-a1-r1/summary.json`
  - `/tmp/booking_quality/p1.6o122-l2-dev-20260313-a1-r1/brief.md`
  - `/tmp/booking_quality/p1.6o122-l2-dev-20260313-a1-r1/responses.jsonl`
  - `/tmp/booking_quality/p1.6o122-l2-dev-20260313-a1-r1/trace_bundle.jsonl`
  - `/tmp/booking_quality/p1.6o122-l2-dev-20260313-a1-r1/manual_audit.md`
  - `truffles-api/app/services/expected_reply_contract.py`
  - `truffles-api/app/routers/webhook/decision.py`
  - `scripts/booking_dialog_scenarios.py`
- Five Whys:
  1. Why did `P1.6o122` still fail after `P1.6o121`? Because the next surfaced family was no longer the covered slot-guidance wording family.
  2. Why did the new family fail? Because the pricing interrupt did not carry one explicit resumed booking contract end-to-end.
  3. Why did runtime miss that contract? Because tool follow-up logic treated slot completeness as authoritative and let `catalog.service_query` fall back to stale expected state when `resolve_tool_expected_reply_contract(...)` returned `None`.
  4. Why did oracle stay stale? Because scenario-governance still preserved `service_choice` on the same contour and had no explicit runtime contract evidence to mirror.
  5. Why is this class-level structural work, not a row-local phrase patch? Because the defect is continuity ownership at the factual-interrupt boundary, not the literal wording of `Сколько стоит маникюр?`.
- Root cause statement:
  - under service-grounded factual `catalog.service_query` interrupts inside active booking continuity, runtime currently relies on stale expected state when booking slots appear complete, so it does not always emit a fresh `catalog_service_booking_progress` contract; oracle/generator then preserves stale `service_choice` because the runtime contract is implicit instead of explicit.
- Fix mechanism:
  - extend the shared tool expected-reply contract so grounded `catalog.service_query` interrupts can emit `catalog_service_booking_progress` even when stale booking slots look complete but booking continuity is still active;
  - make the tool-registry path stamp fresh `question_contract` evidence through the existing follow-up path;
  - align scenario sanitize/post-coverage repair only for the bounded service-grounded `price` / `duration` / `promo` contour so oracle follows the explicit runtime contract instead of old `service_choice` preservation;
  - cover the contour with targeted contract/unit/runtime/oracle regressions.

## Reuse-first decision (mandatory)

- Reuse:
  - `resolve_tool_expected_reply_contract(...)`
  - existing tool-registry follow-up path in `decision.py`
  - existing `_looks_like_service_grounded_booking(...)`
  - existing sanitize / post-coverage repair flow
  - existing quality response guard evaluator
- Integrate:
  - integrate one bounded continuity rule into the shared expected-reply contract and one bounded scenario sync rule tied to the same explicit contract
- Build:
  - no new runtime module
  - no new matrix row
  - no new expensive rerun in this block

## Release safety

- Rollout: none; local deterministic runtime/oracle block
- Go signal:
  - shared tool contract emits `catalog_service_booking_progress` on the bounded contour
  - runtime stamps fresh `question_contract` evidence on the bounded contour
  - scenario/oracle expects the same explicit contract
  - targeted runtime, contract, scenario, and quality-oracle checks are green
- No-go signal:
  - runtime still falls back to stale `policy_core_degraded_collect` reason on the bounded contour
  - scenario still keeps stale `service_choice`
  - the fix broadens generic info interrupts without grounded service evidence
- Rollback:
  - revert the bounded runtime/scenario changes and TP/doc sync; do not reinterpret `P1.6o122` as closure evidence

## Invariant

1. `P1.6o119`, `P1.6o121`, and `P1.6o122` truth stays intact.
2. No new phrase/regex semantic branching is added to `decision.py`.
3. Only service-grounded factual `price` / `duration` / `promo` interrupts under active booking continuity are affected.
4. Generic service-missing turns must still remain `service_choice`.
5. Oracle may only change where runtime emits explicit continuity evidence for the same bounded contour.

## Scope

- bounded shared expected-reply contract update for `catalog.service_query`
- bounded tool-registry continuity evidence fix
- bounded scenario/oracle sync for service-grounded factual interrupts under stale active `service_choice`
- targeted contract, runtime, scenario, and quality-oracle tests

## Out of scope

- broad timeout/degrade migration beyond `P1.6o119`
- new matrix row admission
- broad generator redesign
- fresh guarded rerun in this block

## Touch-list

- `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o123-active-service-choice-service-info-progress-scenario-convergence-a1.md`
- `truffles-api/app/services/expected_reply_contract.py`
- `truffles-api/app/routers/webhook/decision.py`
- `scripts/booking_dialog_scenarios.py`
- `truffles-api/tests/test_expected_reply_contract.py`
- `truffles-api/tests/test_message_endpoint.py`
- `truffles-api/tests/test_booking_dialog_scenarios_script.py`
- `truffles-api/tests/test_booking_quality_response_guard.py`
- `STATE.md`
- `STRUCTURE.md`
- `docs/SESSIONS/SESSION-2026-02-19-llm-first-firebreak-a1.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`

## Plan (1..N)

1. Freeze `P1.6o122` truth as a runtime continuity continuation, not a pure scenario-only continuation.
2. Extend the shared tool expected-reply contract for bounded service-grounded factual interrupts under active booking continuity.
3. Verify the existing tool-registry follow-up path now stamps explicit `question_contract` evidence on that contour.
4. Align sanitize and post-coverage repair for the same bounded contour so oracle follows the explicit contract.
5. Add targeted contract, runtime, scenario, and quality-oracle regressions.
6. Run deterministic checks and sync closure truth.

## DoD

1. Service-grounded `price` / `duration` / `promo` interrupts under active booking continuity emit explicit `catalog_service_booking_progress` contract evidence.
2. The bounded runtime contour no longer falls back to stale `policy_core_degraded_collect` reason on the surfaced family.
3. Scenario/oracle advances the same bounded contour from stale `service_choice` to canonical `time` only when the interrupt is service-grounded.
4. Generic service-missing turns still remain `service_choice`.
5. Targeted contract, runtime, scenario, and quality-oracle checks are green.
6. Active docs/state/session are synced truthfully.

## Checks

- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o122-l2-dev-20260313-a1-r1 --status done --strict-artifacts`
- `python3 -m py_compile truffles-api/app/services/expected_reply_contract.py truffles-api/app/routers/webhook/decision.py scripts/booking_dialog_scenarios.py truffles-api/tests/test_expected_reply_contract.py truffles-api/tests/test_message_endpoint.py truffles-api/tests/test_booking_dialog_scenarios_script.py truffles-api/tests/test_booking_quality_response_guard.py`
- `pytest -q truffles-api/tests/test_expected_reply_contract.py -k "catalog_service_booking_progress"`
- `pytest -q truffles-api/tests/test_message_endpoint.py -k "service_info_interrupt_under_degraded_collect or owner_resolver_m33_preserves_active_name_service_info_resume"`
- `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "service_choice_price_interrupt_booking_progress or preserves_time_after_price_interrupt"`
- `pytest -q truffles-api/tests/test_booking_quality_response_guard.py -k "service_choice_price_interrupt_booking_progress"`
- `git diff --check`
- `SESSION_AGENT=a1 scripts/session_check.sh`

## Execution status

- `done`
- `P1.6o123` is deterministically closed after truthful `P1.6o122` class triage
- bounded runtime-first closure truth:
  - shared expected-reply contract emits explicit `catalog_service_booking_progress` on the surfaced service-grounded pricing/duration/promo interrupt contour
  - decision follow-up stamping now preserves explicit question-contract evidence on that contour instead of falling back to stale degraded continuity state
  - owner-resolver precedence for existing active-name service-info follow-up remains intact; the bounded continuity slice does not regress the earlier `M33` contour
  - scenario/oracle advances only the same bounded service-grounded interrupt contour from stale `service_choice` to canonical `time`
  - generic service-missing turns remain `service_choice`

## Evidence

1. `/tmp/booking_quality/p1.6o122-l2-dev-20260313-a1-r1/summary.json`
2. `/tmp/booking_quality/p1.6o122-l2-dev-20260313-a1-r1/brief.md`
3. `/tmp/booking_quality/p1.6o122-l2-dev-20260313-a1-r1/responses.jsonl`
4. `/tmp/booking_quality/p1.6o122-l2-dev-20260313-a1-r1/trace_bundle.jsonl`
5. `/tmp/booking_quality/p1.6o122-l2-dev-20260313-a1-r1/manual_audit.md`
6. `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o122-post-track-a-proof-lane-a1.md`

## Rollback

1. Revert the bounded runtime/scenario helper changes.
2. Restore `P1.6o123` to `in_progress` / open state if deterministic checks fail.
3. Do not reinterpret `P1.6o122` as closure evidence.

## No-go

- no new row-local TP or matrix admission for this block
- no generator-only normalization without runtime contract evidence
- no weakening of oracle/gates to accept stale runtime metadata
- no new expensive rerun before deterministic closure

## Risks / blockers

- stale booking completeness heuristics could still hide the active continuity contract if the shared contract rule is under-scoped
- generator sync could accidentally broaden generic info interrupts if not tied to explicit service grounding
- neighboring service-info families may still remain after this bounded contour closes

## Residual architecture debt (mandatory)

### Current residuals accepted in this block

- scenario-governance still remains a parallel truth source; this block only syncs one more bounded contour to explicit runtime evidence
- broader timeout/degrade extraction beyond `P1.6o119` remains open
- broader success-path owner coverage outside the resolver remains open

### Why not in this block

- this block is intentionally limited to the surfaced bounded continuity contour from `P1.6o122`

### Risk if deferred

- the team could keep oscillating between runtime and oracle interpretations of the same family without ever emitting one explicit contract source of truth

### Linked follow-up Task Package(s)

- `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o124-post-service-info-progress-proof-lane-a1.md`
- later structural runtime owner-coverage extraction if proof still surfaces success-path owner drift

### Expiry/trigger to stop deferral

- before any new guarded `dev/acceptance` rerun after `P1.6o122`

## Next-block contract (mandatory)

### Next block objective

- publish and run `P1.6o124`, one fresh proof-lane TP after deterministic closure of `P1.6o123`, so the worktree can verify that the surfaced service-grounded factual interrupt family is no longer the first fail and truthfully classify the next remaining class, if any

### First deterministic check command

- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o122-l2-dev-20260313-a1-r1 --status done --strict-artifacts && pytest -q truffles-api/tests/test_expected_reply_contract.py -k "catalog_service_booking_progress" && pytest -q truffles-api/tests/test_message_endpoint.py -k "service_info_interrupt_under_degraded_collect or owner_resolver_m33_preserves_active_name_service_info_resume" && git diff --check`

### Blocked-by conditions

- `P1.6o123` deterministic closure is not synced truthfully in active docs/state/session
- targeted `P1.6o123` runtime/scenario/oracle checks are not green
- the team attempts a new local continuity/oracle patch instead of the selected proof-lane `P1.6o124`

### Owner role for closure

- `Top Architect + Brain`

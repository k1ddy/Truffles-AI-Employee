# TP-2026-03-06-p6.2-open-world-translit-code-switch-coverage-a1

- Название/цель: расширить `P6` следующим атомарным блоком после `P6.1`: дать генератору сценариев детерминированный `mixed_translit` профиль, чтобы те же booking/info/handoff контракты проходили через смешанный кириллица/латиница surface без новых runtime-core эвристик и без LLM-бюджета.
- Parent TP: `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- Canon refs: `AGENTS.md`, `STATE.md`, `STRUCTURE.md`, `STRATEGY/REQUIREMENTS.md`, `TECH.md`, `docs/runbooks/BOOKING_CONFIRM_VERIFY.md`, `docs/TASK_PACKAGES/TP-2026-02-19-llm-first-firebreak-program.md`
- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`

## One web search (mandatory before implementation)

- Query: `site:arxiv.org transliteration robustness multilingual llm`
- Time (UTC): `2026-03-06T13:11:25Z`
- Sources:
  - `arXiv: Towards Transliteration-Robust Language Models in Low-Resource Settings`
- Ready solutions found:
  - transliterated surface variants can be generated synthetically and used as a controlled robustness axis
  - transliteration robustness should preserve the same task semantics instead of redefining labels or intent
- Decision (`reuse/integrate/build`): `reuse + integrate`
  - reuse the new `language_profile` mutator from `P6.1`
  - integrate a deterministic transliteration layer on top of the same tag-aware mixed-surface templates
- Rejected options:
  - add translit-specific runtime routers in core
  - wait for a future expensive LLM quality window before generating any translit evidence

## Root cause (mandatory)

- Symptom:
  - `P6.1` added `ru/kk/mixed`, but the generator still emits only Cyrillic surfaces and does not cover the translit/code-switch class required by the firebreak program.

- Minimal reproduction:
  1. inspect `LANGUAGE_PROFILE_CHOICES` and `_apply_language_profile(...)` in `scripts/booking_dialog_scenarios.py`
  2. `rg -n "mixed_translit|translit|latin" scripts/booking_dialog_scenarios.py truffles-api/tests/test_booking_dialog_scenarios_script.py`
  3. observe no deterministic mixed-script variant exists

- Evidence:
  - `scripts/booking_dialog_scenarios.py`
    - current profiles are `ru`, `kk`, `mixed`
    - no transliteration layer exists after surface mutation
  - `docs/TASK_PACKAGES/TP-2026-02-19-llm-first-firebreak-program.md`
    - mandatory multilingual adversarial battery explicitly calls out translit and code-switch

- Five Whys:
  1. Why is mixed-script coverage still missing: generator only changes language surface, not script.
  2. Why does that matter: many real user turns arrive as Latin translit or mixed-script shortcuts.
  3. Why is generator the right layer: this is still an evidence-generation problem, not a runtime semantics problem.
  4. Why not do ASR/typo in the same block: that would broaden the mutation space too much for one atomic closure.
  5. Why now: `P6.1` already created the reusable mutation entrypoint; translit is the next smallest extension.

- Root cause statement:
  - the new language-profile mutator still lacks a script-level transformation axis, so multilingual open-world coverage remains incomplete for transliterated inputs.

- Fix mechanism:
  - add `mixed_translit` to the language-profile surface
  - transliterate mutated mixed-profile turns through a deterministic Cyrillic-to-Latin mapping
  - prove seed-stable mixed-script output with targeted tests

## Invariant

1. Transliteration does not change `tags` or `expect`.
2. The same seed yields identical mixed-translit output.
3. Script mutation stays in the scenario generator, not in runtime-core logic.

## Scope

- `scripts/booking_dialog_scenarios.py`
- `truffles-api/tests/test_booking_dialog_scenarios_script.py`
- `docs/runbooks/BOOKING_CONFIRM_VERIFY.md`
- `docs/TASK_PACKAGES/TP-2026-03-06-p6.2-open-world-translit-code-switch-coverage-a1.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`

## Out of scope

- ASR typo/noise mutation family
- acceptance `lock/replay/full`
- runtime-core multilingual fixes

## Plan (1..N)

1. Extend `language_profile` with `mixed_translit`.
2. Add deterministic transliteration over mixed-profile mutated turns.
3. Add targeted tests for mixed-script output and seed stability.
4. Sync docs/evidence.

## DoD

1. Generator accepts `mixed_translit`.
2. Mixed-translit output contains Latin-script surface while keeping `tags` and `expect`.
3. Deterministic tests are green.
4. `git diff --check` is clean.

## Checks

- `python3 scripts/booking_dialog_scenarios.py --count 2 --seed 42 --language-profile mixed_translit --output /tmp/booking_dialog_scenarios_mixed_translit.json`
- `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "translit"`
- `git diff --check`

## Execution status

- `done`

## Evidence

1. Code:
   - `scripts/booking_dialog_scenarios.py`
   - `truffles-api/tests/test_booking_dialog_scenarios_script.py`
   - `docs/runbooks/BOOKING_CONFIRM_VERIFY.md`
2. Commands:
   - `python3 scripts/booking_dialog_scenarios.py --count 2 --seed 42 --language-profile mixed_translit --output /tmp/booking_dialog_scenarios_mixed_translit.json`
   - `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "translit"` -> `1 passed`
   - `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py` -> `19 passed`
   - `git diff --check` -> `clean`

## Rollback

1. revert translit profile additions
2. rerun scenario-generator tests

## No-go

- no runtime-core translit heuristics
- no nondeterministic mutation logic
- no mutation that rewrites `tags` / `expect`

## Risks/Blockers

1. overly aggressive transliteration can damage slot readability if mapping is inconsistent
2. mixed-translit must preserve enough Cyrillic/Latin contrast to actually test mixed-script handling

## Residual architecture debt (mandatory)

- Current residuals accepted in this block:
  - ASR typo/noise and broader adversarial paraphrase families remain deferred
- Why not in this block:
  - this block is limited to script mutation only
- Risk if deferred:
  - open-world proof still misses typo/noisy tails even after translit coverage exists
- Linked follow-up Task Package(s):
  - `docs/TASK_PACKAGES/TP-2026-03-06-p6.3-open-world-typo-surface-coverage-a1.md`
- Expiry/trigger to stop deferral:
  - before claiming complete `P6` coverage

## Next-block contract (mandatory)

- Next block objective:
  - add typo/ASR-like surface perturbations on top of the new multilingual+translit scenario surface
- First deterministic check command:
  - `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "typo or asr or adversarial"`
- Blocked-by conditions:
  - no deterministic noisy-perturbation family exists
  - no tests prove contract preservation under typo/ASR-like surface changes
- Owner role for closure:
  - `Top Architect + Brain`

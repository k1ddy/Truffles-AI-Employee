# TP-2026-03-11-p1.6o70-invalid-schema-booking-request-specialist-followup-a1

- Название/цель: закрыть surfaced `r95` blocker family, где после bounded `M18` closure fresh guarded `dev L2` больше не падает на declarative time-window slot-constraint, но turn `Можно ли записаться к Айгерим?` под активным `expected_reply_type=time` и известной услуге снова ловит `llm_policy_core invalid_schema`; runtime сохраняет только generic `booking_prompt`, потому что invalid-schema specialist-followup recovery требует `resolve_master_intent.explicit` и не доходит до specialist-hint extraction для booking-request без слова `мастер`.

## Canon refs

- Parent verdict: `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o60a-remaining-closure-architecture-verdict-a1.md`
- Owner matrix: `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o60b-remaining-closure-owner-matrix-a1.md`
- Program TP: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Proof bundle: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`
- Prior child: `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o69-time-window-slot-constraint-collect-owner-a1.md`
- Related bounded closures:
  - `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o65-invalid-schema-specialist-followup-truth-gate-a1.md`
  - `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o66-policy-core-named-specialist-followup-owner-a1.md`
- STATE: `STATE.md`
- CA: remaining closure row after `M18`

## One web search (mandatory before implementation)

- Query: `Rasa forms validate slot custom extraction keep form active official docs`
- Date/time: `2026-03-11T15:52:06+05:00`
- Sources opened:
  - official Rasa docs: `https://rasa.com/docs/rasa/forms/`
- Source quality:
  - official product documentation
- Found reusable ideas:
  - when the requested slot is still active and a user provides a side-request that should not advance the slot, custom extraction/validation should preserve the active slot and run a bounded recovery path rather than fall through to the default unhappy path;
  - custom validation can fill auxiliary entities while keeping the same requested slot active.
- Decision:
  - `reuse/integrate`
  - reuse the existing invalid-schema specialist-followup recovery and integrate the timeout-path booking-request hint fallback so named-specialist booking requests without the explicit `master` lexeme still resolve to specialist follow-up under active `time` collect.
- Rejected options:
  - weakening oracle to accept missing specialist-followup evidence;
  - prompt-only patch without runtime recovery for invalid-schema degrade;
  - generic keyword hardcode outside the bounded invalid-schema specialist-followup guard.

## Root cause (mandatory)

- Symptom:
  - fresh guarded `dev L2` run `p1.6o6-l2-dev-20260311-a1-r95` stops at dialog `3`, turn `4`, user text `Можно ли записаться к Айгерим?`
- Minimal reproduction:
  1. Inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r95/scenarios.json` dialog `3`, turns `1-4`.
  2. Confirm turn `4` expects `pending_question_target=specialist` with `expected_reply_type=time` under active booking/time collect.
  3. Inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r95/responses.jsonl` and `trace_bundle.jsonl` for dialog `3`, turn `4`.
  4. Confirm `question_contract` is blocked by info, `llm_policy_core` ends in `invalid_schema`, and final runtime falls back to generic `degraded_collect` / `booking_prompt`.
  5. Compare `decision.py` invalid-schema specialist-followup helper with the timeout specialist-followup helper and confirm only the invalid-schema path returns before `specialist_hint` unless `resolve_master_intent.explicit` is already true.
- Evidence:
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r95/summary.json`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r95/brief.md`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r95/scenarios.json`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r95/responses.jsonl`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r95/trace_bundle.jsonl`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r95/manual_audit.md`
- Five Whys:
  1. Why did turn `4` fail? Final meta/trace lost `booking_specialist_followup` evidence and only preserved a generic booking prompt.
  2. Why was specialist-followup evidence lost? The invalid-schema recovery did not trigger.
  3. Why did recovery not trigger? The helper returns early when `resolve_master_intent.explicit` is false and no specialist is already in booking context.
  4. Why is that wrong here? `Можно ли записаться к Айгерим?` is still a booking-request specialist follow-up under active `time` collect; it should be allowed to recover via specialist-hint extraction even without the literal `мастер` lexeme.
  5. Why is this a new bounded family? `P1.6o65` closed invalid-schema specialist follow-up for explicit master wording, but booking-request named-specialist surfaces without `master` still fall through.
- Root cause statement:
  - `truffles-api/app/routers/webhook/decision.py` invalid-schema specialist-followup recovery is stricter than the timeout analogue: it requires `resolve_master_intent.explicit` before attempting specialist-hint extraction, so booking-request named-specialist turns without `master` wording cannot recover to `booking_specialist_followup`.
- Fix mechanism:
  - freeze matrix row `M19` for invalid-schema named specialist booking requests under active `time` collect;
  - allow the invalid-schema specialist-followup recovery to attempt specialist-hint extraction on bounded booking-request surfaces even when `resolve_master_intent.explicit` is false;
  - add deterministic regressions proving the new path recovers to `booking_specialist_followup` while adjacent generic invalid-schema collect rows stay unchanged.

## Reuse-first decision (mandatory)

- Reuse:
  - existing invalid-schema specialist-followup recovery
  - existing timeout specialist-hint booking-request fallback pattern
  - existing `booking_specialist_followup` response-guard contract
- Integrate:
  - bounded invalid-schema helper in `truffles-api/app/routers/webhook/decision.py`
- Do not build:
  - new specialist resolver
  - new prompt-only routing rule
  - new oracle waiver

## Execution profile

- TP mode: `implementation`
- Doc touch budget (files): `9`
- Code dominance: `required`
- Override token: `none`
- Why this profile fits: surfaced family is a narrow runtime recovery asymmetry between existing timeout and invalid-schema specialist-followup guards.

## Invariant

1. `P1.6o69/M18` remains closed.
2. Existing invalid-schema specialist-followup behavior for explicit `мастер` wording remains unchanged.
3. Generic invalid-schema degraded collect without a recoverable specialist hint stays generic.
4. Final recovery remains explicit in `decision_meta` / `decision_trace`.

## Scope

- publish surfaced `r95` family as matrix row `M19`
- implement bounded invalid-schema booking-request specialist-followup recovery only

## Out of scope

- acceptance lock/replay
- prompt redesign for specialist referent extraction
- timeout specialist-followup path already closed
- broader expected-reply info-block heuristics

## Touch-list

- `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o70-invalid-schema-booking-request-specialist-followup-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o60b-remaining-closure-owner-matrix-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o60a-remaining-closure-architecture-verdict-a1.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`
- `truffles-api/app/routers/webhook/decision.py`
- `truffles-api/tests/test_message_endpoint.py`
- `truffles-api/tests/test_booking_quality_response_guard.py`

## Plan (1..N)

1. Freeze `r95` as audited non-closure evidence and publish `M19`.
2. Align invalid-schema specialist-followup guard with the bounded booking-request hint fallback already used in the timeout path.
3. Add targeted runtime/response-guard regressions plus one adjacent negative invalid-schema collect case.
4. Run focused checks and `git diff --check`.
5. Run exactly one fresh guarded `dev L2` only after deterministic green.

## DoD

1. `r95` is recorded in active docs as audited non-closure evidence.
2. Booking-request named-specialist turns under invalid-schema degrade preserve `booking_specialist_followup` owner evidence.
3. Final meta/trace preserve `pending_question_target=specialist`, `active_question_relation=referent_followup`, and `expected_reply_type=time`.
4. Adjacent generic invalid-schema collect rows remain unchanged.
5. Focused deterministic runtime/evidence regressions are green.
6. Fresh guarded `dev L2` no longer stops on the `r95` family.

## Checks

- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r95 --status done --strict-artifacts`
- `pytest -q truffles-api/tests/test_message_endpoint.py -k "invalid_schema_specialist_followup or question_like_named_specialist or collect_slot_order_invalid_time_window"`
- `pytest -q truffles-api/tests/test_booking_quality_response_guard.py -k "invalid_schema_specialist_followup or specialist_followup_owner_gap"`
- `git diff --check`

## Evidence

1. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r95/summary.json`
2. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r95/brief.md`
3. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r95/scenarios.json`
4. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r95/responses.jsonl`
5. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r95/trace_bundle.jsonl`
6. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r95/manual_audit.md`
7. `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o69-time-window-slot-constraint-collect-owner-a1.md`
8. `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o70-invalid-schema-booking-request-specialist-followup-a1.md`


## Closure update

- Deterministic implementation for `M19` is complete.
- Fresh guarded `dev L2` `r96` no longer first-failed on this row and rolled to `P1.6o71`.
- This block is now `closed_bounded`; proof-chain closure remains blocked on later surfaced families and infra-valid canonical evidence.

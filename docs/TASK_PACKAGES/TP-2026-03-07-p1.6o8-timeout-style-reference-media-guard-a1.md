# TP-2026-03-07-p1.6o8-timeout-style-reference-media-guard-a1

- Название/цель: закрыть новый `demo_salon` blocker из `P1.6o6`, где text-only style-reference turn (`Вот фото референса`) при `llm_policy_core` timeout ошибочно уходит в `pack_fact_fallback/off_topic`, теряет media contract и даёт `fact_without_evidence + judge_fail`.
- Parent TP: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Canon refs: `AGENTS.md`, `STATE.md`, `STRUCTURE.md`, `STRATEGY/REQUIREMENTS.md`, `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`, `docs/TASK_PACKAGES/TP-2026-03-06-p1.6d-timeout-degrade-fallback-hardening-a1.md`, `docs/TASK_PACKAGES/TP-2026-03-06-p4.1-fact-evidence-linkage-fail-closed-a1.md`
- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`

## One web search (mandatory before implementation)

- Query: `site:sre.google graceful degradation fail early cheaply fallback`
- Time (UTC): `2026-03-07T15:11:36Z`
- Sources:
  - `https://sre.google/sre-book/addressing-cascading-failures/`
- Ready solutions found:
  - degraded paths must stay simple and explicit; otherwise fallback logic creates new failure modes instead of containing the original timeout
  - fail early/cheaply is preferable to routing a turn into a semantically wrong fallback that the runtime cannot justify
- Decision (`reuse/integrate/build`): `reuse + integrate`
  - reuse the existing `style_reference` pending/media prompt contract and traces
  - integrate it directly into the timeout-degrade guard before `pack_fact_fallback` is considered
- Rejected options:
  - keeping `pack_fact_fallback` for text-only style-reference turns and trying to paper over it with evidence/meta changes
  - broadening generic timeout clarify for all non-booking turns instead of routing the known style-reference capability explicitly

## Root cause (mandatory)

- Symptom:
  - fresh guarded `dev L2` rerun `p1.6o6-l2-dev-20260307-a1-r2` stopped on `fact_without_evidence` and `judge_fail`
  - failing turn: `dialog_index=7`, `turn_index=2`, message `Вот фото референса`
  - actual reply: `Я помогаю только по салону — по услугам, записи, ценам и адресу.`

- Minimal reproduction:
  1. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260307-a1-r2/brief.md`
  2. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260307-a1-r2/failure_families.json`
  3. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260307-a1-r2/responses.jsonl` for `dialog_index=7`, `turn_index=2`
  4. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260307-a1-r2/trace_bundle.jsonl` for `message_id=LLM-QUAL-p1.6o6-l2-dev-20260307-a1-r2-007-02-425115`
  5. probe current runtime helper path: `get_pack_decision("Вот фото референса", client_slug="demo_salon", intent_decomp={"intents": ["other"], "primary_intent": "other"})`

- Evidence:
  - `decision_meta.policy_core_mode=degraded_fallback`
  - `decision_meta.policy_core_failure.code=timeout`
  - `decision_meta.policy_core_guard_recovery=pack_fact_fallback`
  - `decision_trace.stage=llm_policy_plan_delta reason=policy_core_timeout_fact_fallback`
  - `decision_trace.stage=fact_resolver fact_evidence_refs=[]`
  - helper probe currently returns `PackDecision(intent='off_topic', action='reply', fact_refs=['off_topic'])`, and `is_timeout_fact_fallback_candidate(...) == True`

- Five Whys:
  1. Why did the turn fail guarded quality: the timeout degrade path returned an off-topic FACT-like reply instead of handling the style-reference request.
  2. Why did timeout degrade choose that path: `pack_fact_fallback` ran before any style-reference media contract handling.
  3. Why was the fallback accepted: the timeout fact-fallback candidate treated the pack `off_topic` reply as valid recovery for this turn.
  4. Why is that wrong: a text-only style-reference request is a known product capability with an explicit `need_media` contract, so routing it to generic off-topic is semantically incorrect.
  5. Why did quality catch it as a contract gap: the wrong fallback both missed the user request and produced a FACT-like branch with no valid evidence linkage in the audited trace.

- Root cause statement:
  - policy-core timeout degrade lets `pack_fact_fallback` preempt the explicit style-reference media contract for text-only photo/reference requests, producing semantically wrong off-topic replies.

- Fix mechanism:
  - detect text-only style-reference turns inside the timeout-degrade guard before `pack_fact_fallback`
  - reuse the existing `style_reference_pending` + `need_media` prompt contract and trace/meta evidence
  - lock it with exact timeout-focused message-endpoint tests, then rerun the mandatory deterministic contour and one fresh guarded `dev L2`

## Invariant

1. Known style-reference turns must not degrade into generic off-topic fact fallback.
2. Timeout degrade for genuine fact/info turns may still use the existing guarded recovery paths.
3. No prompt-only or oracle-only workaround; the runtime/trace/meta contract must change.

## Scope

- `truffles-api/app/routers/webhook/decision.py`
- `truffles-api/tests/test_message_endpoint.py`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`

## Out of scope

- acceptance `lock/replay` inside this TP
- broad timeout fallback redesign outside style-reference/media turns
- changing judge thresholds or quality gates

## Touch-list

- `truffles-api/app/routers/webhook/decision.py`
- `truffles-api/tests/test_message_endpoint.py`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o8-timeout-style-reference-media-guard-a1.md`

## Plan (1..N)

1. Reproduce the guarded blocker from `r2` and confirm the timeout fallback path locally.
2. Short-circuit timeout degrade for text-only style-reference turns before `pack_fact_fallback`.
3. Reuse the existing pending-media prompt/trace/meta contract instead of inventing a new fallback branch.
4. Add exact timeout regression tests for non-booking and booking-context style-reference text turns.
5. Run exact tests, then the full mandatory deterministic contour.
6. Return to `P1.6o6` for one fresh guarded `dev L2` only after this block is green.

## DoD

1. Text-only style-reference turn under policy-core timeout no longer returns `pack_fact_fallback/off_topic`.
2. Runtime emits `style_reference`/`need_media` trace-meta evidence in the timeout path.
3. Exact timeout regressions and mandatory deterministic contour are green.
4. `P1.6o6` can truthfully resume from one fresh guarded `dev L2`.

## Checks

- `pytest -q truffles-api/tests/test_message_endpoint.py -k "timeout_style_reference"`
- `pytest -q truffles-api/tests/test_message_endpoint.py`
- `pytest -q truffles-api/tests/test_booking_quality_response_guard.py`
- `pytest -q truffles-api/tests/test_demo_salon_eval.py`
- `pytest -q truffles-api/tests/test_booking_chaos_dialogs.py`
- `pytest -q truffles-api/tests/test_pack_runtime_service.py`
- `git diff --check`

## Execution status

- `done`
- exact timeout regressions:
  - `pytest -q truffles-api/tests/test_message_endpoint.py -k "timeout_style_reference"` -> `2 passed, 315 deselected`
- deterministic contour on the fixed runtime:
  - `pytest -q truffles-api/tests/test_message_endpoint.py` -> `317 passed, 2 warnings`
  - `pytest -q truffles-api/tests/test_booking_quality_response_guard.py` -> `67 passed`
  - `pytest -q truffles-api/tests/test_demo_salon_eval.py` -> `20 passed`
  - `pytest -q truffles-api/tests/test_booking_chaos_dialogs.py` -> `1 passed`
  - `pytest -q truffles-api/tests/test_pack_runtime_service.py` -> `22 passed`
  - `git diff --check` -> `clean`
- guarded follow-up:
  - fresh `dev L2` `p1.6o6-l2-dev-20260307-a1-r4` passed the previously blocked style-reference turn and surfaced a new family outside this TP (`fact_without_evidence`), reopened as `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o10-service-clarify-fact-evidence-oracle-boundary-a1.md`

## Evidence

1. `/tmp/booking_quality/p1.6o6-l2-dev-20260307-a1-r2/summary.json`
2. `/tmp/booking_quality/p1.6o6-l2-dev-20260307-a1-r2/brief.md`
3. `/tmp/booking_quality/p1.6o6-l2-dev-20260307-a1-r2/failure_families.json`
4. `/tmp/booking_quality/p1.6o6-l2-dev-20260307-a1-r2/manual_audit.md`
5. future deterministic green outputs from this TP
6. `/tmp/booking_quality/p1.6o6-l2-dev-20260307-a1-r4/summary.json`
7. `/tmp/booking_quality/p1.6o6-l2-dev-20260307-a1-r4/brief.md`
8. `/tmp/booking_quality/p1.6o6-l2-dev-20260307-a1-r4/manual_audit.md`

## Rollback

1. Revert only the timeout style-reference guard if it regresses unrelated timeout recovery paths.
2. Keep `P1.6o6` blocked and do not start acceptance `lock/replay` until the new family is closed.

## No-go

- no oracle weakening to hide `judge_fail` or `fact_without_evidence`
- no post-hoc phrase rewrite after the wrong fallback is already chosen
- no generic off-topic fallback for known style-reference capability turns

## Risks/Blockers

1. A too-broad timeout style guard could steal unrelated info/photo phrases.
2. A booking-context style-reference timeout may require a booking-specific prompt variant; if it differs from the info-context fix, cover it with an exact regression in this TP rather than widening the rule blindly.

## Residual architecture debt (mandatory)

- Current residuals accepted in this block:
  - `P1.6o6` proof bundle remains blocked until this family is removed
- Why not in this block:
  - this block closes only one newly surfaced runtime family
- Risk if deferred:
  - closure evidence would remain invalid and partial guarded runs would continue to surface false off-topic timeout recoveries
- Linked follow-up Task Package(s):
  - `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`
- Expiry/trigger to stop deferral:
  - before any new acceptance `lock/replay` or baseline update

## Next-block contract (mandatory)

- Next block objective:
  - return to `P1.6o6` and rerun exactly one fresh guarded `dev L2` on the fixed runtime
- First deterministic check command:
  - `pytest -q truffles-api/tests/test_message_endpoint.py -k "timeout_style_reference"`
- Blocked-by conditions:
  - timeout style-reference regressions are not green
  - `P1.6o6` audited `r2` blocker is not recorded in docs
- Owner role for closure:
  - `Hands` for code/tests, `Brain + Top Architect` for proof-bundle resume decision

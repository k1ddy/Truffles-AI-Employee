# TP-2026-03-07-p1.6n-weekend-availability-service-referent-arbitration-a1

- Название/цель: закрыть свежий `demo_salon` `dev L2` blocker, где вопрос о записи на выходные для уже обсуждаемой услуги теряет referent текущей услуги и деградирует в `booking_prompt` вместо contract-required `hours/schedule` info response.
- Parent TP: `docs/TASK_PACKAGES/TP-2026-03-06-p1.6-local-realism-canonical-lock-a1.md`
- Program TP: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Canon refs: `AGENTS.md`, `STATE.md`, `STRATEGY/REQUIREMENTS.md`, `TECH.md`, `docs/runbooks/BOOKING_CONFIRM_VERIFY.md`, `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`

## One web search (mandatory before implementation)

- Query: `site:rasa.com/docs slot carry over reference from previous turn`
- Time (UTC): `2026-03-07T05:53:24Z`
- Sources:
  - `https://rasa.com/docs/reference/primitives/slots/`
  - `https://legacy-docs-oss.rasa.com/docs/rasa/forms/`
- Ready solutions found:
  - slots are the assistant memory and must preserve relevant resolved context across turns
  - requested-slot / form handling must validate user input explicitly and should not greedily consume or discard context when one slot is being requested
- Decision (`reuse/integrate/build`): `integrate + build`
  - integrate existing slot-memory / expected-reply discipline with referent carryover from current consult/service context
  - build a local arbitration fix so weekend-availability questions can resolve to `hours/schedule` info without demo-specific hardcode
- Rejected options:
  - add phrase hardcode for `на выходные` in runtime core
  - accept the current generic `booking_prompt` when service referent is available in context
  - rerun expensive `dev L2` before code delta

## Root cause (mandatory)

- Symptom:
  - fresh guarded `demo_salon` `dev L2` run `p1.6-l2-dev-seed42-r22` stopped on the first new strict failure family:
    - `expected_info_section_miss`
    - `info_section_miss`
  - failing turn:
    - `dialog_index=4`
    - `turn_index=4`
    - user text: `Можно ли записаться на эту услугу на выходные?`
    - bot reply: `На какую услугу хотите записаться? После этого сразу проверю свободное время.`
- Minimal reproduction:
  1. inspect `/tmp/booking_quality/p1.6-l2-dev-seed42-r22/brief.md`
  2. inspect `/tmp/booking_quality/p1.6-l2-dev-seed42-r22/responses.jsonl` for message id `LLM-QUAL-p1.6-l2-dev-seed42-r22-004-04-baa8d4`
  3. inspect `/tmp/booking_quality/p1.6-l2-dev-seed42-r22/trace_bundle.jsonl` for the same message id
  4. inspect `truffles-api/app/routers/webhook/decision.py` around consult-return + booking prompt arbitration and `service_choice` expected-reply setting
  5. inspect current service carryover / service_query reuse for booking-like follow-ups from consult context
- Evidence:
  - `/tmp/booking_quality/p1.6-l2-dev-seed42-r22/summary.json`
  - `/tmp/booking_quality/p1.6-l2-dev-seed42-r22/brief.md`
  - `/tmp/booking_quality/p1.6-l2-dev-seed42-r22/responses.jsonl`
  - `/tmp/booking_quality/p1.6-l2-dev-seed42-r22/trace_bundle.jsonl`
  - `/tmp/booking_quality/p1.6-l2-dev-seed42-r22/manual_audit.json`
  - `/tmp/booking_quality/p1.6-l2-dev-seed42-r22/manual_audit.md`
  - turn forensic facts:
    - turn expectations required `info_sections = [hours, working_hours, schedule]`
    - `llm_policy_core` emitted `consult/fact/info` with empty `service_query`
    - final runtime action became `booking_prompt`
    - `consult_return` was attached and `expected_reply_type=service_choice` was set, so the system mixed consult context with a new generic booking collect instead of answering weekend availability
- Five Whys:
  1. Why did the contract fail: the turn required an `hours/schedule` answer, but runtime emitted a missing-service booking prompt.
  2. Why did runtime emit booking prompt: booking arbitration treated the message as a generic booking request with missing service.
  3. Why was service considered missing: the referent `эту услугу` was not resolved from current consult/service context.
  4. Why did no info answer survive: policy-core produced consult/info intent, but the final runtime path still set `service_choice` and returned `booking_prompt`.
  5. Why is this systemic: referent carryover and weekend-availability arbitration are not aligned between consult context, service_query reuse, and booking prompt ownership.
- Root cause statement:
  - a weekend-availability booking question that refers to the current consult/service context loses its service referent before final arbitration, so runtime falls back to generic `booking_prompt` with `service_choice` instead of producing the contract-required `hours/schedule` info response.
- Fix mechanism:
  - preserve or recover the current service referent for booking-like follow-ups from consult context
  - make weekend-availability questions resolve through contract-safe `hours/schedule` info arbitration when service is already implicit in context
  - keep booking prompt ownership only for truly missing-service turns
  - add focused regressions; no expensive rerun in this block

## Invariant

1. No demo-specific phrase hardcode in runtime core.
2. Weekend-availability / hours answers must remain contract-traceable through `info_sections`.
3. If service is truly missing, booking prompt may still ask for service; this block must not fake a service.
4. No new expensive `dev L2 / lock / replay` run starts before deterministic closure of this block.

## Scope

- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-06-p1.6-local-realism-canonical-lock-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6n-weekend-availability-service-referent-arbitration-a1.md`
- `truffles-api/app/routers/webhook/decision.py`
- `truffles-api/tests/test_message_endpoint.py`
- `truffles-api/tests/test_demo_salon_eval.py`

## Out of scope

- new expensive `demo_salon` quality run in this block
- `P1.7` replay
- `canary/full`
- unrelated booking contact / expected-reply families already closed

## Plan (1..N)

1. Record the `r22` blocker and RCA in TP/master docs.
2. Trace where consult/service referent is lost before final booking/info arbitration.
3. Restore referent carryover and contract-safe weekend-availability info routing without semantic hardcode.
4. Add focused regressions for `this service on weekend` follow-up from consult context.
5. Rerun deterministic checks only.

## DoD

1. A weekend-availability follow-up for the current consult/service context no longer degrades to generic `service_choice` booking prompt.
2. Final meta/trace carries `info_sections` for `hours/schedule` on the repaired turn.
3. Truly missing-service booking turns still ask for service.
4. `r22` family is closed on deterministic evidence without a new expensive rerun.

## Checks

- `pytest -q truffles-api/tests/test_message_endpoint.py::test_weekend_availability_followup_uses_current_service_referent`
- `pytest -q truffles-api/tests/test_message_endpoint.py::test_deictic_capability_question_clarifies_when_referent_missing`
- `pytest -q truffles-api/tests/test_message_endpoint.py`
- `pytest -q truffles-api/tests/test_booking_quality_response_guard.py`
- `pytest -q truffles-api/tests/test_demo_salon_eval.py`
- `pytest -q truffles-api/tests/test_booking_chaos_dialogs.py`
- `ruff check truffles-api/app/routers/webhook/decision.py truffles-api/tests/test_message_endpoint.py truffles-api/tests/test_demo_salon_eval.py`
- `git diff --check`

## Execution status

- `queued under P1.6o`
- `r22` has been audited as partial forensic evidence only; it is not lock evidence and not replay evidence.
- this family is the first open child slice inside the `demo_salon` architecture closure program and must not be closed as an isolated phrase-fix.

## Evidence

1. `/tmp/booking_quality/p1.6-l2-dev-seed42-r22/summary.json`
2. `/tmp/booking_quality/p1.6-l2-dev-seed42-r22/brief.md`
3. `/tmp/booking_quality/p1.6-l2-dev-seed42-r22/responses.jsonl`
4. `/tmp/booking_quality/p1.6-l2-dev-seed42-r22/trace_bundle.jsonl`
5. `/tmp/booking_quality/p1.6-l2-dev-seed42-r22/manual_audit.json`
6. `/tmp/booking_quality/p1.6-l2-dev-seed42-r22/manual_audit.md`

## Rollback

1. revert only the weekend-availability / service-referent arbitration patch
2. keep expensive `demo_salon` runs blocked until deterministic contour is green again

## No-go

- no expensive rerun before code delta and deterministic closure
- no threshold/oracle weakening
- no phrase hardcode for `на выходные`, `по выходным`, or similar runtime-core branches

## Risks/Blockers

1. Service referent may be lost in more than one owner path (`consult`, `booking`, `service_query` reuse).
2. Over-eager reuse of consult context could incorrectly attach a service to unrelated later turns.

## Residual architecture debt (mandatory)

- Current residuals accepted in this block:
  - no fresh post-fix `demo_salon` `dev L2` evidence yet
  - `P1.7` still not created
  - `P6` remains deferred
- Why not in this block:
  - this block only restores correct service-referent / weekend-availability arbitration on the `demo_salon` lane
- Risk if deferred:
  - every next expensive `demo_salon` run can stop on the same info-section miss family
- Linked follow-up Task Package(s):
  - `docs/TASK_PACKAGES/TP-2026-03-06-p1.6-local-realism-canonical-lock-a1.md`
  - `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
  - `TBD` for `P1.7 Acceptance replay + regression gate`
- Expiry/trigger to stop deferral:
  - before the next guarded `demo_salon` `dev L2`

## Next-block contract (mandatory)

- Next block objective:
  - execute `P1.6o1` first, then `P1.6o2/P1.6o3`, and only after that close weekend-availability / current-service referent arbitration on deterministic contour so the next allowed expensive action can return to one fresh guarded `demo_salon` `dev L2`
- First deterministic check command:
  - `pytest -q truffles-api/tests/test_llm_policy_core.py::test_validate_llm_policy_core_output_accepts_semantic_envelope_fields && pytest -q truffles-api/tests/test_message_endpoint.py::test_llm_policy_core_semantic_envelope_fields_persist_in_meta`
- Blocked-by conditions:
  - `P1.6o1` semantic envelope contract not yet green
  - `P1.6o2/P1.6o3` child slices not yet executed
  - weekend-availability arbitration fix not merged into the local runtime code
  - deterministic tests for `message_endpoint` / `demo_salon_eval` not green
  - runtime `http://127.0.0.1:18184` not refreshed to the current worktree state after the code delta
- Owner role for closure:
  - `Top Architect + Brain`

# TP-2026-03-13-p1.6o135-beauty-salon-canonical-control-plane-roadmap-a1

- Название/цель: зафиксировать binding roadmap и target architecture для `beauty-salon canonical closure`, чтобы весь дальнейший ход в этом worktree шёл только в порядке `fresh proof rerun -> contract compiler -> executable owner coverage -> boundary statechart -> proof bundle`, а не через новый row-local churn.

## Canon refs

- Parent closure program: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Main remediation: `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- Process reset / class triage: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o118-class-level-closure-process-reset-a1.md`
- Current proof-process admission: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o134-audited-infra-non-canonical-lock-retry-admission-a1.md`
- Product contract: `STRATEGY/REQUIREMENTS.md`
- Architecture canon: `SPECS/ARCHITECTURE.md`
- Consultant canon: `SPECS/CONSULTANT.md`
- State baseline: `STATE.md`

## Git context

- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `origin/main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect` remove branch + worktree after merge

## One web search (mandatory before implementation)

- Query: `site:stately.ai/docs model-based testing state machines single source of truth`
- Date/time: `2026-03-13T14:37:41+05:00`
- Sources opened:
  - official Stately docs: `https://stately.ai/docs/graph`
  - official Stately docs: `https://stately.ai/docs/guards`
- Source quality:
  - official product documentation
- Found reusable ideas:
  - executable graph/path coverage should come from one explicit machine, not from several handwritten expectation layers;
  - guards should keep boundary decisions pure and explicit instead of spreading branch-heavy routing logic across the router.
- Decision:
  - `reuse`
  - treat the future beauty-salon boundary statechart as a compiled artifact from the interaction contract, not as a second handwritten semantic router.
- Rejected options:
  - implementing the statechart before contract/oracle unification;
  - another round of row-local patches before a fresh proof rerun;
  - keeping runtime, oracle, and audit as separate semantic sources.

## Root cause (mandatory)

- Symptom:
  - the system still fails on canonical beauty-salon dialogs even after many bounded row closures, and the first fail keeps moving between runtime/oracle/audit families.
- Minimal reproduction:
  1. Read `STATE.md` around the post-`P1.6o134` truth (`STATE.md:6785`..`STATE.md:6793`).
  2. Confirm the next admissible step is still one fresh proof rerun, not another local patch.
  3. Inspect executable owner coverage: `truffles-api/app/services/owner_resolver.py` only resolves rows that have `runtime_match` + `runtime_effects`.
  4. Inspect the matrix artifact and confirm only `M27` and `M33` currently expose executable runtime matching.
  5. Inspect `truffles-api/app/routers/webhook/decision.py`, `scripts/booking_dialog_scenarios.py`, and `ops/diagnose.py` and confirm pending-question semantics, owner names, and oracle expectations are still duplicated there.
- Evidence:
  - `STATE.md`
  - `truffles-api/app/services/owner_resolver.py`
  - `truffles-api/app/knowledge/generic/INTERACTION_OWNER_MATRIX.yaml`
  - `truffles-api/app/routers/webhook/decision.py`
  - `scripts/booking_dialog_scenarios.py`
  - `ops/diagnose.py`
- Five Whys:
  1. Why does first fail keep moving? Because semantic truth is still split across runtime, scenario/oracle, audit, and the policy core.
  2. Why is the split still present? Because the machine-readable owner matrix is only partially executable and most families still rely on handwritten router/oracle branches.
  3. Why does that matter even after bounded closures? Because each bounded fix can move the first fail without reducing the number of semantic engines.
  4. Why is a statechart not the next immediate answer? Because without first compiling runtime/oracle/audit from one contract, the statechart would become yet another semantic layer.
  5. Why must the next step still be a proof rerun? Because `P1.6o134` only restored admissible proof-lane admission; it did not surface the next truthful class on recovered runtime.
- Root cause statement:
  - the remaining blocker is structural duplication, not missing row-local phrase coverage: today `beauty-salon` closure is driven by four semantic engines (`llm_policy_core`, handwritten runtime owner/degrade logic, handwritten scenario/oracle logic, and audit inference), while executable matrix coverage remains narrow. Until the worktree converges onto one compiled interaction contract, first-fail churn will continue.
- Fix mechanism:
  - freeze a binding beauty-salon control-plane roadmap;
  - run exactly one fresh post-`P1.6o134` proof rerun;
  - only after truthful class routing, implement the next structural block in this order:
    1. contract compiler,
    2. executable owner coverage,
    3. boundary statechart,
    4. proof bundle.

## Reuse-first decision (mandatory)

- Reuse:
  - existing `InteractionState` contract
  - machine-readable interaction owner matrix
  - `OwnerResolver`
  - existing proof-lane guarded runner and audit chain
  - existing failure-family aggregation in `ops/diagnose.py`
- Integrate:
  - compile runtime expectations, scenario expectations, and audit evidence rules from one interaction contract instead of continuing handwritten convergence in each layer
- Build:
  - new compiler and statechart only after the fresh rerun confirms the next truthful class

## Release safety

- Rollout: none; this block is doc/program governance only
- Go signal:
  - active docs all point to the same ordered plan and forbid statechart-before-compiler
  - the next admissible operational step is explicit and singular
- No-go signal:
  - docs still allow local runtime/oracle patching before the rerun
  - docs imply that statechart can be implemented before contract compiler/unification
- Rollback:
  - revert the roadmap sync and return to the pre-roadmap wording; no runtime behavior changes are included here

## Invariant

1. `beauty-salon canonical closure` remains the only admissible closure target before any business-agnostic or multilingual claim.
2. No new semantic source of truth is introduced.
3. Statechart work is forbidden before contract compiler + executable owner coverage.
4. The immediate next admissible operational step remains exactly one fresh proof rerun after `P1.6o134`.

## Scope

- publish a binding roadmap for beauty-salon canonical closure
- freeze the target architecture and workstreams
- define the 30-day execution order and precise beauty-salon DoD
- sync parent remediation/program docs and `STATE.md` to this roadmap

## Out of scope

- running the proof rerun itself
- new runtime/scenario/oracle code in this block
- acceptance `lock/replay/full`
- multilingual or multi-pack closure claims

## Touch-list

- `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o135-beauty-salon-canonical-control-plane-roadmap-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o136-post-p1.6o134-fresh-proof-rerun-a1.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `STATE.md`
- `docs/SESSIONS/SESSION-2026-02-19-llm-first-firebreak-a1.md`
- `docs/SESSION_INDEX.md`

## Plan (1..N)

1. Publish the binding beauty-salon canonical control-plane roadmap.
2. Sync parent remediation/program docs and `STATE.md` to the new roadmap.
3. Publish the next admissible proof-lane TP (`P1.6o136`) so the rerun is explicit.
4. After the rerun, route the surfaced class only through the frozen structural order.

## 30-Day Roadmap

| Window | Workstream | Objective | Exit signal |
|---|---|---|---|
| Days 1-3 | Proof | run one fresh post-`P1.6o134` rerun and classify the first fail truthfully | rerun audited; class routed without row-local patch reflex |
| Days 3-10 | Oracle | build interaction-contract compiler for scenario expectations and audit evidence rules | runtime/oracle/audit stop carrying separate owner names for the covered class |
| Days 7-16 | Runtime | extend executable owner coverage beyond `M27`/`M33` for the active surfaced family set | covered family resolves through matrix/resolver path, not branch-local owner writes |
| Days 14-22 | Runtime Boundary | introduce boundary statechart for `collect/info_interrupt/pending/handoff/degraded/resume` compiled from the contract | timeout/pending transitions no longer depend on giant-router local branches |
| Days 20-27 | Dataset | assemble golden beauty-salon corpus (`booking/info/interrupt/handoff/timeout/RU/KZ/code-switch`) | corpus versioned and used by deterministic + proof lanes |
| Days 25-30 | Proof Bundle | run `deterministic -> dev L2 -> lock -> replay -> audit` on beauty-salon only | proof bundle green or blocked by one explicit remaining family |

## Target Architecture (binding)

```text
LLM policy core
  -> canonical interaction contract
  -> compiler outputs:
       - runtime owner/boundary tables
       - scenario/oracle expectations
       - audit evidence rules
  -> runtime boundary engine
       - collect
       - info_interrupt
       - pending
       - handoff
       - degraded
       - resume
  -> proof bundle
       - deterministic
       - dev L2
       - lock
       - replay
       - audit
```

## Workstreams (binding)

| Workstream | What is allowed | What is forbidden | First deterministic gate |
|---|---|---|---|
| Runtime | move owner/boundary behavior into executable contract paths | new owner-specific handwritten branches in `decision.py` | `pytest -q truffles-api/tests/test_owner_resolver.py` |
| Oracle | compile scenario expectations from canonical contract | tag-by-tag handwritten semantic drift fixes as the main path | `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py` |
| Proof | failure-family routing, strict audit, admissible reruns | treating non-canonical runs as closure proof | `python3 ops/diagnose.py llm-quality-audit --run-dir <run-dir> --status done --strict-artifacts` |
| Dataset | golden beauty-salon corpus only | multilingual/business-agnostic claim expansion before salon closure | deterministic corpus validation command for the published corpus |
| Product | beauty-salon canonical closure only | P6 closure claims before beauty-salon proof bundle | beauty-salon DoD checklist |

## Beauty-Salon Canonical Closure DoD (binding)

1. One semantic source of truth exists for the covered families: runtime/oracle/audit are compiled from the same interaction contract.
2. Executable owner coverage is no longer limited to `M27` and `M33` for the active surfaced families.
3. Boundary lifecycle transitions (`collect/info_interrupt/pending/handoff/degraded/resume`) are explicit and auditable.
4. No text-inferred audit failure can override valid contract/meta/trace evidence on active booking turns.
5. Golden beauty-salon corpus exists and is versioned for deterministic + guarded proof lanes.
6. One full beauty-salon proof bundle is green: deterministic, fresh `dev L2`, `lock`, `replay`, `audit`.
7. `STATE.md` contains closure evidence for beauty-salon only; no multilingual or business-agnostic closure claim is made here.

## DoD

1. A binding roadmap doc exists in the current worktree.
2. Parent remediation and closure-program docs point to the same ordered plan.
3. `STATE.md` records the roadmap truth and the singular next admissible operational step.
4. The next code work is constrained to the frozen order from this block.

## Checks

- `git diff --check`
- `SESSION_AGENT=a1 scripts/session_check.sh`

## Execution status

- `done`
- the beauty-salon canonical control-plane roadmap is now the binding execution order for this worktree
- all active parent docs and `STATE.md` point to one immediate operational next step: fresh proof rerun `P1.6o136`
- the worktree is explicitly forbidden from implementing a boundary statechart before contract compiler + executable owner coverage

## Evidence

1. `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o135-beauty-salon-canonical-control-plane-roadmap-a1.md`
2. `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o136-post-p1.6o134-fresh-proof-rerun-a1.md`
3. `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
4. `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
5. `STATE.md`

## Rollback

1. Revert the roadmap doc and parent-doc sync.
2. Restore the previous wording that only pointed to a fresh rerun without the frozen execution order.
3. Do not reinterpret this doc-only block as runtime/proof closure evidence.

## No-go

- do not implement the boundary statechart before the contract compiler
- do not open multilingual/business-agnostic closure work from this block
- do not publish another row-local semantic patch before the fresh rerun
- do not treat this roadmap as closure proof

## Risks / blockers

- the team can still regress to row-local churn under pressure
- the fresh rerun can surface a genuinely new class that forces another update to the roadmap details
- executable matrix coverage may stay too narrow if compiler work is scoped as doc-only and never reaches runtime/oracle/audit

## Residual architecture debt (mandatory)

### Current residuals accepted in this block

- executable matrix coverage is still narrow (`M27` and `M33` only)
- runtime/oracle/audit still duplicate semantic ownership today
- no fresh post-`P1.6o134` rerun evidence yet

### Why not in this block

- this block freezes the roadmap and next-step order; it does not execute the proof rerun or structural code

### Risk if deferred

- future work can again branch into local fixes without reducing the number of semantic engines

### Linked follow-up Task Package(s)

- `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o136-post-p1.6o134-fresh-proof-rerun-a1.md`
- next structural block selected only after `P1.6o136` truthful class routing

### Expiry/trigger to stop deferral

- immediately after `P1.6o136` finishes and the next structural class is known

## Next-block contract (mandatory)

### Next block objective

- execute exactly one fresh post-`P1.6o134` proof rerun on the recovered fingerprint-verified runtime and classify the surfaced class truthfully before any new structural implementation.

### First deterministic check command

- `git rev-parse HEAD && curl -sf http://127.0.0.1:18184/admin/version && git diff --check`

### Blocked-by conditions

- parent docs and `STATE.md` are not yet synced to this roadmap
- local runtime fingerprint does not match `HEAD`
- the team attempts structural code before the rerun

### Owner role for closure

- `Top Architect + Brain`

# TP-2026-03-09-p1.6o54-timeout-master-info-interrupt-name-collect-owner-a1.md

- Название/цель: закрыть surfaced `r64` blocker family, где после `P1.6o53` runtime больше не падает на question-like specialist follow-up `Можно к Айгерим?`, но следующий turn `Можно к мастеру Динара?` под активным `expected_reply_type=name` уходит в `policy_core timeout -> degraded booking collect` и теряет existing `master info interrupt` owner.
- Parent TP: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Canon refs: `AGENTS.md`, `STATE.md`, `STRUCTURE.md`, `STRATEGY/REQUIREMENTS.md`, `docs/runbooks/BOOKING_CONFIRM_VERIFY.md`, `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o53-specialist-followup-hint-timeout-blocked-by-info-a1.md`
- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect remove worktree + branch after merge`

## One web search (mandatory before implementation)

- Query: `Rasa forms slot filling interruptions questions unhappy path official docs`
- Time (UTC): `2026-03-09T10:18:05Z`
- Sources:
  - `https://rasa.com/docs/rasa/forms/`
- Ready solutions found:
  - form/slot ownership should survive side-question interruptions and resume the active slot after the info reply;
  - when primary policy/extraction fails, reuse the existing owner contract rather than degrading to a generic slot prompt.
- Decision (`reuse/integrate/build`): `reuse + integrate`
  - reuse existing `booking_interrupt -> master info` owner under active `name` collect;
  - integrate one bounded timeout-degrade recovery into the existing policy-core guard path;
  - build no new semantic regex router in runtime core.
- Rejected options:
  - accepting generic `booking_prompt` as equivalent to master info interrupt;
  - weakening `expected_info_section` oracle;
  - adding specialist-name phrase branches in timeout fallback.

## Root cause (mandatory)

- Symptom:
  - guarded `dev L2` run `p1.6o6-l2-dev-20260309-a1-r64` stops on dialog 1 turn 5 with `expected_info_section_miss` / `info_section_miss` for `Можно к мастеру Динара?`.

- Minimal reproduction:
  1. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r64/scenarios.json` dialog 1 turns 4-5 and confirm active `reply_type=name` is preserved after pricing interrupt, while turn 5 expects `master` info sections plus resume of `name` collect;
  2. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r64/trace_bundle.jsonl` dialog 1 turn 5 and confirm `llm_policy_core` times out, `policy_core_guard` falls back to `timeout_booking_collect`, and runtime emits only degraded `booking_prompt` with `missing_slot=name`;
  3. inspect `truffles-api/app/routers/webhook/decision.py` timeout-degrade path and confirm it already has special recovery for `timeout_pending_slot_question` and `timeout_specialist_followup`, but no bounded recovery for generic `master info interrupt` under active `name` collect.

- Evidence:
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r64/summary.json`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r64/brief.md`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r64/scenarios.json`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r64/responses.jsonl`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r64/trace_bundle.jsonl`
  - `truffles-api/app/routers/webhook/decision.py`

- Five Whys:
  1. Why did the turn fail: scenario expected `master` info interrupt, but runtime produced a generic degraded booking prompt.
  2. Why did runtime produce a generic degraded prompt: policy-core timed out and timeout fallback stayed in `booking_collect`.
  3. Why did timeout fallback stay in collect: timeout guard only has bounded recoveries for slot-question and specialist-followup families, not for generic master-info interrupt under active `name` collect.
  4. Why is that a contract gap: the runtime already owns this family in the non-timeout path through `_handle_booking_interrupt`, so timeout degrade is losing an existing owner.
  5. Why is this architectural: degrade-path observability exists, but owner continuity is incomplete for booking-state info interrupts during collect.

- Root cause statement:
  - policy-core timeout degrade does not reuse the existing `master info interrupt` owner when the active booking question is `name`, so a bounded info-interrupt turn collapses into generic degraded collect and loses `master` info evidence.

- Fix mechanism:
  - add one bounded timeout-degrade recovery that reuses existing `booking_interrupt` master-info owner under active `name` collect;
  - add targeted timeout/master-info regressions;
  - rerun guarded dev after bounded closure.

## Release safety

- Strategy: bounded timeout master-info recovery only
- Go signals:
  - timeout-degraded master questions under active `name` collect answer `master` info and resume `name`
  - existing non-timeout master-info interrupt behavior stays unchanged
- No-go signals:
  - generic collect still wins for the same family
  - new phrase-hardcode in core
- Rollback verification:
  - revert only the timeout master-info recovery delta and its regressions if it steals unrelated timeout collect turns

## Invariant

1. `P1.6o53` closure remains intact: specialist follow-up under active `time` collect still survives hint timeout.
2. Generic master info interrupts under active `name` collect keep answering info first, then resume `name`.
3. No timeout fallback invents availability or booking confirmation facts.

## Scope

- `truffles-api/app/routers/webhook/decision.py`
- targeted timeout/master-info regressions
- tracker docs after rerun handoff

## Out of scope

- full timeout-degrade redesign
- acceptance/full proof closure
- infra/tool-evidence red in guarded runs

## Touch-list

- `truffles-api/app/routers/webhook/decision.py`
- `truffles-api/tests/test_message_endpoint.py`
- `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o54-timeout-master-info-interrupt-name-collect-owner-a1.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`

## Plan (1..N)

1. Freeze `r64` as bounded runtime timeout evidence.
2. Reuse existing `booking_interrupt` master-info owner in timeout-degrade path under active `name` collect.
3. Add targeted timeout/master-info regressions.
4. Run focused checks and `git diff --check`.
5. Run one fresh guarded dev rerun beyond the `r64` family.

## DoD

1. Timeout-degraded `master` question under active `name` collect answers `master` info and resumes `name`.
2. Focused regressions pass.
3. One fresh guarded rerun no longer stops on the `r64` family.

## Checks

- `pytest -q truffles-api/tests/test_message_endpoint.py -k "master_info_interrupt or timeout_master_info or specialist_followup"`
- `git diff --check`

## Execution status

- `done`

## Evidence

1. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r64/summary.json`
2. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r64/brief.md`
3. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r64/scenarios.json`
4. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r64/responses.jsonl`
5. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r64/trace_bundle.jsonl`
6. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r64/manual_audit.md`

## Rollback

1. Revert only the timeout master-info recovery delta and targeted regressions.
2. Keep `P1.6o6` blocked; do not reinterpret `r64` as closure evidence.

## No-go

- no phrase-hardcode for specialist/master names
- no oracle waiver for missing `master` info sections
- no silent degrade to generic collect for the same contract

## Risks/Blockers

1. `r64` remains infra-invalid on unrelated tool-evidence checks, so it is architectural evidence only.
2. The next surfaced family may still be another timeout-degrade owner gap.

## Residual architecture debt (mandatory)

- Current residuals accepted in this block:
  - broader timeout-degrade owner matrix is still incomplete outside this bounded `master/name` family
- Why not in this block:
  - this slice only closes the surfaced `master info interrupt` timeout gap
- Risk if deferred:
  - name-collect side questions about specialists will keep degrading to generic prompts whenever policy-core times out
- Linked follow-up Task Package(s):
  - `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`
- Expiry/trigger to stop deferral:
  - next guarded rerun after bounded runtime closure

## Next-block contract (mandatory)

- Next block objective:
  - execute `P1.6o55` and run guarded `r66` so the next surfaced blocker is no longer the timeout master-info interrupt family from `r64`
- First deterministic check command:
  - `pytest -q truffles-api/tests/test_message_endpoint.py -k "master_info_interrupt or timeout_master_info or specialist_followup" && git diff --check`
- Blocked-by conditions:
  - timeout master-info regressions are not green
  - `r64` audit not reflected in tracker docs
  - fresh rerun preflight invalid
- Owner role for closure:
  - `Hands` for bounded runtime closure + rerun evidence, `Top Architect + Brain` for proof-bundle promotion

# TP-2026-03-08-p1.6o32-specialist-reference-time-collect-normalization-a1.md

- Название/цель: закрыть новый `r31` deterministic blocker family, где specialist-reference booking turns under active `reply_type=time` drift back to `service_choice` expectations, although runtime and judge both treat them as continuing time collect.
- Parent TP: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Canon refs: `AGENTS.md`, `STATE.md`, `STRUCTURE.md`, `STRATEGY/REQUIREMENTS.md`, `docs/runbooks/BOOKING_CONFIRM_VERIFY.md`, `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`, `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o31-tool-registry-missing-slot-pending-question-evidence-a1.md`
- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect remove worktree + branch after merge`

## One web search (mandatory before implementation)

- Query: `site:rasa.com/docs/rasa/forms requested_slot slot value correction follow-up answer specialist preference`
- Time (UTC): `2026-03-08T11:58:00Z`
- Sources:
  - `https://rasa.com/docs/rasa/forms/`
- Ready solutions found:
  - when a follow-up utterance supplies an additional preference while the form still waits on another requested slot, the requested-slot contract should stay active unless the new utterance truly changes the requested slot; preference mentions should not reset the form back to an unrelated slot family.
- Decision (`reuse/integrate/build`): `reuse + integrate`
  - reuse the existing active `reply_type=time` collect contract;
  - integrate scenario normalization so specialist-reference booking turns under active time collect do not drift back to `service_choice`;
  - build no new runtime branching until deterministic taxonomy is aligned.
- Rejected options:
  - forcing runtime to emit `service_choice` just to match the scenario;
  - changing pending-question specialist runtime ownership without deterministic proof that the current failure is runtime-side;
  - phrase-specific fixes outside existing specialist-reference detection.

## Root cause (mandatory)

- Symptom:
  - guarded `dev L2` rerun `p1.6o6-l2-dev-20260308-a1-r31` progressed beyond the `P1.6o31` family, then stopped on dialog `1`, turn `3`, text `Хотелось бы к Айгерим.` with `expected_meta_mismatch` / `expected_trace_miss`.

- Minimal reproduction:
  1. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r31/scenarios.json` for dialog `1`, turns `2-4`
  2. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r31/responses.jsonl` for message id `LLM-QUAL-p1.6o6-l2-dev-20260308-a1-r31-001-03-7d1c21`
  3. compare scenario expectation `expected_reply_type=service_choice` against runtime meta/trace `expected_reply_type=time`
  4. inspect `scripts/booking_dialog_scenarios.py` stateful booking expectation normalization for active time collect with specialist-reference text

- Evidence:
  - turn `2` (`Могу я выбрать специалиста?`) already passed with:
    - `source=booking_info_contract`
    - `pending_question_target=specialist`
    - `expected_reply_type=time`
  - turn `3` runtime then replied `На какую дату и время вам удобно?`, kept `expected_reply_type=time`, and judge marked the turn `pass`.
  - scenario still expected `service_choice` on turn `3`, and turn `4` was generated with the same drift.
  - current failure is therefore deterministic taxonomy/oracle drift after a specialist-reference follow-up, not the already-closed `P1.6o31` runtime evidence gap.

- Five Whys:
  1. Why did `r31` fail: scenario expected `service_choice`, but runtime preserved `time`.
  2. Why did scenario expect `service_choice`: booking-tag specialist-reference follow-up kept an LLM-provided `service_choice` override.
  3. Why is that wrong here: the dialog was already in active `reply_type=time` collect after turn `2`; specialist preference did not reopen missing service collection.
  4. Why was it not normalized: current stateful normalization handles service-grounded booking turns, mixed slot questions, and explicit slot fills, but not specialist-reference booking turns under active time collect.
  5. Why did it surface now: `P1.6o31` removed the previous runtime observability blocker, so the next deterministic drift became first failure.

- Root cause statement:
  - scenario sanitizer lacks stateful normalization for specialist-reference booking turns under active `reply_type=time`, so those turns drift back to `service_choice` expectations even though the collect contract remains on time.

- Fix mechanism:
  - normalize booking-tag specialist-reference turns back to `reply_type=time` when the active collect contract already waits for time;
  - add deterministic regressions for both positive and negative cases;
  - then run one fresh guarded rerun `r32`.

## Release safety

- Strategy: deterministic scenario/oracle normalization only
- Go signals:
  - specialist-reference booking turns under active time collect no longer expect `service_choice`
  - generic service-choice flows without active time collect remain unchanged
- No-go signals:
  - runtime-core changes in this block
  - weakening oracle to accept both `service_choice` and `time` for the same turn family
- Rollback verification:
  - revert only the new normalization if unrelated specialist/service-choice families regress

## Invariant

1. `P1.6o31` stays closed.
2. Active time collect must remain `time`; specialist preference must not silently reopen service collection.
3. Generic non-active specialist/service-choice clarifications must remain available outside this bounded family.
4. Fresh expensive iteration stays exactly one guarded rerun after deterministic closure.

## Scope

- `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o32-specialist-reference-time-collect-normalization-a1.md`
- `scripts/booking_dialog_scenarios.py`
- `truffles-api/tests/test_booking_dialog_scenarios_script.py`

## Out of scope

- runtime specialist-preference capture
- pending-question specialist owner redesign
- acceptance replay/full promotion

## Touch-list

- `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o32-specialist-reference-time-collect-normalization-a1.md`
- `scripts/booking_dialog_scenarios.py`
- `truffles-api/tests/test_booking_dialog_scenarios_script.py`

## Plan (1..N)

1. Freeze `r31` as non-closure evidence and isolate the new failure as taxonomy drift after the now-closed `P1.6o31`.
2. Normalize specialist-reference booking turns under active `reply_type=time` back to `time`.
3. Add deterministic regressions for active-time positive case and inactive negative case.
4. Run one fresh guarded rerun `r32` only after deterministic checks are green.

## DoD

1. Specialist-reference booking turns under active time collect no longer retain `service_choice` expectation drift.
2. Deterministic scenario-script regression is green.
3. `r31` is recorded as non-closure evidence; `P1.6o31` closure stands.
4. Next expensive step is exactly one fresh guarded rerun `r32`.

## Expensive run control (mandatory)

- Hypothesis:
  - after specialist-reference normalization, the next guarded rerun will not fail first on the `r31` `service_choice` drift family.
- Expected measurable effect:
  - either the run proceeds beyond dialog `1`, turn `3`, or a different blocker family surfaces.
- Stop condition:
  - run exactly one fresh guarded rerun plus strict audit; if the same mismatch persists, stop and reopen RCA instead of stacking more normalization patches.

## Checks

- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r31 --status done --strict-artifacts`
- `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "specialist_reference or pending_question or service_grounded_booking"`
- `git diff --check`

## Execution status

- `in_progress`

## Evidence

1. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r31/summary.json`
2. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r31/brief.md`
3. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r31/scenarios.json`
4. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r31/responses.jsonl`
5. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r31/trace_bundle.jsonl`
6. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r31/manual_audit.md`

## Rollback

1. Revert only the specialist-reference normalization if unrelated `service_choice` families regress.
2. Keep `P1.6o6` blocked; do not reinterpret `r31` as closure evidence.

## No-go

- no runtime-core edits
- no oracle weakening to accept contradictory `service_choice` / `time` contracts
- no fresh guarded rerun before deterministic scenario regression is green

## Risks/Blockers

1. A distinct runtime family may surface on the next turn once the deterministic drift is removed.
2. Infra tool-evidence invalidity remains orthogonal and can still make the guarded run non-canonical.

## Residual architecture debt (mandatory)

- Current residuals accepted in this block:
  - specialist-target runtime ownership beyond scenario normalization remains open until surfaced by a post-normalization rerun
- Why not in this block:
  - current first blocker is deterministic expectation drift, and runtime should not be rewritten to match a contradictory scenario
- Risk if deferred:
  - expensive runs keep stopping before deeper specialist-target runtime issues can surface honestly
- Linked follow-up Task Package(s):
  - `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`
- Expiry/trigger to stop deferral:
  - before the next guarded rerun after `P1.6o32`

## Next-block contract (mandatory)

- Next block objective:
  - close bounded `P1.6o32`, then run one fresh guarded rerun `r32` to verify that specialist-reference booking turns no longer drift to `service_choice`
- First deterministic check command:
  - `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "specialist_reference or pending_question or service_grounded_booking" && git diff --check`
- Blocked-by conditions:
  - `P1.6o32` is not deterministically green
  - guarded preflight is not green
  - runtime `http://127.0.0.1:18184` is unavailable
- Owner role for closure:
  - `Hands` for doc/code/evidence, `Top Architect + Brain` for closure gating

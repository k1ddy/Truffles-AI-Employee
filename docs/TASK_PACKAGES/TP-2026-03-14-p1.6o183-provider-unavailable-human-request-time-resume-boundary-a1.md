# TP-2026-03-14-p1.6o183-provider-unavailable-human-request-time-resume-boundary-a1

- Название/цель: закрыть surfaced `P1.6o182` boundary family, где strict-audited rerun показал `dialog 1 / turn 7 / "У вас есть время на 15:00?"`: после `provider_unavailable` booking interrupt на turn `5` и `human_request -> pending -> manager_active -> bot_active` на turn `6` runtime теряет booking-interrupt time resume, очищает grounded datetime continuity, и на следующем time-availability followup уходит в generic `policy_core_degraded_collect` с ответом `На какую дату и время вам удобно?`, который judge маркирует как `missed_question`.

## Canon refs

- Binding roadmap: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o135-beauty-salon-canonical-control-plane-roadmap-a1.md`
- Prior proof lane: `docs/TASK_PACKAGES/TP-2026-03-14-p1.6o182-post-p1.6o181-proof-lane-a1.md`
- Related pending/handoff boundary slices:
  - `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o127-pending-handoff-resume-boundary-first-slice-a1.md`
  - `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o141-timeout-degrade-pending-boundary-statechart-first-slice-a1.md`
  - `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o143-pending-soft-pass-booking-resume-slot-timeout-boundary-a1.md`
- Related provider-unavailable contour: `truffles-api/tests/test_message_endpoint.py`
- Parent closure program: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Main remediation: `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- State baseline: `STATE.md`

## Git context

- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `origin/main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect` remove branch + worktree after merge

## One web search (mandatory before implementation)

- Query: `site:stately.ai/docs history states resume previous state after interruption`
- Time: `2026-03-14T13:31:46+05:00`
- Sources opened:
  - `https://stately.ai/docs/history-states`
- Source quality:
  - official product documentation
- Ready solutions found:
  - when a parent state is exited and later re-entered, a history state restores the last active child state instead of falling back to an unrelated initial/default child state;
  - this is the correct statechart model for handoff/resolution loops that must resume the interrupted booking substate.
- Decision (`reuse/integrate/build`): `reuse + integrate`
  - reuse the existing pending-resume / boundary resume contracts and booking-interrupt expected-reply state;
  - integrate one bounded restore path for the provider-unavailable + human-request resolved contour so bot re-enters the prior booking time followup instead of generic collect.
- Rejected options:
  - weakening scenario/judge expectations to accept the generic date-time prompt;
  - silently dropping booking-interrupt resume state on human request;
  - broad rewrite of all manager-active / resolve logic outside the surfaced contour.

## Root cause (mandatory)

- Symptom:
  - `P1.6o182` is admissible (`infra_valid=true`) but stops on `dialog 1 / turn 7 / "У вас есть время на 15:00?"` with `expected_meta_mismatch`, `expected_trace_miss`, and `judge_fail`.
- Minimal reproduction:
  1. inspect `/tmp/booking_quality/p1.6o182-l2-dev-20260314-a1-r1/responses.jsonl` for turns `5-7`;
  2. confirm turn `5` records `tool_registry decision=provider_unavailable` for `calendar.book_slot`, then sets `expected_reply_type=time` with `reason=booking_interrupt`;
  3. confirm turn `6` is `human_request`, bypasses expected-reply state, transitions `pending -> manager_active -> bot_active`, and clears the active time resume contract;
  4. confirm turn `7` arrives in `bot_active` without restored booking-interrupt time followup, `llm_policy_core` times out, and runtime falls through to generic `policy_core_degraded_collect` / `expected_reply_type=time`;
  5. confirm judge marks the response as `missed_question`, because the bot ignores the explicit availability request at `15:00` and asks again for date and time.
- Evidence:
  - `/tmp/booking_quality/p1.6o182-l2-dev-20260314-a1-r1/summary.json`
  - `/tmp/booking_quality/p1.6o182-l2-dev-20260314-a1-r1/brief.md`
  - `/tmp/booking_quality/p1.6o182-l2-dev-20260314-a1-r1/responses.jsonl`
  - `/tmp/booking_quality/p1.6o182-l2-dev-20260314-a1-r1/trace_bundle.jsonl`
  - `/tmp/booking_quality/p1.6o182-l2-dev-20260314-a1-r1/manual_audit.json`
  - `truffles-api/app/routers/webhook/decision.py`
  - `truffles-api/tests/test_message_endpoint.py`
- Five Whys:
  1. Why did the rerun fail? Because post-handoff runtime answered with a generic date-time prompt and missed the explicit availability question.
  2. Why did runtime answer generically? Because by turn `7` the prior `booking_interrupt -> expected_reply_type=time` resume state was gone.
  3. Why was the resume state gone? Because turn `6` `human_request` bypassed expected-reply handling and the subsequent `pending -> manager_active -> bot_active` resolve path did not restore the interrupted booking child state.
  4. Why is that a bug? Because manager handoff/resolution is an interruption boundary; once it resolves, the booking flow should re-enter the last active booking child state instead of a default generic collect path.
  5. Why is this not only scenario drift? Because judge independently fails the runtime response as a missed question.
- Root cause statement:
  - the provider-unavailable booking-interrupt time resume contract is not preserved across the `human_request -> pending -> manager_active -> bot_active` resolved boundary, so the next booking availability followup re-enters generic collect instead of the interrupted booking child state.
- Fix mechanism:
  - add one bounded boundary-resume restore for the surfaced provider-unavailable + human-request resolved contour, so resolved manager handoff re-enters the prior booking-interrupt time followup contract with preserved booking context and expected-reply evidence.

## Reuse-first decision (mandatory)

- Reuse:
  - existing pending-resume capture/restore helpers
  - existing booking-interrupt `expected_reply_type=time` contract
  - existing pending/handoff boundary traces and manager simulation flow
- Integrate:
  - bounded restore after resolved human-request interruption in the surfaced contour
  - targeted runtime regression for provider-unavailable + manager-resolved time followup
- Build:
  - no new state machine framework in this block

## Release safety

- Rollout: bounded provider-unavailable human-request resume restore only
- Go signal:
  - after resolved human-request interruption, explicit time-availability followup no longer collapses to generic `policy_core_degraded_collect`
- No-go signal:
  - unresolved/pending conversations are incorrectly forced back to bot-active booking collect
  - unrelated human requests restore stale booking state
  - booking-interrupt/provider-unavailable turns lose timeout/handoff provenance
- Rollback:
  - revert only the bounded resume-restore branch and targeted regression test

## Invariant

1. No oracle/judge weakening.
2. No phrase-hardcode in core.
3. No broad rewrite of all handoff resolution logic.
4. Existing pending soft-pass / timeout resume boundary slices remain green.
5. No expensive proof rerun before deterministic closure is green.

## Scope

- freeze `P1.6o182` truth in active docs/state/session
- publish `P1.6o183`
- implement bounded provider-unavailable human-request time-resume restore in runtime
- add targeted deterministic regression
- publish the next proof lane only after deterministic closure is green

## Out of scope

- broader manager queue redesign
- scenario/oracle rewrites outside the surfaced family
- acceptance lock/replay/full
- multilingual/open-world work

## Touch-list

- `docs/TASK_PACKAGES/TP-2026-03-14-p1.6o182-post-p1.6o181-proof-lane-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-14-p1.6o183-provider-unavailable-human-request-time-resume-boundary-a1.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `STATE.md`
- `docs/SESSIONS/SESSION-2026-02-19-llm-first-firebreak-a1.md`
- `docs/SESSION_INDEX.md`
- `truffles-api/app/routers/webhook/decision.py`
- `truffles-api/tests/test_message_endpoint.py`

## Plan

1. Freeze `P1.6o182` truth as an admissible provider-unavailable human-request resume family and publish `P1.6o183`.
2. Reuse pending-resume / booking-interrupt resume primitives for the resolved handoff contour.
3. Add one targeted runtime regression for the surfaced family.
4. Run focused checks and session gate.
5. Sync canon and publish the next admissible proof lane.

## DoD

1. `P1.6o182` truth is synced and `P1.6o183` is the selected continuation.
2. Resolved human-request interruption no longer destroys provider-unavailable booking time resume on deterministic contour.
3. Runtime preserves explicit boundary provenance plus restored booking-interrupt followup contract.
4. Focused deterministic checks and session gate are green.
5. The next admissible block is a fresh post-`P1.6o183` proof lane.

## Checks

- `python3 -m py_compile truffles-api/app/routers/webhook/decision.py truffles-api/tests/test_message_endpoint.py`
- `pytest -q truffles-api/tests/test_message_endpoint.py -k "provider_unavailable and human_request and pending_resume"`
- `git diff --check`
- `SESSION_AGENT=a1 scripts/session_check.sh`

## Evidence

1. `/tmp/booking_quality/p1.6o182-l2-dev-20260314-a1-r1/summary.json`
2. `/tmp/booking_quality/p1.6o182-l2-dev-20260314-a1-r1/brief.md`
3. `/tmp/booking_quality/p1.6o182-l2-dev-20260314-a1-r1/responses.jsonl`
4. `/tmp/booking_quality/p1.6o182-l2-dev-20260314-a1-r1/trace_bundle.jsonl`
5. `/tmp/booking_quality/p1.6o182-l2-dev-20260314-a1-r1/manual_audit.json`
6. `truffles-api/app/routers/webhook/decision.py`
7. `truffles-api/tests/test_message_endpoint.py`
8. updated `STATE.md` / session docs

## Rollback

1. Keep `P1.6o182` proof truth intact.
2. Revert only the bounded resolved-handoff resume restore and targeted regression.
3. Do not mutate proof artifacts or gates.

## No-go

- no oracle weakening
- no phrase-hardcode in core
- no new expensive proof rerun before deterministic closure is green

## Risks / blockers

- resume restore could over-apply to genuine finished handoffs
- provider-unavailable booking interrupt may still carry incomplete slot state that needs careful re-entry gating
- proof remains non-canonical until the next rerun is audited

## Residual architecture debt (mandatory)

### Current residuals accepted in this block

- resolved handoff resume continuity still lives in `decision.py`, not an explicit boundary statechart/history-state artifact
- proof remains `dev` lane only until the next rerun confirms the surfaced family moved off first fail

### Why not in this block

- this block closes only the surfaced provider-unavailable human-request resume family

### Risk if deferred

- resolved handoff interruptions keep erasing interrupted booking child-state continuity and hiding later remaining families

### Linked follow-up Task Package(s)

- post-`P1.6o183` proof lane TP to be published after deterministic closure

### Expiry/trigger to stop deferral

- immediately after deterministic closure and next strict-audited rerun selection

## Next-block contract (mandatory)

### Next block objective

- run one fresh guarded proof rerun after deterministic closure of `P1.6o183` and determine whether the surfaced resolved-handoff resume family moved off first fail.

### First deterministic check command

- `python3 -m py_compile truffles-api/app/routers/webhook/decision.py truffles-api/tests/test_message_endpoint.py && pytest -q truffles-api/tests/test_message_endpoint.py -k "provider_unavailable and human_request and pending_resume"`

### Blocked-by conditions

- targeted deterministic checks are not green
- `P1.6o182` truth is not synced in active docs/state/session
- any new expensive rerun is attempted before this deterministic closure is complete

### Owner role for closure

- `Top Architect + Brain`

## Execution status

- `done`

## Execution result

- Deterministic closure is green:
  - `python3 -m py_compile truffles-api/app/routers/webhook/decision.py truffles-api/tests/test_message_endpoint.py`
  - `pytest -q truffles-api/tests/test_message_endpoint.py -k "provider_unavailable and human_request and pending_resume"` -> `3 passed, 411 deselected`
  - `git diff --check`
  - `SESSION_AGENT=a1 scripts/session_check.sh`
- Runtime change:
  - `truffles-api/app/routers/webhook/decision.py` now restores resolved-handoff booking resume before canonical sync can erase the prior `booking_interrupt` reason, reuses the bounded booking resume payload, and emits explicit `resolved_handoff_resume_boundary` trace/meta evidence.
- Targeted regression:
  - `truffles-api/tests/test_message_endpoint.py` now covers positive restore, negative no-boundary skip, and timeout resume-boundary continuity after `provider_unavailable -> human_request -> manager-resolve -> bot_active`.
- Published `P1.6o184` as the next admissible continuation:
  - exactly one fresh guarded proof rerun is now allowed after deterministic closure of `P1.6o183`;
  - no additional runtime refactor is admissible before that rerun is strict-audited.

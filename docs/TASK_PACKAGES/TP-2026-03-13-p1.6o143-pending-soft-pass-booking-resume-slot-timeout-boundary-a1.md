# TP-2026-03-13-p1.6o143-pending-soft-pass-booking-resume-slot-timeout-boundary-a1

- Название/цель: после surfaced `P1.6o142` закрыть следующий bounded boundary-statechart slice: under `pending -> soft_pass -> policy_core timeout`, runtime must preserve or re-derive active booking resume-slot continuity from live contract state and return collect prompt instead of collapsing the turn into `policy_core_degraded_hold`.

## Canon refs

- Binding roadmap: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o135-beauty-salon-canonical-control-plane-roadmap-a1.md`
- Prior proof lane: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o142-post-p1.6o141-proof-lane-a1.md`
- Prior boundary slice: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o141-timeout-degrade-pending-boundary-statechart-first-slice-a1.md`
- Prior pending/handoff boundary slice: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o127-pending-handoff-resume-boundary-first-slice-a1.md`
- Class-level routing: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o118-class-level-closure-process-reset-a1.md`
- Parent closure program: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Main remediation: `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- State baseline: `STATE.md`

## Git context

- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `origin/main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect` remove branch + worktree after merge

## One web search (mandatory before implementation)

- Query: `site:stately.ai/docs xstate guards context transitions pending handoff restore`
- Date/time: `2026-03-13T16:36:36+05:00`
- Sources opened:
  - official Stately/XState docs: `https://stately.ai/docs/guards`
  - official Stately/XState docs: `https://stately.ai/docs/context`
  - official Stately/XState docs: `https://stately.ai/docs/transitions`
- Source quality:
  - official product documentation
- Found reusable ideas:
  - the pending/handoff branch should choose its next transition from explicit guards over preserved context, not from an implicit fallback after context reset;
  - context must stay the single runtime truth that guarded timeout/degrade transitions read before selecting `collect` vs `handoff`.
- Decision:
  - `reuse`
  - reuse the guarded-transition pattern for the next boundary slice: preserve or re-derive booking resume-slot state before `pending_hold`, and allow the timeout boundary to resolve through one explicit collect recovery path.
- Rejected options:
  - another scenario/oracle-only patch;
  - another proof rerun before structural closure;
  - broad state reset exemptions for all pending turns.

## Root cause (mandatory)

- Symptom:
  - `P1.6o142` is infra-valid and proves `P1.6o141` moved off first fail, but surfaces a new first failure at `dialog 2 / turn 5 / "Если да, то в какое время?"`: runtime ends the turn in `pending` with `policy_core_guard_pending_hold` instead of preserving booking follow-up continuity.
- Minimal reproduction:
  1. Open `/tmp/booking_quality/p1.6o142-l2-dev-20260313-a1-r1/summary.json` and confirm `infra_valid=true`, `semantic_valid=false`, `stop_reason=max_failures_reached:1`.
  2. Inspect `/tmp/booking_quality/p1.6o142-l2-dev-20260313-a1-r1/responses.jsonl` for `message_id=LLM-QUAL-p1.6o142-l2-dev-20260313-a1-r1-002-05-e1bccd`.
  3. Confirm the failing turn records:
     - `conversation_state=pending`
     - `decision_meta.pending_guard=policy_core_degraded`
     - `decision_meta.pending_action=policy_core_degraded_hold`
     - `decision_meta.delivery_state=transport_degraded`
     - `decision_meta.policy_core_degrade_reason=policy_error:timeout`
  4. Inspect the same turn trace and confirm runtime records:
     - `session_memory decision=reset reason=handover`
     - `re_entry decision=required reason=handover`
     - `pending_guard decision=soft_pass`
     - `llm_policy_plan_delta reason=policy_core_guard_pending_hold`
  5. Compare with live conversation context after the run and confirm booking continuity still exists outside the cleared expected-reply fields:
     - `booking.active=true`
     - `booking.last_question=datetime`
     - `session_memory.active_goal=booking`
     - `session_memory.last_question_type=time`
- Evidence:
  - `/tmp/booking_quality/p1.6o142-l2-dev-20260313-a1-r1/summary.json`
  - `/tmp/booking_quality/p1.6o142-l2-dev-20260313-a1-r1/brief.md`
  - `/tmp/booking_quality/p1.6o142-l2-dev-20260313-a1-r1/responses.jsonl`
  - `/tmp/booking_quality/p1.6o142-l2-dev-20260313-a1-r1/trace_bundle.jsonl`
  - `/tmp/booking_quality/p1.6o142-l2-dev-20260313-a1-r1/manual_audit.json`
- Five Whys:
  1. Why did the turn fail? Because the pending soft-pass branch ended in `policy_core_degraded_hold` instead of a collect follow-up.
  2. Why did timeout degrade choose pending hold? Because the pending branch had no explicit bounded recovery path before the generic `pending_hold` fallback.
  3. Why was the recovery path unavailable? Because runtime had already lost the explicit `expected_reply_type`, even though `booking.last_question` and `session_memory.last_question_type` still proved resumable booking continuity.
  4. Why does that matter architecturally? Because pending/handoff boundary still behaves like a second semantic owner: it can erase or ignore valid runtime contract state before timeout/degrade arbitration runs.
  5. Why is this not just oracle drift? Because runtime trace itself shows the reset + pending-hold sequence before oracle evaluation; stale expectations are downstream.
- Root cause statement:
  - pending soft-pass timeout handling currently depends on live `expected_reply_type` only; when that field is cleared but booking resume-slot evidence still exists in live booking/session state, the boundary resets context and falls into generic `policy_core_degraded_hold` instead of contract-first collect recovery.
- Fix mechanism:
  - teach the pending/handoff boundary to recognize resumable booking continuity from live booking/session state, not only from explicit `expected_reply_type`;
  - allow the timeout/degrade branch under pending soft-pass to resolve through one bounded collect recovery path before generic `pending_hold`;
  - extend contract-first response guard so this bounded pending collect recovery is accepted on state/action/trace/meta evidence.

## Reuse-first decision (mandatory)

- Reuse:
  - existing pending/handoff resume boundary in `truffles-api/app/routers/webhook/decision.py`
  - existing timeout owner boundary contract in `truffles-api/app/services/owner_resolver.py`
  - existing quality guard contract in `ops/diagnose.py`
- Integrate:
  - extend the existing boundary path; do not add a separate router
- Build:
  - none

## Invariant

1. `llm_policy_core` remains the semantic owner; deterministic logic only recovers bounded timeout/degrade boundary state.
2. `P1.6o142` remains the canonical proof artifact for this surfaced family.
3. Real pending/handoff status flows keep their existing guard behavior.
4. No scenario/oracle weakening or phrase-hardcode enters core runtime.

## Scope

- pending soft-pass booking resume-slot continuity under timeout/degrade
- bounded runtime change in `decision.py`
- bounded response guard acceptance in `ops/diagnose.py`
- targeted deterministic tests
- truth sync docs/state/session

## Out of scope

- new proof rerun before deterministic closure
- broad pending-state redesign
- scenario generation/oracle compiler work
- unrelated transport/billing remediation

## Touch-list

- `truffles-api/app/routers/webhook/decision.py`
- `ops/diagnose.py`
- `truffles-api/tests/test_message_endpoint.py`
- `truffles-api/tests/test_booking_quality_response_guard.py`
- `STATE.md`
- `docs/SESSIONS/SESSION-2026-02-19-llm-first-firebreak-a1.md`
- `docs/SESSION_INDEX.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o142-post-p1.6o141-proof-lane-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o143-pending-soft-pass-booking-resume-slot-timeout-boundary-a1.md`

## Plan

1. Freeze `P1.6o142` truth as the canonical surfaced boundary family.
2. Extend pending soft-pass contract detection so live booking/session resume-slot evidence can keep the boundary active even when `expected_reply_type` is absent.
3. Add one bounded timeout-degrade collect recovery path before generic `policy_core_degraded_hold`.
4. Extend response-guard acceptance for this bounded pending collect recovery.
5. Run targeted deterministic checks.
6. Sync truth and publish the next proof-lane continuation.

## DoD

1. The surfaced `P1.6o142` turn no longer collapses to `policy_core_degraded_hold` when pending soft-pass still carries resumable booking resume-slot evidence.
2. Runtime emits bounded collect recovery evidence (`policy_core_timeout_owner_boundary` and matching trace/meta) on the covered contour.
3. Generic pending/handoff status handling remains unchanged outside this bounded contour.
4. Targeted runtime + response-guard tests are green.
5. Canon/state/session reflect `P1.6o142` result and `P1.6o143` closure truthfully.

## Checks

- `python3 -m py_compile truffles-api/app/routers/webhook/decision.py ops/diagnose.py truffles-api/tests/test_message_endpoint.py truffles-api/tests/test_booking_quality_response_guard.py`
- `pytest -q truffles-api/tests/test_message_endpoint.py -k "pending_soft_pass_timeout_booking_resume_boundary or transport_degraded_pending_reentry_restores_booking_resume"`
- `pytest -q truffles-api/tests/test_booking_quality_response_guard.py -k "pending_soft_pass_timeout_booking_resume_boundary or timeout_resume_contract_boundary_collect"`
- `git diff --check`
- `SESSION_AGENT=a1 scripts/session_check.sh`

## Evidence

1. `/tmp/booking_quality/p1.6o142-l2-dev-20260313-a1-r1/summary.json`
2. `/tmp/booking_quality/p1.6o142-l2-dev-20260313-a1-r1/brief.md`
3. `/tmp/booking_quality/p1.6o142-l2-dev-20260313-a1-r1/responses.jsonl`
4. `/tmp/booking_quality/p1.6o142-l2-dev-20260313-a1-r1/trace_bundle.jsonl`
5. `/tmp/booking_quality/p1.6o142-l2-dev-20260313-a1-r1/manual_audit.json`

## Rollback

1. Revert the bounded pending soft-pass timeout recovery and its targeted tests.
2. Keep `P1.6o142` truth frozen as the canonical surfaced family.
3. Do not reinterpret the proof result as scenario/oracle drift.

## No-go

- no new proof rerun before deterministic closure
- no broad pending-state rewrites
- no phrase/regex shortcuts in runtime core
- no weakening of `expected_state_mismatch` or pending-state gates outside the bounded contour

## Risks / blockers

- under-scoping may preserve session memory but still miss timeout collect recovery
- over-scoping may allow booking prompts on unrelated pending turns
- response-guard relaxation may accidentally mask real pending-state regressions if not bounded by trace/meta evidence

## Residual architecture debt (mandatory)

### Current residuals accepted in this block

- full boundary-statechart extraction is still incomplete
- pending/handoff boundary logic still lives inside the giant router
- expensive proof closure remains blocked on the next fresh rerun

### Why not in this block

- this block is limited to the surfaced pending soft-pass timeout family from `P1.6o142`

### Risk if deferred

- proof reruns will keep reopening transport-degraded pending timeout turns under new surfaces

### Linked follow-up Task Package(s)

- next proof-lane TP after deterministic closure of `P1.6o143`

### Expiry/trigger to stop deferral

- before any new guarded rerun after `P1.6o142`

## Next-block contract (mandatory)

### Next block objective

- run one fresh guarded proof rerun after deterministic closure of `P1.6o143`, then truthfully classify the next surfaced family.

### First deterministic check command

- `python3 -m py_compile truffles-api/app/routers/webhook/decision.py ops/diagnose.py truffles-api/tests/test_message_endpoint.py truffles-api/tests/test_booking_quality_response_guard.py && git diff --check`

### Blocked-by conditions

- `P1.6o143` deterministic checks are not green
- `P1.6o142` truth is not synced in active docs/state/session

### Owner role for closure

- `Top Architect + Brain`

## Execution status

- `done`


## Execution result

- Closed the surfaced pending soft-pass timeout boundary on deterministic contour:
  - `truffles-api/app/routers/webhook/decision.py` now derives pending booking resume continuity from live booking/session contract state even when explicit `expected_reply_type` is absent.
  - the pending/handoff boundary can restore the surfaced contour from `resume_contract` and route one bounded `timeout_owner_boundary_collect` recovery before generic `policy_core_degraded_hold`.
  - `ops/diagnose.py` now accepts this bounded pending collect recovery only when `action/state/trace/meta` evidence proves the timeout owner boundary path.
- Targeted deterministic checks are green:
  - `python3 -m py_compile truffles-api/app/routers/webhook/decision.py ops/diagnose.py truffles-api/tests/test_message_endpoint.py truffles-api/tests/test_booking_quality_response_guard.py`
  - `pytest -q truffles-api/tests/test_message_endpoint.py -k "pending_soft_pass_timeout_booking_resume_boundary or transport_degraded_pending_reentry_restores_booking_resume"`
  - `pytest -q truffles-api/tests/test_booking_quality_response_guard.py -k "pending_soft_pass_timeout_booking_resume_boundary or timeout_resume_contract_boundary_collect"`
  - `git diff --check`
  - `SESSION_AGENT=a1 scripts/session_check.sh`
- Published `P1.6o144` as the next admissible proof-only continuation after deterministic closure of this slice.

# TP-2026-03-09-p1.6o61-grounded-time-specialist-availability-owner-a1

- Название/цель: закрыть surfaced `r77` blocker, где после active `time` collect и уже grounded day/time follow-up (`Есть ли свободные слоты на завтра?` -> `А какие мастера доступны?`) runtime отвечает через generic `booking_info_contract/master` / `master_service_not_found` вместо explicit specialist-availability owner.
- Parent TP: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Canon refs: `AGENTS.md`, `STATE.md`, `STRUCTURE.md`, `STRATEGY/REQUIREMENTS.md`, `docs/runbooks/BOOKING_CONFIRM_VERIFY.md`, `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o60b-remaining-closure-owner-matrix-a1.md`, `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o60-master-availability-followup-active-time-owner-a1.md`
- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect remove worktree + branch after merge`

## One web search (mandatory before implementation)

- Query: `site:rasa.com/docs/rasa/forms active loop side question choose option during slot filling`
- Time (UTC): `2026-03-09T14:20:00Z`
- Sources:
  - `https://rasa.com/docs/rasa/forms/`
- Ready solutions found:
  - side questions during active slot collection still need an explicit owner; they should not fall through generic fallback just because the user asks about another axis;
  - the system must explicitly decide whether the side question only answers and resumes the current slot or also transitions to the next slot.
- Decision (`reuse/integrate/build`): `reuse + integrate`
  - reuse the active pending-question contract, booking interrupt owner, and specialist-availability response builder;
  - integrate one new matrix row for grounded-time specialist availability instead of stretching `M7` with phrase fitting;
  - build no runtime text router.
- Rejected options:
  - declaring `M7` “close enough” and absorbing this family without a new row;
  - accepting `master_service_not_found` / admin-check text as success for live specialist availability;
  - post-hoc semantic rewrite after runtime already chose generic `booking_info_contract/master`.

## Root cause (mandatory)

- Symptom:
  - fresh guarded `dev L2` run `p1.6o6-l2-dev-20260309-a1-r77` stops at dialog `1`, turn `3`, user text `А какие мастера доступны?`

- Minimal reproduction:
  1. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r77/scenarios.json` dialog `1`, turns `1-3`
  2. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r77/responses.jsonl` turn `3` and confirm outbox text `Po usluge "Маникюр" utochnu dostupnyh masterov u administratora.`
  3. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r77/trace_bundle.jsonl` turn `3` and confirm `llm_policy_core` keeps `subject_kind=specialist`, `capability=live_availability`, `temporal_scope=specific_time`, `next_question=name`, but leaves `pending_question_target` and `active_question_relation` empty
  4. confirm runtime then falls through `booking_interrupt -> info_reply` with `source=booking_info_contract`, `abstain_reason=master_service_not_found`, and no `pending_question_interaction`
  5. confirm judge fails with `missed_question`

- Evidence:
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r77/summary.json`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r77/brief.md`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r77/scenarios.json`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r77/responses.jsonl`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r77/trace_bundle.jsonl`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r77/manual_audit.md`
  - `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o60-master-availability-followup-active-time-owner-a1.md`
  - `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o60b-remaining-closure-owner-matrix-a1.md`

- Five Whys:
  1. Why did `r77` fail: the bot did not answer which masters are available after the user grounded booking time to tomorrow.
  2. Why did runtime not answer: it used generic `booking_info_contract/master` and the pack resolver returned `master_service_not_found`, producing an admin-check fallback.
  3. Why did runtime choose the generic path: there is no explicit owner row for generic specialist availability once day/time is partially grounded under active `time` collect.
  4. Why could the boundary not preserve the owner: `llm_policy_core` kept `subject_kind=specialist` and `capability=live_availability`, but emitted no `pending_question_target` / `active_question_relation`.
  5. Why is this architecture debt rather than a phrase bug: the published matrix covers named specialist (`M4`), generic specialist availability under `name` collect (`M6`), and date-range/day specialist availability (`M7`), but not grounded `specific_time/day` specialist availability after the time axis is already partially resolved.

- Root cause statement:
  - after `M7`, the remaining gap is a new cross-product row: generic specialist live availability under active `time` collect with grounded `specific_time/day` has no published semantic relation, no explicit execution owner, and no frozen resume-transition contract.

- Fix mechanism:
  - publish a new matrix row (`M10`) for grounded-time specialist availability;
  - freeze whether the owner preserves `expected_reply_type=time` or explicitly transitions to `name`;
  - then implement the bounded prompt/runtime/test closure for that row only.

## Reuse-first plan (mandatory)

- **Internal reuse:** `P1.6o37`, `P1.6o43`, `P1.6o51`, `P1.6o60`, `booking_interrupt`, `_build_specialist_availability_followup_response`, active pending-question contract.
- **External reuse:** official Rasa forms guidance for active-loop side questions.
- **Why not reinvent the wheel:** existing specialist follow-up machinery already solves adjacent rows; this block should add one more explicit row, not a new routing paradigm.

## Execution profile (mandatory for non-doc blocks)

- **TP mode:** `implementation`
- **Doc touch budget (files):** `6`
- **Code dominance:** `required`
- **Override token:** `none`
- **Why this profile fits:** the surfaced family is a bounded semantic/runtime/test row once the matrix contract is explicit.

## Release safety

- Strategy: no new expensive rerun until the row is frozen and deterministic checks are green
- Go signals:
  - the surfaced `r77` family no longer falls through `master_service_not_found`
  - decision meta/trace preserve explicit specialist owner and resume contract
- No-go signals:
  - phrase routing in runtime core
  - implicit `time -> name` transition without contract/tests
  - weakening scenario/judge expectations to accept admin-check fallback
- Rollback verification:
  - revert only the bounded `M10` owner delta once implemented

## Invariant

1. No phrase hardcode in runtime core.
2. Generic `booking_info_contract/master` with `master_service_not_found` is forbidden as success path for this row.
3. The runtime must keep active booking context and emit explicit meta/trace evidence for the specialist owner.
4. `P1.6o60/M7` and adjacent rows `M4/M6` must not regress.

## Scope

- publish the surfaced family as a bounded child row from the owner matrix
- then implement the prompt/runtime/test closure for that row only

## Out of scope

- acceptance `lock/replay`
- broader model-routing refactor
- generic pack/catalog redesign

## Touch-list

- `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o61-grounded-time-specialist-availability-owner-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o60b-remaining-closure-owner-matrix-a1.md`
- `prompts/llm_policy_core.md`
- `truffles-api/app/routers/webhook/decision.py`
- `scripts/booking_dialog_scenarios.py`
- `truffles-api/tests/test_booking_dialog_scenarios_script.py`
- `truffles-api/tests/test_message_endpoint.py`
- `truffles-api/tests/test_intent.py`

## Plan (1..N)

1. Freeze `r77` evidence and publish the new matrix row.
2. Decide the explicit resume contract for grounded-time specialist availability.
3. Implement the bounded semantic/runtime owner path.
4. Add deterministic prompt/runtime/scenario tests.
5. Run one fresh guarded `dev L2` only after deterministic closure.

## DoD

1. Policy-core emits explicit `pending_question_target=specialist` and relation for the `r77` family.
2. Runtime no longer falls through `master_service_not_found` / admin-check text for this row.
3. Decision meta/trace capture the specialist-availability owner and explicit resume contract.
4. `P1.6o60/M7` and adjacent specialist rows remain green.
5. Fresh guarded `dev L2` no longer stops on the `r77` family.

## Checks

- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r77 --status done --strict-artifacts`
- `pytest -q truffles-api/tests/test_intent.py::TestPolicyCoreTimeoutRetry::test_policy_core_preserves_specialist_availability_followup_contract truffles-api/tests/test_intent.py::TestPolicyCoreTimeoutRetry::test_policy_core_preserves_grounded_specialist_availability_transition_contract truffles-api/tests/test_intent.py::TestPolicyCoreTimeoutRetry::test_policy_core_prompt_free_slots_question_keeps_pending_time_contract truffles-api/tests/test_message_endpoint.py::test_policy_collect_interrupt_arbitration_preserves_specialist_availability_followup_owner truffles-api/tests/test_message_endpoint.py::test_specialist_availability_followup_owner_allows_grounded_name_transition truffles-api/tests/test_message_endpoint.py::test_llm_policy_core_specialist_availability_followup_keeps_time_collect truffles-api/tests/test_message_endpoint.py::test_llm_policy_core_grounded_specialist_availability_transitions_to_name_collect truffles-api/tests/test_message_endpoint.py::test_llm_policy_core_question_like_specialist_followup_acknowledges_preference_and_keeps_time_collect truffles-api/tests/test_message_endpoint.py::test_llm_policy_core_master_info_interrupt_resumes_name_collect truffles-api/tests/test_message_endpoint.py::test_llm_policy_core_bare_named_specialist_question_rewrites_collect_to_master_info truffles-api/tests/test_booking_dialog_scenarios_script.py::test_sanitize_llm_turns_retags_specialist_availability_followup_under_active_time_collect truffles-api/tests/test_booking_dialog_scenarios_script.py::test_sanitize_llm_turns_retags_grounded_specialist_availability_transition truffles-api/tests/test_booking_dialog_scenarios_script.py::test_sanitize_llm_turns_retags_generic_master_info_interrupt_and_keeps_time_resume_contract_only_when_active`
- `git diff --check`
- future deterministic checks must cover:
  - policy-core semantic output for grounded-time specialist availability
  - runtime owner/arbitration boundary for the same row
  - scenario sanitization for the surfaced `slot_compare -> specialist availability` shape

## Execution status

- `done (bounded closure proven by r81 rollover)`
- `r77` RCA is frozen and audited
- `M10` resume contract is now explicit: grounded specialist-availability follow-up keeps `active_question_relation=specialist_availability_followup` and transitions to `next_question=name` / `expected_reply_type=name`
- bounded prompt/runtime/scenario/test closure is implemented in this worktree and focused deterministic checks are green
- fresh guarded `dev L2` `p1.6o6-l2-dev-20260311-a1-r81` no longer stopped on the `r77/M10` family; the next surfaced blocker moved to `dialog_id=1`, `turn_index=2`, user text `Какой ближайший слот доступен?`, reopened as `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o62-timeout-ask-about-requested-slot-booking-limit-a1.md`

## Evidence

1. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r77/summary.json`
2. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r77/brief.md`
3. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r77/scenarios.json`
4. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r77/responses.jsonl`
5. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r77/trace_bundle.jsonl`
6. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r77/manual_audit.md`
7. `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o60-master-availability-followup-active-time-owner-a1.md`
8. `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o60b-remaining-closure-owner-matrix-a1.md`
9. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r81/summary.json`
10. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r81/brief.md`
11. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r81/scenarios.json`
12. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r81/responses.jsonl`
13. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r81/trace_bundle.jsonl`
14. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r81/manual_audit.md`

## Rollback

1. Keep `P1.6o6` blocked.
2. Revert only the bounded `M10` implementation if it regresses adjacent rows.

## No-go

- no phrase fitting
- no oracle weakening
- no admin-check fallback accepted as semantic success
- no reinterpretation of `r81` as proof that `M10` is still open

## Risks/Blockers

1. `r81` already surfaced a different timeout/degrade family, so proof continuation is blocked on `P1.6o62`, not on `M10`.
2. Pack-level master availability data may still be insufficient even after owner repair, but it is no longer the first failing family on guarded contour.
3. Acceptance tool-evidence infra is still red, so `r81` remains architecture evidence only, not closure evidence.

## Residual architecture debt (mandatory)

- Current residuals accepted in this block:
  - `r81` surfaced a new timeout/degrade family after `M10` closure; proof continuation moved to `P1.6o62`
- Why not in this block:
  - `P1.6o61` only owns grounded specialist-availability semantics and runtime arbitration, not repeated-timeout guard limits for active slot questions
- Risk if deferred:
  - the proof chain may incorrectly blame `M10` for a different runtime latency/degrade defect
- Linked follow-up Task Package(s):
  - `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o62-timeout-ask-about-requested-slot-booking-limit-a1.md`
  - `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`
- Expiry/trigger to stop deferral:
  - before any claim that the remaining closure lane is blocked by specialist-availability semantics

## Next-block contract (mandatory)

- Next block objective:
  - implement bounded timeout/degrade closure for `M11` (`P1.6o62`) so `r81` is no longer the first surfaced family after `M10` closure
- First deterministic check command:
  - `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r81 --status done --strict-artifacts && git diff --check`
- Blocked-by conditions:
  - `M11` row contract is not frozen
  - repeated-timeout regressions are not green
  - acceptance tool-evidence infra remains red
- Owner role for closure:
  - `Hands` for implementation, `Top Architect + Brain` for matrix/program/proof sync

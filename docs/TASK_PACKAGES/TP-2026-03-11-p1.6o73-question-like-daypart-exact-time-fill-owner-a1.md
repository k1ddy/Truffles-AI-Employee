# TP-2026-03-11-p1.6o73-question-like-daypart-exact-time-fill-owner-a1

- Название/цель: закрыть surfaced `r102` blocker family, где после bounded `M21` closure fresh guarded `dev L2` больше не падает на active-name deictic requested-slot follow-up, но under active `time` collect question-like daypart phrasing `А можно на утро, скажем, на 10 утра?` still stays on `ask_about_requested_slot(time)` and generic time guidance instead of grounding the explicit `10 утра` and advancing canonical resume to `name`; contract-first first fail then surfaces downstream on pricing interrupt `Сколько стоит маникюр?`.

## Canon refs

- Parent verdict: `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o60a-remaining-closure-architecture-verdict-a1.md`
- Owner matrix: `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o60b-remaining-closure-owner-matrix-a1.md`
- Program TP: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Proof bundle: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`
- Prior child: `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o72-active-name-deictic-time-availability-owner-a1.md`
- Related bounded closures: `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o45-question-like-explicit-time-fill-normalization-a1.md`, `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o57-question-like-daypart-fill-trace-normalization-a1.md`
- STATE: `STATE.md`
- CA: remaining closure row after `M21`

## One web search (mandatory before implementation)

- Query: `site:rasa.com/docs/rasa/forms explicit slot value follow-up question active form`
- Date/time: `2026-03-11T17:31:53+05:00`
- Sources opened:
  - official Rasa docs: `https://rasa.com/docs/rasa/forms/`
- Source quality:
  - official product documentation
- Found reusable ideas:
  - when a slot value is explicit enough to fill the requested slot, the form should consume that value instead of keeping the slot unresolved behind a follow-up question;
  - interruptions and follow-up questions must preserve the active slot contract explicitly, but they should not block a direct fill when the message already contains the needed concrete value.
- Decision:
  - `reuse/integrate`
  - reuse the existing question-like explicit-time fill path and integrate a bounded owner rule for question-like daypart phrasing that also contains an explicit exact clock time.
- Rejected options:
  - weakening scenario/oracle to accept stale `reply_type=time`;
  - treating generic time guidance as closure when `10 утра` is already present;
  - broad phrase-hardcode branching in runtime outside the active `time` collect boundary.

## Root cause (mandatory)

- Symptom:
  - fresh guarded `dev L2` run `p1.6o6-l2-dev-20260311-a1-r102` surfaces the next blocker in dialog `1`; the contract-first fail is at turn `3`, user text `Сколько стоит маникюр?`, but the true semantic miss starts on turn `2`, user text `А можно на утро, скажем, на 10 утра?`
- Minimal reproduction:
  1. Inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r102/scenarios.json` dialog `1`, turns `1-3`.
  2. Confirm turn `1` opens booking for `Маникюр` on `завтра` with canonical `reply_type=time`.
  3. Confirm generated scenario turn `2` already expects `reply_type=name` because the utterance contains an explicit exact time token (`10 утра`) even though the phrasing is question-like.
  4. Inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r102/trace_bundle.jsonl` for dialog `1`, turn `2` and confirm `llm_policy_core` keeps `resolution_mode=referent_followup`, `pending_question_act=ask_about_requested_slot`, `pending_question_target=time`, `active_question_relation=ask_about_requested_slot`, `next_question=datetime`, and `slots.datetime=""`.
  5. Inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r102/responses.jsonl` for dialog `1`, turn `2` and confirm judge fails the visible answer as `missed_question` because the bot replies with generic time guidance instead of addressing the explicit `10 утра`.
  6. Inspect dialog `1`, turn `3` in both `responses.jsonl` and `trace_bundle.jsonl` and confirm pricing itself is judge-pass, but contract-first arbitration fails because runtime still carries `expected_reply_type=time` rather than the canonical post-grounding `name`, producing `expected_meta_mismatch` and `expected_trace_miss`.
- Evidence:
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r102/summary.json`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r102/brief.md`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r102/scenarios.json`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r102/responses.jsonl`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r102/trace_bundle.jsonl`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r102/manual_audit.md`
- Five Whys:
  1. Why did the chain fail? The booking flow remained on `time` and later pricing interrupt inherited stale `expected_reply_type=time`.
  2. Why did the flow remain on `time`? Turn `2` did not ground the explicit `10 утра` even though the utterance included a concrete exact time.
  3. Why was `10 утра` not grounded? `llm_policy_core` classified the turn as `ask_about_requested_slot(time)` / `referent_followup` and left `slots.datetime` empty.
  4. Why is that classification wrong here? The utterance is question-like, but it still contains a direct exact-time fill that is sufficient to consume the active `time` slot.
  5. Why is this a new bounded family? `P1.6o45` closed question-like explicit-time fill normalization and `P1.6o57` closed daypart-fill trace normalization, but neither closed the cross-product where daypart phrasing plus exact clock token still falls through the requested-slot follow-up path under active `time` collect.
- Root cause statement:
  - the remaining closure lane lacks a bounded owner contract for question-like daypart phrasing that also contains an explicit exact time under active `time` collect: generated scenario already expects canonical grounding to `name`, but semantic owner/runtime still treat the turn as an unresolved requested-slot question and return generic time guidance.
- Fix mechanism:
  - freeze matrix row `M22` for question-like daypart exact-time fill;
  - align prompt/runtime so an explicit exact clock token inside this phrasing grounds `datetime` and advances resume to `name` rather than remaining on `ask_about_requested_slot(time)`;
  - keep scenario/oracle synchronized with the canonical grounded outcome and add targeted deterministic regressions plus one fresh guarded rerun only after green.

## Reuse-first decision (mandatory)

- Reuse:
  - existing question-like explicit-time fill normalization from `P1.6o45`
  - existing daypart trace/governance coverage from `P1.6o57`
  - existing active `time` collect runtime/trace seams in `decision.py`
- Integrate:
  - bounded semantic owner/runtime rule for question-like daypart + exact-time fill
  - bounded scenario contract alignment only if canonical grounded outcome differs from current generated expectation details
- Do not build:
  - new generic booking loop
  - new oracle waiver
  - phrase-hardcode fallback outside the bounded active-`time` slot-fill path

## Execution profile

- TP mode: `implementation`
- Doc touch budget (files): `10`
- Code dominance: `required`
- Override token: `none`
- Why this profile fits: surfaced family is a new bounded row with an explicit semantic miss on turn `2`, a downstream contract failure on turn `3`, and a clear adjacency boundary to previously closed exact-time/daypart rows.

## Invariant

1. `P1.6o72/M21` remains closed.
2. `P1.6o45` question-like explicit-time fill normalization remains behaviorally consistent.
3. `P1.6o57` daypart-fill trace normalization remains behaviorally consistent.
4. Info/pricing interrupts after a correctly grounded exact time must inherit `expected_reply_type=name`.
5. Any bounded fix must keep observability explicit in `decision_meta` / `decision_trace`.

## Scope

- publish surfaced `r102` family as matrix row `M22`
- implement bounded active-`time` question-like daypart exact-time fill owner only

## Out of scope

- acceptance lock/replay
- global model-routing changes
- generic daypart resolver redesign outside this row
- re-opening `M21` after it already rolled to a different family

## Touch-list

- `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o73-question-like-daypart-exact-time-fill-owner-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o60b-remaining-closure-owner-matrix-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o60a-remaining-closure-architecture-verdict-a1.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`
- `prompts/llm_policy_core.md`
- `scripts/booking_dialog_scenarios.py`
- `truffles-api/app/routers/webhook/decision.py`
- `truffles-api/tests/test_booking_dialog_scenarios_script.py`
- `truffles-api/tests/test_message_endpoint.py`
- `truffles-api/tests/test_booking_quality_response_guard.py`

## Plan (1..N)

1. Freeze `r102` as audited non-closure evidence and publish `M22`.
2. Align semantic owner/runtime so explicit `10 утра` inside question-like daypart phrasing grounds `datetime` under active `time` collect.
3. Keep scenario/oracle synchronized with the canonical grounded outcome if any expectation fields still lag the runtime contract.
4. Add targeted deterministic regressions plus one adjacent negative case for daypart-without-exact-time ambiguity.
5. Run focused checks and `git diff --check`.
6. Run exactly one fresh guarded `dev L2` only after deterministic green.

## DoD

1. `r102` is recorded in active docs as audited non-closure evidence.
2. Question-like daypart phrasing with explicit exact time under active `time` collect grounds `datetime` instead of remaining on `ask_about_requested_slot(time)`.
3. Final meta/trace advance canonical resume to `name` and downstream info/pricing interrupts inherit `expected_reply_type=name`.
4. Adjacent deictic requested-slot and prior exact-time/daypart rows remain unchanged.
5. Focused deterministic scenario/runtime/evidence regressions are green.
6. Fresh guarded `dev L2` no longer stops on the `r102` family.

## Checks

- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r102 --status done --strict-artifacts`
- `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "question_like_explicit_time or daypart_fill or exact_time_fill"`
- `pytest -q truffles-api/tests/test_message_endpoint.py -k "question_like_explicit_time or daypart_exact_time or requested_slot"`
- `pytest -q truffles-api/tests/test_booking_quality_response_guard.py -k "question_like_explicit_time or exact_time_fill or pricing_interrupt"`
- `git diff --check`

## Execution status

- `open`
- fresh guarded `dev L2` `p1.6o6-l2-dev-20260311-a1-r102` is audited and frozen as the next blocker
- deterministic implementation has not started yet

## Evidence

1. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r102/summary.json`
2. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r102/brief.md`
3. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r102/scenarios.json`
4. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r102/responses.jsonl`
5. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r102/trace_bundle.jsonl`
6. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r102/manual_audit.md`
7. `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o72-active-name-deictic-time-availability-owner-a1.md`
8. `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o73-question-like-daypart-exact-time-fill-owner-a1.md`

## Rollback

1. Keep `P1.6o6` blocked.
2. Revert only the future bounded `M22` delta if it regresses adjacent exact-time/daypart rows.

## No-go

- no phrase fitting
- no oracle weakening
- no reinterpretation of `r102` as proof that `M21` is still open
- no generic time guidance accepted as closure when explicit `10 утра` is already present
- no stale `expected_reply_type=time` accepted as closure for downstream pricing/info after exact-time grounding

## Risks/Blockers

1. Scenario and runtime may each require a bounded change; fixing only one layer can leave the other as the first blocker.
2. Daypart phrases without an explicit exact time token must not be over-upgraded into grounded exact-time fill.
3. `r102` remains non-canonical proof evidence (`infra_valid=false`, `semantic_valid=false`), so this block can only claim bounded closure plus rerun rollover.

## Residual architecture debt (mandatory)

- Current residuals accepted in this block:
  - `M22` remains open after bounded `M21` closure.
- Why not in this block:
  - `r102` surfaced only after the next fresh guarded `dev L2` beyond `M21`.
- Risk if deferred:
  - question-like exact-time fills can keep the booking flow on stale `time` resume and hide later remaining rows behind downstream expectation drift.
- Linked follow-up Task Package(s):
  - `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`
- Expiry/trigger to stop deferral:
  - before the next guarded rerun beyond `r102`.

## Next-block contract (mandatory)

- Next block objective:
  - implement `M22`, then rerun focused deterministic scenario/runtime checks before any new guarded `dev L2`.
- First deterministic check command:
  - `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r102 --status done --strict-artifacts && git diff --check`
- Blocked-by conditions:
  - `M22` is not published in the owner matrix
  - question-like daypart exact-time-fill regressions are not green
  - acceptance tool-evidence infra remains red
- Owner role for closure:
  - `Hands` for bounded implementation, `Top Architect + Brain` for matrix/program/proof sync

# TP-2026-03-12-p1.6o85-slot-compare-explicit-time-fill-scenario-governance-a1

- Название/цель: закрыть surfaced `M28` scenario-governance/oracle drift, где после bounded `M27` closure fresh guarded `dev L2` больше не падает на pricing interrupt, но question-like explicit exact-time candidate `Можно записаться на 15:00?` под активным `slot_compare/time` всё ещё генерируется как pending-question `slot_compare/time`, хотя runtime канонически consume-ит explicit time и переводит resume в `name`.

## Canon refs

- Parent closure program: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Main remediation: `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- Historical exact-time governance closure: `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o45-question-like-explicit-time-fill-normalization-a1.md`
- Historical slot-compare owner closure: `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o48-slot-compare-time-guidance-owner-a1.md`
- Historical daypart exact-time closure: `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o73-question-like-daypart-exact-time-fill-owner-a1.md`
- Proof lane that surfaced the row: `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o84-m27-proof-lane-and-next-row-admission-a1.md`
- State + matrix baseline: `STATE.md`, `truffles-api/app/knowledge/generic/INTERACTION_OWNER_MATRIX.yaml`

## Git context

- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect` remove branch + worktree after merge

## One web search (mandatory before implementation)

- Query: `site:rasa.com/docs/rasa/forms slot filling user provides value in question`
- Date/time: `2026-03-12T11:04:37+05:00`
- Sources opened:
  - official Rasa docs: `https://rasa.com/docs/rasa/forms/`
- Source quality:
  - official product documentation
- Found reusable ideas:
  - when a requested slot receives a concrete value from the user, the form should treat it as slot filling instead of keeping the dialogue on the previous request prompt;
  - surface phrasing as a question should not outweigh the presence of a concrete slot value.
- Decision:
  - `reuse + integrate`
  - reuse the existing exact-time-fill normalization pattern already used for `ask_about_requested_slot` / `slot_constraint`, and integrate the same canonical rule for `slot_compare` under active `time` collect.
- Rejected options:
  - weakening evaluator expectations for the surfaced turn without changing scenario normalization;
  - runtime patching to emit fake `slot_compare` pending-question evidence after exact-time grounding;
  - broad rewrite of all pending-question scenario ontology in this block.

## Root cause (mandatory)

- Symptom:
  - fresh guarded `dev L2` `p1.6o84-l2-dev-20260312-a1-r2` no longer stops on `M27`, but now fails on dialog `1`, turn `8` with `expected_meta_mismatch` / `expected_trace_miss` because scenario expectations still insist on `slot_compare/time` after the user says `Можно записаться на 15:00?`.
- Minimal reproduction:
  1. Inspect `/tmp/booking_quality/p1.6o84-l2-dev-20260312-a1-r2/scenarios.json` dialog `1`, turn `8`.
  2. Confirm generated turn tags remain `["slot_compare"]` and `turn.expect.meta_any` still requires `pending_question_act=slot_compare`, `pending_question_target=time`, `expected_reply_type=time`.
  3. Inspect `/tmp/booking_quality/p1.6o84-l2-dev-20260312-a1-r2/responses.jsonl` dialog `1`, turn `8`.
  4. Confirm runtime produces `expected_reply_type=name`, no `pending_question_interaction slot_compare/time`, and the bot answers `Отлично, время подходит. Как вас зовут?`.
  5. Inspect `scripts/booking_dialog_scenarios.py` and confirm explicit exact-time normalization exists for `ask_about_requested_slot` / `slot_constraint`, but not for `slot_compare`.
- Evidence:
  - `/tmp/booking_quality/p1.6o84-l2-dev-20260312-a1-r2/scenarios.json`
  - `/tmp/booking_quality/p1.6o84-l2-dev-20260312-a1-r2/responses.jsonl`
  - `/tmp/booking_quality/p1.6o84-l2-dev-20260312-a1-r2/trace_bundle.jsonl`
  - `scripts/booking_dialog_scenarios.py`
  - `truffles-api/tests/test_booking_dialog_scenarios_script.py`
- Five Whys:
  1. Why did the run fail? Because scenario meta/trace expected a still-open `slot_compare/time` pending-question contract.
  2. Why did scenario expect that? Because `slot_compare` turns are still mapped to pending-question expectations by default.
  3. Why wasn’t this normalized away? Because current normalization covers explicit exact-time fills for `booking`, `ask_about_requested_slot`, and `slot_constraint`, but not `slot_compare`.
  4. Why is the runtime already ahead? Because under active `time` collect, explicit exact clock value is sufficient to ground the slot and advance resume to `name`.
  5. Why is this a bounded scenario-governance row? Because runtime behavior is canonical; the remaining defect is stale generated expectation/trace contract for one adjacent pending-question family.
- Root cause statement:
  - the scenario generator still treats explicit exact-time `slot_compare` turns as unresolved pending-question comparisons, because it lacks the same exact-time normalization already applied to other active-`time` surfaces; this leaves stale `slot_compare/time` expectations after runtime has canonically grounded `datetime` and advanced to `name`.
- Fix mechanism:
  - add bounded normalization for `slot_compare` + explicit exact-time under active `time` collect so tags canonicalize to `time`;
  - add deterministic regression coverage for both sanitize-time generation and post-coverage repair safety;
  - sync matrix/docs so `M28` is truthfully closed on deterministic contour and the next expensive lane remains blocked until the next proof block.

## Reuse-first decision (mandatory)

- Reuse:
  - `_looks_like_explicit_time_fill`
  - existing exact-time normalization pattern in `scripts/booking_dialog_scenarios.py`
  - existing deterministic test harness in `truffles-api/tests/test_booking_dialog_scenarios_script.py`
- Integrate:
  - one new bounded normalization path for `slot_compare`
  - one new sanitize regression
  - one repair regression
- Build:
  - no runtime changes
  - no evaluator/judge changes
  - no new scenario ontology layer

## Release safety

- Rollout: deterministic scenario-governance only; no production runtime change
- Go signal:
  - explicit exact-time `slot_compare` turns normalize to canonical `time -> name`
  - generic compare surfaces like `утром или вечером` remain `slot_compare/time`
  - `git diff --check` and `session_check` stay green
- No-go signal:
  - specialist-availability `slot_compare` regression
  - generic compare turns collapsing to `time`
  - post-coverage repair path reintroducing stale pending-question expectations
- Rollback:
  - revert only the new `slot_compare` exact-time normalization and matching tests

## Invariant

1. `M27` stays boundedly closed by `P1.6o84`.
2. Generic `slot_compare(time)` turns without exact concrete time remain pending-question turns.
3. Runtime behavior is untouched in this block.
4. No oracle weakening or expectation waiver.

## Scope

- bounded scenario-governance/oracle closure for explicit exact-time `slot_compare`
- deterministic tests
- matrix/docs/state/session sync

## Out of scope

- new guarded `dev L2`
- acceptance `lock/replay`
- runtime semantic changes
- migration of rows other than `M28`

## Touch-list

- `scripts/booking_dialog_scenarios.py`
- `truffles-api/tests/test_booking_dialog_scenarios_script.py`
- `truffles-api/app/knowledge/generic/INTERACTION_OWNER_MATRIX.yaml`
- `truffles-api/tests/test_interaction_owner_matrix_contract.py`
- `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o85-slot-compare-explicit-time-fill-scenario-governance-a1.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `STATE.md`
- `STRUCTURE.md`
- `docs/SESSIONS/SESSION-2026-02-19-llm-first-firebreak-a1.md`

## Plan (1..N)

1. Publish `P1.6o85` with RCA + one web search + bounded touch-list.
2. Add explicit exact-time normalization for `slot_compare` under active `time` collect.
3. Add deterministic sanitize/repair regressions plus matrix contract update.
4. Run targeted deterministic checks and hygiene gates.
5. Sync active docs/state/session with factual deterministic closure and next admissible proof block.

## DoD

1. `Можно записаться на 15:00?`-style turns canonicalize to `time` tag under active `time` collect.
2. Generated expectations advance to `reply_type=name` and drop stale `slot_compare/time` pending-question evidence for that family.
3. Generic compare turns still preserve `slot_compare/time`.
4. Deterministic tests are green.
5. `M28` is truthfully synced in matrix/docs/state as deterministic closure, with the next proof block identified.

## Checks

- `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "slot_compare and explicit_time"`
- `pytest -q truffles-api/tests/test_interaction_owner_matrix_contract.py`
- `git diff --check`
- `SESSION_AGENT=a1 scripts/session_check.sh`

## Execution status

- `done`

## Evidence

1. `/tmp/booking_quality/p1.6o84-l2-dev-20260312-a1-r2/scenarios.json`
2. `/tmp/booking_quality/p1.6o84-l2-dev-20260312-a1-r2/responses.jsonl`
3. `/tmp/booking_quality/p1.6o84-l2-dev-20260312-a1-r2/trace_bundle.jsonl`
4. `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "slot_compare and explicit_time"` -> `2 passed, 88 deselected`
5. `pytest -q truffles-api/tests/test_interaction_owner_matrix_contract.py` -> `2 passed`
6. `git diff --check`
7. `SESSION_AGENT=a1 scripts/session_check.sh`
8. updated matrix/docs/state/session references

## Rollback

1. Revert only the new scenario normalization and matching deterministic docs/tests.
2. Keep `P1.6o84` proof evidence and `M27` closure intact.
3. Do not reinterpret `M28` as runtime defect if deterministic closure is rolled back.

## No-go

- no runtime edits
- no new expensive run in this block
- no weakening `slot_compare` contract globally
- no tag-specific phrase hardcode for only one exact string

## Risks / blockers

- `slot_compare` shares nearby semantics with specialist-availability and generic compare families, so the bounded rule must stay tied to explicit exact-time fill under active `time`.
- If deterministic closure is too broad, it may erase legitimate `slot_compare/time` rows that still need pending-question evidence.

## Residual architecture debt (mandatory)

### Current residuals accepted in this block

- Scenario ontology still uses localized normalization heuristics for some pending-question cross-products.
- Acceptance/proof lane remains blocked until a fresh rerun confirms `M28` is gone.

### Why not in this block

- This block is a bounded scenario-governance repair, not a broader ontology redesign or fresh expensive proof step.

### Risk if deferred

- New expensive runs will keep stopping on a false-red expectation row and hide later remaining families.

### Linked follow-up Task Package(s)

- `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o86-post-m28-proof-lane-a1.md`

### Expiry/trigger to stop deferral

- before any next guarded `dev L2` / `lock` / `replay`

## Next-block contract (mandatory)

### Next block objective

- run the next fresh proof admission after `M28` deterministic closure: one guarded `dev L2` to confirm the explicit exact-time `slot_compare` row is gone and to truthfully surface the next family.

### First deterministic check command

- `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "slot_compare and explicit_time" && pytest -q truffles-api/tests/test_interaction_owner_matrix_contract.py && git diff --check`

### Blocked-by conditions

- `P1.6o85` deterministic tests are red
- matrix/docs/state are not synced truthfully for `M28`
- `M27` bounded closure evidence from `P1.6o84` is not preserved

### Owner role for closure

- `Top Architect + Brain`

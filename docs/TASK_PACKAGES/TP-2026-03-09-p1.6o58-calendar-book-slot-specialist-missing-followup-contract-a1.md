# TP-2026-03-09-p1.6o58-calendar-book-slot-specialist-missing-followup-contract-a1.md

- Название/цель: закрыть surfaced `r71` blocker family, где `calendar.book_slot -> specialist_missing` не закрепляет explicit follow-up contract. Из-за этого specialist clarification reply остаётся без owner/evidence, а следующий non-booking turn уже встречает stale expected-reply drift.
- Parent TP: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Canon refs: `AGENTS.md`, `STATE.md`, `STRUCTURE.md`, `STRATEGY/REQUIREMENTS.md`, `docs/runbooks/BOOKING_CONFIRM_VERIFY.md`, `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o57-question-like-daypart-fill-trace-normalization-a1.md`
- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect remove worktree + branch after merge`

## One web search (mandatory before implementation)

- Query: `Rasa forms unrelated input preserve requested slot side question official docs`
- Time (UTC): `2026-03-09T11:43:37Z`
- Sources:
  - `https://rasa.com/docs/rasa/forms/`
- Ready solutions found:
  - when the assistant is collecting a slot and the user sends unrelated side input, the form should answer or acknowledge it without silently changing the requested slot;
  - slot-followup contract must stay explicit after side turns, otherwise later turns drift.
- Decision (`reuse/integrate/build`): `reuse + integrate`
  - reuse existing expected-reply contract and `booking_specialist_followup` owner model;
  - integrate the missing `calendar.book_slot -> specialist_missing` follow-up contract instead of inventing a new special runtime path.
- Rejected options:
  - flattening the next turn in scenario/oracle to match stale runtime drift;
  - adding phrase heuristics for `фото/ногти`;
  - weakening expected-reply or trace oracle for specialist clarification families.

## Root cause (mandatory)

- Symptom:
  - guarded `dev L2` run `p1.6o6-l2-dev-20260309-a1-r71` stops on dialog 1 turn 7 (`А какой адрес вашего салона?`) with `expected_meta_mismatch` + `expected_trace_miss`.

- Minimal reproduction:
  1. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r71/responses.jsonl` turn 5 and confirm `calendar.book_slot` returns `tool_decision=specialist_missing` for `Я бы хотел специалиста Асем.`;
  2. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r71/trace_bundle.jsonl` turn 5 and confirm there is no explicit `question_contract:set` or `pending_question_interaction=booking_specialist_followup` after the specialist-missing tool reply;
  3. inspect turn 7 and confirm the next info interrupt inherits drifted expected-reply contract, surfacing as scenario mismatch on `expected_reply_type`;
  4. inspect `truffles-api/app/services/expected_reply_contract.py` and confirm `calendar.book_slot` does not define a `specialist_missing` follow-up contract, unlike `calendar.list_slots`.

- Evidence:
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r71/summary.json`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r71/brief.md`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r71/responses.jsonl`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r71/trace_bundle.jsonl`
  - `truffles-api/app/services/expected_reply_contract.py`
  - `truffles-api/app/routers/webhook/decision.py`

- Five Whys:
  1. Why did turn 7 fail: scenario expected preserved specialist-clarification contract, but runtime exposed a different expected-reply state.
  2. Why did runtime drift: the preceding `specialist_missing` tool outcome did not emit an explicit follow-up contract.
  3. Why was the contract missing: `calendar.book_slot` lacks a `specialist_missing` branch in expected-reply contract resolution.
  4. Why does that matter architecturally: specialist clarification is a booking-state operation and must stay owned as such across subsequent side turns.
  5. Why is this not a scenario-fitting target: `r71` only surfaced the mismatch on turn 7; the actual gap is the missing contract on turn 5.

- Root cause statement:
  - `calendar.book_slot -> specialist_missing` is missing the same explicit follow-up contract that already exists for `calendar.list_slots -> specialist_missing`, so the runtime leaves specialist clarification under-specified and later turns drift.

- Fix mechanism:
  - add an explicit expected-reply contract for `calendar.book_slot -> specialist_missing`;
  - emit `booking_specialist_followup` trace/meta evidence on that tool path;
  - add focused regressions that lock the specialist-missing contract before the next guarded rerun.

## Release safety

- Strategy: bounded runtime contract fix only
- Go signals:
  - specialist-missing tool reply preserves explicit follow-up contract and owner evidence;
  - next non-booking info turn no longer surfaces stale expected-reply mismatch from this family
- No-go signals:
  - generic slot-order logic changes for unrelated calendar outcomes;
  - tool reply text is hardcoded around specific names
- Rollback verification:
  - revert only the specialist-missing contract branch and its tests if unrelated calendar.book_slot outcomes regress

## Invariant

1. Existing `calendar.list_slots -> specialist_missing` contract remains unchanged.
2. `calendar.book_slot -> ok/conflict/missing_slot` behavior stays unchanged.
3. No phrase-hardcode is added in runtime core.

## Scope

- `truffles-api/app/services/expected_reply_contract.py`
- `truffles-api/app/routers/webhook/decision.py`
- focused tests
- tracker docs after rerun handoff

## Out of scope

- acceptance proof closure
- unrelated infra/tool-evidence blockers
- taxonomy weakening for turn 7

## Touch-list

- `truffles-api/app/services/expected_reply_contract.py`
- `truffles-api/app/routers/webhook/decision.py`
- `truffles-api/tests/test_expected_reply_contract.py`
- `truffles-api/tests/test_message_endpoint.py`
- `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o58-calendar-book-slot-specialist-missing-followup-contract-a1.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`

## Plan (1..N)

1. Freeze `r71` as runtime RCA evidence.
2. Add the missing `calendar.book_slot -> specialist_missing` expected-reply contract.
3. Emit explicit `booking_specialist_followup` trace/meta evidence on that tool path.
4. Add focused deterministic regressions.
5. Run targeted checks and one fresh guarded rerun beyond `r71`.

## DoD

1. `calendar.book_slot -> specialist_missing` leaves an explicit specialist follow-up contract in `decision_meta/decision_trace`.
2. Focused regressions pass.
3. One fresh guarded rerun no longer stops on the `r71` family.

## Checks

- `pytest -q truffles-api/tests/test_expected_reply_contract.py -k "specialist_followup or calendar_book_slot"`
- `pytest -q truffles-api/tests/test_message_endpoint.py -k "specialist_missing or specialist_followup"`
- `git diff --check`

## Execution status

- `done`
- Deterministic contract/tests are green:
  - `pytest -q truffles-api/tests/test_expected_reply_contract.py -k "test_tool_contract_book_slot_specialist_missing_requests_name or test_tool_contract_list_slots_specialist_missing_requests_name"`
  - `pytest -q truffles-api/tests/test_message_endpoint.py -k "book_slot_specialist_missing_preserves_specialist_followup_contract or specialist_missing_prefers_tool_info_sections_and_name_followup or booking_specialist_followup_persists_preference_and_resumes_name_collect or timeout_specialist_followup"`
- Fresh guarded rerun `p1.6o6-l2-dev-20260309-a1-r72` no longer stopped on the `r71` family; it surfaced a different semantic blocker (`Когда у вас есть свободные слоты?`), which is now tracked separately in `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o59-live-availability-slot-question-policy-core-contract-a1.md`.

## Evidence

1. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r71/summary.json`
2. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r71/brief.md`
3. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r71/responses.jsonl`
4. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r71/trace_bundle.jsonl`
5. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r71/manual_audit.md`
6. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r72/summary.json`
7. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r72/brief.md`
8. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r72/manual_audit.md`

## Rollback

1. Revert only the new specialist-missing expected-reply contract and trace/meta wiring.
2. Keep `P1.6o6` blocked; do not reinterpret `r71` as closure evidence.

## No-go

- no turn-7 oracle weakening without fixing turn-5 contract
- no phrase regex for `фото`, `салон`, or specialist names in core
- no silent fallback that clears expected-reply state

## Risks/Blockers

1. After this closure, the next surfaced family may still be a downstream scenario-governance drift and should be evaluated separately.
2. `r71` remains non-canonical proof evidence because infra/tool-evidence findings still invalidate acceptance use.

## Residual architecture debt (mandatory)

- Current residuals accepted in this block:
  - booking tool outcomes still mix explicit follow-up contracts and generic slot-order derivation
- Why not in this block:
  - this slice closes only the surfaced `calendar.book_slot -> specialist_missing` gap
- Risk if deferred:
  - later turns keep surfacing stale-state mismatches instead of revealing the next independent blocker
- Linked follow-up Task Package(s):
  - `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`
- Expiry/trigger to stop deferral:
  - next guarded rerun after bounded closure

## Next-block contract (mandatory)

- Next block objective:
  - bounded closure complete; follow-up block is `P1.6o59`
- First deterministic check command:
  - `pytest -q truffles-api/tests/test_intent.py -k "pending_question_contract or free_slots" && git diff --check`
- Blocked-by conditions:
  - `P1.6o59` prompt regression is not green
- Owner role for closure:
  - `Hands` for bounded closure + rerun evidence, `Top Architect + Brain` for proof-bundle promotion

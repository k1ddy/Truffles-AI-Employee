# TP-2026-03-12-p1.6o114-active-time-time-preference-timeout-guidance-owner-a1

- Название/цель: закрыть surfaced `r127` blocker family, где после admissible `P1.6o112` proof lane generic time-preference statement `У меня есть предпочтения по времени.` под активным `time` collect и `policy_core timeout` проваливается в `booking_interrupt info_reply` + `truth_gate service_duration` вместо bounded `booking_slot_guidance` с сохранением `ask_about_requested_slot(time)`.

## Canon refs

- Parent closure program: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Main remediation: `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- Prior proof lane: `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o112-post-m40-proof-lane-a1.md`
- Infra unblock prerequisite: `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o113-openai-preflight-transport-dedupe-timeout-alignment-a1.md`
- Owner matrix baseline: `truffles-api/app/knowledge/generic/INTERACTION_OWNER_MATRIX.yaml`
- State baseline: `STATE.md`

## Git context

- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect` remove branch + worktree after merge

## One web search (mandatory before implementation)

- Query: `site:docs.python.org python re word boundary documentation`
- Date/time: `2026-03-12T22:58:00+05:00`
- Sources opened:
  - official Python `re` docs: `https://docs.python.org/3/library/re.html`
- Source quality:
  - official language/runtime documentation
- Found reusable ideas:
  - bounded text guards should use explicit normalized phrase checks and word-boundary-safe matching instead of broad accidental substring semantics;
  - phrase-level recovery can stay local if it is contract-scoped and does not weaken the hard gate.
- Decision:
  - `reuse + integrate`
  - reuse the existing timeout pending-slot-question recovery path and integrate one bounded normalized signal for generic time-preference statements instead of building another fallback branch.
- Rejected options:
  - broadening `M26` beyond its published scope;
  - disabling info blocking globally for `по времени`;
  - another expensive rerun before deterministic closure.

## Root cause (mandatory)

- Symptom:
  - fresh admissible `P1.6o112` run no longer stops on `M40`, but dialog `1`, turn `3`, text `У меня есть предпочтения по времени.` still ends as `truth_gate service_duration` with `booking_info_interrupt=True` under active `expected_reply_type=time`.
- Minimal reproduction:
  1. Inspect `/tmp/booking_quality/p1.6o112-l2-dev-20260312-a1-r3/responses.jsonl` and `trace_bundle.jsonl` for dialog `1`, turn `3`.
  2. Confirm `llm_policy_core` failed with `timeout`, `expected_reply_type=time` stayed active, and final meta/trace still resolved to `source=truth_gate`, `intent=service_duration`, `info_sections=['duration','service_duration']`.
  3. Confirm `_is_timeout_pending_time_slot_question(...)` in `truffles-api/app/routers/webhook/decision.py` accepts question-like slot questions and daypart cues, but rejects non-question generic time-preference statements.
  4. Confirm the same turn still contains enough bounded evidence to stay in requested-slot guidance: active `time` resume, no explicit specialist interrupt, no explicit booking verification/reschedule, no grounded datetime answer, and no real service-duration question.
- Evidence:
  - `/tmp/booking_quality/p1.6o112-l2-dev-20260312-a1-r3/summary.json`
  - `/tmp/booking_quality/p1.6o112-l2-dev-20260312-a1-r3/brief.md`
  - `/tmp/booking_quality/p1.6o112-l2-dev-20260312-a1-r3/responses.jsonl`
  - `/tmp/booking_quality/p1.6o112-l2-dev-20260312-a1-r3/trace_bundle.jsonl`
  - `truffles-api/app/routers/webhook/decision.py`
- Five Whys:
  1. Why did the run fail after `M40` moved out of first place? Because a new active-time degraded-turn family collapsed into `truth_gate service_duration`.
  2. Why did it collapse into `truth_gate`? Because `expected_reply_blocked_by_info=true` and the timeout pending-slot-question recovery owner did not recognize the turn as a slot-guidance follow-up.
  3. Why was the recovery owner skipped? Because the helper only accepted question-like slot questions or daypart markers, not generic time-preference statements.
  4. Why is that wrong? Because under active `time` collect, `У меня есть предпочтения по времени.` is neither a factual duration question nor a completed slot fill; it is still a bounded requested-slot follow-up needing clarification/guidance.
  5. Why is this a bounded runtime row instead of a new architecture reset? Because the executable interaction core already models `ask_about_requested_slot(time)`; the missing piece is one explicit degraded recovery admission into that existing owner.
- Root cause statement:
  - timeout pending-slot-question recovery is too narrow: it preserves `ask_about_requested_slot(time)` for question-like or daypart turns, but misses generic time-preference statements, so the degraded path falls through to `booking_interrupt info_reply` + `truth_gate service_duration` and compresses the active `time` resume contract.
- Fix mechanism:
  - add one bounded normalized `time_preference_statement` detector;
  - admit that detector into `_is_timeout_pending_time_slot_question(...)` so timeout-degraded generic time-preference turns recover via `booking_slot_guidance`;
  - cover the helper and the full timeout runtime path with deterministic tests.

## Reuse-first decision (mandatory)

- Reuse:
  - existing timeout pending-slot-question recovery path in `truffles-api/app/routers/webhook/decision.py`
  - existing booking signal helpers in `truffles-api/app/services/booking_signal_service.py`
- Integrate:
  - bounded normalized time-preference detector
  - focused timeout guidance tests
- Do not build:
  - new fallback subsystem
  - broad info-signal rewrite

## Release safety

- Strategy: local deterministic runtime fix only
- Go signals:
  - generic time-preference statement under active `time` collect routes to `booking_slot_guidance` on timeout-degraded path
  - existing pending-slot-question timeout regressions stay green
  - no new info/truth-gate regressions in targeted tests
- No-go signals:
  - the fix weakens explicit duration/hours questions
  - the fix broadens into unrelated info interrupts
- Rollback:
  - revert only the bounded time-preference helper + timeout recovery delta if targeted regressions fail or proof resurfaces the same family

## Invariant

1. `M40` stays closed.
2. Existing timeout pending-slot-question rows remain green.
3. Real duration/hours questions must still be info interrupts.
4. No semantic hardcode outside the bounded degraded recovery owner.

## Scope

- bounded runtime owner fix for generic time-preference statements under active `time` collect + `policy_core timeout`
- focused deterministic tests
- matrix/doc/state sync for surfaced `M41`

## Out of scope

- acceptance `lock/replay/full`
- broad info-class lexicon redesign
- non-timeout booking turns

## Touch-list

- `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o114-active-time-time-preference-timeout-guidance-owner-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o112-post-m40-proof-lane-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o113-openai-preflight-transport-dedupe-timeout-alignment-a1.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `STATE.md`
- `STRUCTURE.md`
- `docs/SESSIONS/SESSION-2026-02-19-llm-first-firebreak-a1.md`
- `truffles-api/app/services/booking_signal_service.py`
- `truffles-api/app/routers/webhook/decision.py`
- `truffles-api/app/knowledge/generic/INTERACTION_OWNER_MATRIX.yaml`
- `truffles-api/tests/test_message_endpoint.py`
- `truffles-api/tests/test_interaction_owner_matrix_contract.py`

## Plan (1..N)

1. Freeze admissible `P1.6o112` evidence as surfaced row `M41`.
2. Add one bounded generic time-preference detector in booking signal helpers.
3. Wire the detector into timeout pending-slot-question recovery.
4. Add focused deterministic tests.
5. Run deterministic checks and sync matrix/docs/state truthfully.

## DoD

1. Generic time-preference statements under active `time` collect no longer collapse to `truth_gate service_duration` on timeout-degraded path.
2. Runtime instead emits bounded `booking_slot_guidance` with `ask_about_requested_slot(time)` evidence.
3. Existing timeout pending-slot-question targeted tests remain green.
4. Matrix/docs/state truthfully record `M41` and its deterministic closure.

## Checks

- `pytest -q truffles-api/tests/test_message_endpoint.py -k "timeout_pending_slot_question or time_preference_statement"` -> `8 passed, 377 deselected`
- `pytest -q truffles-api/tests/test_interaction_owner_matrix_contract.py` -> `2 passed`
- `python3 -m py_compile truffles-api/app/services/booking_signal_service.py truffles-api/app/routers/webhook/decision.py truffles-api/tests/test_message_endpoint.py` -> `pass`
- `git diff --check` -> `pass`
- `SESSION_AGENT=a1 scripts/session_check.sh` -> `Session OK`

## Execution status

- `done`
- bounded timeout recovery now recognizes generic time-preference statements under active `time` collect and routes them through the existing `booking_slot_guidance` owner instead of letting degraded fallback collapse into `booking_interrupt info_reply` + `truth_gate service_duration`
- bounded degraded-fallback guard now suppresses the false-positive `duration` info signal for this family before `pending_info_signal` short-circuits the booking owner, with observable `policy_core_guard` trace evidence
- `M41` is closed on deterministic contour and the next admissible block is the fresh proof lane `P1.6o115`

## Evidence

1. `/tmp/booking_quality/p1.6o112-l2-dev-20260312-a1-r3/summary.json`
2. `/tmp/booking_quality/p1.6o112-l2-dev-20260312-a1-r3/brief.md`
3. `/tmp/booking_quality/p1.6o112-l2-dev-20260312-a1-r3/responses.jsonl`
4. `/tmp/booking_quality/p1.6o112-l2-dev-20260312-a1-r3/trace_bundle.jsonl`
5. `truffles-api/app/services/booking_signal_service.py`
6. `truffles-api/app/routers/webhook/decision.py`
7. `truffles-api/tests/test_message_endpoint.py`
8. `truffles-api/app/knowledge/generic/INTERACTION_OWNER_MATRIX.yaml`
9. `truffles-api/tests/test_interaction_owner_matrix_contract.py`
10. `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o115-post-m41-proof-lane-a1.md`

## Rollback

1. Revert only the bounded generic time-preference timeout recovery delta.
2. Keep `P1.6o112` proof evidence intact.
3. Do not rewrite `M41` as solved without deterministic checks.

## No-go

- no broad suppression of `по времени` across all info flows
- no new expensive rerun before deterministic closure is green
- no weakening of hard preflight or semantic validity gates

## Risks / blockers

- phrase detection may overreach into real duration questions if preference markers are too broad
- timeout guidance path must stay bounded to active `time` collect only

## Residual architecture debt (mandatory)

### Current residuals accepted in this block

- the broader info-signal lexicon still overloads `по времени`; this block only closes the bounded timeout recovery gap
- proof lane remains pending after deterministic closure

### Why not in this block

- the surfaced failure is specifically a degraded runtime owner gap; global info-signal cleanup would broaden scope beyond one row

### Risk if deferred

- fresh guarded runs will keep collapsing this family into `truth_gate service_duration`, blocking further row admission

### Linked follow-up Task Package(s)

- `P1.6o115` post-`M41` proof lane

### Expiry/trigger to stop deferral

- before any next guarded `dev L2` after this deterministic closure

## Next-block contract (mandatory)

### Next block objective

- publish and run `P1.6o115`: prove on one fresh guarded `dev L2` that `M41` is gone from the first-fail position and truthfully admit the next family, if any.

### First deterministic check command

- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o112-l2-dev-20260312-a1-r3 --status done --strict-artifacts && pytest -q truffles-api/tests/test_message_endpoint.py -k "timeout_pending_slot_question or time_preference_statement" && pytest -q truffles-api/tests/test_interaction_owner_matrix_contract.py && git diff --check`

### Blocked-by conditions

- targeted timeout guidance tests are not green
- `M41` is not published truthfully in matrix/docs/state
- `P1.6o115` is not published as the next proof block

### Owner role for closure

- `Top Architect + Brain`

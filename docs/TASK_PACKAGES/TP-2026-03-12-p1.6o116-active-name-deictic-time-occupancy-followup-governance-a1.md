# TP-2026-03-12-p1.6o116-active-name-deictic-time-occupancy-followup-governance-a1

- Название/цель: re-close reopened `M21` on deterministic contour by extending active-name deictic time requested-slot scenario-governance to occupancy phrasing like `А если это время занято?`, so the family stays on canonical `booking_time_availability_followup` / `expected_reply_type=name` instead of stale `service_choice` expectations.

## Canon refs

- Parent closure program: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Main remediation: `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- Prior proof lane: `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o115-post-m41-proof-lane-a1.md`
- Prior related bounded row: `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o72-active-name-deictic-time-availability-owner-a1.md`
- Owner matrix baseline: `truffles-api/app/knowledge/generic/INTERACTION_OWNER_MATRIX.yaml`
- State baseline: `STATE.md`

## Git context

- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect` remove branch + worktree after merge

## One web search (mandatory before implementation)

- Query: `site:docs.python.org python re alternation word boundary documentation`
- Date/time: `2026-03-12T23:22:00+05:00`
- Sources opened:
  - official Python `re` docs: `https://docs.python.org/3/library/re.html`
- Source quality:
  - official language/runtime documentation
- Found reusable ideas:
  - bounded phrase families should be modeled via explicit small regex sets rather than broad substring checks;
  - word-boundary-safe alternation is the right tool for adding one adjacent lexical surface without widening unrelated matches.
- Decision:
  - `reuse + integrate`
  - reuse the existing active-name requested-slot scenario-governance path and integrate one bounded occupancy-wording detector for deictic time follow-ups.
- Rejected options:
  - creating a new row family when existing `M21` already predicted the semantic class;
  - broad availability-lexicon expansion without a contract test;
  - another expensive rerun before deterministic closure.

## Root cause (mandatory)

- Symptom:
  - fresh admissible `P1.6o115` no longer stops on `M41`, but dialog `1`, turn `4`, text `А если это время занято?` still keeps stale scenario expectation `reply_type=service_choice` while runtime canonically preserves `booking_time_availability_followup` with `expected_reply_type=name`.
- Minimal reproduction:
  1. Inspect `/tmp/booking_quality/p1.6o115-l2-dev-20260312-a1-r1/responses.jsonl` and `trace_bundle.jsonl` for dialog `1`, turn `4`.
  2. Confirm runtime keeps `pending_question_act=ask_about_requested_slot`, `pending_question_target=time`, `active_question_relation=ask_about_requested_slot`, and `expected_reply_type=name`.
  3. Inspect `/tmp/booking_quality/p1.6o115-l2-dev-20260312-a1-r1/scenarios.json` and confirm the generated turn still expects `reply_type=service_choice`.
  4. Inspect `_apply_active_name_time_availability_followup_expectations(...)` and `_looks_like_grounded_time_availability_probe(...)` in `scripts/booking_dialog_scenarios.py`.
  5. Confirm the existing deictic follow-up normalizer accepts `А есть ли у вас места в это время?` but misses occupancy wording `А если это время занято?` because the probe only matches availability-question lexicon, not bounded occupancy wording.
- Evidence:
  - `/tmp/booking_quality/p1.6o115-l2-dev-20260312-a1-r1/summary.json`
  - `/tmp/booking_quality/p1.6o115-l2-dev-20260312-a1-r1/brief.md`
  - `/tmp/booking_quality/p1.6o115-l2-dev-20260312-a1-r1/scenarios.json`
  - `/tmp/booking_quality/p1.6o115-l2-dev-20260312-a1-r1/responses.jsonl`
  - `/tmp/booking_quality/p1.6o115-l2-dev-20260312-a1-r1/trace_bundle.jsonl`
  - `scripts/booking_dialog_scenarios.py`
- Five Whys:
  1. Why did the proof lane fail after `M41` moved out of first place? Because an already predicted active-name deictic time follow-up family resurfaced on an adjacent occupancy wording.
  2. Why did it resurface as a strict fail? Because scenario-governance still expected stale `service_choice` rather than canonical `name` resume with requested-slot follow-up evidence.
  3. Why did scenario-governance miss the turn? Because the probe only recognized explicit availability-question wording, not deictic occupancy phrasing like `если это время занято`.
  4. Why is this a reopen of `M21`, not a new row? Because the semantic class is the same active-name deictic specific-time requested-slot follow-up already modeled by `M21`; only one adjacent surface variant remained outside the bounded scenario normalizer.
  5. Why is a bounded scenario-governance fix sufficient? Because runtime evidence is already canonical; only sanitize/post-coverage expectations lag behind the existing owner contract.
- Root cause statement:
  - the active-name deictic time requested-slot scenario normalizer is too narrow: it recognizes explicit availability phrasing, but misses occupancy wording like `А если это время занято?`, so reopened `M21` falls back to stale `service_choice` expectations even though runtime already preserves `booking_time_availability_followup` with `expected_reply_type=name`.
- Fix mechanism:
  - extend the grounded-time follow-up probe with one bounded occupancy-wording detector;
  - reuse the existing active-name requested-slot expectation override so deictic occupancy turns normalize to canonical `name` resume;
  - add focused deterministic sanitize regressions.

## Reuse-first decision (mandatory)

- Reuse:
  - existing `_apply_active_name_time_availability_followup_expectations(...)`
  - existing grounded-time deictic follow-up test coverage in `truffles-api/tests/test_booking_dialog_scenarios_script.py`
- Integrate:
  - bounded occupancy-wording detector into the existing grounded-time follow-up probe
  - focused sanitize regressions
- Do not build:
  - new scenario-governance subsystem
  - new matrix row family for an already predicted semantic class

## Release safety

- Strategy: deterministic scenario-governance fix only
- Go signals:
  - occupancy phrasing like `А если это время занято?` under active `name` resume now normalizes to canonical `booking_time_availability_followup`
  - existing deictic time/day follow-up regressions stay green
  - matrix/docs truthfully record reopened `M21` and its re-closure
- No-go signals:
  - the fix broadens beyond active-name deictic specific-time follow-up
  - unrelated `service_choice` booking turns are rewritten
- Rollback:
  - revert only the bounded occupancy-wording detector + targeted regressions if adjacent requested-slot governance families regress

## Invariant

1. `M41` stays closed.
2. Existing active-name deictic time/day requested-slot follow-up regressions remain green.
3. No runtime contract weakening.
4. No new matrix row is created for a family that existing `M21` already predicted.

## Scope

- bounded scenario-governance fix for reopened `M21` on deictic occupancy wording
- focused deterministic sanitize + matrix regressions
- doc/state/session sync and publish next proof-lane TP ID

## Out of scope

- fresh guarded `dev L2`
- runtime owner changes
- broad availability lexicon redesign

## Touch-list

- `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o115-post-m41-proof-lane-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o116-active-name-deictic-time-occupancy-followup-governance-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o72-active-name-deictic-time-availability-owner-a1.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `STATE.md`
- `STRUCTURE.md`
- `docs/SESSIONS/SESSION-2026-02-19-llm-first-firebreak-a1.md`
- `scripts/booking_dialog_scenarios.py`
- `truffles-api/app/knowledge/generic/INTERACTION_OWNER_MATRIX.yaml`
- `truffles-api/tests/test_booking_dialog_scenarios_script.py`
- `truffles-api/tests/test_interaction_owner_matrix_contract.py`

## Plan (1..N)

1. Freeze admissible `P1.6o115` evidence as reopened `M21` rather than admitting a new row.
2. Add one bounded deictic occupancy-wording detector into the grounded-time follow-up probe.
3. Add focused deterministic sanitize + matrix regressions.
4. Run focused checks and `git diff --check`.
5. Publish the next proof-lane block ID only after deterministic re-closure is green.

## DoD

1. `P1.6o115` is recorded truthfully as reopened existing `M21`, not as a new row family.
2. `А если это время занято?` under active `name` resume no longer keeps stale `service_choice` expectations.
3. Existing deictic time/day follow-up deterministic regressions remain green.
4. Matrix/docs/state truthfully record reopened `M21` and its deterministic re-closure.
5. The next admissible block is the fresh proof lane after reopened `M21` re-closure.

## Checks

- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o115-l2-dev-20260312-a1-r1 --status done --strict-artifacts`
- `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "deictic_time_availability_followup or deictic_day_availability_followup or occupancy_followup"` -> `3 passed, 103 deselected`
- `pytest -q truffles-api/tests/test_interaction_owner_matrix_contract.py` -> `2 passed`
- `python3 -m py_compile scripts/booking_dialog_scenarios.py truffles-api/tests/test_booking_dialog_scenarios_script.py truffles-api/tests/test_interaction_owner_matrix_contract.py` -> `pass`
- `git diff --check` -> `pass`
- `SESSION_AGENT=a1 scripts/session_check.sh` -> `Session OK`

## Execution status

- `done`
- reopened `M21` is now deterministically re-closed: deictic occupancy wording like `А если это время занято?` under active `name` resume now normalizes to canonical `booking_time_availability_followup` with `expected_reply_type=name` instead of stale `service_choice`
- no new row was admitted; this block truthfully treats the `P1.6o115` failure as a reopened existing family that `M21` already predicted
- the next admissible block is the fresh proof lane `P1.6o117`

## Evidence

1. `/tmp/booking_quality/p1.6o115-l2-dev-20260312-a1-r1/summary.json`
2. `/tmp/booking_quality/p1.6o115-l2-dev-20260312-a1-r1/brief.md`
3. `/tmp/booking_quality/p1.6o115-l2-dev-20260312-a1-r1/scenarios.json`
4. `/tmp/booking_quality/p1.6o115-l2-dev-20260312-a1-r1/responses.jsonl`
5. `/tmp/booking_quality/p1.6o115-l2-dev-20260312-a1-r1/trace_bundle.jsonl`
6. `scripts/booking_dialog_scenarios.py`
7. `truffles-api/tests/test_booking_dialog_scenarios_script.py`
8. `truffles-api/app/knowledge/generic/INTERACTION_OWNER_MATRIX.yaml`
9. `truffles-api/tests/test_interaction_owner_matrix_contract.py`
10. `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o117-post-m21-reclosure-proof-lane-a1.md`

## Rollback

1. Keep `P1.6o115` proof truth intact.
2. Revert only the bounded reopened-`M21` scenario-governance delta and targeted regressions if adjacent requested-slot governance families regress.
3. Do not relabel the surfaced family as new if existing `M21` already predicted it.

## No-go

- no new matrix row for the reopened `M21` family
- no runtime owner patch when runtime evidence is already canonical
- no second expensive run before deterministic re-closure is green

## Risks / blockers

- occupancy wording detection can overreach into unrelated booking conflict phrasings if not kept deictic and specific-time bounded
- sanitize/post-coverage repair can silently rewrite unrelated `booking` turns if the new detector ignores active `name` context

## Residual architecture debt (mandatory)

### Current residuals accepted in this block

- scenario-governance still duplicates part of the runtime requested-slot follow-up truth instead of being generated directly from the executable owner matrix
- closure proof remains on `dev L2`; `lock/replay/full` stay blocked

### Why not in this block

- this block only re-closes reopened `M21` on deterministic contour

### Risk if deferred

- active-name deictic occupancy phrasing keeps reopening stale `service_choice` expectations and masks later remaining families

### Linked follow-up Task Package(s)

- `P1.6o117` post-`M21` reclosure proof lane
- `P1.7` acceptance replay gate after the closure lane is green enough to advance

### Expiry/trigger to stop deferral

- before any next guarded `dev L2` after reopened `M21` deterministic closure

## Next-block contract (mandatory)

### Next block objective

- publish and run `P1.6o117`: prove on one fresh guarded `dev L2` that reopened `M21` is gone from the first-fail position and truthfully admit the next family, if any.

### First deterministic check command

- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o115-l2-dev-20260312-a1-r1 --status done --strict-artifacts && pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "deictic_time_availability_followup or deictic_day_availability_followup or occupancy_followup" && pytest -q truffles-api/tests/test_interaction_owner_matrix_contract.py && git diff --check`

### Blocked-by conditions

- reopened `M21` proof evidence is not synced truthfully into active docs/state
- targeted deictic occupancy deterministic evidence is not green
- `P1.6o117` is not published as the next proof block

### Owner role for closure

- `Top Architect + Brain`

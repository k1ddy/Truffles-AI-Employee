# TP-2026-03-09-p1.6o57-question-like-daypart-fill-trace-normalization-a1.md

- Название/цель: закрыть surfaced `r67` blocker family, где scenario всё ещё требует `slot_constraint` pending-question trace для `У вас есть слоты на утро?` под активным `time` collect, тогда как runtime и judge уже трактуют ход как valid partial datetime fill (`datetime=утром`) с переходом к `name`.
- Parent TP: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Canon refs: `AGENTS.md`, `STATE.md`, `STRUCTURE.md`, `STRATEGY/REQUIREMENTS.md`, `docs/runbooks/BOOKING_CONFIRM_VERIFY.md`, `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o56-standalone-named-specialist-booking-normalization-a1.md`
- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect remove worktree + branch after merge`

## One web search (mandatory before implementation)

- Query: `Rasa forms slot filling partial time preference question official docs`
- Time (UTC): `2026-03-09T11:00:00Z`
- Sources:
  - `https://rasa.com/docs/rasa/forms/`
- Ready solutions found:
  - if a user utterance fills part of the requested slot, the form should advance to the next missing slot rather than forcing a separate question-answer owner;
  - question-shaped surfaces do not automatically imply a side-question contract when the content still fills the requested slot.
- Decision (`reuse/integrate/build`): `reuse + integrate`
  - reuse existing time-fill normalization and slot-order collect contract;
  - integrate a deterministic scenario/oracle normalization for question-like daypart fills;
  - build no runtime workaround unless deterministic evidence disproves this framing.
- Rejected options:
  - forcing runtime to emit fake `slot_constraint` trace when it already advanced collect correctly;
  - weakening trace oracle globally for `slot_constraint`;
  - phrase-hardcoding `утро` in runtime core.

## Root cause (mandatory)

- Symptom:
  - guarded `dev L2` run `p1.6o6-l2-dev-20260309-a1-r67` stops on dialog 1 turn 5 with `expected_trace_miss` for `У вас есть слоты на утро?`.

- Minimal reproduction:
  1. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r67/scenarios.json` dialog 1 turns 4-5 and confirm turn 5 keeps tag `slot_constraint` with `trace_contains` expecting `pending_question_interaction`;
  2. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r67/trace_bundle.jsonl` dialog 1 turn 5 and confirm runtime captures `answer_slot=datetime`, `answer_value=утром`, advances `expected_reply_type=name`, and does not emit `slot_constraint` trace;
  3. inspect `scripts/booking_dialog_scenarios.py` and confirm question-like daypart fills are still overclaimed as `slot_constraint` under this state sequence.

- Evidence:
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r67/summary.json`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r67/brief.md`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r67/scenarios.json`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r67/responses.jsonl`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r67/trace_bundle.jsonl`
  - `scripts/booking_dialog_scenarios.py`

- Five Whys:
  1. Why did the turn fail: scenario required `slot_constraint` trace that runtime did not emit.
  2. Why did runtime not emit it: policy-core treated the utterance as a partial datetime answer and moved collect to the next missing slot.
  3. Why did scenario still require `slot_constraint`: taxonomy still overweights question-shaped daypart surfaces even when they satisfy the active requested slot.
  4. Why is this a contract gap: the product contract is slot progress, but scenario keeps a side-question expectation.
  5. Why is this likely deterministic first: judge and runtime already agree, so scenario-governance is the first suspect until disproven.

- Root cause statement:
  - scenario taxonomy still overclaims question-like daypart fills as `slot_constraint` after an active time collect sequence, even when runtime correctly treats the utterance as partial datetime fill and advances collect.

- Fix mechanism:
  - inspect and normalize the scenario path so question-like daypart fills can collapse to canonical time-fill when they actually satisfy the requested slot;
  - add targeted deterministic regressions;
  - rerun guarded dev beyond `r67`.

## Release safety

- Strategy: deterministic scenario/oracle normalization first
- Go signals:
  - question-like daypart fills no longer require stale `slot_constraint` trace
  - existing true `slot_constraint` families remain intact
- No-go signals:
  - runtime changes are introduced before deterministic proof
  - daypart constraint families lose their bounded coverage entirely
- Rollback verification:
  - revert only the scenario normalization and its tests if real `slot_constraint` turns start getting flattened incorrectly

## Invariant

1. True `slot_constraint(time)` families remain covered.
2. Canonical time-fill progression to `name` stays accepted.
3. No runtime phrase-hardcode is introduced in this block.

## Scope

- `scripts/booking_dialog_scenarios.py`
- `truffles-api/tests/test_booking_dialog_scenarios_script.py`
- tracker docs after rerun handoff

## Out of scope

- runtime changes unless deterministic evidence fails
- full proof closure
- infra/tool-evidence cleanup

## Touch-list

- `scripts/booking_dialog_scenarios.py`
- `truffles-api/tests/test_booking_dialog_scenarios_script.py`
- `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o57-question-like-daypart-fill-trace-normalization-a1.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`

## Plan (1..N)

1. Freeze `r67` as deterministic trace-expectation evidence.
2. Normalize question-like daypart fill surfaces to canonical time-fill when they actually satisfy the active requested slot.
3. Add targeted deterministic regressions.
4. Run focused checks and `git diff --check`.
5. Run one fresh guarded dev rerun beyond `r67`.

## DoD

1. `У вас есть слоты на утро?`-style fills no longer require stale `slot_constraint` trace when runtime advances to `name`.
2. Deterministic regressions pass.
3. One fresh guarded rerun no longer stops on the `r67` family.

## Checks

- `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "slot_constraint or grounded_time or pending_question"`
- `git diff --check`

## Execution status

- `in_progress`

## Evidence

1. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r67/summary.json`
2. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r67/brief.md`
3. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r67/scenarios.json`
4. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r67/responses.jsonl`
5. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r67/trace_bundle.jsonl`
6. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r67/manual_audit.md`

## Rollback

1. Revert only the deterministic daypart-fill normalization and its tests.
2. Keep `P1.6o6` blocked; do not reinterpret `r67` as closure evidence.

## No-go

- no runtime workaround before deterministic proof
- no global weakening of `expected_trace_miss`
- no phrase hardcode in runtime core

## Risks/Blockers

1. `r67` remains non-canonical proof evidence because unrelated infra/tool-evidence issues still make the run invalid for proof.
2. The next surfaced family may still be another deterministic governance drift after this one closes.

## Residual architecture debt (mandatory)

- Current residuals accepted in this block:
  - scenario taxonomy still carries several question-shaped temporal surfaces that may require canonical fill normalization
- Why not in this block:
  - this slice only closes the surfaced daypart trace overclaim
- Risk if deferred:
  - generated runs keep stopping on expectation-side drift instead of revealing the next runtime blocker
- Linked follow-up Task Package(s):
  - `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`
- Expiry/trigger to stop deferral:
  - next guarded rerun after bounded deterministic closure

## Next-block contract (mandatory)

- Next block objective:
  - run one fresh guarded dev rerun beyond `r67`
- First deterministic check command:
  - `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "slot_constraint or grounded_time or pending_question" && git diff --check`
- Blocked-by conditions:
  - deterministic regressions are not green
  - `r67` audit not reflected in tracker docs
  - fresh rerun preflight invalid
- Owner role for closure:
  - `Hands` for deterministic closure + rerun evidence, `Top Architect + Brain` for proof-bundle promotion

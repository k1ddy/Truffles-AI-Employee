# TP-2026-03-14-p1.6o169-active-name-specialist-time-followup-scenario-governance-a1

- Название/цель: закрыть surfaced `P1.6o168` scenario-governance drift, где explicit specialist-referent booking turn (`"Могу я записаться к ней на 14:00?"`) после active-name master-info interrupt деградирует в orphan `service_choice` expectation вместо сохранения runtime-consistent `booking_time_availability_followup -> expected_reply_type=name` контракта.

## Canon refs

- Parent closure program: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Main remediation: `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- Binding roadmap: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o135-beauty-salon-canonical-control-plane-roadmap-a1.md`
- Prior proof lane: `docs/TASK_PACKAGES/TP-2026-03-14-p1.6o168-post-p1.6o167-proof-lane-a1.md`
- Related scenario slices: `docs/TASK_PACKAGES/TP-2026-03-14-p1.6o161-active-name-requested-slot-followup-scenario-governance-a1.md`, `docs/TASK_PACKAGES/TP-2026-03-14-p1.6o165-partial-date-daypart-info-interrupt-anchor-scenario-governance-a1.md`
- Class routing: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o118-class-level-closure-process-reset-a1.md`
- State baseline: `STATE.md`

## Git context

- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `origin/main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect` remove branch + worktree after merge

## One web search (mandatory before implementation)

- Query: `site:stately.ai/docs/context state machine context extended state guarded transitions`
- Date/time: `2026-03-14T10:23:29+05:00`
- Sources opened:
  - Stately docs: `https://stately.ai/docs/context`
  - Stately docs: `https://stately.ai/docs/guards`
- Source quality:
  - official vendor documentation
- Found reusable ideas:
  - extended context should preserve the active state needed for the next guarded decision after an interrupt;
  - a bounded guard should reuse existing context instead of collapsing into a generic fallback state when the relevant continuation is still known.
- Decision:
  - `reuse`
  - reuse the existing active-name requested-slot followup compiler path and widen it narrowly so explicit time-fill specialist-referent turns preserve the known `name` continuation instead of being rewritten as orphan `service_choice`.
- Rejected options:
  - accepting the stale `service_choice` oracle as canonical;
  - broad orphan-pending-question redesign;
  - runtime changes before compiler/oracle closure.

## Root cause (mandatory)

- Symptom:
  - `P1.6o168` strict-audited rerun is `infra_valid=true`, `semantic_valid=false` and surfaces `dialog 1 / turn 10 / "Могу я записаться к ней на 14:00?"`, where scenario still requires `reply_type=service_choice`, but runtime canonically emits `pending_question_act=ask_about_requested_slot`, `pending_question_target=time`, `pending_question_owner=booking_time_availability_followup`, and `expected_reply_type=name`.
- Minimal reproduction:
  1. Inspect `/tmp/booking_quality/p1.6o168-l2-dev-20260314-a1-r1/scenarios.json` dialog `1` turn `10` and confirm `expect.reply_type=service_choice`, `meta_any.expected_reply_type=["service_choice"]`, and `trace_contains.question_contract.expected_reply_type=service_choice`.
  2. Inspect `/tmp/booking_quality/p1.6o168-l2-dev-20260314-a1-r1/responses.jsonl` dialog `1` turn `10` and confirm runtime emits `action=booking_prompt`, `source=llm_policy_core`, `pending_question_act=ask_about_requested_slot`, `pending_question_target=time`, `pending_question_owner=booking_time_availability_followup`, and `expected_reply_type=name`.
  3. Inspect `scripts/booking_dialog_scenarios.py` and confirm `_apply_active_name_time_availability_followup_expectations(...)` only restores the active-name followup for grounded availability probes or requested-slot questions without temporal scope, but not for explicit time fills.
  4. Confirm the same turn therefore passes through orphan pending-question rewrite, which strips the pending tag and locks `service_choice` via `_orphan_pending_question_expect_override()`.
- Evidence:
  - `/tmp/booking_quality/p1.6o168-l2-dev-20260314-a1-r1/summary.json`
  - `/tmp/booking_quality/p1.6o168-l2-dev-20260314-a1-r1/brief.md`
  - `/tmp/booking_quality/p1.6o168-l2-dev-20260314-a1-r1/scenarios.json`
  - `/tmp/booking_quality/p1.6o168-l2-dev-20260314-a1-r1/responses.jsonl`
  - `/tmp/booking_quality/p1.6o168-l2-dev-20260314-a1-r1/trace_bundle.jsonl`
  - `scripts/booking_dialog_scenarios.py`
  - `truffles-api/tests/test_booking_dialog_scenarios_script.py`
- Five Whys:
  1. Why did the turn fail? Because scenario expected `service_choice` while runtime preserved `name` followup.
  2. Why did scenario expect `service_choice`? Because the turn was rewritten as an orphan pending-question followup.
  3. Why was it treated as orphan? Because active context entering the turn is `name`, and compiler did not recognize the explicit-time followup as a valid continuation of the existing requested-slot question.
  4. Why did compiler miss that continuation? Because `_apply_active_name_time_availability_followup_expectations(...)` only covers non-temporal requested-slot questions / availability probes, not explicit time fills after an interrupt.
  5. Why is that architectural debt? Because scenario/oracle loses preserved interaction context after a bounded master-info interrupt and invents a second semantic branch that runtime does not have.
- Root cause statement:
  - active-name requested-slot continuation is incompletely compiled: explicit time-fill specialist-referent turns after master-info interrupt are not recognized as valid followups, so orphan fallback rewrites them to stale `service_choice`.
- Fix mechanism:
  - extend the existing active-name requested-slot followup compiler to cover explicit time fills under preserved pending-question context;
  - add targeted sanitize/repair regressions proving this contour keeps `booking_time_availability_followup -> expected_reply_type=name` and no longer falls into orphan `service_choice`.

## Reuse-first decision (mandatory)

- Reuse:
  - existing `_apply_active_name_time_availability_followup_expectations(...)`
  - existing orphan-repair pass `_repair_post_coverage_orphan_pending_question_turns(...)`
  - existing active-name requested-slot scenario contract from `P1.6o161`
- Integrate:
  - widen one bounded scenario compiler predicate; do not build a new expectation engine
- Build:
  - no new dependency or framework

## Invariant

1. Genuine orphan pending-question fallback remains unchanged outside the surfaced contour.
2. Active-name requested-slot followup behavior from `P1.6o161` remains unchanged.
3. Runtime is not modified in this block.
4. Oracle stays contract-first and is not weakened.

## Scope

- `scripts/booking_dialog_scenarios.py`
- `truffles-api/tests/test_booking_dialog_scenarios_script.py`
- truth sync docs for `P1.6o168` / `P1.6o169`

## Out of scope

- runtime/core changes
- proof-lane rerun itself
- broad orphan-pending-question redesign
- acceptance/lock updates

## Touch-list

- `scripts/booking_dialog_scenarios.py`
- `truffles-api/tests/test_booking_dialog_scenarios_script.py`
- `STATE.md`
- `docs/SESSIONS/SESSION-2026-02-19-llm-first-firebreak-a1.md`
- `docs/SESSION_INDEX.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-14-p1.6o168-post-p1.6o167-proof-lane-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-14-p1.6o169-active-name-specialist-time-followup-scenario-governance-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-14-p1.6o170-post-p1.6o169-proof-lane-a1.md`

## Plan

1. Freeze `P1.6o168` truth and publish `P1.6o169`.
2. Widen the existing active-name requested-slot followup compiler so explicit-time specialist-referent turns preserve `name` continuation.
3. Add sanitize and repair regressions for the surfaced contour.
4. Run targeted deterministic checks.
5. Sync truth and publish the next proof lane.

## DoD

1. The surfaced `dialog 1 / turn 10` contour no longer compiles to orphan `service_choice`.
2. Scenario expectations preserve `pending_question_act=ask_about_requested_slot`, `pending_question_target=time`, `pending_question_owner=booking_time_availability_followup`, and `expected_reply_type=name`.
3. Existing orphan fallback and active-name requested-slot followup regressions remain green.
4. Active docs/state/session reflect that `P1.6o168` surfaced a bounded scenario-governance family and `P1.6o169` is the selected continuation.

## Checks

- `python3 -m py_compile scripts/booking_dialog_scenarios.py truffles-api/tests/test_booking_dialog_scenarios_script.py`
- `cd truffles-api && pytest -q tests/test_booking_dialog_scenarios_script.py -k "active_name_time_availability_followup or active_name_requested_slot_followup_without_temporal_scope or active_name_specialist_time_followup_after_master_interrupt or repair_post_coverage_orphan_pending_question_turns_keeps_active_name_specialist_time_followup"`
- `git diff --check`
- `SESSION_AGENT=a1 scripts/session_check.sh`

## Evidence

1. `/tmp/booking_quality/p1.6o168-l2-dev-20260314-a1-r1/summary.json`
2. `/tmp/booking_quality/p1.6o168-l2-dev-20260314-a1-r1/brief.md`
3. `/tmp/booking_quality/p1.6o168-l2-dev-20260314-a1-r1/scenarios.json`
4. `/tmp/booking_quality/p1.6o168-l2-dev-20260314-a1-r1/responses.jsonl`
5. `/tmp/booking_quality/p1.6o168-l2-dev-20260314-a1-r1/trace_bundle.jsonl`
6. `scripts/booking_dialog_scenarios.py`
7. `truffles-api/tests/test_booking_dialog_scenarios_script.py`

## Rollback

1. Revert the bounded compiler predicate widening and its regressions.
2. Keep `P1.6o168` truth frozen as the surfaced scenario-governance drift.
3. Do not reinterpret the failure as runtime drift without new evidence.

## No-go

- no runtime/core change
- no oracle weakening
- no broad orphan-pending-question redesign
- no proof rerun before deterministic closure

## Risks / blockers

- overbroad predicate widening could steal genuine orphan turns into preserved `name` followup
- under-scoping the fix could still leave explicit specialist-referent time fills on stale `service_choice`
- next proof may surface an adjacent active-name scenario contour once this one moves off first fail

## Residual architecture debt (mandatory)

### Current residuals accepted in this block

- scenario compiler still contains bounded semantic heuristics
- active interaction context is still carried implicitly by compiler passes, not by one machine-readable contract source
- full proof bundle closure remains incomplete

### Why not in this block

- this block is a bounded scenario-governance correction only

### Risk if deferred

- expensive proof remains blocked by stale compiler expectations on a runtime-consistent active-name followup contour

### Linked follow-up Task Package(s)

- `docs/TASK_PACKAGES/TP-2026-03-14-p1.6o170-post-p1.6o169-proof-lane-a1.md`

### Expiry/trigger to stop deferral

- before any new expensive rerun after `P1.6o168`

## Next-block contract (mandatory)

### Next block objective

- publish and run one fresh proof-lane rerun after deterministic closure of `P1.6o169`, then truthfully classify the next surfaced family.

### First deterministic check command

- `python3 -m py_compile scripts/booking_dialog_scenarios.py truffles-api/tests/test_booking_dialog_scenarios_script.py && cd truffles-api && pytest -q tests/test_booking_dialog_scenarios_script.py -k "active_name_time_availability_followup or active_name_requested_slot_followup_without_temporal_scope or active_name_specialist_time_followup_after_master_interrupt or repair_post_coverage_orphan_pending_question_turns_keeps_active_name_specialist_time_followup" && git diff --check`

### Blocked-by conditions

- `P1.6o168` truth is not synced in active docs/state/session
- targeted `P1.6o169` scenario checks are not green
- the team attempts another expensive rerun before deterministic closure of this block

### Owner role for closure

- `Top Architect + Brain`

## Execution status

- `done`

## Execution result

- `scripts/booking_dialog_scenarios.py` now extends the existing active-name requested-slot followup compiler so explicit-time specialist-referent turns after master-info interrupt preserve `booking_time_availability_followup -> expected_reply_type=name` instead of degrading to orphan `service_choice`.
- The same bounded continuity is now mirrored in `_repair_post_coverage_orphan_pending_question_turns(...)`, so sanitize and repair flows converge on the same contract for the surfaced contour.
- `truffles-api/tests/test_booking_dialog_scenarios_script.py` now proves both sanitize and repair keep the active-name specialist/time followup contract after master-info interrupt and explicitly reject stale `question_contract.expected_reply_type=service_choice`.


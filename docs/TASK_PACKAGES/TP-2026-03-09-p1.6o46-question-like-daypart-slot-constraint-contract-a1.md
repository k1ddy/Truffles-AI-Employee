# TP-2026-03-09-p1.6o46-question-like-daypart-slot-constraint-contract-a1.md

- Название/цель: закрыть surfaced `r50` blocker, где question-like daypart constraint (`А можно на утро?`) под active `time` collect остаётся несинхронной между taxonomy и runtime: scenario ждёт `ask_about_requested_slot`, а runtime теряет `slot_constraint` evidence в `LLM policy core` path.
- Parent TP: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Canon refs: `AGENTS.md`, `STATE.md`, `STRUCTURE.md`, `STRATEGY/REQUIREMENTS.md`, `docs/runbooks/BOOKING_CONFIRM_VERIFY.md`, `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o45-question-like-explicit-time-fill-normalization-a1.md`, `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o36-question-like-date-slot-constraint-policy-core-degrade-a1.md`
- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect remove worktree + branch after merge`

## One web search (mandatory before implementation)

- Query: `Rasa forms partial slot value clarification daypart official docs`
- Time (UTC): `2026-03-09T05:50:00Z`
- Sources:
  - `https://rasa.com/docs/rasa/forms/`
- Ready solutions found:
  - partial slot values and constraints inside an active requested slot should preserve the requested-slot contract rather than being downgraded to unrelated slot-question semantics;
  - the same requested-slot contract must stay consistent between dialog generation/oracle and runtime execution.
- Decision (`reuse/integrate/build`): `reuse + integrate`
  - reuse existing `slot_constraint` contract already present in `question_contract` and response guard;
  - integrate question-like daypart/date constraint detection into both runtime helper and scenario normalization;
  - build no phrase-specific runtime hardcode.
- Rejected options:
  - keeping taxonomy on `ask_about_requested_slot` and waiving trace/meta mismatch;
  - degrading runtime to generic `booking_prompt` without `slot_constraint` evidence;
  - special-casing only `утро` or only one phrase shape.

## Root cause (mandatory)

- Symptom:
  - guarded `dev L2` run `p1.6o6-l2-dev-20260309-a1-r50` fails on dialog 1 turn 3 (`А можно на утро?`) with `expected_meta_mismatch` and `expected_trace_miss`.

- Minimal reproduction:
  1. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r50/scenarios.json` dialog 1 turn 3
  2. observe the turn is tagged as `ask_about_requested_slot`
  3. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r50/trace_bundle.jsonl`
  4. confirm `question_contract` matched `datetime=утром`, but `pending_question_interaction=slot_constraint` was not emitted
  5. inspect `decision.py::_is_question_like_time_slot_constraint_candidate` and see daypart candidates are explicitly excluded; compare with scenario normalization and note there is no matching `ask_about_requested_slot -> slot_constraint` rewrite for question-like daypart/date constraints

- Evidence:
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r50/scenarios.json`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r50/responses.jsonl`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r50/trace_bundle.jsonl`
  - `truffles-api/app/routers/webhook/decision.py`
  - `scripts/booking_dialog_scenarios.py`

- Five Whys:
  1. Why did the turn fail: scenario expected pending-question evidence that runtime did not emit.
  2. Why did runtime not emit `slot_constraint`: policy-core path excludes daypart candidates from question-like slot-constraint detection.
  3. Why did scenario not align with runtime: taxonomy left the turn on `ask_about_requested_slot` because there is no bounded normalization for question-like daypart/date constraints.
  4. Why is that incorrect: `А можно на утро?` is a constraint on the requested slot, not a generic question about the slot.
  5. Why is this a contract gap and not one phrase: the missing class is `question-like partial time constraint` under active `time` collect.

- Root cause statement:
  - question-like partial time constraints are not modeled consistently: runtime excludes daypart candidates from `slot_constraint` evidence, while scenario taxonomy also leaves the same family on `ask_about_requested_slot`, so the slot-constraint contract never closes under `LLM policy core`.

- Fix mechanism:
  - remove the daypart exclusion from question-like slot-constraint detection in runtime;
  - normalize question-like daypart/date constraint surfaces from `ask_about_requested_slot` to `slot_constraint` in scenario taxonomy when active slot is `time` and there is no explicit clock-time fill;
  - add deterministic runtime + scenario regressions before the next guarded rerun.

## Release safety

- Strategy: bounded contract sync for `slot_constraint`
- Go signals:
  - question-like daypart/date constraints emit `pending_question_interaction=slot_constraint` and keep `expected_reply_type=time`
  - exact clock-time candidates stay out of `slot_constraint`
- No-go signals:
  - broadening question detection so far that generic slot questions collapse into constraints
  - runtime phrase-hardcode in core
- Rollback verification:
  - revert only the daypart exclusion removal and matching taxonomy/runtime regressions if it over-classifies unrelated questions

## Invariant

1. Exact clock-time slot fills from `P1.6o45` remain `time` fills.
2. Ungrounded slot questions stay `ask_about_requested_slot`.
3. Existing `slot_constraint` guard contract remains the source of truth.
4. No judge/oracle waiver is added.

## Scope

- `truffles-api/app/routers/webhook/decision.py`
- `scripts/booking_dialog_scenarios.py`
- `truffles-api/tests/test_message_endpoint.py`
- `truffles-api/tests/test_booking_dialog_scenarios_script.py`
- `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o46-question-like-daypart-slot-constraint-contract-a1.md`
- tracker docs after deterministic closure and rerun handoff:
  - `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
  - `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
  - `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`

## Out of scope

- broader pending-question ontology redesign
- tool-evidence infra remediation
- lock/replay promotion

## Touch-list

- `truffles-api/app/routers/webhook/decision.py`
- `scripts/booking_dialog_scenarios.py`
- `truffles-api/tests/test_message_endpoint.py`
- `truffles-api/tests/test_booking_dialog_scenarios_script.py`
- `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o46-question-like-daypart-slot-constraint-contract-a1.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`

## Plan (1..N)

1. Freeze `r50` as bounded slot-constraint contract evidence.
2. Sync scenario taxonomy from `ask_about_requested_slot` to `slot_constraint` for question-like daypart/date constraints.
3. Remove the daypart exclusion from runtime slot-constraint detection.
4. Add deterministic scenario/runtime regressions.
5. Run targeted checks and `git diff --check`.
6. Then hand off the next guarded rerun.

## DoD

1. `А можно на утро?`-style turns under active `time` collect emit canonical `slot_constraint` expectations/evidence.
2. Exact clock-time candidates remain `time` fills, not `slot_constraint`.
3. Deterministic runtime/scenario regressions pass.
4. `git diff --check` stays clean.

## Checks

- `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "slot_constraint or pending_question"`
- `pytest -q truffles-api/tests/test_message_endpoint.py -k "slot_constraint_contract or question_like_daypart"`
- `git diff --check`

## Execution status

- `in_progress`

## Evidence

1. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r50/scenarios.json`
2. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r50/responses.jsonl`
3. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r50/trace_bundle.jsonl`
4. `truffles-api/app/routers/webhook/decision.py`
5. `scripts/booking_dialog_scenarios.py`

## Rollback

1. Revert only the bounded slot-constraint contract sync and matching regressions.
2. Keep `P1.6o6` blocked; do not reinterpret `r50` as closure evidence.

## No-go

- no runtime phrase-hardcode in core
- no weakening of `slot_constraint` response guard
- no scenario waiver for missing trace/meta evidence
- no guarded rerun before deterministic checks are green

## Risks/Blockers

1. Another partial-time constraint family may surface after this sync; it should open a new bounded TP if it cannot be expressed as `slot_constraint`.
2. Acceptance remains infra-invalid for unrelated tool-evidence gaps; this block only closes the surfaced slot-constraint contract family from `r50`.

## Residual architecture debt (mandatory)

- Current residuals accepted in this block:
  - runtime and scenario still duplicate some slot-constraint heuristics
- Why not in this block:
  - this slice closes the surfaced family without refactoring shared heuristic ownership
- Risk if deferred:
  - future partial-time constraint surfaces can drift again between taxonomy and runtime
- Linked follow-up Task Package(s):
  - `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`
- Expiry/trigger to stop deferral:
  - next partial-time constraint family that still diverges after this sync

## Next-block contract (mandatory)

- Next block objective:
  - rerun guarded acceptance after `P1.6o46` so the next surfaced blocker is not the missing slot-constraint contract for question-like daypart constraints
- First deterministic check command:
  - `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "slot_constraint or pending_question" && pytest -q truffles-api/tests/test_message_endpoint.py -k "slot_constraint_contract or question_like_daypart" && git diff --check`
- Blocked-by conditions:
  - deterministic scenario/runtime regressions are not green
  - `r50` audited evidence is not recorded in tracker docs
  - guarded rerun preflight is invalid
- Owner role for closure:
  - `Hands` for deterministic/runtime closure + rerun evidence, `Top Architect + Brain` for proof-bundle promotion

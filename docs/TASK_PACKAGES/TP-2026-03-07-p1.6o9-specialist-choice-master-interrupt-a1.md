# TP-2026-03-07-p1.6o9-specialist-choice-master-interrupt-a1

- Название/цель: закрыть новый `demo_salon` blocker из `P1.6o6`, где booking-active вопрос про выбор специалиста (`Могу ли я выбрать специалиста?`) не распознаётся как `master` info interrupt, остаётся в booking collect и даёт `expected_info_section_miss + info_section_miss + judge_fail`.
- Parent TP: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Canon refs: `AGENTS.md`, `STATE.md`, `STRUCTURE.md`, `STRATEGY/REQUIREMENTS.md`, `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`, `docs/TASK_PACKAGES/TP-2026-03-06-p1.6e-booking-master-query-pack-enforcement-a1.md`, `docs/TASK_PACKAGES/TP-2026-03-06-p1.6f-master-availability-query-ownership-a1.md`
- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`

## One web search (mandatory before implementation)

- Query: `site:rasa.com/docs regex features lookup tables intent classification`
- Time (UTC): `2026-03-07T15:35:22Z`
- Sources:
  - `https://rasa.com/docs/reference/primitives/intents-and-entities/`
- Ready solutions found:
  - lexical variants should be represented through data-driven features/lookup signals, not ad-hoc code branching for each phrase
  - the classifier/runtime should reuse one signal family so new variants automatically feed the same interrupt/intent contract
- Decision (`reuse/integrate/build`): `reuse + integrate`
  - reuse the existing lexicon-backed `resolve_master_intent()` and booking collect interrupt owner
  - integrate the missing `choose/select specialist` lexical surface into that existing resolver path
- Rejected options:
  - adding a new phrase-hardcode branch in `decision.py` for `выбрать специалиста`
  - weakening guarded oracle expectations so booking collect remains acceptable for master-info turns

## Root cause (mandatory)

- Symptom:
  - fresh guarded `dev L2` rerun `p1.6o6-l2-dev-20260307-a1-r3` stopped at `dialog_index=1`, `turn_index=3`
  - user turn: `Могу ли я выбрать специалиста?`
  - actual runtime reply stayed `booking_prompt` (`На какую дату и время вам удобно?`) instead of answering `master/specialist` info

- Minimal reproduction:
  1. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260307-a1-r3/brief.md`
  2. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260307-a1-r3/failure_families.json`
  3. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260307-a1-r3/responses.jsonl` for `dialog_index=1`, `turn_index=3`
  4. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260307-a1-r3/trace_bundle.jsonl` for `message_id=LLM-QUAL-p1.6o6-l2-dev-20260307-a1-r3-001-03-f0f97f`
  5. probe `resolve_master_intent(message_text="Могу ли я выбрать специалиста?", client_slug="demo_salon", service_query="Маникюр")`

- Evidence:
  - quality family: `expected_info_section_miss`, `info_section_miss`, `judge_fail`
  - failing turn meta stays `action=booking_prompt`, `intent=booking`, no `info_sections`
  - current runtime resolver logic requires `master_query_direct_terms` or `person_term + action_term`; current action lexicon does not cover `выбрать/подобрать`, so `resolve_master_intent(...).explicit == False`
  - `_resolve_policy_collect_interrupt_arbitration()` depends on `_derive_policy_info_refs() -> _detect_info_class_intents() -> resolve_master_intent()`, so the booking collect interrupt owner never sees `master`

- Five Whys:
  1. Why did guarded quality fail: the system answered a master-info turn with another booking slot prompt.
  2. Why did booking prompt survive: the collect interrupt owner did not rewrite the policy-core `collect` plan into an info interrupt.
  3. Why did the owner miss the interrupt: `_detect_info_class_intents()` did not derive `master` from `выбрать специалиста`.
  4. Why did master detection fail: `resolve_master_intent()` has no lexical action signal for choose/select specialist phrasing.
  5. Why is this architectural and not cosmetic: booking/info arbitration is a contract boundary, and the same lexicon-backed resolver is the intended single owner for these variants.

- Root cause statement:
  - the master-intent lexical surface is incomplete for `choose/select specialist` phrasing, so booking-active collect interrupt arbitration never upgrades that turn into a `master` info interrupt.

- Fix mechanism:
  - extend the existing lexicon-backed master action signal set with `choose/select specialist` variants
  - prove it at resolver level and at booking-active message-endpoint level
  - rerun the mandatory deterministic contour, then return to one fresh guarded `dev L2`

## Invariant

1. Master/specialist questions remain data-driven through the existing resolver, not a new semantic branch in core.
2. Booking-active master interrupts must answer with `master/specialist` info or fail-closed clarify, never with another blind booking prompt.
3. Existing hours/location/pricing/duration interrupt behavior must not regress.

## Scope

- `truffles-api/app/knowledge/generic/SYSTEM_LEXICONS.yaml`
- `truffles-api/tests/test_pack_runtime_service.py`
- `truffles-api/tests/test_message_endpoint.py`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`

## Out of scope

- acceptance `lock/replay`
- generic master-query redesign outside the missing lexical variant
- changing judge/oracle thresholds

## Touch-list

- `truffles-api/app/knowledge/generic/SYSTEM_LEXICONS.yaml`
- `truffles-api/tests/test_pack_runtime_service.py`
- `truffles-api/tests/test_message_endpoint.py`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o9-specialist-choice-master-interrupt-a1.md`

## Plan (1..N)

1. Prove the missing lexical signal at resolver level.
2. Extend the existing master action lexicon with choose/select specialist variants.
3. Add exact tests for resolver and booking-active message-endpoint interrupt arbitration.
4. Run exact tests, then the full mandatory deterministic contour.
5. Return to `P1.6o6` for one fresh guarded `dev L2` only after this block is green.

## DoD

1. `resolve_master_intent()` marks choose/select specialist phrasing as explicit when service context is available.
2. Booking-active `collect` plan on `Могу ли я выбрать специалиста?` is rewritten into `master` info interrupt with `info_sections` evidence.
3. Exact tests and mandatory deterministic contour are green.
4. `P1.6o6` can truthfully resume from one fresh guarded `dev L2`.

## Checks

- `pytest -q truffles-api/tests/test_pack_runtime_service.py -k "choose_specialist"`
- `pytest -q truffles-api/tests/test_message_endpoint.py -k "choose_specialist"`
- `pytest -q truffles-api/tests/test_message_endpoint.py`
- `pytest -q truffles-api/tests/test_booking_quality_response_guard.py`
- `pytest -q truffles-api/tests/test_demo_salon_eval.py`
- `pytest -q truffles-api/tests/test_booking_chaos_dialogs.py`
- `pytest -q truffles-api/tests/test_pack_runtime_service.py`
- `git diff --check`

## Execution status

- `done`
- exact resolver/endpoint checks:
  - `pytest -q truffles-api/tests/test_pack_runtime_service.py -k "choose_specialist"` -> `2 passed, 20 deselected`
  - `pytest -q truffles-api/tests/test_message_endpoint.py -k "choose_specialist"` -> `2 passed, 315 deselected`
- deterministic contour on the fixed runtime:
  - `pytest -q truffles-api/tests/test_message_endpoint.py` -> `317 passed, 2 warnings`
  - `pytest -q truffles-api/tests/test_booking_quality_response_guard.py` -> `65 passed`
  - `pytest -q truffles-api/tests/test_demo_salon_eval.py` -> `20 passed`
  - `pytest -q truffles-api/tests/test_booking_chaos_dialogs.py` -> `1 passed`
  - `pytest -q truffles-api/tests/test_pack_runtime_service.py` -> `22 passed`
  - `ruff check truffles-api/app/routers/webhook/booking.py truffles-api/tests/test_message_endpoint.py truffles-api/tests/test_pack_runtime_service.py` -> `pass`
  - `git diff --check` -> `clean`
- guarded follow-up:
  - fresh `dev L2` `p1.6o6-l2-dev-20260307-a1-r4` passed the previously blocked choose-specialist turn and surfaced a new family outside this TP (`fact_without_evidence`), reopened as `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o10-service-clarify-fact-evidence-oracle-boundary-a1.md`

## Evidence

1. `/tmp/booking_quality/p1.6o6-l2-dev-20260307-a1-r3/summary.json`
2. `/tmp/booking_quality/p1.6o6-l2-dev-20260307-a1-r3/brief.md`
3. `/tmp/booking_quality/p1.6o6-l2-dev-20260307-a1-r3/failure_families.json`
4. `/tmp/booking_quality/p1.6o6-l2-dev-20260307-a1-r3/manual_audit.md`
5. future deterministic green outputs from this TP
6. `/tmp/booking_quality/p1.6o6-l2-dev-20260307-a1-r4/summary.json`
7. `/tmp/booking_quality/p1.6o6-l2-dev-20260307-a1-r4/brief.md`
8. `/tmp/booking_quality/p1.6o6-l2-dev-20260307-a1-r4/manual_audit.md`

## Rollback

1. Revert only the lexical signal expansion if it steals unrelated turns.
2. Keep `P1.6o6` blocked and do not run acceptance `lock/replay` while this family is open.

## No-go

- no phrase-hardcode branch in `decision.py`
- no weakening `info_section_miss` or `judge_fail` oracle
- no broad regex expansion without resolver-level proof and exact tests

## Risks/Blockers

1. A too-broad action stem may over-match unrelated specialist/choice phrases.
2. If the missing surface is broader than lexicon alone, open a follow-up TP instead of turning this block into a generic master-query rewrite.

## Residual architecture debt (mandatory)

- Current residuals accepted in this block:
  - `P1.6o6` proof bundle remains blocked until this family is removed
- Why not in this block:
  - this block closes only one newly surfaced lexical/resolver gap
- Risk if deferred:
  - guarded closure remains invalid and booking-active specialist questions keep leaking into booking prompts
- Linked follow-up Task Package(s):
  - `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`
- Expiry/trigger to stop deferral:
  - before any new acceptance `lock/replay` or baseline update

## Next-block contract (mandatory)

- Next block objective:
  - return to `P1.6o6` and rerun exactly one fresh guarded `dev L2` on the fixed runtime
- First deterministic check command:
  - `pytest -q truffles-api/tests/test_pack_runtime_service.py -k "choose_specialist" && pytest -q truffles-api/tests/test_message_endpoint.py -k "choose_specialist"`
- Blocked-by conditions:
  - choose-specialist resolver/interrupt tests are not green
  - `P1.6o6` audited `r3` blocker is not recorded in docs
- Owner role for closure:
  - `Hands` for code/tests, `Brain + Top Architect` for proof-bundle resume decision

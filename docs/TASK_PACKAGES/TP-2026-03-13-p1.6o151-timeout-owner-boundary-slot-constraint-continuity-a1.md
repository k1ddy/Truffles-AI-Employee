# TP-2026-03-13-p1.6o151-timeout-owner-boundary-slot-constraint-continuity-a1

- Название/цель: закрыть surfaced timeout owner-boundary continuity drift так, чтобы grounded `datetime` fill под активным `time` collect сохранял bounded `slot_constraint(time)` meta/trace evidence даже когда `llm_policy_core` падает в `policy_error:deadline_exceeded` и runtime уходит в `policy_core_timeout_owner_boundary`.

## Canon refs

- Binding roadmap: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o135-beauty-salon-canonical-control-plane-roadmap-a1.md`
- Prior deterministic closure: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o149-partial-date-availability-slot-constraint-governance-a1.md`
- Prior proof lane: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o150-post-p1.6o149-proof-lane-a1.md`
- Historical timeout slot-question boundary closure: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o147-timeout-slot-question-info-lock-degraded-collect-boundary-a1.md`
- Class-level routing: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o118-class-level-closure-process-reset-a1.md`
- Parent closure program: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Main remediation: `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- State baseline: `STATE.md`

## Git context

- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `origin/main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect` remove branch + worktree after merge

## One web search (mandatory before implementation)

- Query: `site:docs.temporal.io retry policies transient failures permanent failures timeout`
- Date/time: `2026-03-13T17:52:00+05:00`
- Sources opened:
  - `https://docs.temporal.io/encyclopedia/retry-policies`
- Source quality:
  - official vendor documentation
- Found reusable ideas:
  - timeout/retry handling should keep transient failure recovery separate from permanent/input failure handling;
  - recovery logic should preserve the workflow state needed to continue the original collect contract instead of collapsing into a generic fallback.
- Decision:
  - `reuse + integrate`
  - reuse the existing `timeout_owner_boundary_collect` recovery and integrate one bounded continuity restamp for grounded `slot_constraint(time)` evidence.
- Rejected options:
  - weakening oracle to stop requiring `slot_constraint` evidence on this contour;
  - moving the whole timeout path into a wider statechart/state refactor in this block;
  - patching scenario generator again even though `P1.6o150` surfaced a runtime evidence drop, not a generator drift.

## Root cause (mandatory)

- Symptom:
  - fresh guarded `dev L2` `p1.6o150-l2-dev-20260313-a1-r1` fails on `dialog 1 / turn 3 / "Мне нужно на следующий вторник."` with `expected_meta_mismatch` and `expected_trace_miss`: scenario expects bounded `slot_constraint(time)` evidence, but final runtime meta/trace under `policy_core_timeout_owner_boundary` only keeps `expected_reply_type=time` and drops `pending_question_act/target/interaction`.
- Minimal reproduction:
  1. inspect `/tmp/booking_quality/p1.6o150-l2-dev-20260313-a1-r1/scenarios.json` dialog `1`, turn `3`;
  2. confirm tags are `slot_constraint` and expectations require `pending_question_act=slot_constraint`, `pending_question_target=time`, and a matching `pending_question_interaction` trace entry;
  3. inspect `/tmp/booking_quality/p1.6o150-l2-dev-20260313-a1-r1/trace_bundle.jsonl` dialog `1`, turn `3`;
  4. confirm runtime records `question_contract decision=matched` with `answer_slot=datetime`, `answer_value=во вторник`, then `question_contract decision=set reason=policy_core_timeout_owner_boundary`, `owner_resolver decision=timeout_owner_boundary_match`, and `policy_core_guard decision=timeout_owner_boundary_collect`, but no `pending_question_interaction slot_constraint` entry;
  5. inspect `/tmp/booking_quality/p1.6o150-l2-dev-20260313-a1-r1/responses.jsonl` dialog `1`, turn `3`;
  6. confirm final meta keeps `expected_reply_type=time`, `expected_reply_reason=policy_core_timeout_owner_boundary`, `booking_slot_fill_applied=["datetime"]`, `owner_resolution_reason_code=timeout_owner_boundary_matched_expected_reply`, but omits `pending_question_act`, `pending_question_target`, `pending_question_interaction`, and `pending_question_owner`;
  7. inspect `truffles-api/app/routers/webhook/decision.py` and confirm the `timeout_owner_boundary_resolution` branch sets expected-reply continuity but does not restamp grounded `slot_constraint` pending-question evidence for matched `datetime` fills that still leave `last_question=datetime`.
- Evidence:
  - `/tmp/booking_quality/p1.6o150-l2-dev-20260313-a1-r1/summary.json`
  - `/tmp/booking_quality/p1.6o150-l2-dev-20260313-a1-r1/brief.md`
  - `/tmp/booking_quality/p1.6o150-l2-dev-20260313-a1-r1/failure_families.json`
  - `/tmp/booking_quality/p1.6o150-l2-dev-20260313-a1-r1/scenarios.json`
  - `/tmp/booking_quality/p1.6o150-l2-dev-20260313-a1-r1/responses.jsonl`
  - `/tmp/booking_quality/p1.6o150-l2-dev-20260313-a1-r1/trace_bundle.jsonl`
  - `/tmp/booking_quality/p1.6o150-l2-dev-20260313-a1-r1/manual_audit.json`
  - `truffles-api/app/routers/webhook/decision.py`
  - `truffles-api/tests/test_message_endpoint.py`
  - `truffles-api/tests/test_booking_quality_response_guard.py`
- Five Whys:
  1. Why did the run fail? Because final meta/trace lost `slot_constraint(time)` evidence on the surfaced timeout owner-boundary turn.
  2. Why was the turn still expected to be `slot_constraint(time)`? Because the user grounded a `datetime` answer while booking still needed a more specific `datetime`, so the collect contract remained on the bounded time-constraint contour.
  3. Why did runtime still look partially correct? Because `question_contract` matched the answer and timeout owner-boundary preserved `expected_reply_type=time` plus booking follow-up prompt.
  4. Why did the contract still fail? Because timeout owner-boundary recovery does not restamp the matched `slot_constraint` pending-question evidence into final meta/trace.
  5. Why is this a runtime/boundary fix and not another generator slice? Because scenario expectations match the canonical runtime contract family already surfaced by trace; the defect is the missing runtime evidence on the recovery path.
- Root cause statement:
  - the bounded `timeout_owner_boundary_collect` recovery path preserves expected-reply continuity after a matched grounded `datetime` fill, but it does not re-emit `slot_constraint(time)` pending-question evidence when the follow-up still expects `time`, so the slot-constraint contract is lost only at the timeout boundary.
- Fix mechanism:
  - add one bounded helper in the timeout owner-boundary recovery path that detects grounded matched `datetime` fills which keep `expected_reply_type=time` and re-emits:
    - `pending_question_act=slot_constraint`
    - `pending_question_target=time`
    - `pending_question_interaction=slot_constraint`
    - `pending_question_owner=question_contract`
    - matching `pending_question_interaction` trace evidence
  - add targeted endpoint and response-guard regressions;
  - keep scenario/oracle unchanged in this block.

## Reuse-first decision (mandatory)

- Reuse:
  - existing `timeout_owner_boundary_collect` recovery path in `truffles-api/app/routers/webhook/decision.py`
  - existing question-contract/slot-constraint contract shape already used by runtime and response guard
  - existing targeted regression harnesses in `truffles-api/tests/test_message_endpoint.py` and `truffles-api/tests/test_booking_quality_response_guard.py`
- Integrate:
  - one bounded continuity helper plus targeted tests
- Build:
  - no new subsystem, no matrix/statechart extraction in this block

## Release safety

- Rollout: bounded runtime continuity fix on the timeout owner-boundary path only
- Go signal:
  - surfaced timeout owner-boundary turn now keeps `slot_constraint(time)` meta/trace evidence together with `expected_reply_type=time`
  - existing non-slot-constraint timeout owner-boundary paths stay green
  - `git diff --check` and `SESSION_AGENT=a1 scripts/session_check.sh` stay green
- No-go signal:
  - fix changes generator/oracle files
  - non-grounded timeout owner-boundary turns start getting forced `slot_constraint`
  - bounded owner resolution reason codes or prompts change outside the surfaced contour
- Rollback:
  - revert only the bounded timeout owner-boundary continuity helper and the matching tests/docs

## Invariant

1. `P1.6o149` remains closed.
2. No scenario/oracle change in this block.
3. Timeout owner-boundary reason codes and prompt selection remain unchanged.
4. No phrase-hardcode for a single utterance.
5. `llm_policy_core` remains the semantic owner; this block only preserves contract evidence on a timeout boundary.

## Scope

- bounded timeout owner-boundary continuity recovery for grounded `datetime` fill -> `slot_constraint(time)` evidence
- targeted runtime and response-guard tests
- truthful docs/state/session sync

## Out of scope

- new proof rerun in this block
- broader boundary-statechart extraction
- generator/oracle compiler changes
- acceptance `lock/replay/full`

## Touch-list

- `truffles-api/app/routers/webhook/decision.py`
- `truffles-api/tests/test_message_endpoint.py`
- `truffles-api/tests/test_booking_quality_response_guard.py`
- `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o149-partial-date-availability-slot-constraint-governance-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o150-post-p1.6o149-proof-lane-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o151-timeout-owner-boundary-slot-constraint-continuity-a1.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `STATE.md`
- `docs/SESSIONS/SESSION-2026-02-19-llm-first-firebreak-a1.md`
- `docs/SESSION_INDEX.md`

## Plan

1. Sync truth for completed `P1.6o149` and proof lane `P1.6o150`.
2. Add a bounded timeout owner-boundary helper that restamps `slot_constraint(time)` continuity only for grounded matched `datetime` fills that still expect `time`.
3. Re-emit matching `pending_question_interaction` trace and final meta in the timeout owner-boundary collect branch.
4. Add targeted endpoint and response-guard regressions for the surfaced contour.
5. Run deterministic checks and hygiene gates.
6. Sync canon truthfully and publish the next proof lane.

## DoD

1. Under the surfaced timeout owner-boundary contour, final meta includes `pending_question_act=slot_constraint`, `pending_question_target=time`, `pending_question_interaction=slot_constraint`, and `pending_question_owner=question_contract`.
2. Final trace includes `pending_question_interaction` for `slot_constraint(time)` together with `question_contract expected_reply_type=time` and `policy_core_guard timeout_owner_boundary_collect`.
3. Existing timeout owner-boundary non-slot-constraint tests remain green.
4. Targeted deterministic tests are green.
5. Canon/docs/session are synced truthfully and the next admissible proof block is identified.

## Checks

- `python3 -m py_compile truffles-api/app/routers/webhook/decision.py truffles-api/tests/test_message_endpoint.py truffles-api/tests/test_booking_quality_response_guard.py`
- `pytest -q truffles-api/tests/test_message_endpoint.py -k "timeout_owner_boundary and slot_constraint"`
- `pytest -q truffles-api/tests/test_booking_quality_response_guard.py -k "timeout_owner_boundary and slot_constraint"`
- `git diff --check`
- `SESSION_AGENT=a1 scripts/session_check.sh`

## Evidence

1. `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o151-timeout-owner-boundary-slot-constraint-continuity-a1.md`
2. `/tmp/booking_quality/p1.6o150-l2-dev-20260313-a1-r1/summary.json`
3. `/tmp/booking_quality/p1.6o150-l2-dev-20260313-a1-r1/brief.md`
4. `/tmp/booking_quality/p1.6o150-l2-dev-20260313-a1-r1/failure_families.json`
5. `/tmp/booking_quality/p1.6o150-l2-dev-20260313-a1-r1/scenarios.json`
6. `/tmp/booking_quality/p1.6o150-l2-dev-20260313-a1-r1/responses.jsonl`
7. `/tmp/booking_quality/p1.6o150-l2-dev-20260313-a1-r1/trace_bundle.jsonl`
8. `/tmp/booking_quality/p1.6o150-l2-dev-20260313-a1-r1/manual_audit.json`

## Rollback

1. Revert only the bounded timeout owner-boundary continuity helper and matching tests/docs.
2. Keep `P1.6o150` proof artifacts unchanged.
3. Do not relabel this family back to generator/oracle drift without new evidence.

## No-go

- no new proof run before deterministic closure is green
- no scenario/oracle patch in this block
- no generic timeout fallback rewrite
- no weakening response-guard/oracle to accept missing `slot_constraint` evidence
- no semantic keyword branching in core to recognize one literal phrase only

## Risks / blockers

- the bounded helper must not stamp `slot_constraint` onto unrelated timeout owner-boundary turns such as name-fill followups.
- timeout owner-boundary path is inside `decision.py`, so localized edits can still affect neighboring branches if not tightly scoped.
- if the surfaced contour depends on deeper canonical state than final meta/trace, one local restamp may be insufficient and would need truthful follow-up proof.

## Residual architecture debt (mandatory)

### Current residuals accepted in this block

- timeout/degrade continuity still lives inside `decision.py`
- full boundary-statechart extraction remains incomplete
- proof bundle closure remains non-canonical beyond the observed prefix

### Why not in this block

- this block is a bounded runtime continuity repair, not the broader statechart/compiler program

### Risk if deferred

- fresh expensive runs will keep stopping on missing timeout owner-boundary contract evidence and hide the next real family

### Linked follow-up Task Package(s)

- next proof lane after deterministic closure of `P1.6o151`

### Expiry/trigger to stop deferral

- before the next guarded `dev L2` continuation after this deterministic block

## Next-block contract (mandatory)

### Next block objective

- publish and run `P1.6o152`: execute one fresh guarded `dev L2` after `P1.6o151` deterministic closure to prove the surfaced timeout owner-boundary slot-constraint family moved off first fail and truthfully surface the next admissible class.

### First deterministic check command

- `pytest -q truffles-api/tests/test_message_endpoint.py -k "timeout_owner_boundary and slot_constraint" && pytest -q truffles-api/tests/test_booking_quality_response_guard.py -k "timeout_owner_boundary and slot_constraint" && git diff --check`

### Blocked-by conditions

- `P1.6o151` deterministic closure is not green
- `P1.6o149` / `P1.6o150` truth is not synced into active docs/state/session
- surfaced contour is not reproduced by targeted endpoint + response-guard tests

### Owner role for closure

- `Top Architect + Brain`

## Execution status

- `done`

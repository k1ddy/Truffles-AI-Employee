# TP-2026-03-13-p1.6o139-active-time-generic-master-info-interrupt-owner-a1

- Название/цель: закрыть surfaced `P1.6o138` family, где turn `Какой у вас мастер?` под активным `expected_reply_type=time` схлопывается в `booking_specialist_followup` / `booking_prompt` вместо bounded `master` info interrupt с сохранённым `time` resume.

## Canon refs

- Binding roadmap: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o135-beauty-salon-canonical-control-plane-roadmap-a1.md`
- Fresh surfaced proof: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o138-post-track-a-contract-compiler-proof-lane-a1.md`
- First compiler slice: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o137-track-a-contract-compiler-active-time-specialist-followup-a1.md`
- Main remediation: `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- Parent closure program: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- State baseline: `STATE.md`
- Canon law: `AGENTS.md`, `STRUCTURE.md`, `STRATEGY/REQUIREMENTS.md`

## Git context

- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `origin/main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect` remove branch + worktree after merge

## One web search (mandatory before implementation)

- Query: `site:rasa.com/docs forms active loop interruptions informational questions preserve requested slot`
- Date/time: `2026-03-13T15:19:15+05:00`
- Sources opened:
  - `https://rasa.com/docs/rasa/forms/`
- Source quality:
  - official product documentation
- Ready solutions found:
  - active slot-filling flows should treat informational side questions as interrupts while preserving the original requested slot / follow-up contract.
- Decision (`reuse/integrate/build`): `reuse + integrate`
  - reuse the existing booking interrupt / `question_contract` resume path;
  - integrate one bounded runtime resolver tweak plus scenario retag for the surfaced generic master-info wording;
  - do not introduce a new owner path or state machine.
- Rejected options:
  - boundary statechart work before the surfaced owner/data family is closed;
  - oracle weakening to ignore `info_section_miss` / judge evidence;
  - phrase-hardcode in runtime core outside the bounded resolver path.

## Root cause (mandatory)

- Symptom:
  - strict-audited rerun `p1.6o138-l2-dev-20260313-a1-r1` stopped on `dialog 1 / turn 8 / "Какой у вас мастер?"` with `info_section_miss` + `judge_fail`.

- Minimal reproduction:
  1. inspect `/tmp/booking_quality/p1.6o138-l2-dev-20260313-a1-r1/scenarios.json` for `dialog_id=1`, `turn_index=8`
  2. inspect `/tmp/booking_quality/p1.6o138-l2-dev-20260313-a1-r1/responses.jsonl` and `/tmp/booking_quality/p1.6o138-l2-dev-20260313-a1-r1/trace_bundle.jsonl` for `message_id=LLM-QUAL-p1.6o138-l2-dev-20260313-a1-r1-001-08-9120cb`
  3. inspect `truffles-api/app/services/pack_runtime_service.py` master resolver logic and `scripts/booking_dialog_scenarios.py` active-time sanitize logic

- Evidence:
  - runtime emitted `action=booking_prompt`, `pending_question_interaction=specialist_followup`, `pending_question_owner=booking_specialist_followup`, `expected_reply_type=time`, `expected_reply_blocked_by_info=true`; judge summary says the bot ignored the master's question and asked for date/time instead.
  - `resolve_master_intent("Какой у вас мастер?")` stays non-explicit because current resolver only matches contiguous direct terms or person+action shapes; the filler span `у вас` falls between `какой` and `мастер`.
  - scenario sanitize keeps this surfaced text under `booking` instead of retagging it to bounded `master` interrupt under active `time`, so generated expectations lag the intended interrupt contract.

- Five Whys:
  1. Why did runtime ask for date/time again? Because the turn stayed on the generic specialist-followup collect path.
  2. Why did it stay there? Because the `master` interrupt signal was not detected for `Какой у вас мастер?`.
  3. Why was it not detected? Because `resolve_master_intent` requires contiguous direct term hits (`какой мастер`) or person+action/person+experience branches; `какой у вас мастер` matches neither.
  4. Why did scenario also drift? Because active-time sanitize only retags `ask_about_requested_slot -> master`, not booking-tag generic master-info wording under the same active `time` resume.
  5. Why is this structural, not phrase-fitting? Because the missing contract is a bounded resolver gap for generic master-info question shape plus the adjacent scenario retag, not a need for more canned phrases in core.

- Root cause statement:
  - generic master-info question shape under active `time` collect is under-detected in the shared master resolver and under-normalized in scenario generation, so runtime and oracle both miss the intended `booking_interrupt/master -> resume time` contract.

- Fix mechanism:
  - extend the bounded master resolver to recognize question-like `какой/какие/... + [filler] + мастер/специалист` surfaces as explicit `master` info intent without weakening specialist-choice or live-availability families;
  - retag booking-tag active-time generic master-info turns to the existing `master` interrupt family and require preserved `time` resume evidence;
  - lock both boundaries with runtime/message-endpoint, scenario, and response-guard tests.

## Reuse-first decision (mandatory)

- Reuse:
  - existing `resolve_master_intent` path in `truffles-api/app/services/pack_runtime_service.py`
  - existing booking interrupt / `question_contract` resume path
  - existing scenario `master` interrupt expectation helpers
- Integrate:
  - bounded resolver question-shape detection
  - bounded active-time booking->master scenario retag
- Build:
  - no new router/statechart layer

## Release safety

- Rollout: bounded local runtime/oracle fix only; no deploy in this block
- Go signals:
  - `Какой у вас мастер?` under active `time` collect answers `master` info first and preserves `expected_reply_type=time`
  - scenario retags the surfaced text to `master` only when active `time` resume is present
  - adjacent generic specialist-choice / specialist-availability families stay green
- No-go signals:
  - `Можно выбрать мастера?` regresses from specialist-followup to generic `master` info
  - `Какой мастер свободен ...` regresses from live-availability / followup into generic `master` truth
  - response-guard requires oracle weakening instead of real info evidence
- Rollback:
  - revert only the bounded resolver + scenario retag delta if adjacent master/specialist rows regress

## Invariant

1. No boundary-statechart work in this block.
2. No oracle weakening for `info_section_miss` or judge failure.
3. Generic specialist-choice followup stays collect-owned.
4. Specialist live-availability turns stay on their existing owner paths.
5. Generic master-info interrupts under active `time` must answer info first and preserve `time` resume.

## Scope

- surfaced `P1.6o138` family only
- runtime master-resolver detection for generic master-info question shape
- active-time scenario retag / expectations for booking-tag generic master-info wording
- focused deterministic regressions
- docs/state/session sync for `P1.6o138` result and `P1.6o139` opening

## Out of scope

- new proof rerun in this block
- statechart extraction
- multilingual / open-world claims
- unrelated `master` / `specialist` wording expansion outside the surfaced family

## Touch-list

- `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o138-post-track-a-contract-compiler-proof-lane-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o139-active-time-generic-master-info-interrupt-owner-a1.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `STATE.md`
- `docs/SESSIONS/SESSION-2026-02-19-llm-first-firebreak-a1.md`
- `docs/SESSION_INDEX.md`
- `truffles-api/app/services/pack_runtime_service.py`
- `scripts/booking_dialog_scenarios.py`
- `truffles-api/tests/test_message_endpoint.py`
- `truffles-api/tests/test_booking_dialog_scenarios_script.py`
- `truffles-api/tests/test_booking_quality_response_guard.py`

## Plan (1..N)

1. Sync `P1.6o138` truth into active docs and publish `P1.6o139` as the next admissible structural block.
2. Add bounded resolver detection for generic master-info question shape under booking.
3. Retag active-time booking-tag generic master-info scenario surfaces to the existing `master` interrupt contract.
4. Add focused runtime/scenario/response-guard regressions plus adjacent negative controls.
5. Re-run deterministic checks, then publish the next proof-lane block only if this bounded slice is green.

## DoD

1. `P1.6o138` is recorded truthfully as surfaced `info_section_miss` / `judge_fail` on `Какой у вас мастер?`.
2. `P1.6o139` lands a bounded runtime fix: the surfaced text routes to `master` info interrupt with preserved `expected_reply_type=time`.
3. Scenario sanitize retags the surfaced booking-tag master question only on active `time` resume and expects booking interrupt evidence.
4. Adjacent specialist-choice and specialist-availability controls stay green.
5. Targeted deterministic tests and hygiene checks pass.
6. The next admissible expensive step is one fresh post-`P1.6o139` proof rerun, not another structural child.

## Checks

- `pytest -q truffles-api/tests/test_message_endpoint.py -k "generic_master_info_interrupt_resumes_time_collect or generic_specialist_choice_followup or master_info_interrupt_resumes_name_collect"`
- `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "generic_master_info_interrupt and active_time"`
- `pytest -q truffles-api/tests/test_booking_quality_response_guard.py -k "master_info_interrupt or generic_specialist_change or active_time"`
- `python3 -m py_compile truffles-api/app/services/pack_runtime_service.py scripts/booking_dialog_scenarios.py`
- `git diff --check`
- `SESSION_AGENT=a1 scripts/session_check.sh`

## Evidence

1. `/tmp/booking_quality/p1.6o138-l2-dev-20260313-a1-r1/summary.json`
2. `/tmp/booking_quality/p1.6o138-l2-dev-20260313-a1-r1/brief.md`
3. `/tmp/booking_quality/p1.6o138-l2-dev-20260313-a1-r1/failure_families.json`
4. `/tmp/booking_quality/p1.6o138-l2-dev-20260313-a1-r1/scenarios.json`
5. `/tmp/booking_quality/p1.6o138-l2-dev-20260313-a1-r1/responses.jsonl`
6. `/tmp/booking_quality/p1.6o138-l2-dev-20260313-a1-r1/trace_bundle.jsonl`
7. `/tmp/booking_quality/p1.6o138-l2-dev-20260313-a1-r1/manual_audit.json`

## Rollback

1. Revert only the bounded master-resolver + scenario-retag delta.
2. Keep `P1.6o138` / `P1.6o139` truth in docs; do not reinterpret surfaced failure as closed.

## No-go

- no statechart/state-machine work
- no acceptance lock/replay/full run here
- no phrase-hardcode in `decision.py` as substitute for resolver contract
- no weakening `ops/diagnose.py` to suppress `info_section_miss`

## Risks / blockers

- adjacent specialist-choice wording can be overly captured if the resolver question-shape rule is too broad
- generic master-info wording without active booking context may need to stay on existing non-booking path
- proof remains non-canonical until the post-fix rerun is executed

## Residual architecture debt (mandatory)

### Current residuals accepted in this block

- this slice does not yet make the wider owner matrix executable for all documentary `master` rows
- fresh proof after the fix remains open

### Why not in this block

- the surfaced family needs one bounded runtime/scenario closure first

### Risk if deferred

- the worktree keeps failing on generic master-info interrupts under active booking continuity and cannot progress to the next truthful family

### Linked follow-up Task Package(s)

- one post-`P1.6o139` proof-lane TP selected only after deterministic closure

### Expiry/trigger to stop deferral

- immediately after `P1.6o139` checks are green

## Next-block contract (mandatory)

### Next block objective

- run one fresh guarded proof rerun after `P1.6o139` and verify the covered generic master-info family moved off first fail.

### First deterministic check command

- `pytest -q truffles-api/tests/test_message_endpoint.py -k "generic_master_info_interrupt_resumes_time_collect" && git diff --check`

### Blocked-by conditions

- `P1.6o139` deterministic checks are not green
- `P1.6o138` truth is not synced in active docs
- recovered proof runtime fingerprint is not verified before the next expensive run

### Owner role for closure

- `Top Architect + Brain`

## Execution status

- `done`

## Execution result

- `truffles-api/app/services/pack_runtime_service.py` now recognizes generic filler-shaped master questions such as `Какой у вас мастер?` as explicit bounded `master` intent when booking service context is already present.
- `truffles-api/app/routers/webhook/decision.py` no longer recovers that surfaced wording into `generic_specialist_choice_followup`: the guard now uses effective merged service context before deciding whether the turn is a real specialist-choice followup.
- `truffles-api/app/routers/webhook/booking.py` now keeps the surfaced turn on `booking_interrupt/master`, preserves the blocked resume contract as `pending_question_target=time`, and appends the booking time follow-up prompt without reopening `specialist_followup`.
- `scripts/booking_dialog_scenarios.py` now retags booking-tag active-time generic master-info surfaces to bounded `master` interrupt expectations and scrubs stale `pending_question_interaction` / `pending_question_owner` carryover for this family.
- Targeted deterministic checks are green:
  - `python3 -m py_compile truffles-api/app/services/pack_runtime_service.py truffles-api/app/routers/webhook/decision.py truffles-api/app/routers/webhook/booking.py scripts/booking_dialog_scenarios.py`
  - `pytest -q truffles-api/tests/test_pack_runtime_service.py -k "resolve_master_intent"` -> `9 passed, 18 deselected`
  - `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "generic_master_info_interrupt and active_time"` -> `1 passed, 112 deselected`
  - `pytest -q truffles-api/tests/test_message_endpoint.py -k "generic_specialist_choice_followup or generic_master_info_interrupt_resumes_time_collect or master_info_interrupt_resumes_name_collect or timeout_master_info_interrupt_keeps_time_collect_for_generic_specialist_change or timeout_master_info_interrupt_resumes_name_collect"` -> `5 passed, 385 deselected`
  - `pytest -q truffles-api/tests/test_booking_quality_response_guard.py -k "active_time_generic_master_info_interrupt or timeout_master_info_interrupt_for_generic_specialist_change"` -> `2 passed, 99 deselected`
  - `git diff --check`
  - `SESSION_AGENT=a1 scripts/session_check.sh` -> `Session OK`

## Evidence

1. `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o139-active-time-generic-master-info-interrupt-owner-a1.md`
2. `truffles-api/app/services/pack_runtime_service.py`
3. `truffles-api/app/routers/webhook/decision.py`
4. `truffles-api/app/routers/webhook/booking.py`
5. `scripts/booking_dialog_scenarios.py`
6. `truffles-api/tests/test_pack_runtime_service.py`
7. `truffles-api/tests/test_booking_dialog_scenarios_script.py`
8. `truffles-api/tests/test_message_endpoint.py`
9. `truffles-api/tests/test_booking_quality_response_guard.py`
10. `/tmp/booking_quality/p1.6o138-l2-dev-20260313-a1-r1/summary.json`
11. `/tmp/booking_quality/p1.6o138-l2-dev-20260313-a1-r1/brief.md`
12. `/tmp/booking_quality/p1.6o138-l2-dev-20260313-a1-r1/responses.jsonl`
13. `/tmp/booking_quality/p1.6o138-l2-dev-20260313-a1-r1/trace_bundle.jsonl`

# TP-2026-03-14-p1.6o197-invalid-schema-booking-request-unique-first-name-catalog-match-a1

- Название/цель: boundedly re-close reopened `M19` after `P1.6o196`, where invalid-schema named-specialist booking request `А можно записаться к Айгерим?` still misses `booking_specialist_followup` because branch-catalog matching only accepts full-name phrase matches while the real branch stores `Айгерим Болатова`-style full names.

## Canon refs

- Binding roadmap: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o135-beauty-salon-canonical-control-plane-roadmap-a1.md`
- Prior proof lane: `docs/TASK_PACKAGES/TP-2026-03-14-p1.6o196-post-p1.6o195-proof-lane-a1.md`
- Prior bounded M19 re-closure: `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o91-invalid-schema-booking-request-specialist-catalog-match-a1.md`
- Parent closure program: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Main remediation: `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- State baseline: `STATE.md`

## One web search (mandatory before implementation)

- Exact query: `site:docs.python.org Python str casefold caseless matching`
- Date/time: `2026-03-14 17:24 +05:00`
- Opened sources:
  - official Python docs `str.casefold`: `https://docs.python.org/3/library/stdtypes.html#str.casefold`
- Found ready solutions:
  - caseless matching should normalize text before bounded token comparison;
  - token-level matching can stay deterministic without another LLM hop when the candidate set is already branch-scoped.
- Decision: `reuse/integrate`
  - reuse the existing normalized Python-space branch-catalog matcher in `_resolve_specialist_name_hint_with_trace(...)` and widen it only to unique first-name token matches before the secondary LLM.
- Rejected variants:
  - adding another LLM retry to recover a specialist already present in branch data;
  - phrase-hardcoding `Айгерим` in degraded runtime logic;
  - weakening the contract to accept generic `policy_core_degraded_collect`.
- Source quality:
  - primary/official source: Python standard library docs.

## Git context

- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `origin/main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect` remove branch + worktree after merge

## Root cause (mandatory)

- Symptom:
  - strict-audited rerun `p1.6o196-l2-dev-20260314-a1-r1` surfaced `dialog 1 / turn 4 / "А можно записаться к Айгерим?"`, where runtime fell back to generic `policy_core_degraded_collect` and missed the already-closed `booking_specialist_followup` contract.
- Minimal reproduction:
  1. keep booking active with grounded service and `expected_reply_type=time`;
  2. trigger `llm_policy_core` degraded fallback with `policy_error:invalid_schema` on `А можно записаться к Айгерим?`;
  3. inspect `/tmp/booking_quality/p1.6o196-l2-dev-20260314-a1-r1/trace_bundle.jsonl` turn `4` and confirm `specialist_hint.source=llm`, `specialist_hint.error=timeout`, and missing `pending_question_interaction=booking_specialist_followup`;
  4. inspect live runtime data and confirm the same conversation has `branch_id=b7f75692-951e-421a-aae6-f5db97394799`, while active specialists include `Айгерим Болатова` rather than the single-token surface `Айгерим`.
- Evidence:
  - `/tmp/booking_quality/p1.6o196-l2-dev-20260314-a1-r1/summary.json`
  - `/tmp/booking_quality/p1.6o196-l2-dev-20260314-a1-r1/brief.md`
  - `/tmp/booking_quality/p1.6o196-l2-dev-20260314-a1-r1/responses.jsonl`
  - `/tmp/booking_quality/p1.6o196-l2-dev-20260314-a1-r1/trace_bundle.jsonl`
  - `/tmp/booking_quality/p1.6o196-l2-dev-20260314-a1-r1/manual_audit.json`
  - `truffles-api/app/routers/webhook/decision.py`
  - `truffles-api/tests/test_message_endpoint.py`
- Five Whys:
  1. Why did `P1.6o196` fail first? Because invalid-schema recovery returned generic `booking_prompt/time` instead of `booking_specialist_followup`.
  2. Why did recovery miss the specialist? Because `_resolve_specialist_name_hint_with_trace(...)` did not match the branch catalog before falling to the secondary LLM.
  3. Why did branch-catalog matching miss? Because it only accepted full normalized specialist-name phrases, while the real branch stores full names such as `Айгерим Болатова` and the user surface mentions only `Айгерим`.
  4. Why did that matter on this turn? Because the secondary LLM hint timed out, so there was no later recovery path.
  5. Why is this a reopened runtime family rather than a new class? Because `M19` already requires invalid-schema named-specialist booking requests under active `time` collect to preserve `booking_specialist_followup`; the remaining gap is only the partial full-name catalog contour.
- Root cause statement:
  - branch-catalog specialist matching is too strict for real salon data: it requires a full-name phrase match and therefore misses unique first-name surfaces like `Айгерим` when the stored specialist is `Айгерим Болатова`, reopening `M19` whenever the secondary hint LLM is unavailable.
- Fix mechanism:
  - widen the existing deterministic branch-catalog matcher so a unique first-name token match can recover the full branch specialist name before the secondary LLM;
  - keep ambiguity fail-closed by falling back to the LLM when more than one active specialist shares the same first-name token;
  - add targeted helper and endpoint regressions for the surfaced contour.

## Reuse-first decision (mandatory)

- Reuse:
  - existing `_resolve_specialist_name_hint_with_trace(...)`
  - existing invalid-schema specialist-followup recovery guard
  - existing message-endpoint tests for `M19` runtime contract
- Integrate:
  - widen only the branch-catalog match predicate; do not add a new resolver or a new fallback path
- Build:
  - no new dependency or framework

## Release safety

- Rollout: local runtime/tests only
- Go signal:
  - unique first-name branch-catalog match recovers the surfaced turn without calling the secondary LLM
- No-go signal:
  - ambiguous first-name surfaces start guessing one specialist deterministically
  - generic degraded collect remains accepted as success for reopened `M19`
- Rollback:
  - revert only the bounded branch-catalog token-match widening and keep `P1.6o196` truth intact

## Invariant

1. `M19` must preserve `booking_specialist_followup` with `expected_reply_type=time`.
2. Ambiguous first-name matches stay fail-closed and fall back to the secondary LLM.
3. No phrase-hardcode in runtime core.
4. No new proof rerun before deterministic closure.

## Scope

- widen deterministic branch-catalog specialist matching for unique first-name surfaces
- add targeted endpoint/helper regressions
- sync canon docs/state/session and publish the next proof lane

## Out of scope

- new proof rerun in this block
- scenario-governance changes
- broad specialist-resolution redesign
- acceptance lock/replay/full

## Touch-list

- `truffles-api/app/routers/webhook/decision.py`
- `truffles-api/tests/test_message_endpoint.py`
- `STATE.md`
- `docs/SESSIONS/SESSION-2026-02-19-llm-first-firebreak-a1.md`
- `docs/SESSION_INDEX.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-14-p1.6o196-post-p1.6o195-proof-lane-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-14-p1.6o197-invalid-schema-booking-request-unique-first-name-catalog-match-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-14-p1.6o198-post-p1.6o197-proof-lane-a1.md`

## Plan

1. Freeze `P1.6o196` truth as reopened `M19` on real full-name branch data.
2. Widen branch-catalog specialist matching to unique first-name token recovery.
3. Add helper + endpoint regressions for unique and ambiguous first-name contours.
4. Run targeted deterministic checks.
5. Sync truth and publish the next proof lane.

## DoD

1. Reopened `M19` is deterministically re-closed for the surfaced full-name branch-catalog contour.
2. Unique first-name surfaces recover the full branch specialist name without a secondary LLM hop.
3. Ambiguous first-name surfaces still fall back to the LLM.
4. `P1.6o198` is published as the next admissible proof lane.

## Checks

- `python3 -m py_compile truffles-api/app/routers/webhook/decision.py truffles-api/tests/test_message_endpoint.py`
- `pytest -q truffles-api/tests/test_message_endpoint.py -k "branch_catalog_match or unique_branch_first_name_match or ambiguous_first_name_uses_llm"`
- `pytest -q truffles-api/tests/test_booking_quality_response_guard.py -k "invalid_schema_specialist_followup or specialist_followup_owner_gap"`
- `git diff --check`
- `SESSION_AGENT=a1 scripts/session_check.sh`

## Evidence

1. `/tmp/booking_quality/p1.6o196-l2-dev-20260314-a1-r1/summary.json`
2. `/tmp/booking_quality/p1.6o196-l2-dev-20260314-a1-r1/brief.md`
3. `/tmp/booking_quality/p1.6o196-l2-dev-20260314-a1-r1/responses.jsonl`
4. `/tmp/booking_quality/p1.6o196-l2-dev-20260314-a1-r1/trace_bundle.jsonl`
5. `/tmp/booking_quality/p1.6o196-l2-dev-20260314-a1-r1/manual_audit.json`
6. `truffles-api/app/routers/webhook/decision.py`
7. `truffles-api/tests/test_message_endpoint.py`

## Rollback

1. Revert the bounded branch-catalog first-name match widening and its regressions.
2. Keep `P1.6o196` truth frozen as reopened `M19` on real full-name branch data.
3. Do not reinterpret the surfaced family as scenario-only drift without new evidence.

## No-go

- no new rerun before deterministic closure
- no guessing across ambiguous first-name matches
- no phrase-hardcode for `Айгерим`
- no weakening of the `booking_specialist_followup` contract

## Risks / blockers

- overbroad token matching could steal unrelated user names into branch specialists
- ambiguous same-first-name specialists must remain unresolved
- next proof may surface adjacent scenario-governance drift on the same turn once runtime re-closes

## Residual architecture debt (mandatory)

### Current residuals accepted in this block

- specialist recovery still lives inside `decision.py`
- scenario contract for the surfaced turn may still lag runtime once the runtime family is re-closed
- full proof bundle closure remains incomplete

### Why not in this block

- this slice is only to re-close the reopened runtime family on real branch data

### Risk if deferred

- proof stays blocked on a runtime regression contour that already has a bounded deterministic fix path

### Linked follow-up Task Package(s)

- `docs/TASK_PACKAGES/TP-2026-03-14-p1.6o198-post-p1.6o197-proof-lane-a1.md`

### Expiry/trigger to stop deferral

- before any new expensive rerun after `P1.6o196`

## Next-block contract (mandatory)

### Next block objective

- run exactly one fresh guarded rerun after `P1.6o197` so proof confirms whether reopened `M19` moved off first fail and truthfully surfaces the next admissible result.

### First deterministic check command

- `python3 -m py_compile truffles-api/app/routers/webhook/decision.py truffles-api/tests/test_message_endpoint.py && pytest -q truffles-api/tests/test_message_endpoint.py -k "branch_catalog_match or unique_branch_first_name_match or ambiguous_first_name_uses_llm" && pytest -q truffles-api/tests/test_booking_quality_response_guard.py -k "invalid_schema_specialist_followup or specialist_followup_owner_gap"`

### Blocked-by conditions

- `P1.6o196` truth is not synced in active docs/state/session
- targeted `P1.6o197` runtime checks are not green
- the team attempts another expensive rerun before deterministic closure of this block

### Owner role for closure

- `Top Architect + Brain`

## Execution status

- `done`

## Execution result

- `_resolve_specialist_name_hint_with_trace(...)` now reuses the existing branch-catalog matcher for unique first-name surfaces before the secondary LLM, while keeping ambiguous same-first-name cases fail-closed.
- `truffles-api/tests/test_message_endpoint.py` now covers the surfaced runtime contour with real full-name branch data (`Айгерим Болатова`) and protects the ambiguous fallback path.
- Targeted deterministic checks are green and `P1.6o198` is published as the next admissible proof lane.

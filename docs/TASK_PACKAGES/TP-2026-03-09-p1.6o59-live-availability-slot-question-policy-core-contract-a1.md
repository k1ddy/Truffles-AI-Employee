# TP-2026-03-09-p1.6o59-live-availability-slot-question-policy-core-contract-a1

- Название/цель: закрыть surfaced `r72` blocker family, где live `llm_policy_core` под активным `expected_reply_type=time` распознаёт booking availability question как `calendar.list_slots + temporal_scope=none`, но не выставляет `pending_question_act/target`. Из-за этого runtime снова схлопывает ход в generic `booking_prompt`.
- Parent TP: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Canon refs: `AGENTS.md`, `STATE.md`, `STRUCTURE.md`, `STRATEGY/REQUIREMENTS.md`, `docs/runbooks/BOOKING_CONFIRM_VERIFY.md`, `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o58-calendar-book-slot-specialist-missing-followup-contract-a1.md`
- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect remove worktree + branch after merge`

## One web search (mandatory before implementation)

- Query: `site:rasa.com/docs/forms requested slot availability question keep requested slot`
- Time (UTC): `2026-03-09T12:02:00Z`
- Sources:
  - `https://rasa.com/docs/rasa/forms/`
- Ready solutions found:
  - while a requested slot stays active, availability/follow-up questions must preserve the same requested slot contract instead of demoting the turn into a generic re-ask;
  - the slot question needs explicit ownership in the semantic contract before runtime boundaries run.
- Decision (`reuse/integrate/build`): `reuse + integrate`
  - reuse the existing `pending_question_act/target` contract and `booking_slot_guidance` owner;
  - integrate the missing live-availability semantic rule into `llm_policy_core` prompt/tests instead of adding runtime phrase routing.
- Rejected options:
  - post-hoc semantic rewrite in `decision.py` based on free-slot wording;
  - weakening scenario/oracle to accept generic `booking_prompt`;
  - adding regex/phrase branching in runtime core.

## Root cause (mandatory)

- Symptom:
  - guarded `dev L2` rerun `p1.6o6-l2-dev-20260309-a1-r72` stopped on dialog `1`, turn `2`, user text `Когда у вас есть свободные слоты?`.

- Minimal reproduction:
  1. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r72/scenarios.json` for dialog `1`, turns `1-2`
  2. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r72/responses.jsonl` turn `2`
  3. confirm `llm_policy_core.payload` keeps `tool_action=calendar.list_slots`, `temporal_scope=none`, `pending_question_act=null`, `pending_question_target=null`
  4. confirm runtime then emits `policy_core_guard -> booking_prompt` with `answer_error=blocked_by_info`
  5. compare with deterministic tests that only cover the branch where policy-core already returns `ask_about_requested_slot(time)`

- Evidence:
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r72/summary.json`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r72/brief.md`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r72/scenarios.json`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r72/responses.jsonl`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r72/trace_bundle.jsonl`
  - `prompts/llm_policy_core.md`
  - `truffles-api/tests/test_intent.py`

- Five Whys:
  1. Why did `r72` fail: scenario expected `pending_question_act=time` evidence, but runtime emitted generic collect prompt.
  2. Why did runtime emit generic prompt: policy-core did not output `pending_question_act/target`, so the bounded owner never activated.
  3. Why did policy-core miss the act: prompt examples/rules explicitly cover "лучшее время" style slot questions, but do not explicitly cover generic live-availability questions under active `datetime` collect.
  4. Why is that a contract gap: availability questions about the requested time slot are still operations over the active pending question, not standalone calendar execution when temporal scope is missing.
  5. Why is runtime hardcode the wrong fix: the missing classification happens before deterministic boundaries; patching it in core would be post-hoc semantic rewrite.

- Root cause statement:
  - the semantic contract in `llm_policy_core` is underspecified for generic live-availability questions asked during active `datetime` collect, so the live model can omit `pending_question_act/target` and fall back into a blocked `calendar.list_slots` plan.

- Fix mechanism:
  - strengthen `llm_policy_core` prompt with an explicit contract for active-slot live-availability questions without temporal scope;
  - add deterministic prompt-regression coverage so this rule remains frozen;
  - rerun guarded `dev L2` once to verify the family no longer surfaces first.

## Release safety

- Strategy: prompt-contract hardening only
- Go signals:
  - prompt explicitly instructs active-slot free-slot questions to stay in `ask_about_requested_slot(time)` / `collect`
  - deterministic prompt regression is green
  - fresh guarded rerun no longer stops on `r72` family
- No-go signals:
  - runtime phrase routing in `decision.py`
  - weakening temporal-scope validation or scenario oracle
- Rollback verification:
  - revert only the prompt/test delta if unrelated semantic families regress

## Invariant

1. Semantic ownership remains in `llm_policy_core`.
2. Runtime deterministic boundaries remain unchanged for this block.
3. No regex/phrase branching is added in runtime core.
4. `calendar.list_slots` still requires grounded temporal scope for real live-availability execution.

## Scope

- `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o59-live-availability-slot-question-policy-core-contract-a1.md`
- `prompts/llm_policy_core.md`
- `truffles-api/tests/test_intent.py`
- tracker docs after rerun handoff

## Out of scope

- runtime boundary redesign
- scenario taxonomy changes
- acceptance lock/replay promotion

## Touch-list

- `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o59-live-availability-slot-question-policy-core-contract-a1.md`
- `prompts/llm_policy_core.md`
- `truffles-api/tests/test_intent.py`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`

## Plan (1..N)

1. Freeze `r72` as semantic RCA evidence.
2. Strengthen `llm_policy_core` prompt for active-slot free-slot questions without temporal scope.
3. Add deterministic prompt regression in `test_intent.py`.
4. Run focused checks.
5. Run one fresh guarded rerun beyond `r72`.
6. Sync tracker docs with `P1.6o58` closure and `P1.6o59` result.

## DoD

1. Prompt explicitly classifies generic free-slot questions under active `datetime` collect as `ask_about_requested_slot(time)` with `tool_action=collect`.
2. Deterministic prompt regression is green.
3. Fresh guarded rerun no longer stops on `r72` family.

## Checks

- `pytest -q truffles-api/tests/test_intent.py -k "pending_question_contract or free_slots"`
- `pytest -q truffles-api/tests/test_message_endpoint.py -k "pending_question_act_time_guidance or timeout_pending_slot_question"`
- `git diff --check`

## Execution status

- `done`
- Deterministic prompt/runtime checks are green:
  - `pytest -q truffles-api/tests/test_intent.py -k "pending_question_contract or free_slots"`
  - `pytest -q truffles-api/tests/test_message_endpoint.py -k "pending_question_act_time_guidance or timeout_pending_slot_question"`
- Fresh guarded rerun `p1.6o6-l2-dev-20260309-a1-r73` no longer stopped on the `r72` free-slot question family. The next surfaced blocker is a different family: active-time specialist availability follow-up (`Какой мастер свободен на этой неделе?`), reopened as `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o60-master-availability-followup-active-time-owner-a1.md`.

## Evidence

1. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r72/summary.json`
2. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r72/brief.md`
3. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r72/scenarios.json`
4. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r72/responses.jsonl`
5. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r72/trace_bundle.jsonl`
6. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r72/manual_audit.md`
7. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r73/summary.json`
8. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r73/brief.md`
9. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r73/scenarios.json`
10. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r73/responses.jsonl`
11. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r73/trace_bundle.jsonl`
12. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r73/manual_audit.md`

## Rollback

1. Revert only the prompt/test delta.
2. Keep `P1.6o6` blocked; do not reinterpret `r72` as closure evidence.

## No-go

- no runtime phrase hardcode
- no oracle weakening
- no semantic post-hoc rewrite in deterministic boundaries

## Risks/Blockers

1. The next surfaced family may still be another live-policy semantic miss and should be evaluated independently.
2. `infra_valid=false` due unrelated tool-evidence checks remains orthogonal to this semantic slice.

## Residual architecture debt (mandatory)

- Current residuals accepted in this block:
  - prompt-regression can freeze the contract, but proof closure still depends on another guarded rerun
- Why not in this block:
  - this slice only closes the newly surfaced policy-core semantic miss
- Risk if deferred:
  - the same semantic family keeps reappearing under different wording and forces reactive reruns
- Linked follow-up Task Package(s):
  - `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`
- Expiry/trigger to stop deferral:
  - before the next guarded rerun beyond `r72`

## Next-block contract (mandatory)

- Next block objective:
  - bounded closure complete; follow-up block is `P1.6o60`
- First deterministic check command:
  - `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r73 --status done --strict-artifacts`
- Blocked-by conditions:
  - `P1.6o60` owner boundary is still ambiguous
- Owner role for closure:
  - `Hands` for bounded fix + rerun evidence, `Top Architect + Brain` for proof-bundle promotion

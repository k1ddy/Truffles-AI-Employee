# TP-2026-03-08-p1.6o34-grounded-time-availability-probe-normalization-a1.md

- Название/цель: закрыть новый `r33` deterministic blocker family, где grounded time availability probes (`Есть ли варианты на 15:00?`) under active `reply_type=time` сохраняются как `ask_about_requested_slot` с обязательным `pending_question_interaction`, хотя runtime уже трактует их как validated slot fill + live availability reply and advances collect.
- Parent TP: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Canon refs: `AGENTS.md`, `STATE.md`, `STRUCTURE.md`, `STRATEGY/REQUIREMENTS.md`, `docs/runbooks/BOOKING_CONFIRM_VERIFY.md`, `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`, `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o33-partial-date-mixed-question-normalization-a1.md`
- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect remove worktree + branch after merge`

## One web search (mandatory before implementation)

- Query: `site:rasa.com/docs/rasa/forms requested_slot specific slot value availability follow up`
- Time (UTC): `2026-03-08T12:30:17Z`
- Sources:
  - `https://rasa.com/docs/rasa/forms/`
- Ready solutions found:
  - when a form already asks for a slot, a user turn that supplies a concrete slot value should keep the requested-slot flow instead of being modeled as a separate side-question family; clarification/question wording matters only when no grounded slot value is supplied.
- Decision (`reuse/integrate/build`): `reuse + integrate`
  - reuse the existing explicit-slot-fill contract (`time` tag / question-contract matched);
  - integrate deterministic normalization so grounded time availability probes stop demanding pending-question interaction evidence;
  - build no new runtime owner while runtime and judge already agree on slot-progress semantics.
- Rejected options:
  - adding another runtime owner for a family that already lands on valid `calendar.list_slots` + collect progress;
  - weakening oracle to accept contradictory pending-question and slot-fill contracts on the same turn;
  - phrase-hardcoding runtime prompts or tool decisions.

## Root cause (mandatory)

- Symptom:
  - guarded `dev L2` rerun `p1.6o6-l2-dev-20260308-a1-r33` progressed beyond `P1.6o33`, then stopped on dialog `1`, turn `3`, text `Есть ли варианты на 15:00?` with `expected_meta_mismatch` and `expected_trace_miss`.

- Minimal reproduction:
  1. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r33/scenarios.json` for dialog `1`, turn `3`
  2. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r33/responses.jsonl` for `dialog_index=1`, `turn_index=3`
  3. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r33/trace_bundle.jsonl` for the same turn
  4. compare scenario expectation `ask_about_requested_slot + pending_question_target=time` against runtime meta/trace
  5. inspect `scripts/booking_dialog_scenarios.py` normalization for explicit grounded time values under active `reply_type=time`

- Evidence:
  - scenario keeps tag `ask_about_requested_slot` and therefore requires:
    - `meta_any.pending_question_act=ask_about_requested_slot`
    - `meta_any.pending_question_target=time`
    - `trace_contains pending_question_interaction`
  - runtime instead does the following coherently:
    - `question_contract matched` on `answer_slot=datetime`, `answer_value=15:00`
    - `llm_policy_core.action=fact`, `tool_action=calendar.list_slots`, `temporal_scope=specific_time`
    - `tool_registry decision=ok`
    - `expected_reply_type=name`
  - judge marks the turn `pass`
  - therefore the surfaced blocker is deterministic taxonomy overclaim: a grounded candidate-slot availability probe is still forced into the ungrounded pending-question owner family.

- Five Whys:
  1. Why did `r33` fail: scenario expected pending-question evidence that runtime did not emit.
  2. Why did runtime not emit it: LLM and runtime treated the turn as concrete slot fill plus live availability resolution.
  3. Why did scenario expect pending-question evidence: `ask_about_requested_slot` survived even though the utterance supplied a grounded exact time.
  4. Why is that wrong here: once the turn contains a concrete `datetime` candidate under active time collect, the contract boundary becomes slot-fill / live availability, not generic time-guidance.
  5. Why did it surface now: `P1.6o33` removed the previous partial-date overclaim, exposing the next adjacent explicit-time overclaim family.

- Root cause statement:
  - scenario taxonomy still overlabels grounded exact-time availability probes as `ask_about_requested_slot(time)` instead of reusing the existing explicit slot-fill contract, so oracle asks for pending-question evidence on a turn runtime already resolves as valid collect progress.

- Fix mechanism:
  - under active `reply_type=time`, normalize grounded exact-time availability probes out of `ask_about_requested_slot` and into the explicit `time` slot-fill contract;
  - add deterministic regressions for positive and negative cases;
  - then run one fresh guarded rerun `r34`.

## Release safety

- Strategy: deterministic scenario/oracle normalization only
- Go signals:
  - grounded exact-time availability probes no longer require `pending_question_interaction`
  - ungrounded slot-guidance questions remain in `ask_about_requested_slot`
- No-go signals:
  - runtime-core edits in this block
  - weakening oracle to accept both pending-question and slot-fill contracts simultaneously for the same turn family
- Rollback verification:
  - revert only the new grounded-time normalization if generic slot-question families regress

## Invariant

1. `P1.6o33` stays closed.
2. Under active `reply_type=time`, explicit grounded time fills remain the stronger contract than question-shaped surface wording.
3. Ungrounded time-guidance turns must remain in `ask_about_requested_slot`.
4. Fresh expensive iteration stays exactly one guarded rerun after deterministic closure.

## Scope

- `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o34-grounded-time-availability-probe-normalization-a1.md`
- `scripts/booking_dialog_scenarios.py`
- `truffles-api/tests/test_booking_dialog_scenarios_script.py`

## Out of scope

- runtime-core semantics for `calendar.list_slots`
- new pending-question acts or targets
- acceptance replay/full promotion

## Touch-list

- `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o34-grounded-time-availability-probe-normalization-a1.md`
- `scripts/booking_dialog_scenarios.py`
- `truffles-api/tests/test_booking_dialog_scenarios_script.py`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`

## Plan (1..N)

1. Freeze `r33` as non-closure evidence and isolate the blocker as grounded-time taxonomy overclaim.
2. Normalize grounded exact-time availability probes from `ask_about_requested_slot` into the explicit `time` slot-fill contract.
3. Add deterministic regressions for positive and negative cases.
4. Run one fresh guarded rerun `r34` only after deterministic checks are green.

## DoD

1. Grounded exact-time availability probes under active time collect no longer require `pending_question_act/target` evidence.
2. Ungrounded guidance questions keep `ask_about_requested_slot` expectations.
3. Deterministic scenario-script regressions are green.
4. Next expensive step is exactly one fresh guarded rerun `r34`.

## Expensive run control (mandatory)

- Hypothesis:
  - after grounded-time normalization, the next guarded rerun will not fail first on the `r33` overclaim family.
- Expected measurable effect:
  - either the run proceeds beyond dialog `1`, turn `3`, or a different blocker family surfaces.
- Stop condition:
  - run exactly one fresh guarded rerun plus strict audit; if the same mismatch persists, stop and reopen RCA instead of stacking more normalization patches.

## Checks

- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r33 --status done --strict-artifacts`
- `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "grounded_time or partial_date or mixed_fill_plus_question or ask_about_requested_slot"`
- `git diff --check`

## Execution status

- `done`
- deterministic taxonomy/oracle fix is green:
  - `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "grounded_time or partial_date or mixed_fill_plus_question or ask_about_requested_slot"`
  - `git diff --check`
- fresh guarded `r36` is audited non-closure evidence and no longer surfaces the grounded exact-time availability probe overclaim family
- `r36` instead surfaces a different deterministic taxonomy family on mixed target-axis overclaim, reopened as `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o35-mixed-slot-question-target-axis-overclaim-a1.md`

## Evidence

1. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r33/summary.json`
2. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r33/brief.md`
3. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r33/scenarios.json`
4. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r33/responses.jsonl`
5. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r33/trace_bundle.jsonl`
6. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r33/manual_audit.md`
7. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r36/summary.json`
8. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r36/brief.md`
9. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r36/manual_audit.md`

## Rollback

1. Revert only the grounded-time availability normalization if unrelated exact-time families regress.
2. Keep `P1.6o6` blocked; do not reinterpret `r33` as closure evidence.

## No-go

- no runtime-core edits
- no oracle weakening to accept contradictory pending-question / explicit time-fill contracts
- no fresh guarded rerun before deterministic scenario regression is green

## Risks/Blockers

1. A different runtime or taxonomy family may surface immediately after deterministic drift is removed.
2. Infra tool-evidence invalidity remains orthogonal and can still make the guarded run non-canonical.

## Residual architecture debt (mandatory)

- Current residuals accepted in this block:
  - the system still lacks an explicit contract for future candidate-slot probe families beyond exact time if they surface as distinct semantics
- Why not in this block:
  - current first blocker is still deterministic overclaim, and runtime/judge already agree on the product behavior for this family
- Risk if deferred:
  - the next adjacent candidate-slot family may reopen taxonomy debt later
- Linked follow-up Task Package(s):
  - `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`
- Expiry/trigger to stop deferral:
  - before the next guarded rerun after `P1.6o34`

## Next-block contract (mandatory)

- Next block objective:
  - close bounded `P1.6o35`, then run one fresh guarded rerun `r37` to verify that mixed slot-question turns no longer drift into target-axis evidence requirements
- First deterministic check command:
  - `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "mixed_fill_plus_question or pending_question_target or grounded_time" && git diff --check`
- Blocked-by conditions:
  - `P1.6o35` is not deterministically green
  - guarded preflight is not green
  - runtime `http://127.0.0.1:18184` is unavailable
- Owner role for closure:
  - `Hands` for doc/code/evidence, `Top Architect + Brain` for closure gating

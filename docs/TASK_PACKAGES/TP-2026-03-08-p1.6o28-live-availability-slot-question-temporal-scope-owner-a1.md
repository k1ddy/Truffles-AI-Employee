# TP-2026-03-08-p1.6o28-live-availability-slot-question-temporal-scope-owner-a1

- Название/цель: закрыть новый `r27` runtime blocker family, где policy-core уже правильно классифицирует ход как `ask_about_requested_slot(time)`, но capability/policy guard по `semantic_temporal_scope_missing` снова схлопывает его в generic `booking_prompt` и теряет `pending_question_interaction` evidence.
- Parent TP: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Canon refs: `AGENTS.md`, `STATE.md`, `STRUCTURE.md`, `STRATEGY/REQUIREMENTS.md`, `docs/runbooks/BOOKING_CONFIRM_VERIFY.md`, `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`, `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o27-booking-tag-stateful-normalization-a1.md`
- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect remove worktree + branch after merge`

## One web search (mandatory before implementation)

- Query: `site:rasa.com/docs forms validation requested_slot interruption resume ask follow-up`
- Time (UTC): `2026-03-08T10:39:56Z`
- Sources:
  - `https://rasa.com/docs/rasa/forms/`
- Ready solutions found:
  - when a requested slot remains active but the user asks a follow-up question instead of filling it, the system should handle the interruption explicitly and then resume the same requested slot rather than silently demoting the turn to a generic re-ask.
- Decision (`reuse/integrate/build`): `reuse + integrate`
  - reuse the existing `pending_question_act/target` contract and `booking_slot_guidance` owner;
  - integrate a temporal-scope-missing runtime branch into the existing bounded owner instead of inventing a new generic prompt path;
  - build only the missing policy-validation boundary that preserves the already-correct LLM act.
- Rejected options:
  - weakening oracle to accept generic `booking_prompt` here;
  - removing `requires_temporal_scope` from capability contract;
  - new phrase hardcodes for `свободные слоты` / `на какое время`.

## Root cause (mandatory)

- Symptom:
  - fresh guarded `dev L2` rerun `p1.6o6-l2-dev-20260308-a1-r27` stopped on `dialog_id=1`, `turn_index=2`, message `На какое время есть свободные слоты?`

- Minimal reproduction:
  1. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r27/brief.md`
  2. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r27/scenarios.json` for dialog `1`, turns `1-2`
  3. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r27/responses.jsonl` for message id `LLM-QUAL-p1.6o6-l2-dev-20260308-a1-r27-001-02-85a9df`
  4. inspect `truffles-api/app/routers/webhook/decision.py` around `semantic_temporal_scope_missing_collect` and existing `booking_slot_guidance` owner branch

- Evidence:
  - policy-core output is already correct: `pending_question_act=ask_about_requested_slot`, `pending_question_target=time`, `tool_action=calendar.list_slots`, `slots.service=Маникюр`, `resolution_mode=clarify_missing_time`.
  - capability contract then flags `requires_temporal_scope=true` and emits `semantic_temporal_scope_missing_collect`.
  - guard rewrites the turn to `policy_core_guard -> booking_prompt`, preserving only `expected_reply_type=time` but dropping `pending_question_interaction` evidence and dedicated owner path.
  - failure is therefore not in LLM semantics, but in post-LLM runtime boundary compression.

- Five Whys:
  1. Why did `r27` fail: scenario expected `pending_question_interaction` evidence, but runtime emitted generic `booking_prompt`.
  2. Why did runtime emit generic prompt: capability/policy guard treated missing temporal scope as generic collect validation failure.
  3. Why is that wrong for this turn: the LLM had already identified a valid `ask_about_requested_slot(time)` act under active booking collect.
  4. Why was the dedicated owner not used: the existing `booking_slot_guidance` branch runs later on the generic collect path and is bypassed by the earlier temporal-scope validation rewrite.
  5. Why did this surface now: `P1.6o26` and `P1.6o27` removed the scenario-side drifts, so the underlying runtime boundary compression became first blocker again.

- Root cause statement:
  - runtime policy-validation still treats `semantic_temporal_scope_missing` as a generic collect fallback even when policy-core has already produced a valid `pending_question_act=ask_about_requested_slot(time)` plan; the guard runs before the dedicated bounded owner and erases the semantic act.

- Fix mechanism:
  - route `semantic_temporal_scope_missing + ask_about_requested_slot(time) + active expected_reply_type=time` into the existing bounded `booking_slot_guidance` owner instead of generic `booking_prompt`;
  - preserve `pending_question_interaction` trace/meta and keep `expected_reply_type=time` without fake calendar availability claims.

## Release safety

- Strategy: bounded runtime owner fix using existing guidance path
- Go signals:
  - `pending_question_interaction` evidence survives temporal-scope validation boundary
  - response stays bounded guidance / resume, not live availability hallucination
  - `expected_reply_type=time` remains active
- No-go signals:
  - weakening `requires_temporal_scope` globally
  - hardcoded phrase routing in runtime core
  - fake availability claims without calendar evidence
- Rollback verification:
  - revert only the bounded temporal-scope owner branch if non-slot collect paths regress

## Invariant

1. LLM semantic owner remains authoritative for `pending_question_act/target`.
2. Capability contract still forbids live availability execution without temporal scope.
3. Runtime must preserve the semantic act and resume contract even when tool execution is blocked.
4. No fake slot progress or fake calendar facts.

## Scope

- `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o28-live-availability-slot-question-temporal-scope-owner-a1.md`
- `truffles-api/app/routers/webhook/decision.py`
- `truffles-api/app/routers/webhook/booking.py` (only if shared helper reuse is needed)
- `truffles-api/tests/test_message_endpoint.py`
- `truffles-api/tests/test_booking_quality_response_guard.py`

## Out of scope

- scenario-generator changes
- capability contract redesign beyond this bounded owner path
- lock/replay acceptance evidence

## Touch-list

- `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o28-live-availability-slot-question-temporal-scope-owner-a1.md`
- `truffles-api/app/routers/webhook/decision.py`
- `truffles-api/app/routers/webhook/booking.py`
- `truffles-api/tests/test_message_endpoint.py`
- `truffles-api/tests/test_booking_quality_response_guard.py`

## Plan (1..N)

1. Record `r27` as audited non-closure evidence and freeze the temporal-scope RCA.
2. Reuse existing `booking_slot_guidance` owner for the temporal-scope-missing branch when the semantic act is already `ask_about_requested_slot(time)`.
3. Add deterministic message-endpoint and quality-guard regressions.
4. Sync active TPs so the next expensive step becomes fresh guarded `r28` only after deterministic closure.

## DoD

1. `r27` is recorded as non-closure evidence.
2. `semantic_temporal_scope_missing` no longer erases `pending_question_interaction` for valid `ask_about_requested_slot(time)` turns.
3. Runtime responds via bounded guidance / resume contract, not generic `booking_prompt`.
4. Deterministic endpoint + guard tests are green.
5. The next admissible expensive step is one fresh guarded rerun on the updated runtime contour.

## Expensive run control (mandatory)

- Hypothesis:
  - after `P1.6o28`, fresh guarded rerun will no longer stop on `semantic_temporal_scope_missing` collapsing a valid `ask_about_requested_slot(time)` act into generic `booking_prompt`
- Expected measurable effect:
  - the next run proceeds beyond dialog `1`, turn `2`; if a new blocker appears, it is a different family than this temporal-scope owner miss
- Stop condition:
  - run exactly one fresh guarded rerun plus strict audit; if the same family persists, stop the loop and reopen RCA instead of chaining more reruns

## Checks

- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r27 --status done --strict-artifacts`
- `pytest -q truffles-api/tests/test_message_endpoint.py -k "pending_question_act_time_guidance or semantic_temporal_scope_missing"`
- `pytest -q truffles-api/tests/test_booking_quality_response_guard.py -k "pending_question_interaction or booking_slot_stall"`
- `git diff --check`

## Execution status

- `done`

## Evidence

1. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r28/summary.json`
2. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r28/brief.md`
3. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r28/scenarios.json`
4. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r28/responses.jsonl`
5. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r28/trace_bundle.jsonl`
6. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r28/manual_audit.md`
7. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r27/summary.json`
8. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r27/manual_audit.md`

## Rollback

1. Revert only the bounded temporal-scope owner branch if unrelated collect flows regress.
2. Keep `P1.6o6` blocked; do not reinterpret `r27` as closure evidence.

## No-go

- no weakening of capability contract
- no phrase hardcode in runtime core
- no fresh guarded rerun before deterministic closure is green

## Risks/Blockers

1. Another adjacent runtime family may surface once this temporal-scope branch preserves the semantic act.
2. Infra/tool-hook invalidity remains orthogonal and may still block canonical evidence.

## Residual architecture debt (mandatory)

- Current residuals accepted in this block:
  - fresh guarded proof remains open via `P1.6o6` after this bounded runtime closure
- Why not in this block:
  - this block only repairs one post-LLM runtime boundary
- Risk if deferred:
  - runtime continues to compress correct LLM acts into generic collect outcomes and expensive runs stay reactive
- Linked follow-up Task Package(s):
  - `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`
- Expiry/trigger to stop deferral:
  - before the next fresh guarded rerun

## Next-block contract (mandatory)

- Next block objective:
  - bounded closure is complete; audited `r28` reopens deterministic follow-up `P1.6o29` instead of the old runtime family
- First deterministic check command:
  - `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "mixed_fill_plus_question or pending_question or service_grounded_booking or orphan_pending_question" && git diff --check`
- Blocked-by conditions:
  - `P1.6o29` is not deterministically green
- Owner role for closure:
  - `Hands` for doc/code/evidence, `Top Architect + Brain` for closure gating

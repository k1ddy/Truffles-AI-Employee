# TP-2026-03-09-p1.6o52-reschedule-followup-service-choice-drift-a1.md

- Название/цель: закрыть surfaced `r62` deterministic blocker, где после `reschedule` handoff поворота follow-up `Нужно на следующую неделю.` остаётся в scenario taxonomy как booking `service_choice`, хотя runtime уже перевёл диалог в `reschedule` / manager-owned path.
- Parent TP: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Canon refs: `AGENTS.md`, `STATE.md`, `STRUCTURE.md`, `STRATEGY/REQUIREMENTS.md`, `docs/runbooks/BOOKING_CONFIRM_VERIFY.md`, `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o51-question-like-specialist-followup-collect-owner-a1.md`
- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect remove worktree + branch after merge`

## One web search (mandatory before implementation)

- Query: `Rasa forms unhappy path questions during slot filling official docs`
- Time (UTC): `2026-03-09T09:26:00Z`
- Sources:
  - `https://rasa.com/docs/rasa/forms/`
- Ready solutions found:
  - once the interaction owner pivots away from active slot collection, follow-up turns must inherit the new owner/state rather than the stale slot contract;
  - governance/state tracking must be updated before deriving the next expected reply.
- Decision (`reuse/integrate/build`): `reuse + integrate`
  - reuse existing `reschedule` governance normalization path from `P1.6o47`;
  - integrate the missing follow-up shape (`Нужно на следующую неделю.` under active `reschedule`) into the same stateful rewrite layer;
  - build no new runtime semantic branch unless deterministic evidence proves runtime is wrong.
- Rejected options:
  - weakening `expected_meta_mismatch` / `expected_trace_miss` for these turns;
  - reinterpreting a manager-owned reschedule turn as booking collect just to satisfy current scenario expectations;
  - adding surface-only waivers instead of fixing governance state.

## Root cause (mandatory)

- Symptom:
  - guarded `dev L2` run `p1.6o6-l2-dev-20260309-a1-r62` stops on dialog 2 turn 3 with `expected_meta_mismatch` and `expected_trace_miss` for `Нужно на следующую неделю.`.

- Minimal reproduction:
  1. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r62/scenarios.json` dialog 2 turns 2-4 and confirm turn 2 is tagged `reschedule`, but turns 3-4 still expect booking `service_choice`;
  2. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r62/responses.jsonl` dialog 2 turns 2-3 and confirm runtime already escalates reschedule on turn 2, then keeps asking for time on turn 3 under the manager-owned path;
  3. inspect `scripts/booking_dialog_scenarios.py` reschedule follow-up normalization and confirm the current detector does not rewrite this follow-up wording under active `reschedule`.

- Evidence:
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r62/summary.json`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r62/brief.md`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r62/scenarios.json`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r62/responses.jsonl`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r62/trace_bundle.jsonl`
  - `scripts/booking_dialog_scenarios.py`

- Five Whys:
  1. Why did the run stop: scenario expected booking `service_choice` evidence after a `reschedule` turn, while runtime stayed on the post-reschedule path.
  2. Why did scenario expect booking again: the follow-up turn remained tagged `booking` instead of inheriting active `reschedule` governance.
  3. Why did the tag stay `booking`: the current reschedule follow-up detector does not recognize this date-only reformulation.
  4. Why is this deterministic rather than runtime: runtime already escalated the reschedule turn coherently on turn 2; the mismatch is in scenario-governance state, not semantic ownership.
  5. Why is this still important architecturally: if governance tracking is stale, guarded proof will keep surfacing false blocker families and hide the next real runtime defect.

- Root cause statement:
  - scenario governance does not rewrite some date-only follow-ups under active `reschedule`, so stale booking `service_choice` expectations survive after the dialog has already pivoted away from booking collect.

- Fix mechanism:
  - extend the existing stateful `reschedule` follow-up normalization to cover this date-only follow-up shape;
  - add deterministic scenario-governance regression;
  - rerun guarded dev after the deterministic fix.

## Release safety

- Strategy: deterministic scenario-governance only
- Go signals:
  - post-reschedule follow-up turns no longer expect booking `service_choice`
  - guarded rerun moves past `r62` without reopening the `r60` family
- No-go signals:
  - runtime changes are introduced before deterministic evidence proves runtime is wrong
  - scenario fix weakens canonical reschedule expectations
- Rollback verification:
  - revert only the scenario-governance normalization and targeted tests if it over-matches ordinary booking turns

## Invariant

1. Once runtime pivots to `reschedule`, scenario governance must not silently return to booking collect.
2. Booking families unrelated to reschedule remain untouched.
3. No oracle waiver is introduced.

## Scope

- `scripts/booking_dialog_scenarios.py`
- `truffles-api/tests/test_booking_dialog_scenarios_script.py`
- tracker docs after rerun handoff

## Out of scope

- runtime changes to reschedule ownership
- handoff transport/billing failures
- acceptance/full proof closure

## Touch-list

- `scripts/booking_dialog_scenarios.py`
- `truffles-api/tests/test_booking_dialog_scenarios_script.py`
- `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o52-reschedule-followup-service-choice-drift-a1.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`

## Plan (1..N)

1. Freeze `r62` as deterministic scenario-governance evidence.
2. Extend existing reschedule follow-up normalization for date-only follow-up wording.
3. Add targeted deterministic regressions.
4. Run focused checks and `git diff --check`.
5. Run one fresh guarded dev rerun beyond the `r62` family.

## DoD

1. Post-reschedule follow-up turns no longer expect booking `service_choice`.
2. Deterministic scenario-governance regressions pass.
3. One fresh guarded rerun no longer stops on the `r62` family.

## Checks

- `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "reschedule or pending_question or named_specialist_master"`
- `git diff --check`

## Execution status

- `done`
- deterministic reschedule-governance closure is complete: focused scenario regressions are green, guarded `r63` no longer stops on the `r62` family, and the next surfaced blocker is a different runtime family.

## Evidence

1. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r62/summary.json`
2. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r62/brief.md`
3. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r62/scenarios.json`
4. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r62/responses.jsonl`
5. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r62/trace_bundle.jsonl`
6. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r62/manual_audit.md`
7. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r63/summary.json`
8. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r63/brief.md`
9. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r63/scenarios.json`
10. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r63/responses.jsonl`
11. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r63/trace_bundle.jsonl`
12. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r63/manual_audit.md`

## Rollback

1. Revert only the deterministic reschedule-governance normalization and targeted tests.
2. Keep `P1.6o6` blocked; do not reinterpret `r62` as closure evidence.

## No-go

- no runtime semantic hardcode
- no oracle waiver for post-reschedule follow-up turns
- no reclassification of runtime `reschedule` behavior into booking collect

## Risks/Blockers

1. The next surfaced family may still be a different governance edge after this one closes.
2. Acceptance remains partial while the guarded dev chain stops at first failure.

## Residual architecture debt (mandatory)

- Current residuals accepted in this block:
  - other post-management follow-up shapes may still be uncovered by deterministic governance
- Why not in this block:
  - this slice only closes the concrete date-only follow-up surfaced by `r62`
- Risk if deferred:
  - guarded dev runs keep spending budget on stale governance drift instead of revealing the next real runtime blocker
- Linked follow-up Task Package(s):
  - `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`
- Expiry/trigger to stop deferral:
  - next guarded rerun after deterministic closure

## Next-block contract (mandatory)

- Next block objective:
  - bounded runtime closure for the specialist-followup hint-timeout family surfaced by `r63`
- First deterministic check command:
  - `pytest -q truffles-api/tests/test_message_endpoint.py -k "specialist_followup or specialist_hint or bare_named_specialist" && git diff --check`
- Blocked-by conditions:
  - specialist-followup recovery regressions are not green
  - `r63` audit not reflected in tracker docs
  - fresh rerun preflight invalid
- Owner role for closure:
  - `Hands` for deterministic closure + rerun evidence, `Top Architect + Brain` for proof-bundle promotion

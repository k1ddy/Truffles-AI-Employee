# TP-2026-03-14-p1.6o163-timeout-active-time-choose-specialist-interrupt-recovery-a1

- Название/цель: закрыть surfaced `P1.6o162` runtime timeout boundary drift, где explicit generic specialist-choice question (`"Могу ли я выбрать специалиста?"`) под active `expected_reply_type=time` и `policy_core` timeout проваливается в generic `booking_info_contract/master` с `pending_question_target=time` вместо bounded specialist-target interrupt с сохранённым `time` resume.

## Canon refs

- Parent closure program: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Main remediation: `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- Binding roadmap: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o135-beauty-salon-canonical-control-plane-roadmap-a1.md`
- Prior proof lane: `docs/TASK_PACKAGES/TP-2026-03-14-p1.6o162-post-p1.6o161-proof-lane-a1.md`
- Related bounded runtime slices: `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o93-generic-specialist-choice-followup-owner-a1.md`, `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o107-active-time-timeout-generic-specialist-choice-followup-a1.md`
- Class routing: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o118-class-level-closure-process-reset-a1.md`
- State baseline: `STATE.md`

## Git context

- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `origin/main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect` remove branch + worktree after merge

## One web search (mandatory before implementation)

- Query: `site:docs.temporal.io retry policies transient failures permanent failures`
- Date/time: `2026-03-14T09:42:00+05:00`
- Sources opened:
  - Temporal docs home: `https://docs.temporal.io/`
- Source quality:
  - official vendor documentation
- Found reusable ideas:
  - timeout/transient failure handling should resume from preserved workflow context instead of collapsing into a generic path that loses intent-specific state;
  - recovery logic should keep the boundary contract observable after a transient failure.
- Decision:
  - `reuse`
  - reuse the existing timeout specialist-interrupt recovery path and widen only its safe-fallback admission so explicit choose-specialist timeout turns are recovered before generic master info fallback wins.
- Rejected options:
  - weakening scenario/oracle to accept generic `booking_info_contract/master` as success;
  - broad timeout fallback refactor;
  - phrase-hardcoding in `llm_policy_core`.

## Root cause (mandatory)

- Symptom:
  - `P1.6o162` strict-audited rerun is `infra_valid=true` and surfaces `dialog 1 / turn 3 / "Могу ли я выбрать специалиста?"`, where scenario and judge both reject runtime output because timeout fallback answers through generic `booking_info_contract/master`, mixes FACT + booking prompt, and keeps `pending_question_target=time` instead of specialist-target interrupt evidence.
- Minimal reproduction:
  1. Inspect `/tmp/booking_quality/p1.6o162-l2-dev-20260314-a1-r1/responses.jsonl` turn `3` and confirm final meta is `action=reply`, `intent=master`, `source=booking_info_contract`, `expected_reply_type=time`, `pending_question_target=time`, `policy_core_degrade_reason=policy_error:timeout`.
  2. Confirm trace contains `booking_interrupt info_reply` with `pending_question_target=time` and no timeout specialist-target recovery trace.
  3. Inspect `decision.py` timeout logic and confirm `timeout_needs_booking_collect = booking_collect_contract_signal and not pending_info_signal`, so explicit specialist-choice turns with `info_class_intents={"master"}` disable `timeout_booking_safe_fallback`.
  4. Confirm `_is_timeout_active_time_specialist_interrupt_candidate(...)` itself would accept the surfaced text (`resolve_master_intent(...).explicit == True`), but that recovery is never reached because `pending_info_signal` blocks the safe-fallback gate earlier.
- Evidence:
  - `/tmp/booking_quality/p1.6o162-l2-dev-20260314-a1-r1/summary.json`
  - `/tmp/booking_quality/p1.6o162-l2-dev-20260314-a1-r1/brief.md`
  - `/tmp/booking_quality/p1.6o162-l2-dev-20260314-a1-r1/responses.jsonl`
  - `/tmp/booking_quality/p1.6o162-l2-dev-20260314-a1-r1/trace_bundle.jsonl`
  - `truffles-api/app/routers/webhook/decision.py`
  - `truffles-api/tests/test_message_endpoint.py`
- Five Whys:
  1. Why did the turn fail? Because runtime answered with generic master info + booking prompt and judge marked it as a missed specialist-choice question.
  2. Why did runtime choose generic master info? Because timeout fallback stayed on the generic info branch.
  3. Why didn’t timeout specialist-target recovery run? Because `timeout_booking_safe_fallback` was disabled.
  4. Why was it disabled? Because `pending_info_signal` was true from `info_class_intents={"master"}` and `timeout_needs_booking_collect` hard-blocks any booking-safe fallback when info is present.
  5. Why is this architectural debt? Because timeout boundary recovery is narrower than the bounded runtime contract: explicit specialist-choice timeout turns should reuse existing specialist-target recovery instead of being preempted by a generic info signal.
- Root cause statement:
  - timeout booking-safe fallback is over-gated by `pending_info_signal`, so explicit generic specialist-choice turns under active `time` collect never reach the existing timeout specialist-interrupt recovery path and collapse into generic `booking_info_contract/master`.
- Fix mechanism:
  - allow timeout booking-safe fallback to stay active for the bounded explicit specialist-choice timeout envelope even when `pending_info_signal` is present;
  - add a regression test that reproduces `expected_reply_type=time + info_class_intents={'master'} + policy_core timeout + "Могу ли я выбрать специалиста?"` and proves the runtime emits specialist-target timeout interrupt evidence instead of generic master fallback.

## Reuse-first decision (mandatory)

- Reuse:
  - existing `_is_timeout_active_time_specialist_interrupt_candidate(...)`
  - existing timeout master-info interrupt recovery path in `decision.py`
  - existing response-guard contract for `timeout_master_info_interrupt`
- Integrate:
  - widen one safe-fallback admission; do not create a new timeout engine
- Build:
  - no new dependency or framework

## Invariant

1. Generic master-info timeout fallback outside the explicit specialist-choice envelope stays unchanged.
2. Successful policy-core specialist-choice path from `P1.6o93` remains unchanged.
3. Scenario/oracle are not weakened.
4. Timeout recovery remains observable in `decision_meta/decision_trace`.

## Scope

- `truffles-api/app/routers/webhook/decision.py`
- `truffles-api/tests/test_message_endpoint.py`
- truth sync docs for `P1.6o162` / `P1.6o163`

## Out of scope

- scenario-governance changes
- proof-lane rerun itself
- broad timeout fallback redesign
- acceptance/lock updates

## Touch-list

- `truffles-api/app/routers/webhook/decision.py`
- `truffles-api/tests/test_message_endpoint.py`
- `STATE.md`
- `docs/SESSIONS/SESSION-2026-02-19-llm-first-firebreak-a1.md`
- `docs/SESSION_INDEX.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-14-p1.6o162-post-p1.6o161-proof-lane-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-14-p1.6o163-timeout-active-time-choose-specialist-interrupt-recovery-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-14-p1.6o164-post-p1.6o163-proof-lane-a1.md`

## Plan

1. Freeze `P1.6o162` truth and publish `P1.6o163`.
2. Widen timeout booking-safe fallback only for the explicit specialist-choice timeout envelope.
3. Add a regression test with `pending_info_signal` present.
4. Run targeted deterministic checks.
5. Sync truth and publish the next proof lane.

## DoD

1. The surfaced `dialog 1 / turn 3` timeout envelope no longer falls through to generic `booking_info_contract/master`.
2. Runtime emits specialist-target timeout interrupt evidence while preserving `expected_reply_type=time`.
3. Existing timeout master-info interrupt and policy-core success specialist-choice regressions remain green.
4. Active docs/state/session reflect that `P1.6o162` surfaced a bounded timeout runtime family and `P1.6o163` is the selected continuation.

## Checks

- `python3 -m py_compile truffles-api/app/routers/webhook/decision.py truffles-api/tests/test_message_endpoint.py`
- `pytest -q truffles-api/tests/test_message_endpoint.py -k "timeout_master_info_interrupt_keeps_time_collect_for_generic_specialist_change or timeout_choose_specialist_interrupt_preserves_specialist_target_with_pending_info_signal or generic_specialist_choice_followup_keeps_time_collect"`
- `git diff --check`
- `SESSION_AGENT=a1 scripts/session_check.sh`

## Evidence

1. `/tmp/booking_quality/p1.6o162-l2-dev-20260314-a1-r1/summary.json`
2. `/tmp/booking_quality/p1.6o162-l2-dev-20260314-a1-r1/brief.md`
3. `/tmp/booking_quality/p1.6o162-l2-dev-20260314-a1-r1/responses.jsonl`
4. `/tmp/booking_quality/p1.6o162-l2-dev-20260314-a1-r1/trace_bundle.jsonl`
5. `truffles-api/app/routers/webhook/decision.py`
6. `truffles-api/tests/test_message_endpoint.py`

## Rollback

1. Revert the bounded timeout safe-fallback admission and its regression test.
2. Keep `P1.6o162` truth frozen as the surfaced timeout runtime drift.
3. Do not reinterpret the turn as scenario-only drift without new evidence.

## No-go

- no scenario/oracle weakening
- no broad timeout-fallback rewrite
- no phrase-hardcode in `llm_policy_core`
- no proof rerun before deterministic closure

## Risks / blockers

- overbroad safe-fallback widening could let unrelated info turns bypass intended generic info fallback
- under-scoping the fix could still leave explicit choose-specialist timeout turns on generic master fallback
- next proof may surface an adjacent scenario/meta mismatch after the runtime family moves off first fail

## Residual architecture debt (mandatory)

### Current residuals accepted in this block

- timeout boundary logic still lives in `decision.py`
- proof bundle closure remains incomplete
- generic specialist-choice timeout semantics are still not matrix-driven

### Why not in this block

- this block is a bounded timeout runtime recovery slice only

### Risk if deferred

- the worktree keeps reopening a corroborated timeout runtime failure on the first executed proof dialog

### Linked follow-up Task Package(s)

- `docs/TASK_PACKAGES/TP-2026-03-14-p1.6o164-post-p1.6o163-proof-lane-a1.md`

### Expiry/trigger to stop deferral

- before any new expensive rerun after `P1.6o162`

## Next-block contract (mandatory)

### Next block objective

- publish and run one fresh proof-lane rerun after deterministic closure of `P1.6o163`, then truthfully decide whether the timeout choose-specialist family moved off first fail.

### First deterministic check command

- `python3 -m py_compile truffles-api/app/routers/webhook/decision.py truffles-api/tests/test_message_endpoint.py && pytest -q truffles-api/tests/test_message_endpoint.py -k "timeout_master_info_interrupt_keeps_time_collect_for_generic_specialist_change or timeout_choose_specialist_interrupt_preserves_specialist_target_with_pending_info_signal or generic_specialist_choice_followup_keeps_time_collect" && git diff --check`

### Blocked-by conditions

- `P1.6o162` truth is not synced in active docs/state/session
- targeted `P1.6o163` runtime checks are not green
- the team attempts another expensive rerun before deterministic closure of this block

### Owner role for closure

- `Top Architect + Brain`

## Execution status

- `done`

## Execution result

- `truffles-api/app/routers/webhook/decision.py` now performs a narrow pre-guard admission for the explicit active-time choose-specialist timeout envelope before `degraded_policy_core_critical` gates the timeout rescue block.
- The fix reuses the existing timeout master-info interrupt recovery path and keeps global info-signal behavior unchanged outside this bounded contour.
- Added and passed targeted regressions proving that `policy_core` timeout + active `expected_reply_type=time` + `pending_info_signal=master` now preserves `pending_question_target=specialist`, `active_question_relation=specialist_availability_interrupt`, and `policy_core_guard_recovery=timeout_master_info_interrupt`.

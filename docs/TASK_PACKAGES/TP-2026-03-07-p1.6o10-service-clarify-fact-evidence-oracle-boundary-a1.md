# TP-2026-03-07-p1.6o10-service-clarify-fact-evidence-oracle-boundary-a1

- Название/цель: закрыть новый `P1.6o6` blocker, где guarded `dev L2` классифицирует collect-like `service_clarify` ответ как `fact_without_evidence`, хотя runtime корректно задаёт missing-slot clarify.
- Parent TP: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Canon refs: `AGENTS.md`, `STATE.md`, `STRUCTURE.md`, `STRATEGY/REQUIREMENTS.md`, `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o5-oracle-acceptance-contract-realignment-a1.md`, `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`
- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`

## One web search (mandatory before implementation)

- Query: `site:rasa.com/docs forms slot filling ask for missing slots utter_ask`
- Time (UTC): `2026-03-07T16:10:51Z`
- Sources:
  - `https://rasa.com/docs/rasa/forms/`
- Ready solutions found:
  - missing-slot prompts belong to slot-filling / collect flow, where the assistant asks for the next required slot rather than returning a knowledge fact;
  - collect questions should be evaluated against the requested-slot contract, not against fact-evidence linkage.
- Decision (`reuse/integrate/build`): `reuse + integrate`
  - reuse the existing guarded oracle and make it distinguish collect-like clarifications from fact-like replies using the current intent/meta contract;
  - integrate the fix into `ops/diagnose.py` and exact acceptance tests instead of changing runtime behavior.
- Rejected options:
  - attaching fake fact evidence to `service_clarify` runtime replies;
  - weakening `fact_without_evidence_rate` gate globally;
  - ignoring the new family without reopening the program.

## Root cause (mandatory)

- Symptom:
  - fresh guarded `dev L2` rerun `p1.6o6-l2-dev-20260307-a1-r4` passed the previously blocked early dialogs, then stopped on `dialog_index=2`, `turn_index=2` with `fact_without_evidence`.
  - failing turn: user says `Вот фото референса`; runtime responds with `service_clarify` and `expected_reply_type=service_choice`.

- Minimal reproduction:
  1. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260307-a1-r4/brief.md`
  2. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260307-a1-r4/failure_families.json`
  3. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260307-a1-r4/responses.jsonl` for `message_id=LLM-QUAL-p1.6o6-l2-dev-20260307-a1-r4-002-02-7680f0`
  4. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260307-a1-r4/trace_bundle.jsonl` for the same `message_id`
  5. inspect `_llm_quality_is_fact_like_reply()` / `_llm_quality_has_fact_without_evidence()` in `ops/diagnose.py`

- Evidence:
  - runtime meta on the failing turn is `action=reply`, `intent=service_clarify`, `source=llm_policy_core`, `expected_reply_type=service_choice`, with `question_contract` trace `missing_slot=service`;
  - no `info_sections`, no `fact_source`, no evidence refs are present, which is correct for a collect clarify;
  - `_llm_quality_is_fact_like_reply()` currently treats broad `reply + source=llm_policy_core` replies as fact-like, so `_llm_quality_has_fact_without_evidence()` fires on a collect prompt.

- Five Whys:
  1. Why did guarded quality fail: a collect-like clarify turn was classified as a fact reply without evidence.
  2. Why was it classified as fact-like: diagnose uses broad reply/source heuristics and does not exclude collect clarifications.
  3. Why is that wrong: `service_clarify` is a missing-slot prompt governed by `expected_reply_type`, not a truth/FACT answer.
  4. Why is this an oracle boundary issue: runtime behavior is already correct and evidenced by `question_contract`; only acceptance classification is wrong.
  5. Why does this need a dedicated block: leaving the oracle misclassified would force fake runtime evidence or block all further guarded closure.

- Root cause statement:
  - the guarded oracle still classifies collect-like clarify replies as fact-like whenever they come from `llm_policy_core`, so `service_clarify` turns are falsely flagged as `fact_without_evidence`.

- Fix mechanism:
  - teach diagnose to treat collect-like clarify intents as non-fact replies;
  - prove it with exact tests around `fact_without_evidence` and run-family reporting;
  - rerun one fresh guarded `dev L2` after oracle fix.

## Invariant

1. Runtime behavior stays unchanged for this block; only oracle classification is corrected.
2. Real FACT replies still require evidence linkage.
3. Collect-like clarifications remain evaluated by question/slot contract, not by fact-evidence contract.

## Scope

- `ops/diagnose.py`
- `truffles-api/tests/test_booking_quality_response_guard.py`
- `truffles-api/tests/test_diagnose_run_command.py`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o10-service-clarify-fact-evidence-oracle-boundary-a1.md`

## Out of scope

- changing runtime slot-filling behavior
- changing judge thresholds or pass-rate thresholds
- guarded `lock/replay`

## Touch-list

- `ops/diagnose.py`
- `truffles-api/tests/test_booking_quality_response_guard.py`
- `truffles-api/tests/test_diagnose_run_command.py`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o10-service-clarify-fact-evidence-oracle-boundary-a1.md`

## Plan (1..N)

1. Reproduce the failing `fact_without_evidence` family from `r4` and confirm runtime contract is collect-like, not fact-like.
2. Narrow the diagnose fact-like classifier so `service_clarify` / collect clarifications do not require fact evidence.
3. Add exact tests for both direct reason detection and failure-family aggregation.
4. Run targeted deterministic suites for diagnose/oracle.
5. Return to `P1.6o6` for exactly one fresh guarded `dev L2`.

## DoD

1. `service_clarify` with `expected_reply_type=service_choice` no longer triggers `fact_without_evidence`.
2. Existing true FACT replies without evidence still trigger the family.
3. Targeted deterministic suites are green.
4. `P1.6o6` can truthfully rerun one fresh guarded `dev L2` from this new baseline.

## Checks

- `pytest -q truffles-api/tests/test_booking_quality_response_guard.py -k "fact_without_evidence or service_clarify"`
- `pytest -q truffles-api/tests/test_diagnose_run_command.py -k "fact_without_evidence or service_clarify"`
- `pytest -q truffles-api/tests/test_booking_quality_response_guard.py`
- `pytest -q truffles-api/tests/test_diagnose_run_command.py`
- `ruff check ops/diagnose.py truffles-api/tests/test_booking_quality_response_guard.py truffles-api/tests/test_diagnose_run_command.py`
- `git diff --check`

## Execution status

- `done`
- exact oracle checks:
  - `pytest -q truffles-api/tests/test_booking_quality_response_guard.py -k "fact_without_evidence or service_clarify"` -> `2 passed, 64 deselected`
  - `pytest -q truffles-api/tests/test_diagnose_run_command.py -k "fact_without_evidence or service_clarify"` -> `1 passed, 16 deselected`
- full targeted suites:
  - `pytest -q truffles-api/tests/test_booking_quality_response_guard.py` -> `66 passed`
  - `pytest -q truffles-api/tests/test_diagnose_run_command.py` -> `17 passed`
  - `ruff check ops/diagnose.py truffles-api/tests/test_booking_quality_response_guard.py truffles-api/tests/test_diagnose_run_command.py` -> `pass`
  - `git diff --check` -> `clean`
- guarded follow-up:
  - fresh `dev L2` `p1.6o6-l2-dev-20260307-a1-r5` removed the `r4` false-red and surfaced a new family outside this TP (`info_section_miss` on `master_query + clarify_missing_subject`), reopened as `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o11-master-query-clarify-info-oracle-boundary-a1.md`

## Evidence

1. `/tmp/booking_quality/p1.6o6-l2-dev-20260307-a1-r4/summary.json`
2. `/tmp/booking_quality/p1.6o6-l2-dev-20260307-a1-r4/brief.md`
3. `/tmp/booking_quality/p1.6o6-l2-dev-20260307-a1-r4/failure_families.json`
4. `/tmp/booking_quality/p1.6o6-l2-dev-20260307-a1-r4/manual_audit.md`
5. exact deterministic green outputs from this TP
6. `/tmp/booking_quality/p1.6o6-l2-dev-20260307-a1-r5/summary.json`
7. `/tmp/booking_quality/p1.6o6-l2-dev-20260307-a1-r5/brief.md`
8. `/tmp/booking_quality/p1.6o6-l2-dev-20260307-a1-r5/manual_audit.md`

## Rollback

1. Revert only the diagnose classifier/test updates if they hide real FACT evidence failures.
2. Keep `P1.6o6` blocked and do not run `lock/replay` while this family is open.

## No-go

- no fake fact evidence on runtime replies
- no weakening `fact_without_evidence` thresholds globally
- no reuse of `r4` as closure evidence beyond RCA

## Risks/Blockers

1. A too-broad exclusion may hide real fact-evidence regressions.
2. If the family is actually a runtime evidence gap and not an oracle gap, reopen scope before broadening the classifier.

## Residual architecture debt (mandatory)

- Current residuals accepted in this block:
  - `P1.6o6` proof bundle remains blocked until this oracle family is removed
- Why not in this block:
  - this block corrects only one new acceptance boundary defect
- Risk if deferred:
  - guarded closure stays blocked by a false-red family and pressures the runtime toward fake evidence
- Linked follow-up Task Package(s):
  - `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`
- Expiry/trigger to stop deferral:
  - before any new guarded `lock/replay` or baseline update

## Next-block contract (mandatory)

- Next block objective:
  - return to `P1.6o6` and rerun exactly one fresh guarded `dev L2` after the oracle fix
- First deterministic check command:
  - `pytest -q truffles-api/tests/test_booking_quality_response_guard.py -k "fact_without_evidence or service_clarify" && pytest -q truffles-api/tests/test_diagnose_run_command.py -k "fact_without_evidence or service_clarify"`
- Blocked-by conditions:
  - targeted oracle tests are not green
  - `r4` blocker is not recorded in docs
- Owner role for closure:
  - `Hands` for code/tests, `Brain + Top Architect` for proof-bundle resume decision

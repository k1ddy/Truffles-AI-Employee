# TP-2026-03-11-p1.6o64-timeout-mixed-date-availability-governance-a1

- Название/цель: закрыть surfaced `r83` blocker family, где после bounded `M12` closure свежий guarded `dev L2` больше не падает на alternate exact-time availability, но turn `Есть ли свободные слоты на завтра?` все еще остается в stale active-`time` scenario contract, хотя runtime детерминированно groundит `datetime=завтра`, сохраняет `pending_question_interaction=slot_constraint`, затем при `policy_core` timeout легально резюмирует missing slot `name` через `policy_core_degraded_collect`; judge pass, значит текущий first fail — scenario-governance / oracle drift, а не reopen runtime owner bug.
- Parent TP: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Canon refs: `AGENTS.md`, `STATE.md`, `STRUCTURE.md`, `STRATEGY/REQUIREMENTS.md`, `docs/runbooks/BOOKING_CONFIRM_VERIFY.md`, `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o60a-remaining-closure-architecture-verdict-a1.md`, `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o60b-remaining-closure-owner-matrix-a1.md`, `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o23-mixed-date-slot-question-taxonomy-oracle-a1.md`, `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o33-partial-date-mixed-question-normalization-a1.md`, `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o62-timeout-ask-about-requested-slot-booking-limit-a1.md`, `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o63-active-name-time-availability-followup-owner-a1.md`
- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect remove worktree + branch after merge`

## One web search (mandatory before implementation)

- Query: `site:rasa.com/docs/rasa/forms requested_slot partial date follow up question yes no`
- Time (UTC): `2026-03-08T12:00:00Z`
- Sources:
  - `https://rasa.com/docs/rasa/forms/`
- Source quality:
  - official Rasa forms documentation (primary vendor reference)
- Ready solutions found:
  - when an active form turn both constrains the requested slot and implicitly advances slot filling, the post-grounding contract should resume the still-missing slot instead of keeping the pre-grounding requested-slot expectation;
  - unhappy-path / timeout handling must preserve the already-grounded slot evidence and continue from the next missing slot, not force the prior slot contract to remain the final oracle for the turn.
- Decision (`reuse/integrate/build`): `reuse + integrate`
  - reuse the existing mixed-date normalization family (`P1.6o23`, `P1.6o33`), timeout-degraded collect evidence from `P1.6o62`, and the contract-first arbitration already frozen by `P1.6o63`;
  - integrate one new matrix row (`M13`) for timeout-degraded mixed-date availability under active `time` collect;
  - do not build new runtime owner logic unless deterministic governance closure fails to explain the mismatch.
- Rejected options:
  - treating `r83` as a reopen of `M12`;
  - weakening oracle so both `reply_type=time` and `reply_type=name` count as success on the same turn;
  - phrase-fitting `на завтра` in runtime core;
  - running another expensive loop before deterministic governance closure is green.
- Reuse note:
  - no new web query was run for this turnover; this TP deliberately reuses the exact official-source search already recorded in `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o33-partial-date-mixed-question-normalization-a1.md` because the surfaced family is the same requested-slot / partial-date class under a different degrade path.

## Root cause (mandatory)

- Symptom:
  - fresh guarded `dev L2` run `p1.6o6-l2-dev-20260311-a1-r83` stops at dialog `1`, turn `2`, user text `Есть ли свободные слоты на завтра?`

- Minimal reproduction:
  1. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r83/summary.json`, `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r83/brief.md`, and `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r83/manual_audit.md`
  2. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r83/scenarios.json` dialog `1`, turn `2` and confirm the generated expectation still pins `reply_type=time`, `meta_any.pending_question_act=slot_constraint`, `meta_any.pending_question_target=time`, and `meta_any.expected_reply_type=time`
  3. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r83/responses.jsonl` and `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r83/trace_bundle.jsonl` for dialog `1`, turn `2` and confirm: runtime records `pending_question_interaction=slot_constraint`, `pending_question_target=time`, deterministically matches `answer_slot=datetime`, `answer_value=завтра`, then `policy_core_guard` emits `timeout_booking_collect` / `degraded_collect` and final meta resumes `expected_reply_type=name` with `expected_reply_reason=policy_core_degraded_collect`
  4. confirm the same turn is `evaluation.ok=false` only because of `expected_meta_mismatch`, while `judge.verdict=pass` and `manual_audit.md` freezes `judge_alignment=conflicted`, `winner=contract`
  5. inspect `scripts/booking_dialog_scenarios.py` and `truffles-api/tests/test_booking_dialog_scenarios_script.py` for existing mixed-date / partial-date normalization and confirm there is no canonical post-grounding rule for timeout-degraded collect that turns final expectation from active `time` to resumed `name`

- Evidence:
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r83/summary.json`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r83/brief.md`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r83/scenarios.json`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r83/responses.jsonl`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r83/trace_bundle.jsonl`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r83/manual_audit.md`
  - `scripts/booking_dialog_scenarios.py`
  - `truffles-api/tests/test_booking_dialog_scenarios_script.py`

- Five Whys:
  1. Why did `r83` fail: scenario expected the turn to remain on active `time`, while runtime finished the turn on resumed `name`.
  2. Why did runtime resume `name`: deterministic `question_contract` had already grounded `datetime=завтра`, so after `policy_core` timeout the degraded collect continued with the next missing slot.
  3. Why did scenario keep `time`: generated mixed-date governance still freezes the pre-grounding requested-slot contract as the final oracle for the turn.
  4. Why is that wrong here: once `datetime` is consumed and trace/meta preserve `slot_constraint(time)` plus timeout provenance, the final contract should reflect the post-grounding resume axis, not the stale active-slot entry state.
  5. Why is this a new bounded family instead of a runtime reopen: `P1.6o63` already removed the previous product defect, `r83` carries no judge fail, and the first fail is pure `expected_meta_mismatch` under contract-first arbitration.

- Root cause statement:
  - timeout-degraded mixed-date booking turns still lack a canonical post-grounding scenario/oracle contract: after deterministic `datetime` grounding has already consumed the active `time` slot, generated expectations keep the stale pre-grounding `expected_reply_type=time` instead of switching to the actual resumed `name` contract preserved by runtime trace/meta.

- Fix mechanism:
  - publish a new matrix row (`M13`) for grounded partial-date availability under timeout-degraded collect;
  - normalize scenario/oracle expectations to preserve `pending_question_act=slot_constraint`, `pending_question_target=time`, and explicit timeout provenance, while accepting final `expected_reply_type=name` once `question_contract` grounded `datetime`;
  - add deterministic regressions for positive and negative cases before the next guarded rerun;
  - do not change runtime owner logic unless deterministic governance closure proves insufficient.

## Reuse-first plan (mandatory)

- **Internal reuse:** `P1.6o23`, `P1.6o33`, `P1.6o62`, `P1.6o63`, `question_contract`, `pending_question_interaction`, timeout-degraded collect trace/meta, mixed-date scenario sanitize helpers.
- **External reuse:** official Rasa forms guidance for requested-slot unhappy paths and post-slot grounding resume semantics.
- **Why not reinvent the wheel:** runtime already emits the relevant contract evidence; the missing artifact is the canonical scenario/oracle rule for the timeout-degraded mixed-date row.

## Execution profile (mandatory for non-doc blocks)

- **TP mode:** `implementation`
- **Doc touch budget (files):** `7`
- **Code dominance:** `required`
- **Override token:** `none`
- **Why this profile fits:** the surfaced family is a bounded scenario/oracle block once `M13` is frozen; runtime changes are explicitly out of scope unless RCA disproves the governance reading.

## Release safety

- Strategy: deterministic scenario/oracle closure only; no runtime-core change unless RCA is falsified
- Go signals:
  - grounded partial-date availability turns under timeout-degraded collect no longer keep stale `reply_type=time` as the final expectation
  - trace/meta still preserve `slot_constraint(time)` and explicit timeout provenance
  - focused scenario regressions show pure `time` rows and active-name rows still diverge correctly
- No-go signals:
  - rewriting runtime owner logic before deterministic governance closure is tested
  - weakening oracle to accept contradictory `reply_type=time` / `reply_type=name` outcomes on the same turn
  - removing `slot_constraint` trace/meta from the canonical contract
- Rollback verification:
  - revert only the bounded `M13` scenario/oracle delta if adjacent mixed-date or timeout families regress

## Invariant

1. No runtime-core semantic changes in this block unless RCA is disproved.
2. `P1.6o23`, `P1.6o33`, `P1.6o62`, and `P1.6o63` must not regress.
3. Grounded `datetime` evidence and timeout provenance must stay explicit in `decision_trace` / `decision_meta`.
4. Fresh expensive iteration remains exactly one guarded rerun after deterministic closure.

## Scope

- publish the surfaced timeout-degraded mixed-date family as matrix row `M13`
- then implement the bounded scenario/oracle closure for that row only

## Out of scope

- runtime owner redesign in `decision.py`
- timeout budget tuning / latency mitigation
- acceptance `lock/replay`

## Touch-list

- `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o64-timeout-mixed-date-availability-governance-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o60b-remaining-closure-owner-matrix-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o60a-remaining-closure-architecture-verdict-a1.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`
- `scripts/booking_dialog_scenarios.py`
- `truffles-api/tests/test_booking_dialog_scenarios_script.py`

## Plan (1..N)

1. Freeze `r83` as audited non-closure evidence and publish `M13`.
2. Confirm that the first fail is contract-first scenario drift, not a runtime-owner reopen.
3. Normalize timeout-degraded mixed-date expectations from pre-grounding `time` to post-grounding resume `name`.
4. Add deterministic regressions for positive and negative cases.
5. Run one fresh guarded `dev L2` only after deterministic closure is green.

## DoD

1. `r83` is recorded in active docs as audited non-closure evidence.
2. Timeout-degraded mixed-date turns like `Есть ли свободные слоты на завтра?` no longer keep stale `reply_type=time` as the final expectation after `datetime` grounding.
3. Canonical expectation still preserves `pending_question_act=slot_constraint`, `pending_question_target=time`, and explicit timeout provenance.
4. Focused deterministic scenario regressions are green.
5. Fresh guarded `dev L2` no longer stops on the `r83` family.

## Expensive run control (mandatory)

- Hypothesis:
  - after the bounded `M13` governance fix, fresh guarded `dev L2` will no longer stop on dialog `1`, turn `2`, `Есть ли свободные слоты на завтра?`
- Expected measurable effect:
  - the next guarded run scores the turn against the post-grounding `name` resume contract instead of stale `reply_type=time`, so either the run proceeds past that turn or a different first blocker family surfaces
- Stop condition:
  - run exactly one fresh guarded `dev L2` plus strict audit; if the same contract drift persists or the run becomes preflight-invalid, stop the expensive loop and reopen RCA instead of chaining another rerun

## Checks

- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r83 --status done --strict-artifacts`
- `python3 - <<'PY'`
  `import json, pathlib`
  `run = pathlib.Path('/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r83')`
  `rows = [json.loads(line) for line in (run / 'responses.jsonl').read_text().splitlines()]`
  `row = rows[1]`
  `print(row['turn_text'])`
  `print(row['decision_meta']['pending_question_interaction'], row['decision_meta']['pending_question_target'])`
  `print(row['decision_meta']['expected_reply_type'], row['decision_meta']['expected_reply_reason'])`
  `print(row['judge']['verdict'], row['evaluation']['strict_reasons'])`
  `PY`
- `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "tomorrow or mixed_date or slot_constraint or policy_core_degraded_collect"`
- `git diff --check`

## Execution status

- `done`
- `r83` RCA is frozen and audited
- deterministic `M13` closure is green via bounded oracle/evaluator contract update
- fresh guarded `dev L2` run `p1.6o6-l2-dev-20260311-a1-r90` no longer stopped on `r83/M13`; the first fail rolled to dialog `1`, turn `4`, `Можно к мастеру Асем?`

## Evidence

1. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r83/summary.json`
2. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r83/brief.md`
3. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r83/scenarios.json`
4. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r83/responses.jsonl`
5. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r83/trace_bundle.jsonl`
6. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r83/manual_audit.md`
7. `ops/diagnose.py`
8. `truffles-api/tests/test_booking_quality_response_guard.py`
9. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r90/summary.json`
10. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r90/brief.md`
11. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r90/scenarios.json`
12. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r90/responses.jsonl`
13. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r90/trace_bundle.jsonl`
14. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r90/manual_audit.md`

## Rollback

1. Keep `P1.6o6` blocked.
2. Revert only the future bounded `M13` scenario/oracle delta if it regresses adjacent mixed-date rows.

## No-go

- no runtime-core edits unless deterministic governance closure fails
- no oracle weakening to accept contradictory pre-grounding / post-grounding contracts
- no fresh guarded rerun before deterministic scenario regression is green

## Risks/Blockers

1. Runtime latency / timeout pressure remains a separate infra risk and may surface again after governance closure.
2. The new normalization may be too broad and accidentally erase legitimate active-`time` rows if negative tests are weak.
3. `r90` is still non-canonical proof evidence (`infra_valid=false`, `semantic_valid=false`), so this block can only claim bounded closure plus rerun rollover.

## Residual architecture debt (mandatory)

- Current residuals accepted in this block:
  - invalid-schema specialist-followup recovery remains outside this bounded governance/oracle row fix
- Why not in this block:
  - this slice only closes one timeout-degraded mixed-date governance row
- Risk if deferred:
  - future guarded runs can progress past `r83` only to rediscover a runtime degrade gap on named specialist follow-up
- Linked follow-up Task Package(s):
  - `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`
- Expiry/trigger to stop deferral:
  - before the next guarded rerun beyond `r90`

## Next-block contract (mandatory)

- Next block objective:
  - implement `M14`, then run one fresh guarded `dev L2` so `r90` is no longer the first surfaced family after `M13` closure
- First deterministic check command:
  - `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r90 --status done --strict-artifacts && git diff --check`
- Blocked-by conditions:
  - `M14` is not published in the owner matrix
  - invalid-schema specialist-followup deterministic regressions are not green
  - acceptance tool-evidence infra remains red
- Owner role for closure:
  - `Hands` for bounded implementation, `Top Architect + Brain` for matrix/program/proof sync

# TP-2026-03-12-p1.6o103-grounded-partial-date-daypart-fill-governance-a1

- Название/цель: закрыть surfaced `M36` scenario-governance drift, где после proof lane `P1.6o102` grounded partial-date + daypart availability turn `Мне нужна информация о свободных слотах на утро.` всё ещё живёт как `mixed_fill_plus_question` / `expected_reply_type=time`, хотя runtime канонически склеивает `завтра` + `утром` в bounded `datetime` fill и двигает collect на `name`.

## Canon refs

- Parent closure program: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Main remediation: `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- Historical partial-date normalization: `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o33-partial-date-mixed-question-normalization-a1.md`
- Historical question-like daypart normalization: `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o57-question-like-daypart-fill-trace-normalization-a1.md`
- Proof lane that surfaced the row: `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o102-post-m35-proof-lane-a1.md`
- State + matrix baseline: `STATE.md`, `truffles-api/app/knowledge/generic/INTERACTION_OWNER_MATRIX.yaml`

## Git context

- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect` remove branch + worktree after merge

## One web search (mandatory before implementation)

- Query: `site:rasa.com/docs/rasa/forms requested slot partial date follow up question daypart`
- Date/time: `2026-03-12T18:00:00+05:00`
- Sources opened:
  - official Rasa forms docs: `https://rasa.com/docs/rasa/forms/`
- Source quality:
  - official product documentation
- Found reusable ideas:
  - once a slot is already active, follow-up user language should be interpreted relative to the active slot state rather than as a fresh question family;
  - partial slot grounding can stay active across follow-up turns, so the next user turn may complete the same slot instead of reopening slot guidance.
- Decision:
  - `reuse + integrate`
  - reuse the existing partial-date/daypart normalization helpers in `scripts/booking_dialog_scenarios.py` and integrate one bounded stateful rule that preserves a partial-date anchor across active `time` collect, then collapses the grounded daypart follow-up to canonical `time -> name`.
- Rejected options:
  - weakening oracle to accept both `mixed_fill_plus_question/time` and `time/name` on the same turn;
  - runtime patching to imitate stale generator governance;
  - broad scenario-ontology redesign in this block.

## Root cause (mandatory)

- Symptom:
  - fresh guarded `dev L2` `p1.6o102-l2-dev-20260312-a1-r1` no longer stops on `M35`, but now fails on dialog `1`, turn `5`, text `Мне нужна информация о свободных слотах на утро.` with `expected_meta_mismatch` because generated expectations keep `mixed_fill_plus_question` / `expected_reply_type=time` while runtime canonically advances to `name`.
- Minimal reproduction:
  1. Inspect `/tmp/booking_quality/p1.6o102-l2-dev-20260312-a1-r1/responses.jsonl` dialog `1`, turns `4-5`.
  2. Confirm turn `4` grounds a partial date (`answer_detected_slot=datetime`, `answer_value=завтра`) while keeping active `time` collect.
  3. Confirm turn `5` text is `Мне нужна информация о свободных слотах на утро.` and runtime grounds `answer_value=утром`, then emits `expected_reply_type=name` and `Отлично, время подходит. Как вас зовут?`.
  4. Inspect `/tmp/booking_quality/p1.6o102-l2-dev-20260312-a1-r1/scenarios.json` and confirm the same turn still carries `mixed_fill_plus_question` with `reply_type=time` / `expected_reply_type=time`.
  5. Inspect `scripts/booking_dialog_scenarios.py` and confirm current normalization tracks active `reply_type`, but does not persist a partial-date anchor that would let the next daypart follow-up collapse into canonical `time -> name`.
- Evidence:
  - `/tmp/booking_quality/p1.6o102-l2-dev-20260312-a1-r1/summary.json`
  - `/tmp/booking_quality/p1.6o102-l2-dev-20260312-a1-r1/brief.md`
  - `/tmp/booking_quality/p1.6o102-l2-dev-20260312-a1-r1/scenarios.json`
  - `/tmp/booking_quality/p1.6o102-l2-dev-20260312-a1-r1/responses.jsonl`
  - `/tmp/booking_quality/p1.6o102-l2-dev-20260312-a1-r1/trace_bundle.jsonl`
  - `scripts/booking_dialog_scenarios.py`
  - `truffles-api/tests/test_booking_dialog_scenarios_script.py`
- Five Whys:
  1. Why did the run fail? Because scenario meta still expected `expected_reply_type=time` on the morning-slots follow-up.
  2. Why did scenario expect that? Because the turn stayed tagged as `mixed_fill_plus_question` even after the prior turn had already grounded the date.
  3. Why was it not normalized away? Because current normalization has no stateful partial-date anchor that survives into the next active-`time` turn.
  4. Why is runtime already ahead? Because policy/runtime treat `завтра` + `утром` as one bounded `datetime` fill and advance collect to `name`.
  5. Why is this a bounded scenario-governance row? Because runtime/judge are already aligned; the remaining defect is stale generated expectation state.
- Root cause statement:
  - the scenario generator lacks a bounded stateful normalization that remembers a grounded partial date under active `time` collect and then collapses the next daypart availability follow-up out of `mixed_fill_plus_question/time` into canonical `time -> name`.
- Fix mechanism:
  - add a bounded partial-date-anchor context in sanitize/repair flows;
  - retag grounded daypart availability follow-ups from `mixed_fill_plus_question` to `time` only when a partial-date anchor is active;
  - update expectations to `expected_reply_type=name` and remove stale pending-question evidence;
  - add targeted sanitize/repair regressions and sync matrix/docs/state truthfully.

## Reuse-first decision (mandatory)

- Reuse:
  - existing partial-date/daypart helper patterns in `scripts/booking_dialog_scenarios.py`
  - existing sanitize/repair deterministic test harness in `truffles-api/tests/test_booking_dialog_scenarios_script.py`
- Integrate:
  - one bounded partial-date-anchor state machine for scenario-governance only
  - one bounded `mixed_fill_plus_question -> time` rewrite when the anchor is active
- Build:
  - no runtime code changes planned in this block

## Release safety

- Rollout: deterministic scenario-governance only; no production runtime change
- Go signal:
  - grounded partial-date + daypart availability follow-ups normalize to canonical `time -> name`
  - the same surface without a partial-date anchor remains `mixed_fill_plus_question`
  - matrix/docs/state/session stay truthful and hygiene gates remain green
- No-go signal:
  - generic mixed availability questions without grounded partial date collapse to `name`
  - runtime files become necessary in this block
- Rollback:
  - revert only the new partial-date-anchor normalization and matching tests/docs

## Invariant

1. `M35` stays cleared by `P1.6o101` + `P1.6o102`.
2. Runtime behavior is untouched in this block.
3. Mixed availability questions without grounded partial-date context remain `mixed_fill_plus_question`.
4. No oracle weakening.

## Scope

- bounded scenario-governance closure for grounded partial-date daypart follow-up overclaim
- deterministic tests
- matrix/docs/state/session sync

## Out of scope

- new guarded `dev L2`
- acceptance `lock/replay`
- runtime semantic changes

## Touch-list

- `scripts/booking_dialog_scenarios.py`
- `truffles-api/tests/test_booking_dialog_scenarios_script.py`
- `truffles-api/app/knowledge/generic/INTERACTION_OWNER_MATRIX.yaml`
- `truffles-api/tests/test_interaction_owner_matrix_contract.py`
- `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o103-grounded-partial-date-daypart-fill-governance-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o102-post-m35-proof-lane-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o104-post-m36-proof-lane-a1.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `STATE.md`
- `STRUCTURE.md`
- `docs/SESSIONS/SESSION-2026-02-19-llm-first-firebreak-a1.md`

## Plan (1..N)

1. Publish `P1.6o103` with RCA + one web search + bounded touch-list.
2. Add the partial-date-anchor-aware normalization in sanitize/repair flows.
3. Add deterministic sanitize/repair regressions plus matrix contract update.
4. Run targeted deterministic checks and hygiene gates.
5. Sync active docs/state/session with factual deterministic closure and publish the next admissible proof block.

## DoD

1. `Мне нужна информация о свободных слотах на утро.` after a grounded partial-date turn canonicalizes to `time -> name`.
2. Generated expectations preserve `expected_reply_type=name` and drop stale pending-question evidence on that family.
3. The same surface without a partial-date anchor remains `mixed_fill_plus_question` / `time`.
4. Deterministic tests are green.
5. `M36` is truthfully synced in matrix/docs/state with the next admissible proof block identified.

## Checks

- `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "grounded_partial_date_daypart or mixed_daypart_question_without_grounded_partial_date"`
- `pytest -q truffles-api/tests/test_interaction_owner_matrix_contract.py`
- `python3 -m py_compile scripts/booking_dialog_scenarios.py truffles-api/tests/test_booking_dialog_scenarios_script.py truffles-api/tests/test_interaction_owner_matrix_contract.py`
- `git diff --check`
- `SESSION_AGENT=a1 scripts/session_check.sh`

## Execution status

- `done`

## Evidence

1. `/tmp/booking_quality/p1.6o102-l2-dev-20260312-a1-r1/summary.json`
2. `/tmp/booking_quality/p1.6o102-l2-dev-20260312-a1-r1/brief.md`
3. `/tmp/booking_quality/p1.6o102-l2-dev-20260312-a1-r1/scenarios.json`
4. `/tmp/booking_quality/p1.6o102-l2-dev-20260312-a1-r1/responses.jsonl`
5. `/tmp/booking_quality/p1.6o102-l2-dev-20260312-a1-r1/trace_bundle.jsonl`
6. `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "grounded_partial_date_daypart or mixed_daypart_question_without_grounded_partial_date"` -> `3 passed, 100 deselected`
7. `pytest -q truffles-api/tests/test_interaction_owner_matrix_contract.py`
8. `python3 -m py_compile scripts/booking_dialog_scenarios.py truffles-api/tests/test_booking_dialog_scenarios_script.py truffles-api/tests/test_interaction_owner_matrix_contract.py`
9. `git diff --check`
10. `SESSION_AGENT=a1 scripts/session_check.sh`
11. updated matrix/docs/state/session references

## Rollback

1. Revert only the new partial-date-anchor normalization and matching deterministic docs/tests.
2. Keep `P1.6o102` proof evidence and `M35` closure intact.
3. Do not reinterpret `M36` as runtime defect if deterministic closure is rolled back.

## No-go

- no runtime edits
- no new expensive run in this block
- no weakening the `mixed_fill_plus_question` family globally
- no phrase hardcode for one exact sentence only

## Risks / blockers

- grounded daypart follow-ups sit close to genuine mixed availability questions, so the bounded rule must stay conditional on the active partial-date anchor.
- repair flow and sanitize flow must stay aligned; otherwise orphan post-coverage repair will reintroduce stale expectations.

## Residual architecture debt (mandatory)

### Current residuals accepted in this block

- Scenario ontology still relies on localized stateful normalization heuristics for some pending-question cross-products.
- Acceptance/proof lane remains blocked until a fresh rerun confirms `M36` is gone.

### Why not in this block

- This block is a bounded scenario-governance repair, not a broader scenario-ontology redesign.

### Risk if deferred

- New expensive runs will keep stopping on false-red grounded daypart follow-up governance and hide later remaining families.

### Linked follow-up Task Package(s)

- `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o104-post-m36-proof-lane-a1.md`

### Expiry/trigger to stop deferral

- before any next guarded `dev L2` / `lock` / `replay`

## Next-block contract (mandatory)

### Next block objective

- publish and run `P1.6o104`: execute one fresh guarded `dev L2` after `P1.6o103` deterministic closure to prove grounded partial-date daypart row `M36` is no longer first fail and truthfully surface the next family, if any.

### First deterministic check command

- `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "grounded_partial_date_daypart or mixed_daypart_question_without_grounded_partial_date" && pytest -q truffles-api/tests/test_interaction_owner_matrix_contract.py && git diff --check`

### Blocked-by conditions

- `P1.6o103` deterministic closure is not synced truthfully into active docs and matrix state
- grounded partial-date daypart deterministic reproduction is not green
- live runtime fingerprint for the proof lane is not proven against current `HEAD`

### Owner role for closure

- `Top Architect + Brain`

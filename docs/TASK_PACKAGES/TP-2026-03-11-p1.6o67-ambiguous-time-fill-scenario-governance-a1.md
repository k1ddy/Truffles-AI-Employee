# TP-2026-03-11-p1.6o67-ambiguous-time-fill-scenario-governance-a1

- Название/цель: закрыть surfaced `r92` scenario-governance blocker family, где lower-bound time preference `Мне нужно в 14:00 или позже.` под активным `expected_reply_type=time` overclaimed as canonical exact-time fill, после чего generated scenario ошибочно переводит resume contract в `name`, а следующий pricing/info interrupt падает как expectation drift.

## Canon refs

- Parent verdict: `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o60a-remaining-closure-architecture-verdict-a1.md`
- Owner matrix: `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o60b-remaining-closure-owner-matrix-a1.md`
- Program TP: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Proof bundle: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`
- Prior child: `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o66-policy-core-named-specialist-followup-owner-a1.md`
- STATE: `STATE.md`
- CA: remaining closure row after `M15`

## One web search (mandatory before implementation)

- Query: `Rasa forms slot validation requested slot official docs`
- Date/time: `2026-03-11T14:18:00+05:00`
- Sources opened:
  - official Rasa docs: `https://rasa.com/docs/rasa/forms/`
- Source quality:
  - official product documentation
- Found reusable ideas:
  - ambiguous slot answers must keep the requested slot open until validation/clarification completes;
  - side questions after an ambiguous slot answer must inherit the still-open requested-slot contract, not a prematurely advanced next slot.
- Decision:
  - `reuse/integrate`
  - keep the existing runtime confirmation contract; repair only scenario normalization/expectation so ambiguous lower-bound time phrases do not overclaim exact-time closure.
- Rejected options:
  - runtime override to force `name` progression for `14:00 или позже`, because runtime already emits `slot_confirmation_required=true` and keeps `expected_reply_type=time`;
  - oracle weakening on downstream pricing turn, because the stale `name` expectation is the defect.

## Root cause (mandatory)

- Symptom:
  - fresh guarded `dev L2` run `p1.6o6-l2-dev-20260311-a1-r92` stops at dialog `1`, turn `5`, user text `Какова цена маникюра?`
- Minimal reproduction:
  1. Inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r92/scenarios.json` dialog `1`, turns `3-5`.
  2. Observe turn `3` text `Мне нужно в 14:00 или позже.` tagged as `time`, so scenario advances `reply_type=name`.
  3. Inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r92/responses.jsonl` and `trace_bundle.jsonl` for turn `3`.
  4. Confirm runtime matches `datetime=14:00` with `slot_confirmation_required=true`, then keeps `expected_reply_type=time` through `booking_confirm_reject`.
  5. Inspect turn `5` and confirm pricing interrupt preserves runtime `expected_reply_type=time`, while scenario still expects `name` and fails on `expected_meta_mismatch` / `expected_trace_miss`.
- Evidence:
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r92/summary.json`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r92/brief.md`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r92/scenarios.json`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r92/responses.jsonl`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r92/trace_bundle.jsonl`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r92/manual_audit.md`
- Five Whys:
  1. Why did turn `5` fail? Scenario expected downstream resume `name`, but runtime correctly kept `time` across the pricing interrupt.
  2. Why did scenario expect `name`? Turn `3` was normalized as canonical exact-time fill.
  3. Why was turn `3` overclaimed? Scenario heuristics treat any explicit `HH:MM` mention as grounded time fill.
  4. Why is that wrong here? `14:00 или позже` is a lower-bound preference, not a single confirmed slot; runtime keeps `slot_confirmation_required=true` and does not advance collect.
  5. Why is this a new bounded family? Existing time-fill normalizers cover exact-time fills and mixed slot questions, but not ambiguous lower-bound/comparative time phrases under active time collect.
- Root cause statement:
  - `scripts/booking_dialog_scenarios.py` overclaims lower-bound/comparative time phrases containing an explicit clock token as canonical `time` fills, so scenario governance advances the resume contract to `name` even when runtime correctly keeps `time` pending behind confirmation/clarification.
- Fix mechanism:
  - freeze matrix row `M16` for ambiguous lower-bound time fill under active `time` collect;
  - add bounded scenario normalization/expectation override so phrases like `14:00 или позже` keep `reply_type=time` and do not seed downstream stale `name` expectations;
  - add deterministic scenario regressions for the direct turn and the downstream pricing/info interrupt chain.

## Reuse-first decision (mandatory)

- Reuse:
  - existing runtime booking confirmation contract
  - existing scenario pending-question expectation overrides
- Integrate:
  - ambiguous lower-bound time detection into `scripts/booking_dialog_scenarios.py`
- Do not build:
  - new runtime override
  - new oracle waiver

## Invariant

1. `P1.6o66/M15` remains closed.
2. Exact grounded time fills under active `time` collect still normalize to `reply_type=name`.
3. Existing slot-constraint and mixed-slot-question scenario closures do not regress.
4. No runtime code changes in this block.

## Scope

- publish `r92` as scenario-governance row `M16`
- repair generated scenario expectations for ambiguous lower-bound/comparative time phrases under active `time` collect

## Out of scope

- runtime booking confirmation behavior
- acceptance `lock/replay`
- infra/tool-evidence issues in `r92`

## Touch-list

- `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o67-ambiguous-time-fill-scenario-governance-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o60b-remaining-closure-owner-matrix-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o60a-remaining-closure-architecture-verdict-a1.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`
- `scripts/booking_dialog_scenarios.py`
- `truffles-api/tests/test_booking_dialog_scenarios_script.py`

## Plan (1..N)

1. Freeze `r92` as audited non-closure evidence and publish `M16`.
2. Add bounded scenario detection for ambiguous lower-bound/comparative time phrases under active `time` collect.
3. Keep the resume contract at `time` for those turns and scrub stale downstream `name` expectations.
4. Add deterministic scenario regressions for the direct turn and the pricing/info follow-up chain.
5. Run targeted deterministic checks and `git diff --check`.
6. Only after deterministic green, decide whether the next fresh guarded `dev L2` is justified.

## DoD

1. `r92` is recorded in active docs as audited non-closure evidence.
2. `Мне нужно в 14:00 или позже.` under active `time` collect no longer advances scenario `reply_type` to `name`.
3. Downstream generic pricing/info interruptions inherit `reply_type=time` after that turn.
4. Targeted scenario regressions are green.
5. No runtime files changed.

## Expensive run control (mandatory)

- Hypothesis:
  - after the bounded `M16` scenario-governance repair, ambiguous lower-bound time phrases will keep the scenario resume contract at `time`, removing the downstream stale `name` expectation that surfaced at `r92`.
- Expected measurable effect:
  - deterministic scenario tests go green without changing runtime traces or runtime owners.
- Stop condition:
  - do not run a new guarded rerun until the new deterministic scenario tests and adjacent regressions are green.

## Checks

- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r92 --status done --strict-artifacts`
- `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "lower_bound or slot_constraint or grounded_time or info_interrupt"`
- `git diff --check`

## Execution status

- `done`
- bounded deterministic `M16` scenario-governance regressions are green
- fresh guarded `dev L2` `p1.6o6-l2-dev-20260311-a1-r93` no longer stopped on the `r92/M16` family and rolled to a different invalid-schema degraded-booking blocker

## Evidence

1. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r92/summary.json`
2. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r92/brief.md`
3. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r92/scenarios.json`
4. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r92/responses.jsonl`
5. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r92/trace_bundle.jsonl`
6. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r92/manual_audit.md`

## Rollback

1. Revert only the bounded scenario normalization/test delta if it regresses exact time-fill governance.
2. Keep `P1.6o6` blocked; do not reinterpret `r92` as runtime closure evidence.

## No-go

- no runtime behavior change
- no oracle weakening
- no reinterpretation of `r92` as `M15` reopen
- no forced `name` progression for ambiguous time preferences

## Risks/Blockers

1. Other comparative time phrases (`не раньше`, `после 15:00`) may surface after the first bounded repair.
2. `r92` remains non-canonical proof evidence (`infra_valid=false`, `semantic_valid=false`), so this block can only claim bounded scenario closure plus rerun rollover.

## Residual architecture debt (mandatory)

- Current residuals accepted in this block:
  - `M17` surfaced after bounded `M16` closure.
- Why not in this block:
  - `r93` surfaced only after the next fresh guarded `dev L2` beyond `M16`.
- Risk if deferred:
  - explicit service-grounded booking turns can still collapse back to `service_choice` on invalid-schema degrade and hide later slot/availability families.
- Linked follow-up Task Package(s):
  - `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o68-invalid-schema-service-grounded-booking-owner-a1.md`
- Expiry/trigger to stop deferral:
  - before the next expensive rerun beyond `r93`.

## Next-block contract (mandatory)

- Next block objective:
  - implement `M17`, then rerun deterministic runtime checks before any new guarded `dev L2`.
- First deterministic check command:
  - `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r93 --status done --strict-artifacts && git diff --check`
- Blocked-by conditions:
  - `M17` is not published in the owner matrix
  - invalid-schema service-grounded regressions are not green
  - `r93` evidence is not synced in tracker docs
- Owner role for closure:
  - `Hands` for bounded implementation, `Top Architect + Brain` for matrix/program/proof sync

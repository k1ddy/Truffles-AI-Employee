# TP-2026-03-13-p1.6o120-post-track-c-proof-lane-a1

- Название/цель: после deterministic closure `P1.6o119` выполнить один fresh guarded `dev L2`, чтобы доказать, что surfaced `Class C` timeout/degrade owner-boundary failure больше не является first fail, и truthfully surface-ить следующий remaining class, если он есть.

## Canon refs

- Parent closure program: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Main remediation: `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- Process reset / class triage: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o118-class-level-closure-process-reset-a1.md`
- Prior proof lane: `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o117-post-m21-reclosure-proof-lane-a1.md`
- Deterministic closure prerequisite: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o119-timeout-degrade-owner-boundary-first-slice-a1.md`
- Machine-readable owner matrix: `truffles-api/app/knowledge/generic/INTERACTION_OWNER_MATRIX.yaml`
- State baseline: `STATE.md`

## Git context

- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `origin/main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect` remove branch + worktree after merge

## One web search (mandatory before implementation)

- Query: `site:www.uvicorn.org/settings uvicorn host port admin version runtime fingerprint`
- Date/time: `reused official proof-lane reference from P1.6o117 (2026-03-12T23:30:00+05:00)`
- Sources opened:
  - official Uvicorn settings docs: `https://www.uvicorn.org/settings/`
- Source quality:
  - official product documentation
- Found reusable ideas:
  - proof runtime startup should stay explicit and reproducible via CLI flags;
  - fingerprint validation must stay external to the app process and be recorded before the guarded run starts.
- Decision:
  - `reuse`
  - reuse the established live proof-run startup pattern on `:18184` and record `/admin/version == HEAD` before the single guarded run.
- Rejected options:
  - detached startup patterns that hide runtime parity;
  - additional expensive runs in the same block.

## Root cause (mandatory)

- Symptom:
  - `P1.6o119` is green on deterministic contour, but there is still no fresh proof run showing that timeout/degrade owner-boundary is no longer the first surfaced family.
- Minimal reproduction:
  1. Use `/tmp/booking_quality/p1.6o117-l2-dev-20260312-a1-r1` as the pre-fix proof baseline.
  2. Confirm it surfaced `dialog 1 / turn 12 / "Меня зовут Динара."` with `question_contract` already matched, `policy_core_mode=degraded_fallback`, and `policy_core_guard_recovery=pack_fact_fallback`.
  3. Confirm `P1.6o119` now deterministically routes the same covered family through shared timeout owner-boundary collect recovery before degraded fact fallback.
  4. Run one fresh guarded `dev L2` on a live runtime whose `/admin/version` matches current `HEAD`.
  5. Audit the resulting run strictly and classify the new first fail through `P1.6o118` rules.
- Evidence:
  - `/tmp/booking_quality/p1.6o117-l2-dev-20260312-a1-r1/summary.json`
  - `/tmp/booking_quality/p1.6o117-l2-dev-20260312-a1-r1/brief.md`
  - `/tmp/booking_quality/p1.6o117-l2-dev-20260312-a1-r1/responses.jsonl`
  - `/tmp/booking_quality/p1.6o117-l2-dev-20260312-a1-r1/trace_bundle.jsonl`
  - `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o119-timeout-degrade-owner-boundary-first-slice-a1.md`
- Five Whys:
  1. Why is closure still incomplete? Because deterministic green alone does not prove the expensive lane rolled past the covered `Class C` family.
  2. Why is a proof lane needed now? Because the process accepts progress only when a fresh guarded run confirms the previous first-fail family is no longer first.
  3. Why can we run it now? Because `P1.6o119` landed the first structural timeout owner-boundary slice and targeted deterministic checks are green.
  4. Why must runtime be fingerprinted first? Because runtime/preflight mismatch makes the run non-canonical.
  5. Why only one run? Because the goal is truthful admission/classification of the next surfaced result, not another expensive rerun loop.
- Root cause statement:
  - the remaining gap after `P1.6o119` is proof, not local implementation: without one fresh guarded `dev L2` on a verified live runtime, `Class C` remains only deterministically closed and the next remaining class stays unproven.
- Fix mechanism:
  - start a live worktree runtime on `:18184` and prove `/admin/version == HEAD`;
  - execute exactly one guarded `dev L2` run and strict audit;
  - sync docs/state/session truthfully based on the new first-fail outcome, applying `P1.6o118` class triage instead of automatic row admission.

## Reuse-first decision (mandatory)

- Reuse:
  - existing guarded entrypoint `scripts/llm_quality_guarded.sh`
  - existing audit command `python3 ops/diagnose.py llm-quality-audit`
  - existing live runtime startup pattern used in prior proof-lane blocks
- Integrate:
  - no runtime code changes unless the proof run surfaces a new deterministic or bounded family
- Build:
  - none planned in this block

## Release safety

- Rollout: none; local proof-only block
- Go signal:
  - runtime fingerprint matches `HEAD`
  - guarded run artifacts are valid enough to audit (`artifact_integrity.valid=true`)
  - first fail is no longer the covered `Class C` timeout owner-boundary family
- No-go signal:
  - invalid runtime fingerprint preflight
  - incomplete/invalid guarded artifacts
  - fresh first fail remains the same covered `Class C` family
- Rollback:
  - stop the proof lane, record evidence truthfully, do not reinterpret invalid/incomplete runs as closure evidence

## Invariant

1. `P1.6o119` deterministic closure stays intact.
2. Targeted timeout owner-boundary deterministic regressions remain green.
3. No runtime/product contract weakening.
4. Exactly one fresh guarded `dev L2` run in this block.

## Scope

- proof-lane runtime startup + fingerprint validation
- one guarded `dev L2`
- strict audit
- doc/state/session sync for the resulting admission

## Out of scope

- new implementation before the proof run surfaces a new class or proves broader `Class C` residual
- acceptance `lock/replay/full`
- broad timeout/degrade migration beyond `P1.6o119`

## Touch-list

- `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o120-post-track-c-proof-lane-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o119-timeout-degrade-owner-boundary-first-slice-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o118-class-level-closure-process-reset-a1.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `STATE.md`
- `STRUCTURE.md`
- `docs/SESSIONS/SESSION-2026-02-19-llm-first-firebreak-a1.md`
- `truffles-api/app/knowledge/generic/INTERACTION_OWNER_MATRIX.yaml`
- `truffles-api/tests/test_interaction_owner_matrix_contract.py`

## Plan (1..N)

1. Publish `P1.6o120` with proof-lane scope and runtime-fingerprint requirements.
2. Re-run the deterministic prerequisite from `P1.6o119`.
3. Start live worktree runtime on `:18184` and prove `/admin/version == HEAD`.
4. Execute exactly one fresh guarded `dev L2` and strict audit.
5. Sync matrix/docs/state/session with truthful post-run admission/classification.

## DoD

1. Fresh guarded `dev L2` is executed on a fingerprint-verified live runtime.
2. Covered `Class C` timeout owner-boundary family is no longer the first fail.
3. The next first fail, if any, is captured truthfully and classified by class, not auto-admitted as a new row.
4. Docs/state/session are synced to the new admission truth.

## Checks

- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o117-l2-dev-20260312-a1-r1 --status done --strict-artifacts`
- `pytest -q truffles-api/tests/test_message_endpoint.py -k "timeout_booking_slot_fill_followup or timeout_pending_slot_question or timeout_specialist_followup or timeout_owner_boundary"`
- `pytest -q truffles-api/tests/test_booking_quality_response_guard.py -k "timeout_booking_slot_fill_followup or timeout_owner_boundary"`
- `pytest -q truffles-api/tests/test_owner_resolver.py`
- `pytest -q truffles-api/tests/test_interaction_owner_matrix_contract.py`
- `TEST_MODE=1 scripts/llm_quality_guarded.sh --mode lock --run-id p1.6o120-l2-dev-20260313-a1-r1 --allow-pending-previous --allow-repeat-fingerprint -- --base-url http://127.0.0.1:18184 --client-slug demo_salon --mode llm --count 10 --min-turns 10 --max-turns 15 --include-media --scenario-coverage booking,info,interrupt,handoff --tool-hooks auto --jid-mode unique --allow-non-allowlist --judge-mode all --quality-lane dev --run-economy-gate block --fail-on-thresholds --max-failures 1 --reset-before-dialog`
- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o120-l2-dev-20260313-a1-r1 --status done --strict-artifacts`
- `git diff --check`
- `SESSION_AGENT=a1 scripts/session_check.sh`

## Execution status

- `done`
- refreshed the live worktree runtime on `127.0.0.1:18184`, verified `/admin/version.git_commit == 21afcead084aa3fdfc5702c5547fb40d74970b5a` (`HEAD`), executed exactly one guarded `dev L2` run `p1.6o120-l2-dev-20260313-a1-r1`, audited it strictly, and stopped the local runtime
- the covered `Class C` timeout/degrade owner-boundary family is no longer the first surfaced fail
- the run remains non-canonical for closure: `artifact_integrity.valid=true`, `infra_valid=false`, `semantic_valid=false`, `stop_reason=max_failures_reached:1`
- the new first surfaced result is structural `Track A` expectation/oracle drift on `dialog 1 / turn 2 / "Какие дни у вас доступны?"`
  - generated scenario expects `tags=["slot_compare"]`, `meta_any.pending_question_act=["slot_compare"]`, and `trace_contains.pending_question_act="slot_compare"`
  - runtime canonically preserves `decision_meta.source="booking_slot_guidance"` with `pending_question_act="ask_about_requested_slot"`, `pending_question_target="time"`, `active_question_relation="ask_about_requested_slot"`, and `expected_reply_type="time"`
- under `P1.6o118`, the next admissible block is structural `Track A`, published as `P1.6o121`, not another timeout patch or row-local child

## Evidence

1. `/tmp/booking_quality/p1.6o120-l2-dev-20260313-a1-r1/summary.json`
2. `/tmp/booking_quality/p1.6o120-l2-dev-20260313-a1-r1/brief.md`
3. `/tmp/booking_quality/p1.6o120-l2-dev-20260313-a1-r1/scenarios.json`
4. `/tmp/booking_quality/p1.6o120-l2-dev-20260313-a1-r1/responses.jsonl`
5. `/tmp/booking_quality/p1.6o120-l2-dev-20260313-a1-r1/trace_bundle.jsonl`
6. `/tmp/booking_quality/p1.6o120-l2-dev-20260313-a1-r1/manual_audit.md`
7. `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o119-timeout-degrade-owner-boundary-first-slice-a1.md`
8. `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o121-booking-slot-guidance-scenario-convergence-first-slice-a1.md`

## Rollback

1. Stop the local runtime and keep `P1.6o119` deterministic closure intact.
2. If the run is invalid/incomplete, record it truthfully and keep `P1.6o120` open.
3. Do not mutate the matrix or docs to hide surfaced evidence.

## No-go

- no second expensive run in this block
- no new implementation before the proof run surfaces the next class
- no reuse of invalid preflight evidence as closure proof
- no automatic `Mxx -> child TP` admission without `P1.6o118` class triage

## Risks / blockers

- local runtime may fail fingerprint validation or die before the guarded run starts
- the next surfaced result may still belong to broader `Class C`, which must open a structural continuation rather than a new row
- the run may remain infra-invalid, which blocks closure admission even if the first fail moves

## Residual architecture debt (mandatory)

### Current residuals accepted in this block

- timeout/degrade migration is still only the first bounded slice from `P1.6o119`
- scenario-governance/oracle still duplicates runtime truth on separate families
- acceptance proof (`lock/replay/full`) remains blocked

### Why not in this block

- this block is proof-only; it verifies whether the first structural `Class C` slice moved the expensive lane

### Risk if deferred

- the team can misread deterministic green as closure and skip the only admissible proof step

### Linked follow-up Task Package(s)

- `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o121-booking-slot-guidance-scenario-convergence-first-slice-a1.md`
- `P1.7` acceptance replay gate after closure-program proof is admissible

### Expiry/trigger to stop deferral

- before any further guarded `dev/acceptance` rerun beyond `P1.6o120`

## Next-block contract (mandatory)

### Next block objective

- execute `P1.6o121`, the first structural `Track A` scenario-governance convergence slice for the surfaced `slot_compare -> ask_about_requested_slot(time)` oracle drift under active booking slot-guidance.

### First deterministic check command

- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o120-l2-dev-20260313-a1-r1 --status done --strict-artifacts && pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py && pytest -q truffles-api/tests/test_interaction_owner_matrix_contract.py && git diff --check`

### Blocked-by conditions

- `P1.6o120` surfaced `Track A` evidence is not synced truthfully in active docs/session
- the first structural `Track A` block is not published
- targeted scenario-generator contract tests are not green

### Owner role for closure

- `Top Architect + Brain`

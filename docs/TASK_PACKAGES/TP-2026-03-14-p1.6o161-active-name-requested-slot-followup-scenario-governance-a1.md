# TP-2026-03-14-p1.6o161-active-name-requested-slot-followup-scenario-governance-a1

- Название/цель: закрыть surfaced `P1.6o160` scenario-governance drift, где active `name` collect + generic requested-slot followup (`"На какое время вы можете меня записать?"`) ошибочно деградирует в orphan `service_choice`, хотя runtime канонически держит `booking_time_availability_followup -> expected_reply_type=name`.

## Canon refs

- Parent closure program: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Main remediation: `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- Binding roadmap: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o135-beauty-salon-canonical-control-plane-roadmap-a1.md`
- Prior proof lane: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o160-post-p1.6o159-proof-lane-a1.md`
- Class routing: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o118-class-level-closure-process-reset-a1.md`
- State baseline: `STATE.md`

## Git context

- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `origin/main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect` remove branch + worktree after merge

## One web search (mandatory before implementation)

- Query: `site:developers.openai.com/api/docs/guides/evaluation-best-practices evaluation best practices use-case-specific evals`
- Date/time: `2026-03-14T09:25:00+05:00`
- Sources opened:
  - OpenAI docs — Evaluation best practices: `https://developers.openai.com/api/docs/guides/evaluation-best-practices`
- Source quality:
  - official vendor documentation
- Found reusable ideas:
  - use-case-specific evals should grade the contract that matters for the scenario, not a generic text pattern;
  - evaluation drift should be fixed by aligning the oracle with the real task outcome, not by weakening runtime evidence.
- Decision:
  - `reuse`
  - reuse the existing scenario-governance compiler path and narrow one expectation rule so active-name requested-slot followups keep the canonical `name` contract instead of orphan fallback.
- Rejected options:
  - weakening oracle/gates to accept both `service_choice` and `name` without contract rationale;
  - runtime patching for a failure already corroborated as scenario-side drift;
  - adding phrase-hardcode into `llm_policy_core`.

## Root cause (mandatory)

- Symptom:
  - `P1.6o160` strict-audited rerun is `infra_valid=true` and surfaces `dialog 1 / turn 3 / "На какое время вы можете меня записать?"`, where scenario expects `service_choice`, but runtime and contract evidence keep `booking_time_availability_followup` with `expected_reply_type=name`.
- Minimal reproduction:
  1. Inspect `/tmp/booking_quality/p1.6o160-l2-dev-20260313-a1-r1/scenarios.json` and confirm `dialog 1 / turn 3` expects `reply_type=service_choice` plus `question_contract.expected_reply_type=service_choice`.
  2. Inspect `/tmp/booking_quality/p1.6o160-l2-dev-20260313-a1-r1/responses.jsonl` and confirm the same turn ends with `expected_reply_type=name`, `pending_question_act=ask_about_requested_slot`, `pending_question_target=time`, `pending_question_owner=booking_time_availability_followup`, and judge `pass`.
  3. Reproduce locally with `_sanitize_llm_turns()` on `[booking -> time -> ask_about_requested_slot]` and confirm the third turn is rewritten to tags `["booking"]` and orphan `service_choice`.
  4. Inspect `scripts/booking_dialog_scenarios.py` and confirm `_has_orphan_pending_question_tags()` rewrites `ask_about_requested_slot` when `active_reply_type != "time"`, while `_apply_active_name_time_availability_followup_expectations()` only rescues explicit/deictic time availability wording via `_looks_like_grounded_time_availability_probe()`.
- Evidence:
  - `/tmp/booking_quality/p1.6o160-l2-dev-20260313-a1-r1/summary.json`
  - `/tmp/booking_quality/p1.6o160-l2-dev-20260313-a1-r1/brief.md`
  - `/tmp/booking_quality/p1.6o160-l2-dev-20260313-a1-r1/scenarios.json`
  - `/tmp/booking_quality/p1.6o160-l2-dev-20260313-a1-r1/responses.jsonl`
  - `/tmp/booking_quality/p1.6o160-l2-dev-20260313-a1-r1/trace_bundle.jsonl`
  - `scripts/booking_dialog_scenarios.py`
  - `truffles-api/tests/test_booking_dialog_scenarios_script.py`
- Five Whys:
  1. Why did `P1.6o160` fail? Because scenario expected `service_choice` while runtime emitted `name`.
  2. Why did scenario expect `service_choice`? Because the turn was downgraded as an orphan pending-question followup.
  3. Why was it treated as orphan? Because `ask_about_requested_slot` under active `name` collect triggers `_has_orphan_pending_question_tags()`.
  4. Why was it not rescued later? Because the active-name availability rescue only accepts explicit/deictic time references, not generic requested-slot followup wording like `"На какое время вы можете меня записать?"`.
  5. Why is this architecture debt? Because the scenario compiler still has a narrower followup envelope than the runtime contract for active-name time availability continuation.
- Root cause statement:
  - scenario-governance under active `name` collect misclassifies generic requested-slot followup wording as orphan pending-question drift because the active-name rescue path is narrower than the runtime contract and misses requested-slot questions without explicit temporal anchors.
- Fix mechanism:
  - extend the bounded active-name time-availability expectation path to also admit generic requested-slot followup wording without temporal scope;
  - add a regression test that reproduces the exact `booking -> time -> ask_about_requested_slot` envelope and proves the turn ends with `expected_reply_type=name` + `booking_time_availability_followup` evidence instead of orphan `service_choice`.

## Reuse-first decision (mandatory)

- Reuse:
  - existing `_apply_active_name_time_availability_followup_expectations()`
  - existing `_looks_like_requested_slot_question_without_temporal_scope()`
  - existing scenario-script regression suite in `truffles-api/tests/test_booking_dialog_scenarios_script.py`
- Integrate:
  - widen one existing scenario-governance rescue path; do not create a second oracle engine
- Build:
  - no new dependency or framework

## Invariant

1. Runtime behavior stays unchanged; this is scenario/oracle-only.
2. Orphan pending-question downgrade stays intact for real orphan families outside the bounded active-name requested-slot envelope.
3. No oracle weakening to green unrelated `service_choice` turns.
4. Contract-first evidence remains `reply_type/meta/trace`, not text-match.

## Scope

- `scripts/booking_dialog_scenarios.py`
- `truffles-api/tests/test_booking_dialog_scenarios_script.py`
- truth sync docs for `P1.6o160` / `P1.6o161`

## Out of scope

- runtime owner changes
- proof-lane rerun itself
- judge rubric redesign
- broader orphan pending-question redesign outside the bounded envelope

## Touch-list

- `scripts/booking_dialog_scenarios.py`
- `truffles-api/tests/test_booking_dialog_scenarios_script.py`
- `STATE.md`
- `docs/SESSIONS/SESSION-2026-02-19-llm-first-firebreak-a1.md`
- `docs/SESSION_INDEX.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o160-post-p1.6o159-proof-lane-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-14-p1.6o161-active-name-requested-slot-followup-scenario-governance-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-14-p1.6o162-post-p1.6o161-proof-lane-a1.md`

## Plan

1. Freeze `P1.6o160` truth and publish `P1.6o161`.
2. Extend the bounded active-name followup expectation path to recognize requested-slot questions without temporal scope.
3. Add a regression test for the exact surfaced envelope.
4. Run targeted deterministic checks.
5. Sync truth and publish the next proof lane.

## DoD

1. The surfaced `dialog 1 / turn 3` envelope no longer downgrades to orphan `service_choice`.
2. Scenario expectation for the bounded envelope matches runtime contract: `expected_reply_type=name` with `booking_time_availability_followup` evidence.
3. Existing orphan fallback and active-name/deictic availability regressions remain green.
4. Active docs/state/session reflect that `P1.6o160` surfaced scenario-governance drift and `P1.6o161` is the selected continuation.

## Checks

- `python3 -m py_compile scripts/booking_dialog_scenarios.py truffles-api/tests/test_booking_dialog_scenarios_script.py`
- `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "active_name_time_availability_followup or active_name_requested_slot_followup_without_temporal_scope or orphan_pending_question"`
- `git diff --check`
- `SESSION_AGENT=a1 scripts/session_check.sh`

## Evidence

1. `/tmp/booking_quality/p1.6o160-l2-dev-20260313-a1-r1/summary.json`
2. `/tmp/booking_quality/p1.6o160-l2-dev-20260313-a1-r1/brief.md`
3. `/tmp/booking_quality/p1.6o160-l2-dev-20260313-a1-r1/scenarios.json`
4. `/tmp/booking_quality/p1.6o160-l2-dev-20260313-a1-r1/responses.jsonl`
5. `/tmp/booking_quality/p1.6o160-l2-dev-20260313-a1-r1/trace_bundle.jsonl`
6. `scripts/booking_dialog_scenarios.py`
7. `truffles-api/tests/test_booking_dialog_scenarios_script.py`

## Rollback

1. Revert the bounded active-name requested-slot followup expectation change and its regression test.
2. Keep `P1.6o160` truth frozen as scenario-governance drift.
3. Do not reinterpret the failure as runtime regression without new evidence.

## No-go

- no runtime/core patch in this block
- no oracle weakening to accept both `service_choice` and `name`
- no broad orphan-pending-question redesign
- no proof rerun before deterministic closure

## Risks / blockers

- overmatching generic booking/service-selection turns could suppress legitimate orphan fallback
- under-scoping the rescue could leave the surfaced envelope on `service_choice`
- next proof may surface another adjacent scenario/runtime family immediately after this closure

## Residual architecture debt (mandatory)

### Current residuals accepted in this block

- orphan pending-question governance is still heuristic outside the surfaced active-name requested-slot envelope
- proof bundle closure remains incomplete
- judge/contract advisory conflicts remain outside this bounded slice

### Why not in this block

- this block is a bounded `Track A` scenario-governance continuation only

### Risk if deferred

- the worktree keeps reopening a covered runtime-success contour as scenario drift on the first admissible proof turn

### Linked follow-up Task Package(s)

- `docs/TASK_PACKAGES/TP-2026-03-14-p1.6o162-post-p1.6o161-proof-lane-a1.md`

### Expiry/trigger to stop deferral

- before any new expensive rerun after `P1.6o160`

## Next-block contract (mandatory)

### Next block objective

- publish and run one fresh proof-lane rerun after deterministic closure of `P1.6o161`, then truthfully decide whether the active-name requested-slot followup scenario family moved off first fail.

### First deterministic check command

- `python3 -m py_compile scripts/booking_dialog_scenarios.py truffles-api/tests/test_booking_dialog_scenarios_script.py && pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "active_name_time_availability_followup or active_name_requested_slot_followup_without_temporal_scope or orphan_pending_question" && git diff --check`

### Blocked-by conditions

- `P1.6o160` truth is not synced in active docs/state/session
- targeted `P1.6o161` scenario-governance checks are not green
- the team attempts another expensive rerun before deterministic closure of this block

### Owner role for closure

- `Top Architect + Brain`

## Execution status

- `done`

## Execution result

- Extended `_apply_active_name_time_availability_followup_expectations()` in `scripts/booking_dialog_scenarios.py` so the bounded active-name rescue also admits generic requested-slot questions without temporal scope via `_looks_like_requested_slot_question_without_temporal_scope()`.
- Added `test_sanitize_llm_turns_preserves_active_name_requested_slot_followup_without_temporal_scope` in `truffles-api/tests/test_booking_dialog_scenarios_script.py` to reproduce the surfaced `booking -> time -> ask_about_requested_slot` envelope.
- Deterministic checks completed:
  - `python3 -m py_compile scripts/booking_dialog_scenarios.py truffles-api/tests/test_booking_dialog_scenarios_script.py`
  - `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "active_name_time_availability_followup or active_name_requested_slot_followup_without_temporal_scope or orphan_pending_question"`
  - `git diff --check`
  - `SESSION_AGENT=a1 scripts/session_check.sh`
- Published `P1.6o162` as the next admissible proof lane after deterministic closure.

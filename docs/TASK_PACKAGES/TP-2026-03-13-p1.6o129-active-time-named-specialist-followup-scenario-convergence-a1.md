# TP-2026-03-13-p1.6o129-active-time-named-specialist-followup-scenario-convergence-a1

- Название/цель: после truthful `P1.6o128` class triage закрыть bounded `Track A` scenario-governance drift, в котором active `time` collect + named specialist preference availability turn сохраняет stale `ask_about_requested_slot/time` expectation вместо generalized specialist-followup contract.

## Canon refs

- Parent closure program: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Main remediation: `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- Process reset / class triage: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o118-class-level-closure-process-reset-a1.md`
- Prior structural slice: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o127-pending-handoff-resume-boundary-first-slice-a1.md`
- Prior proof lane: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o128-post-pending-handoff-resume-proof-lane-a1.md`
- State baseline: `STATE.md`

## Git context

- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `origin/main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect` remove branch + worktree after merge

## One web search (mandatory before implementation)

- Query: `site:docs.python.org/3/library/re.html python regex word boundary ignorecase unicode`
- Date/time: `2026-03-13T12:43:00+05:00`
- Sources opened:
  - official Python `re` docs: `https://docs.python.org/3/library/re.html`
- Source quality:
  - official language reference / primary source
- Found reusable ideas:
  - keep the heuristic delta narrow and explicit through a dedicated regex contour instead of widening unrelated text classifiers;
  - preserve Unicode-safe case-insensitive matching for Russian/Kazakh name-bearing phrases.
- Decision:
  - `reuse`
  - reuse the existing scenario sanitizer and add one bounded named-preference availability contour inside the active-time specialist-followup expectation path.
- Rejected options:
  - runtime/router patch for a proof-routed scenario-governance class;
  - path-specific oracle owner hardcode (`policy_core_invalid_schema_*`) in generated expectations;
  - broad master/info classifier expansion beyond the surfaced contour.

## Root cause (mandatory)

- Symptom:
  - `P1.6o128` is infra-valid and truthfully displaces the pending/handoff runtime-owner family, but stops on `dialog 1 / turn 5 / "Я предпочитаю Айгерим, она свободна?"` with `expected_meta_mismatch` + `expected_trace_miss`.
- Minimal reproduction:
  1. Open `/tmp/booking_quality/p1.6o128-l2-dev-20260313-a1-r1/responses.jsonl`.
  2. Inspect `message_id=LLM-QUAL-p1.6o128-l2-dev-20260313-a1-r1-001-05-90ffe4`.
  3. Confirm runtime meta/trace are coherent with generalized specialist followup under degraded invalid-schema path: `pending_question_target=specialist`, `pending_question_interaction=specialist_followup`, `active_question_relation=referent_followup`, `expected_reply_type=time`, trace decision `booking_specialist_followup`.
  4. Inspect `/tmp/booking_quality/p1.6o128-l2-dev-20260313-a1-r1/scenarios.json`.
  5. Confirm generated expectation still requires stale requested-slot contract: `pending_question_act=ask_about_requested_slot`, `pending_question_target=time`.
  6. Reproduce locally through `_sanitize_llm_turns(...)` on the surfaced dialog prefix.
- Evidence:
  - `/tmp/booking_quality/p1.6o128-l2-dev-20260313-a1-r1/summary.json`
  - `/tmp/booking_quality/p1.6o128-l2-dev-20260313-a1-r1/brief.md`
  - `/tmp/booking_quality/p1.6o128-l2-dev-20260313-a1-r1/responses.jsonl`
  - `/tmp/booking_quality/p1.6o128-l2-dev-20260313-a1-r1/scenarios.json`
  - `truffles-api/tests/test_message_endpoint.py`
  - `truffles-api/tests/test_booking_quality_response_guard.py`
- Five Whys:
  1. Why did `P1.6o128` fail after `P1.6o127` closed? Because the first remaining fail is no longer pending/handoff runtime-owner drift.
  2. Why did evaluation fail? Because scenario meta/trace expectations stayed on `ask_about_requested_slot/time`.
  3. Why did scenario expectations stay stale? Because current specialist heuristics miss named-preference availability phrasing like `предпочитаю Айгерим, она свободна?`.
  4. Why is runtime not the primary blocker here? Because runtime response, trace, and existing contract tests already agree on generalized specialist followup semantics for this contour.
  5. Why must the fix stay generalized? Because scenario expectations must accept canonical semantics across success/degraded specialist-followup paths without path-specific owner duplication.
- Root cause statement:
  - named specialist preference availability turns under active `time` collect fall outside current scenario specialist heuristics, so generated expectations keep stale requested-slot meta/trace instead of the canonical generalized specialist-followup contract already enforced by runtime and response guard.
- Fix mechanism:
  - add one bounded named-preference availability heuristic in the scenario sanitizer;
  - on that contour, switch expectations from requested-slot act/time target to owner-agnostic specialist-followup meta/trace while preserving `expected_reply_type=time`;
  - prove acceptance with scenario-sanitizer and response-guard tests.

## Reuse-first decision (mandatory)

- Reuse:
  - existing scenario sanitizer in `scripts/booking_dialog_scenarios.py`
  - existing response guard acceptance harness in `truffles-api/tests/test_booking_quality_response_guard.py`
  - existing runtime contract truth in `truffles-api/tests/test_message_endpoint.py`
- Integrate:
  - align generator/oracle expectations to the already-tested runtime contract for specialist followup
- Build:
  - one new bounded heuristic and targeted tests only

## Invariant

1. `P1.6o127` remains deterministically closed.
2. `P1.6o128` remains the canonical proof artifact for this surfaced class.
3. Runtime specialist-followup semantics are not changed in this block.
4. Scenario expectations stay owner-agnostic where multiple runtime recovery paths share the same semantic contract.

## Scope

- `scripts/booking_dialog_scenarios.py`
- targeted scenario-sanitizer tests for surfaced named-preference availability contour
- targeted response-guard acceptance for generalized specialist-followup expectation
- truth sync for the surfaced `P1.6o128` class and this deterministic closure

## Out of scope

- runtime/router/owner-resolver changes
- broader master/info taxonomy redesign
- new proof rerun before deterministic closure is synced truthfully

## Touch-list

- `scripts/booking_dialog_scenarios.py`
- `truffles-api/tests/test_booking_dialog_scenarios_script.py`
- `truffles-api/tests/test_booking_quality_response_guard.py`
- `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o129-active-time-named-specialist-followup-scenario-convergence-a1.md`
- `STATE.md`
- `STRUCTURE.md`
- `docs/SESSIONS/SESSION-2026-02-19-llm-first-firebreak-a1.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o127-pending-handoff-resume-boundary-first-slice-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o128-post-pending-handoff-resume-proof-lane-a1.md`

## Plan

1. Freeze `P1.6o128` as the canonical class-routing proof artifact for this surfaced family.
2. Reproduce the stale scenario expectation on the surfaced dialog prefix through the local sanitizer.
3. Add one bounded named-preference availability heuristic inside the active-time specialist-followup expectation path.
4. Keep generated expectations owner-agnostic while matching canonical specialist-followup trace/meta semantics.
5. Add targeted scenario-sanitizer and response-guard tests.
6. Sync truth into active docs/state/session and publish the next proof-lane block only after deterministic green.

## DoD

1. The surfaced `P1.6o128` named-preference availability turn no longer expects `pending_question_act=ask_about_requested_slot` / `pending_question_target=time`.
2. Scenario expectations now accept generalized specialist-followup semantics with `expected_reply_type=time`.
3. Targeted scenario-sanitizer and response-guard tests are green.
4. Active docs/state/session reflect that `P1.6o128` routed into `Track A` and `P1.6o129` is deterministically closed.

## Checks

- `python3 -m py_compile scripts/booking_dialog_scenarios.py truffles-api/tests/test_booking_dialog_scenarios_script.py truffles-api/tests/test_booking_quality_response_guard.py`
- `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "named_specialist_preference_availability_keeps_generalized_followup"`
- `pytest -q truffles-api/tests/test_booking_quality_response_guard.py -k "generalized_expectation or invalid_schema_booking_request_specialist_followup"`
- `git diff --check`
- `SESSION_AGENT=a1 scripts/session_check.sh`

## Execution status

- `done`
- `P1.6o129` is deterministically closed: scenario-governance now recognizes active-time named specialist preference availability turns like `Я предпочитаю Айгерим, она свободна?` and emits generalized specialist-followup expectations (`pending_question_target=specialist`, `pending_question_interaction=specialist_followup`, `active_question_relation=referent_followup`, `expected_reply_type=time`) without path-specific owner hardcode.
- Closure evidence is intentionally bounded:
  - targeted checks are green on the surfaced contour;
  - a broader pre-existing red case in `truffles-api/tests/test_booking_dialog_scenarios_script.py` around `master` -> `booking` tag normalization remains outside this slice and is not used as closure evidence.

## Evidence

1. `/tmp/booking_quality/p1.6o128-l2-dev-20260313-a1-r1/summary.json`
2. `/tmp/booking_quality/p1.6o128-l2-dev-20260313-a1-r1/brief.md`
3. `/tmp/booking_quality/p1.6o128-l2-dev-20260313-a1-r1/responses.jsonl`
4. `/tmp/booking_quality/p1.6o128-l2-dev-20260313-a1-r1/scenarios.json`
5. `scripts/booking_dialog_scenarios.py`
6. `truffles-api/tests/test_booking_dialog_scenarios_script.py`
7. `truffles-api/tests/test_booking_quality_response_guard.py`

## Rollback

1. Revert the bounded scenario heuristic and its targeted tests.
2. Keep `P1.6o128` truth frozen as the canonical surfaced `Track A` proof artifact.
3. Do not reinterpret the surfaced class as runtime-owner drift after rollback.

## No-go

- no runtime/router patch for this family
- no path-specific scenario hardcode to `policy_core_invalid_schema_specialist_followup`
- no broad master/info classifier rewrite
- no proof rerun before deterministic closure is synced truthfully

## Risks / blockers

- under-scoping can leave adjacent named specialist availability phrasings outside the bounded contour
- over-scoping can misclassify generic master/info questions as specialist followup
- inherited `hardcode_core_violation` may still keep proof runs semantic-red after this family closes

## Residual architecture debt (mandatory)

### Current residuals accepted in this block

- broader scenario-governance convergence from runtime/matrix remains open
- one unrelated scenario-script red case in the dirty worktree remains outside this slice
- acceptance proof (`lock/replay/full`) remains blocked

### Why not in this block

- this block is limited to the surfaced named-preference availability family from `P1.6o128`

### Risk if deferred

- fresh proof reruns can keep reopening this same specialist-followup expectation drift under new phrasing variants

### Linked follow-up Task Package(s)

- next proof-lane TP after deterministic closure of `P1.6o129`
- later broader `Track A` convergence block if fresh proof reopens another scenario/runtime truth split

### Expiry/trigger to stop deferral

- before any further guarded `dev/acceptance` rerun beyond `P1.6o128`

## Next-block contract (mandatory)

### Next block objective

- publish and run one fresh proof-lane rerun after deterministic closure of `P1.6o129`, then truthfully decide whether the covered named specialist followup family is gone from first-fail position on admissible evidence.

### First deterministic check command

- `python3 -m py_compile scripts/booking_dialog_scenarios.py truffles-api/tests/test_booking_dialog_scenarios_script.py truffles-api/tests/test_booking_quality_response_guard.py && git diff --check`

### Blocked-by conditions

- `P1.6o128` truthful proof result is not synced in active docs/state/session
- targeted `P1.6o129` scenario/guard checks are not green
- the team attempts another expensive rerun before deterministic closure of this structural slice

### Owner role for closure

- `Top Architect + Brain`

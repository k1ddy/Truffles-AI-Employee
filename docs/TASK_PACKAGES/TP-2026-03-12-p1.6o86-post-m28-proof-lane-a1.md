# TP-2026-03-12-p1.6o86-post-m28-proof-lane-a1

- Название/цель: после deterministic closure `P1.6o85` выполнить один fresh guarded `dev L2`, чтобы доказать, что `M28` больше не является first fail, и truthfully surface-ить следующую remaining family, если она есть.

## Canon refs

- Parent closure program: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Main remediation: `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- Prior proof lane: `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o84-m27-proof-lane-and-next-row-admission-a1.md`
- Deterministic closure prerequisite: `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o85-slot-compare-explicit-time-fill-scenario-governance-a1.md`
- Owner matrix baseline: `truffles-api/app/knowledge/generic/INTERACTION_OWNER_MATRIX.yaml`
- State baseline: `STATE.md`

## Git context

- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect` remove branch + worktree after merge

## One web search (mandatory before implementation)

- Query: `site:www.uvicorn.org/settings uvicorn host port app-dir env-file`
- Date/time: `2026-03-12T13:40:00+05:00`
- Sources opened:
  - official Uvicorn settings docs: `https://www.uvicorn.org/settings/`
- Source quality:
  - official product documentation
- Found reusable ideas:
  - CLI flags such as `--host`, `--port`, and `--app-dir` are the canonical way to bind the server for a local proof run;
  - `--env-file` is for the ASGI application environment rather than `UVICORN_*` server settings.
- Decision:
  - `reuse`
  - keep the proof runtime startup explicit and deterministic with CLI host/port/app-dir flags inside the worktree shell session.
- Rejected options:
  - detached launch patterns that make runtime fingerprint validation ambiguous;
  - implicit `UVICORN_*` env-file configuration for the server process.

## Root cause (mandatory)

- Symptom:
  - after `P1.6o85`, deterministic contour is green, but there is still no fresh proof run showing that `M28` disappeared from the first-fail position.
- Minimal reproduction:
  1. Use the last fresh run `p1.6o84-l2-dev-20260312-a1-r2` as the pre-fix baseline.
  2. Confirm it failed first on dialog `1`, turn `8`, text `Можно записаться на 15:00?`.
  3. Confirm `P1.6o85` now deterministically rewrites that family to canonical `time -> name`.
  4. Run one fresh guarded `dev L2` on a live runtime whose `/admin/version` matches current `HEAD`.
  5. Audit the resulting run strictly and compare the new first fail, if any, against the pre-fix surfaced row.
- Evidence:
  - `/tmp/booking_quality/p1.6o84-l2-dev-20260312-a1-r2/summary.json`
  - `/tmp/booking_quality/p1.6o84-l2-dev-20260312-a1-r2/scenarios.json`
  - `/tmp/booking_quality/p1.6o84-l2-dev-20260312-a1-r2/responses.jsonl`
  - `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o85-slot-compare-explicit-time-fill-scenario-governance-a1.md`
- Five Whys:
  1. Why is closure still incomplete? Because bounded deterministic closure alone does not prove the expensive lane rolled past `M28`.
  2. Why is a proof lane needed? Because the quality chain admits closure only when a fresh guarded run confirms the surfaced family is no longer first.
  3. Why can we run it now? Because `P1.6o85` removed the stale scenario-governance contract on deterministic contour.
  4. Why must the runtime be fingerprinted first? Because preflight/runtime mismatch makes the run non-canonical.
  5. Why only one run? Because the goal is admission of the next family, not another reactive multi-run loop.
- Root cause statement:
  - the remaining gap after `P1.6o85` is proof, not implementation: without one fresh guarded `dev L2` on a verified live runtime, `M28` remains only deterministically closed and the next remaining family stays unknown.
- Fix mechanism:
  - publish this proof-lane TP;
  - start a live worktree runtime on `:18184` and prove `/admin/version == HEAD`;
  - execute exactly one guarded `dev L2` run and strict audit;
  - sync docs/state/matrix truthfully based on the new first fail outcome.

## Reuse-first decision (mandatory)

- Reuse:
  - existing guarded entrypoint `scripts/llm_quality_guarded.sh`
  - existing audit command `python3 ops/diagnose.py llm-quality-audit`
  - existing live runtime startup pattern used in `P1.6o84`
- Integrate:
  - no runtime code changes unless the proof run surfaces a new deterministic or bounded family that requires a separate TP
- Build:
  - none planned in this block

## Release safety

- Rollout: none; local proof-only block
- Go signal:
  - runtime fingerprint matches `HEAD`
  - guarded run artifacts are valid enough to audit (`artifact_integrity.valid=true`)
  - first fail is no longer the `M28` row
- No-go signal:
  - invalid runtime fingerprint preflight
  - incomplete/invalid guarded artifacts
  - fresh first fail remains the same `M28` family
- Rollback:
  - stop the proof lane, record evidence truthfully, do not reinterpret invalid/incomplete runs as closure evidence

## Invariant

1. `M27` stays boundedly cleared by `P1.6o84`.
2. `M28` stays deterministically closed by `P1.6o85`.
3. No runtime/product contract weakening.
4. Exactly one fresh guarded `dev L2` run in this block.

## Scope

- proof-lane runtime startup + fingerprint validation
- one guarded `dev L2`
- strict audit
- doc/state/session sync for the resulting admission

## Out of scope

- new runtime implementation before the proof run surfaces a new row
- acceptance `lock/replay/full`
- matrix redesign

## Touch-list

- `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o86-post-m28-proof-lane-a1.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `STATE.md`
- `STRUCTURE.md`
- `docs/SESSIONS/SESSION-2026-02-19-llm-first-firebreak-a1.md`
- `truffles-api/app/knowledge/generic/INTERACTION_OWNER_MATRIX.yaml`
- `truffles-api/tests/test_interaction_owner_matrix_contract.py`

## Plan (1..N)

1. Publish `P1.6o86` with proof-lane scope and runtime-fingerprint requirements.
2. Re-run the deterministic prerequisite check from `P1.6o85`.
3. Start live worktree runtime on `:18184` and prove `/admin/version == HEAD`.
4. Execute exactly one fresh guarded `dev L2` and strict audit.
5. Sync matrix/docs/state/session with truthful post-run admission.

## DoD

1. Fresh guarded `dev L2` is executed on a fingerprint-verified live runtime.
2. `M28` is no longer the first fail family.
3. The next first fail, if any, is captured truthfully with artifacts.
4. Docs/state/session are synced to the new admission truth.

## Checks

- `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "slot_compare and explicit_time"`
- `pytest -q truffles-api/tests/test_interaction_owner_matrix_contract.py`
- `scripts/llm_quality_guarded.sh --mode lock --run-id p1.6o86-l2-dev-20260312-a1-r1 --allow-pending-previous -- --base-url http://127.0.0.1:18184 --client-slug demo_salon --mode llm --count 10 --min-turns 10 --max-turns 15 --include-media --scenario-coverage booking,info,interrupt,handoff --tool-hooks auto --jid-mode unique --judge-mode all --quality-lane dev --run-economy-gate block --fail-on-thresholds --max-failures 1 --reset-before-dialog`
- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o86-l2-dev-20260312-a1-r1 --status done --strict-artifacts`
- `git diff --check`
- `SESSION_AGENT=a1 scripts/session_check.sh`

## Execution status

- `done`

## Evidence

1. `/tmp/booking_quality/p1.6o84-l2-dev-20260312-a1-r2/summary.json`
2. `/tmp/booking_quality/p1.6o84-l2-dev-20260312-a1-r2/scenarios.json`
3. `/tmp/booking_quality/p1.6o84-l2-dev-20260312-a1-r2/responses.jsonl`
4. fresh runtime `/admin/version` output proving `git_commit=21afcead084aa3fdfc5702c5547fb40d74970b5a`
5. `/tmp/booking_quality/p1.6o86-l2-dev-20260312-a1-r1/summary.json`
6. `/tmp/booking_quality/p1.6o86-l2-dev-20260312-a1-r1/manual_audit.md`
7. `/tmp/booking_quality/p1.6o86-l2-dev-20260312-a1-r2/summary.json`
8. `/tmp/booking_quality/p1.6o86-l2-dev-20260312-a1-r2/brief.md`
9. `/tmp/booking_quality/p1.6o86-l2-dev-20260312-a1-r2/scenarios.json`
10. `/tmp/booking_quality/p1.6o86-l2-dev-20260312-a1-r2/responses.jsonl`
11. `/tmp/booking_quality/p1.6o86-l2-dev-20260312-a1-r2/trace_bundle.jsonl`
12. `/tmp/booking_quality/p1.6o86-l2-dev-20260312-a1-r2/manual_audit.md`
13. updated matrix/docs/state/session references

## Rollback

1. Stop the local runtime and keep `P1.6o85` deterministic closure intact.
2. If the run is invalid/incomplete, record it truthfully and keep `P1.6o86` open.
3. Do not mutate the matrix to hide surfaced evidence.

## No-go

- no second expensive run in this block
- no default-path runtime patch before the proof run surfaces the next family
- no reuse of invalid preflight evidence as closure proof

## Risks / blockers

- local runtime may fail fingerprint validation or die before the guarded run starts
- the next surfaced row may be runtime, scenario, or oracle, and must not be pre-labeled before evidence

## Residual architecture debt (mandatory)

### Current residuals accepted in this block

- closure proof is still on the `dev L2` lane only; `lock/replay/full` remain blocked
- scenario ontology still contains localized normalization rules rather than a unified executable generator contract

### Why not in this block

- this block is only the admission proof after `M28` deterministic closure

### Risk if deferred

- the program stays stuck with no truthful next-family admission after `M28`

### Linked follow-up Task Package(s)

- next surfaced row TP if the proof lane finds a new family
- `P1.7` acceptance replay gate after the closure program proof lane is green enough to advance

### Expiry/trigger to stop deferral

- before any next guarded `lock/replay/full`

## Next-block contract (mandatory)

### Next block objective

- publish and close `P1.6o87` so booking-tag requested-slot questions like `На какое время у вас есть свободные слоты?` normalize back to canonical `ask_about_requested_slot(time)` instead of inheriting stale `service_choice` expectations after booking-management contamination.

### First deterministic check command

- `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "slot_question or check_booking" && pytest -q truffles-api/tests/test_interaction_owner_matrix_contract.py && git diff --check`

### Blocked-by conditions

- `P1.6o86` proof evidence is not synced truthfully into active docs and matrix state
- deterministic reproduction for the booking-tag requested-slot family is not green
- the fresh run remains unanalyzed or is misclassified as a runtime regression

### Owner role for closure

- `Top Architect + Brain`

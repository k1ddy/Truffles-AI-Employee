# TP-2026-03-06-p6.4-open-world-paraphrase-synonym-coverage-a1

- Название/цель: следующий атомарный блок `P6` после multilingual/script/noise surfaces — добавить генератору детерминированные paraphrase/synonym families, чтобы один и тот же action/slot contract проверялся не только на шаблонных фразах, но и на смыслово-эквивалентных перефразировках.
- Parent TP: `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- Canon refs: `AGENTS.md`, `STATE.md`, `STRUCTURE.md`, `STRATEGY/REQUIREMENTS.md`, `TECH.md`, `docs/runbooks/BOOKING_CONFIRM_VERIFY.md`, `docs/TASK_PACKAGES/TP-2026-02-19-llm-first-firebreak-program.md`
- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`

## One web search (mandatory before implementation)

- Query: `site:arxiv.org llm input phrasing sensitivity paraphrase robustness`
- Time (UTC): `2026-03-06T13:15:12Z`
- Sources:
  - `arXiv: On the Sensitivity of Large Language Models to Input Phrasing and Demonstration Ordering`
- Ready solutions found:
  - LLM behavior changes materially with semantically equivalent phrasing shifts
  - controlled paraphrase families are a necessary robustness axis, not optional eval sugar
- Decision (`reuse/integrate/build`): `reuse + integrate`
  - reuse the existing tag-aware scenario mutation pipeline
  - integrate paraphrase families as another contract-preserving surface layer
- Rejected options:
  - rely only on current template wording plus typo/translit axes
  - push paraphrase robustness entirely into future expensive LLM generation

## Root cause (mandatory)

- Symptom:
  - after `P6.1`..`P6.3`, the generator still relies on a small set of canonical surface forms and does not yet cover meaning-preserving paraphrases or synonym substitutions.

- Minimal reproduction:
  1. inspect `scripts/booking_dialog_scenarios.py` for paraphrase/synonym mutation support
  2. `rg -n "paraphrase|synonym" scripts/booking_dialog_scenarios.py truffles-api/tests/test_booking_dialog_scenarios_script.py`
  3. observe no dedicated paraphrase mutation family exists

- Evidence:
  - `scripts/booking_dialog_scenarios.py`
    - current mutation axes cover language, translit, and typo surface only
  - `docs/TASK_PACKAGES/TP-2026-02-19-llm-first-firebreak-program.md`
    - mandatory open-world battery requires adversarial phrasing and synonym variation

- Five Whys:
  1. Why is open-world proof still incomplete: current surfaces are still close to a small template lexicon.
  2. Why is that risky: semantic regressions can hide behind narrow wording coverage.
  3. Why can invariant gates not solve this alone: they only block surfaced failures, they do not create new wording families.
  4. Why should this be the next block: it is the next contract-preserving mutation after language/script/noise.
  5. Why keep it separate: paraphrase families need their own deterministic oracle and should not be mixed with typo/translit closure.

- Root cause statement:
  - the generator lacks a semantic-equivalence mutation family for paraphrases and synonyms.

- Fix mechanism:
  - extend the tag-aware mutator with paraphrase/synonym templates
  - preserve `tags` and `expect`
  - prove deterministic paraphrase coverage in generator tests

## Invariant

1. Paraphrase mutation does not alter `tags` or `expect`.
2. The same seed yields identical paraphrase output.
3. Broader phrasing diversity is created in the generator/test layer, not runtime-core.

## Scope

- `scripts/booking_dialog_scenarios.py`
- `truffles-api/tests/test_booking_dialog_scenarios_script.py`
- `docs/runbooks/BOOKING_CONFIRM_VERIFY.md`
- `docs/TASK_PACKAGES/TP-2026-03-06-p6.4-open-world-paraphrase-synonym-coverage-a1.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`

## Out of scope

- expensive LLM quality runs
- runtime-core wording heuristics
- new invariant metrics

## Plan (1..N)

1. Inventory current tag-aware templates and choose safe paraphrase families.
2. Add deterministic paraphrase/synonym mutation support.
3. Add targeted tests for contract preservation and seed stability.
4. Sync docs/evidence.

## DoD

1. Generator has a deterministic paraphrase/synonym mutation family.
2. Paraphrase output preserves `tags` and `expect`.
3. Deterministic tests are green.
4. `git diff --check` is clean.

## Checks

- `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "paraphrase or synonym"`
- `git diff --check`

## Execution status

- `done`

## Evidence

1. Code:
   - `scripts/booking_dialog_scenarios.py`
   - `truffles-api/tests/test_booking_dialog_scenarios_script.py`
   - `docs/runbooks/BOOKING_CONFIRM_VERIFY.md`
2. Commands:
   - `python3 scripts/booking_dialog_scenarios.py --count 2 --seed 42 --semantic-variation-profile synonym --output /tmp/booking_dialog_scenarios_synonym.json`
   - `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "semantic_variation or paraphrase or synonym"` -> `3 passed`
   - `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py` -> `24 passed`
   - `git diff --check` -> `clean`

## Rollback

1. revert paraphrase family additions
2. rerun generator tests

## No-go

- no runtime-core semantic rewrites
- no nondeterministic paraphrase generation
- no mutation that changes `tags` / `expect`

## Risks/Blockers

1. paraphrase families can accidentally collapse into lexicon-fitting if they are too narrow
2. broader wording changes must stay semantically aligned with slot/action expectations

## Residual architecture debt (mandatory)

- Current residuals accepted in this block:
  - format-level open-world mutation families for time/phone/date remain deferred
- Why not in this block:
  - this block closes semantic-equivalent paraphrase coverage only
- Risk if deferred:
  - generator still under-covers numeric/format drift even after wording coverage improved
- Linked follow-up Task Package(s):
  - `TBD (create before start): P6.5 format mutation coverage`
- Expiry/trigger to stop deferral:
  - before claiming `P6` closure

## Next-block contract (mandatory)

- Next block objective:
  - add deterministic time/phone/date format mutation families so the same slot contract is exercised under punctuation/layout variants
- First deterministic check command:
  - `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "format_variant or phone_format or time_format"`
- Blocked-by conditions:
  - no deterministic format-variant family implemented
  - no tests prove slot-contract preservation under format changes
- Owner role for closure:
  - `Top Architect + Brain`

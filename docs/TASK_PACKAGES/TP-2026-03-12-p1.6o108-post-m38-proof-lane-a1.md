# TP-2026-03-12-p1.6o108-post-m38-proof-lane-a1

- Название/цель: после deterministic closure `P1.6o107` выполнить один fresh guarded `dev L2`, чтобы доказать, что `M38` больше не является first fail, и truthfully surface-ить следующую remaining family, если она есть.

## Canon refs

- Parent closure program: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Main remediation: `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- Prior proof lane: `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o106-post-m37-proof-lane-a1.md`
- Deterministic closure prerequisite: `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o107-active-time-timeout-generic-specialist-choice-followup-a1.md`
- Owner matrix baseline: `truffles-api/app/knowledge/generic/INTERACTION_OWNER_MATRIX.yaml`
- State baseline: `STATE.md`

## Git context

- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect` remove branch + worktree after merge

## One web search (mandatory before implementation)

- Query: `site:www.uvicorn.org/settings uvicorn host port admin version runtime fingerprint`
- Date/time: `reused official proof-lane reference from P1.6o106 (2026-03-12T18:13:23+05:00)`
- Sources opened:
  - official Uvicorn settings docs: `https://www.uvicorn.org/settings/`
- Source quality:
  - official product documentation
- Found reusable ideas:
  - proof runtime startup should stay explicit and reproducible via CLI flags;
  - fingerprint validation must stay external to the app process and be recorded before the guarded run starts.
- Decision:
  - `reuse`
  - reuse the established live proof-run startup pattern on `:18184` and record `/admin/version == HEAD` before the single guarded run.
- Rejected options:
  - detached startup patterns that hide runtime parity;
  - additional expensive runs in the same block.

## Root cause (mandatory)

- Symptom:
  - after `P1.6o107`, deterministic closure is green, but there is no fresh proof run showing `M38` disappeared from the first-fail position.
- Minimal reproduction:
  1. Use `p1.6o106-l2-dev-20260312-a1-r1` as the pre-fix proof baseline.
  2. Confirm it failed first on dialog `2`, turn `8`, text `Могу я поменять специалиста?` with `policy_core_timeout_degrade_booking_limit` / `handoff`.
  3. Confirm `P1.6o107` now deterministically keeps the same family inside the bounded specialist-target timeout interrupt with explicit `time` resume.
  4. Run one fresh guarded `dev L2` on a live runtime whose `/admin/version` matches current `HEAD`.
  5. Audit the resulting run strictly and compare the new first fail against `M38`.
- Evidence:
  - `/tmp/booking_quality/p1.6o106-l2-dev-20260312-a1-r1/summary.json`
  - `/tmp/booking_quality/p1.6o106-l2-dev-20260312-a1-r1/brief.md`
  - `/tmp/booking_quality/p1.6o106-l2-dev-20260312-a1-r1/responses.jsonl`
  - `/tmp/booking_quality/p1.6o106-l2-dev-20260312-a1-r1/trace_bundle.jsonl`
  - `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o107-active-time-timeout-generic-specialist-choice-followup-a1.md`
- Five Whys:
  1. Why is closure still incomplete? Because deterministic closure alone does not prove the expensive lane rolled past `M38`.
  2. Why is a proof lane needed? Because the quality chain admits closure only when a fresh guarded run confirms the surfaced family is no longer first.
  3. Why can we run it now? Because `P1.6o107` boundedly closes the timeout specialist-change row on deterministic contour.
  4. Why must runtime be fingerprinted first? Because preflight/runtime mismatch makes the run non-canonical.
  5. Why only one run? Because the goal is admission of the next family, not another reactive rerun loop.
- Root cause statement:
  - the remaining gap after `P1.6o107` is proof, not implementation: without one fresh guarded `dev L2` on a verified live runtime, `M38` remains only deterministically closed and the next remaining family stays unknown.
- Fix mechanism:
  - start a live worktree runtime on `:18184` and prove `/admin/version == HEAD`;
  - execute exactly one guarded `dev L2` run and strict audit;
  - sync docs/state/matrix truthfully based on the new first-fail outcome.

## Reuse-first decision (mandatory)

- Reuse:
  - existing guarded entrypoint `scripts/llm_quality_guarded.sh`
  - existing audit command `python3 ops/diagnose.py llm-quality-audit`
  - existing live runtime startup pattern used in prior proof-lane blocks
- Integrate:
  - no runtime code changes unless the proof run surfaces a new deterministic or bounded family
- Build:
  - none planned in this block

## Release safety

- Rollout: none; local proof-only block
- Go signal:
  - runtime fingerprint matches `HEAD`
  - guarded run artifacts are valid enough to audit (`artifact_integrity.valid=true`)
  - first fail is no longer `M38`
- No-go signal:
  - invalid runtime fingerprint preflight
  - incomplete/invalid guarded artifacts
  - fresh first fail remains the same `M38` family
- Rollback:
  - stop the proof lane, record evidence truthfully, do not reinterpret invalid/incomplete runs as closure evidence

## Invariant

1. `M38` stays deterministically closed by `P1.6o107`.
2. Existing timeout specialist-followup and timeout master-info interrupt rows remain boundedly green.
3. No runtime/product contract weakening.
4. Exactly one fresh guarded `dev L2` run in this block.

## Scope

- proof-lane runtime startup + fingerprint validation
- one guarded `dev L2`
- strict audit
- doc/state/session sync for the resulting admission

## Out of scope

- new runtime implementation before the proof run surfaces a new row
- acceptance `lock/replay/full`
- matrix redesign

## Touch-list

- `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o108-post-m38-proof-lane-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o107-active-time-timeout-generic-specialist-choice-followup-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o106-post-m37-proof-lane-a1.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `STATE.md`
- `STRUCTURE.md`
- `docs/SESSIONS/SESSION-2026-02-19-llm-first-firebreak-a1.md`
- `truffles-api/app/knowledge/generic/INTERACTION_OWNER_MATRIX.yaml`
- `truffles-api/tests/test_interaction_owner_matrix_contract.py`

## Plan (1..N)

1. Publish `P1.6o108` with proof-lane scope and runtime-fingerprint requirements.
2. Re-run the deterministic prerequisite from `P1.6o107`.
3. Start live worktree runtime on `:18184` and prove `/admin/version == HEAD`.
4. Execute exactly one fresh guarded `dev L2` and strict audit.
5. Sync matrix/docs/state/session with truthful post-run admission.

## DoD

1. Fresh guarded `dev L2` is executed on a fingerprint-verified live runtime.
2. `M38` is no longer the first fail family.
3. The next first fail, if any, is captured truthfully with artifacts.
4. Docs/state/session are synced to the new admission truth.

## Checks

- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o106-l2-dev-20260312-a1-r1 --status done --strict-artifacts`
- `pytest -q truffles-api/tests/test_message_endpoint.py -k "timeout_specialist_followup_keeps_time_collect or timeout_master_info_interrupt"` -> `3 passed, 379 deselected`
- `pytest -q truffles-api/tests/test_booking_quality_response_guard.py -k "timeout_specialist_followup or timeout_master_info_interrupt"` -> `1 passed, 94 deselected`
- `pytest -q truffles-api/tests/test_interaction_owner_matrix_contract.py` -> `2 passed`
- `TEST_MODE=1 scripts/llm_quality_guarded.sh --mode lock --run-id p1.6o108-l2-dev-20260312-a1-r1 --allow-pending-previous --allow-repeat-fingerprint -- --base-url http://127.0.0.1:18184 --client-slug demo_salon --mode llm --count 10 --min-turns 10 --max-turns 15 --include-media --scenario-coverage booking,info,interrupt,handoff --tool-hooks auto --jid-mode unique --allow-non-allowlist --judge-mode all --quality-lane dev --run-economy-gate block --fail-on-thresholds --max-failures 1 --reset-before-dialog` -> `run_dir=/tmp/booking_quality/p1.6o108-l2-dev-20260312-a1-r1`
- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o108-l2-dev-20260312-a1-r1 --status done --strict-artifacts` -> `artifact_integrity.valid=true`, `infra_valid=false`, `semantic_valid=false`, `stop_reason=max_failures_reached:1`
- `git diff --check` -> `pass`
- `SESSION_AGENT=a1 scripts/session_check.sh` -> `Session OK`

## Execution status

- `done`
- refreshed the live `:18184` worktree runtime, verified `/admin/version` matched current `HEAD` commit `21afcead084aa3fdfc5702c5547fb40d74970b5a`, and executed exactly one guarded `dev L2` run `p1.6o108-l2-dev-20260312-a1-r1`
- `M38` is no longer the first fail family; the new first fail is surfaced row `M39` on dialog `2`, turn `2`, text `Какие услуги по стрижке волос вы предлагаете?`
- the surfaced gap is a runtime owner miss: policy-core already marks the turn as grounded service info, but runtime keeps stale `tool_action=collect`, reopens `service_choice`, and misses the factual side-question
- the next admissible bounded closure block is `P1.6o109`

## Evidence

1. `/tmp/booking_quality/p1.6o106-l2-dev-20260312-a1-r1/summary.json`
2. `/tmp/booking_quality/p1.6o106-l2-dev-20260312-a1-r1/brief.md`
3. `/tmp/booking_quality/p1.6o106-l2-dev-20260312-a1-r1/scenarios.json`
4. `/tmp/booking_quality/p1.6o106-l2-dev-20260312-a1-r1/responses.jsonl`
5. `/tmp/booking_quality/p1.6o106-l2-dev-20260312-a1-r1/trace_bundle.jsonl`
6. `/tmp/booking_quality/p1.6o106-l2-dev-20260312-a1-r1/manual_audit.md`
7. `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o107-active-time-timeout-generic-specialist-choice-followup-a1.md`
8. `/tmp/booking_quality/p1.6o108-l2-dev-20260312-a1-r1/summary.json`
9. `/tmp/booking_quality/p1.6o108-l2-dev-20260312-a1-r1/brief.md`
10. `/tmp/booking_quality/p1.6o108-l2-dev-20260312-a1-r1/responses.jsonl`
11. `/tmp/booking_quality/p1.6o108-l2-dev-20260312-a1-r1/trace_bundle.jsonl`
12. `/tmp/booking_quality/p1.6o108-l2-dev-20260312-a1-r1/manual_audit.md`
13. `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o109-active-time-services-overview-interrupt-owner-a1.md`

## Rollback

1. Stop the local runtime and keep `P1.6o107` deterministic closure intact.
2. If the run is invalid/incomplete, record it truthfully and keep `P1.6o108` open.
3. Do not mutate the matrix to hide surfaced evidence.

## No-go

- no second expensive run in this block
- no default-path runtime patch before the proof run surfaces the next family
- no reuse of invalid preflight evidence as closure proof

## Risks / blockers

- local runtime may fail fingerprint validation or die before the guarded run starts
- the next surfaced row may be runtime, scenario, or oracle, and must not be pre-labeled before evidence

## Residual architecture debt (mandatory)

### Current residuals accepted in this block

- closure proof is still on the `dev L2` lane only; `lock/replay/full` remain blocked
- timeout specialist-change recovery still lives as a bounded pair across `decision.py` + `booking.py`, not a dedicated owner handler

### Why not in this block

- this block is only the proof admission after `M38` deterministic closure; it does not implement the next surfaced family

### Risk if deferred

- closure lane stays stuck on deterministic-only evidence without a truthfully admitted next family

### Linked follow-up Task Package(s)

- future bounded child TP for the next surfaced family, if any
- `P1.7` acceptance replay gate after the closure program proof lane is green enough to advance

### Expiry/trigger to stop deferral

- before any further guarded `dev/acceptance` rerun beyond `P1.6o108`

## Next-block contract (mandatory)

### Next block objective

- publish and complete the next bounded child TP for the newly surfaced family, if any, after `P1.6o108` truthfully captures the next first fail.

### First deterministic check command

- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o106-l2-dev-20260312-a1-r1 --status done --strict-artifacts && pytest -q truffles-api/tests/test_message_endpoint.py -k "timeout_specialist_followup_keeps_time_collect or timeout_master_info_interrupt" && pytest -q truffles-api/tests/test_booking_quality_response_guard.py -k "timeout_specialist_followup or timeout_master_info_interrupt" && pytest -q truffles-api/tests/test_interaction_owner_matrix_contract.py && git diff --check`

### Blocked-by conditions

- `P1.6o107` deterministic closure is not synced truthfully into active docs and the owner matrix
- timeout generic specialist-change deterministic reproductions are not green
- `P1.6o108` is not published as the next proof block

### Owner role for closure

- `Top Architect + Brain`

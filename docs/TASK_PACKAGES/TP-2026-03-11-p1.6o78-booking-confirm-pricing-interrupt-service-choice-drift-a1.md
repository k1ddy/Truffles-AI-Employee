# TP-2026-03-11-p1.6o78-booking-confirm-pricing-interrupt-service-choice-drift-a1

- Название/цель: закрыть surfaced `r112` blocker family, где после bounded `M26` closure fresh guarded `dev L2` больше не падает на daypart-preference false-info row, но under active booking `time` collect + `slot_confirmation_required=true` pricing interrupt `А сколько это будет стоить?` теряет grounded service и reopening drift уходит в `service_clarify` / `expected_reply_type=service_choice`.

## Canon refs

- Parent verdict: `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o60a-remaining-closure-architecture-verdict-a1.md`
- Owner matrix: `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o60b-remaining-closure-owner-matrix-a1.md`
- Program TP: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Proof bundle: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`
- Prior child: `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o77-active-time-daypart-preference-info-signal-false-positive-a1.md`
- Related bounded closures: `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o67-ambiguous-time-fill-scenario-governance-a1.md`, `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o76-initial-timeout-requested-slot-owner-a1.md`, `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o77-active-time-daypart-preference-info-signal-false-positive-a1.md`
- STATE: `STATE.md`
- CA: remaining closure row after `M26`

## One web search (mandatory before implementation)

- Query: `site:rasa.com/docs/rasa/forms slot confirmation user interrupt preserve active slot context`
- Date/time: `2026-03-11T22:57:00+05:00`
- Sources opened:
  - official Rasa docs: `https://rasa.com/docs/rasa/forms/`
- Source quality:
  - official product documentation
- Found reusable ideas:
  - when a slot remains unresolved, side questions should not clear the active slot contract;
  - interruptions can be answered, but the active slot and resume target must stay explicit until confirmation/validation finishes.
- Decision:
  - `reuse/integrate`
  - reuse the existing booking info-interrupt owner and slot-confirm contract, then integrate one bounded guard that preserves grounded service + `expected_reply_type=time` during pricing interrupts while booking time confirmation is still open.
- Rejected options:
  - weakening scenario/oracle to accept `service_choice` after pricing interrupt;
  - broad rewrite of booking-confirm flow;
  - phrase-fitting `после 15:00` in runtime core.

## Root cause (mandatory)

- Symptom:
  - fresh guarded `dev L2` run `p1.6o6-l2-dev-20260311-a1-r112` no longer stops on `M26`, but the next first strict fail appears in dialog `1`, turn `4`, user text `А сколько это будет стоить?`
- Minimal reproduction:
  1. Inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r112/scenarios.json` dialog `1`, turns `1-4`.
  2. Confirm turn `1` grounds booking service `маникюр`, turn `2` is `ask_about_requested_slot(time)`, and turn `3` fills a lower-bound time constraint (`Мне нужно что-то после 15:00.`) while runtime keeps `expected_reply_type=time` with `slot_confirmation_required=true`.
  3. Inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r112/trace_bundle.jsonl` dialog `1`, turn `3` and confirm `question_contract` matches `datetime` but records `slot_confirmation_required=true`, followed by `question_contract reason=booking_confirm_reject` with `expected_reply_type=time`.
  4. Inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r112/responses.jsonl` dialog `1`, turn `4` and confirm the pricing follow-up ends with `action=reply`, `intent=service_clarify`, `expected_reply_type=service_choice`, `booking_slots={}` and no `pending_question_interaction` / `question_contract expected_reply_type=time` evidence.
  5. Confirm scenario expects pricing info sections plus preserved `reply_type=time`, so strict contract fails with `expected_meta_mismatch` and `expected_trace_miss`.
- Evidence:
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r112/summary.json`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r112/brief.md`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r112/scenarios.json`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r112/responses.jsonl`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r112/trace_bundle.jsonl`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r112/manual_audit.md`
- Five Whys:
  1. Why did the row fail? The pricing interrupt did not preserve the active `time` resume contract.
  2. Why did resume drift? Runtime reopened `expected_reply_type=service_choice` and asked `service_clarify`.
  3. Why did runtime reopen `service_choice`? The pricing path lost the grounded booking service and therefore could not answer price truthfully inside booking interrupt ownership.
  4. Why was service lost? Under `booking_confirm_reject` / `slot_confirmation_required=true`, the pricing follow-up no longer projected the active booking/service context into the pricing owner path.
  5. Why is this a new bounded family? `P1.6o67` froze scenario-governance for comparative time fills, and `P1.6o77` froze info-signal separation, but neither closed the cross-product `booking_confirm_reject + pricing interrupt + grounded service carryover`.
- Root cause statement:
  - pricing interrupt handling under active `time` collect does not preserve grounded service/resume context when the previous time answer is still under booking confirmation (`slot_confirmation_required=true` / `booking_confirm_reject`), so the follow-up pricing turn degrades into `service_clarify` and reopens `service_choice` instead of answering pricing truthfully and preserving `expected_reply_type=time`.
- Fix mechanism:
  - freeze matrix row `M27` for pricing interrupts during booking time confirmation;
  - preserve grounded service carryover and active `time` resume when pricing interrupt runs under `booking_confirm_reject`;
  - add targeted deterministic regressions for `booking -> ask_about_requested_slot -> lower-bound time constraint -> pricing interrupt`.

## Reuse-first decision (mandatory)

- Reuse:
  - existing booking info-interrupt owner
  - existing booking confirmation contract (`slot_confirmation_required`, `booking_confirm_reject`)
  - existing pricing truth-gate / service carryover machinery
- Integrate:
  - one bounded guard that keeps service carryover + `expected_reply_type=time` alive during pricing interrupts while time confirmation is still open
- Do not build:
  - new booking confirmation subsystem
  - new pricing router
  - scenario/oracle waiver for `service_choice`

## Execution profile

- TP mode: `implementation`
- Doc touch budget (files): `10`
- Code dominance: `required`
- Override token: `none`
- Why this profile fits: surfaced family is a bounded runtime owner/carryover drift on one pricing-interrupt row under active time confirmation.

## Invariant

1. `P1.6o77/M26` remains closed.
2. Existing valid pricing/info interrupts outside booking-confirm pending remain intact.
3. Comparative time constraints like `после 15:00` still keep `expected_reply_type=time` until confirmation completes.
4. No oracle weakening to accept `service_choice` on this row.
5. No phrase hardcode in runtime core.

## Scope

- publish surfaced `r112` family as matrix row `M27`
- implement bounded preservation of grounded service + `expected_reply_type=time` for pricing interrupts under active booking time confirmation

## Out of scope

- acceptance lock/replay
- generic booking-confirm refactor
- broad pricing owner redesign
- infra cleanup for tool-evidence gaps or pipeline budget overruns outside this row

## Touch-list

- `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o78-booking-confirm-pricing-interrupt-service-choice-drift-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o60b-remaining-closure-owner-matrix-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o60a-remaining-closure-architecture-verdict-a1.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`
- `truffles-api/app/routers/webhook/decision.py`
- `truffles-api/app/routers/webhook/info.py`
- `truffles-api/tests/test_message_endpoint.py`
- `truffles-api/tests/test_booking_quality_response_guard.py`

## Plan (1..N)

1. Freeze `r112` as audited non-closure evidence and publish `M27`.
2. Trace where pricing interrupt loses booking service/resume while `booking_confirm_reject` is active.
3. Implement a bounded guard that preserves grounded service + `expected_reply_type=time` for this pricing interrupt row.
4. Add targeted deterministic regressions plus one adjacent negative case for genuine service-missing pricing clarifies.
5. Run focused checks and `git diff --check`.
6. Authorize a fresh guarded `dev L2` only after deterministic green.

## DoD

1. `r112` is recorded in active docs as audited non-closure evidence.
2. Pricing interrupt under active booking time confirmation no longer reopens `service_choice` when service is already grounded.
3. Final meta/trace preserve `expected_reply_type=time` and booking/service carryover on this row.
4. Genuine service-missing pricing requests can still use `service_clarify`.
5. Focused deterministic regressions are green.
6. Fresh guarded `dev L2` no longer stops on the `r112` family.

## Checks

- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r112 --status done --strict-artifacts`
- `pytest -q truffles-api/tests/test_message_endpoint.py -k "booking_info_interrupt and booking_confirm"`
- `pytest -q truffles-api/tests/test_booking_quality_response_guard.py -k "service_choice and pricing and booking"`
- `git diff --check`

## Execution status

- `done`
- fresh guarded `dev L2` `p1.6o6-l2-dev-20260311-a1-r112` remains the audited surfaced evidence that opened `M27`
- bounded executable closure is now published through `P1.6o83` + `P1.6o84`
- fresh guarded `dev L2` `p1.6o84-l2-dev-20260312-a1-r2` no longer stops on the pricing interrupt family: grounded service survives pricing/promo/duration interrupts and the run rolls to a different first fail at dialog `1`, turn `8`, user text `Можно записаться на 15:00?`
- remaining lane is now the next surfaced scenario-governance/oracle row, not `M27`

## Evidence

1. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r112/summary.json`
2. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r112/brief.md`
3. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r112/scenarios.json`
4. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r112/responses.jsonl`
5. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r112/trace_bundle.jsonl`
6. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r112/manual_audit.md`
7. `/tmp/booking_quality/p1.6o84-l2-dev-20260312-a1-r2/summary.json`
8. `/tmp/booking_quality/p1.6o84-l2-dev-20260312-a1-r2/brief.md`
9. `/tmp/booking_quality/p1.6o84-l2-dev-20260312-a1-r2/responses.jsonl`
10. `/tmp/booking_quality/p1.6o84-l2-dev-20260312-a1-r2/trace_bundle.jsonl`
11. `/tmp/booking_quality/p1.6o84-l2-dev-20260312-a1-r2/manual_audit.md`
12. `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o77-active-time-daypart-preference-info-signal-false-positive-a1.md`
13. `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o78-booking-confirm-pricing-interrupt-service-choice-drift-a1.md`

## Rollback

1. Keep `P1.6o6` blocked.
2. Revert only the future bounded `M27` delta if it regresses adjacent pricing/info interrupt rows or genuine service-missing clarifies.
3. Do not roll back prior bounded rows (`M26`, `M25`, `M16`).

## No-go

- no oracle weakening to accept `service_choice` on this row
- no phrase-hardcode branch for `после 15:00`
- no changes to unrelated booking-confirm commit/yes-no families
- no broad rewrite of pricing routing outside the bounded confirmation row

## Risks / blockers

- `r112` remains non-closure evidence because `infra_valid=false`, `semantic_valid=false`, and the run still carries unrelated tool-evidence gaps.
- The same service-carryover path is shared by other pricing/info interrupts, so the bounded guard must preserve genuine service-missing clarifies.
- `r110` and `r111` were invalid manual-audit placeholders; they are non-evidence and must not be used for comparison.

## Residual architecture debt (mandatory)

### Current residuals accepted in this block

- Booking confirmation and booking info-interrupt ownership still share cross-cutting state in runtime.
- Tool-evidence / pipeline-latency invalidity remains outside this bounded row.

### Why not in this block

- `M27` is specifically about service carryover + resume preservation during pricing interrupt under booking time confirmation. Broadening into all booking-confirm plumbing would violate bounded-row closure.

### Risk if deferred

- If the closure evidence were not recorded, later work could wrongly reopen `M27` even though the next fresh proof lane already moved past it.

### Linked follow-up Task Package(s)

- `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o85-slot-compare-explicit-time-fill-scenario-governance-a1.md`

### Expiry/trigger to stop deferral

- before any attempt to reopen `M27` without contradicting the fresh `p1.6o84-l2-dev-20260312-a1-r2` evidence bundle.

## Next-block contract (mandatory)

### Next block objective

- publish `P1.6o85` so the newly surfaced explicit exact-time `slot_compare` scenario-governance/oracle drift is closed on deterministic contour before any next guarded rerun.

### First deterministic check command

- `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "slot_compare and explicit_time" && git diff --check`

### Blocked-by conditions

- `P1.6o84` evidence is not synced truthfully into active docs
- deterministic scenario-governance reproduction for the new row is red
- `M27` bounded closure is not recorded before the next row is opened

### Owner role for closure

- `Top Architect + Brain` for proof-lane admission and closure signoff

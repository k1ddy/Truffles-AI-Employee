# TP-2026-03-13-p1.6o126-post-tool-evidence-proof-lane-a1

- Название/цель: после deterministic closure `P1.6o125` выполнить один fresh guarded `dev L2`, чтобы получить admissible proof evidence и truthfully понять, остаётся ли surfaced turn-9 family из `P1.6o124` первым fail на каноничном rerun.

## Canon refs

- Parent closure program: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Main remediation: `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- Process reset / class triage: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o118-class-level-closure-process-reset-a1.md`
- Prior proof lane: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o124-post-service-info-progress-proof-lane-a1.md`
- Prior proof-validity slice: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o125-proof-lane-tool-evidence-fail-fast-validity-a1.md`
- Prior structural slice: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o123-active-service-choice-service-info-progress-scenario-convergence-a1.md`
- State baseline: `STATE.md`

## Git context

- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `origin/main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect` remove branch + worktree after merge

## One web search (mandatory before implementation)

- Query: `site:www.uvicorn.org/settings uvicorn command line host port settings`
- Date/time: `2026-03-13T12:10:00+05:00`
- Sources opened:
  - official Uvicorn settings docs: `https://www.uvicorn.org/settings/`
- Source quality:
  - official product documentation
- Found reusable ideas:
  - proof runtime startup should stay explicit and reproducible through CLI flags;
  - host/port/runtime launch parameters should be recorded outside the app process before the guarded run starts.
- Decision:
  - `reuse`
  - reuse the established live proof-run startup pattern on `:18184` and record `/admin/version == HEAD` before the single guarded rerun.
- Rejected options:
  - detached startup patterns that hide runtime parity;
  - multiple reruns in the same proof block;
  - routing from the stale non-canonical `P1.6o124` artifact.

## Root cause (mandatory)

- Symptom:
  - `P1.6o124` already proved the covered service-info continuity family is no longer first fail, but the run stayed non-canonical because strict `tool_evidence` treated unexecuted booking-tail evidence as missing after fail-fast stop.
- Minimal reproduction:
  1. Open `/tmp/booking_quality/p1.6o124-l2-dev-20260313-a1-r1/summary.json`.
  2. Confirm the run stopped at `max_failures_reached:1` with `artifact_integrity.valid=true` but `infra_valid=false`, `semantic_valid=false`, and `run_integrity_reasons=["run_completion_gap"]`.
  3. Confirm `P1.6o125` is now green on deterministic contour and its tests prove fail-fast prefixes without observed booking-tool opportunity no longer become false `infra_invalid`.
  4. Start a live runtime whose `/admin/version` matches current `HEAD`.
  5. Run one fresh guarded `dev L2` and strict audit it.
  6. Truthfully classify the first surfaced result under `P1.6o118` rules.
- Evidence:
  - `/tmp/booking_quality/p1.6o124-l2-dev-20260313-a1-r1/summary.json`
  - `/tmp/booking_quality/p1.6o124-l2-dev-20260313-a1-r1/brief.md`
  - `/tmp/booking_quality/p1.6o124-l2-dev-20260313-a1-r1/manual_audit.md`
  - `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o125-proof-lane-tool-evidence-fail-fast-validity-a1.md`
- Five Whys:
  1. Why is closure still incomplete after `P1.6o125`? Because the worktree still lacks one fresh admissible proof rerun after the proof-validity blocker was removed.
  2. Why can't `P1.6o124` itself decide the next class? Because that run remained non-canonical.
  3. Why can we rerun now? Because `P1.6o125` closed the bounded proof-validity blocker deterministically.
  4. Why must runtime be fingerprinted first? Because proof evidence is admissible only if the live runtime matches current `HEAD`.
  5. Why only one rerun? Because the goal is truthful class admission, not another expensive replay loop.
- Root cause statement:
  - after `P1.6o125`, the remaining gap is proof admissibility, not local implementation: one fresh fingerprint-verified guarded rerun is needed to determine whether the surfaced turn-9 family is still first fail on canonical evidence.
- Fix mechanism:
  - start a live worktree runtime on `:18184` and prove `/admin/version == HEAD`;
  - execute exactly one guarded `dev L2` rerun and strict audit it;
  - route the surfaced result by `P1.6o118` class triage instead of automatic row admission.

## Reuse-first decision (mandatory)

- Reuse:
  - existing guarded entrypoint `scripts/llm_quality_guarded.sh`
  - existing audit command `python3 ops/diagnose.py llm-quality-audit`
  - existing live runtime startup pattern used in prior proof-lane blocks
- Integrate:
  - no runtime/generator code changes unless the rerun surfaces a new deterministic or bounded family
- Build:
  - none planned in this block

## Invariant

1. `P1.6o123`, `P1.6o124`, and `P1.6o125` truth stays intact.
2. No new row-local `Mxx -> child TP` admission happens without `P1.6o118` class triage.
3. Proof evidence is accepted only from the fresh fingerprint-verified rerun and strict audit artifacts.
4. Oracle/gates are not weakened to manufacture a green result.

## Scope

- Refresh the live worktree runtime on `127.0.0.1:18184`.
- Verify `/admin/version.git_commit == HEAD`.
- Execute exactly one guarded `dev L2` proof rerun after `P1.6o125`.
- Strictly audit the run.
- Truthfully sync docs/state/session and publish the next admissible structural block only if the surfaced class requires it.

## Out of scope

- new runtime/scenario/oracle code changes before the proof rerun surfaces a new class
- acceptance `lock/replay/full`
- new matrix-row admission without class-level routing

## Touch-list

- `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o126-post-tool-evidence-proof-lane-a1.md`
- `STATE.md`
- `STRUCTURE.md`
- `docs/SESSIONS/SESSION-2026-02-19-llm-first-firebreak-a1.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `/tmp/booking_quality/p1.6o126-l2-dev-20260313-a1-r1/**`

## Plan

1. Reconfirm `P1.6o125` deterministic closure and sync active docs/state/session.
2. Start a live worktree runtime on `127.0.0.1:18184` and prove `/admin/version.git_commit == HEAD`.
3. Run exactly one guarded `dev L2` proof lane as `p1.6o126-l2-dev-20260313-a1-r1`.
4. Strictly audit the run and inspect the first fail.
5. Classify the surfaced result using `P1.6o118` rules.
6. Sync truth into docs/state/session and publish the next admissible block only if the surfaced class requires it.

## DoD

1. Exactly one fresh guarded `dev L2` run is executed after deterministic closure of `P1.6o125`.
2. Live runtime fingerprint parity with current `HEAD` is recorded before the run.
3. Strict audit completes for `/tmp/booking_quality/p1.6o126-l2-dev-20260313-a1-r1`.
4. Active docs/state/session record whether the surfaced turn-9 family remains first fail on canonical proof evidence.
5. Any surfaced next step is routed by class-level triage, not by automatic row admission.

## Checks

- `git rev-parse HEAD`
- `curl -sf http://127.0.0.1:18184/admin/version`
- `TEST_MODE=1 scripts/llm_quality_guarded.sh --mode lock --run-id p1.6o126-l2-dev-20260313-a1-r1 --allow-pending-previous --allow-repeat-fingerprint -- --base-url http://127.0.0.1:18184 --client-slug demo_salon --mode llm --count 10 --min-turns 10 --max-turns 15 --include-media --scenario-coverage booking,info,interrupt,handoff --tool-hooks auto --jid-mode unique --allow-non-allowlist --judge-mode all --quality-lane dev --run-economy-gate block --fail-on-thresholds --max-failures 1 --reset-before-dialog`
- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o126-l2-dev-20260313-a1-r1 --status done --strict-artifacts`
- `git diff --check`
- `SESSION_AGENT=a1 scripts/session_check.sh`

## Execution status

- `done`
- `P1.6o126` completed with one fresh guarded `dev L2` `p1.6o126-l2-dev-20260313-a1-r1` on fingerprint-verified runtime, restored `infra_valid=true`, proved the `P1.6o124` tool-evidence blocker is gone, and truthfully surfaced runtime owner/resume drift at the pending/handoff transport boundary on `dialog 1 / turn 8 / "Какой у вас прайс на маникюр?"`.

## Evidence

1. `/tmp/booking_quality/p1.6o124-l2-dev-20260313-a1-r1/summary.json`
2. `/tmp/booking_quality/p1.6o124-l2-dev-20260313-a1-r1/brief.md`
3. `/tmp/booking_quality/p1.6o124-l2-dev-20260313-a1-r1/manual_audit.md`
4. `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o125-proof-lane-tool-evidence-fail-fast-validity-a1.md`

## Rollback

1. Stop the local runtime and keep `P1.6o125` deterministic closure intact.
2. If the rerun is invalid/incomplete, record it truthfully and do not reinterpret `P1.6o124`.
3. Do not mutate the matrix or docs to hide surfaced evidence.

## No-go

- no second expensive run in this block
- no new implementation before the proof rerun surfaces the next class
- no reuse of stale non-canonical `P1.6o124` evidence as closure proof
- no automatic `Mxx -> child TP` admission without `P1.6o118` class triage

## Risks / blockers

- local runtime may fail fingerprint validation or die before the guarded rerun starts
- the rerun may still stay semantic-red because of inherited `hardcode_core_violation` even if infra validity is restored
- the next surfaced result may expose broader runtime owner-coverage or scenario-governance debt, which must open a structural continuation rather than a local patch

## Residual architecture debt (mandatory)

### Current residuals accepted in this block

- surfaced semantic family from `P1.6o124` remains unadmitted until canonical proof rerun confirms or displaces it
- inherited `hardcode_core_violation` remains outside this proof-only block
- acceptance proof (`lock/replay/full`) remains blocked

### Why not in this block

- this block is proof-only; it determines the next remaining class on admissible evidence

### Risk if deferred

- the team can keep routing from stale non-canonical evidence instead of fresh proof

### Linked follow-up Task Package(s)

- one structural continuation selected by `P1.6o118` after truthful `P1.6o126` class triage
- later hardcode-core cleanup block if the inherited gate remains active on the selected lane

### Expiry/trigger to stop deferral

- before any further guarded `dev/acceptance` rerun beyond `P1.6o126`

## Next-block contract (mandatory)

### Next block objective

- truthfully sync the surfaced `P1.6o126` result and land one bounded runtime-owner slice that preserves booking follow-up continuity across pending/handoff transport re-entry before the next proof rerun.

### First deterministic check command

- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o126-l2-dev-20260313-a1-r1 --status done --strict-artifacts && git diff --check`

### Blocked-by conditions

- `P1.6o126` truthful proof result is not synced truthfully in active docs/state/session
- the surfaced runtime-owner class is misread as pure oracle drift or an automatic row-local child
- the team attempts another expensive rerun before processing the surfaced `P1.6o126` result structurally

### Owner role for closure

- `Top Architect + Brain`

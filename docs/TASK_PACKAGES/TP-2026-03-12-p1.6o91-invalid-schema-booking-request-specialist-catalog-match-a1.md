# TP-2026-03-12-p1.6o91-invalid-schema-booking-request-specialist-catalog-match-a1

- Название/цель: boundedly re-close reopened `M19` after `P1.6o90`: when active booking `time` collect hits `policy_error:invalid_schema` on named-specialist booking request `Можно записаться к Айгерим?`, runtime must recover `booking_specialist_followup` via branch-catalog specialist match even if the secondary specialist-hint LLM returns empty.

## Canon refs

- Parent closure program: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Main remediation: `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- Fresh proof admission that reopened the family: `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o90-post-m30-proof-lane-a1.md`
- Prior bounded row closure: `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o70-invalid-schema-booking-request-specialist-followup-a1.md`
- Owner matrix baseline: `truffles-api/app/knowledge/generic/INTERACTION_OWNER_MATRIX.yaml`
- State baseline: `STATE.md`

## Git context

- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect` remove branch + worktree after merge

## One web search (mandatory before implementation)

- Query: `site:docs.sqlalchemy.org SQLAlchemy ilike case insensitive string matching ORM`
- Date/time: `2026-03-12T12:54:17+05:00`
- Sources opened:
  - official SQLAlchemy operator reference: `https://docs.sqlalchemy.org/en/20/core/operators.html#sqlalchemy.sql.operators.ColumnOperators.ilike`
- Source quality:
  - official framework documentation
- Found reusable ideas:
  - bounded case-insensitive matching should stay explicit and data-driven;
  - dialect-specific DB operators exist, but small branch-scoped catalogs can be matched deterministically in normalized Python space without another LLM hop.
- Decision:
  - `integrate/build`
  - reuse branch specialist data as the semantic boundary source of truth and build a tiny normalized catalog matcher in the existing specialist-hint resolver before the secondary LLM.
- Rejected options:
  - expanding phrase-specific regexes in the degraded fallback;
  - adding another expensive LLM retry to recover a specialist name already present in branch data;
  - DB-dialect-specific substring heuristics for a tiny branch catalog.

## Root cause (mandatory)

- Symptom:
  - fresh guarded `dev L2` `p1.6o90-l2-dev-20260312-a1-r1` rolled past `M30`, but failed first on dialog `1`, turn `5`, user text `Можно записаться к Айгерим?`.
- Minimal reproduction:
  1. Start booking and keep `expected_reply_type=time` active with grounded service.
  2. Trigger `llm_policy_core` non-retryable `invalid_schema` on a named-specialist booking request.
  3. Observe `specialist_hint` returns `empty` and runtime falls back to generic `booking_prompt`.
  4. Contract-first evaluation fails because `pending_question_target=specialist`, `active_question_relation=referent_followup`, and `booking_specialist_followup` evidence are missing.
- Evidence:
  - `/tmp/booking_quality/p1.6o90-l2-dev-20260312-a1-r1/summary.json`
  - `/tmp/booking_quality/p1.6o90-l2-dev-20260312-a1-r1/brief.md`
  - `/tmp/booking_quality/p1.6o90-l2-dev-20260312-a1-r1/responses.jsonl`
  - `/tmp/booking_quality/p1.6o90-l2-dev-20260312-a1-r1/trace_bundle.jsonl`
  - `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o70-invalid-schema-booking-request-specialist-followup-a1.md`
- Five Whys:
  1. Why did `P1.6o90` fail first? Because invalid-schema recovery returned a generic `booking_prompt` instead of `booking_specialist_followup`.
  2. Why did recovery miss the specialist? Because the bounded follow-up recovery depended on secondary specialist-hint extraction for this surface form.
  3. Why was the secondary hint insufficient? Because the specialist-hint LLM returned `empty` on `Можно записаться к Айгерим?` even though the branch catalog contains `Айгерим`.
  4. Why did runtime not recover from that empty hint? Because there was no deterministic branch-catalog specialist match in the boundary resolver before falling back to generic collect.
  5. Why is that a contract breach? Because the family was already predicted by `M19`: invalid-schema booking-request specialist follow-up must preserve `pending_question_target=specialist`, `active_question_relation=referent_followup`, and resume `time`.
- Root cause statement:
  - `P1.6o70` closed only the secondary-hint path of `M19`; the runtime still lacked a deterministic branch-catalog specialist match in `_resolve_specialist_name_hint_with_trace`, so the same family reopened on a nearby booking-request surface where the secondary hint LLM returned empty.
- Fix mechanism:
  - add bounded branch-catalog specialist matching before the secondary specialist-hint LLM;
  - stamp explicit `specialist_hint_source=branch_catalog` trace/meta provenance when that deterministic match fires;
  - cover the reopened `Можно записаться к Айгерим?` variant with targeted runtime tests;
  - re-close `M19` truthfully on deterministic contour and publish the next proof lane.

## Reuse-first decision (mandatory)

- Reuse:
  - existing `booking_specialist_followup` owner
  - existing invalid-schema degraded recovery path in `truffles-api/app/routers/webhook/decision.py`
  - branch specialist catalog already stored in runtime data
- Integrate:
  - resolve specialist preference from active branch data before secondary LLM hint extraction
- Build:
  - no new subsystem; only a bounded resolver addition and targeted regressions

## Release safety

- Rollout: local runtime/runtime-tests only
- Go signal:
  - invalid-schema booking-request follow-up recovers to `booking_specialist_followup` via branch catalog without calling the secondary LLM
  - targeted runtime tests are green
- No-go signal:
  - generic `booking_prompt` still drops specialist follow-up axes
  - fix requires phrase-hardcode instead of branch-data matching
- Rollback:
  - revert the bounded branch-catalog specialist match and keep `M19` reopened truthfully

## Invariant

1. `M30` stays cleared by `P1.6o89` + `P1.6o90`.
2. `M19` must preserve `pending_question_target=specialist`, `active_question_relation=referent_followup`, and resume `time` under invalid-schema degrade.
3. No new semantic hardcode in default runtime path.
4. Secondary LLM hint remains available as fallback; deterministic catalog match only short-circuits when branch data yields one explicit specialist.

## Scope

- bounded runtime recovery for reopened `M19`
- targeted runtime tests for branch-catalog specialist match
- matrix/docs/state/session sync

## Out of scope

- new expensive guarded run in this block
- acceptance `lock/replay/full`
- broader refactor of specialist extraction architecture

## Touch-list

- `truffles-api/app/routers/webhook/decision.py`
- `truffles-api/tests/test_message_endpoint.py`
- `truffles-api/tests/test_booking_quality_response_guard.py`
- `truffles-api/app/knowledge/generic/INTERACTION_OWNER_MATRIX.yaml`
- `truffles-api/tests/test_interaction_owner_matrix_contract.py`
- `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o90-post-m30-proof-lane-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o91-invalid-schema-booking-request-specialist-catalog-match-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o92-post-m19-reclosure-proof-lane-a1.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `STATE.md`
- `STRUCTURE.md`
- `docs/SESSIONS/SESSION-2026-02-19-llm-first-firebreak-a1.md`

## Plan (1..N)

1. Publish `P1.6o91` as the bounded re-closure block for reopened `M19`.
2. Add deterministic branch-catalog specialist matching before secondary hint extraction.
3. Add targeted runtime regressions for the reopened phrase variant and the direct helper path.
4. Sync matrix/docs/state/session and publish the next proof lane `P1.6o92`.

## DoD

1. Reopened `M19` is deterministically re-closed on runtime contour.
2. Invalid-schema booking-request specialist follow-up preserves `booking_specialist_followup` via branch catalog when the specialist is explicitly named.
3. Targeted runtime/guard/matrix checks are green.
4. `P1.6o92` is published as the next admissible proof lane.

## Checks

- `pytest -q truffles-api/tests/test_message_endpoint.py -k "branch_catalog_match or invalid_schema_question_like_named_specialist_followup_keeps_time_collect"`
- `pytest -q truffles-api/tests/test_booking_quality_response_guard.py -k "invalid_schema_specialist_followup or specialist_followup_owner_gap"`
- `pytest -q truffles-api/tests/test_interaction_owner_matrix_contract.py`
- `python3 -m py_compile truffles-api/app/routers/webhook/decision.py truffles-api/tests/test_message_endpoint.py`
- `git diff --check`
- `SESSION_AGENT=a1 scripts/session_check.sh`

## Execution status

- `done`

## Evidence

1. `/tmp/booking_quality/p1.6o90-l2-dev-20260312-a1-r1/summary.json`
2. `/tmp/booking_quality/p1.6o90-l2-dev-20260312-a1-r1/brief.md`
3. `/tmp/booking_quality/p1.6o90-l2-dev-20260312-a1-r1/responses.jsonl`
4. `/tmp/booking_quality/p1.6o90-l2-dev-20260312-a1-r1/trace_bundle.jsonl`
5. `pytest -q truffles-api/tests/test_message_endpoint.py -k "branch_catalog_match or invalid_schema_question_like_named_specialist_followup_keeps_time_collect"` -> `3 passed`
6. `pytest -q truffles-api/tests/test_booking_quality_response_guard.py -k "invalid_schema_specialist_followup or specialist_followup_owner_gap"` -> `1 passed`
7. updated matrix/docs/state/session references

## Rollback

1. Remove the deterministic branch-catalog specialist match from `_resolve_specialist_name_hint_with_trace`.
2. Restore `M19` to reopened/open truth in docs and matrix if deterministic evidence no longer holds.

## No-go

- no new expensive run in this block
- no phrase-hardcode for `Можно записаться ...`
- no weakening of the `booking_specialist_followup` contract to accept generic `booking_prompt`

## Risks / blockers

- branch catalog may be unavailable in tests or runtime if branch context is missing
- multi-specialist name ambiguity must remain unresolved rather than guessed

## Residual architecture debt (mandatory)

### Current residuals accepted in this block

- proof of reopened `M19` re-closure still requires one fresh guarded `dev L2`
- specialist recovery is still implemented inside `decision.py`, not yet migrated into a dedicated owner handler

### Why not in this block

- this block only re-closes the reopened runtime family on deterministic contour

### Risk if deferred

- closure lane stays stuck on a reopened family without truthful re-admission

### Linked follow-up Task Package(s)

- `P1.6o92`
- later owner-handler extraction in the executable interaction-core lane

### Expiry/trigger to stop deferral

- before any further guarded `dev/acceptance` rerun beyond `P1.6o92`

## Next-block contract (mandatory)

### Next block objective

- run one fresh guarded `dev L2` after `P1.6o91` to prove reopened `M19` is no longer first fail and truthfully admit the next family, if any.

### First deterministic check command

- `pytest -q truffles-api/tests/test_message_endpoint.py -k "branch_catalog_match or invalid_schema_question_like_named_specialist_followup_keeps_time_collect" && pytest -q truffles-api/tests/test_booking_quality_response_guard.py -k "invalid_schema_specialist_followup or specialist_followup_owner_gap" && pytest -q truffles-api/tests/test_interaction_owner_matrix_contract.py && git diff --check`

### Blocked-by conditions

- `P1.6o91` deterministic closure is not synced truthfully into active docs and owner-matrix state
- targeted runtime evidence is not green
- live runtime fingerprint for the next proof lane is not verified

### Owner role for closure

- `Top Architect + Brain`

# TP-2026-03-07-p1.6o3-referent-resolver-capability-arbitration-a1

- Название/цель: добавить referent resolver и capability arbitration поверх canonical dialog state, чтобы деиктические follow-up turns выбирали `fact/tool/clarify/handoff` по контракту, а не деградировали в `booking_prompt`; этот блок закрывает family `P1.6n`.
- Parent TP: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Canon refs: `AGENTS.md`, `STATE.md`, `STRUCTURE.md`, `STRATEGY/REQUIREMENTS.md`, `TECH.md`, `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`, `docs/TASK_PACKAGES/TP-2026-03-06-p1.6-local-realism-canonical-lock-a1.md`, `docs/TASK_PACKAGES/TP-2026-03-07-p1.6n-weekend-availability-service-referent-arbitration-a1.md`
- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`

## One web search (mandatory before implementation)

- Query: `site:rasa.com/docs requested_slot unhappy paths forms official`
- Time (UTC): `2026-03-07T11:06:42Z`
- Sources:
  - `https://rasa.com/docs/reference/primitives/slots/`
  - `https://legacy-docs-oss.rasa.com/docs/rasa/forms/`
- Ready solutions found:
  - slots are the assistant memory and should preserve resolved context across turns instead of rebuilding it from the latest utterance
  - unhappy-path handling should branch explicitly while keeping the active requested subject/slot visible, then resume or clarify rather than silently dropping context
- Decision (`reuse/integrate/build`): `integrate + build`
  - integrate canonical dialog state as the one active referent store and reuse it before any per-turn fallback reconstruction
  - build a narrow capability arbitration layer that maps the semantic envelope to `policy_fact`, `live_calendar`, `clarify_missing_subject`, or `handoff` with traceable evidence
- Rejected options:
  - phrase-specific runtime branches for `на выходные` / `по выходным`
  - leaving referent recovery to ad-hoc booking/info fallbacks
  - starting guarded realism before deterministic resolver/arbitration closure

## Root cause (mandatory)

- Symptom:
  - current service follow-up `Можно ли записаться на эту услугу на выходные?` loses the service referent and degrades to generic booking collect.
- Minimal reproduction:
  1. inspect `/tmp/booking_quality/p1.6-l2-dev-seed42-r22/responses.jsonl`
  2. inspect `/tmp/booking_quality/p1.6-l2-dev-seed42-r22/trace_bundle.jsonl`
  3. inspect `truffles-api/app/routers/webhook/decision.py` around consult-return and booking prompt arbitration
  4. inspect current info/runtime routing after service-carryover injection
- Evidence:
  - `docs/TASK_PACKAGES/TP-2026-03-07-p1.6n-weekend-availability-service-referent-arbitration-a1.md`
  - `/tmp/booking_quality/p1.6-l2-dev-seed42-r22/manual_audit.md`
- Five Whys:
  1. Why does runtime ask for service again: it cannot recover the current referent.
  2. Why can it not recover it: no dedicated referent resolver consumes canonical state.
  3. Why does path choice drift: the runtime does not classify capability questions by contract.
  4. Why is that important: `hours/policy/calendar/clarify` are distinct sources and outcomes.
  5. Why is this systemic: any deictic or capability-bearing question will repeat the same failure shape.
- Root cause statement:
  - runtime lacks a generic referent resolver and capability arbitration layer, so turns that depend on current object continuity and source-of-truth choice degrade into generic collect or ambiguous fallback paths.
- Fix mechanism:
  - resolve implicit referent from canonical state;
  - map capability questions to `static fact`, `live calendar`, `clarify`, or `handoff` by explicit contract;
  - close `P1.6n` without phrase-specific handlers.

## Invariant

1. Resolver must be generic, not phrase-specific.
2. Clarify is allowed only when referent/capability remains unresolved after contract resolution.
3. Existing fact/tool boundaries must remain traceable and fail-closed.

## Scope

- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6n-weekend-availability-service-referent-arbitration-a1.md`
- `truffles-api/app/routers/webhook/decision.py`
- `truffles-api/app/routers/webhook/info.py`
- `truffles-api/app/routers/webhook/response.py`
- `truffles-api/app/services/pack_runtime_service.py`
- `truffles-api/app/services/tool_registry_service.py`
- `ops/diagnose.py`
- `truffles-api/tests/test_message_endpoint.py`
- `truffles-api/tests/test_booking_quality_response_guard.py`
- `truffles-api/tests/test_demo_salon_eval.py`
- `truffles-api/tests/test_pack_runtime_service.py`

## Out of scope

- full `_legacy` removal
- full oracle realignment
- expensive proof bundle

## Plan (1..N)

1. Define referent-resolution contract from canonical state.
2. Define capability arbitration contract (`fact` vs `calendar` vs `clarify` vs `handoff`) with explicit trace/meta evidence requirements.
3. Implement the active `demo_salon` runtime path using those contracts.
4. Add deterministic regressions that close `P1.6n` and adjacent deictic/capability variants.

## DoD

1. Current-service deictic follow-ups resolve the current referent from canonical state.
2. Capability questions choose the correct source-of-truth path by contract.
3. `P1.6n` closes on deterministic contour without phrase hardcode.
4. Trace/meta prove the resolved referent, referent source, capability class, and chosen resolution mode.

## Checks

- `pytest -q truffles-api/tests/test_pack_runtime_service.py::test_capability_question_contract_selects_fact_for_hours_scope`
- `pytest -q truffles-api/tests/test_message_endpoint.py::test_weekend_availability_followup_uses_current_service_referent`
- `pytest -q truffles-api/tests/test_message_endpoint.py::test_deictic_capability_question_clarifies_when_referent_missing`
- `pytest -q truffles-api/tests/test_booking_quality_response_guard.py::test_weekend_availability_requires_resolved_referent_trace`
- `pytest -q truffles-api/tests/test_message_endpoint.py`
- `pytest -q truffles-api/tests/test_demo_salon_eval.py`
- `pytest -q truffles-api/tests/test_booking_quality_response_guard.py`
- `pytest -q truffles-api/tests/test_booking_chaos_dialogs.py`
- `ruff check truffles-api/app/routers/webhook/decision.py truffles-api/app/routers/webhook/info.py truffles-api/app/routers/webhook/response.py truffles-api/app/services/pack_runtime_service.py truffles-api/app/services/tool_registry_service.py truffles-api/tests/test_message_endpoint.py truffles-api/tests/test_demo_salon_eval.py truffles-api/tests/test_pack_runtime_service.py`
- `git diff --check`

## Execution status

- `implemented; deterministic contour green`
- `P1.6o3` now persists capability contract + referent resolver evidence in runtime meta/trace
- deictic current-service follow-up resolves canonical referent instead of degrading into generic collect
- missing service referent for live-availability questions now fails closed into explicit service collect with `clarify_missing_subject` evidence
- no fresh guarded `dev L2` run in this block; next system-level proof remains blocked on `P1.6o4` + `P1.6o5`

## Evidence

1. `docs/TASK_PACKAGES/TP-2026-03-07-p1.6n-weekend-availability-service-referent-arbitration-a1.md`
2. `/tmp/booking_quality/p1.6-l2-dev-seed42-r22/manual_audit.md`
3. deterministic exact checks:
   - `pytest -q truffles-api/tests/test_pack_runtime_service.py::test_capability_question_contract_selects_fact_for_hours_scope`
   - `pytest -q truffles-api/tests/test_message_endpoint.py::test_weekend_availability_followup_uses_current_service_referent`
   - `pytest -q truffles-api/tests/test_message_endpoint.py::test_deictic_capability_question_clarifies_when_referent_missing`
   - `pytest -q truffles-api/tests/test_booking_quality_response_guard.py::test_weekend_availability_requires_resolved_referent_trace`
4. broader deterministic contour:
   - `pytest -q truffles-api/tests/test_pack_runtime_service.py`
   - `pytest -q truffles-api/tests/test_booking_quality_response_guard.py`
   - `pytest -q truffles-api/tests/test_message_endpoint.py`
   - `pytest -q truffles-api/tests/test_demo_salon_eval.py`
   - `pytest -q truffles-api/tests/test_booking_chaos_dialogs.py`
   - `ruff check ...`
   - `git diff --check`

## Rollback

1. revert only resolver/arbitration changes from this block
2. retain canonical state and semantic envelope work from prior blocks

## No-go

- no phrase hardcodes for `на выходные` and variants
- no direct fallback to `booking_prompt` when current referent is available
- no expensive run before deterministic closure
- no grep-style `-k` selector as primary closure evidence for this block

## Risks/Blockers

1. Capability taxonomy can drift if `P1.6o5` is not aligned quickly after implementation.
2. Over-eager referent reuse can incorrectly attach state to unrelated turns.

## Residual architecture debt (mandatory)

- Current residuals accepted in this block:
  - `_legacy` path still exists outside the active path
  - oracle realignment still pending
- Why not in this block:
  - resolver/arbitration should close active runtime behavior first
- Risk if deferred:
  - even a correct resolver may still be judged by an ambiguous oracle
- Linked follow-up Task Package(s):
  - `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o4-runtime-ownership-cleanup-backedge-reduction-a1.md`
  - `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o5-oracle-acceptance-contract-realignment-a1.md`
- Expiry/trigger to stop deferral:
  - before fresh guarded `dev L2`

## Next-block contract (mandatory)

- Next block objective:
  - execute `P1.6o4` and `P1.6o5` so the repaired runtime path has one owner path and one correct acceptance oracle before the fresh guarded `dev L2`
- First deterministic check command:
  - `pytest -q truffles-api/tests/test_message_endpoint.py::test_weekend_availability_followup_uses_current_service_referent && pytest -q truffles-api/tests/test_message_endpoint.py::test_deictic_capability_question_clarifies_when_referent_missing`
- Blocked-by conditions:
  - `P1.6o1` and `P1.6o2` not green
  - canonical referent resolver not implemented
- Owner role for closure:
  - `Top Architect + Brain`

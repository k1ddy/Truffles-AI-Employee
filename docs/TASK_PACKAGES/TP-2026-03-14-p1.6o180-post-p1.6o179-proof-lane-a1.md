# TP-2026-03-14-p1.6o180-post-p1.6o179-proof-lane-a1

- Название/цель: выполнить один свежий guarded `dev L2` rerun после детерминированного закрытия `P1.6o179`, чтобы доказать, что surfaced timeout completed-booking family на `"Меня зовут Лена."` ушла с first fail и truthfully surfaced следующий admissible result.

## Canon refs

- Binding roadmap: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o135-beauty-salon-canonical-control-plane-roadmap-a1.md`
- Prior deterministic closure: `docs/TASK_PACKAGES/TP-2026-03-14-p1.6o179-timeout-completed-booking-name-fill-continuity-a1.md`
- Prior proof lane: `docs/TASK_PACKAGES/TP-2026-03-14-p1.6o178-post-p1.6o177-proof-lane-a1.md`
- Class-level routing: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o118-class-level-closure-process-reset-a1.md`
- Parent closure program: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Main remediation: `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- State baseline: `STATE.md`

## Git context

- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `origin/main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect` remove branch + worktree after merge

## One web search (mandatory before implementation)

- Not applicable in this block: proof-only continuation, no implementation.
- Decision:
  - `reuse`
  - reuse the existing guarded `dev L2` proof contour and strict artifact audit to determine whether the covered `P1.6o179` timeout completed-booking family moved off first fail.

## Root cause (mandatory)

- Symptom:
  - `P1.6o179` is deterministically green, but there is still no fresh proof showing the covered timeout completed-booking family moved off first fail.
- Minimal reproduction:
  1. finish `P1.6o179` deterministic closure;
  2. verify `/admin/version` on `:18184` matches current `HEAD`;
  3. execute one fresh guarded `dev L2` rerun with explicit `--quality-lane dev` and `--allow-pending-previous` because the previous proof lock run remains intentionally non-canonical after fail-fast;
  4. strict-audit the run and determine the next admissible family or blocker.
- Evidence:
  - `docs/TASK_PACKAGES/TP-2026-03-14-p1.6o179-timeout-completed-booking-name-fill-continuity-a1.md`
  - `/tmp/booking_quality/p1.6o178-l2-dev-20260314-a1-r1/summary.json`
  - `/tmp/booking_quality/p1.6o178-l2-dev-20260314-a1-r1/manual_audit.json`
- Five Whys:
  1. Why is another proof block needed? Because deterministic closure alone does not prove the expensive lane moved beyond the surfaced timeout completed-booking family.
  2. Why not route a new class from `P1.6o179` without rerun? Because we need a fresh strict-audited result after the code change.
  3. Why reuse the same proof contour? Because parity drift would make the result non-comparable.
  4. Why `--allow-pending-previous`? Because the latest lock-mode run remains intentionally fail-fast/non-canonical even though manual audit is complete.
  5. Why one run only? Because this block is proof/classification, not another expensive loop.
- Root cause statement:
  - after `P1.6o179`, the missing evidence is one fresh strict-audited rerun showing whether the surfaced timeout completed-booking family moved off first fail.
- Fix mechanism:
  - verify runtime parity, run one guarded `dev L2`, strict-audit it, and select the next continuation only from the admissible result.

## Reuse-first decision (mandatory)

- Reuse:
  - existing `scripts/llm_quality_guarded.sh` proof contour
  - existing strict audit command
  - live runtime on `:18184`
- Integrate:
  - no code changes in this block
- Build:
  - none

## Release safety

- Rollout: none; proof-only block
- Go signal:
  - `/admin/version.git_commit == HEAD`
  - strict audit completes for the fresh rerun
- No-go signal:
  - runtime parity mismatch
  - preflight invalid
  - strict audit missing or artifact bundle incomplete
- Rollback:
  - no runtime/code rollback in this block; only stop after audit and publish truthful routing

## Invariant

1. `P1.6o179` deterministic closure remains intact.
2. No new runtime/oracle patch is allowed before the rerun result is audited.
3. Proof stays on a fingerprint-verified runtime on `:18184`.
4. Evidence remains contract-first and class-routed.

## Scope

- runtime parity check on `:18184`
- one fresh guarded `dev L2`
- strict audit of the run
- admissibility/class routing from the surfaced result
- truthful docs/state/session sync

## Out of scope

- new structural runtime/oracle changes before proof result
- lock/replay/full acceptance bundle
- baseline updates
- scenario regeneration changes beyond the single run

## Touch-list

- `docs/TASK_PACKAGES/TP-2026-03-14-p1.6o179-timeout-completed-booking-name-fill-continuity-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-14-p1.6o180-post-p1.6o179-proof-lane-a1.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `STATE.md`
- `docs/SESSIONS/SESSION-2026-02-19-llm-first-firebreak-a1.md`
- `docs/SESSION_INDEX.md`
- `/tmp/booking_quality/p1.6o180-l2-dev-20260314-a1-r1/**`

## Plan

1. Verify runtime parity on `:18184`.
2. Execute exactly one fresh guarded `dev L2` rerun with `--allow-pending-previous`.
3. Strict-audit the run.
4. Determine whether semantic class routing is now admissible.
5. Sync truth and publish the next admissible continuation.

## DoD

1. A fresh guarded rerun exists under `/tmp/booking_quality/p1.6o180-l2-dev-20260314-a1-r1`.
2. Strict audit records `artifact_integrity.valid=true`.
3. `P1.6o179` is proven off first fail or a new blocker/class is truthfully documented.
4. Active docs/state/session show `P1.6o180` as the proof checkpoint after `P1.6o179`.

## Checks

- `python3 - <<'PY'
import json, subprocess, urllib.request
head = subprocess.check_output(['git', 'rev-parse', 'HEAD'], text=True).strip()
with urllib.request.urlopen('http://127.0.0.1:18184/admin/version', timeout=10) as r:
    runtime = json.load(r).get('git_commit')
print({'head': head, 'runtime': runtime, 'match': head == runtime})
PY`
- `scripts/llm_quality_guarded.sh --mode lock --run-id p1.6o180-l2-dev-20260314-a1-r1 --allow-pending-previous -- --base-url http://127.0.0.1:18184 --client-slug demo_salon ... --quality-lane dev`
- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o180-l2-dev-20260314-a1-r1 --status done --strict-artifacts`
- `git diff --check`

## Evidence

1. `/tmp/booking_quality/p1.6o180-l2-dev-20260314-a1-r1/summary.json`
2. `/tmp/booking_quality/p1.6o180-l2-dev-20260314-a1-r1/brief.md`
3. `/tmp/booking_quality/p1.6o180-l2-dev-20260314-a1-r1/scenarios.json`
4. `/tmp/booking_quality/p1.6o180-l2-dev-20260314-a1-r1/responses.jsonl`
5. `/tmp/booking_quality/p1.6o180-l2-dev-20260314-a1-r1/trace_bundle.jsonl`
6. `/tmp/booking_quality/p1.6o180-l2-dev-20260314-a1-r1/manual_audit.json`

## Rollback

1. No code rollback in this proof-only block.
2. If parity or audit is invalid, stop and keep `P1.6o179` as the active deterministic closure with no new semantic class routing.

## No-go

- no structural patching before strict audit
- no baseline update from this run
- no acceptance claims from a `dev` rerun

## Risks / blockers

- another proof-validity blocker may still keep the rerun non-admissible
- runtime instability can still keep the run non-canonical for closure/baseline
- next admissible family may move into a different slice once this timeout family is removed

## Residual architecture debt (mandatory)

### Current residuals accepted in this block

- proof remains non-canonical until a later full closure bundle exists
- timeout boundary continuity still converges slice by slice instead of from one generated contract source

### Why not in this block

- this block is proof-only and exists to verify the blocker removal

### Risk if deferred

- the worktree could drift into unsanctioned new fixes without proof that `P1.6o179` actually moved the surfaced family off first fail

### Linked follow-up Task Package(s)

- next TP selected only after `P1.6o180` strict audit

### Expiry/trigger to stop deferral

- immediately after `P1.6o180` finishes and the next blocker/family is known

## Next-block contract (mandatory)

### Next block objective

- publish the next bounded structural block from the surfaced `P1.6o180` admissible result, or truthfully record that proof remains blocked.

### First deterministic check command

- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o180-l2-dev-20260314-a1-r1 --status done --strict-artifacts && git diff --check`

### Blocked-by conditions

- `P1.6o180` has not finished with strict audit
- surfaced result is not yet classified from artifacts
- docs/state/session are not synced with truthful run status

### Owner role for closure

- `Top Architect + Brain`

## Execution status

- `done`

## Execution result

- Fresh guarded `dev L2` rerun `p1.6o180-l2-dev-20260314-a1-r1` completed with `artifact_integrity.valid=true`, `infra_valid=true`, `semantic_valid=false`, `stop_reason=max_failures_reached:1`, `run_integrity_reasons=[run_completion_gap]`, and `oracle_arbitration.winner=contract`.
- `P1.6o179` is off first fail: the surfaced completed-booking timeout family on `Меня зовут Лена.` no longer falls into `pack_fact_fallback` / `fact_without_evidence`.
- Surfaced next admissible class: `dialog 1 / turn 5 / "А есть ли возможность записаться на 14:00?"`, where timeout-degraded runtime keeps `expected_reply_type=name` but drops canonical `booking_time_availability_followup` meta/trace continuity; published `P1.6o181` as the next bounded structural block.

# TP-2026-03-13-p1.6o130-post-active-time-named-specialist-followup-proof-lane-a1

- Название/цель: после deterministic closure `P1.6o129` выполнить один fresh guarded `dev L2`, чтобы получить admissible proof evidence и truthfully понять, ушёл ли active-time named specialist followup `Track A` family из first-fail позиции на каноничном rerun.

## Canon refs

- Parent closure program: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Main remediation: `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- Process reset / class triage: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o118-class-level-closure-process-reset-a1.md`
- Prior proof lane: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o128-post-pending-handoff-resume-proof-lane-a1.md`
- Prior structural slice: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o129-active-time-named-specialist-followup-scenario-convergence-a1.md`
- State baseline: `STATE.md`

## Git context

- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `origin/main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect` remove branch + worktree after merge

## One web search (mandatory before implementation)

- Query: `site:www.uvicorn.org/settings uvicorn command line host port settings admin version runtime fingerprint`
- Date/time: `2026-03-13T12:58:00+05:00`
- Sources opened:
  - official Uvicorn settings docs: `https://www.uvicorn.org/settings/`
- Source quality:
  - official product documentation
- Found reusable ideas:
  - proof runtime startup should remain explicit and auditable through CLI parameters;
  - runtime fingerprint validation must stay explicit via `/admin/version` before the guarded run begins.
- Decision:
  - `reuse`
  - reuse the established live proof-run startup pattern on `:18184` and record `/admin/version == HEAD` before the single guarded rerun.
- Rejected options:
  - detached startup patterns that hide runtime parity;
  - multiple reruns in the same proof block;
  - routing from deterministic `P1.6o129` alone without fresh proof evidence.

## Root cause (mandatory)

- Symptom:
  - `P1.6o129` is green on deterministic contour, but the worktree still lacks one fresh admissible proof rerun to confirm whether the surfaced named specialist followup `Track A` family is gone from first fail on canonical evidence.
- Minimal reproduction:
  1. Open `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o129-active-time-named-specialist-followup-scenario-convergence-a1.md`.
  2. Confirm the bounded scenario-governance slice is deterministically green on the surfaced contour.
  3. Start or reuse a live runtime whose `/admin/version` matches current `HEAD`.
  4. Run exactly one guarded `dev L2` proof lane and strict audit it.
  5. Truthfully classify the first surfaced result under `P1.6o118` rules.
- Evidence:
  - `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o129-active-time-named-specialist-followup-scenario-convergence-a1.md`
  - `scripts/booking_dialog_scenarios.py`
  - `truffles-api/tests/test_booking_dialog_scenarios_script.py`
  - `truffles-api/tests/test_booking_quality_response_guard.py`
- Five Whys:
  1. Why is closure still incomplete after `P1.6o129`? Because deterministic green alone does not prove first-fail displacement on the guarded proof lane.
  2. Why can the worktree rerun now? Because the bounded `Track A` slice is deterministically closed.
  3. Why must runtime be fingerprinted first? Because proof evidence is admissible only if the live runtime matches current `HEAD`.
  4. Why only one rerun? Because the goal is truthful class admission, not another expensive replay loop.
  5. Why does class triage still matter after a green structural slice? Because the rerun can surface a different already-known class, and continuation must follow class-level routing rather than row-local reflex.
- Root cause statement:
  - after deterministic `P1.6o129` closure, the remaining gap is proof admissibility: one fresh fingerprint-verified guarded rerun is needed to determine whether the named specialist followup `Track A` family is actually gone from first-fail position.
- Fix mechanism:
  - verify or refresh the live worktree runtime on `:18184` and prove `/admin/version.git_commit == HEAD`;
  - execute exactly one guarded `dev L2` rerun and strict audit it;
  - route the surfaced result by `P1.6o118` class triage instead of automatic row admission.

## Reuse-first decision (mandatory)

- Reuse:
  - existing guarded entrypoint `scripts/llm_quality_guarded.sh`
  - existing audit command `python3 ops/diagnose.py llm-quality-audit`
  - existing live runtime startup pattern used in prior proof-lane blocks
- Integrate:
  - no runtime/scenario/oracle code changes unless the rerun surfaces a new deterministic or bounded family
- Build:
  - none planned in this block

## Invariant

1. `P1.6o128` remains the canonical proof artifact for the surfaced named specialist followup family.
2. `P1.6o129` deterministic closure truth stays intact.
3. No new row-local `Mxx -> child TP` admission happens without `P1.6o118` class triage.
4. Proof evidence is accepted only from the fresh fingerprint-verified rerun and strict audit artifacts.

## Scope

- Reconfirm the live worktree runtime on `127.0.0.1:18184`.
- Verify `/admin/version.git_commit == HEAD`.
- Execute exactly one guarded `dev L2` proof rerun after `P1.6o129`.
- Strictly audit the run.
- Truthfully sync docs/state/session and publish the next admissible structural block only if the surfaced class requires it.

## Out of scope

- new runtime/scenario/oracle code changes before the proof rerun surfaces a new class
- acceptance `lock/replay/full`
- new matrix-row admission without class-level routing

## Touch-list

- `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o130-post-active-time-named-specialist-followup-proof-lane-a1.md`
- `STATE.md`
- `STRUCTURE.md`
- `docs/SESSIONS/SESSION-2026-02-19-llm-first-firebreak-a1.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `/tmp/booking_quality/p1.6o130-l2-dev-20260313-a1-r1/**`

## Plan

1. Sync deterministic `P1.6o129` closure truth into the active docs.
2. Reconfirm the live worktree runtime on `127.0.0.1:18184` and prove `/admin/version.git_commit == HEAD`.
3. Run exactly one guarded `dev L2` proof lane as `p1.6o130-l2-dev-20260313-a1-r1`.
4. Strictly audit the run and inspect the first fail.
5. Classify the surfaced result using `P1.6o118` rules.
6. Sync truth into docs/state/session and publish the next admissible block only if the surfaced class requires it.

## DoD

1. Exactly one fresh guarded `dev L2` run is executed after deterministic closure of `P1.6o129`.
2. Live runtime fingerprint parity with current `HEAD` is recorded before the run.
3. Strict audit completes for `/tmp/booking_quality/p1.6o130-l2-dev-20260313-a1-r1`.
4. Active docs/state/session record whether the named specialist followup `Track A` family remains first fail on canonical proof evidence.
5. Any surfaced next step is routed by class-level triage, not by automatic row admission.

## Checks

- `git rev-parse HEAD`
- `curl -sf http://127.0.0.1:18184/admin/version`
- `TEST_MODE=1 scripts/llm_quality_guarded.sh --mode lock --run-id p1.6o130-l2-dev-20260313-a1-r1 --allow-pending-previous --allow-repeat-fingerprint -- --base-url http://127.0.0.1:18184 --client-slug demo_salon --mode llm --count 10 --min-turns 10 --max-turns 15 --include-media --scenario-coverage booking,info,interrupt,handoff --tool-hooks auto --jid-mode unique --allow-non-allowlist --judge-mode all --quality-lane dev --run-economy-gate block --fail-on-thresholds --max-failures 1 --reset-before-dialog`
- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o130-l2-dev-20260313-a1-r1 --status done --strict-artifacts`
- `git diff --check`
- `SESSION_AGENT=a1 scripts/session_check.sh`

## Execution status

- `done`
- `P1.6o130` executed exactly one fresh guarded `dev L2` run `p1.6o130-l2-dev-20260313-a1-r1` on fingerprint-verified runtime.
- Strict audit truth for `P1.6o130`:
  - `artifact_integrity.valid=true`
  - `infra_valid=true`
  - `semantic_valid=false`
  - `stop_reason=max_failures_reached:1`
- The covered active-time named specialist followup `Track A` family is no longer first fail.
- The surfaced turn `dialog 1 / turn 3 / "Я предпочла бы утренние часы."` initially appeared as `info_section_miss`, but later manual audit plus the bounded proof/oracle fix proved it was a tooling false-positive rather than the next semantic class.
- Continuation from this block truthfully routed into proof/oracle validity, then post-fix rerun, and only after that into bounded proof-process admission; no semantic row-child was admitted directly from `P1.6o130`.

## Evidence

1. `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o129-active-time-named-specialist-followup-scenario-convergence-a1.md`
2. `scripts/booking_dialog_scenarios.py`
3. `truffles-api/tests/test_booking_dialog_scenarios_script.py`
4. `truffles-api/tests/test_booking_quality_response_guard.py`

## Rollback

1. Stop the local runtime and keep `P1.6o129` deterministic closure intact.
2. If the rerun is invalid/incomplete, record it truthfully and do not reinterpret `P1.6o129`.
3. Do not mutate the matrix or docs to hide surfaced evidence.

## No-go

- no second expensive run in this block
- no new implementation before the proof rerun surfaces the next class
- no reuse of deterministic `P1.6o129` alone as closure proof
- no automatic `Mxx -> child TP` admission without `P1.6o118` class triage

## Risks / blockers

- local runtime may fail fingerprint validation or die before the guarded rerun starts
- the rerun may still stay semantic-red because of inherited `hardcode_core_violation` even if the named specialist followup family is gone
- the next surfaced result may expose broader runtime-owner or scenario-governance debt, which must open a structural continuation rather than a local patch

## Residual architecture debt (mandatory)

### Current residuals accepted in this block

- surfaced semantic family after `P1.6o129` remains unknown until a fresh proof rerun confirms or displaces it
- inherited `hardcode_core_violation` remains outside this proof-only block
- acceptance proof (`lock/replay/full`) remains blocked

### Why not in this block

- this block is proof-only; it determines the next remaining class on admissible evidence

### Risk if deferred

- the team can keep routing from deterministic closure alone instead of fresh proof evidence

### Linked follow-up Task Package(s)

- one structural continuation selected by `P1.6o118` after truthful `P1.6o130` class triage
- later hardcode-core cleanup block if the inherited gate remains active on the selected lane

### Expiry/trigger to stop deferral

- before any further guarded `dev/acceptance` rerun beyond `P1.6o130`

## Next-block contract (mandatory)

### Next block objective

- truthfully close the surfaced proof/oracle false-positive after `P1.6o130`, rerun the proof lane once on recovered runtime, and restore admissible proof-lane admission before any further expensive rerun.

### First deterministic check command

- `python3 -m py_compile ops/diagnose.py truffles-api/tests/test_booking_quality_info_sections.py truffles-api/tests/test_booking_quality_status_gate.py && git diff --check`

### Blocked-by conditions

- `P1.6o130` surfaced result is misread as a semantic child admission instead of a proof/oracle validity blocker
- post-`P1.6o130` runtime recovery is not reflected truthfully in active docs/state/session
- the team attempts another expensive rerun before the proof-process admission slice is closed deterministically

### Owner role for closure

- `Top Architect + Brain`

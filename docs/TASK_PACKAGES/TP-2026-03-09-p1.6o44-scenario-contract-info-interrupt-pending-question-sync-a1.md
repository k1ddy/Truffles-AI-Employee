# TP-2026-03-09-p1.6o44-scenario-contract-info-interrupt-pending-question-sync-a1.md

- Название/цель: закрыть surfaced `r48` scenario-contract blocker, где acceptance preflight считает generic `info` turn orphan-break для active pending-question context, хотя canonical scenario sanitizer уже сохраняет этот context через bounded `info interrupt` path.
- Parent TP: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Canon refs: `AGENTS.md`, `STATE.md`, `STRUCTURE.md`, `STRATEGY/REQUIREMENTS.md`, `docs/runbooks/BOOKING_CONFIRM_VERIFY.md`, `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o43-booking-master-info-interrupt-name-collect-a1.md`, `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o42-generic-info-interrupt-pending-question-context-a1.md`
- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect remove worktree + branch after merge`

## One web search (mandatory before implementation)

- Query: `Rasa forms interrupted slot filling side questions requested slot official docs`
- Time (UTC): `2026-03-09T05:37:00Z`
- Sources:
  - `https://rasa.com/docs/rasa/forms/`
- Ready solutions found:
  - side questions during an active slot request should be answered without dropping the active requested slot contract;
  - validation/governance around slot-filling must stay aligned with the same requested-slot lifecycle that runtime/scenario state machine uses.
- Decision (`reuse/integrate/build`): `reuse + integrate`
  - reuse the existing pending-question context contract already encoded in `scripts/booking_dialog_scenarios.py`;
  - integrate the same preserve-tag rule into `ops/diagnose.py` scenario-contract preflight;
  - build no new scenario family and no runtime phrase logic.
- Rejected options:
  - waive `orphan_pending_question_turn` in acceptance preflight;
  - rewrite generated `info` turns out of the scenario set instead of fixing the contract drift;
  - add broader preserve tags without matching scenario sanitizer evidence.

## Root cause (mandatory)

- Symptom:
  - guarded `dev L2` run `p1.6o6-l2-dev-20260309-a1-r48` aborts in preflight with `invalid_scenario_contract_preflight` and reason `orphan_pending_question_turn:d5:t6`.

- Minimal reproduction:
  1. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r48/summary.json`
  2. observe `stop_reason=invalid_scenario_contract_preflight`
  3. reproduce with generated scenario contract check and inspect an offending dialog where `booking -> info -> price/duration -> slot_compare(time)` is flagged orphan even though scenario sanitizer preserved the active pending-question context through generic `info`
  4. compare `scripts/booking_dialog_scenarios.py` vs `ops/diagnose.py` and note that `info` exists in the sanitizer preserve-set but not in the preflight preserve-set

- Evidence:
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r48/summary.json`
  - `/tmp/booking_quality/r48-repro-scenarios.json`
  - `scripts/booking_dialog_scenarios.py`
  - `ops/diagnose.py`

- Five Whys:
  1. Why did `r48` stop before execution: acceptance scenario contract said a pending-question turn was orphaned.
  2. Why did contract think it was orphaned: active reply context reset on a generic `info` turn.
  3. Why did active reply reset there: `ops/diagnose.py` preflight preserve-tags omit `info`.
  4. Why is that wrong: canonical sanitizer in `scripts/booking_dialog_scenarios.py` already preserves pending-question context across bounded generic `info` interrupts.
  5. Why did this surface now: `P1.6o42` closed the sanitizer side, but the acceptance preflight contract was not synced to the same rule.

- Root cause statement:
  - scenario generation and acceptance preflight diverged on the pending-question context contract: the generator preserves active pending-question context across bounded generic `info` interrupts, but `ops/diagnose.py` still resets context on `info`, producing false orphan failures before runtime execution.

- Fix mechanism:
  - sync `ops/diagnose.py` preserve-tags with the canonical sanitizer by adding bounded generic `info` preservation;
  - add deterministic regression covering `booking -> info -> pending_question(time)` acceptance;
  - rerun guarded acceptance only after the contract check is green.

## Release safety

- Strategy: deterministic contract sync only; no runtime behavior change
- Go signals:
  - scenario contract accepts `booking -> info -> pending_question(time)` when active time context is still valid
  - orphan detection still rejects pending-question turns after non-preserving contexts like `name`
- No-go signals:
  - weakening orphan detection globally
  - changing runtime core or judge/oracle thresholds
- Rollback verification:
  - revert only the preflight preserve-set sync and its regression if it masks true orphan pending-question families

## Invariant

1. `P1.6o42` generic info context closure remains canonical.
2. `orphan_pending_question_turn` stays enforced for genuinely orphaned turns after non-time contexts.
3. No acceptance gate is weakened by waiver.
4. No runtime-core semantic routing is changed in this block.

## Scope

- `ops/diagnose.py`
- `truffles-api/tests/test_booking_quality_scenario_contract_gate.py`
- `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o44-scenario-contract-info-interrupt-pending-question-sync-a1.md`
- tracker docs after deterministic closure and next rerun handoff:
  - `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
  - `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
  - `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`

## Out of scope

- new runtime owner changes
- broad pending-question ontology redesign
- lock/replay promotion

## Touch-list

- `ops/diagnose.py`
- `truffles-api/tests/test_booking_quality_scenario_contract_gate.py`
- `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o44-scenario-contract-info-interrupt-pending-question-sync-a1.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`

## Plan (1..N)

1. Freeze `r48` as scenario-contract drift evidence, not runtime evidence.
2. Sync `ops/diagnose.py` preserve-tags with canonical scenario sanitizer.
3. Add deterministic acceptance regression for `booking -> info -> pending_question(time)`.
4. Run targeted deterministic checks and `git diff --check`.
5. Audit `r48` as invalid preflight evidence and hand off the next guarded rerun.

## DoD

1. Acceptance scenario contract no longer flags bounded `booking -> info -> pending_question(time)` as orphan.
2. Orphan detection still rejects pending-question turns after `name`/non-preserving contexts.
3. Deterministic contract tests pass.
4. `git diff --check` stays clean.
5. Tracker docs point to the next guarded rerun after this deterministic sync.

## Checks

- `pytest -q truffles-api/tests/test_booking_quality_scenario_contract_gate.py`
- `git diff --check`

## Execution status

- `in_progress`

## Evidence

1. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r48/summary.json`
2. `/tmp/booking_quality/r48-repro-scenarios.json`
3. `ops/diagnose.py`
4. `scripts/booking_dialog_scenarios.py`

## Rollback

1. Revert only the scenario-contract preserve-tag sync and matching regression.
2. Keep `P1.6o6` blocked; do not reinterpret `r48` as valid closure evidence.

## No-go

- no runtime-core edits in this block
- no preflight waiver for orphan pending-question turns
- no hidden seed pinning or scenario suppression to dodge the failure
- no guarded rerun before deterministic contract test is green

## Risks/Blockers

1. Another generated family may surface a different scenario-contract drift after this sync; that should open a new bounded TP instead of broadening preserve-tags blindly.
2. Acceptance remains blocked on next surfaced family; this block only removes the false `info`-interrupt orphan.

## Residual architecture debt (mandatory)

- Current residuals accepted in this block:
  - generator/preflight contract still duplicates pending-question lifecycle constants in two places
- Why not in this block:
  - this slice only syncs the surfaced `info` preserve mismatch without refactoring the ownership of scenario-contract constants
- Risk if deferred:
  - future taxonomy changes can drift again and invalidate expensive guarded runs before execution
- Linked follow-up Task Package(s):
  - `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`
- Expiry/trigger to stop deferral:
  - next scenario-contract mismatch on pending-question lifecycle after this sync

## Next-block contract (mandatory)

- Next block objective:
  - rerun guarded acceptance after `P1.6o44` so the next surfaced blocker is not a false orphan on generic `info` interrupts
- First deterministic check command:
  - `pytest -q truffles-api/tests/test_booking_quality_scenario_contract_gate.py && git diff --check`
- Blocked-by conditions:
  - scenario-contract regression not green
  - `r48` invalid preflight evidence not audited and documented
  - runtime `http://127.0.0.1:18184` not refreshed before the next guarded rerun
- Owner role for closure:
  - `Hands` for deterministic sync + invalid evidence handoff, `Top Architect + Brain` for proof-bundle promotion

# TP-2026-03-08-p1.6o24-generic-master-info-interrupt-taxonomy-resume-contract-a1

- Название/цель: закрыть новый `r21` blocker family, где generic master-info question под активным booking collect (`А кто будет делать маникюр?`) остаётся tagged как `ask_about_requested_slot`, хотя runtime корректно маршрутизирует его через `booking_info_contract` и сохраняет resume contract `expected_reply_type=time`.
- Parent TP: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Canon refs: `AGENTS.md`, `STATE.md`, `STRUCTURE.md`, `STRATEGY/REQUIREMENTS.md`, `docs/runbooks/BOOKING_CONFIRM_VERIFY.md`, `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`, `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o23-mixed-date-slot-question-taxonomy-oracle-a1.md`
- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect remove worktree + branch after merge`

## One web search (mandatory before implementation)

- Query: `site:rasa.com/docs forms active loop interruptions faq questions slot filling`
- Time (UTC): `2026-03-08T09:40:25Z`
- Sources:
  - `https://rasa.com/docs/rasa/forms/`
- Ready solutions found:
  - active slot-filling loops must tolerate informational side-questions without losing the original requested slot;
  - ontology/oracle should distinguish side-question interrupts from pure slot-question acts and preserve the resume contract explicitly.
- Decision (`reuse/integrate/build`): `reuse + integrate`
  - reuse the existing `master` info family and existing `booking_interrupt` / `question_contract` runtime evidence;
  - integrate missing sanitize/oracle normalization for generic master-info question surfaces under pending booking collect;
  - build only the bounded scenario/oracle resume-contract helper for this family.
- Rejected options:
  - runtime changes in `decision.py` or `booking.py`;
  - weakening `ask_about_requested_slot` oracle generally;
  - introducing a new tag for the same owner path.

## Root cause (mandatory)

- Symptom:
  - fresh guarded `dev L2` rerun `p1.6o6-l2-dev-20260308-a1-r21` stopped on `dialog_id=1`, `turn_index=3`, message `А кто будет делать маникюр?`

- Minimal reproduction:
  1. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r21/brief.md`
  2. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r21/responses.jsonl` and `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r21/trace_bundle.jsonl` for `message_id=LLM-QUAL-p1.6o6-l2-dev-20260308-a1-r21-001-03-24dacf`
  3. inspect `scripts/booking_dialog_scenarios.py` around pending-question normalize/expectation helpers
  4. inspect `truffles-api/tests/test_booking_dialog_scenarios_script.py` specialist-target and `u vas` regressions

- Evidence:
  - Scenario tagged the turn as `ask_about_requested_slot` and required `pending_question_interaction` with `pending_question_act=ask_about_requested_slot`, `pending_question_target=time`.
  - Runtime produced coherent booking info interrupt evidence instead:
    - `decision_meta.intent=master`
    - `decision_meta.source=booking_info_contract`
    - `decision_meta.booking_interrupt_info=true`
    - `decision_meta.pending_question_target=time`
    - `decision_trace.stage=booking_interrupt`, `decision=info_reply`
    - `question_contract` stayed set to `expected_reply_type=time`
  - Failure reasons are expectation-only (`expected_meta_mismatch`, `expected_trace_miss`), not action/tool/state regressions.

- Five Whys:
  1. Why did `r21` fail: oracle expected a pending-question act, while runtime emitted booking-info-interrupt evidence.
  2. Why did oracle expect that: sanitize/oracle only retargets pending-question turns when text looks like named specialist reference.
  3. Why is that incomplete: generic service-scoped master questions (`кто будет делать`, `какой мастер`) are also info interrupts, just without explicit specialist name.
  4. Why does runtime look correct: active booking collect remains on `time`, while the side-question is answered by the master/info owner.
  5. Why did this surface now: `P1.6o23` removed the previous mixed date-slot-question drift, exposing the next adjacent taxonomy/oracle gap.

- Root cause statement:
  - pending-question sanitize/oracle remains too narrow: it only recognizes named specialist references as non-slot owner paths, so generic master-info questions under active booking collect are still scored as `ask_about_requested_slot` instead of `master` info interrupt with preserved time resume contract.

- Fix mechanism:
  - extend scenario sanitize so generic master-info question surfaces under `ask_about_requested_slot` retag to `master`;
  - inject explicit resume-contract expectations for this bounded family: `reply_type=time`, `pending_question_target=time`, `booking_interrupt_info=true`, `booking_interrupt` trace, preserved `question_contract` trace.

## Release safety

- Strategy: taxonomy/oracle-only closure; no runtime changes
- Go signals:
  - `r21` is recorded as audited non-closure evidence
  - generic master-info question surfaces retag from pending-question to `master`
  - resume contract stays explicit in `meta_any` / `trace_contains`
  - focused scenario-script regressions prove `u vas` time-question and named-specialist question stay on their existing paths
- No-go signals:
  - weakening pure `ask_about_requested_slot(time)` expectations
  - changing runtime behavior or owner routing in this block
  - adding a new tag instead of reusing `master`
- Rollback verification:
  - revert only the bounded normalize/resume-contract helper if pure master turns or pure slot-question turns start cross-matching

## Invariant

1. No runtime-core semantic changes in this block.
2. Named specialist pending-question surfaces keep the existing `pending_question_target=specialist` contract.
3. Pure time slot-question surfaces keep strict `pending_question_interaction` requirements.
4. Generic master-info side-questions under booking collect must preserve resume contract `expected_reply_type=time`.

## Scope

- `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o24-generic-master-info-interrupt-taxonomy-resume-contract-a1.md`
- `scripts/booking_dialog_scenarios.py`
- `truffles-api/tests/test_booking_dialog_scenarios_script.py`

## Out of scope

- runtime changes in `truffles-api/app/routers/webhook/*`
- fresh lock/replay acceptance evidence
- unrelated master resolver expansion

## Touch-list

- `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o24-generic-master-info-interrupt-taxonomy-resume-contract-a1.md`
- `scripts/booking_dialog_scenarios.py`
- `truffles-api/tests/test_booking_dialog_scenarios_script.py`

## Plan (1..N)

1. Record `r21` as audited non-closure evidence and freeze the generic master-info interrupt ontology gap.
2. Add bounded sanitize detection for generic master-info question surfaces that are currently mis-tagged as `ask_about_requested_slot`.
3. Attach explicit resume-contract oracle expectations for that family without changing runtime.
4. Add focused deterministic regressions for `кто будет делать ...` and adjacent negative controls.
5. Sync active TPs so the next expensive step becomes fresh guarded `r22` only after this deterministic closure.

## DoD

1. `r21` is recorded as non-closure evidence.
2. `А кто будет делать маникюр?` no longer inherits `pending_question_interaction` expectations.
3. The same bounded family now expects `master` info interrupt evidence plus preserved `expected_reply_type=time`.
4. Named-specialist and `u vas` negative controls remain green.
5. Focused scenario-script tests are green.
6. The next admissible expensive step is fresh guarded `r22`.

## Expensive run control (mandatory)

- Hypothesis:
  - after `P1.6o24`, fresh guarded `r22` will no longer stop on the generic master-info interrupt surface from `r21`
- Expected measurable effect:
  - `r22` no longer fails with `expected_meta_mismatch + expected_trace_miss` for `dialog_id=1`, `turn_index=3`; either the run proceeds past that turn or a different first blocker family surfaces
- Stop condition:
  - run exactly one fresh guarded `r22` plus strict audit; if the same family persists or the run becomes infra-invalid, stop the expensive loop and reopen RCA instead of chaining another rerun

## Checks

- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r21 --status done --strict-artifacts`
- `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "master_info_interrupt or specialist_target or u_vas"`
- `git diff --check`

## Execution status

- `done`

## Evidence

1. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r21/summary.json`
2. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r21/brief.md`
3. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r21/failure_families.json`
4. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r21/scenarios.json`
5. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r21/responses.jsonl`
6. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r21/trace_bundle.jsonl`
7. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r21/manual_audit.md`

## Rollback

1. Revert only the bounded generic master-info normalize/resume-contract helper if it over-rewrites pure slot-question turns.
2. Keep `P1.6o6` blocked; do not reinterpret `r21` as closure evidence.

## No-go

- no runtime behavior changes
- no broad relaxation of pending-question oracle
- no fresh guarded rerun before deterministic closure is green

## Risks/Blockers

1. Another adjacent booking-info interrupt family may surface on the next guarded rerun.
2. Infra invalidity remains orthogonal and does not close `P1.6o6`.

## Residual architecture debt (mandatory)

- Current residuals accepted in this block:
  - fresh guarded proof remains open via `P1.6o6` after this deterministic taxonomy/oracle closure
- Why not in this block:
  - this block only realigns taxonomy/oracle and cannot itself provide guarded proof
- Risk if deferred:
  - valid master info interrupt turns keep failing as expectation drift, blocking the next surfaced family
- Linked follow-up Task Package(s):
  - `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`
- Expiry/trigger to stop deferral:
  - before fresh guarded `r22`

## Next-block contract (mandatory)

- Next block objective:
  - keep `P1.6o24` closed via `r24` rollover evidence and continue with bounded `P1.6o25` before fresh guarded `r25`
- First deterministic check command:
  - `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "booking_management_coverage or confirm or check_booking or required_llm_tags" && pytest -q truffles-api/tests/test_booking_quality_scenario_contract_gate.py && git diff --check`
- Blocked-by conditions:
  - `P1.6o25` is not deterministically green
  - runtime `http://127.0.0.1:18184` is not refreshed after the next code delta
  - guarded preflight is not green
- Owner role for closure:
  - `Hands` for doc/code/evidence, `Top Architect + Brain` for closure gating

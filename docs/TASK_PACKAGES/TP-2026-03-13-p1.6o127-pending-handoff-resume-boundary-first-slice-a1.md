# TP-2026-03-13-p1.6o127-pending-handoff-resume-boundary-first-slice-a1

- Название/цель: после canonical class-routing evidence из `P1.6o126` закрыть первый bounded runtime-owner slice, в котором pending/handoff transport boundary сбрасывает booking follow-up continuity до того, как factual interrupt успевает сохранить `expected_reply_type=time`.

## Canon refs

- Parent closure program: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Main remediation: `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- Process reset / class triage: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o118-class-level-closure-process-reset-a1.md`
- Prior proof-validity slice: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o125-proof-lane-tool-evidence-fail-fast-validity-a1.md`
- Prior proof lane: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o126-post-tool-evidence-proof-lane-a1.md`
- State baseline: `STATE.md`

## Git context

- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `origin/main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect` remove branch + worktree after merge

## One web search (mandatory before implementation)

- Query: `site:stately.ai/docs guards context transitions state re-entry pending handoff restore context`
- Date/time: `2026-03-13T12:35:00+05:00`
- Sources opened:
  - official Stately docs: `https://stately.ai/docs/guards`
  - official Stately docs: `https://stately.ai/docs/context`
- Source quality:
  - official product documentation
- Found reusable ideas:
  - guarded transitions should choose the correct branch from explicit state/context conditions instead of implicit side branches;
  - context should be updated as a single source of truth, not silently discarded before the next transition evaluates.
- Decision:
  - `reuse`
  - reuse the guarded-boundary idea for runtime: the pending/handoff path should decide whether to preserve restored booking continuity from pending resume before generic handover reset clears it.
- Rejected options:
  - another local scenario/oracle patch;
  - another proof rerun before structural closure;
  - weakening pending/handoff guards or forcing expected reply by post-hoc oracle fit.

## Root cause (mandatory)

- Symptom:
  - `P1.6o126` is infra-valid and surfaces `dialog 1 / turn 8 / "Какой у вас прайс на маникюр?"` as the first fail: pricing fact is answered, but booking follow-up `expected_reply_type=time` disappears after pending/handoff transport re-entry.
- Minimal reproduction:
  1. Open `/tmp/booking_quality/p1.6o126-l2-dev-20260313-a1-r1/summary.json`.
  2. Confirm `infra_valid=true`, `semantic_valid=false`, first fail `message_id=LLM-QUAL-p1.6o126-l2-dev-20260313-a1-r1-001-08-26c0c7`.
  3. Inspect `responses.jsonl` for turn 7 and confirm booking verification ended with `delivery_state=transport_degraded`.
  4. Inspect `trace_bundle.jsonl` for turn 8 and confirm runtime records:
     - `session_memory decision=reset reason=handover`
     - `re_entry decision=required reason=handover`
     - `pending_guard decision=soft_pass state=pending`
     - `llm_policy_core decision=fact intent=info tool_action=catalog.service_query`
  5. Confirm turn expectations still require post-interrupt booking follow-up `expected_reply_type=time`, but runtime emits no matching `question_contract`.
- Evidence:
  - `/tmp/booking_quality/p1.6o126-l2-dev-20260313-a1-r1/summary.json`
  - `/tmp/booking_quality/p1.6o126-l2-dev-20260313-a1-r1/brief.md`
  - `/tmp/booking_quality/p1.6o126-l2-dev-20260313-a1-r1/responses.jsonl`
  - `/tmp/booking_quality/p1.6o126-l2-dev-20260313-a1-r1/trace_bundle.jsonl`
- Five Whys:
  1. Why did turn 8 fail? Because pricing fact reply did not preserve booking follow-up `expected_reply_type=time`.
  2. Why did booking follow-up disappear? Because runtime reset `session_memory` as soon as it saw `conversation.state=pending`.
  3. Why was that reset wrong on this contour? Because the pending state came from transport-degraded handoff boundary after active booking continuity, not from a genuine new semantic conversation.
  4. Why does that matter architecturally? Because pending/handoff transport boundary becomes a second semantic engine that can erase runtime continuity outside resolver logic.
  5. Why is this not just oracle drift? Because trace shows runtime itself lost the contract before oracle comparison; stale oracle is downstream, not primary.
- Root cause statement:
  - pending/handoff transport re-entry currently clears `session_memory` and booking follow-up continuity before semantic owner resolution, so factual interrupts after degraded booking boundaries are treated as pure info turns instead of preserving the active booking contract.
- Fix mechanism:
  - introduce one shared pending/handoff resume boundary that checks restored pending context before generic handover memory reset;
  - preserve booking follow-up `expected_reply_type` and interaction state when a pending soft-pass turn is still semantically attached to active booking continuity;
  - add targeted runtime and response-guard regressions for transport-degraded pending re-entry plus pricing/check-booking factual interrupts.

## Reuse-first decision (mandatory)

- Reuse:
  - existing pending resume snapshot/restore path in `truffles-api/app/services/state_service.py`
  - existing pending guard path in `truffles-api/app/routers/webhook/pending.py`
  - existing session-memory/context-manager helpers in `truffles-api/app/routers/webhook/context_manager.py` and `truffles-api/app/routers/webhook/session_memory.py`
- Integrate:
  - add a bounded shared boundary helper instead of another local `decision.py` phrase patch
- Build:
  - no new framework or external dependency

## Invariant

1. `P1.6o126` remains the canonical class-routing proof artifact for this family.
2. Pending/handoff guard semantics stay intact for real handoff/status flows.
3. Booking continuity is preserved only when restored context proves the turn is still attached to the active booking contract.
4. No oracle/gate weakening or row-local phrase fitting is introduced.

## Scope

- `truffles-api/app/routers/webhook/decision.py`
- `truffles-api/app/routers/webhook/pending.py`
- `truffles-api/app/services/state_service.py`
- targeted tests in `truffles-api/tests/test_message_endpoint.py`
- targeted guard evidence in `truffles-api/tests/test_booking_quality_response_guard.py`
- truth sync docs/state/session

## Out of scope

- new proof rerun before deterministic closure
- scenario/oracle normalization for this family
- hardcode-core remediation outside this bounded runtime-owner slice

## Touch-list

- `truffles-api/app/routers/webhook/decision.py`
- `truffles-api/app/routers/webhook/pending.py`
- `truffles-api/app/services/state_service.py`
- `truffles-api/tests/test_message_endpoint.py`
- `truffles-api/tests/test_booking_quality_response_guard.py`
- `STATE.md`
- `STRUCTURE.md`
- `docs/SESSIONS/SESSION-2026-02-19-llm-first-firebreak-a1.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o126-post-tool-evidence-proof-lane-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o127-pending-handoff-resume-boundary-first-slice-a1.md`

## Plan

1. Freeze `P1.6o126` truth as canonical class-routing evidence for pending/handoff runtime-owner drift.
2. Introduce a shared pending/handoff resume boundary before generic `session_memory` handover reset.
3. Preserve booking follow-up continuity when restored pending context proves active booking contract is still alive.
4. Add targeted runtime and response-guard regressions for pricing/check-booking interrupts after transport-degraded pending re-entry.
5. Run deterministic checks and sync truth.
6. Only after deterministic closure publish the next proof-lane rerun.

## DoD

1. Transport-degraded pending re-entry no longer erases booking follow-up continuity before pricing/check-booking factual interrupts resolve.
2. Runtime emits matching booking follow-up contract evidence (`expected_reply_type=time` and trace/meta support) on the covered contour.
3. Real pending/handoff status flows keep their existing guard behavior.
4. Targeted runtime and response-guard tests are green.
5. Active docs/state/session reflect that `P1.6o126` surfaced runtime-owner drift and `P1.6o127` is the selected structural continuation.

## Checks

- `python3 -m py_compile truffles-api/app/routers/webhook/decision.py truffles-api/app/routers/webhook/pending.py truffles-api/app/services/state_service.py truffles-api/tests/test_message_endpoint.py truffles-api/tests/test_booking_quality_response_guard.py`
- `pytest -q truffles-api/tests/test_state_service.py -k "preserve_context_restores_pending_resume_snapshot"`
- `pytest -q truffles-api/tests/test_message_endpoint.py -k "pending_handoff_pricing_interrupt_preserves_time_followup or transport_degraded_pending_reentry_restores_booking_resume"`
- `pytest -q truffles-api/tests/test_booking_quality_response_guard.py -k "pending_handoff_pricing_interrupt_keeps_time_followup"`
- `git diff --check`
- `SESSION_AGENT=a1 scripts/session_check.sh`

## Execution status

- `done`
- `P1.6o127` is deterministically closed:
  - pending/handoff soft-pass restores booking continuity from `pending_resume` before generic handover reset;
  - `expected_reply_reason` is preserved together with `expected_reply_type`;
  - targeted runtime-owner regressions are green on the surfaced transport-degraded pricing interrupt contour.

## Evidence

1. `/tmp/booking_quality/p1.6o126-l2-dev-20260313-a1-r1/summary.json`
2. `/tmp/booking_quality/p1.6o126-l2-dev-20260313-a1-r1/brief.md`
3. `/tmp/booking_quality/p1.6o126-l2-dev-20260313-a1-r1/responses.jsonl`
4. `/tmp/booking_quality/p1.6o126-l2-dev-20260313-a1-r1/trace_bundle.jsonl`

## Rollback

1. Revert the bounded pending/handoff boundary change and its targeted tests.
2. Keep `P1.6o126` truth frozen as the canonical surfaced runtime-owner class.
3. Do not reinterpret the proof result as pure oracle drift.

## No-go

- no new proof rerun before deterministic closure
- no scenario/oracle-only patch for this family
- no direct phrase/regex branching in core as a shortcut for pending/handoff continuity
- no automatic row-child admission from `P1.6o126`

## Risks / blockers

- under-scoping the fix could preserve `expected_reply_type` in one path while losing `interaction_state` or `context_manager` elsewhere
- over-scoping could weaken real pending/handoff guard semantics
- inherited `hardcode_core_violation` may still keep semantic status red after this family closes

## Residual architecture debt (mandatory)

### Current residuals accepted in this block

- hardcode-core cleanup remains outside this bounded runtime-owner slice
- broader owner/degrade migration out of `decision.py` remains open
- acceptance proof (`lock/replay/full`) remains blocked

### Why not in this block

- this block is limited to the surfaced pending/handoff resume boundary that `P1.6o126` exposed

### Risk if deferred

- future proof reruns will keep reopening transport/handoff-side continuity loss under new factual interrupt surfaces

### Linked follow-up Task Package(s)

- next proof-lane TP after deterministic closure of `P1.6o127`
- later hardcode-core cleanup block if the inherited gate remains active on the selected lane

### Expiry/trigger to stop deferral

- before any further guarded `dev/acceptance` rerun beyond `P1.6o126`

## Next-block contract (mandatory)

### Next block objective

- publish and run one fresh proof-lane rerun after deterministic closure of `P1.6o127`, then truthfully decide whether the pending/handoff runtime-owner family is gone from first-fail position on admissible evidence.

### First deterministic check command

- `python3 -m py_compile truffles-api/app/routers/webhook/decision.py truffles-api/app/routers/webhook/pending.py truffles-api/app/services/state_service.py truffles-api/tests/test_message_endpoint.py truffles-api/tests/test_booking_quality_response_guard.py && git diff --check`

### Blocked-by conditions

- `P1.6o126` truthful proof result is not synced in active docs/state/session
- targeted `P1.6o127` runtime-owner regressions are not green
- the team attempts another expensive rerun before deterministic closure of this structural slice

### Owner role for closure

- `Top Architect + Brain`

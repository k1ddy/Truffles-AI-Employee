# TP-2026-03-13-p1.6o119-timeout-degrade-owner-boundary-first-slice-a1

- Название/цель: после truthfully completed `P1.6o117` запустить первый structural `Track C` block вместо нового row-child: вынести приоритет timeout/degrade owner boundary над degraded info-hints / `pack_fact_fallback` для matched booking-collect turns, чтобы active interaction contract сохранялся через один shared boundary slice, а не через ещё одну family-specific patch-ветку в `decision.py`.

## Canon refs

- Main remediation: `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- Closure program: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Process reset / class triage: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o118-class-level-closure-process-reset-a1.md`
- Tactical proof gate just completed: `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o117-post-m21-reclosure-proof-lane-a1.md`
- Persisted interaction state: `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o82-persisted-interaction-state-a1.md`
- First resolver slice: `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o83-owner-resolver-m27-vertical-slice-a1.md`
- Machine-readable matrix: `truffles-api/app/knowledge/generic/INTERACTION_OWNER_MATRIX.yaml`
- Runtime code in scope: `truffles-api/app/routers/webhook/decision.py`, `truffles-api/app/services/owner_resolver.py`
- State baseline: `STATE.md`

## Git context

- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `origin/main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect` remove branch + worktree after merge

## One web search (mandatory before implementation)

- Query: `site:stately.ai/docs guards context state machine transitions official`
- Date/time: `2026-03-13 08:42 +0500`
- Sources opened:
  - `https://stately.ai/docs/guards`
  - `https://stately.ai/docs/context`
- Source quality:
  - official documentation / primary source
- Found reusable ideas:
  - guard logic should validate and route around one explicit context contract, not reconstruct business meaning from scattered branches;
  - state/context must remain the single model around which fallback and transitions operate;
  - if fallback branches become alternate semantic engines, the machine ceases to be predictable and test expectations drift from runtime truth.
- Decision:
  - `reuse + integrate`
  - reuse persisted `InteractionState`, the machine-readable owner matrix, and `OwnerResolver` direction already started in `P1.6o82..P1.6o83`;
  - integrate one shared timeout owner-boundary slice ahead of degraded info/fact fallback instead of adding another family-specific timeout guard.
- Rejected options:
  - another local timeout patch keyed to one exact phrase/slot combination;
  - weakening `fact_without_evidence` or judge/oracle gates so the surfaced turn turns green;
  - broad "rewrite all timeout branches now" without a bounded vertical slice.

## Root cause (mandatory)

- Symptom:
  - fresh guarded `dev L2` `p1.6o117-l2-dev-20260312-a1-r1` no longer fails first on reopened `M21`, but now surfaces a timeout/degrade family on dialog `1`, turn `12`, text `Меня зовут Динара.`.
- Minimal reproduction:
  1. Inspect `/tmp/booking_quality/p1.6o117-l2-dev-20260312-a1-r1/responses.jsonl` and confirm for message `LLM-QUAL-p1.6o117-l2-dev-20260312-a1-r1-001-12-a1187d`:
     - `expected_reply_type=name`
     - `booking_active=true`
     - `booking_slots.service=Маникюр`
     - `booking_slots.datetime=завтра`
     - `booking_slots.name=Динара`
     - `policy_core_mode=degraded_fallback`
     - `policy_core_degrade_reason=policy_error:timeout`
     - `policy_core_guard_recovery=pack_fact_fallback`
     - strict reason `fact_without_evidence`
  2. Inspect `/tmp/booking_quality/p1.6o117-l2-dev-20260312-a1-r1/trace_bundle.jsonl` for the same message and confirm:
     - `question_contract` already matched `name`
     - `llm_policy_core` timed out
     - `llm_policy_plan_delta` overrode to `to_action=fact` and `to_tool_action=pack.fact_fallback`
  3. Inspect `truffles-api/app/routers/webhook/decision.py:14094-14525` and confirm timeout fact fallback is only reachable when `timeout_needs_booking_collect` is false.
  4. Inference from code plus the failing trace:
     - on this turn `expected_reply_type=name`, so `booking_collect_contract_signal` is true;
     - therefore the only way the timeout path can still reach factual fallback is that a degraded side gate (`pending_info_signal` / info-hint path) preempts booking-collect continuity before a shared owner boundary resolves the turn.
  5. Inspect `truffles-api/app/services/owner_resolver.py` and confirm resolver coverage exists for bounded success-path runtime contracts, but there is no equivalent shared timeout-owner boundary invoked before degraded info/fact fallback.
- Evidence:
  - `/tmp/booking_quality/p1.6o117-l2-dev-20260312-a1-r1/summary.json`
  - `/tmp/booking_quality/p1.6o117-l2-dev-20260312-a1-r1/brief.md`
  - `/tmp/booking_quality/p1.6o117-l2-dev-20260312-a1-r1/responses.jsonl`
  - `/tmp/booking_quality/p1.6o117-l2-dev-20260312-a1-r1/trace_bundle.jsonl`
  - `/tmp/booking_quality/p1.6o117-l2-dev-20260312-a1-r1/manual_audit.md`
  - `truffles-api/app/routers/webhook/decision.py`
  - `truffles-api/app/services/owner_resolver.py`
- Five Whys:
  1. Why did `P1.6o117` surface a new first fail after reopened `M21` cleared? Because timeout degrade still overrode a matched booking-collect turn into `pack_fact_fallback`.
  2. Why was factual fallback still eligible? Because the timeout path decided semantics locally before a shared owner boundary enforced the active collect contract.
  3. Why can the timeout path still decide semantics locally? Because `OwnerResolver` coverage stops at bounded success-path slices and the timeout/degrade branch still lives as bespoke logic in `decision.py`.
  4. Why is this architecturally dangerous? Because each timeout family can reopen as a new surface even when `InteractionState` and matrix truth are already available.
  5. Why is the next step structural rather than another row fix? Because `P1.6o118` explicitly forbids another same-shape timeout-local patch for an already-known class and requires reduction of the class source itself.
- Root cause statement:
  - the remaining defect is not primarily slot extraction anymore; it is timeout/degrade owner precedence. A matched booking-collect turn can still be reclassified by degraded info/fact fallback before one shared owner boundary consumes persisted interaction state and expected-reply continuity. This keeps timeout/degrade as a second semantic engine outside the resolver path.
- Fix mechanism:
  - add one shared timeout owner-boundary slice that runs before degraded info hints and factual fallback;
  - feed that boundary with persisted `InteractionState`, expected-reply contract, booking state, and question-contract match evidence;
  - reuse `OwnerResolver` / matrix semantics where possible and reuse booking follow-up helpers where the boundary decides to stay in collect;
  - emit explicit reason-coded trace/meta so the timeout boundary becomes observable and contract-testable;
  - keep the slice bounded to matched booking-collect turns first, then prove it before broader timeout migration.

## Reuse-first decision (mandatory)

- Reuse:
  - persisted `InteractionState`
  - machine-readable owner matrix
  - `OwnerResolver` and matrix loader
  - existing booking-slot update/follow-up helpers
  - existing timeout trace/meta reason-code infrastructure
- Integrate:
  - one shared timeout owner-boundary helper/service before degraded info/fact fallback
  - targeted deterministic regressions for matched booking slot-fill turns under timeout degrade
- Build:
  - only the missing boundary slice and the minimum tests/docs required to prove it

## Execution profile

- TP mode: `implementation`
- Doc touch budget (files): `12`
- Code dominance: `required`
- Override token: `none`
- Why this profile fits: this is the first bounded structural slice for already-known `Class C`; it must land in runtime code, not as another narrative-only or row-local patch.

## Release safety

- Strategy: bounded timeout owner-boundary slice only
- Go signals:
  - matched booking-collect turns under timeout degrade no longer reach `pack_fact_fallback` before shared owner-boundary evaluation
  - trace/meta records the shared boundary reason-code and preserved expected-reply continuity
  - adjacent bounded timeout families (`M37`, `M38`, `M41`) stay green on deterministic contour
- No-go signals:
  - non-booking self-introduction turns start entering booking collect
  - another local semantic branch is added directly in `decision.py` instead of one shared boundary/helper
  - `fact_without_evidence` is suppressed instead of removing the factual fallback on the covered path
- Rollback verification:
  - revert only the shared timeout owner-boundary slice and its targeted tests if adjacent timeout families regress

## Invariant

1. No oracle weakening and no use of invalid/non-canonical runs as closure proof.
2. No phrase-fitting or regex-hardcode in core as the primary fix mechanism.
3. Covered timeout turns must preserve active collect ownership from state/contract before degraded info/fact fallback becomes eligible.
4. Non-booking turns must not be pulled into booking collect by the new boundary.
5. No expensive guarded run in this block.

## Scope

- publish the first structural `Track C` implementation TP
- add one shared timeout owner-boundary slice for matched booking-collect turns
- wire the timeout-degrade path to call the boundary before degraded info/fact fallback
- add targeted deterministic runtime + response-guard + matrix/resolver regressions
- sync active docs/state/session and publish the next proof-lane TP only after deterministic closure is green

## Out of scope

- full migration of every timeout/interrupt branch out of `decision.py`
- new scenario-governance/oracle convergence work
- acceptance `lock/replay/full`
- broad runtime rewrite without bounded slice boundaries

## Touch-list

- `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o119-timeout-degrade-owner-boundary-first-slice-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o117-post-m21-reclosure-proof-lane-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o118-class-level-closure-process-reset-a1.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `STATE.md`
- `STRUCTURE.md`
- `docs/SESSIONS/SESSION-2026-02-19-llm-first-firebreak-a1.md`
- `truffles-api/app/routers/webhook/decision.py`
- `truffles-api/app/services/owner_resolver.py`
- `truffles-api/app/services/interaction_owner_matrix_service.py`
- `truffles-api/app/knowledge/generic/INTERACTION_OWNER_MATRIX.yaml`
- `truffles-api/tests/test_message_endpoint.py`
- `truffles-api/tests/test_booking_quality_response_guard.py`
- `truffles-api/tests/test_owner_resolver.py`
- `truffles-api/tests/test_interaction_owner_matrix_contract.py`

## Plan (1..N)

1. Freeze `P1.6o117` truthfully as a Class C structural signal: no new row admission, no oracle patch, no second expensive run.
2. Implement one shared timeout owner-boundary helper/service that evaluates active interaction contract before degraded info/fact fallback.
3. Reuse matrix/resolver/state data so covered timeout turns preserve collect continuity through a single contract path.
4. Add targeted deterministic positive and negative tests for matched booking slot-fill turns and adjacent timeout families.
5. Sync docs/state/session and publish the next proof-lane block only after deterministic closure is green.

## DoD

1. The surfaced `P1.6o117` failing turn can no longer reach `policy_core_timeout_fact_fallback` / `pack_fact_fallback` on deterministic contour.
2. Covered timeout booking-collect continuity is decided by one shared boundary/helper rather than another bespoke branch ordering in `decision.py`.
3. `decision_meta/decision_trace` preserve explicit timeout owner-boundary evidence and expected-reply continuity.
4. Targeted deterministic runtime/response-guard/matrix tests are green.
5. The next admissible expensive block is a fresh proof lane after this structural slice, not another same-shape local patch.

## Expensive run control (mandatory)

- Hypothesis:
  - if timeout owner-boundary precedence is enforced structurally, the next guarded proof lane will no longer stop first on matched booking slot-fill turns collapsing into factual fallback.
- Expected measurable effect:
  - deterministic tests turn green for the surfaced family and adjacent timeout regressions remain green
- Stop condition:
  - no guarded expensive run inside this block; stop after deterministic closure and publish the next proof-lane TP

## Checks

- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o117-l2-dev-20260312-a1-r1 --status done --strict-artifacts`
- `pytest -q truffles-api/tests/test_message_endpoint.py -k "timeout_booking_slot_fill_followup or timeout_pending_slot_question or timeout_specialist_followup or timeout_owner_boundary"`
- `pytest -q truffles-api/tests/test_booking_quality_response_guard.py -k "timeout_booking_slot_fill_followup or timeout_owner_boundary"`
- `pytest -q truffles-api/tests/test_owner_resolver.py`
- `pytest -q truffles-api/tests/test_interaction_owner_matrix_contract.py`
- `python3 -m py_compile truffles-api/app/routers/webhook/decision.py truffles-api/app/services/owner_resolver.py truffles-api/app/services/interaction_owner_matrix_service.py truffles-api/tests/test_message_endpoint.py truffles-api/tests/test_booking_quality_response_guard.py truffles-api/tests/test_owner_resolver.py truffles-api/tests/test_interaction_owner_matrix_contract.py`
- `git diff --check`
- `SESSION_AGENT=a1 scripts/session_check.sh`

## Execution status

- `deterministic_closure_green`
- shared timeout owner-boundary slice landed for the covered `Class C` path:
  matched booking expected-reply continuity now resolves before degraded info/fact fallback
- targeted deterministic checks are green; no fresh expensive proof run was started inside this block
- next admissible block is a fresh proof-lane TP, not another timeout-local child patch

## Evidence

1. `/tmp/booking_quality/p1.6o117-l2-dev-20260312-a1-r1/summary.json`
2. `/tmp/booking_quality/p1.6o117-l2-dev-20260312-a1-r1/brief.md`
3. `/tmp/booking_quality/p1.6o117-l2-dev-20260312-a1-r1/scenarios.json`
4. `/tmp/booking_quality/p1.6o117-l2-dev-20260312-a1-r1/responses.jsonl`
5. `/tmp/booking_quality/p1.6o117-l2-dev-20260312-a1-r1/trace_bundle.jsonl`
6. `/tmp/booking_quality/p1.6o117-l2-dev-20260312-a1-r1/manual_audit.md`
7. `truffles-api/app/routers/webhook/decision.py`
8. `truffles-api/app/services/owner_resolver.py`
9. `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o118-class-level-closure-process-reset-a1.md`
10. `truffles-api/tests/test_message_endpoint.py`
11. `truffles-api/tests/test_booking_quality_response_guard.py`
12. `truffles-api/tests/test_owner_resolver.py`

## Rollback

1. Keep `P1.6o117` proof truth intact.
2. Revert only the shared timeout owner-boundary slice and its targeted tests if adjacent timeout families regress.
3. Do not mutate the matrix or docs to hide the surfaced Class C evidence.

## No-go

- no new phrase/regex hardcode in core as the primary fix
- no new same-shape timeout-local child TP in place of this structural slice
- no weakening of `fact_without_evidence`, proof gates, or judge/oracle validity rules
- no big-bang timeout rewrite without bounded slice verification

## Risks / blockers

- the shared boundary may still leave adjacent non-booking timeout families outside the first slice
- moving precedence can regress already-closed timeout families if the gating is too broad
- `decision.py` extraction may require careful sequencing to avoid fighting existing unmerged local edits in the worktree

## Residual architecture debt (mandatory)

### Current residuals accepted in this block

- timeout/degrade logic still remains broader than the first shared boundary slice
- scenario-governance/oracle still duplicates runtime truth on separate families
- acceptance proof (`lock/replay/full`) remains blocked

### Why not in this block

- this block is intentionally the first bounded structural slice for `Class C`, not the total runtime migration

### Risk if deferred

- timeout/degrade will continue reopening adjacent surfaces even when `InteractionState` and matrix truth are already correct

### Linked follow-up Task Package(s)

- `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o120-post-track-c-proof-lane-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o121-booking-slot-guidance-scenario-convergence-first-slice-a1.md`
- later `Track B` resolver coverage expansion
- later `Track A` scenario-governance convergence
- `P1.7` acceptance replay gate after structural tracks land

### Expiry/trigger to stop deferral

- before any next guarded `dev/acceptance` rerun after `P1.6o119` deterministic closure

## Next-block contract (mandatory)

### Next block objective

- `P1.6o120` has already executed the required proof-lane rerun; the next admissible block is now `P1.6o121`, the first structural `Track A` scenario-governance convergence slice selected from truthful class triage.

### First deterministic check command

- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o120-l2-dev-20260313-a1-r1 --status done --strict-artifacts && pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py && pytest -q truffles-api/tests/test_interaction_owner_matrix_contract.py && git diff --check`

### Blocked-by conditions

- `P1.6o120` surfaced `Track A` evidence is not synced truthfully in active docs/state
- the shared timeout owner-boundary slice is not landed with deterministic tests green
- the first `Track A` structural block is not published against the surfaced `slot_compare -> ask_about_requested_slot(time)` oracle drift

### Owner role for closure

- `Top Architect + Brain`

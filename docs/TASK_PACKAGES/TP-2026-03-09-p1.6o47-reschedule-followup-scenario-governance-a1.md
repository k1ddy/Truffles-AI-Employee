# TP-2026-03-09-p1.6o47-reschedule-followup-scenario-governance-a1.md

- Название/цель: закрыть surfaced `r51` blocker, где после явного `reschedule` pivot scenario продолжает трактовать follow-up `Могу я перенести запись на послезавтра?` как pending-question `slot_compare`, хотя runtime корректно переводит ход в `reschedule`/`hard_law` management path.
- Parent TP: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Canon refs: `AGENTS.md`, `STATE.md`, `STRUCTURE.md`, `STRATEGY/REQUIREMENTS.md`, `docs/runbooks/BOOKING_CONFIRM_VERIFY.md`, `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o46-question-like-daypart-slot-constraint-contract-a1.md`, `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o25-booking-check-confirm-scenario-governance-a1.md`
- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect remove worktree + branch after merge`

## One web search (mandatory before implementation)

- Query: `Rasa forms cancel or reschedule interruption official docs`
- Time (UTC): `2026-03-09T06:04:00Z`
- Sources:
  - `https://rasa.com/docs/rasa/forms/`
  - `https://rasa.com/docs/reference/primitives/patterns/`
- Ready solutions found:
  - once a collect flow is interrupted by a cancel/change-mind path, subsequent turns should stay on the interruption flow instead of being forced back into requested-slot semantics;
  - interruption governance should reuse the active interruption pattern rather than mixing it with the previous slot-filling contract.
- Decision (`reuse/integrate/build`): `reuse + integrate`
  - reuse existing `reschedule`/management governance already present in runtime and scenario coverage;
  - integrate a stateful normalization so follow-up turns after a `reschedule` pivot do not remain on pending-question tags;
  - build no runtime core workaround unless deterministic taxonomy sync proves insufficient.
- Rejected options:
  - waiving `expected_meta_mismatch` / `expected_trace_miss` for reschedule follow-ups;
  - rewriting runtime hard-law path to fit `slot_compare` expectations;
  - letting scenario mix `pending` reschedule state with active booking slot-question tags.

## Root cause (mandatory)

- Symptom:
  - guarded `dev L2` run `p1.6o6-l2-dev-20260309-a1-r51` fails on dialog 2 turn 4 with `expected_meta_mismatch` and `expected_trace_miss`.

- Minimal reproduction:
  1. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r51/scenarios.json` dialog 2 turn 3 and turn 4
  2. observe turn 3 is explicit `reschedule`, but turn 4 is still tagged as pending-question `slot_compare`
  3. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r51/trace_bundle.jsonl` and confirm runtime follows `policy_gate -> reschedule -> escalate`
  4. inspect `scripts/booking_dialog_scenarios.py` and verify there is no stateful governance rewrite from active `reschedule` follow-up back into the reschedule family

- Evidence:
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r51/scenarios.json`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r51/responses.jsonl`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r51/trace_bundle.jsonl`
  - `scripts/booking_dialog_scenarios.py`

- Five Whys:
  1. Why did the turn fail: scenario expected booking slot-question evidence, runtime escalated reschedule.
  2. Why did runtime escalate reschedule: the user explicitly asked to move an existing booking and runtime law/policy routes that family to management.
  3. Why did scenario expect slot-question evidence: taxonomy kept the follow-up on `slot_compare` after an earlier `reschedule` pivot.
  4. Why is that wrong: once dialog state pivots to reschedule, subsequent transfer/change-date follow-ups are owned by the reschedule family, not booking collect.
  5. Why is this a governance gap: scenario state machine does not yet carry reschedule interruption ownership across subsequent turns.

- Root cause statement:
  - scenario governance does not preserve the active `reschedule` interruption family, so follow-up transfer/date-change turns are mislabeled as booking slot-question acts even though runtime correctly keeps them in the reschedule management path.

- Fix mechanism:
  - add stateful scenario normalization/governance for reschedule-active dialogs;
  - only if deterministic sync proves insufficient, inspect runtime for a missing bounded reschedule-follow-up owner.

## Release safety

- Strategy: start with deterministic scenario governance only
- Go signals:
  - follow-up turns after a `reschedule` pivot no longer expect booking slot-question evidence
  - unrelated booking slot-question families remain unchanged
- No-go signals:
  - weakening hard-law reschedule escalation
  - runtime workaround before deterministic governance is tested
- Rollback verification:
  - revert only the reschedule governance normalization if it over-classifies unrelated booking turns

## Invariant

1. `P1.6o46` slot-constraint closure remains closed.
2. Hard-law `reschedule` runtime behavior remains unchanged unless deterministic sync proves insufficient.
3. Booking slot-question families stay owned by booking collect only when no reschedule pivot exists.
4. No runtime phrase-hardcode is added in this block by default.

## Scope

- `scripts/booking_dialog_scenarios.py`
- `truffles-api/tests/test_booking_dialog_scenarios_script.py`
- `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o47-reschedule-followup-scenario-governance-a1.md`
- tracker docs after closure / next rerun handoff:
  - `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
  - `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
  - `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`

## Out of scope

- tool-evidence infra remediation
- lock/replay promotion
- broad reschedule product redesign

## Touch-list

- `scripts/booking_dialog_scenarios.py`
- `truffles-api/tests/test_booking_dialog_scenarios_script.py`
- `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o47-reschedule-followup-scenario-governance-a1.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`

## Plan (1..N)

1. Freeze `r51` as reschedule governance evidence.
2. Add stateful taxonomy normalization for follow-ups after an explicit `reschedule` pivot.
3. Add deterministic scenario regression for `reschedule -> follow-up date change`.
4. Run targeted checks and `git diff --check`.
5. Then hand off the next guarded rerun.

## DoD

1. Follow-up turns after a `reschedule` pivot no longer expect booking pending-question evidence.
2. Deterministic reschedule governance regression passes.
3. `git diff --check` stays clean.

## Checks

- `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "reschedule or booking_management_coverage"`
- `git diff --check`

## Execution status

- `done`
- deterministic governance closure is complete
- invalid guarded `r52`/`r53` were operator/preflight misses and were audited as invalid artifacts, not product evidence
- fresh guarded `r54` no longer stopped on the post-`reschedule` follow-up family and rolled to bounded runtime `P1.6o48`

## Evidence

1. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r51/scenarios.json`
2. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r51/responses.jsonl`
3. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r51/trace_bundle.jsonl`
4. `scripts/booking_dialog_scenarios.py`
5. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r52/summary.json`
6. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r53/summary.json`
7. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r54/summary.json`
8. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r54/brief.md`
9. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r54/manual_audit.md`

## Rollback

1. Revert only the reschedule scenario-governance normalization and matching regressions.
2. Keep `P1.6o6` blocked; do not reinterpret `r51` as closure evidence.

## No-go

- no hard-law/runtime waiver for reschedule
- no scenario waiver for missing meta/trace
- no guarded rerun before deterministic governance is green

## Risks/Blockers

1. If deterministic governance is not enough, the next slice may need a bounded runtime owner for reschedule follow-ups.
2. Acceptance remains infra-invalid for unrelated tool-evidence gaps; this block only closes the surfaced reschedule governance drift from `r51`.

## Residual architecture debt (mandatory)

- Current residuals accepted in this block:
  - scenario governance still duplicates some interruption-state heuristics
- Why not in this block:
  - this slice only closes the surfaced reschedule follow-up drift
- Risk if deferred:
  - generated runs will keep mixing pending reschedule state with booking slot-question expectations
- Linked follow-up Task Package(s):
  - `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`
- Expiry/trigger to stop deferral:
  - next reschedule follow-up family after this governance sync

## Next-block contract (mandatory)

- Next block objective:
  - bounded runtime closure of `P1.6o48`, then fresh guarded rerun beyond the reschedule governance family
- First deterministic check command:
  - `pytest -q truffles-api/tests/test_intent.py -k "pending_question_contract or slot_compare" && pytest -q truffles-api/tests/test_message_endpoint.py -k "slot_compare_time_guidance or pending_question_act_time_guidance or list_slots_missing_slot_pending_question" && pytest -q truffles-api/tests/test_booking_quality_response_guard.py -k "slot_compare_guidance or missing_slot_slot_compare or booking_slot_stall" && git diff --check`
- Blocked-by conditions:
  - `r54` audited evidence not reflected in tracker docs
  - bounded runtime follow-up tests are not green
  - guarded rerun preflight invalid
- Owner role for closure:
  - `Hands` for deterministic governance + rerun evidence, `Top Architect + Brain` for proof-bundle promotion

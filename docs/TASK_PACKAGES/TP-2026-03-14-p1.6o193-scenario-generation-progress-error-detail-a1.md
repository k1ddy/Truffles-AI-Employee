# TP-2026-03-14-p1.6o193-scenario-generation-progress-error-detail-a1

- Название/цель: закрыть surfaced proof-lane blocker после `P1.6o192`, где scenario-generation timeout теперь уже evidenceful по `batch/attempt/event`, но normalized reason всё ещё теряет сам `batch_attempt_error.error`, из-за чего next deterministic slice остаётся недостаточно точным.

## Canon refs

- Binding roadmap: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o135-beauty-salon-canonical-control-plane-roadmap-a1.md`
- Prior deterministic closure: `docs/TASK_PACKAGES/TP-2026-03-14-p1.6o191-scenario-generation-timeout-progress-evidence-a1.md`
- Blocked proof lane: `docs/TASK_PACKAGES/TP-2026-03-14-p1.6o192-post-p1.6o191-proof-lane-a1.md`
- Parent closure program: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Main remediation: `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- State baseline: `STATE.md`

## One web search (mandatory before implementation)

- Exact query: `python subprocess stderr capture official docs`
- Date/time: `2026-03-14 16:22 +05:00`
- Opened sources:
  - official Python subprocess docs (`https://docs.python.org/3/library/subprocess.html`)
- Found ready solutions:
  - child-process stderr is the canonical place to carry machine-readable progress/error diagnostics back to the parent process;
  - preserving the stderr detail in the parent-side classification is the narrowest way to improve timeout RCA without changing process behavior.
- Decision: `integrate/build`
  - keep scenario generation behavior unchanged;
  - extend timeout normalization so it carries last `batch_attempt_error.error` detail when progress evidence exists.
- Rejected variants:
  - changing timeout math before the inner error detail is surfaced;
  - another rerun without preserving the error detail;
  - broad logging redesign.
- Source quality:
  - primary/official source: `docs.python.org`.

## Git context

- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `origin/main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect` remove branch + worktree after merge

## Root cause (mandatory)

- Symptom:
  - `p1.6o192-l2-dev-20260314-a1-r1` now reports `scenario_generation_timeout (batch=3 attempt=1 event=batch_attempt_error)`, but the normalized reason still omits the actual `error` payload emitted by `scripts/booking_dialog_scenarios.py` for `batch_attempt_error`.
- Minimal reproduction:
  1. run guarded proof lane `P1.6o192` on parity-verified runtime `http://127.0.0.1:18184`;
  2. let `scripts/booking_dialog_scenarios.py` emit `booking_scenario_llm_progress` with `event=batch_attempt_error` and an `error` field;
  3. observe `_normalize_scenario_generation_error(...)` keep `batch/attempt/event` but discard `error` detail;
  4. note that next RCA still cannot distinguish the concrete inner failure without another tooling step.
- Evidence:
  - `/tmp/booking_quality/p1.6o192-l2-dev-20260314-a1-r1/summary.json`
  - `/tmp/booking_quality/p1.6o192-l2-dev-20260314-a1-r1/brief.md`
  - `/tmp/booking_quality/p1.6o192-l2-dev-20260314-a1-r1/manual_audit.json`
  - `ops/diagnose.py`
  - `scripts/booking_dialog_scenarios.py`
  - `truffles-api/tests/test_diagnose_run_command.py`
- Five Whys:
  1. Why is the blocker now more precise than `P1.6o190`? Because `P1.6o191` surfaced `batch/attempt/event`.
  2. Why is it still not precise enough for the next structural slice? Because the actual inner error string is still missing from the normalized reason.
  3. Why is the error string missing? Because timeout normalization only serializes `batch/attempt/event` from the last progress payload.
  4. Why is that a blocker? Because the next admissible deterministic fix depends on whether the inner failure is transport timeout, JSON repair timeout, or another batch error.
  5. Why is carrying `error` detail the right next slice? Because it is the narrowest observability change that improves RCA without changing behavior or timeout budgets.
- Root cause statement:
  - the surfaced scenario-generation timeout blocker still loses the most useful RCA datum because `_normalize_scenario_generation_error(...)` ignores `batch_attempt_error.error` from the last progress payload.
- Fix mechanism:
  - preserve bounded `error` detail from the last `booking_scenario_llm_progress` payload in the normalized timeout reason and add targeted tests for that path.

## Reuse-first decision (mandatory)

- Reuse:
  - existing `booking_scenario_llm_progress` event schema in `scripts/booking_dialog_scenarios.py`
  - existing timeout normalization path in `ops/diagnose.py`
  - existing tests in `truffles-api/tests/test_diagnose_run_command.py`
- Integrate:
  - keep the fix inside proof-tooling observability only; no runtime business logic changes
- Build:
  - add only the bounded error-detail preservation and tests for the surfaced timeout contour

## Release safety

- Rollout: local tooling-only change
- Go signal:
  - scenario-generation timeout reasons now carry bounded last-error detail when progress evidence exists
- No-go signal:
  - change alters scenario generation behavior instead of only observability
- Rollback:
  - revert only the error-detail normalization change and tests; keep `P1.6o192` blocked truth intact

## Invariant

1. No weakening of proof gates.
2. No timeout-budget changes in this block.
3. No runtime/business behavior changes.
4. No extra proof rerun before deterministic closure.

## Scope

- extend `_normalize_scenario_generation_error(...)` to preserve bounded `batch_attempt_error.error` detail
- add targeted tests for progress-error normalization
- sync canon docs/state/session after deterministic closure

## Out of scope

- fresh guarded rerun before deterministic closure
- changing scenario timeout math
- any booking/runtime semantic changes

## Touch-list

- `ops/diagnose.py`
- `truffles-api/tests/test_diagnose_run_command.py`
- `STATE.md`
- `docs/SESSIONS/SESSION-2026-02-19-llm-first-firebreak-a1.md`
- `docs/SESSION_INDEX.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-14-p1.6o192-post-p1.6o191-proof-lane-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-14-p1.6o193-scenario-generation-progress-error-detail-a1.md`

## Plan

1. Add targeted unit coverage that proves last-progress `error` detail is preserved in normalized timeout reasons.
2. Implement bounded error-detail preservation in `_normalize_scenario_generation_error(...)`.
3. Re-run targeted tooling tests and static checks.
4. Sync `P1.6o192` truth as an evidenceful scenario-generation blocker and mark `P1.6o193` complete if green.
5. Publish the next proof-lane TP.

## DoD

1. Scenario-generation timeout reasons now carry bounded last-error detail when progress evidence exists.
2. Targeted tooling tests are green.
3. Docs/state/session truthfully record `P1.6o192` as blocked on an evidenceful scenario-generation timeout and publish the next admissible rerun TP.

## Checks

- `python3 -m py_compile ops/diagnose.py truffles-api/tests/test_diagnose_run_command.py`
- `pytest -q truffles-api/tests/test_diagnose_run_command.py -k "scenario_timeout or generate_batch"`
- `git diff --check`
- `SESSION_AGENT=a1 scripts/session_check.sh`

## Evidence

1. `/tmp/booking_quality/p1.6o192-l2-dev-20260314-a1-r1/summary.json`
2. `/tmp/booking_quality/p1.6o192-l2-dev-20260314-a1-r1/brief.md`
3. `/tmp/booking_quality/p1.6o192-l2-dev-20260314-a1-r1/manual_audit.json`
4. `ops/diagnose.py`
5. `scripts/booking_dialog_scenarios.py`
6. `truffles-api/tests/test_diagnose_run_command.py`
7. updated docs listed above

## Rollback

1. Revert only the error-detail normalization change and tests.
2. Keep `P1.6o192` invalid-run truth intact.
3. Do not mutate proof artifacts.

## No-go

- no silent pass when scenario generation times out
- no extra proof rerun before deterministic closure of this blocker
- no timeout-budget changes in this block
- no runtime or oracle changes in this block

## Risks / blockers

- the next rerun may still time out, but with clearer inner error detail
- progress error strings may need bounded truncation to stay stable
- another proof-validity blocker may surface once the inner error is explicit

## Residual architecture debt (mandatory)

### Current residuals accepted in this block

- proof admission still depends on synchronous OpenAI transport probing and scenario-generation subprocess execution
- guarded rerun remains blocked until post-fix proof evidence exists

### Why not in this block

- this slice is only to preserve the surfaced inner error detail, not to change timeout math before the exact inner failure is known

### Risk if deferred

- the worktree stays blocked on a partially classified timeout contour and risks routing the wrong timeout-budget or generator fix

### Linked follow-up Task Package(s)

- next proof-lane or timeout-budget TP after deterministic closure of `P1.6o193`

### Expiry/trigger to stop deferral

- immediately after deterministic closure and one fresh guarded rerun classification

## Next-block contract (mandatory)

### Next block objective

- run exactly one fresh guarded rerun after `P1.6o193` so proof surfaces the concrete inner scenario-generation error or the next admissible semantic/proof blocker.

### First deterministic check command

- `python3 -m py_compile ops/diagnose.py truffles-api/tests/test_diagnose_run_command.py && pytest -q truffles-api/tests/test_diagnose_run_command.py -k "scenario_timeout or generate_batch"`

### Blocked-by conditions

- `P1.6o193` deterministic closure is not green
- `P1.6o192` invalid-run truth is not synced
- targeted tooling tests are not green

### Owner role for closure

- `Top Architect + Brain`

## Execution status

- `done`

## Execution result

- `ops/diagnose.py` now preserves bounded last `batch_attempt_error.error` detail inside normalized `scenario_generation_timeout (...)` reasons instead of dropping the inner failure cause.
- `truffles-api/tests/test_diagnose_run_command.py` now proves timeout normalization keeps the surfaced inner error text while generated LLM batch command assembly remains green.
- Deterministic checks are green and `P1.6o194` is published as the next admissible proof lane.

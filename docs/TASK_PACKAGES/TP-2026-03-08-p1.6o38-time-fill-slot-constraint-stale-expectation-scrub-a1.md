# TP-2026-03-08-p1.6o38-time-fill-slot-constraint-stale-expectation-scrub-a1.md

- Название/цель: детерминированно закрыть surfaced `r39` blocker, где exact time fill уже нормализовался из `slot_constraint` в обычный `time`, но stale scenario override продолжал требовать `pending_question_act=slot_constraint` и `trace` для несуществующего interaction.
- Parent TP: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Canon refs: `AGENTS.md`, `STATE.md`, `STRUCTURE.md`, `STRATEGY/REQUIREMENTS.md`, `docs/runbooks/BOOKING_CONFIRM_VERIFY.md`, `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o37-booking-specialist-followup-owner-contract-a1.md`
- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect remove worktree + branch after merge`

## One web search (mandatory before implementation)

- Query: `site:rasa.com/docs/rasa/forms change previously filled slot requested_slot correction form`
- Time (UTC): `2026-03-08T13:22:40Z`
- Sources:
  - `https://rasa.com/docs/rasa/forms/`
- Ready solutions found:
  - when an active form collects slots, a grounded fill that resolves the requested slot should stay a fill and should not keep stale correction/question metadata from the pre-normalized surface class.
- Decision (`reuse/integrate/build`): `reuse + integrate`
  - reuse existing `time` slot-fill contract and normalization pipeline;
  - integrate a stale-override scrub at the taxonomy boundary;
  - build no new runtime branch.
- Rejected options:
  - weakening oracle to accept stale `slot_constraint` meta/trace after tag normalization;
  - changing runtime to emit fake `slot_constraint` evidence for a plain time fill.

## Root cause (mandatory)

- Symptom:
  - guarded `dev L2` rerun `p1.6o6-l2-dev-20260308-a1-r39` no longer fails on `P1.6o37`, but stops on dialog `1`, turn `5`, text `Я хочу записаться на 15:00.` with `expected_meta_mismatch` and `expected_trace_miss`.

- Minimal reproduction:
  1. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r39/scenarios.json` for dialog `1`, turn `5`
  2. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r39/responses.jsonl` for `dialog_index=1`, `turn_index=5`
  3. compare scenario expectation (`pending_question_act=slot_constraint`, `expected_reply_type=time`) against runtime outcome (`question_contract matched`, `booking_prompt`, `expected_reply_type=name`)
  4. inspect `scripts/booking_dialog_scenarios.py` normalization path for `slot_constraint -> time`

- Evidence:
  - scenario turn `5` keeps:
    - `tags=["time"]`
    - `meta_any.pending_question_act=["slot_constraint"]`
    - `trace_contains` with `pending_question_interaction slot_constraint`
  - runtime correctly does:
    - `question_contract matched`
    - `answer_slot=datetime`
    - `action=booking_prompt`
    - `expected_reply_type=name`
  - so the fail is taxonomy/oracle stale state, not runtime behavior.

- Five Whys:
  1. Why did `r39` fail: scenario still expected `slot_constraint` evidence on a plain time fill.
  2. Why did scenario keep that evidence: tag normalization rewrote `slot_constraint -> time`, but did not scrub the old override payload.
  3. Why is that wrong: once a turn is normalized to `time`, the canonical contract is ordinary slot fill, not slot-constraint interaction.
  4. Why did runtime look correct: expected-reply contract matched the time answer and resumed booking collect to `name`.
  5. Why did it surface now: `P1.6o37` removed the prior specialist-followup blocker, exposing the next stale taxonomy family.

- Root cause statement:
  - the scenario normalization pipeline rewrote tags from `slot_constraint` to `time` for grounded exact-time fills, but failed to scrub stale override `meta_any/trace_contains`, so acceptance still demanded the old interaction class after canonical normalization.

- Fix mechanism:
  - make `slot_constraint -> time` normalization return an explicit replacement signal;
  - when that replacement happens, reset the turn override before `_merge_expectations`, so canonical `time` expectations are rebuilt cleanly;
  - cover it with deterministic regression tests only, then rerun guarded `dev L2`.

## Release safety

- Strategy: deterministic taxonomy scrub before guarded rerun
- Go signals:
  - `P1.6o37` remains closed in evidence
  - exact time fill no longer carries stale `slot_constraint` meta/trace
- No-go signals:
  - runtime changes for a pure scenario/oracle defect
  - oracle weakening that accepts stale interaction evidence
- Rollback verification:
  - revert only `scripts/booking_dialog_scenarios.py` and scenario tests if normalization regresses other pending-question families

## Invariant

1. `P1.6o37` stays closed.
2. Genuine `slot_constraint` turns without grounded time fill remain `slot_constraint`.
3. Exact time fills remain canonical `time` slot-fill turns.
4. No new runtime phrase-hardcode is introduced.

## Scope

- `scripts/booking_dialog_scenarios.py`
- `truffles-api/tests/test_booking_dialog_scenarios_script.py`
- `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o38-time-fill-slot-constraint-stale-expectation-scrub-a1.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`

## Out of scope

- runtime changes
- lock/replay/full promotion
- action/schema changes in policy-core

## Touch-list

- `scripts/booking_dialog_scenarios.py`
- `truffles-api/tests/test_booking_dialog_scenarios_script.py`
- `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o38-time-fill-slot-constraint-stale-expectation-scrub-a1.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`

## Plan (1..N)

1. Freeze `r39` as bounded non-closure evidence.
2. Make `slot_constraint -> time` normalization stateful and observable to the sanitizer.
3. Scrub stale override payload when the normalized turn is now canonical `time` fill.
4. Add deterministic regressions and only then authorize guarded `r40`.

## DoD

1. `r39` blocker is documented as taxonomy/oracle stale-state, not runtime.
2. Exact time fills normalized out of `slot_constraint` rebuild clean `time` expectations.
3. Deterministic scenario tests pass.
4. `git diff --check` stays clean.
5. Master/program/proof-bundle docs point to guarded `r40` as the next expensive step.

## Checks

- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r39 --status done --strict-artifacts`
- `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "slot_constraint or specialist_followup or pending_question_target"`
- `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py::test_sanitize_llm_turns_scrubs_stale_slot_constraint_expect_override_after_time_normalization truffles-api/tests/test_booking_dialog_scenarios_script.py::test_sanitize_llm_turns_normalizes_explicit_time_fill_out_of_slot_constraint --junitxml=/tmp/booking_quality/p1.6o38-l1-junit.xml`
- `git diff --check`

## Execution status

- `done`

## Evidence

1. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r39/summary.json`
2. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r39/brief.md`
3. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r39/scenarios.json`
4. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r39/responses.jsonl`
5. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r39/trace_bundle.jsonl`
6. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r39/manual_audit.md`
7. `/tmp/booking_quality/p1.6o38-l1-junit.xml`

## Rollback

1. Revert only the scenario normalization scrub if it creates drift in other pending-question families.
2. Keep `P1.6o6` blocked; do not reinterpret `r39` as closure evidence.

## No-go

- no runtime fix for a scenario-only stale expectation defect
- no fake `slot_constraint` trace/meta to satisfy the old oracle
- no new guarded rerun before deterministic scenario tests are green

## Risks/Blockers

1. Other pending-question normalizers may have similar stale-override leakage and can surface after `r40`.
2. Acceptance runs remain infra-invalid due tool-evidence gaps; proof promotion stays blocked independently of semantic closure.

## Residual architecture debt (mandatory)

- Current residuals accepted in this block:
  - scenario normalization still has multiple stateful rewrite points that can leak stale overrides in other families
- Why not in this block:
  - this block closes only the exact `slot_constraint -> time` stale-expectation family surfaced by `r39`
- Risk if deferred:
  - adjacent normalized families may continue surfacing as post-factum taxonomy drift
- Linked follow-up Task Package(s):
  - `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`
- Expiry/trigger to stop deferral:
  - next surfaced expectation-only failure after `r40`

## Next-block contract (mandatory)

- Next block objective:
  - run a fresh guarded `dev L2` rerun to verify that the stale slot-constraint expectation family no longer surfaces and to expose the next honest blocker
- First deterministic check command:
  - `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py::test_sanitize_llm_turns_scrubs_stale_slot_constraint_expect_override_after_time_normalization truffles-api/tests/test_booking_dialog_scenarios_script.py::test_sanitize_llm_turns_normalizes_explicit_time_fill_out_of_slot_constraint --junitxml=/tmp/booking_quality/p1.6o38-l1-junit.xml && git diff --check`
- Blocked-by conditions:
  - deterministic scenario scrub tests are not green
  - guarded runtime preflight for `r40` is invalid
  - `P1.6o37`/`P1.6o38` docs are not synced
- Owner role for closure:
  - `Hands` for deterministic closure + rerun evidence, `Top Architect + Brain` for proof-bundle promotion

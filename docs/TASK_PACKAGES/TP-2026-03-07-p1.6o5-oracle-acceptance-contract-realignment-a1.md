# TP-2026-03-07-p1.6o5-oracle-acceptance-contract-realignment-a1

- Название/цель: зафиксировать correct `fact/tool/clarify/handoff` oracle для capability-bearing turns в `demo_salon`, чтобы runtime не чинился под неоднозначное ожидание и proof bundle сравнивал корректный бизнес-контракт.
- Parent TP: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Canon refs: `AGENTS.md`, `STATE.md`, `STRUCTURE.md`, `STRATEGY/REQUIREMENTS.md`, `TECH.md`, `docs/runbooks/BOOKING_CONFIRM_VERIFY.md`, `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`

## One web search (mandatory before implementation)

- Status: `completed`
- Exact query: `site:rasa.com/docs testing assistant evaluation official`
- Executed at: `2026-03-07T18:17:00+05:00`
- Opened sources:
  - Rasa Docs, `Testing your assistant`, `https://rasa.com/docs/reference/testing/test-cases/`
  - Rasa Docs, `Assertions`, `https://rasa.com/docs/reference/testing/assertions/`
- Source quality:
  - official vendor testing reference for structured assertions and outcome-level evaluation
- Found ready-made solutions:
  - assertions should validate structured outcomes/events instead of relying on surface wording
  - scenario contracts can combine response presence with state/slot/trace-style assertions
- Decision: `integrate + build`
  - keep Truffles contract-first evaluation model;
  - add structured meta/trace oracle support for capability-bearing turns;
  - reduce phrase-level `must_include` dependence where deterministic runtime evidence already exists
- Rejected options:
  - phrase-only oracle hardening: still ambiguous for capability-bearing turns
  - judge-only arbitration without deterministic contract update: too weak for regression closure
- Intended decision surface:
  - how to encode outcome-level expectations instead of phrase-level expectations

## Root cause (mandatory)

- Symptom:
  - questions like `Можно ли записаться на эту услугу на выходные?` can legitimately map to `hours`, `policy`, `calendar availability`, or `clarify`, but current eval/oracle does not fully freeze which outcome is canonical.
- Minimal reproduction:
  1. inspect `/tmp/booking_quality/p1.6-l2-dev-seed42-r22/manual_audit.md`
  2. inspect current `demo_salon` eval expectations
  3. inspect response guard logic and judge outcome contracts
- Evidence:
  - `/tmp/booking_quality/p1.6-l2-dev-seed42-r22/manual_audit.md`
  - `docs/runbooks/BOOKING_CONFIRM_VERIFY.md`
  - `truffles-api/tests/test_booking_quality_response_guard.py`
  - `truffles-api/tests/test_demo_salon_eval.py`
- Five Whys:
  1. Why can the team fix the wrong thing: oracle leaves source-of-truth ambiguous.
  2. Why is that dangerous: runtime can become tuned to an accidental expectation.
  3. Why is phrase-level oracle insufficient: the same business-correct outcome can be phrased differently.
  4. Why does this matter before proof bundle: dev L2/lock/replay must compare against the right contract.
  5. Why is this a root cause family: ambiguous oracle makes every later red less trustworthy.
- Root cause statement:
  - acceptance oracle for capability-bearing turns is underspecified, so runtime and evaluation can disagree even when the architecture is moving in the right direction.
- Fix mechanism:
  - freeze outcome-level oracle by object, capability, source-of-truth, and allowed clarify/abstain conditions;
  - align deterministic tests and judge/response guard to the same contract.

## Invariant

1. Oracle must validate contract outcome, not exact text.
2. No threshold weakening.
3. No use of oracle change to hide a real runtime bug.

## Scope

- `docs/runbooks/BOOKING_CONFIRM_VERIFY.md`
- `ops/diagnose.py`
- `scripts/booking_dialog_scenarios.py`
- `truffles-api/app/knowledge/demo_salon/EVAL.yaml`
- `truffles-api/tests/test_booking_dialog_scenarios_script.py`
- `truffles-api/tests/test_diagnose_run_command.py`
- `truffles-api/tests/test_booking_quality_response_guard.py`
- `truffles-api/tests/test_demo_salon_eval.py`
- child TPs whose expectations require clarification

## Out of scope

- runtime-state implementation
- broad eval rewrite unrelated to current `demo_salon` families
- expensive runs until oracle is frozen

## Touch-list

- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o5-oracle-acceptance-contract-realignment-a1.md`
- `docs/runbooks/BOOKING_CONFIRM_VERIFY.md`
- `ops/diagnose.py`
- `scripts/booking_dialog_scenarios.py`
- `truffles-api/app/knowledge/demo_salon/EVAL.yaml`
- `truffles-api/tests/test_booking_dialog_scenarios_script.py`
- `truffles-api/tests/test_diagnose_run_command.py`
- `truffles-api/tests/test_booking_quality_response_guard.py`
- `truffles-api/tests/test_demo_salon_eval.py`

## Plan (1..N)

1. Freeze canonical outcome rules for capability-bearing turns.
2. Align deterministic acceptance/response-guard expectations.
3. Document allowed `fact/tool/clarify/handoff` boundaries.
4. Keep proof bundle blocked until this oracle is green.

## DoD

1. Ambiguous capability turns have one documented canonical outcome contract.
2. Deterministic tests and quality guards enforce that contract.
3. No phrase-level must-include workaround is introduced.

## Checks

- `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py`
- `pytest -q truffles-api/tests/test_diagnose_run_command.py`
- `pytest -q truffles-api/tests/test_booking_quality_response_guard.py`
- `pytest -q truffles-api/tests/test_demo_salon_eval.py`
- `pytest -q truffles-api/tests/test_message_endpoint.py`
- `ruff check ops/diagnose.py scripts/booking_dialog_scenarios.py truffles-api/tests/test_booking_dialog_scenarios_script.py truffles-api/tests/test_diagnose_run_command.py truffles-api/tests/test_booking_quality_response_guard.py truffles-api/tests/test_demo_salon_eval.py`
- `git diff --check`

## Execution status

- `completed`
- closure date: `2026-03-07`
- upstream dependency status: `P1.6o3` and `P1.6o4` already green in this worktree
- deterministic closure: `green`

## Evidence

1. Structured oracle surface added:
   - `ops/diagnose.py` now treats `expect.meta`, `expect.meta_any`, `expect.meta_contains`, and `expect.trace_contains` as strong contract expectations and evaluates them with dedicated failure reasons
   - `scripts/booking_dialog_scenarios.py` now preserves those structured oracle fields in scenario overrides/generation
   - `docs/runbooks/BOOKING_CONFIRM_VERIFY.md` now documents when capability-bearing turns must use structured meta/trace oracle instead of phrase-level wording
2. Demo-salon ambiguous cases realigned:
   - consult-followup booking CTA (`E566`, `E568`) now freezes `booking_prompt + expected_reply_type=service_choice`
   - service-known weekend booking (`SCN5-03`) now freezes `booking_prompt + expected_reply_type=time`
   - service-missing booking with time only (`SCN4-07`) now freezes `booking_prompt + expected_reply_type=service_choice`
3. Deterministic outputs:
   - `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py` -> `31 passed`
   - `pytest -q truffles-api/tests/test_diagnose_run_command.py` -> `16 passed`
   - `pytest -q truffles-api/tests/test_booking_quality_response_guard.py` -> `65 passed`
   - `pytest -q truffles-api/tests/test_demo_salon_eval.py` -> `20 passed`
   - `pytest -q truffles-api/tests/test_message_endpoint.py` -> `310 passed, 2 warnings`
   - `ruff check ops/diagnose.py scripts/booking_dialog_scenarios.py truffles-api/tests/test_booking_dialog_scenarios_script.py truffles-api/tests/test_diagnose_run_command.py truffles-api/tests/test_booking_quality_response_guard.py truffles-api/tests/test_demo_salon_eval.py` -> `All checks passed!`
   - `git diff --check` -> `clean`

## Rollback

1. revert only oracle/guard changes from this block
2. keep runtime fixes visible and keep `P1.6o` open

## No-go

- no oracle weakening to make runs look green
- no phrase-driven must-include as primary acceptance mechanism
- no expensive runs before deterministic closure

## Risks/Blockers

1. Oracle realignment may reveal stale scenario expectations.
2. Judge rubric changes can lag behind deterministic guard changes.

## Residual architecture debt (mandatory)

- Current residuals accepted in this block:
  - multi-pack/open-world oracle remains outside `demo_salon`
- Why not in this block:
  - this block only freezes the `demo_salon` closure oracle
- Risk if deferred:
  - proof bundle can still judge the wrong outcome
- Linked follow-up Task Package(s):
  - `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`
- Expiry/trigger to stop deferral:
  - before the first fresh guarded `dev L2` after runtime fixes

## Next-block contract (mandatory)

- Next block objective:
  - run `P1.6o6` proof bundle only after runtime path and oracle are both green
- First deterministic check command:
  - `pytest -q truffles-api/tests/test_booking_quality_response_guard.py && pytest -q truffles-api/tests/test_demo_salon_eval.py`
- Blocked-by conditions:
  - runtime path blocks not green
  - oracle not yet frozen in deterministic checks
- Owner role for closure:
  - `Top Architect + Brain`

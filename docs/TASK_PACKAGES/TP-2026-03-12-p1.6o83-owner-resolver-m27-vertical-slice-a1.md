# TP-2026-03-12-p1.6o83-owner-resolver-m27-vertical-slice-a1

- Название/цель: перевести `M27` из documentary owner row в первый исполняемый runtime slice: machine-readable row contract + pure `OwnerResolver` + bounded integration в `decision.py`, чтобы pricing interrupt после `booking_confirm_reject` мог восстановить grounded service из persisted `InteractionState`, сохранить `expected_reply_type=time` и зафиксировать deterministic owner evidence.

## Canon refs

- Parent verdict: `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o60a-remaining-closure-architecture-verdict-a1.md`
- Owner matrix: `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o60b-remaining-closure-owner-matrix-a1.md`
- Redesign reset: `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o79-executable-interaction-core-redesign-reset-a1.md`
- Base canon sync: `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o80-base-canon-interaction-model-sync-a1.md`
- Machine-readable matrix: `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o81-machine-readable-owner-matrix-a1.md`
- Persisted InteractionState: `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o82-persisted-interaction-state-a1.md`
- Tactical M27 row: `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o78-booking-confirm-pricing-interrupt-service-choice-drift-a1.md`
- STATE: `STATE.md`
- CA: remaining closure row `M27`

## Git context

- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect` remove branch + worktree after merge

## One web search (mandatory before implementation)

- Query: `site:stately.ai/docs context guards transitions state machine pure transition context`
- Date/time: `2026-03-12T09:54:39+05:00`
- Sources opened:
  - official Stately docs: `https://stately.ai/docs/context`
  - official Stately docs: `https://stately.ai/docs/guards`
  - official Stately docs: `https://stately.ai/docs/transitions`
- Source quality:
  - official product documentation
- Found reusable ideas:
  - owner resolution should be a pure transition over persisted context plus current event, not a scattered set of branches;
  - guards should validate transition boundaries without becoming the semantic owner;
  - runtime state should carry enough context to let side paths resume the active interaction deterministically.
- Decision:
  - `integrate/build`
  - integrate the state-machine discipline, but build an internal bounded resolver because swapping the webhook runtime to an external framework in this block would be too invasive.
- Rejected options:
  - another direct `decision.py` / `booking.py` row patch without a reusable resolver;
  - broad migration of all matrix rows in one block;
  - weakening M27 oracle to accept `service_choice` reopen.

## Root cause (mandatory)

- Symptom:
  - after `P1.6o82`, `M27` is still open because runtime still has no executable owner step that consumes persisted `InteractionState` plus the machine-readable matrix to recover grounded service and emit owner evidence for pricing interrupt after `booking_confirm_reject`.
- Minimal reproduction:
  1. Inspect `truffles-api/app/knowledge/generic/INTERACTION_OWNER_MATRIX.yaml` row `M27` and confirm it is still the only `open` row.
  2. Inspect `truffles-api/app/routers/webhook/decision.py` policy-info/service-clarify arbitration and confirm there is no owner resolver between `policy_info_set=pricing` and `service_clarify` fallback.
  3. Inspect `truffles-api/app/routers/webhook/booking.py` pricing interrupt path and confirm service-enriched pricing only consults current booking state / local carryover, not the persisted `interaction_state` contract.
  4. Reconfirm audited evidence in `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r112` dialog `1`, turns `3-4`: after `booking_confirm_reject`, the pricing interrupt reopens `service_choice` instead of preserving grounded service + `time` resume.
- Evidence:
  - `truffles-api/app/knowledge/generic/INTERACTION_OWNER_MATRIX.yaml`
  - `contracts/policy/interaction_owner_matrix.v1.jsonschema`
  - `truffles-api/app/routers/webhook/decision.py`
  - `truffles-api/app/routers/webhook/booking.py`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r112/summary.json`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r112/responses.jsonl`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r112/trace_bundle.jsonl`
- Five Whys:
  1. Why is `M27` still open? Because the system has matrix data and persisted interaction state, but no executable resolver that binds them on the turn.
  2. Why does the turn still drift? Because pricing interrupt arbitration only sees branch-local booking/service hints and not a canonical owner decision.
  3. Why is grounded service still lossy? Because `booking.py` pricing enrichment depends on current booking state; it does not contractually recover service from persisted `interaction_state` referents.
  4. Why is owner evidence still weak? Because `decision.py` records row-specific metadata directly, but no pure resolver emits a row id / execution owner / reason code before the runtime branch executes.
  5. Why does this block matter architecturally? Because without one resolver-driven row slice, the machine-readable matrix remains documentary and cannot prove the redesign chain is executable.
- Root cause statement:
  - the runtime still lacks a contract-driven owner resolver that matches `M27` from machine-readable row data and persisted `interaction_state`, so pricing interrupt after `booking_confirm_reject` depends on ad hoc branch-local carryover and can still reopen `service_choice` when grounded service is absent from current booking state.
- Fix mechanism:
  - extend the owner-matrix artifact with bounded runtime-match/runtime-effects fields for `M27`;
  - add a cached loader for the matrix and a pure `OwnerResolver` that matches `M27` against persisted `interaction_state` + current interrupt facts;
  - integrate the resolver in `decision.py` before pricing/service-clarify arbitration so it can recover grounded service, stamp `interaction_owner`, and emit trace/meta;
  - add targeted unit + integration tests, including one adjacent negative case where grounded service is genuinely missing.

## Reuse-first decision (mandatory)

- Reuse:
  - `INTERACTION_OWNER_MATRIX.yaml` + existing JSON schema lane from `P1.6o81`
  - persisted `interaction_state` lane from `P1.6o82`
  - existing booking interrupt pricing reply path in `booking.py`
- Integrate:
  - one matrix loader service
  - one pure owner resolver
  - one bounded `decision.py` hook before pricing/service-clarify arbitration
- Build:
  - no new routing subsystem
  - no global state-machine framework migration in this block

## Release safety

- Rollout: guarded local deterministic slice only; no rollout/promotion beyond worktree in this block
- Go signal:
  - resolver unit tests green
  - targeted `test_message_endpoint` pricing-interrupt regression green
  - matrix contract tests green
- No-go signal:
  - service-missing pricing clarify regresses
  - M27 positive row still reopens `service_choice`
  - `git diff --check` or `session_check` fail
- Rollback procedure:
  - revert only the new owner-resolver files, matrix runtime fields, and bounded `decision.py` hook
  - keep `P1.6o79..P1.6o82` doc/canon/state work intact

## Invariant

1. `P1.6o79..P1.6o82` remain binding.
2. `M27` is the only open row until new evidence proves otherwise.
3. No phrase/regex semantic hardcode in runtime core.
4. Genuine service-missing pricing questions can still use `service_clarify`.
5. No broad refactor of booking flow or global owner migration in this block.

## Scope

- add executable runtime fields for `M27` in the owner-matrix artifact/schema
- add cached matrix loader + pure owner resolver
- integrate one bounded resolver hook for pricing interrupt after `booking_confirm_reject`
- add targeted unit/integration tests and sync active docs/state/session

## Out of scope

- fresh guarded `dev L2` / lock / replay / full runs
- migration of rows other than `M27`
- broad booking-confirm redesign
- model-routing unification

## Touch-list

- `contracts/policy/interaction_owner_matrix.v1.jsonschema`
- `truffles-api/app/knowledge/generic/INTERACTION_OWNER_MATRIX.yaml`
- `truffles-api/app/services/interaction_owner_matrix_service.py`
- `truffles-api/app/services/owner_resolver.py`
- `truffles-api/app/routers/webhook/decision.py`
- `truffles-api/tests/test_interaction_owner_matrix_contract.py`
- `truffles-api/tests/test_owner_resolver.py`
- `truffles-api/tests/test_message_endpoint.py`
- `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o83-owner-resolver-m27-vertical-slice-a1.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o78-booking-confirm-pricing-interrupt-service-choice-drift-a1.md`
- `STATE.md`
- `STRUCTURE.md`
- `docs/SESSIONS/SESSION-2026-02-19-llm-first-firebreak-a1.md`

## Plan (1..N)

1. Publish `P1.6o83` with RCA + web-search + bounded touch-list.
2. Extend the owner-matrix schema/artifact with runtime match/effects for `M27`.
3. Implement cached loader + pure resolver.
4. Integrate resolver into `decision.py` before pricing/service-clarify arbitration and stamp owner/meta/trace.
5. Add unit + integration tests, including an adjacent negative service-missing case.
6. Run deterministic checks and sync docs/state/session.

## DoD

1. Owner matrix has machine-readable runtime fields for `M27`.
2. Runtime exposes a pure resolver that consumes matrix + persisted `interaction_state`.
3. Positive M27 regression recovers grounded service from persisted interaction state and preserves `expected_reply_type=time`.
4. Adjacent negative service-missing pricing case still yields `service_clarify`.
5. Targeted deterministic tests are green.
6. Active remediation/closure docs point to the next admissible proof block rather than another row patch.

## Checks

- `pytest -q truffles-api/tests/test_interaction_owner_matrix_contract.py truffles-api/tests/test_owner_resolver.py`
- `pytest -q truffles-api/tests/test_message_endpoint.py -k "owner_resolver_m27 or booking_confirmation_pending or reschedule_pricing_interrupt"`
- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r112 --status done --strict-artifacts`
- `git diff --check`
- `SESSION_AGENT=a1 scripts/session_check.sh`

## Execution status

- `done`
- published matrix runtime fields for `M27`, cached loader, pure resolver, and one bounded `decision.py` hook
- deterministic slice is green:
  - `pytest -q truffles-api/tests/test_interaction_owner_matrix_contract.py truffles-api/tests/test_owner_resolver.py truffles-api/tests/test_message_endpoint.py -k "owner_resolver_m27 or booking_confirmation_pending or reschedule_pricing_interrupt"` -> `4 passed`
  - `python3 -m py_compile truffles-api/app/services/interaction_owner_matrix_service.py truffles-api/app/services/owner_resolver.py truffles-api/app/routers/webhook/decision.py truffles-api/tests/test_interaction_owner_matrix_contract.py truffles-api/tests/test_owner_resolver.py truffles-api/tests/test_message_endpoint.py` -> `pass`
  - `git diff --check` -> `pass`
  - `SESSION_AGENT=a1 scripts/session_check.sh` -> `pass`
- `M27` remains open truthfully until `P1.6o84` runs the proof lane

## Evidence

1. Updated matrix schema/artifact diff
2. New resolver code diff
3. Targeted pytest output
4. `llm-quality-audit` output for frozen `r112`
5. Updated `STATE.md` / session log references

## Rollback

1. Revert only `P1.6o83` code/doc deltas.
2. Keep `P1.6o79..P1.6o82` intact.
3. If the adjacent negative row regresses, back out the resolver hook before any guarded rerun.

## No-go

- no new branch-local `if message == ...` pricing fixes
- no weakening of `M27` oracle or expected-reply contract
- no broad migration of other matrix rows in this block
- no guarded rerun before deterministic green

## Risks / blockers

- `decision.py` and `test_message_endpoint.py` are already heavily modified in this worktree, so the hook must stay minimal.
- `M27` still depends on frozen invalid run `r112` for historical surfacing evidence; this block cannot claim proof closure.
- The runtime row contract may still need a follow-up proof block even after deterministic green.

## Residual architecture debt (mandatory)

### Current residuals accepted in this block

- Only `M27` gets executable row matching/effects in this block.
- Global owner routing remains in `decision.py` until later extraction.
- Model-routing parity risk remains unresolved.

### Why not in this block

- The first objective is to prove the redesign chain is executable with one bounded row slice before migrating the full matrix.

### Risk if deferred

- Additional rows may still rely on documentary matrix data and branch-local carryover instead of the resolver.

### Linked follow-up Task Package(s)

- `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o84-m27-proof-lane-and-next-row-admission-a1.md`

### Expiry/trigger to stop deferral

- before any new guarded rerun claim for `M27` or before admitting a second resolver-driven row without proof of this slice.

## Next-block contract (mandatory)

### Next block objective

- run the first proof lane for the resolver-driven `M27` slice: deterministic contract corpus + fresh guarded `dev L2` admission decision, then either close `M27` boundedly or publish the next surfaced row truthfully.

### First deterministic check command

- `pytest -q truffles-api/tests/test_interaction_owner_matrix_contract.py truffles-api/tests/test_owner_resolver.py truffles-api/tests/test_message_endpoint.py -k "owner_resolver_m27 or booking_confirmation_pending or reschedule_pricing_interrupt" && python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r112 --status done --strict-artifacts && git diff --check`

### Blocked-by conditions

- `P1.6o83` deterministic checks are red
- owner resolver does not stamp row id / execution owner evidence
- positive M27 regression still reopens `service_choice` or drops `expected_reply_type=time`

### Owner role for closure

- `Top Architect + Brain` for proof-lane admission and closure verdict

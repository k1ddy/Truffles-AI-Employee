# TP-2026-03-14-p1.6o167-active-time-choose-specialist-master-interrupt-owner-a1

- Название/цель: закрыть surfaced `P1.6o166` runtime response/owner drift, где question-like generic specialist-choice turn (`"А можно выбрать специалиста?"`) под active `expected_reply_type=time` сохраняет `booking_specialist_followup` trace/meta, но отвечает голым `MSG_BOOKING_ASK_DATETIME` и проваливает `info_section_miss + judge_fail` вместо bounded master-info interrupt с сохранённым `time` resume.

## Canon refs

- Parent closure program: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Main remediation: `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- Binding roadmap: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o135-beauty-salon-canonical-control-plane-roadmap-a1.md`
- Prior proof lane: `docs/TASK_PACKAGES/TP-2026-03-14-p1.6o166-post-p1.6o165-proof-lane-a1.md`
- Related runtime slices: `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o93-generic-specialist-choice-followup-owner-a1.md`, `docs/TASK_PACKAGES/TP-2026-03-14-p1.6o163-timeout-active-time-choose-specialist-interrupt-recovery-a1.md`
- Class routing: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o118-class-level-closure-process-reset-a1.md`
- State baseline: `STATE.md`

## Git context

- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `origin/main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect` remove branch + worktree after merge

## One web search (mandatory before implementation)

- Query: `site:stately.ai/docs/guards guarded transitions explicit conditions state machine`
- Date/time: `2026-03-14T10:04:25+05:00`
- Sources opened:
  - Stately docs: `https://stately.ai/docs/guards`
- Source quality:
  - official vendor documentation
- Found reusable ideas:
  - guarded transitions should move a turn into a different bounded path when explicit conditions are true;
  - the decision should be expressed as a narrow guard on observable context, not by broad side effects later in the branch.
- Decision:
  - `reuse`
  - reuse the existing master-info interrupt path and widen only the success-path guard so question-like explicit choose-specialist turns enter that interrupt when no named specialist is present.
- Rejected options:
  - weakening proof/oracle to accept generic datetime prompt as sufficient answer;
  - broad specialist followup redesign;
  - phrase-hardcoding reply text without changing the bounded routing contract.

## Root cause (mandatory)

- Symptom:
  - `P1.6o166` strict-audited rerun is `infra_valid=true`, `semantic_valid=false` and surfaces `dialog 1 / turn 5 / "А можно выбрать специалиста?"`, where runtime keeps `pending_question_interaction=booking_specialist_followup` and `pending_question_target=specialist`, but the actual reply is still `На какую дату и время вам удобно?`, producing `info_section_miss` and judge `missed_question`.
- Minimal reproduction:
  1. Inspect `/tmp/booking_quality/p1.6o166-l2-dev-20260314-a1-r1/responses.jsonl` turn `5` and confirm `decision_meta.action=booking_prompt`, `source=llm_policy_core`, `pending_question_target=specialist`, `active_question_relation=referent_followup`, `info_answered.master=False`, and `outbox_text="На какую дату и время вам удобно?"`.
  2. Inspect `/tmp/booking_quality/p1.6o166-l2-dev-20260314-a1-r1/trace_bundle.jsonl` turn `5` and confirm trace records `pending_question_interaction decision=booking_specialist_followup`, but no `booking_interrupt info_reply` with `info_sections=["master"]`.
  3. Inspect `truffles-api/app/routers/webhook/decision.py` success path and confirm `_should_interrupt_generic_master_info_specialist_followup(...)` rejects explicit choose/pick signals, so `"выбрать специалиста"` stays on the specialist-followup branch.
  4. Confirm `_format_specialist_followup_prompt(...)` only changes the prompt when a concrete `specialist_name` exists; without a named specialist it returns the base datetime prompt unchanged.
- Evidence:
  - `/tmp/booking_quality/p1.6o166-l2-dev-20260314-a1-r1/summary.json`
  - `/tmp/booking_quality/p1.6o166-l2-dev-20260314-a1-r1/brief.md`
  - `/tmp/booking_quality/p1.6o166-l2-dev-20260314-a1-r1/responses.jsonl`
  - `/tmp/booking_quality/p1.6o166-l2-dev-20260314-a1-r1/trace_bundle.jsonl`
  - `truffles-api/app/routers/webhook/decision.py`
  - `truffles-api/tests/test_message_endpoint.py`
  - `truffles-api/tests/test_booking_quality_response_guard.py`
- Five Whys:
  1. Why did the turn fail? Because the bot did not answer the specialist-choice question and only repeated the generic datetime prompt.
  2. Why did it repeat the generic datetime prompt? Because runtime stayed on the plain specialist-followup collect branch.
  3. Why did runtime stay on that branch? Because the success-path generic master-info interrupt guard explicitly excludes choose/pick signals.
  4. Why is the followup branch insufficient here? Because `_format_specialist_followup_prompt(...)` only adds an acknowledgment when a concrete specialist name is present; generic choose-specialist questions receive no info answer at all.
  5. Why is this architectural debt? Because success-path routing diverges from the already-fixed timeout path for the same semantic envelope and leaves the user’s explicit specialist-choice question unanswered.
- Root cause statement:
  - explicit question-like choose-specialist turns under active `time` collect are incorrectly kept on the plain `booking_specialist_followup` success path, whose generic prompt cannot answer the specialist question when no named specialist is present.
- Fix mechanism:
  - widen the bounded success-path guard so explicit choose/pick-specialist questions with no named specialist reuse the existing master-info interrupt path and preserve `time` resume;
  - add targeted endpoint and response-guard regressions proving the turn now emits master interrupt evidence instead of bare specialist-followup prompt.

## Reuse-first decision (mandatory)

- Reuse:
  - existing `_should_interrupt_generic_master_info_specialist_followup(...)`
  - existing `_handle_booking_interrupt(...)`
  - existing active-time generic master-info interrupt response/guard contract
- Integrate:
  - widen one success-path guard; do not build a new owner branch
- Build:
  - no new dependency or framework

## Invariant

1. Named-specialist followup behavior remains unchanged.
2. Existing generic master-info interrupt behavior stays unchanged outside the explicit choose/pick-specialist envelope.
3. Timeout choose-specialist recovery from `P1.6o163` remains unchanged.
4. Runtime/oracle are not weakened.

## Scope

- `truffles-api/app/routers/webhook/decision.py`
- `truffles-api/tests/test_message_endpoint.py`
- `truffles-api/tests/test_booking_quality_response_guard.py`
- truth sync docs for `P1.6o166` / `P1.6o167`

## Out of scope

- scenario-governance changes
- proof-lane rerun itself
- broad specialist followup redesign
- acceptance/lock updates

## Touch-list

- `truffles-api/app/routers/webhook/decision.py`
- `truffles-api/tests/test_message_endpoint.py`
- `truffles-api/tests/test_booking_quality_response_guard.py`
- `STATE.md`
- `docs/SESSIONS/SESSION-2026-02-19-llm-first-firebreak-a1.md`
- `docs/SESSION_INDEX.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-14-p1.6o166-post-p1.6o165-proof-lane-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-14-p1.6o167-active-time-choose-specialist-master-interrupt-owner-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-14-p1.6o168-post-p1.6o167-proof-lane-a1.md`

## Plan

1. Freeze `P1.6o166` truth and publish `P1.6o167`.
2. Widen the success-path guard so explicit choose/pick-specialist questions enter master-info interrupt when no named specialist is present.
3. Add endpoint and response-guard regressions for the surfaced contour.
4. Run targeted deterministic checks.
5. Sync truth and publish the next proof lane.

## DoD

1. The surfaced `dialog 1 / turn 5` contour no longer answers with bare `MSG_BOOKING_ASK_DATETIME`.
2. Runtime emits master interrupt evidence (`intent=master`, `booking_interrupt_info=true`, `info_sections=["master"]`) while preserving `expected_reply_type=time`.
3. Existing named-specialist followup and active-time generic master-info interrupt regressions remain green.
4. Active docs/state/session reflect that `P1.6o166` surfaced a bounded runtime owner/response family and `P1.6o167` is the selected continuation.

## Checks

- `python3 -m py_compile truffles-api/app/routers/webhook/decision.py truffles-api/tests/test_message_endpoint.py truffles-api/tests/test_booking_quality_response_guard.py`
- `pytest -q truffles-api/tests/test_message_endpoint.py -k "generic_master_info_interrupt_resumes_time_collect or generic_master_info_interrupt_bypasses_specialist_followup_and_keeps_time_resume or choose_specialist_question_rewrites_collect_to_master_info"`
- `pytest -q truffles-api/tests/test_booking_quality_response_guard.py -k "active_time_generic_master_info_interrupt or choose_specialist_master_interrupt"`
- `git diff --check`
- `SESSION_AGENT=a1 scripts/session_check.sh`

## Evidence

1. `/tmp/booking_quality/p1.6o166-l2-dev-20260314-a1-r1/summary.json`
2. `/tmp/booking_quality/p1.6o166-l2-dev-20260314-a1-r1/brief.md`
3. `/tmp/booking_quality/p1.6o166-l2-dev-20260314-a1-r1/responses.jsonl`
4. `/tmp/booking_quality/p1.6o166-l2-dev-20260314-a1-r1/trace_bundle.jsonl`
5. `truffles-api/app/routers/webhook/decision.py`
6. `truffles-api/tests/test_message_endpoint.py`
7. `truffles-api/tests/test_booking_quality_response_guard.py`

## Rollback

1. Revert the bounded success-path guard widening and its regression tests.
2. Keep `P1.6o166` truth frozen as the surfaced runtime owner/response drift.
3. Do not reinterpret the failure as scenario-only drift without new evidence.

## No-go

- no oracle weakening
- no broad specialist followup redesign
- no phrase-hardcode in `llm_policy_core`
- no proof rerun before deterministic closure

## Risks / blockers

- overbroad guard widening could steal non-question specialist followup turns into master interrupt
- under-scoping the fix could still leave choose-specialist turns on the generic datetime prompt
- next proof may surface an adjacent active-time specialist family once this miss moves off first fail

## Residual architecture debt (mandatory)

### Current residuals accepted in this block

- runtime success-path branching still lives in `decision.py`
- specialist choice vs master-info distinction is still implemented through bounded guards, not an explicit state model
- proof bundle closure remains incomplete

### Why not in this block

- this block is a bounded runtime owner/response correction only

### Risk if deferred

- expensive proof remains blocked by a corroborated runtime miss on an explicit specialist-choice question

### Linked follow-up Task Package(s)

- `docs/TASK_PACKAGES/TP-2026-03-14-p1.6o168-post-p1.6o167-proof-lane-a1.md`

### Expiry/trigger to stop deferral

- before any new expensive rerun after `P1.6o166`

## Next-block contract (mandatory)

### Next block objective

- publish and run one fresh proof-lane rerun after deterministic closure of `P1.6o167`, then truthfully classify the next surfaced family.

### First deterministic check command

- `python3 -m py_compile truffles-api/app/routers/webhook/decision.py truffles-api/tests/test_message_endpoint.py truffles-api/tests/test_booking_quality_response_guard.py && pytest -q truffles-api/tests/test_message_endpoint.py -k "generic_master_info_interrupt_resumes_time_collect or generic_master_info_interrupt_bypasses_specialist_followup_and_keeps_time_resume or choose_specialist_question_rewrites_collect_to_master_info" && pytest -q truffles-api/tests/test_booking_quality_response_guard.py -k "active_time_generic_master_info_interrupt or choose_specialist_master_interrupt" && git diff --check`

### Blocked-by conditions

- `P1.6o166` truth is not synced in active docs/state/session
- targeted `P1.6o167` runtime checks are not green
- the team attempts another expensive rerun before deterministic closure of this block

### Owner role for closure

- `Top Architect + Brain`

## Execution status

- `done`

## Execution result

- `truffles-api/app/routers/webhook/decision.py` no longer excludes explicit choose/pick-specialist questions from `_should_interrupt_generic_master_info_specialist_followup(...)`, so the surfaced success-path contour now reuses the existing master-info interrupt response before resuming active `time` collect.
- `truffles-api/tests/test_message_endpoint.py` now proves question-like `"Можно выбрать мастера?"` turns answer through bounded master interrupt (`intent=master`, `booking_interrupt_info=true`, `info_sections=["master"]`) instead of bare specialist-followup prompt.
- `truffles-api/tests/test_booking_quality_response_guard.py` now accepts that same contract-first master interrupt envelope and explicitly rejects `info_section_miss` for the bounded choose-specialist contour.

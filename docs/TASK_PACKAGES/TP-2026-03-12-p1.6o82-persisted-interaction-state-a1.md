# TP-2026-03-12-p1.6o82-persisted-interaction-state-a1

## Block identity
- `BLOCK_ID`: `P1.6O82-PERSISTED-INTERACTION-STATE-A1`
- `PARENT_BLOCK_ID`: `P1.6O81-MACHINE-READABLE-OWNER-MATRIX-A1`
- `DEPENDS_ON`: `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o79-executable-interaction-core-redesign-reset-a1.md`, `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o80-base-canon-interaction-model-sync-a1.md`, `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o81-machine-readable-owner-matrix-a1.md`, audited `r112` evidence in `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r112`
- `UNLOCKS`: `P1.6O83-OWNER-RESOLVER-M27-VERTICAL-SLICE-A1`

## Название/цель
Сделать `InteractionState` first-class persisted runtime state: добавить явную нормализацию и хранение active pending-question row в canonical dialog state и `session_memory`, чтобы `resume_slot / interaction_target / interaction_relation / interaction_owner / grounded_referents / confirmation_state / degrade_reason` больше не жили только в текущем turn payload и scattered traces.

## Canon refs
- `AGENTS.md`
- `STATE.md`
- `STRUCTURE.md`
- `STRATEGY/REQUIREMENTS.md`
- `SPECS/CONSULTANT.md`
- `SPECS/ARCHITECTURE.md`
- `docs/runbooks/BOOKING_CONFIRM_VERIFY.md`
- `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o60a-remaining-closure-architecture-verdict-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o60b-remaining-closure-owner-matrix-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o79-executable-interaction-core-redesign-reset-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o80-base-canon-interaction-model-sync-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o81-machine-readable-owner-matrix-a1.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`

## FACT pre-check (before implementation)
- `Impacted runtime/tests/docs`:
  - `truffles-api/app/schemas/webhook.py`
  - `truffles-api/app/routers/webhook/context_manager.py`
  - `truffles-api/app/routers/webhook/session_memory.py`
  - `truffles-api/app/routers/webhook/decision.py`
  - `truffles-api/tests/test_message_endpoint.py`
  - `truffles-api/tests/test_state_service.py`
- `Baseline commands`:
  - `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r112 --status done --strict-artifacts`
  - `sed -n '449,457p' SPECS/ARCHITECTURE.md && sed -n '505,534p' SPECS/ARCHITECTURE.md`
  - `sed -n '57,120p' truffles-api/app/routers/webhook/context_manager.py`
  - `sed -n '9290,9355p' truffles-api/app/routers/webhook/decision.py`
  - `sed -n '20,50p' truffles-api/app/services/state_service.py`
  - `sed -n '111,135p' truffles-api/app/routers/webhook/pending.py`
- `FACT findings`:
  - base canon already requires `InteractionState` as first-class persisted state in `SPECS/ARCHITECTURE.md`, but `context_manager.py` currently normalizes only `current_referents`, `pending_question_contract`, and `consult_state`; `interaction_state` is absent.
  - the policy-core/runtime memory payload built in `decision.py` still exposes only `pending_question_contract` and referents; there is no first-class persisted interaction row to carry `interaction_target`, `interaction_relation`, or owner evidence between turns.
  - pending-resume snapshot plumbing already persists `context_manager` and `session_memory`, so a canonical `interaction_state` nested under those existing containers will survive pending/manager handoff without inventing a new top-level store.
  - `r112` remains non-canonical proof evidence; this block is runtime-state plumbing only and does not attempt guarded rerun closure.
- `Detected drift (canon vs runtime)`:
  - `SPECS/ARCHITECTURE.md` and `SPECS/CONSULTANT.md` already define `interaction_state`, but runtime persistence still stops at `pending_question_contract`, so the canon is ahead of the executable state model.

## One web search (mandatory before implementation)
- **Query (exact):** `site:stately.ai/docs context guards state persistence state machine context`
- **Date/time (local, reviewed):** `2026-03-12 09:27 +0500`
- **Why this query is precise:** it targets official state-machine guidance for explicit persisted context, pure guard boundaries, and state persistence; those are the exact concerns for introducing `InteractionState` without widening semantic hardcode.
- **Sources opened (from this query):**
  - `Stately / XState Context` — `https://stately.ai/docs/context`
  - `Stately / XState Guards` — `https://stately.ai/docs/guards`
  - `Stately / XState Persistence` — `https://stately.ai/docs/persistence`
- **Source quality:** official documentation / primary source.
- **Existing solutions found:**
  - active extended state should be stored explicitly in context instead of reconstructed from scattered branches;
  - guard logic should validate / block / degrade transitions, not invent semantic ownership post hoc;
  - persisted state should be restored from the same canonical structure that runtime mutates, instead of copying semantics into parallel ad hoc stores.
- **Decision:** `reuse + integrate` — reuse existing `context_manager.canonical_dialog_state` + `session_memory` + pending-resume snapshot plumbing, and integrate `interaction_state` into those containers rather than creating a new top-level runtime silo.
- **Rejected options:**
  - keep `interaction_target` / `interaction_relation` only in per-turn policy payload and trace/meta;
  - add a brand-new top-level context store separate from `context_manager` and `session_memory`;
  - start `OwnerResolver` extraction before the persisted state contract exists.

## Root cause (mandatory)
- **Symptom:** parent verdict, base canon, and machine-readable owner matrix all treat `InteractionState` as a first-class contract, but runtime still persists only `pending_question_contract`, so active pending-question target/relation/owner evidence can disappear between turns and during pending/handover transitions.
- **Minimal reproduction:**
  1. read `SPECS/ARCHITECTURE.md:449-457` and `SPECS/ARCHITECTURE.md:505-533`; confirm `InteractionState` is canonical and must persist through `session_memory` / `pending_resume`.
  2. read `truffles-api/app/routers/webhook/context_manager.py:57-120`; confirm canonical dialog state does not normalize `interaction_state`.
  3. read `truffles-api/app/routers/webhook/decision.py:9290-9355`; confirm memory payload still projects only `pending_question_contract` and `current_referents`.
  4. read `truffles-api/app/services/state_service.py:20-50` and `truffles-api/app/routers/webhook/pending.py:111-135`; confirm pending-resume already captures `context_manager` and `session_memory`, so the persistence gap is inside those structures, not in snapshot transport.
- **Evidence to capture:**
  - `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o82-persisted-interaction-state-a1.md`
  - `truffles-api/app/schemas/webhook.py`
  - `truffles-api/app/routers/webhook/context_manager.py`
  - `truffles-api/app/routers/webhook/session_memory.py`
  - `truffles-api/app/routers/webhook/decision.py`
  - `truffles-api/tests/test_message_endpoint.py`
  - `truffles-api/tests/test_state_service.py`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r112/summary.json`
- **Five Whys (or equivalent):**
  1. Why does the interaction model remain documentary? Because runtime only persists `pending_question_contract`, not the full interaction row.
  2. Why is `pending_question_contract` insufficient? Because it preserves only resume-slot facts (`slot`, `expected_reply_type`, `reason`, `value`) and drops target/relation/owner/degrade continuity.
  3. Why does that matter across turns? Because pending resumes, side-questions, and next-turn policy-core calls all reconstruct from incomplete state.
  4. Why can’t trace/meta alone solve this? Because trace/meta are evidence, not the canonical state container that later runtime steps restore from.
  5. Why do this before `OwnerResolver`? Because resolver extraction needs a stable persisted state input instead of recomputing row semantics from scattered branches.
- **Root cause statement:** the architecture now names `InteractionState` as canon, but runtime still lacks a canonical persisted container and normalization path for that state, so the owner-matrix semantics remain per-turn hints instead of stable cross-turn runtime data.
- **Fix mechanism:**
  - extend canonical dialog state to normalize and persist `interaction_state`;
  - extend `session_memory` to hold the same normalized `interaction_state` contract;
  - synchronize `interaction_state` from expected-reply + grounded referents + policy-core target/relation signals using the existing `context_manager` update path;
  - rely on existing pending-resume snapshot plumbing (`context_manager` + `session_memory`) to preserve the new state during pending/handover without creating a new silo.

## Reuse-first plan (mandatory)
- **Internal reuse:**
  - `context_manager.canonical_dialog_state` as the existing versioned runtime state container;
  - `session_memory` normalization / reset / TTL path;
  - pending-resume snapshot plumbing in `state_service.py` and `pending.py`;
  - `P1.6o81` owner matrix artifact as the contract vocabulary for target/relation/owner.
- **External reuse:**
  - official Stately docs for context, guards, and persistence.
- **Why not reinvent the wheel:** the repo already has stable containers for persisted dialog state; the missing work is to materialize `InteractionState` inside them, not to introduce a parallel state subsystem.

## Execution profile (mandatory for non-doc blocks)
- **TP mode:** `runtime_state_contract`
- **Doc touch budget (files):** `8`
- **Code dominance:** `bounded`
- **Override token:** `none`
- **Why this profile fits:** the block adds first-class persisted state and deterministic coverage without yet extracting `OwnerResolver` or launching a guarded rerun.

## Invariant
- `expected_reply_type` remains the resume axis and is not replaced by `interaction_target`.
- No new phrase/regex semantic hardcode in core paths.
- No guarded rerun in this block.
- `M27` remains open truthfully; this block only creates its persisted-state prerequisite.
- Pending/handover resume continues to restore booking/session context through existing snapshot plumbing.

## Scope
- Publish `P1.6o82`.
- Add first-class `interaction_state` contract to canonical dialog state and `session_memory`.
- Synchronize `interaction_state` from existing runtime inputs (`expected_reply_type`, referents, booking confirmation, policy-core target/relation signals).
- Keep pending-resume compatibility by reusing existing `context_manager` / `session_memory` snapshots.
- Update active redesign docs so `P1.6o82` becomes `done` and `P1.6o83` becomes the next admissible block.

## Out of scope
- `OwnerResolver` extraction.
- Machine-readable family test generation.
- Guarded `dev L2 / lock / replay / full` reruns.
- `M27` runtime closure.
- Model-routing split.

## Touch-list
- `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o82-persisted-interaction-state-a1.md`
- `truffles-api/app/schemas/webhook.py`
- `truffles-api/app/routers/webhook/context_manager.py`
- `truffles-api/app/routers/webhook/session_memory.py`
- `truffles-api/app/routers/webhook/decision.py`
- `truffles-api/tests/test_message_endpoint.py`
- `truffles-api/tests/test_state_service.py`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o78-booking-confirm-pricing-interrupt-service-choice-drift-a1.md`
- `STATE.md`
- `STRUCTURE.md`
- `docs/SESSIONS/SESSION-2026-02-19-llm-first-firebreak-a1.md`

## Plan (1..N)
1. Add the explicit `P1.6o82` TP and freeze the bounded state-contract scope.
2. Extend runtime schemas and canonical dialog-state normalization to include `interaction_state`.
3. Synchronize canonical/session-memory `interaction_state` from expected-reply + current referents + policy-core target/relation signals, without extracting a resolver yet.
4. Add deterministic tests for canonical persistence and pending-resume preservation.
5. Sync active program docs/state/session so `P1.6o83` becomes the next admissible block.

## Persisted InteractionState contract (binding)
- Runtime storage locations for this block:
  - `conversation.context.context_manager.canonical_dialog_state.interaction_state`
  - `conversation.context.session_memory.interaction_state`
- Minimum fields:
  - `resume_slot`
  - `interaction_target`
  - `interaction_relation`
  - `interaction_owner`
  - `grounded_referents`
  - `confirmation_state`
  - `degrade_reason`
- Update rules for this block:
  - `resume_slot` is derived from active pending-question contract / `expected_reply_type`.
  - `grounded_referents` are projected from canonical referents and current grounded booking carryover.
  - `interaction_target` / `interaction_relation` come from policy-core outputs when present; otherwise the previous canonical state may persist only while the same active resume slot remains open.
  - `interaction_owner` in this block is a persisted runtime token, not yet the final resolver artifact; the fully authoritative owner-row arbitration lands in `P1.6o83`.
  - `confirmation_state` reflects active booking confirmation payload when present.
  - `degrade_reason` records observed runtime degrade reason when available; absence of this field means no explicit degrade was recorded in the persisted state.
- Snapshot compatibility for this block:
  - no new top-level snapshot silo is introduced;
  - pending-resume preservation is satisfied by existing `context_manager` + `session_memory` snapshot transport.

## DoD
- `P1.6o82` is published.
- `interaction_state` is normalized and persisted in canonical dialog state.
- `interaction_state` is explicitly carried in `session_memory`.
- Existing pending-resume capture/restore preserves `interaction_state` through `context_manager` / `session_memory` snapshots.
- Deterministic tests cover canonical interaction-state sync and pending-resume preservation.
- Active remediation/program docs now point from `P1.6o82` to `P1.6o83`.
- No guarded rerun was launched.

## Checks
- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r112 --status done --strict-artifacts`
- `pytest -q truffles-api/tests/test_message_endpoint.py::test_canonical_dialog_state_syncs_interaction_state_from_policy_contract truffles-api/tests/test_message_endpoint.py::test_reuse_active_handover_captures_interaction_state_in_pending_resume truffles-api/tests/test_state_service.py::TestManagerResolve::test_preserve_context_restores_pending_resume_snapshot`
- `git diff --check`
- `SESSION_AGENT=a1 scripts/session_check.sh`

## Evidence
- `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o82-persisted-interaction-state-a1.md`
- `truffles-api/app/schemas/webhook.py`
- `truffles-api/app/routers/webhook/context_manager.py`
- `truffles-api/app/routers/webhook/session_memory.py`
- `truffles-api/app/routers/webhook/decision.py`
- `truffles-api/tests/test_message_endpoint.py`
- `truffles-api/tests/test_state_service.py`
- `STATE.md`
- `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r112/summary.json`

## Token / run budget (mandatory for expensive suites)
- **Max full runs:** `0`
- **Fail-fast / scenario lock:** reuse audited `r112` evidence only
- **Stop condition:** if `interaction_state` cannot be expressed inside existing canonical state/session-memory containers without introducing a parallel silo or semantic hardcode, stop and escalate to `P1.6o79`/`P1.6o83` instead of widening this block.
- **Escalation path:** `Top Architect + Brain`

## Release safety (mandatory for non-doc changes)
- **Strategy:** bounded runtime-state contract only; no rollout of new business semantics.
- **Go/no-go signals:** deterministic tests green; no session-gate regression; `interaction_state` persists through canonical state and pending-resume tests.
- **Rollback:** revert this block’s code/docs commit; runtime falls back to pre-existing `pending_question_contract`-only behavior.
- **Post-release monitoring window:** `P1.6o83` must consume the persisted state before any new guarded rerun or owner-row closure claim.

## Rollback
- Revert the `interaction_state` code/docs changes if they break session-memory normalization, pending-resume restore, or context-manager canonical-state compatibility.

## No-go
- No new phrase/regex semantic branching to infer owner rows.
- No new top-level context silo for interaction state.
- No guarded rerun.
- No claim that `M27` or closure proof is solved by this block alone.
- No semantic owner arbitration logic that belongs in `P1.6o83`.

## Risks / blockers
- Existing branch-heavy runtime can still overwrite owner semantics locally; this block intentionally persists state but does not yet centralize arbitration.
- `interaction_owner` remains provisional until `OwnerResolver` lands.
- Large pre-existing diff in `decision.py` requires careful merge discipline; this block must stay narrowly scoped.

## Residual architecture debt (mandatory)
- `Current residuals accepted in this block`:
  - `interaction_owner` remains a persisted runtime token, not yet the authoritative resolver output.
  - runtime still has row-specific owner/degrade branches in `decision.py`.
  - `OwnerResolver` does not exist yet.
  - `M27` remains open.
  - model-routing split remains unresolved.
- `Why not in this block`:
  - this block only materializes persisted state; owner arbitration and `M27` migration belong to the next block.
- `Risk if deferred`:
  - if persisted state is not in place before `P1.6o83`, the resolver will still be forced to reconstruct row semantics from ephemeral payloads and branch-local hints.
- `Linked follow-up Task Package(s)`:
  - `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o83-owner-resolver-m27-vertical-slice-a1.md`
  - `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o78-booking-confirm-pricing-interrupt-service-choice-drift-a1.md`
- `Expiry/trigger to stop deferral`:
  - before any new runtime row patch or guarded rerun.

## Next-block contract (mandatory)
- `Next block objective`:
  - publish `P1.6o83` so one `OwnerResolver` consumes the new persisted `interaction_state` plus the machine-readable owner matrix and migrates `M27` through that mechanism as the first vertical slice.
- `First deterministic check command`:
  - `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r112 --status done --strict-artifacts && pytest -q truffles-api/tests/test_message_endpoint.py::test_canonical_dialog_state_syncs_interaction_state_from_policy_contract truffles-api/tests/test_message_endpoint.py::test_reuse_active_handover_captures_interaction_state_in_pending_resume truffles-api/tests/test_state_service.py::TestManagerResolve::test_preserve_context_restores_pending_resume_snapshot && git diff --check`
- `Blocked-by conditions`:
  - `P1.6o82` not published
  - persisted `interaction_state` missing from canonical state or `session_memory`
  - pending-resume does not preserve the new state
- `Owner role for closure`:
  - `Top Architect + Brain` for persisted-state signoff; `Hands` only after `P1.6o83` is published

## Handoff (for zero-context next agent)
- `Ready for next agent`: `yes`
- `Start from`: `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o82-persisted-interaction-state-a1.md`
- `Do not touch`: guarded reruns, closure claims, or new row patches before `P1.6o83`

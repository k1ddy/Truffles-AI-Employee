# TP-2026-03-07-p1.6l-llm-policy-core-message-endpoint-family-a1

- Название/цель: закрыть красную `LLM policy core` failure family в `truffles-api/tests/test_message_endpoint.py`, не ломая уже очищенный `demo_salon` release lane и не включая дорогие `dev L2 / acceptance` прогоны до полного зелёного `L1`.
- Parent TP: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6k-demo-salon-release-lane-cleanup-a1.md`
- Canon refs: `AGENTS.md`, `STATE.md`, `STRATEGY/REQUIREMENTS.md`, `TECH.md`, `docs/runbooks/BOOKING_CONFIRM_VERIFY.md`, `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`

## One web search (mandatory before implementation)

- Query: `W3C SCXML transition guard condition priority document order first enabled transition`
- Time (UTC): `2026-03-07T03:26:00Z`
- Sources:
  - `https://www.w3.org/TR/scxml/`
- Ready solutions found:
  - state-machine transitions must have explicit guard precedence
  - once a guarded transition is selected, lower-priority alternate transitions must not silently replace it
- Decision (`reuse/integrate/build`): `integrate + build`
  - integrate guard-precedence discipline into the policy-core/expected-reply fallback path
  - build a local fix because the bug is in current runtime/test orchestration, not a reusable external component
- Rejected options:
  - masking the family by loosening tests or removing assertions
  - adding more phrase/lexicon shortcuts in runtime
  - running `dev L2` before full `message_endpoint` is green

## Root cause (mandatory)

- Symptom:
  - after the `demo_salon` cleanup block, full `pytest -q truffles-api/tests/test_message_endpoint.py` is red: `11 failed, 287 passed`
  - failures cluster around `LLM policy core` collect/tool/service-query/master behavior
- Minimal reproduction:
  1. `pytest -q truffles-api/tests/test_message_endpoint.py`
  2. inspect failing tests:
     - `test_llm_policy_core_collect_expected_reply_slot_still_respects_slot_order`
     - `test_llm_policy_core_low_confidence_book_slot_with_complete_slots_is_allowed`
     - `test_llm_policy_core_book_slot_missing_phone_sets_expected_reply_phone`
     - `test_llm_policy_core_book_slot_uses_booking_phone_from_context`
     - `test_llm_policy_core_list_slots_missing_service_degrades_before_service_hint`
     - `test_llm_policy_core_reschedule_uses_booking_context_appointment_id`
     - `test_llm_policy_core_catalog_services_overview_sets_followup_without_info_sections`
     - `test_llm_policy_core_specialist_availability_question_enforces_master_reply`
     - `test_llm_policy_core_get_booking_ok_does_not_force_handoff`
     - `test_llm_policy_core_service_query_without_master_override_reason_keeps_plan_reply`
     - `test_llm_policy_core_semantic_arbitration_off_still_enforces_master_guard`
  3. inspect captured logs for unexpected auxiliary `answer_interpreter_llm` / `controller_llm_ms` calls under `OPENAI_API_KEY=test-key`
- Evidence:
  - local run `pytest -q truffles-api/tests/test_message_endpoint.py` on `2026-03-07` => `11 failed, 287 passed`
  - failing logs show unexpected fallback/controller LLM paths with invalid `test-key`, plus contract drift in expected-reply/tool-action/master arbitration behavior
- Five Whys:
  1. Why is `message_endpoint` still red: a shared `LLM policy core` family is not aligned with current runtime fallback behavior.
  2. Why is that blocking `demo_salon`: `message_endpoint` is part of the mandatory local-first deterministic contour before any fresh `dev L2`.
  3. Why can this not be ignored: otherwise the next expensive run would mix a known `L1` regression family with runtime-quality findings.
  4. Why is the exact root cause not yet closed: the family may be either stale test harness or a real guard-precedence/runtime regression.
  5. Why is the block status still open: the family must first be isolated into one concrete root-cause statement and one minimal write scope.
- Root cause statement:
  - the red family was mixed, not singular:
    - real runtime bug: successful `calendar.*` tool outcomes were still eligible for `fact_guard`, which could override a valid tool reply with `missing_fact_payload`
    - real runtime gap: master-intent resolver accepted `direct` and `person+action` master questions, but missed explicit `person + service relation` asks such as `мастер/специалист по <service>`
    - remaining reds were stale test expectations after already-landed invariants (`collect_slot_order_invalid`, `services_overview` followup contract, master-guard ownership)
- Fix mechanism:
  - skip `fact_guard` for `calendar.*` tool actions
  - extend master-intent resolver with deterministic `person_service_signal` detection
  - realign stale tests to the post-`P1.6i` slot-order and post-cleanup master-guard contract

## Invariant

1. `demo_salon` remains the only heavy acceptance lane.
2. No new expensive `dev L2 / lock / replay / canary / full` run starts while full `message_endpoint` is red.
3. No new lexical/prompt patch-fitting is allowed as a fix mechanism.
4. Existing cleanup result for parking carryover remains intact.

## Scope

- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6k-demo-salon-release-lane-cleanup-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6l-llm-policy-core-message-endpoint-family-a1.md`
- `truffles-api/tests/test_message_endpoint.py`
- minimal runtime files proven by RCA

## Out of scope

- fresh `dev L2 / lock / replay / canary / full`
- new multi-pack or `P6` work
- broad refactors outside the minimal write scope proven by RCA

## Plan (1..N)

1. Record this family as the next atomic block in TP/docs.
2. Reproduce and cluster the 11 failing tests into one shared path.
3. Prove whether the family is runtime regression or stale test harness.
4. Apply one minimal contract-safe fix.
5. Rerun the focused failing cluster, then full `pytest -q truffles-api/tests/test_message_endpoint.py`.
6. Only after green `message_endpoint` restate readiness for one fresh `demo_salon` `dev L2`.

## DoD

1. Root cause is proven, not guessed.
2. The red family is closed with one minimal write scope.
3. Full `pytest -q truffles-api/tests/test_message_endpoint.py` is green.
4. No expensive `demo_salon` run is started in this block.

## Checks

- `pytest -q truffles-api/tests/test_message_endpoint.py -k "llm_policy_core_collect_expected_reply_slot_still_respects_slot_order or llm_policy_core_low_confidence_book_slot_with_complete_slots_is_allowed or llm_policy_core_book_slot_missing_phone_sets_expected_reply_phone or llm_policy_core_book_slot_uses_booking_phone_from_context"`
- `pytest -q truffles-api/tests/test_pack_runtime_service.py -k "resolve_master_intent_person_service_query_is_explicit or resolve_master_intent_person_term_without_relation_stays_non_explicit"`
- `pytest -q truffles-api/tests/test_message_endpoint.py -k "llm_policy_core_specialist_availability_question_enforces_master_reply or llm_policy_core_service_query_without_master_override_reason_keeps_plan_reply or llm_policy_core_semantic_arbitration_off_still_enforces_master_guard or llm_policy_core_collect_expected_reply_slot_still_respects_slot_order or llm_policy_core_list_slots_missing_service_degrades_before_service_hint or llm_policy_core_catalog_services_overview_sets_followup_without_info_sections"`
- `pytest -q truffles-api/tests/test_message_endpoint.py`
- `pytest -q truffles-api/tests/test_pack_runtime_service.py`
- `pytest -q truffles-api/tests/test_demo_salon_eval.py`
- `ruff check truffles-api/app/services/pack_runtime_service.py truffles-api/tests/test_pack_runtime_service.py truffles-api/tests/test_message_endpoint.py`
- `git diff --check`

## Execution status

- `done`

## Evidence

1. initial full local `message_endpoint` run output on `2026-03-07` -> `11 failed, 287 passed`
2. focused closure subset:
   - `pytest -q truffles-api/tests/test_pack_runtime_service.py -k "resolve_master_intent_person_service_query_is_explicit or resolve_master_intent_person_term_without_relation_stays_non_explicit"` -> `2 passed`
   - `pytest -q truffles-api/tests/test_message_endpoint.py -k "llm_policy_core_specialist_availability_question_enforces_master_reply or llm_policy_core_service_query_without_master_override_reason_keeps_plan_reply or llm_policy_core_semantic_arbitration_off_still_enforces_master_guard or llm_policy_core_collect_expected_reply_slot_still_respects_slot_order or llm_policy_core_list_slots_missing_service_degrades_before_service_hint or llm_policy_core_catalog_services_overview_sets_followup_without_info_sections"` -> `6 passed`
3. final green deterministic suite output:
   - `pytest -q truffles-api/tests/test_pack_runtime_service.py` -> `19 passed`
   - `pytest -q truffles-api/tests/test_message_endpoint.py` -> `299 passed, 2 warnings`
   - `pytest -q truffles-api/tests/test_demo_salon_eval.py` -> `17 passed`
   - `ruff check truffles-api/app/services/pack_runtime_service.py truffles-api/tests/test_pack_runtime_service.py truffles-api/tests/test_message_endpoint.py` -> `All checks passed!`
   - `git diff --check` -> clean

## Rollback

1. revert only the minimal runtime/test patch for this family
2. keep `dev L2` blocked until full `message_endpoint` is green again

## No-go

- no acceptance or `dev L2` run before closing this family
- no hardcoded bypasses in runtime just to satisfy unit tests
- no blanket deletion of failing tests without proven test drift

## Risks/Blockers

1. the 11 failures may split into more than one family after RCA
2. auxiliary LLM/controller invocations under `test-key` may hide the primary runtime contract defect

## Residual architecture debt (mandatory)

- Current residuals accepted in this block:
  - `P1.6` still has no fresh `dev L2`
  - `P1.7` still not created
  - `P6` remains deferred
- Why not in this block:
  - this block was only for the red `message_endpoint` prerequisite
- Risk if deferred:
  - the next expensive run stays blocked until fresh `demo_salon` `dev L2` evidence exists
- Linked follow-up Task Package(s):
  - `docs/TASK_PACKAGES/TP-2026-03-07-p1.6k-demo-salon-release-lane-cleanup-a1.md`
  - `docs/TASK_PACKAGES/TP-2026-03-06-p1.6-local-realism-canonical-lock-a1.md`
- Expiry/trigger to stop deferral:
  - before any new expensive `demo_salon` run

## Next-block contract (mandatory)

- Next block objective:
  - run one fresh guarded `demo_salon` `dev L2` on the cleaned release lane, then decide whether `P1.6` can move from `budget-blocked` to `lock-ready`
- First deterministic check command:
  - `pytest -q truffles-api/tests/test_message_endpoint.py && pytest -q truffles-api/tests/test_demo_salon_eval.py`
- Blocked-by conditions:
  - `OPENAI_API_KEY` or judge access missing for guarded `dev L2`
  - runtime `http://127.0.0.1:18184` is not the refreshed current worktree runtime
  - no fresh `PG checklist` / run-id prepared for the next guarded `dev L2`
- Owner role for closure:
  - `Top Architect + Brain`

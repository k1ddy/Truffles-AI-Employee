# TP-2026-03-11-p1.6o66-policy-core-named-specialist-followup-owner-a1

- Название/цель: закрыть surfaced `r91` blocker family, где после bounded `M14` closure свежий guarded `dev L2` больше не падает на `invalid_schema -> truth_gate master`, но turn `Я хочу записаться к Айгерим.` под активным `expected_reply_type=time` проходит через успешный `llm_policy_core collect`, при этом semantic output теряет `pending_question_target` и `active_question_relation`, а runtime отвечает generic `booking_prompt` без `booking_specialist_followup` owner evidence.

## Canon refs

- Parent verdict: `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o60a-remaining-closure-architecture-verdict-a1.md`
- Owner matrix: `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o60b-remaining-closure-owner-matrix-a1.md`
- Program TP: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Proof bundle: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`
- Prior child: `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o65-invalid-schema-specialist-followup-truth-gate-a1.md`
- STATE: `STATE.md`
- CA: remaining closure row after `M14`

## One web search (mandatory before implementation)

- Query: `Rasa forms requested slot slot filling official docs`
- Date/time: `2026-03-11T13:44:36+05:00`
- Sources opened:
  - official Rasa docs: `https://rasa.com/docs/rasa/forms/`
- Source quality:
  - official product documentation
- Found reusable ideas:
  - active slot ownership must remain explicit while side information is handled;
  - requested-slot continuity is a contract boundary, not a byproduct of response text.
- Decision:
  - `reuse/integrate`
  - keep using existing `booking_specialist_followup` owner and explicit pending-question trace/meta axes; extend the success-path semantic contract instead of inventing a new runtime-only fallback.
- Rejected options:
  - broad booking interrupt heuristic patch, because it would reopen semantic hardcode in runtime core;
  - oracle weakening to accept generic `booking_prompt`, because the missing owner evidence is the actual defect.

## Root cause (mandatory)

- Symptom:
  - fresh guarded `dev L2` run `p1.6o6-l2-dev-20260311-a1-r91` stops at dialog `1`, turn `5`, user text `Я хочу записаться к Айгерим.`
- Minimal reproduction:
  1. Start active booking flow with service already known and `expected_reply_type=time`.
  2. Allow an info interrupt that preserves resume `time`.
  3. Send explicit named specialist booking statement `Я хочу записаться к Айгерим.`.
  4. Observe `llm_policy_core` success output: `subject_kind=specialist`, `capability=bookability`, `next_question=datetime`, but `pending_question_target=null` and `active_question_relation=null`.
  5. Runtime answers with generic `booking_prompt` and loses `booking_specialist_followup` trace/meta.
- Evidence:
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r91/summary.json`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r91/brief.md`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r91/scenarios.json`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r91/responses.jsonl`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r91/trace_bundle.jsonl`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r91/manual_audit.md`
- Five Whys:
  1. Why did the turn fail? The response preserved `reply_type=time` but missed `pending_question_target=specialist` and the expected specialist-followup trace row.
  2. Why was owner evidence missing? Successful `llm_policy_core` output returned a generic collect contract with blank `pending_question_target` and blank `active_question_relation`.
  3. Why did runtime accept that? Success-path arbitration currently trusts `next_question=datetime` / slot completeness more than explicit referent-followup ownership for declarative specialist selection.
  4. Why is this not covered by `M4` or `M14`? `M4` closed question-like named specialist follow-up, and `M14` closed degraded `invalid_schema` fallback; `r91` is a policy-core-success row with a statement-like specialist booking turn.
  5. Why is it a new bounded family? The semantic contract for active-time specialist follow-up is still incomplete on the success path when the user states the referent instead of asking about it.
- Root cause statement:
  - `llm_policy_core` success-path schema/prompt contract under-specifies declarative named specialist follow-up under active `time` collect, so semantic owner correctly picks booking collect but fails to emit the referent-followup ownership axes required by runtime and oracle.
- Fix mechanism:
  - freeze a new matrix row `M15`;
  - extend prompt/schema/runtime owner preservation so explicit named specialist booking statements under active `time` collect emit `pending_question_target=specialist` and `active_question_relation=referent_followup` on the success path;
  - keep final resume `expected_reply_type=time` and preserve `booking_specialist_followup` trace/meta.

## Reuse-first decision (mandatory)

- Reuse:
  - existing `booking_specialist_followup` owner
  - existing `referent_followup` relation
  - existing specialist-followup formatting / evidence path
- Integrate:
  - success-path semantic contract from `llm_policy_core`
  - runtime preservation in `decision.py`
- Do not build:
  - new phrase router
  - new generic interrupt owner

## Execution profile

- TP mode: `implementation`
- Doc touch budget (files): `9`
- Code dominance: `required`
- Override token: `none`
- Why this profile fits: surfaced family is a bounded semantic-success owner gap with a frozen active row and clear adjacent regressions.

## Release safety

- Strategy: bounded semantic-contract + owner-preservation change only
- Go signals:
  - explicit named specialist booking statements under active `time` collect preserve `pending_question_target=specialist`
  - final trace includes `pending_question_interaction decision=booking_specialist_followup`
  - adjacent rows `M4` and `M14` remain green on deterministic contour
- No-go signals:
  - treating generic `booking_prompt` without specialist-followup evidence as success
  - broadening the fix to unrelated generic booking collect turns
  - losing the explicit `llm_policy_core` success provenance in `decision_trace` / `decision_meta`
- Rollback verification:
  - revert only the bounded `M15` semantic/runtime/test delta if adjacent specialist-followup rows regress

## Invariant

1. Named specialist booking follow-up under active `time` collect must preserve specialist ownership on both success and degrade paths.
2. `P1.6o51/M4` and `P1.6o65/M14` must not regress.
3. Success-path semantic owner output must remain observable in `decision_meta` / `decision_trace`.
4. Exactly one fresh guarded `dev L2` runs after deterministic green.

## Scope

- publish the surfaced policy-core-success specialist-followup family as matrix row `M15`
- then implement the bounded success-path semantic owner repair for that row only

## Out of scope

- global prompt rewrite or model-routing alignment
- acceptance `lock/replay`
- unrelated booking interrupt / truth-gate families already closed by `M14`

## Touch-list

- `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o66-policy-core-named-specialist-followup-owner-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o60b-remaining-closure-owner-matrix-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o60a-remaining-closure-architecture-verdict-a1.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`
- `prompts/llm_policy_core.md`
- `truffles-api/app/routers/webhook/decision.py`
- `truffles-api/tests/test_message_endpoint.py`
- `truffles-api/tests/test_booking_quality_response_guard.py`

## Plan (1..N)

1. Freeze `r91` as audited non-closure evidence and publish `M15`.
2. Extend policy-core success-path contract for declarative named specialist follow-up under active `time` collect.
3. Preserve specialist-followup trace/meta on the runtime path without reopening runtime hardcode.
4. Add targeted runtime + evidence regressions with adjacent negative cases.
5. Run focused checks and `git diff --check`.
6. Run one fresh guarded `dev L2` only after deterministic closure is green.

## DoD

1. `r91` is recorded in active docs as audited non-closure evidence.
2. Named specialist booking statement under active `time` collect no longer loses `pending_question_target=specialist` and `active_question_relation=referent_followup` on the success path.
3. Final meta/trace preserve `pending_question_target=specialist`, `pending_question_interaction=booking_specialist_followup`, `expected_reply_type=time`, and explicit `llm_policy_core` success provenance.
4. Focused deterministic runtime/evidence regressions are green.
5. Fresh guarded `dev L2` no longer stops on the `r91` family.

## Expensive run control (mandatory)

- Hypothesis:
  - after the bounded `M15` semantic owner repair, fresh guarded `dev L2` will no longer stop on dialog `1`, turn `5`, `Я хочу записаться к Айгерим.`
- Expected measurable effect:
  - the next guarded run preserves specialist-followup owner evidence for that turn and either proceeds beyond it or surfaces a different first blocker family
- Stop condition:
  - run exactly one fresh guarded `dev L2` plus strict audit; if the same success-path owner gap persists or the run becomes preflight-invalid, stop the expensive loop and reopen RCA instead of chaining another rerun

## Checks

- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r91 --status done --strict-artifacts`
- `python3 - <<'PY'`
  `import json, pathlib`
  `run = pathlib.Path('/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r91')`
  `rows = [json.loads(line) for line in (run / 'responses.jsonl').read_text().splitlines()]`
  `row = rows[4]`
  `print(row['turn_text'])`
  `print(row['decision_meta']['source'], row['decision_meta']['intent'], row['decision_meta']['expected_reply_type'])`
  `print(row['decision_meta'].get('pending_question_target'), row['decision_meta'].get('pending_question_interaction'))`
  `print(row['decision_trace'][6]['stage'], row['decision_trace'][6]['pending_question_target'])`
  `PY`
- `pytest -q truffles-api/tests/test_message_endpoint.py -k "specialist_followup or referent_followup or invalid_schema or booking_prompt"`
- `pytest -q truffles-api/tests/test_booking_quality_response_guard.py -k "specialist_followup or expected_meta_mismatch or expected_trace_miss"`

## Execution status

- `done`
- bounded `M15` closure is green on deterministic contour
- fresh guarded `dev L2` `p1.6o6-l2-dev-20260311-a1-r92` no longer stopped on the `r91/M15` family and rolled to a different scenario-governance blocker

## Evidence

1. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r91/summary.json`
2. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r91/brief.md`
3. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r91/scenarios.json`
4. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r91/responses.jsonl`
5. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r91/trace_bundle.jsonl`
6. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r91/manual_audit.md`
7. `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o65-invalid-schema-specialist-followup-truth-gate-a1.md`
8. `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o66-policy-core-named-specialist-followup-owner-a1.md`

## Rollback

1. Keep `P1.6o6` blocked.
2. Revert only the future bounded `M15` semantic/runtime delta if it regresses adjacent specialist-followup rows.

## No-go

- no phrase fitting
- no oracle weakening
- no reinterpretation of `r91` as proof that `M14` is still open
- no generic `booking_prompt` accepted as closure for `M15` without specialist-followup evidence

## Risks/Blockers

1. The fix may require prompt/schema tightening in addition to runtime preservation because the surfaced defect is on the policy-core success path.
2. Adjacent statement-like vs question-like specialist follow-up rows can regress if the row boundary is made too broad.
3. `r91` remains non-canonical proof evidence (`infra_valid=false`, `semantic_valid=false`), so this block can only claim bounded closure plus rerun rollover.

## Residual architecture debt (mandatory)

- Current residuals accepted in this block:
  - `M16` surfaced after bounded `M15` closure.
- Why not in this block:
  - `r92` surfaced only after the next fresh guarded `dev L2` beyond `M15`.
- Risk if deferred:
  - generated scenario governance will keep overclaiming exact-time closure and produce stale downstream `name` expectations even when runtime behavior is correct.
- Linked follow-up Task Package(s):
  - `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o67-ambiguous-time-fill-scenario-governance-a1.md`
- Expiry/trigger to stop deferral:
  - before the next guarded rerun beyond `r92`.

## Next-block contract (mandatory)

- Next block objective:
  - implement `M16`, then rerun deterministic scenario checks before any new guarded `dev L2`.
- First deterministic check command:
  - `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r92 --status done --strict-artifacts && git diff --check`
- Blocked-by conditions:
  - `M16` is not published in the owner matrix
  - ambiguous-time scenario-governance regressions are not green
  - acceptance tool-evidence infra remains red
- Owner role for closure:
  - `Hands` for bounded implementation, `Top Architect + Brain` for matrix/program/proof sync

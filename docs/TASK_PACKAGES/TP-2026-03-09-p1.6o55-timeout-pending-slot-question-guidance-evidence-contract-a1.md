# TP-2026-03-09-p1.6o55-timeout-pending-slot-question-guidance-evidence-contract-a1.md

- Название/цель: закрыть surfaced `r65` blocker family, где bounded timeout recovery for `ask_about_requested_slot(time)` уже сохраняет resume contract, но всё ещё маркируется как `policy_core_guard` FACT-like reply без valid evidence linkage, поэтому guard ловит `fact_without_evidence` вместо принятия этого хода как collect/guidance owner.
- Parent TP: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Canon refs: `AGENTS.md`, `STATE.md`, `STRUCTURE.md`, `STRATEGY/REQUIREMENTS.md`, `docs/runbooks/BOOKING_CONFIRM_VERIFY.md`, `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o54-timeout-master-info-interrupt-name-collect-owner-a1.md`
- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect remove worktree + branch after merge`

## One web search (mandatory before implementation)

- Query: `Rasa forms side questions slot filling responses collect vs inform official docs`
- Time (UTC): `2026-03-09T11:01:00Z`
- Sources:
  - `https://rasa.com/docs/rasa/forms/`
- Ready solutions found:
  - when the active slot remains unresolved, side-question handling should answer in a way that preserves and resumes slot filling rather than reclassifying the turn as a different product contract;
  - failure/degrade handling should reuse the existing slot owner contract instead of inventing a parallel semantic owner.
- Decision (`reuse/integrate/build`): `reuse + integrate`
  - reuse existing `booking_slot_guidance` collect owner;
  - integrate timeout pending-slot-question recovery into that owner contract instead of keeping a timeout-specific pseudo-owner in `decision_meta`;
  - build no new oracle waiver and no new fact-evidence exception.
- Rejected options:
  - weakening `fact_without_evidence` for `policy_core_guard` replies;
  - treating timeout guidance as a FACT with synthetic evidence refs;
  - adding phrase routing for `Когда у вас есть свободные слоты?`.

## Root cause (mandatory)

- Symptom:
  - guarded `dev L2` run `p1.6o6-l2-dev-20260309-a1-r65` stops on dialog 1 turn 2 with `fact_without_evidence` even though runtime already keeps `pending_question_interaction=ask_about_requested_slot`, `expected_reply_type=time`, and a collect-style guidance reply.

- Minimal reproduction:
  1. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r65/responses.jsonl` dialog 1 turn 2 and confirm response text is collect-style guidance (`На какую дату и время вам удобно?`) with `pending_question_interaction=ask_about_requested_slot`;
  2. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r65/trace_bundle.jsonl` dialog 1 turn 2 and confirm timeout recovery records `policy_core_guard -> timeout_pending_slot_question`, then `pending_question_interaction`, but final `decision_meta.source=policy_core_guard` and `pending_question_owner=policy_core_timeout_pending_question`;
  3. inspect `ops/diagnose.py` and confirm `action=reply` + `source=policy_core_guard` is treated as fact-like unless explicit evidence refs exist;
  4. inspect `truffles-api/app/routers/webhook/decision.py` and confirm timeout pending-slot-question recovery writes a timeout-specific pseudo-owner instead of reusing the existing `booking_slot_guidance` owner contract.

- Evidence:
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r65/summary.json`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r65/brief.md`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r65/responses.jsonl`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r65/trace_bundle.jsonl`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r65/manual_audit.md`
  - `truffles-api/app/routers/webhook/decision.py`
  - `ops/diagnose.py`

- Five Whys:
  1. Why did the turn fail: guard classified the reply as FACT-like without evidence.
  2. Why did guard classify it as FACT-like: `decision_meta.action=reply` and `decision_meta.source=policy_core_guard` match the fact-like heuristic.
  3. Why was source still `policy_core_guard`: timeout recovery created its own pseudo-owner (`policy_core_timeout_pending_question`) instead of reusing the existing `booking_slot_guidance` owner.
  4. Why is that a contract gap: product behavior is still collect/guidance, but meta/trace identity says “generic guard fallback,” so acceptance sees a FACT-like reply instead of a bounded collect owner.
  5. Why does this matter architecturally: degrade-path observability exists, but owner identity is still split between “real owner” and “timeout pseudo-owner,” which reintroduces semantic compression post-recovery.

- Root cause statement:
  - timeout pending-slot-question recovery preserves the right booking behavior but writes the wrong owner identity in `decision_meta/trace`, so the guard evaluates a collect/guidance turn as `policy_core_guard` FACT-like output and flags `fact_without_evidence`.

- Fix mechanism:
  - align timeout pending-slot-question recovery with the existing `booking_slot_guidance` owner contract in `decision_meta/trace/question_contract`;
  - keep timeout provenance only in `policy_core_guard_recovery` / `policy_core_timeout_retry_path`;
  - add targeted runtime + response-guard regressions;
  - rerun guarded dev beyond `r65`.

## Release safety

- Strategy: bounded owner-identity realignment only
- Go signals:
  - timeout pending-slot-question replies remain collect/guidance and stop tripping `fact_without_evidence`
  - timeout provenance remains visible in `policy_core_guard_recovery`
- No-go signals:
  - guard is weakened instead of runtime contract being realigned
  - timeout path starts pretending to have pack/tool evidence
- Rollback verification:
  - revert only the timeout guidance identity delta and targeted regressions if unrelated degraded collects start matching `booking_slot_guidance`

## Invariant

1. Timeout recovery for pending slot questions remains a collect-style guidance turn, not a FACT claim.
2. Existing non-timeout `booking_slot_guidance` behavior stays unchanged.
3. Timeout provenance remains observable in meta/trace.

## Scope

- `truffles-api/app/routers/webhook/decision.py`
- targeted runtime/guard regressions
- tracker docs after rerun handoff

## Out of scope

- generic `policy_core_guard` fact-like heuristic redesign
- pipeline budget tuning
- acceptance/full proof closure

## Touch-list

- `truffles-api/app/routers/webhook/decision.py`
- `truffles-api/tests/test_message_endpoint.py`
- `truffles-api/tests/test_booking_quality_response_guard.py`
- `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o55-timeout-pending-slot-question-guidance-evidence-contract-a1.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`

## Plan (1..N)

1. Freeze `r65` as bounded owner-identity/evidence-contract failure.
2. Realign timeout pending-slot-question recovery to `booking_slot_guidance` owner semantics.
3. Add targeted runtime and guard regressions.
4. Run focused checks and `git diff --check`.
5. Run one fresh guarded dev rerun beyond `r65`.

## DoD

1. Timeout pending-slot-question recovery no longer surfaces `fact_without_evidence`.
2. `decision_meta.source/pending_question_owner/expected_reply_reason` reflect `booking_slot_guidance` while timeout provenance stays explicit.
3. Focused regressions pass.
4. One fresh guarded rerun no longer stops on the `r65` family.

## Checks

- `pytest -q truffles-api/tests/test_message_endpoint.py -k "timeout_pending_slot_question or timeout_master_info or specialist_followup"`
- `pytest -q truffles-api/tests/test_booking_quality_response_guard.py -k "fact_without_evidence or pending_question_interaction or booking_slot_stall"`
- `git diff --check`

## Execution status

- `done`

## Evidence

1. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r65/summary.json`
2. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r65/brief.md`
3. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r65/responses.jsonl`
4. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r65/trace_bundle.jsonl`
5. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r65/manual_audit.md`

## Rollback

1. Revert only the timeout pending-slot-question guidance identity delta and targeted regressions.
2. Keep `P1.6o6` blocked; do not reinterpret `r65` as closure evidence.

## No-go

- no guard waiver for `fact_without_evidence`
- no synthetic evidence refs for a collect reply
- no phrase hardcode in timeout path

## Risks/Blockers

1. `r65` is still infra-invalid because of unrelated tool-evidence and pipeline budget issues, so it is architectural evidence only.
2. The next surfaced family may be another timeout/degrade owner gap after this one closes.

## Residual architecture debt (mandatory)

- Current residuals accepted in this block:
  - timeout/degrade owner matrix is still incomplete outside this pending-slot-question guidance family
  - guarded dev remains non-canonical for proof while unrelated tool-evidence infra stays red
- Why not in this block:
  - this slice only realigns owner identity for one surfaced timeout recovery
- Risk if deferred:
  - collect/guidance turns keep being audited as FACT-like failures, hiding the next real blocker family
- Linked follow-up Task Package(s):
  - `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`
- Expiry/trigger to stop deferral:
  - next guarded rerun after bounded closure

## Next-block contract (mandatory)

- Next block objective:
  - execute `P1.6o56` and run guarded `r67` so the next surfaced blocker is no longer the timeout pending-slot-question evidence family from `r65`
- First deterministic check command:
  - `pytest -q truffles-api/tests/test_message_endpoint.py -k "timeout_pending_slot_question or timeout_master_info or specialist_followup" && pytest -q truffles-api/tests/test_booking_quality_response_guard.py -k "fact_without_evidence or pending_question_interaction or booking_slot_stall" && git diff --check`
- Blocked-by conditions:
  - timeout pending-slot-question regressions are not green
  - `r65` audit not reflected in tracker docs
  - fresh rerun preflight invalid
- Owner role for closure:
  - `Hands` for bounded runtime closure + rerun evidence, `Top Architect + Brain` for proof-bundle promotion

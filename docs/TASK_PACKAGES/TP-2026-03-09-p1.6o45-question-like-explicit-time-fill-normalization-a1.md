# TP-2026-03-09-p1.6o45-question-like-explicit-time-fill-normalization-a1.md

- Название/цель: закрыть surfaced `r49` expectation blocker, где question-like explicit time candidate (`Может быть, на 14:00?`) under active `time` collect остаётся в taxonomy как `ask_about_requested_slot`, хотя runtime корректно трактует его как bounded slot fill.
- Parent TP: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Canon refs: `AGENTS.md`, `STATE.md`, `STRUCTURE.md`, `STRATEGY/REQUIREMENTS.md`, `docs/runbooks/BOOKING_CONFIRM_VERIFY.md`, `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o44-scenario-contract-info-interrupt-pending-question-sync-a1.md`, `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o34-grounded-time-availability-probe-normalization-a1.md`
- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect remove worktree + branch after merge`

## One web search (mandatory before implementation)

- Query: `Rasa forms slot filling user proposes value with question mark official docs`
- Time (UTC): `2026-03-09T05:43:00Z`
- Sources:
  - `https://rasa.com/docs/rasa/forms/`
- Ready solutions found:
  - when the user proposes a concrete slot value, the form/requested-slot should treat it as slot filling even if the utterance is phrased as a tentative question;
  - follow-up governance should key off the concrete slot value, not punctuation or question phrasing alone.
- Decision (`reuse/integrate/build`): `reuse + integrate`
  - reuse the existing explicit-time-fill normalization already used for grounded availability probes and slot-constraint answers;
  - integrate the same contract for question-like exact-time candidates under active `time` collect;
  - build no runtime phrase router.
- Rejected options:
  - changing runtime to emit fake pending-question evidence for a real slot fill;
  - waiving `expected_meta_mismatch` / `expected_trace_miss` for this family;
  - adding surface-specific hardcode only for `может быть`.

## Root cause (mandatory)

- Symptom:
  - guarded `dev L2` run `p1.6o6-l2-dev-20260309-a1-r49` fails on dialog 1 turn 4 with `expected_meta_mismatch` and `expected_trace_miss`.

- Minimal reproduction:
  1. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r49/scenarios.json` dialog 1 turn 4
  2. observe text `Может быть, на 14:00?` tagged as `ask_about_requested_slot`
  3. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r49/responses.jsonl` and `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r49/trace_bundle.jsonl`
  4. confirm runtime matches `question_contract` on `datetime=14:00`, sets `expected_reply_type=name`, and does not emit `pending_question_interaction`
  5. compare with `scripts/booking_dialog_scenarios.py` and note that `ask_about_requested_slot -> time` normalization only fires for explicit-time availability probes, not for tentative exact-time candidates without availability lexicon

- Evidence:
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r49/scenarios.json`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r49/responses.jsonl`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r49/trace_bundle.jsonl`
  - `scripts/booking_dialog_scenarios.py`

- Five Whys:
  1. Why did the turn fail: scenario expected pending-question evidence, runtime produced slot-fill evidence.
  2. Why did scenario expect pending-question evidence: the turn kept tag `ask_about_requested_slot`.
  3. Why did the tag stay there: current normalization only rewrites explicit-time ask turns when availability/probe lexicon is present.
  4. Why is that too narrow: an explicit exact-time candidate under active `time` collect is a slot fill regardless of tentative punctuation or `может быть` phrasing.
  5. Why is this not a runtime bug: runtime already matched `datetime` and advanced collect to `name`; only taxonomy/oracle lagged behind.

- Root cause statement:
  - scenario normalization under-specifies explicit-time slot fills: it rewrites grounded availability probes but not tentative question-like exact-time candidates, so the same semantic slot fill is mislabeled as `ask_about_requested_slot` and rejected by contract expectations.

- Fix mechanism:
  - broaden `ask_about_requested_slot -> time` normalization for active `time` collect to all explicit exact-time fills, not only availability probes;
  - add deterministic regression for question-like exact-time fill surfaces;
  - rerun guarded acceptance only after deterministic normalization is green.

## Release safety

- Strategy: deterministic taxonomy/oracle normalization only
- Go signals:
  - question-like exact-time candidate under active `time` collect normalizes to canonical `time -> name`
  - ungrounded slot questions remain `ask_about_requested_slot`
- No-go signals:
  - runtime changes for this block
  - weakening pending-question expectations globally
- Rollback verification:
  - revert only the broadened ask->time normalization and its regression if it over-classifies ungrounded slot questions

## Invariant

1. `P1.6o44` scenario-contract sync remains closed.
2. Genuine ungrounded `ask_about_requested_slot` turns stay pending-question turns.
3. Runtime owner behavior from `P1.6o43` remains unchanged.
4. No runtime phrase-hardcode is added.

## Scope

- `scripts/booking_dialog_scenarios.py`
- `truffles-api/tests/test_booking_dialog_scenarios_script.py`
- `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o45-question-like-explicit-time-fill-normalization-a1.md`
- tracker docs after deterministic closure and next rerun handoff:
  - `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
  - `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
  - `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`

## Out of scope

- runtime booking-owner changes
- judge rubric changes
- lock/replay promotion

## Touch-list

- `scripts/booking_dialog_scenarios.py`
- `truffles-api/tests/test_booking_dialog_scenarios_script.py`
- `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o45-question-like-explicit-time-fill-normalization-a1.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`

## Plan (1..N)

1. Freeze `r49` as taxonomy/oracle drift evidence.
2. Broaden explicit-time normalization for `ask_about_requested_slot` under active `time` collect.
3. Add deterministic regression for question-like exact-time candidate surfaces.
4. Run targeted checks and `git diff --check`.
5. Then hand off the next guarded rerun.

## DoD

1. `Может быть, на 14:00?`-style turns normalize to canonical `time` slot fill under active `time` collect.
2. Ungrounded slot questions remain pending-question turns.
3. Deterministic scenario tests pass.
4. `git diff --check` stays clean.

## Checks

- `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "grounded_time or pending_question"`
- `git diff --check`

## Execution status

- `in_progress`

## Evidence

1. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r49/scenarios.json`
2. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r49/responses.jsonl`
3. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r49/trace_bundle.jsonl`
4. `scripts/booking_dialog_scenarios.py`

## Rollback

1. Revert only the broadened exact-time normalization and matching regression.
2. Keep `P1.6o6` blocked; do not reinterpret `r49` as closure evidence.

## No-go

- no runtime edits in this block
- no judge/oracle waiver for slot-fill evidence
- no phrase-specific special-case for a single wording
- no guarded rerun before deterministic normalization is green

## Risks/Blockers

1. Another question-like exact-time surface may reveal a different normalization gap after this broadened rule; that should open a new bounded TP if it cannot be expressed as explicit-time fill.
2. Acceptance still remains infra-invalid for unrelated tool-evidence gaps; this block only closes the surfaced expectation drift from `r49`.

## Residual architecture debt (mandatory)

- Current residuals accepted in this block:
  - scenario taxonomy still relies on local normalization heuristics for some exact-time surfaces
- Why not in this block:
  - this slice only closes the surfaced explicit-time candidate drift without redesigning scenario ontology generation
- Risk if deferred:
  - generated acceptance runs will keep mislabeling explicit time fills as pending questions and waste expensive runtime cycles
- Linked follow-up Task Package(s):
  - `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`
- Expiry/trigger to stop deferral:
  - next exact-time surface that still mislabels a runtime-confirmed slot fill as pending-question after this normalization

## Next-block contract (mandatory)

- Next block objective:
  - rerun guarded acceptance after `P1.6o45` so the next surfaced blocker is not a false pending-question expectation on question-like exact-time fills
- First deterministic check command:
  - `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "grounded_time or pending_question" && git diff --check`
- Blocked-by conditions:
  - deterministic scenario regression not green
  - `r49` audited evidence not recorded in tracker docs
  - guarded rerun preflight invalid
- Owner role for closure:
  - `Hands` for deterministic normalization + rerun evidence, `Top Architect + Brain` for proof-bundle promotion

# TP-2026-03-13-p1.6o158-post-p1.6o157-proof-lane-a1

- Название/цель: выполнить один свежий guarded `dev L2` rerun после детерминированного закрытия `P1.6o157`, чтобы восстановить admissible proof validity на surfaced `provider_unavailable` calendar contour и truthfully определить, остаётся ли `dialog 1 / turn 11 / "Можно на 19:00?"` first fail на новом strict-audited прогоне.

## Canon refs

- Binding roadmap: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o135-beauty-salon-canonical-control-plane-roadmap-a1.md`
- Prior deterministic closure: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o157-provider-unavailable-calendar-hook-proof-validity-a1.md`
- Prior proof lane: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o156-post-p1.6o155-proof-lane-a1.md`
- Class-level routing: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o118-class-level-closure-process-reset-a1.md`
- Parent closure program: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Main remediation: `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- State baseline: `STATE.md`

## Git context

- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `origin/main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect` remove branch + worktree after merge

## One web search (mandatory before implementation)

- Not applicable in this block: proof-only continuation, no implementation.
- Decision:
  - `reuse`
  - reuse the existing guarded `dev L2` proof contour and strict artifact audit to determine whether `P1.6o157` restored admissible proof validity on the surfaced calendar-failure contour.

## Root cause (mandatory)

- Symptom:
  - `P1.6o157` is green on deterministic contour, but there is still no fresh proof showing whether the surfaced `provider_unavailable` calendar-failure family remains first fail once the hook-validity blocker is removed.
- Minimal reproduction:
  1. finish `P1.6o157` deterministic proof-tooling closure;
  2. verify `/admin/version` on `:18184` matches current `HEAD`;
  3. execute one fresh guarded `dev L2` rerun with explicit `--quality-lane dev`;
  4. strict-audit the run and classify the next admissible class from the surfaced first fail only.
- Evidence:
  - `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o157-provider-unavailable-calendar-hook-proof-validity-a1.md`
  - `/tmp/booking_quality/p1.6o156-l2-dev-20260313-a1-r1/summary.json`
  - `/tmp/booking_quality/p1.6o156-l2-dev-20260313-a1-r1/manual_audit.json`
- Five Whys:
  1. Why is another proof block needed? Because deterministic proof-tooling closure alone does not prove the expensive lane moved onto admissible evidence.
  2. Why not open a semantic/runtime slice immediately? Because `P1.6o156` was blocked by proof validity, not by an admitted semantic class.
  3. Why reuse the same live proof contour? Because parity drift would make first-fail routing non-admissible.
  4. Why explicit `--quality-lane dev`? Because proof continuation must avoid accidental acceptance-chain preflight.
  5. Why one run only? Because this block is classification/proof, not another expensive loop.
- Root cause statement:
  - after `P1.6o157`, the only missing evidence is one fresh strict-audited proof rerun that shows whether the surfaced calendar-failure turn is still first fail on admissible proof evidence.
- Fix mechanism:
  - verify runtime parity, run one guarded `dev L2`, strict-audit it, and select the next continuation only from the surfaced result.

## Reuse-first decision (mandatory)

- Reuse:
  - existing `ops/diagnose.py llm-quality` proof-lane contour
  - existing strict audit command
  - live runtime on `:18184`, relaunched from current worktree if parity is missing
- Integrate:
  - no code changes in this block
- Build:
  - none

## Release safety

- Rollout: none; proof-only block
- Go signal:
  - `/admin/version.git_commit == HEAD`
  - strict audit completes for the fresh rerun
- No-go signal:
  - runtime parity mismatch
  - preflight invalid
  - strict audit missing or artifact bundle incomplete
- Rollback:
  - no runtime/code rollback in this block; only stop after audit and publish truthful class routing

## Invariant

1. `P1.6o157` deterministic closure remains intact.
2. No new runtime/oracle patch is allowed before the rerun result is audited.
3. Proof stays on a fingerprint-verified runtime on `:18184`.
4. Evidence remains contract-first and class-routed.

## Scope

- runtime parity check on `:18184`
- one fresh guarded `dev L2`
- strict audit of the run
- class routing from the surfaced first fail
- truthful docs/state/session sync

## Out of scope

- new structural runtime/oracle changes before proof result
- lock/replay/full acceptance bundle
- baseline updates
- scenario regeneration changes beyond the single run

## Touch-list

- `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o157-provider-unavailable-calendar-hook-proof-validity-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o158-post-p1.6o157-proof-lane-a1.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `STATE.md`
- `docs/SESSIONS/SESSION-2026-02-19-llm-first-firebreak-a1.md`
- `docs/SESSION_INDEX.md`
- `/tmp/booking_quality/p1.6o158-l2-dev-20260313-a1-r1/**`

## Plan

1. Verify runtime parity on `:18184`; relaunch a fresh runtime from current worktree if needed.
2. Execute exactly one fresh guarded `dev L2` rerun.
3. Strict-audit the run.
4. Route the surfaced first fail under `P1.6o118`.
5. Sync truth and publish the next admissible block.

## DoD

1. One fresh guarded rerun completed on parity-verified runtime `:18184`.
2. Strict audit is done and artifacts are complete.
3. `P1.6o156`'s proof-validity blocker is no longer the reason the lane is non-admissible.
4. The next admissible block is selected from the surfaced class only.

## Checks

- `python3 - <<'PY'` parity check on `http://127.0.0.1:18184/admin/version`
- `python3 ops/diagnose.py llm-quality --base-url http://127.0.0.1:18184 --client-slug demo_salon --count 10 --mode llm --min-turns 10 --max-turns 15 --include-media --media-mode text --media-kind photo --scenario-coverage booking,info,interrupt,handoff --batch-size 5 --retry-count 2 --retry-backoff 0.6 --min-wait 0.2 --max-wait 0.4 --jid-mode unique --allow-non-allowlist --timeout-profile realistic --timeout 30.0 --poll-timeout 25.0 --poll-interval 0.5 --trace-timeout 25.0 --trace-interval 0.5 --manager-mode simulate --manager-channel telegram --manager-actions take,resolve --manager-wait 1.0 --pending-mode ack --ack-text 'ок' --tool-hooks auto --tool-confirm-text 'да' --tool-cancel-text 'отмена' --tool-calendar-text 'проверь запись' --tool-hook-wait 0.8 --tool-hook-limit 2 --tool-evidence-policy strict --reset-before-dialog --console-env /home/zhan/secrets/console-contract.env --console-mode real --output-dir /tmp/booking_quality/p1.6o158-l2-dev-20260313-a1-r1 --run-id p1.6o158-l2-dev-20260313-a1-r1 --history-max 20 --fail-on-thresholds --max-failures 1 --regression-tolerance 0.02 --max-post-llm-semantic-rewrite-rate 0.0 --max-keyword-override-rate 0.0 --lexicon-regex-delta-gate block --delta-gate-base-ref origin/main --hardcode-core-gate block --hardcode-core-base-ref origin/main --run-economy-gate block --run-economy-base-ref origin/main --manual-audit-gate block --forensic-sla-gate block --oracle-conflict-gate block --secret-transport-gate block --scenario-governance-gate block --scenario-governance-registry /tmp/booking_quality/_scenario_governance_registry.json --allow-non-canonical-lock-retry --quality-lane dev --judge-mode all --judge-sample 0.1 --judge-model gpt-4o-mini --judge-base-url https://api.openai.com --judge-timeout 25.0 --judge-max-tokens 320 --judge-cache-max-entries 5000`
- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o158-l2-dev-20260313-a1-r1 --status done --strict-artifacts`
- `git diff --check`

## Evidence

1. `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o158-post-p1.6o157-proof-lane-a1.md`
2. `/tmp/booking_quality/p1.6o158-l2-dev-20260313-a1-r1/summary.json`
3. `/tmp/booking_quality/p1.6o158-l2-dev-20260313-a1-r1/brief.md`
4. `/tmp/booking_quality/p1.6o158-l2-dev-20260313-a1-r1/failure_families.json`
5. `/tmp/booking_quality/p1.6o158-l2-dev-20260313-a1-r1/responses.jsonl`
6. `/tmp/booking_quality/p1.6o158-l2-dev-20260313-a1-r1/trace_bundle.jsonl`
7. `/tmp/booking_quality/p1.6o158-l2-dev-20260313-a1-r1/manual_audit.json`

## Rollback

1. No code rollback in this proof-only block.
2. If preflight or audit is invalid, stop and publish the invalid run truthfully.
3. Do not open a new structural TP from unaudited or non-admissible evidence.

## No-go

- no new runtime/oracle patch before the rerun result
- no baseline update from this run
- no silent retry loop beyond the single admissible run
- no stale class routing from pre-`P1.6o157` artifacts

## Risks / blockers

- runtime parity may drift after deterministic closure
- run may still stay non-canonical if infrastructure noise reappears
- surfaced class may still be another proof-validity or runtime continuity family and require careful routing discipline

## Residual architecture debt (mandatory)

### Current residuals accepted in this block

- collect/guard arbitration still lives inside `decision.py`
- proof bundle closure is still not complete
- full executable owner coverage remains incomplete outside the covered envelope

### Why not in this block

- this block is proof-only

### Risk if deferred

- the worktree would reopen structural coding without one fresh proof checkpoint after `P1.6o157`

### Linked follow-up Task Package(s)

- next structural or proof-validity TP selected only after `P1.6o158` strict audit

### Expiry/trigger to stop deferral

- immediately after `P1.6o158` finishes and the next surfaced class is known

## Next-block contract (mandatory)

### Next block objective

- publish the next bounded structural or proof-validity block from the surfaced `P1.6o158` first fail family.

### First deterministic check command

- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o158-l2-dev-20260313-a1-r1 --status done --strict-artifacts && git diff --check`

### Blocked-by conditions

- `P1.6o158` has not finished with strict audit
- surfaced class has not been routed truthfully under `P1.6o118`

### Owner role for closure

- `Top Architect + Brain`

## Execution status

- `done`

## Execution result

- Fresh guarded `dev L2` rerun `p1.6o158-l2-dev-20260313-a1-r1` completed on parity-verified runtime `:18184` and proved the covered `P1.6o157` hook-validity blocker moved off first fail.
- Strict audit is admissible for class routing on the executed prefix:
  - `artifact_integrity.valid=true`
  - `infra_valid=true`
  - `semantic_valid=false`
  - `stop_reason=max_failures_reached:1`
  - `dialogs_seen=[1,2]`
  - `oracle_arbitration.winner=contract`
- The surfaced first fail is `dialog 2 / turn 8 / "Какой мастер будет делать маникюр в субботу?"`:
  - runtime recorded `source=llm_policy_core`, `action=booking_prompt`, `expected_reply_type=time`, and generic `question_contract:policy_core_degraded_collect`;
  - `llm_policy_core` itself emitted `goal=info`, `subject_kind=specialist`, `capability=live_availability`, `resolution_mode=clarify_missing_time`, `pending_question_target=specialist`, and `active_question_relation=ask_about_requested_slot`, but runtime did not reuse the existing specialist-availability followup response/meta/trace path, so `master/specialist` evidence was lost and judge marked `missed_question`.
- Final classification:
  - this is a new admissible runtime owner-coverage miss, not a proof-validity blocker;
  - published `P1.6o159` as the next bounded `Track B` continuation.

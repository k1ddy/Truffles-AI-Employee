# TP-2026-03-11-p1.6o74-active-name-named-specialist-followup-owner-a1

- Название/цель: закрыть surfaced `r103` blocker family, где после bounded `M22` closure fresh guarded `dev L2` больше не падает на question-like daypart exact-time fill, но under active `name` collect declarative named-specialist follow-up `Я хотел бы записаться к Айгерим.` still loses the active-name specialist-followup contract: generated scenario falls back to stale `service_choice`, `llm_policy_core` emits direct `calendar.book_slot` with blank specialist-followup axes, and runtime accepts `Айгерим` both as `specialist_name` and `customer_name` instead of preserving `referent_followup` and resume `name`.

## Canon refs

- Parent verdict: `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o60a-remaining-closure-architecture-verdict-a1.md`
- Owner matrix: `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o60b-remaining-closure-owner-matrix-a1.md`
- Program TP: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Proof bundle: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`
- Prior child: `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o73-question-like-daypart-exact-time-fill-owner-a1.md`
- Related bounded closures: `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o66-policy-core-named-specialist-followup-owner-a1.md`, `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o72-active-name-deictic-time-availability-owner-a1.md`
- STATE: `STATE.md`
- CA: remaining closure row after `M22`

## One web search (mandatory before implementation)

- Query: `site:rasa.com/docs/rasa/forms change filled slot while another slot requested follow-up`
- Date/time: `2026-03-11T18:07:00+05:00`
- Sources opened:
  - official Rasa docs: `https://rasa.com/docs/rasa/forms/`
- Source quality:
  - official product documentation
- Found reusable ideas:
  - active slot collection must keep the requested-slot contract explicit even when the user temporarily supplies information for another slot or referent;
  - when the new message changes a different referent, the flow should preserve the original requested slot instead of silently committing a completed form action.
- Decision:
  - `reuse/integrate`
  - reuse the existing specialist-followup collect contract and integrate a bounded active-name row that prevents direct booking commit until the customer-name slot is explicitly supplied.
- Rejected options:
  - weakening scenario/oracle to accept stale `service_choice`;
  - accepting direct `calendar.book_slot` when `Айгерим` is simultaneously treated as both specialist and customer name;
  - broad phrase-hardcode branching outside the active `name` collect boundary.

## Root cause (mandatory)

- Symptom:
  - fresh guarded `dev L2` run `p1.6o6-l2-dev-20260311-a1-r103` no longer stops on `M22`, but the next first strict fail appears in dialog `1`, turn `8`, user text `Я хотел бы записаться к Айгерим.`
- Minimal reproduction:
  1. Inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r103/scenarios.json` dialog `1`, turns `5-8`.
  2. Confirm turn `5` grounds `datetime=15:00` and canonical resume becomes `reply_type=name`.
  3. Confirm turns `6-7` are info interrupts and do not clear the active `name` collect contract.
  4. Inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r103/scenarios.json` dialog `1`, turn `8` and confirm generated scenario still expects stale `reply_type=service_choice`.
  5. Inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r103/trace_bundle.jsonl` dialog `1`, turn `8` and confirm `llm_policy_core` succeeds but emits `tool_action=calendar.book_slot`, `subject_kind=specialist`, `capability=bookability`, `resolution_mode=direct`, while leaving `pending_question_target` and `active_question_relation` blank.
  6. Inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r103/responses.jsonl` dialog `1`, turn `8` and confirm runtime books immediately, setting the same token `Айгерим` as both specialist preference and customer name; judge passes the visible reply, but contract-first evaluation fails on `expected_meta_mismatch` and `expected_trace_miss`.
- Evidence:
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r103/summary.json`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r103/brief.md`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r103/scenarios.json`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r103/responses.jsonl`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r103/trace_bundle.jsonl`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r103/manual_audit.md`
- Five Whys:
  1. Why did the chain fail? The row closed by `M22` was gone, but the next turn committed booking directly instead of preserving the active `name` collect contract.
  2. Why did booking commit directly? `llm_policy_core` treated the named specialist statement as a direct `calendar.book_slot` action under already grounded time.
  3. Why is that wrong? The active missing slot is still `name`; the user selected a specialist, not their own customer name.
  4. Why did runtime allow it? Direct-booking validation accepted a plan where `slots.name` matched the specialist referent and no explicit customer-name signal was present.
  5. Why is this a new bounded family? `P1.6o66` closed named specialist follow-up under active `time` collect, and `P1.6o72` / `P1.6o73` closed grounded-time follow-ups, but the cross-product for declarative named specialist preference under active `name` collect was never frozen.
- Root cause statement:
  - the remaining closure lane lacks a bounded owner contract for declarative named-specialist follow-up under active `name` collect: scenario governance reopens stale `service_choice`, semantic owner can emit direct booking with blank specialist-followup axes, and runtime does not stop the specialist token from being misused as the customer-name slot.
- Fix mechanism:
  - freeze matrix row `M23` for active-name named-specialist follow-up;
  - align prompt + scenario expectations so this turn preserves `pending_question_target=specialist`, `active_question_relation=referent_followup`, and resume `name`;
  - add a bounded runtime guard that blocks direct `calendar.book_slot` when the active slot is still `name` and the turn only supplies a specialist preference, then resume through the existing specialist-followup collect owner.

## Reuse-first decision (mandatory)

- Reuse:
  - existing specialist-followup owner path in `decision.py`
  - existing active-name scenario normalization seams in `scripts/booking_dialog_scenarios.py`
  - existing quality guard patterns for specialist-followup meta/trace
- Integrate:
  - bounded active-name named-specialist row over the existing collect owner
  - bounded direct-booking validation guard when customer-name evidence is absent
- Do not build:
  - new generic booking flow
  - new specialist resolver stack
  - oracle waiver for stale `service_choice`

## Execution profile

- TP mode: `implementation`
- Doc touch budget (files): `12`
- Code dominance: `required`
- Override token: `none`
- Why this profile fits: surfaced family is a bounded active-name cross-product row with clear prompt/runtime/scenario seams and an explicit no-go against direct booking commit without customer-name evidence.

## Invariant

1. `P1.6o73/M22` remains closed.
2. Existing active-time specialist-followup rows (`M4`, `M15`) remain behaviorally consistent.
3. Active-name time-availability rows (`M12`, `M21`) remain behaviorally consistent.
4. Runtime must not silently accept specialist preference as customer name while `expected_reply_type=name` is still open.
5. Any bounded fix must preserve explicit `decision_meta` / `decision_trace` evidence.

## Scope

- publish surfaced `r103` family as matrix row `M23`
- implement bounded active-name named-specialist follow-up owner only

## Out of scope

- acceptance lock/replay
- generic name parser redesign
- calendar provider behavior changes
- re-opening `M22` after it already rolled to a different family

## Touch-list

- `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o74-active-name-named-specialist-followup-owner-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o60b-remaining-closure-owner-matrix-a1.md`
- `prompts/llm_policy_core.md`
- `scripts/booking_dialog_scenarios.py`
- `truffles-api/app/routers/webhook/decision.py`
- `truffles-api/tests/test_booking_dialog_scenarios_script.py`
- `truffles-api/tests/test_message_endpoint.py`
- `truffles-api/tests/test_booking_quality_response_guard.py`

## Plan (1..N)

1. Freeze `r103` as audited non-closure evidence and publish `M23`.
2. Align prompt/scenario contract so declarative named-specialist follow-up under active `name` collect keeps the active-name specialist-followup row instead of stale `service_choice`.
3. Add a bounded runtime guard that rejects direct booking commit when the turn changes specialist preference but does not explicitly provide customer name.
4. Add targeted deterministic regressions plus one adjacent negative case for real customer-name fill.
5. Run focused checks and `git diff --check`.
6. Authorize a fresh guarded `dev L2` only after deterministic green.

## DoD

1. `r103` is recorded in active docs as audited non-closure evidence.
2. Active-name named-specialist follow-up no longer inherits stale `service_choice` in generated scenarios.
3. Runtime no longer commits `calendar.book_slot` when the specialist token is the only new referent and the customer-name slot is still missing.
4. Final meta/trace preserve `pending_question_target=specialist`, `active_question_relation=referent_followup`, and `expected_reply_type=name`.
5. Adjacent explicit customer-name booking and active-name time-availability rows remain unchanged.
6. Focused deterministic scenario/runtime/evidence regressions are green.

## Checks

- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r103 --status done --strict-artifacts`
- `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "specialist_followup or active_name or service_choice"`
- `pytest -q truffles-api/tests/test_message_endpoint.py -k "specialist_followup and name"`
- `pytest -q truffles-api/tests/test_booking_quality_response_guard.py -k "specialist_followup and name"`
- `git diff --check`

## Execution status

- `closed_bounded`
- deterministic prompt/scenario/runtime/evidence closure is complete in this worktree
- fresh guarded `dev L2` `p1.6o6-l2-dev-20260311-a1-r104` no longer stops on `M23` and instead surfaces `M24`

## Evidence

1. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r103/summary.json`
2. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r103/brief.md`
3. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r103/scenarios.json`
4. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r103/responses.jsonl`
5. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r103/trace_bundle.jsonl`
6. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r103/manual_audit.md`
7. `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o73-question-like-daypart-exact-time-fill-owner-a1.md`
8. `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o74-active-name-named-specialist-followup-owner-a1.md`

## Rollback

1. Keep `P1.6o6` blocked.
2. Revert only the future bounded `M23` delta if it regresses adjacent active-name or active-time rows.
3. Do not roll back prior bounded rows (`M22`, `M21`, `M15`).

## No-go

- no oracle weakening to accept stale `service_choice`
- no direct `calendar.book_slot` acceptance when customer-name evidence is absent
- no phrase-hardcode branch outside the bounded active-name specialist-followup row
- no changes to unrelated booking-management or handoff families

## Risks / blockers

- `judge` already passes the visible reply on `r103`, so contract-first trace/meta invariants must remain the winner.
- The live row is compound: scenario governance is stale, but runtime also commits an incorrect booking intent. Both seams must be aligned together.
- `r103` is still non-canonical proof evidence (`infra_valid=false`, `semantic_valid=false`). This block can only claim bounded closure plus rerun rollover.

## Residual architecture debt (mandatory)

### Current residuals accepted in this block

- Active-name specialist availability and generic master-info follow-up rows remain separate bounded families.
- `llm_policy_core` / runtime still rely on shared `pending_question_target` + `active_question_relation` instead of a fuller owner object.

### Why not in this block

- `M23` is specifically about declarative named-specialist referent change under active `name` collect. Broadening into other active-name rows would violate bounded-row closure.

### Risk if deferred

- Adjacent active-name specialist families can still surface on later guarded reruns, but `M23` must be closed before those can be interpreted cleanly.

### Linked follow-up Task Package(s)

- `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o75-active-time-consult-topic-shift-service-choice-owner-a1.md`

### Expiry/trigger to stop deferral

- before the next guarded rerun after `M23`, if active-name specialist referent changes still do not have deterministic green evidence.

## Next-block contract (mandatory)

### Next block objective

- keep `M23` closed and implement the next surfaced consult topic-shift row `M24` before any new guarded `dev L2`.

### First deterministic check command

- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r104 --status done --strict-artifacts && git diff --check`

### Blocked-by conditions

- `M23` deterministic closure regresses
- `M24` is not published in the owner matrix
- active-time consult topic-shift scenario/runtime regressions are not green

### Owner role for closure

- `Hands` for deterministic implementation/evidence, `Top Architect + Brain` for guarded-rerun authorization and closure

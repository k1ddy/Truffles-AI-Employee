# TP-2026-03-13-p1.6o153-active-time-master-info-specialist-followup-owner-a1

- Название/цель: закрыть surfaced owner/runtime drift так, чтобы explicit generic `master` info question под активным `time` collect и `specialist_followup` envelope отвечалась как bounded info interrupt, а не схлопывалась обратно в `booking_prompt`.

## Canon refs

- Binding roadmap: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o135-beauty-salon-canonical-control-plane-roadmap-a1.md`
- Prior deterministic closure: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o151-timeout-owner-boundary-slot-constraint-continuity-a1.md`
- Prior proof lane: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o152-post-p1.6o151-proof-lane-a1.md`
- Historical generic master-info owner closure: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o139-active-time-generic-master-info-interrupt-owner-a1.md`
- Historical specialist-followup owner slices: `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o93-generic-specialist-choice-followup-owner-a1.md`
- Class-level routing: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o118-class-level-closure-process-reset-a1.md`
- Parent closure program: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Main remediation: `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- State baseline: `STATE.md`

## Git context

- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `origin/main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect` remove branch + worktree after merge

## One web search (mandatory before implementation)

- Query: `site:stately.ai/docs guards context state machines`
- Date/time: `2026-03-13T18:16:00+05:00`
- Sources opened:
  - `https://stately.ai/docs/guards`
- Source quality:
  - official documentation
- Found reusable ideas:
  - guarded transitions should be explicit and ordered, with the first matching guard taking the transition;
  - the bounded fix should be expressed as one additional guard on the surfaced owner envelope rather than as a broad rewrite of the collect path.
- Decision:
  - `reuse + integrate`
  - reuse the existing booking interrupt path and integrate one bounded guard that lets explicit generic master-info questions bypass the specialist-followup collect owner only on the surfaced envelope.
- Rejected options:
  - weakening oracle so missing `master` info no longer fails;
  - reopening scenario/oracle compiler even though runtime trace already shows the miss;
  - general refactor of all followup owner transitions in this block.

## Root cause (mandatory)

- Symptom:
  - fresh guarded `dev L2` `p1.6o152-l2-dev-20260313-a1-r1` fails first on `dialog 2 / turn 8 / "Какой мастер будет со мной?"` with `info_section_miss` and `judge_fail`: runtime returns `booking_prompt` (`На какую дату и время вам удобно?`) instead of answering the explicit `master` info question.
- Minimal reproduction:
  1. inspect `/tmp/booking_quality/p1.6o152-l2-dev-20260313-a1-r1/scenarios.json` dialog `2`, turn `8`;
  2. confirm the turn text is `Какой мастер будет со мной?` and tags are `consult`;
  3. inspect `/tmp/booking_quality/p1.6o152-l2-dev-20260313-a1-r1/responses.jsonl` dialog `2`, turn `8`;
  4. confirm final meta is `action=booking_prompt`, `intent=booking`, `source=llm_policy_core`, `expected_reply_type=time`, `expected_reply_reason=booking_prompt`, `pending_question_target=specialist`, `pending_question_interaction=specialist_followup`, `pending_question_owner=booking_specialist_followup`, `subject_kind=specialist`, `referent_key=master`, and `info_sections` is empty;
  5. inspect the same turn in `/tmp/booking_quality/p1.6o152-l2-dev-20260313-a1-r1/trace_bundle.jsonl`;
  6. confirm `question_contract` is `missed` with `expected_reply_blocked_by_info=true`, `signal_snapshot.info_class.intents=['master']`, `llm_policy_core` still returns `booking/collect` with `resolution_mode=referent_followup`, `pending_question_target=specialist`, and the trace ends with `question_contract decision=set reason=booking_prompt` instead of `booking_interrupt info_reply`;
  7. inspect `truffles-api/app/routers/webhook/decision.py` and confirm:
     - `_should_preserve_specialist_followup_owner(...)` returns `true` on the surfaced envelope (`target=specialist`, `collect_slot=datetime`, `expected_reply_type=time`, `resolution_mode=referent_followup`);
     - `generic_master_info_collect_interrupt` only opens `_handle_booking_interrupt(...)` when `policy_collect_slot == "name"` and `not specialist_followup`;
     - so the explicit generic master-info question never reaches the info-interrupt path.
- Evidence:
  - `/tmp/booking_quality/p1.6o152-l2-dev-20260313-a1-r1/summary.json`
  - `/tmp/booking_quality/p1.6o152-l2-dev-20260313-a1-r1/brief.md`
  - `/tmp/booking_quality/p1.6o152-l2-dev-20260313-a1-r1/failure_families.json`
  - `/tmp/booking_quality/p1.6o152-l2-dev-20260313-a1-r1/scenarios.json`
  - `/tmp/booking_quality/p1.6o152-l2-dev-20260313-a1-r1/responses.jsonl`
  - `/tmp/booking_quality/p1.6o152-l2-dev-20260313-a1-r1/trace_bundle.jsonl`
  - `/tmp/booking_quality/p1.6o152-l2-dev-20260313-a1-r1/manual_audit.json`
  - `truffles-api/app/routers/webhook/decision.py`
  - `truffles-api/tests/test_message_endpoint.py`
- Five Whys:
  1. Why did the run fail? Because the bot did not answer the explicit `master` question and instead repeated the booking time prompt.
  2. Why did runtime stay on booking collect? Because `llm_policy_core` returned `collect` inside a `referent_followup` specialist envelope.
  3. Why did the runtime not override to info interrupt? Because the generic master-info collect interrupt guard only admits `collect_slot=name` and `not specialist_followup`.
  4. Why is `specialist_followup` true here? Because the surfaced turn keeps `pending_question_target=specialist` with `resolution_mode=referent_followup` while `expected_reply_type` is still `time`.
  5. Why is this a runtime-owner slice and not another scenario/oracle fix? Because info-class detection already sees `master`, judge and contract both flag the missing answer, and the miss is caused by guard ordering in runtime.
- Root cause statement:
  - explicit generic `master` info questions inside an active `time` collect + `specialist_followup` envelope are blocked from the booking info-interrupt path by overly narrow owner guards, so runtime falls back to `booking_prompt` instead of answering `master` info first and preserving `time` resume.
- Fix mechanism:
  - add one bounded guard that lets explicit generic `master` info questions under active `time` collect + `specialist_followup` reuse `_handle_booking_interrupt(...)` before the specialist-followup collect owner continues;
  - preserve `expected_reply_type=time` and resume prompt after the info answer;
  - add targeted endpoint regression and keep response-guard coverage green on the existing generic master-info contract.

## Reuse-first decision (mandatory)

- Reuse:
  - existing booking info-interrupt path in `_handle_booking_interrupt(...)`
  - existing master-intent detection via `resolve_master_intent(...)`
  - existing generic master-info response-guard contract
- Integrate:
  - one bounded guard for the surfaced active-time specialist-followup envelope
  - targeted endpoint regression
- Build:
  - no new subsystem, no statechart/compiler expansion in this block

## Release safety

- Rollout: bounded runtime-owner fix on one surfaced master-info envelope only
- Go signal:
  - explicit generic `master` question under surfaced `specialist_followup` envelope returns info reply with `master` section and preserved `time` resume
  - existing generic master-info and specialist-followup tests remain green
  - `git diff --check` and `SESSION_AGENT=a1 scripts/session_check.sh` stay green
- No-go signal:
  - named-specialist preference turns start bypassing specialist-followup into generic master info
  - scenario/oracle files become necessary in this block
  - `time` resume is lost after the info interrupt
- Rollback:
  - revert only the bounded guard and the matching regression/docs

## Invariant

1. `P1.6o151` remains closed.
2. `P1.6o152` truth remains unchanged.
3. No scenario/oracle changes in this block.
4. Explicit named-specialist followup behavior must remain intact.
5. Generic master-info interrupts must still preserve `time` resume instead of reopening stale `specialist_followup`.

## Scope

- bounded runtime-owner fix for explicit generic `master` info under active `time` collect + `specialist_followup`
- targeted endpoint regression
- truthful docs/state/session sync

## Out of scope

- new proof rerun in this block
- generator/oracle/compiler changes
- broad specialist-followup refactor
- acceptance `lock/replay/full`

## Touch-list

- `truffles-api/app/routers/webhook/decision.py`
- `truffles-api/tests/test_message_endpoint.py`
- `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o152-post-p1.6o151-proof-lane-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o153-active-time-master-info-specialist-followup-owner-a1.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `STATE.md`
- `docs/SESSIONS/SESSION-2026-02-19-llm-first-firebreak-a1.md`
- `docs/SESSION_INDEX.md`

## Plan

1. Sync truth for completed `P1.6o152` and publish `P1.6o153`.
2. Add a bounded guard that detects explicit generic `master` info inside the surfaced active-time specialist-followup envelope.
3. Route that guard through the existing booking info-interrupt path before specialist-followup collect continues.
4. Add targeted endpoint regression and rerun existing generic master-info response-guard coverage.
5. Run deterministic checks and hygiene gates.
6. Sync canon truthfully and publish the next proof lane.

## DoD

1. Surfaced turn shape `Какой мастер будет со мной?` answers `master` info first and preserves `expected_reply_type=time`.
2. Final meta includes `booking_interrupt_info=true`, `info_sections` containing `master`, and `pending_question_target=time`.
3. Trace includes `booking_interrupt decision=info_reply` with `master` info and preserved `time` resume.
4. Existing generic master-info and specialist-followup regressions stay green.
5. Canon/docs/session are synced truthfully and the next admissible proof block is identified.

## Checks

- `python3 -m py_compile truffles-api/app/routers/webhook/decision.py truffles-api/tests/test_message_endpoint.py`
- `pytest -q truffles-api/tests/test_message_endpoint.py -k "generic_master_info_time_collect or generic_master_info_specialist_followup"`
- `pytest -q truffles-api/tests/test_booking_quality_response_guard.py -k "active_time_generic_master_info_interrupt"`
- `git diff --check`
- `SESSION_AGENT=a1 scripts/session_check.sh`

## Evidence

1. `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o153-active-time-master-info-specialist-followup-owner-a1.md`
2. `/tmp/booking_quality/p1.6o152-l2-dev-20260313-a1-r1/summary.json`
3. `/tmp/booking_quality/p1.6o152-l2-dev-20260313-a1-r1/brief.md`
4. `/tmp/booking_quality/p1.6o152-l2-dev-20260313-a1-r1/failure_families.json`
5. `/tmp/booking_quality/p1.6o152-l2-dev-20260313-a1-r1/scenarios.json`
6. `/tmp/booking_quality/p1.6o152-l2-dev-20260313-a1-r1/responses.jsonl`
7. `/tmp/booking_quality/p1.6o152-l2-dev-20260313-a1-r1/trace_bundle.jsonl`
8. `/tmp/booking_quality/p1.6o152-l2-dev-20260313-a1-r1/manual_audit.json`

## Rollback

1. Revert only the bounded master-info specialist-followup guard and matching tests/docs.
2. Keep `P1.6o152` proof artifacts unchanged.
3. Do not reinterpret this surfaced miss as scenario drift without new evidence.

## No-go

- no new proof run before deterministic closure is green
- no scenario/oracle changes
- no weakening info-section/judge gates to hide the miss
- no broad specialist-followup or generic master-info rewrite
- no phrase-hardcode for one literal master question

## Risks / blockers

- the bounded guard must not steal named-specialist preference/availability turns away from specialist-followup.
- booking interrupt must still preserve `time` resume after answering `master` info.
- if the surfaced turn depends on pending/handoff state beyond the current envelope, one runtime guard may be insufficient and would require truthful follow-up proof.

## Residual architecture debt (mandatory)

### Current residuals accepted in this block

- owner arbitration still lives inside `decision.py`
- full executable owner coverage for all specialist/master envelopes remains incomplete
- proof bundle closure remains non-canonical beyond the observed prefix

### Why not in this block

- this block is a bounded runtime-owner slice, not the broader owner-matrix/statechart program

### Risk if deferred

- fresh expensive runs will keep stopping on a reopened generic master-info miss inside specialist-followup context and hide the next real family

### Linked follow-up Task Package(s)

- next proof lane after deterministic closure of `P1.6o153`

### Expiry/trigger to stop deferral

- before the next guarded `dev L2` continuation after this bounded runtime slice

## Next-block contract (mandatory)

### Next block objective

- publish and run `P1.6o154`: execute one fresh guarded `dev L2` after `P1.6o153` deterministic closure to prove the surfaced master-info specialist-followup owner family moved off first fail and truthfully surface the next admissible class.

### First deterministic check command

- `pytest -q truffles-api/tests/test_message_endpoint.py -k "generic_master_info_time_collect or generic_master_info_specialist_followup" && pytest -q truffles-api/tests/test_booking_quality_response_guard.py -k "active_time_generic_master_info_interrupt" && git diff --check`

### Blocked-by conditions

- `P1.6o153` deterministic closure is not green
- `P1.6o152` truth is not synced into active docs/state/session
- surfaced contour is not reproduced by targeted message-endpoint regression

### Owner role for closure

- `Top Architect + Brain`

## Execution status

- `done`


## Execution result

- `truffles-api/app/routers/webhook/decision.py` now adds one bounded guard for explicit generic `master` info inside the surfaced active `time` collect + `specialist_followup` envelope (`collect_slot=datetime`, `pending_question_target=specialist`, `resolution_mode=referent_followup`, no named specialist preference) and routes only that contour through `_handle_booking_interrupt(...)`.
- The bounded guard passes `pending_question_target=time` into booking interrupt, so runtime answers the explicit `master` info question first and preserves the blocked `time` resume instead of reopening stale `booking_specialist_followup` owner.
- No scenario/oracle/compiler files changed in this block. Existing named-specialist and generic specialist-choice followup contours remain on their prior collect owner paths.
- Deterministic validation is green:
  - `python3 -m py_compile truffles-api/app/routers/webhook/decision.py truffles-api/tests/test_message_endpoint.py truffles-api/tests/test_booking_quality_response_guard.py`
  - `pytest -q truffles-api/tests/test_message_endpoint.py -k "generic_specialist_choice_followup or generic_master_info_interrupt_resumes_time_collect or generic_master_info_interrupt_bypasses_specialist_followup or named_master_question_rewrites_collect_to_master_info"`
  - `pytest -q truffles-api/tests/test_booking_quality_response_guard.py -k "active_time_generic_master_info_interrupt"`
  - `git diff --check`
  - `SESSION_AGENT=a1 scripts/session_check.sh`
- Published `P1.6o154` as the next admissible proof continuation.

# TP-2026-03-08-p1.6o31-tool-registry-missing-slot-pending-question-evidence-a1.md

- Название/цель: закрыть новый `r30` runtime blocker family, где `llm_policy_core` корректно планирует `ask_about_requested_slot(time)` для `calendar.list_slots`, но финальный `tool_registry missing_slot` reply теряет `pending_question_interaction` evidence в `decision_meta/decision_trace`.
- Parent TP: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Canon refs: `AGENTS.md`, `STATE.md`, `STRUCTURE.md`, `STRATEGY/REQUIREMENTS.md`, `docs/runbooks/BOOKING_CONFIRM_VERIFY.md`, `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`, `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o30-slot-constraint-answer-normalization-a1.md`
- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect remove worktree + branch after merge`

## One web search (mandatory before implementation)

- Query: `site:rasa.com/docs/rasa/forms requested_slot follow up question missing slot state`
- Time (UTC): `2026-03-08T11:42:00Z`
- Sources:
  - `https://rasa.com/docs/rasa/forms/`
- Ready solutions found:
  - when the user asks about the currently requested slot instead of filling it, the form/runtime must preserve the requested-slot state and resume contract; follow-up clarification should remain attached to the active slot rather than being flattened into a generic response outcome.
- Decision (`reuse/integrate/build`): `reuse + integrate`
  - reuse the existing `pending_question_act/target` contract emitted by `llm_policy_core`;
  - integrate that evidence into the existing `tool_registry missing_slot` reply path;
  - build no new phrase heuristics and no new runtime family beyond the bounded `ask_about_requested_slot(time)` contract.
- Rejected options:
  - weakening scenario/oracle to stop requiring `pending_question_interaction` evidence;
  - rewriting the user turn post-hoc into another semantic family;
  - replacing the tool reply with a hardcoded phrase path just to satisfy `r30`.

## Root cause (mandatory)

- Symptom:
  - guarded `dev L2` rerun `p1.6o6-l2-dev-20260308-a1-r30` progressed beyond the `P1.6o30` family, then stopped on dialog `1`, turn `3`, text `Есть ли свободные слоты на этой неделе?` with `expected_meta_mismatch` and `expected_trace_miss`.

- Minimal reproduction:
  1. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r30/scenarios.json` for dialog `1`, turn `3`
  2. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r30/responses.jsonl` for message id `LLM-QUAL-p1.6o6-l2-dev-20260308-a1-r30-001-03-fafa3d`
  3. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r30/trace_bundle.jsonl` for the same turn
  4. compare `llm_policy_core` trace payload (`pending_question_act=ask_about_requested_slot`, `pending_question_target=time`) against the final `tool_registry missing_slot` meta/trace

- Evidence:
  - scenario expected:
    - `decision_meta.pending_question_act=ask_about_requested_slot`
    - `decision_meta.pending_question_target=time`
    - `decision_trace` stage `pending_question_interaction`
  - runtime actually did:
    - `llm_policy_core` payload: `tool_action=calendar.list_slots`, `pending_question_act=ask_about_requested_slot`, `pending_question_target=time`
    - `tool_registry` result: `tool_decision=missing_slot`
    - reply text: `На какую дату и время вам удобно?`
    - `question_contract` stayed active with `expected_reply_type=time`
  - final `decision_meta` lost `pending_question_act/target`, and final trace had no `pending_question_interaction` stage, so acceptance saw an observability contract miss rather than a product reply error.

- Five Whys:
  1. Why did `r30` fail: final meta/trace did not carry pending-question interaction evidence.
  2. Why was the evidence missing: the `tool_registry missing_slot` reply path only set expected-reply follow-up and generic `llm_policy_core_tool` trace.
  3. Why is that insufficient: this family is not plain collect; it is `ask_about_requested_slot(time)` with mandatory resume evidence.
  4. Why did earlier fixes not cover it: `P1.6o28` only patched the `semantic_temporal_scope_missing` degrade branch, not the normal live-calendar `missing_slot` path.
  5. Why did it surface now: `P1.6o30` removed the previous deterministic taxonomy blocker, so the next real runtime compression became first failure.

- Root cause statement:
  - the normal `tool_registry calendar.list_slots -> missing_slot` reply path preserves `expected_reply_type=time` but drops the already-resolved `pending_question_act/target`, so runtime observability compresses a bounded pending-question interaction into a generic missing-slot reply.

- Fix mechanism:
  - preserve `pending_question_act/target` and emit `pending_question_interaction` trace when `llm_policy_core` planned `ask_about_requested_slot(time)` and the executed tool path resolves to `calendar.list_slots/missing_slot` with `expected_reply_type=time`;
  - keep the existing tool reply text and resume contract;
  - add deterministic regression on message endpoint plus guard/oracle acceptance.

## Release safety

- Strategy: bounded runtime observability fix on existing `tool_registry` path
- Go signals:
  - final `decision_meta` keeps `pending_question_act/target` for the bounded family
  - final trace includes `pending_question_interaction`
  - reply text and `expected_reply_type=time` stay unchanged
- No-go signals:
  - new phrase/regex branching in runtime core
  - changing the semantic owner away from `llm_policy_core`
  - weakening oracle instead of preserving evidence
- Rollback verification:
  - revert only the pending-question evidence hook if unrelated `calendar.list_slots/missing_slot` outcomes regress

## Invariant

1. `P1.6o30` stays closed; no rollback to slot-constraint fitting.
2. `llm_policy_core` remains the semantic owner; runtime only preserves already decided interaction evidence.
3. Tool reply content for `calendar.list_slots/missing_slot` remains contract-compatible.
4. Fresh expensive iteration stays exactly one guarded rerun after deterministic closure.

## Scope

- `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o31-tool-registry-missing-slot-pending-question-evidence-a1.md`
- `truffles-api/app/routers/webhook/decision.py`
- `truffles-api/tests/test_message_endpoint.py`
- `truffles-api/tests/test_booking_quality_response_guard.py`

## Out of scope

- scenario taxonomy changes
- broader `pending_question_target` redesign
- acceptance lock/replay promotion

## Touch-list

- `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o31-tool-registry-missing-slot-pending-question-evidence-a1.md`
- `truffles-api/app/routers/webhook/decision.py`
- `truffles-api/tests/test_message_endpoint.py`
- `truffles-api/tests/test_booking_quality_response_guard.py`

## Plan (1..N)

1. Freeze `r30` as non-closure evidence and isolate the blocker as runtime observability loss, not taxonomy drift.
2. Patch the bounded `tool_registry missing_slot` path to preserve pending-question interaction evidence.
3. Add deterministic regressions for endpoint behavior and quality guard acceptance.
4. Run one fresh guarded rerun `r31` only after deterministic checks are green.

## DoD

1. `calendar.list_slots/missing_slot` under `ask_about_requested_slot(time)` keeps `pending_question_act/target` in final `decision_meta`.
2. The same path emits `decision_trace` stage `pending_question_interaction`.
3. Deterministic endpoint and guard regressions are green.
4. Next expensive step is exactly one fresh guarded rerun `r31`.

## Expensive run control (mandatory)

- Hypothesis:
  - after preserving bounded pending-question evidence in the normal tool path, the next guarded rerun will not fail first on the `r30` observability gap.
- Expected measurable effect:
  - either the run proceeds beyond dialog `1`, turn `3`, or a different blocker family surfaces.
- Stop condition:
  - run exactly one fresh guarded rerun plus strict audit; if the same mismatch persists, stop and reopen RCA instead of stacking more evidence-only patches.

## Checks

- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r30 --status done --strict-artifacts`
- `pytest -q truffles-api/tests/test_message_endpoint.py -k "pending_question_act_time_guidance or list_slots_missing_slot_pending_question"`
- `pytest -q truffles-api/tests/test_booking_quality_response_guard.py -k "pending_question_interaction or booking_slot_stall"`
- `git diff --check`

## Execution status

- `done`
- bounded runtime fix is green:
  - `pytest -q truffles-api/tests/test_message_endpoint.py -k "pending_question_act_time_guidance or list_slots_missing_slot_pending_question"`
  - `pytest -q truffles-api/tests/test_booking_quality_response_guard.py -k "pending_question_interaction or booking_slot_stall"`
  - `git diff --check`
- fresh guarded `r31` is audited non-closure evidence and no longer surfaces the missing pending-question evidence family
- `r31` instead surfaces a different deterministic taxonomy family on specialist-reference booking follow-up, reopened as `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o32-specialist-reference-time-collect-normalization-a1.md`

## Evidence

1. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r30/summary.json`
2. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r30/brief.md`
3. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r30/scenarios.json`
4. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r30/responses.jsonl`
5. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r30/trace_bundle.jsonl`
6. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r30/manual_audit.md`
7. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r31/summary.json`
8. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r31/brief.md`
9. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r31/manual_audit.md`

## Rollback

1. Revert only the pending-question evidence hook if unrelated `tool_registry` outcomes regress.
2. Keep `P1.6o6` blocked; do not reinterpret `r30` as closure evidence.

## No-go

- no scenario/oracle weakening
- no phrase-hardcoded reply routing in runtime core
- no new expensive rerun before deterministic regressions are green

## Risks/Blockers

1. Another adjacent live-calendar family may surface once pending-question evidence is preserved.
2. Guarded run can still remain non-canonical because of orthogonal tool-evidence infra invalidity.

## Residual architecture debt (mandatory)

- Current residuals accepted in this block:
  - broader pending-question interaction coverage beyond `ask_about_requested_slot(time)` remains open
- Why not in this block:
  - this block only repairs the first surfaced runtime compression on the normal `tool_registry missing_slot` path
- Risk if deferred:
  - acceptance continues to see correct reply/resume behavior as contract drift because interaction evidence disappears post-tool
- Linked follow-up Task Package(s):
  - `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`
  - `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o32-specialist-reference-time-collect-normalization-a1.md`
- Expiry/trigger to stop deferral:
  - before the next guarded rerun after `r31`

## Next-block contract (mandatory)

- Next block objective:
  - keep `P1.6o31` closed and close bounded `P1.6o32`, then run one fresh guarded rerun `r32` to verify that specialist-reference booking turns no longer drift to `service_choice`
- First deterministic check command:
  - `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "specialist_reference or pending_question or service_grounded_booking" && git diff --check`
- Blocked-by conditions:
  - `P1.6o32` is not deterministically green
  - guarded preflight is not green
  - runtime `http://127.0.0.1:18184` is unavailable
- Owner role for closure:
  - `Hands` for doc/code/evidence, `Top Architect + Brain` for closure gating

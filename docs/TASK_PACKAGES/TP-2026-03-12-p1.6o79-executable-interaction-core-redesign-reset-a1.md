# TP-2026-03-12-p1.6o79-executable-interaction-core-redesign-reset-a1

## Block identity
- `BLOCK_ID`: `P1.6O79-EXECUTABLE-INTERACTION-CORE-REDESIGN-RESET-A1`
- `PARENT_BLOCK_ID`: `P1.6O-DEMO-SALON-ARCHITECTURE-CLOSURE-PROGRAM-A1`
- `DEPENDS_ON`: `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o60a-remaining-closure-architecture-verdict-a1.md`, `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o60b-remaining-closure-owner-matrix-a1.md`, audited `r112` evidence in `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r112`, active blocker doc `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o78-booking-confirm-pricing-interrupt-service-choice-drift-a1.md`
- `UNLOCKS`: `P1.6O80-BASE-CANON-INTERACTION-MODEL-SYNC-A1`, `P1.6O81-MACHINE-READABLE-OWNER-MATRIX-A1`, `P1.6O82-PERSISTED-INTERACTION-STATE-A1`, `P1.6O83-OWNER-RESOLVER-M27-VERTICAL-SLICE-A1`

## Название/цель
Зафиксировать forensic reset для remaining closure phase: признать `P1.6o16..P1.6o78` mined evidence corpus и regression set, остановить reactive child loop как default closure path, и перевести следующий этап в executable interaction core (`InteractionState` + machine-readable owner matrix + `OwnerResolver`) вместо очередного row patch в `decision.py`.

## Canon refs
- `AGENTS.md`
- `STATE.md`
- `STRUCTURE.md`
- `STRATEGY/REQUIREMENTS.md`
- `SPECS/ARCHITECTURE.md`
- `SPECS/CONSULTANT.md`
- `SPECS/SYSTEM_REFERENCE.md`
- `docs/runbooks/BOOKING_CONFIRM_VERIFY.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o60a-remaining-closure-architecture-verdict-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o60b-remaining-closure-owner-matrix-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o78-booking-confirm-pricing-interrupt-service-choice-drift-a1.md`

## FACT pre-check (before implementation)
- `Impacted code/contracts/tests`:
  - `STRATEGY/REQUIREMENTS.md`
  - `SPECS/ARCHITECTURE.md`
  - `SPECS/CONSULTANT.md`
  - `docs/runbooks/BOOKING_CONFIRM_VERIFY.md`
  - `truffles-api/app/routers/webhook/decision.py`
  - `truffles-api/app/routers/webhook/booking.py`
  - `truffles-api/app/schemas/intent.py`
  - `truffles-api/app/services/intent_service.py`
- `Baseline commands`:
  - `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r112 --status done --strict-artifacts`
  - `wc -l truffles-api/app/routers/webhook/decision.py scripts/booking_dialog_scenarios.py truffles-api/tests/test_message_endpoint.py`
  - `sed -n '9305,9348p' truffles-api/app/routers/webhook/decision.py`
  - `sed -n '495,524p' SPECS/ARCHITECTURE.md`
  - `sed -n '54,60p' STRATEGY/REQUIREMENTS.md`
  - `sed -n '150,190p' truffles-api/app/services/intent_service.py && sed -n '220,230p' truffles-api/app/services/ai_service.py`
- `FACT findings`:
  - audited `r112` remains non-canonical proof evidence (`infra_valid=false`, `semantic_valid=false`) even though it truthfully surfaces the next remaining row `M27`.
  - prompt/schema now expose `pending_question_act`, `pending_question_target`, and `active_question_relation`, but persisted cross-turn state still stores only `slot`, `expected_reply_type`, `reason`, and `value` in `pending_question_contract`.
  - base canon already requires pending-question interaction separation, but `SPECS/ARCHITECTURE.md` session memory does not yet persist interaction target / relation / owner.
  - row knowledge remains compiled into runtime helpers inside `decision.py` / `booking.py`, not into one executable owner mechanism.
  - controller / plan / policy-core still run on a split model lineage relative to `FAST_MODEL`, which the parent verdict already marks as a semantic-drift amplifier.
  - current file pressure confirms the structural problem: `decision.py` is still a god-file and `message_endpoint` / scenario scripts have become the practical owner-matrix surrogate.
- `Detected drift (docs vs code)`:
  - parent docs describe a target architecture where `expected_reply_type` is the resume axis and `pending_question_target` / `active_question_relation` define the current interaction row, but runtime persists only the resume contract and reconstructs row semantics ad hoc.
  - global canon (`STRATEGY/REQUIREMENTS.md`, `SPECS/ARCHITECTURE.md`, `SPECS/CONSULTANT.md`) lags behind the parent matrix docs and therefore still cannot serve as the single source of truth for the remaining closure lane.

## One web search (mandatory before implementation)
- **Query (exact):** `site:stately.ai/docs context guards state machines hierarchy`
- **Date/time (local):** `2026-03-12 08:18 +0500`
- **Why this query is precise:** it targets official state-machine guidance for persistent context, pure guarded transitions, and hierarchy, which are the missing architectural ingredients behind the current row-by-row runtime branching.
- **Sources opened (from this query):**
  - `Stately / XState Context` — `https://stately.ai/docs/context`
  - `Stately / XState Guards` — `https://stately.ai/docs/guards`
  - `Stately / XState Parent states` — `https://stately.ai/docs/parent-states`
- **Source quality:** official product / primary documentation.
- **Existing solutions found:**
  - persistent actor context should hold extended state explicitly, not be reconstructed from side effects;
  - guards should stay pure and synchronous, deciding admissible transitions without mutating semantics;
  - parent/hierarchical states are the right way to model a stable top-level booking flow with nested interruption / follow-up sub-states.
- **Decision:** `reuse + integrate` — reuse existing owner-matrix discoveries and integrate them into an explicit state/context/resolver architecture.
- **Rejected options:**
  - continue shipping row-specific preserve/recover helpers as the main closure mechanism;
  - treat prompt/schema enrichment alone as closure;
  - weaken oracle/runbook so `P1.6o78` can pass without architectural migration.

## Root cause (mandatory)
- **Symptom:** bounded child closures are truthful and useful, but each fresh guarded `dev L2` keeps surfacing the next adjacent owner row because the owner matrix is documentary rather than executable.
- **Minimal reproduction:**
  1. audit `r112` and confirm `M27` is the next surfaced row.
  2. inspect `truffles-api/app/routers/webhook/decision.py:9305-9348` and confirm persisted pending-question state omits interaction target / relation / owner.
  3. inspect `SPECS/ARCHITECTURE.md:495-524` and confirm session memory does not define first-class interaction state.
  4. inspect `truffles-api/app/routers/webhook/decision.py:6017-6265` and confirm row-specific preserve helpers are still the effective owner engine.
  5. inspect `truffles-api/app/services/intent_service.py:155-183` plus `truffles-api/app/services/ai_service.py:223-224` and confirm model split remains active.
- **Evidence to capture:**
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r112/summary.json`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r112/brief.md`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r112/trace_bundle.jsonl`
  - file/line references above
  - this redesign TP and synced canon/program docs
- **Five Whys (or equivalent):**
  1. Why does a new family keep surfacing after each bounded fix? Because the fixes close rows locally but do not create a global executable owner mechanism.
  2. Why is there no executable owner mechanism? Because runtime still persists only the resume contract and reconstructs row semantics with helper branches.
  3. Why does runtime reconstruct semantics ad hoc? Because the owner matrix is markdown-only and not compiled into a machine-readable contract plus pure resolver.
  4. Why is this allowed to continue? Because base canon still stops at generic pending-question interaction rules and does not yet encode interaction target / relation / owner / degrade constraints as first-class runtime obligations.
  5. Why is closure still invalid even with many green bounded rows? Because expensive runs are still discovering structure the architecture should already predict, so the proof lane demonstrates mined truth, not compiled architecture.
- **Root cause statement:** the remaining closure problem is architectural: Truffles has already mined a typed interaction protocol for active pending-question operations, but the runtime still persists only a reduced resume contract and compiles row semantics into monolithic branching, so the owner matrix cannot act as the predictive, executable source of truth.
- **Fix mechanism:**
  - freeze a redesign reset that reclassifies the child chain as mined evidence corpus;
  - promote the interaction model into base canon;
  - publish a machine-readable owner matrix artifact;
  - add first-class persisted `InteractionState`;
  - extract a pure `OwnerResolver` and migrate `M27` through that mechanism as the first vertical slice.

## Reuse-first plan (mandatory)
- **Internal reuse:**
  - `P1.6o60a` parent architecture verdict
  - `P1.6o60b` binding owner matrix
  - current prompt/schema axes (`pending_question_act`, `pending_question_target`, `active_question_relation`)
  - existing `question_contract`, `booking_confirm_reject`, `current_referents`, `decision_trace/meta`, and bounded regression corpus from `P1.6o16..P1.6o78`
- **External reuse:**
  - official Stately / XState state-machine guidance for explicit context, pure guards, and hierarchical states
- **Why not reinvent the wheel:** the system already discovered the hard interaction families; the missing step is to compile them into one state/context/resolver layer rather than rediscover them through guarded reruns.

## Execution profile (mandatory for non-doc blocks)
- **TP mode:** `doc_only`
- **Doc touch budget (files):** `7`
- **Code dominance:** `off`
- **Override token:** `none`
- **Why this profile fits:** this block is a contract-first redesign reset; runtime edits before it is published would just continue the reactive row-patch loop.

## Invariant
- No runtime phrase-fitting.
- No oracle weakening.
- No new expensive guarded run from this block.
- `P1.6o78/M27` remains open truthfully; it is reclassified as tactical containment, not hidden or softened.
- Previously closed bounded rows remain preserved as regression corpus.

## Scope
- Publish the redesign reset for the remaining closure lane.
- Reclassify `P1.6o16..P1.6o78` as mined evidence corpus + regression set.
- Define the target executable interaction-core architecture.
- Sync the active main TP / closure program / state ledger so `P1.6o78` is no longer the default next step.

## Out of scope
- Runtime implementation of `InteractionState`.
- Machine-readable owner matrix artifact creation.
- `M27` runtime fix.
- Fresh `dev L2` / lock / replay / full quality runs.

## Touch-list
- `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o79-executable-interaction-core-redesign-reset-a1.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o78-booking-confirm-pricing-interrupt-service-choice-drift-a1.md`
- `STRUCTURE.md`
- `STATE.md`
- `docs/SESSIONS/SESSION-2026-02-19-llm-first-firebreak-a1.md`

## Plan (1..N)
1. Freeze the current child chain as mined evidence corpus rather than closure proof.
2. Publish the target architecture for executable interaction ownership.
3. Reframe `P1.6o78` as tactical containment only.
4. Sync main TP / program TP / state ledger / structure map.
5. Block any new expensive run until the redesign chain starts with canon sync.

## Target architecture reset (binding)
- The next closure phase is measured by mechanisms, not by the count of bounded child rows.
- Required mechanisms:
  - first-class persisted `InteractionState`
  - machine-readable owner matrix artifact
  - pure `OwnerResolver`
  - deterministic family tests generated from the same owner matrix source
- Required `InteractionState` minimum fields:
  - `resume_slot`
  - `interaction_target`
  - `interaction_relation`
  - `interaction_owner`
  - `grounded_referents`
  - `confirmation_state`
  - `degrade_reason`
- Contract rules:
  - `expected_reply_type` remains the resume axis
  - `pending_question_target` is the interaction target, not a fallback resume field
  - `active_question_relation` is mandatory for any turn operating over active pending-question state
  - deterministic runtime may validate / block / degrade, but may not invent a different owner row after policy-core without an explicit contract reason-code
- Closure metrics for the redesign phase:
  - persisted interaction state exists
  - machine-readable owner matrix exists
  - one resolver owns row admission / degrade / forbidden compression
  - `M27` is migrated through that mechanism and then replayed through guarded quality

## Delivery program reset (binding)
- `P1.6o78` stays open as tactical containment of `M27`; it is not the default next block.
- Default redesign chain:
  1. `P1.6o80` — base-canon interaction-model sync
  2. `P1.6o81` — machine-readable owner matrix artifact
  3. `P1.6o82` — persisted `InteractionState`
  4. `P1.6o83` — `OwnerResolver` extraction + `M27` vertical slice
  5. guarded `dev L2 -> lock/replay/full` only after the above are green on deterministic checks
- `P1.6o16..P1.6o78` are preserved as regression corpus and acceptance evidence history; they are not thrown away.

## DoD
- This redesign reset TP is published and linked from the active program docs.
- Main TP no longer names `P1.6o78` as the default next step.
- Program TP no longer names `P1.6o78` as the default next block.
- `P1.6o78` is explicitly marked tactical-only pending the redesign chain.
- `STATE.md` records the reset with evidence.
- No runtime code is touched in this block.

## Checks
- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r112 --status done --strict-artifacts`
- `rg -n "Pending-question interaction contract|Session Memory v1.1|pending_question_contract|P1\.6o78|P1\.6o79" STRATEGY/REQUIREMENTS.md SPECS/ARCHITECTURE.md docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md docs/TASK_PACKAGES/TP-2026-03-11-p1.6o78-booking-confirm-pricing-interrupt-service-choice-drift-a1.md`
- `git diff --check`
- `SESSION_AGENT=a1 scripts/session_check.sh`

## Evidence
- `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o79-executable-interaction-core-redesign-reset-a1.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o78-booking-confirm-pricing-interrupt-service-choice-drift-a1.md`
- `STRUCTURE.md`
- `STATE.md`
- `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r112/summary.json`
- `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r112/trace_bundle.jsonl`

## Token / run budget (mandatory for expensive suites)
- **Max full runs:** `0`
- **Fail-fast / scenario lock:** reuse audited `r112` evidence only
- **Stop condition:** if the redesign cannot be expressed as explicit executable mechanisms and concrete follow-up blocks, stop and reopen the parent verdict instead of creating another bounded child TP
- **Escalation path:** `Top Architect + Brain`

## Release safety (mandatory for non-doc changes)
- **Strategy:** documentation-only redesign reset
- **Go/no-go signals:** active docs all point to the redesign chain; no runtime diff; `session_check` green
- **Rollback:** revert the doc-only reset commit
- **Post-release monitoring window:** next child TP must start with canon sync before any runtime patch or guarded rerun

## Rollback
- Revert the redesign reset docs and restore `P1.6o78` as the default next block only if the reset introduces canon drift or breaks session gates.

## No-go
- No claim that `P1.6o78` alone can close the lane.
- No new row-by-row closure loop as the primary plan.
- No hidden downgrade of `M27` from runtime blocker to oracle noise.
- No fresh guarded rerun before the redesign chain starts.
- No new semantic branching in `decision.py` from this block.

## Risks / blockers
- Existing runtime diff is large and branch-heavy; if more row patches land before the redesign chain is synced, the god-file and proof debt will grow.
- `r112` remains non-canonical proof evidence; architecture reset does not itself make the proof lane valid.
- Follow-up blocks will need careful sync with existing dirty worktree changes in `decision.py`, `booking.py`, and prompt/schema files.

## Residual architecture debt (mandatory)
- `Current residuals accepted in this block`:
  - `M27` remains open in runtime.
  - machine-readable owner matrix does not exist yet.
  - persisted `InteractionState` does not exist yet.
  - model-routing split remains unresolved.
- `Why not in this block`:
  - this block is intentionally doc-only so the redesign contract is frozen before any new runtime migration.
- `Risk if deferred`:
  - the team will keep shipping truthful but non-closing bounded fixes and the remaining proof lane will stay reactive.
- `Linked follow-up Task Package(s)`:
  - `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o80-base-canon-interaction-model-sync-a1.md`
  - `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o81-machine-readable-owner-matrix-a1.md`
  - `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o82-persisted-interaction-state-a1.md`
  - `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o83-owner-resolver-m27-vertical-slice-a1.md`
  - `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o78-booking-confirm-pricing-interrupt-service-choice-drift-a1.md`
- `Expiry/trigger to stop deferral`:
  - before any new guarded quality run or any new row-specific runtime child beyond `M27`.

## Next-block contract (mandatory)
- `Next block objective`:
  - publish `P1.6o80` as the first redesign child and sync global canon (`STRATEGY/REQUIREMENTS.md`, `SPECS/CONSULTANT.md`, `SPECS/ARCHITECTURE.md`, `docs/runbooks/BOOKING_CONFIRM_VERIFY.md`) with the interaction model already frozen in `P1.6o60a/P1.6o60b`.
- `First deterministic check command`:
  - `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r112 --status done --strict-artifacts && rg -n "Pending-question interaction contract|Session Memory v1.1|Booking/info interrupt contract" STRATEGY/REQUIREMENTS.md SPECS/CONSULTANT.md SPECS/ARCHITECTURE.md docs/runbooks/BOOKING_CONFIRM_VERIFY.md && git diff --check`
- `Blocked-by conditions`:
  - redesign reset not published
  - global canon still lacks interaction target / relation / owner semantics
  - no exact follow-up TP exists for canon sync
- `Owner role for closure`:
  - `Top Architect + Brain` for this redesign reset and canon sync signoff; `Hands` only after executable interaction-core child TPs are published

## Handoff (for zero-context next agent)
- `Ready for next agent`: `yes`
- `Start from`: `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o79-executable-interaction-core-redesign-reset-a1.md`
- `Do not touch`: runtime `decision.py` / `booking.py` semantics in this block; do not resume guarded quality until `P1.6o80` exists and is green

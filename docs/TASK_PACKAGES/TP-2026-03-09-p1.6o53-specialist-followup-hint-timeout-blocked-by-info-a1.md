# TP-2026-03-09-p1.6o53-specialist-followup-hint-timeout-blocked-by-info-a1.md

- Название/цель: закрыть surfaced `r63` blocker family, где generic pricing/info interrupt ошибочно сохраняет stale `service_choice` в scenario-governance, а затем question-like specialist follow-up `Можно к Айгерим?` под активным `expected_reply_type=time` уже имеет `pending_question_target=specialist` в policy-core, но runtime при `specialist_hint timeout/empty` схлопывается в generic `booking_prompt` без `booking_specialist_followup` evidence.
- Parent TP: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Canon refs: `AGENTS.md`, `STATE.md`, `STRUCTURE.md`, `STRATEGY/REQUIREMENTS.md`, `docs/runbooks/BOOKING_CONFIRM_VERIFY.md`, `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o52-reschedule-followup-service-choice-drift-a1.md`
- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect remove worktree + branch after merge`

## One web search (mandatory before implementation)

- Query: `Rasa forms unhappy path questions during slot filling official docs`
- Time (UTC): `2026-03-09T09:26:00Z`
- Sources:
  - `https://rasa.com/docs/rasa/forms/`
- Ready solutions found:
  - slot-owner continuity should survive side-question interrupts when the form already knows the active slot;
  - if auxiliary extraction fails, runtime should prefer an existing deterministic/contractual follow-up owner over collapsing to a generic prompt.
- Decision (`reuse/integrate/build`): `reuse + integrate`
  - reuse existing `booking_specialist_followup` owner and existing specialist-hint recovery path;
  - integrate a non-LLM / fail-closed recovery when `pending_question_target=specialist` survives but `specialist_hint` times out;
  - build no new phrase router in core.
- Rejected options:
  - waiving the missing `pending_question_interaction` evidence;
  - teaching core special cases for `Айгерим`;
  - accepting generic `booking_prompt` as equivalent to specialist follow-up when the contract explicitly says `specialist`.

## Root cause (mandatory)

- Symptom:
  - guarded `dev L2` run `p1.6o6-l2-dev-20260309-a1-r63` stops on dialog 1 turn 3 with `expected_meta_mismatch` and `expected_trace_miss` for `Можно к Айгерим?`; inspection also shows turn 2 price interrupt keeps stale `service_choice` in generated scenario instead of preserving active `time`.

- Minimal reproduction:
  1. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r63/scenarios.json` dialog 1 turn 3 and confirm expectation requires `pending_question_target=specialist` plus `booking_specialist_followup` trace;
  2. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r63/scenarios.json` dialog 1 turns 2-3 and confirm turn 2 price interrupt expects `reply_type=service_choice`, so active pending-question context drifts before turn 3 is normalized;
  3. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r63/trace_bundle.jsonl` dialog 1 turn 3 and confirm `llm_policy_core` emits `subject_kind=specialist`, `resolution_mode=referent_followup`, `pending_question_target=specialist`, but `question_contract` still records `blocked_by_info`, `specialist_hint` is `timeout/empty`, and final trace is only generic `booking_prompt`;
  4. inspect `scripts/booking_dialog_scenarios.py` plus `truffles-api/app/routers/webhook/decision.py` and confirm generic info interrupts do not normalize stale `service_choice` back to the active time contract, while specialist follow-up recovery still depends on resolved specialist preference surviving hint extraction.

- Evidence:
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r63/summary.json`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r63/brief.md`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r63/scenarios.json`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r63/responses.jsonl`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r63/trace_bundle.jsonl`
  - `truffles-api/app/routers/webhook/decision.py`

- Five Whys:
  1. Why did the turn fail: scenario expected specialist follow-up evidence, but runtime emitted only a generic booking prompt.
  2. Why did scenario expect the wrong active slot context: generic price/info interrupt preserved a stale `service_choice` override instead of the active `time` contract.
  3. Why did runtime still miss specialist follow-up evidence after correcting the active slot context: specialist preference was not recovered in time, so the collect owner never switched into `booking_specialist_followup`.
  4. Why was specialist preference not recovered: `question_contract` first marked the turn `blocked_by_info`, then `specialist_hint` timed out/returned empty.
  5. Why did that become a contract gap: the semantic owner had already kept `pending_question_target=specialist`, but runtime treated auxiliary hint failure as permission to collapse back to generic collect.

- Root cause statement:
  - `r63` combines two contract gaps: scenario-governance lets generic info interrupts keep stale `service_choice` instead of the active `time` question, and runtime specialist-followup ownership still depends on successful hint recovery; when hint extraction times out, the contract collapses to generic `booking_prompt` and loses specialist-followup evidence.

- Fix mechanism:
  - add deterministic scenario normalization so generic info interrupts preserve the active pending-question reply type instead of stale `service_choice`;
  - add bounded recovery so `pending_question_target=specialist` can keep `booking_specialist_followup` even when `specialist_hint` fails;
  - add targeted runtime regressions;
  - rerun guarded dev after deterministic/runtime closure.

## Release safety

- Strategy: bounded specialist-followup recovery only
- Go signals:
  - `pending_question_target=specialist` no longer degrades to generic `booking_prompt` on hint timeout
  - no live availability claim is introduced
- No-go signals:
  - new phrase-hardcode in core
  - `master info` or generic collect still wins for the same contract
- Rollback verification:
  - revert only the specialist-followup recovery delta and targeted regressions if it over-matches generic specialist info turns

## Invariant

1. Generic pricing/info interrupts still preserve the active pending time question.
2. Specialist follow-up remains a booking-state operation when the semantic contract says `specialist`.
3. No calendar availability claim is invented.

## Scope

- `truffles-api/app/routers/webhook/decision.py`
- `scripts/booking_dialog_scenarios.py`
- `truffles-api/tests/test_booking_dialog_scenarios_script.py`
- targeted runtime regressions
- tracker docs after rerun handoff

## Out of scope

- broad info-interrupt redesign
- acceptance/full proof closure
- transport/tool-evidence infra cleanup

## Touch-list

- `truffles-api/app/routers/webhook/decision.py`
- `scripts/booking_dialog_scenarios.py`
- `truffles-api/tests/test_booking_dialog_scenarios_script.py`
- `truffles-api/tests/test_message_endpoint.py`
- `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o53-specialist-followup-hint-timeout-blocked-by-info-a1.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`

## Plan (1..N)

1. Freeze `r63` as bounded runtime + scenario-governance evidence.
2. Normalize generic info interrupts so active pending-question reply type survives `price/info` turns.
3. Close fail-closed specialist-followup recovery when hint extraction times out.
4. Add targeted deterministic/runtime regressions.
5. Run focused checks and `git diff --check`.
6. Run one fresh guarded dev rerun beyond the `r63` family.

## DoD

1. Generic price/info interrupts preserve active `reply_type=time` instead of stale `service_choice`.
2. `pending_question_target=specialist` no longer degrades to generic `booking_prompt` on hint timeout under active time collect.
3. Focused deterministic/runtime regressions pass.
4. One fresh guarded rerun no longer stops on the `r63` family.

## Checks

- `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "price_interrupt or specialist_followup or orphan_pending_question"`
- `pytest -q truffles-api/tests/test_message_endpoint.py -k "specialist_followup or specialist_hint or bare_named_specialist"`
- `git diff --check`

## Execution status

- `done`

## Evidence

1. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r63/summary.json`
2. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r63/brief.md`
3. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r63/scenarios.json`
4. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r63/responses.jsonl`
5. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r63/trace_bundle.jsonl`
6. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r63/manual_audit.md`

## Rollback

1. Revert only the specialist-followup recovery delta and targeted regressions.
2. Keep `P1.6o6` blocked; do not reinterpret `r63` as closure evidence.

## No-go

- no phrase-hardcode for specialist names
- no oracle waiver for missing specialist-followup trace/meta
- no availability claims without tool evidence

## Risks/Blockers

1. `r63` is infra-invalid on unrelated tool-evidence checks, so it is architectural evidence only.
2. The next surfaced family may still be a deterministic governance drift after this runtime slice.

## Residual architecture debt (mandatory)

- Current residuals accepted in this block:
  - infra/tool-evidence red still makes guarded dev runs non-canonical for proof closure
- Why not in this block:
  - this slice only closes the specialist-followup recovery boundary
- Risk if deferred:
  - named specialist follow-ups after info interrupts will keep collapsing to generic collect when hint recovery is flaky
- Linked follow-up Task Package(s):
  - `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`
- Expiry/trigger to stop deferral:
  - next guarded rerun after bounded runtime closure

## Next-block contract (mandatory)

- Next block objective:
  - execute `P1.6o54` and run guarded `r65` so the next surfaced blocker is no longer the specialist-followup hint-timeout family from `r63`
- First deterministic check command:
  - `pytest -q truffles-api/tests/test_message_endpoint.py -k "specialist_followup or specialist_hint or bare_named_specialist" && git diff --check`
- Blocked-by conditions:
  - specialist-followup recovery regressions are not green
  - `r63` audit not reflected in tracker docs
  - fresh rerun preflight invalid
- Owner role for closure:
  - `Hands` for bounded runtime closure + rerun evidence, `Top Architect + Brain` for proof-bundle promotion

# TP-2026-03-11-p1.6o77-active-time-daypart-preference-info-signal-false-positive-a1

- Название/цель: закрыть surfaced `r106` blocker family, где после bounded `M25` closure fresh guarded `dev L2` больше не дает first-fail на initial requested-slot timeout, но under active `time` collect turn `Я предпочитаю утренние часы.` still keeps booking collect while runtime одновременно эмитит `hours` / `duration` info-class signals и strict contract падает в `info_section_miss`.

## Canon refs

- Parent verdict: `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o60a-remaining-closure-architecture-verdict-a1.md`
- Owner matrix: `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o60b-remaining-closure-owner-matrix-a1.md`
- Program TP: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Proof bundle: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`
- Prior child: `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o76-initial-timeout-requested-slot-owner-a1.md`
- Related bounded closures: `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o67-ambiguous-time-fill-scenario-governance-a1.md`, `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o69-time-window-slot-constraint-collect-owner-a1.md`, `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o76-initial-timeout-requested-slot-owner-a1.md`
- STATE: `STATE.md`
- CA: remaining closure row after `M25`

## One web search (mandatory before implementation)

- Query: `site:rasa.com/docs/rasa/forms slot filling user preference not info question`
- Date/time: `2026-03-11T20:41:00+05:00`
- Sources opened:
  - official Rasa docs: `https://rasa.com/docs/rasa/forms/`
- Source quality:
  - official product documentation
- Found reusable ideas:
  - while an active slot is being filled, a user preference utterance should stay attached to the active slot contract unless there is an explicit intent switch;
  - unrelated question/info handling should not leak into slot-fill bookkeeping when the turn is already consumed as slot input.
- Decision:
  - `reuse/integrate`
  - reuse the existing `question_contract` / `slot_constraint(time)` contract and integrate one bounded guard that suppresses false info-class signals on matched time-answer turns.
- Rejected options:
  - weakening oracle to ignore `info_section_miss` without fixing runtime meta;
  - phrase-fitting `утренние часы` in runtime core;
  - broad refactor of the whole info-class detector.

## Root cause (mandatory)

- Symptom:
  - fresh guarded `dev L2` run `p1.6o6-l2-dev-20260311-a1-r106` no longer stops on `M25`, but the next first strict fail appears in dialog `1`, turn `3`, user text `Я предпочитаю утренние часы.`
- Minimal reproduction:
  1. Inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r106/scenarios.json` dialog `1`, turns `1-3`.
  2. Confirm turn `1` opens booking for `маникюр` and turn `2` is a valid pricing/info interrupt.
  3. Confirm turn `3` expects only `reply_type=time` and no info sections.
  4. Inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r106/trace_bundle.jsonl` dialog `1`, turn `3` and confirm the turn is consumed as matched `datetime` answer (`answer_value=утром`) with booking collect fallback.
  5. Inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r106/responses.jsonl` turn `3` and confirm `signal_snapshot.info_class.intents=['duration','hours']`, `basic_info_message=true`, `info_tags=['hours']`, and strict evaluation fails with `info_section_miss` while judge passes.
- Evidence:
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r106/summary.json`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r106/brief.md`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r106/scenarios.json`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r106/responses.jsonl`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r106/trace_bundle.jsonl`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r106/manual_audit.md`
- Five Whys:
  1. Why did the row fail? Strict contract reported `info_section_miss` on a booking collect turn.
  2. Why did strict contract expect info sections? Runtime emitted `hours` / `duration` info-class signals and `basic_info_message=true`.
  3. Why were info-class signals emitted? Lexical info heuristics still fired on `утренние часы` even after the turn had already matched the active `time` slot.
  4. Why is this not oracle-only drift? Judge passes and the visible reply is acceptable, but runtime meta/trace claim an info-class interpretation that the product path did not actually take.
  5. Why is this a new bounded family? Adjacent time-answer rows already close slot grounding, timeout fallback, and slot-constraint ownership, but they never froze fail-closed separation between matched time-answer turns and hours-info signals.
- Root cause statement:
  - matched `time` answers under active booking collect still allow runtime info-class heuristics to leak `hours` / `duration` signals into final meta on turns like `Я предпочитаю утренние часы.`: `_detect_info_class_intents` plus lexicon anchors (`hours_keywords`, `info_anchor_groups.duration`) overfire on `час/часы`, so contract/oracle sees a phantom info request even though runtime stays on booking collect.
- Fix mechanism:
  - freeze matrix row `M26` for active-time daypart-preference slot-fill turns;
  - tighten `_detect_info_class_intents` / lexicon-anchor behavior so matched booking time-answer turns do not emit conflicting `hours` / `duration` info-class signals;
  - add targeted deterministic regressions around `booking -> pricing interrupt -> daypart preference answer`.

## Reuse-first decision (mandatory)

- Reuse:
  - existing `question_contract` matched-answer flow
  - existing `slot_constraint(time)` evidence path
  - existing strict response-guard contract
- Integrate:
  - one bounded guard between matched time-answer handling and info-class signal emission
- Do not build:
  - new info classifier subsystem
  - new booking/time router
  - oracle waiver for `info_section_miss`

## Execution profile

- TP mode: `implementation`
- Doc touch budget (files): `10`
- Code dominance: `required`
- Override token: `none`
- Why this profile fits: surfaced family is a bounded meta/trace false-positive on one active-time slot-fill row.

## Invariant

1. `P1.6o76/M25` remains closed.
2. Existing valid pricing/info interrupts under booking collect remain intact.
3. Matched `time` answers still preserve booking progress and resume contract.
4. No oracle weakening to hide false info tags.
5. No phrase hardcode in runtime core.

## Scope

- publish surfaced `r106` family as matrix row `M26`
- implement bounded suppression of false `hours` / `duration` info-class signals on matched active-time daypart-preference fills only

## Out of scope

- acceptance lock/replay
- pricing/info interrupt redesign
- general cleanup of all info-class heuristics
- infra cleanup for `CHATFLOW_BILLING_BLOCKED`, dedup latency, or run-level hardcode gate

## Touch-list

- `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o77-active-time-daypart-preference-info-signal-false-positive-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o60b-remaining-closure-owner-matrix-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o60a-remaining-closure-architecture-verdict-a1.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`
- `truffles-api/app/routers/webhook/info.py`
- `truffles-api/app/knowledge/generic/SYSTEM_LEXICONS.yaml`
- `truffles-api/tests/test_master_info_flow.py`
- `truffles-api/tests/test_booking_quality_response_guard.py`

## Plan (1..N)

1. Freeze `r106` as audited non-closure evidence and publish `M26`.
2. Trace the exact signal path that marks `hours` / `duration` on matched time-answer turns.
3. Implement a bounded guard that keeps matched active-time daypart-preference answers inside booking collect only.
4. Add targeted deterministic regressions plus one adjacent negative case for real hours info queries.
5. Run focused checks and `git diff --check`.
6. Authorize a fresh guarded `dev L2` only after deterministic green.

## DoD

1. `r106` is recorded in active docs as audited non-closure evidence.
2. `Я предпочитаю утренние часы.` under active `time` collect no longer emits false `hours` / `duration` info-class signals when the turn is consumed as booking collect.
3. Final meta/trace keep booking/time contract coherent and no longer trip `info_section_miss` on this row.
4. Real hours info questions still emit info-class signals.
5. Focused deterministic regressions are green.
6. Fresh guarded `dev L2` no longer stops on the `r106` family.

## Checks

- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r106 --status done --strict-artifacts`
- `pytest -q truffles-api/tests/test_master_info_flow.py -k "daypart and hours"`
- `pytest -q truffles-api/tests/test_booking_quality_response_guard.py -k "hours and time and booking"`
- `git diff --check`

## Execution status

- `closed_bounded`
- bounded info-signal fix is implemented in `truffles-api/app/routers/webhook/info.py` + `truffles-api/app/knowledge/generic/SYSTEM_LEXICONS.yaml`
- deterministic checks are green:
  - `pytest -q truffles-api/tests/test_master_info_flow.py -k "daypart and hours"` -> `3 passed`
  - `pytest -q truffles-api/tests/test_booking_quality_response_guard.py -k "hours and time and booking"` -> `2 passed`
  - `pytest -q truffles-api/tests/test_message_endpoint.py -k "booking_time_question_like_daypart_reply_keeps_partial_datetime_without_info_block or booking_time_question_like_daypart_exact_time_reply_advances_to_name_without_policy_core or llm_policy_core_question_like_daypart_constraint_preserves_slot_constraint_contract or llm_policy_core_declarative_daypart_constraint_preserves_slot_constraint_contract"` -> `4 passed`
- fresh guarded `dev L2` `p1.6o6-l2-dev-20260311-a1-r112` no longer stops on the `r106/M26` family and instead surfaces a different blocker at dialog `1`, turn `4`, user text `А сколько это будет стоить?`

## Evidence

1. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r106/summary.json`
2. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r106/brief.md`
3. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r106/scenarios.json`
4. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r106/responses.jsonl`
5. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r106/trace_bundle.jsonl`
6. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r106/manual_audit.md`
7. `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o76-initial-timeout-requested-slot-owner-a1.md`
8. `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o77-active-time-daypart-preference-info-signal-false-positive-a1.md`

## Rollback

1. Keep `P1.6o6` blocked.
2. Revert only the future bounded `M26` delta if it regresses adjacent booking time-answer or real hours-info rows.
3. Do not roll back prior bounded rows (`M25`, `M24`, `M18`).

## No-go

- no oracle weakening to ignore `info_section_miss`
- no phrase-hardcode branch for `утренние часы`
- no changes to unrelated requested-slot timeout or consult-topic-shift families
- no broad rewrite of info-class detection outside the bounded active-time row

## Risks / blockers

- `r106` remains non-closure evidence because `infra_valid=false`, `semantic_valid=false`, and the run still carries unrelated tool-evidence gaps.
- Judge already passes the turn, so the defect can look tempting to dismiss as oracle-only; doing that would hide a real runtime meta/trace contract inconsistency.
- The same lexical stems can appear in legitimate hours questions, so the bounded guard must preserve real info intent.

## Residual architecture debt (mandatory)

### Current residuals accepted in this block

- Info-class heuristics remain centralized and can still overfire outside this bounded row.
- Run-level hardcode gate red remains outside this bounded row.

### Why not in this block

- `M26` is specifically about false `hours` / `duration` signals on matched active-time daypart-preference fills. Broadening into all info heuristics would violate bounded-row closure.

### Risk if deferred

- Later guarded reruns can continue to fail strict contract on coherent booking turns because meta/trace advertise phantom info requests.

### Linked follow-up Task Package(s)

- next surfaced row after fresh guarded rerun, if any

### Expiry/trigger to stop deferral

- before the next guarded rerun after `M26`, if active-time daypart-preference turns still leak false info-class signals.

## Next-block contract (mandatory)

### Next block objective

- implement `M26`, then rerun focused deterministic booking/time-info separation checks before any new guarded `dev L2`.

### First deterministic check command

- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r106 --status done --strict-artifacts && git diff --check`

### Blocked-by conditions

- `M26` is not published in the owner matrix
- active-time daypart-preference false-info regressions are not green
- runtime still emits `hours` / `duration` info signals on matched booking time-answer turns

### Owner role for closure

- `Hands` for deterministic implementation/evidence, `Top Architect + Brain` for guarded-rerun authorization and closure

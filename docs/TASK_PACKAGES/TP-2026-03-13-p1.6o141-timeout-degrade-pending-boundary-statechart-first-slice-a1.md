# TP-2026-03-13-p1.6o141-timeout-degrade-pending-boundary-statechart-first-slice-a1

- Название/цель: после surfaced `P1.6o140` timeout/degrade drift вынести первый boundary-statechart slice для активного booking collect, чтобы timeout retry exhaustion не схлопывал time-answer turn в `pending/escalate`, когда у runtime уже есть валидный resume contract.

## Canon refs

- Binding roadmap: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o135-beauty-salon-canonical-control-plane-roadmap-a1.md`
- Fresh proof surface: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o140-post-p1.6o139-proof-lane-a1.md`
- Prior deterministic owner slice: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o139-active-time-generic-master-info-interrupt-owner-a1.md`
- Class-level routing: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o118-class-level-closure-process-reset-a1.md`
- Parent closure program: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Main remediation: `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- State baseline: `STATE.md`

## Git context

- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `origin/main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect` remove branch + worktree after merge

## One web search (mandatory before implementation)

- Query: `site:stately.ai/docs xstate guards context transitions state machine`
- Date/time: `2026-03-13T15:55:55+05:00`
- Sources opened:
  - official Stately/XState docs: `https://stately.ai/docs/guards`
  - official Stately/XState docs: `https://stately.ai/docs/context`
- Source quality:
  - official product documentation
- Found reusable ideas:
  - boundary lifecycle should be modeled through explicit guarded transitions + context, not branch-local ad hoc rewrites.
- Decision:
  - `integrate/build`
  - integrate the statechart discipline conceptually, but build the first slice in Python runtime boundary code rather than introducing a second semantic router or a frontend-only library dependency.
- Rejected options:
  - another scenario/oracle-only patch for a runtime timeout/degrade surface;
  - broad timeout threshold relaxation;
  - continuing giant-router local branches without explicit boundary model.

## Root cause (mandatory)

- Symptom:
  - fresh guarded rerun `p1.6o140-l2-dev-20260313-a1-r1` surfaced `expected_meta_mismatch` + `expected_trace_miss` at `dialog 1 / turn 5 / "Мне нужно на следующую неделю."`, where scenario expected booking progress to `reply_type=name`, but runtime ended in `state=pending` with `action=escalate`, `intent=policy_timeout_degrade`.
- Minimal reproduction:
  1. inspect `/tmp/booking_quality/p1.6o140-l2-dev-20260313-a1-r1/scenarios.json` for `dialog 1 / turn 5`;
  2. inspect `/tmp/booking_quality/p1.6o140-l2-dev-20260313-a1-r1/responses.jsonl` and `trace_bundle.jsonl` for message `LLM-QUAL-p1.6o140-l2-dev-20260313-a1-r1-001-05-c70b0c`;
  3. inspect timeout/degrade + pending escalation branches in `truffles-api/app/routers/webhook/decision.py` and related boundary helpers.
- Evidence:
  - strict audit: `artifact_integrity.valid=true`, `infra_valid=true`, `semantic_valid=false`, `run_integrity_reasons=[run_completion_gap]`, `oracle_arbitration.winner=contract`
  - failing turn has full `decision_meta` + `decision_trace` evidence despite non-canonical prefix
  - runtime final turn truth includes:
    - `conversation_state=pending`
    - `decision_meta.action=escalate`
    - `decision_meta.intent=policy_timeout_degrade`
    - `decision_meta.policy_core_degrade_reason=policy_error:deadline_exceeded`
    - `decision_meta.policy_core_timeout_retry_exhausted=true`
    - `interaction_owner=question_contract:policy_core_degraded_collect`
  - trace shows timeout/degrade branch setting degraded collect contract and then collapsing the turn into pending instead of preserving booking progression after the time answer.
- Five Whys:
  1. Why did scenario fail? Because runtime ended in pending/escalate instead of staying on booking collect continuity.
  2. Why did runtime end in pending/escalate? Because timeout retry exhaustion on the active collect boundary still routes through branch-local degrade/pending logic.
  3. Why is that structural? Because the boundary transition is still resolved by giant-router branches, not by one explicit lifecycle model.
  4. Why is this now admissible? Because compiler slice (`P1.6o137`) and owner slice (`P1.6o139`) are already complete, so frozen order now permits the first boundary-statechart slice.
  5. Why not treat it as pure infra noise? Because `infra_valid=true`, judge arbitration does not contradict contract evidence, and the failing turn itself is fully observable.
- Root cause statement:
  - timeout/degrade/pending transitions for active booking collect are still encoded as branch-heavy local logic, so retry exhaustion can incorrectly collapse a resumable booking turn into `pending` instead of preserving the explicit collect/resume boundary contract.
- Fix mechanism:
  - extract a first explicit boundary-statechart slice for `collect -> degraded_collect -> info_interrupt -> pending/resume` around active booking time collect, driven by guards + context and leaving semantic act ownership with `llm_policy_core`.

## Reuse-first decision (mandatory)

- Reuse:
  - existing timeout/degrade reason-codes and trace/meta contracts
  - existing `question_contract`, `session_memory.interaction_state`, and pending/handoff evidence paths
- Integrate:
  - first explicit boundary transition model for timeout/degrade/pending on active booking collect
- Build:
  - one bounded Python statechart slice; no new semantic engine

## Release safety

- Rollout: local bounded structural slice only; no deploy in this block
- Go signals:
  - active time-answer turns no longer collapse to `pending/escalate` solely because of timeout retry exhaustion when resumable collect context exists
  - `reason_code`, `decision_meta`, and `decision_trace` remain explicit
  - existing pending/handoff and info-interrupt families do not regress
- No-go signals:
  - semantic act ownership moves out of `llm_policy_core`
  - timeout/degrade gates are weakened instead of made explicit
  - statechart slice becomes a new semantic classifier
- Rollback:
  - revert only the bounded boundary-statechart slice if adjacent pending/handoff or timeout families regress

## Invariant

1. Semantic ownership remains with `llm_policy_core`.
2. No phrase-hardcode in core as a substitute for boundary modeling.
3. No oracle weakening.
4. Statechart scope is boundary lifecycle only: `collect`, `info_interrupt`, `degraded`, `pending`, `resume`.
5. No new proof rerun before deterministic closure of this slice.

## Scope

- surfaced `P1.6o140` family only
- first boundary-statechart slice for active booking collect timeout/degrade/pending continuity
- focused deterministic regressions
- docs/state/session sync for `P1.6o140` result and `P1.6o141` opening

## Out of scope

- global statechart rollout
- new contract compiler family
- multilingual/open-world claims
- acceptance lock/replay/full

## Touch-list

- `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o140-post-p1.6o139-proof-lane-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o141-timeout-degrade-pending-boundary-statechart-first-slice-a1.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `STATE.md`
- `docs/SESSIONS/SESSION-2026-02-19-llm-first-firebreak-a1.md`
- `docs/SESSION_INDEX.md`
- `truffles-api/app/routers/webhook/decision.py`
- boundary helper modules already participating in timeout/degrade/pending flow
- targeted tests under `truffles-api/tests/`

## Plan (1..N)

1. Sync `P1.6o140` truth and publish `P1.6o141` as the next admissible structural block.
2. Isolate the active booking collect timeout/degrade/pending transition points and define explicit guards + context inputs.
3. Implement the first boundary-statechart slice without moving semantic act classification out of `llm_policy_core`.
4. Lock the surfaced family and adjacent pending/handoff controls with targeted deterministic tests.
5. Re-run proof only after deterministic closure.

## DoD

1. `P1.6o140` is recorded truthfully as timeout/degrade pending boundary drift on active time-answer turn.
2. First boundary-statechart slice is explicit and bounded.
3. Active booking time-answer turn preserves resumable collect continuity instead of collapsing to pending on surfaced contour.
4. Adjacent pending/handoff/info-interrupt controls stay green.
5. Targeted deterministic checks and hygiene checks pass.
6. Next expensive step after deterministic closure is one fresh proof rerun.

## Checks

- targeted boundary/pending/timeout pytest set to be defined during implementation
- `git diff --check`
- `SESSION_AGENT=a1 scripts/session_check.sh`

## Evidence

1. `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o140-post-p1.6o139-proof-lane-a1.md`
2. `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o141-timeout-degrade-pending-boundary-statechart-first-slice-a1.md`
3. `/tmp/booking_quality/p1.6o140-l2-dev-20260313-a1-r1/summary.json`
4. `/tmp/booking_quality/p1.6o140-l2-dev-20260313-a1-r1/brief.md`
5. `/tmp/booking_quality/p1.6o140-l2-dev-20260313-a1-r1/failure_families.json`
6. `/tmp/booking_quality/p1.6o140-l2-dev-20260313-a1-r1/responses.jsonl`
7. `/tmp/booking_quality/p1.6o140-l2-dev-20260313-a1-r1/trace_bundle.jsonl`
8. `/tmp/booking_quality/p1.6o140-l2-dev-20260313-a1-r1/manual_audit.json`

## Rollback

1. Revert only the bounded boundary-statechart slice.
2. Keep `P1.6o140` truth in docs/state.
3. Do not reopen proof loop before deterministic closure.

## No-go

- no second proof rerun in this block
- no statechart expansion beyond surfaced boundary family
- no semantic-router duplication in statechart
- no threshold/gate weakening to hide timeout drift

## Risks / blockers

- runtime latency can still surface as infra pressure even after boundary fix
- pending/handoff continuity may have hidden shared branches outside the first extracted slice
- statechart extraction can accidentally duplicate existing guards if not bounded

## Residual architecture debt (mandatory)

### Current residuals accepted in this block

- broader executable owner coverage is still incomplete
- audit remains partially semantic outside covered families
- boundary-statechart extraction remains slice-based, not system-wide

### Why not in this block

- the surfaced family needs one bounded first slice, not a whole-system rewrite

### Risk if deferred

- proof will keep surfacing timeout/degrade pending drift and block movement toward proof bundle closure

### Linked follow-up Task Package(s)

- next proof-lane TP after deterministic closure of `P1.6o141`

### Expiry/trigger to stop deferral

- immediately after `P1.6o141` checks are green

## Next-block contract (mandatory)

### Next block objective

- run one fresh guarded proof rerun after `P1.6o141` and verify the surfaced timeout/degrade pending family moved off first fail.

### First deterministic check command

- `git diff --check && SESSION_AGENT=a1 scripts/session_check.sh`

### Blocked-by conditions

- `P1.6o141` deterministic checks are not green
- `P1.6o140` truth is not synced in active docs

### Owner role for closure

- `Top Architect + Brain`


## Execution status

- `done`

## Execution result

- Implemented the first bounded boundary-statechart slice for surfaced `P1.6o140` timeout/degrade drift:
  - `truffles-api/app/services/owner_resolver.py` adds explicit timeout boundary source `resume_contract`.
  - `truffles-api/app/routers/webhook/decision.py` now routes active `time` collect under `booking_interrupt` through `resume_contract -> boundary_state.resume_collect -> collect`, and stamps explicit `booking_resume_collect_boundary` meta instead of forcing `pending/policy_timeout_degrade` on retry exhaustion.
  - existing timeout info-interrupt branches such as `timeout_master_info_interrupt` remain in control for non-time slices.
- Targeted deterministic checks are green:
  - `python3 -m py_compile truffles-api/app/services/owner_resolver.py truffles-api/app/routers/webhook/decision.py truffles-api/tests/test_owner_resolver.py truffles-api/tests/test_message_endpoint.py truffles-api/tests/test_booking_quality_response_guard.py`
  - `pytest -q truffles-api/tests/test_owner_resolver.py -k "timeout_owner_boundary"`
  - `pytest -q truffles-api/tests/test_booking_quality_response_guard.py -k "timeout_owner_boundary_collect or timeout_resume_contract_boundary_collect"`
  - `pytest -q truffles-api/tests/test_message_endpoint.py -k "timeout_booking_interrupt_resume_boundary_bypasses_exhausted_limit or degraded_timeout_booking_safe_second_hit_escalates or timeout_master_info_interrupt_resumes_name_collect or degraded_timeout_matched_name_continuity_uses_owner_boundary or degraded_timeout_active_booking_name_fill_resumes_followup or pending_handoff_pricing_interrupt_preserves_time_followup or transport_degraded_pending_reentry_restores_booking_resume"`
  - `git diff --check`
  - `SESSION_AGENT=a1 scripts/session_check.sh`
- Published `P1.6o142` as the next admissible proof-only continuation after deterministic closure of this slice.

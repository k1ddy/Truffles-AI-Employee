# TP-2026-03-06-p6.3-open-world-typo-surface-coverage-a1

- Название/цель: расширить `P6` после `P6.2`: добавить генератору сценариев детерминированный `surface_noise_profile=typo`, чтобы тот же booking/info/handoff контракт воспроизводился под контролируемыми misspelling/ASR-like surface искажениями без LLM run и без runtime-core hardcode.
- Parent TP: `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- Canon refs: `AGENTS.md`, `STATE.md`, `STRUCTURE.md`, `STRATEGY/REQUIREMENTS.md`, `TECH.md`, `docs/runbooks/BOOKING_CONFIRM_VERIFY.md`, `docs/TASK_PACKAGES/TP-2026-02-19-llm-first-firebreak-program.md`
- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`

## One web search (mandatory before implementation)

- Query: `site:arxiv.org misspelling robustness language model`
- Time (UTC): `2026-03-06T13:13:23Z`
- Sources:
  - `arXiv: How Robust are LLMs to In-Context Misspellings? A Multilingual Study`
- Ready solutions found:
  - misspelling robustness should be checked with controlled perturbation families that preserve task meaning
  - multilingual evaluation should include typo/noisy surface variants instead of assuming clean orthography
- Decision (`reuse/integrate/build`): `reuse + integrate`
  - reuse the existing tag-aware surface mutation pipeline from `P6.1/P6.2`
  - integrate a deterministic typo profile instead of adding ad-hoc case-specific strings in tests
- Rejected options:
  - add typo heuristics to runtime-core
  - wait for a future expensive LLM lane to cover misspellings

## Root cause (mandatory)

- Symptom:
  - after `P6.1/P6.2`, the generator can vary language and script, but it still emits mostly clean orthography and therefore misses the typo/noisy input family required by the firebreak program.

- Minimal reproduction:
  1. inspect `scripts/booking_dialog_scenarios.py` for `surface_noise_profile` / typo mutation support
  2. `rg -n "typo|misspell|surface_noise" scripts/booking_dialog_scenarios.py truffles-api/tests/test_booking_dialog_scenarios_script.py`
  3. observe no deterministic typo mutation family exists

- Evidence:
  - `scripts/booking_dialog_scenarios.py`
    - current mutation axes cover language/script only
  - `docs/TASK_PACKAGES/TP-2026-02-19-llm-first-firebreak-program.md`
    - mandatory multilingual adversarial battery includes ASR/noise/typo-like perturbations

- Five Whys:
  1. Why does open-world proof remain incomplete: clean text still dominates generator output.
  2. Why is that risky: many real user turns arrive with minor misspellings or ASR-like noise.
  3. Why can existing tests miss those cases: they validate contracts on clean surface forms.
  4. Why should this live in the generator: the problem is missing evidence coverage, not missing core semantics.
  5. Why is the block atomic: controlled typo mutation can be added as one orthogonal post-processing layer.

- Root cause statement:
  - scenario generation still lacks a reusable, contract-preserving typo/noise mutation axis.

- Fix mechanism:
  - add `surface_noise_profile=clean|typo`
  - mutate a seed-stable subset of turns through tag-aware typo templates
  - prove `tags`/`expect` stability in deterministic tests

## Invariant

1. Typo mutation does not alter `tags` or `expect`.
2. Typo mutation is deterministic under the same seed.
3. Typo coverage stays in the generator/test layer, not runtime-core.

## Scope

- `scripts/booking_dialog_scenarios.py`
- `truffles-api/tests/test_booking_dialog_scenarios_script.py`
- `docs/runbooks/BOOKING_CONFIRM_VERIFY.md`
- `docs/TASK_PACKAGES/TP-2026-03-06-p6.3-open-world-typo-surface-coverage-a1.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`

## Out of scope

- new LLM quality runs
- runtime-core typo handling
- broader paraphrase generation beyond the controlled typo family

## Plan (1..N)

1. Add `surface_noise_profile` to the deterministic generator.
2. Implement tag-aware typo templates and seed-stable application.
3. Add deterministic tests for typo output and metadata.
4. Sync docs/evidence.

## DoD

1. Generator accepts `surface_noise_profile=typo`.
2. Typo output changes surface text while keeping `tags` and `expect`.
3. Deterministic typo tests are green.
4. `git diff --check` is clean.

## Checks

- `python3 scripts/booking_dialog_scenarios.py --count 2 --seed 42 --surface-noise-profile typo --output /tmp/booking_dialog_scenarios_typo.json`
- `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "typo"`
- `git diff --check`

## Execution status

- `done`

## Evidence

1. Code:
   - `scripts/booking_dialog_scenarios.py`
   - `truffles-api/tests/test_booking_dialog_scenarios_script.py`
   - `docs/runbooks/BOOKING_CONFIRM_VERIFY.md`
2. Commands:
   - `python3 scripts/booking_dialog_scenarios.py --count 2 --seed 42 --surface-noise-profile typo --output /tmp/booking_dialog_scenarios_typo.json`
   - `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "typo"` -> `1 passed`
   - `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py` -> `21 passed`
   - `git diff --check` -> `clean`

## Rollback

1. revert typo profile additions
2. rerun scenario-generator tests

## No-go

- no runtime-core typo routing
- no nondeterministic typo mutation
- no mutation that rewrites `tags` / `expect`

## Risks/Blockers

1. typo templates can become unrealistic if they over-distort slot-bearing text
2. phone/time values must remain readable enough for downstream evaluators

## Residual architecture debt (mandatory)

- Current residuals accepted in this block:
  - broader paraphrase and adversarial synonym families remain deferred
- Why not in this block:
  - this block is limited to typo/noisy surface only
- Risk if deferred:
  - open-world proof still under-covers semantic-preserving paraphrase drift
- Linked follow-up Task Package(s):
  - `docs/TASK_PACKAGES/TP-2026-03-06-p6.4-open-world-paraphrase-synonym-coverage-a1.md`
- Expiry/trigger to stop deferral:
  - before claiming complete `P6` closure

## Next-block contract (mandatory)

- Next block objective:
  - add broader paraphrase/adversarial synonym families on top of multilingual/script/noise coverage
- First deterministic check command:
  - `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "paraphrase or adversarial"`
- Blocked-by conditions:
  - no deterministic paraphrase family exists
  - no tests prove contract preservation under broader semantic-preserving rewording
- Owner role for closure:
  - `Top Architect + Brain`

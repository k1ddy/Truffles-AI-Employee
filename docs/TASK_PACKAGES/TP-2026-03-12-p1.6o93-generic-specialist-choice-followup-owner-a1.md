# TP-2026-03-12-p1.6o93-generic-specialist-choice-followup-owner-a1

- Название/цель: закрыть surfaced `M31`, где после proof admission `P1.6o92` generic specialist-choice question `Можно выбрать мастера?` под активным `expected_reply_type=time` уже проходит через успешный `llm_policy_core collect` с `subject_kind=specialist`, `resolution_mode=referent_followup`, `next_question=name`, но blank interaction axes позволяют runtime схлопнуть ход в generic `booking_info_contract/master` / `master_service_not_found` вместо явного specialist-choice follow-up owner.

## Canon refs

- Parent verdict: `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o60a-remaining-closure-architecture-verdict-a1.md`
- Closure program: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Main remediation: `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- Prior proof lane: `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o92-post-m19-reclosure-proof-lane-a1.md`
- Related bounded rows: `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o51-question-like-specialist-followup-collect-owner-a1.md`, `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o66-policy-core-named-specialist-followup-owner-a1.md`
- Owner matrix artifact: `truffles-api/app/knowledge/generic/INTERACTION_OWNER_MATRIX.yaml`
- State baseline: `STATE.md`

## Git context

- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect` remove branch + worktree after merge

## One web search (mandatory before implementation)

- Query: `site:rasa.com/docs/rasa/forms unhappy path requested slot interruptions official docs`
- Date/time: `2026-03-12T13:53:48+05:00`
- Sources opened:
  - official Rasa forms docs: `https://rasa.com/docs/rasa/forms/`
- Source quality:
  - official product documentation
- Found reusable ideas:
  - once the active slot owner already knows the pending slot, side questions and unhappy-path turns must preserve the slot-owner contract explicitly instead of falling back to a generic handler;
  - interruption handling should distinguish genuine FAQ/info turns from turns that continue the active slot-filling relation.
- Decision:
  - `reuse + integrate`
  - reuse the existing `booking_specialist_followup` owner, `referent_followup` relation, and owner-matrix runtime layer;
  - integrate a bounded success-path guard so generic specialist-choice turns cannot be post-hoc compressed into generic `master` info when semantic owner already emitted specialist-choice collect.
- Rejected options:
  - weakening scenario/oracle to accept generic `master` info as equivalent closure for this family;
  - adding phrase-hardcode in core for `выбрать мастера`;
  - changing the product canon back to pre-redesign `master info always wins` for every specialist-choice phrase.

## Root cause (mandatory)

- Symptom:
  - fresh guarded `dev L2` `p1.6o92-l2-dev-20260312-a1-r1` stops at dialog `1`, turn `6`, user text `Можно выбрать мастера?`
- Minimal reproduction:
  1. Inspect `/tmp/booking_quality/p1.6o92-l2-dev-20260312-a1-r1/scenarios.json` dialog `1`, turn `6`.
  2. Inspect `/tmp/booking_quality/p1.6o92-l2-dev-20260312-a1-r1/responses.jsonl` for `message_id=LLM-QUAL-p1.6o92-l2-dev-20260312-a1-r1-001-06-e16bde`.
  3. Inspect `/tmp/booking_quality/p1.6o92-l2-dev-20260312-a1-r1/trace_bundle.jsonl` for the same turn and confirm:
     - `llm_policy_core` succeeds with `subject_kind=specialist`, `resolution_mode=referent_followup`, `next_question=name`, `open_questions=["name"]`;
     - `pending_question_target` and `active_question_relation` stay blank on the successful semantic output;
     - runtime still emits `booking_interrupt info_reply`, `intent=master`, `source=booking_info_contract`, `abstain_reason=master_service_not_found`.
  4. Compare the surfaced turn against `P1.6o51` and `P1.6o66` to show it is adjacent to, but not identical with, the earlier named-specialist families.
- Evidence:
  - `/tmp/booking_quality/p1.6o92-l2-dev-20260312-a1-r1/summary.json`
  - `/tmp/booking_quality/p1.6o92-l2-dev-20260312-a1-r1/brief.md`
  - `/tmp/booking_quality/p1.6o92-l2-dev-20260312-a1-r1/scenarios.json`
  - `/tmp/booking_quality/p1.6o92-l2-dev-20260312-a1-r1/responses.jsonl`
  - `/tmp/booking_quality/p1.6o92-l2-dev-20260312-a1-r1/trace_bundle.jsonl`
  - `/tmp/booking_quality/p1.6o92-l2-dev-20260312-a1-r1/manual_audit.md`
- Five Whys:
  1. Why did the turn fail? Runtime answered with generic `booking_info_contract/master` while scenario and judge both rejected the missing specialist-choice follow-up contract.
  2. Why did scenario expect specialist-choice follow-up? Because active `time` collect remained open and `llm_policy_core` itself emitted a successful specialist-choice collect plan (`subject_kind=specialist`, `resolution_mode=referent_followup`, `next_question=name`).
  3. Why did runtime still route to generic `master` info? Because the successful semantic output left `pending_question_target` / `active_question_relation` blank, so collect-vs-info arbitration still let the generic `master` interrupt owner win.
  4. Why is this not already closed by `P1.6o51` or `P1.6o66`? `P1.6o51` bounded question-like named-specialist follow-up, and `P1.6o66` bounded declarative named-specialist success-path ownership; `r117` surfaces the generic specialist-choice variant without a concrete named referent.
  5. Why is this a new row instead of another `P1.6o9` replay? Because under the executable interaction model the policy-core success plan now explicitly chooses specialist-followup semantics, so generic `master` info is a forbidden post-hoc compression, not the semantic owner.
- Root cause statement:
  - generic specialist-choice under active `time` collect is still missing a first-class executable row: policy-core success semantics already move the turn into specialist-choice follow-up, but runtime arbitration can still compress that row into generic `master` info because the success-path interaction axes are incomplete.
- Fix mechanism:
  - freeze new row `M31` in the machine-readable owner matrix;
  - preserve `pending_question_target=specialist`, `active_question_relation=referent_followup`, and `pending_question_owner=booking_specialist_followup` when policy-core success output emits specialist-choice collect semantics under active `time` collect;
  - add bounded runtime/evidence regressions so generic master-truth `M5` stays intact while `M31` stops collapsing into `booking_info_contract/master`.

## Reuse-first decision (mandatory)

- Reuse:
  - existing `booking_specialist_followup` owner
  - existing `referent_followup` relation
  - existing owner-matrix artifact + contract test
  - existing choose-specialist info-intent coverage from `P1.6o9`
- Integrate:
  - success-path interaction-axis preservation in runtime / resolver
  - targeted runtime + response-guard regressions
- Do not build:
  - new phrase router
  - new generic interrupt owner

## Execution profile

- TP mode: `implementation`
- Doc touch budget (files): `8`
- Code dominance: `required`
- Override token: `none`
- Why this profile fits: surfaced family is a bounded success-path owner gap with a new frozen matrix row and clear adjacent negatives.

## Release safety

- Strategy: bounded semantic-owner preservation only
- Go signals:
  - generic specialist-choice under active `time` collect preserves explicit specialist-followup owner evidence
  - response acknowledges specialist choice without making unsupported live-availability claims
  - adjacent rows `M5`, `M15`, and `M19` remain green
- No-go signals:
  - generic `booking_info_contract/master` / `master_service_not_found` still wins for the same semantic contract
  - any phrase-hardcode branch is added to runtime core
  - named-specialist or generic master-truth rows regress
- Rollback verification:
  - revert only the bounded `M31` owner-preservation delta and targeted regressions if adjacent master/specialist families regress

## Invariant

1. Generic master-truth/info turns from `M5` remain `booking_info_contract/master`.
2. Named-specialist follow-up rows `M4`, `M15`, and `M19` must not regress.
3. No live specialist availability claim is introduced without tool evidence.
4. Exactly one fresh guarded `dev L2` runs after deterministic closure is green.

## Scope

- publish surfaced row `M31`
- implement bounded runtime owner preservation for generic specialist-choice success path under active `time` collect
- add deterministic runtime/evidence regressions

## Out of scope

- global prompt/model-routing redesign
- acceptance `lock/replay/full`
- generic master-info response redesign outside the bounded `M31` row

## Touch-list

- `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o93-generic-specialist-choice-followup-owner-a1.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `truffles-api/app/knowledge/generic/INTERACTION_OWNER_MATRIX.yaml`
- `truffles-api/app/routers/webhook/decision.py`
- `truffles-api/tests/test_message_endpoint.py`
- `truffles-api/tests/test_booking_quality_response_guard.py`
- `truffles-api/tests/test_interaction_owner_matrix_contract.py`
- `STATE.md`

## Plan (1..N)

1. Freeze `r117` as audited non-closure evidence and publish `M31`.
2. Preserve specialist-choice interaction axes on the policy-core success path under active `time` collect.
3. Block generic `master` info compression for `M31` while keeping `M5` intact.
4. Add targeted deterministic/runtime regressions with adjacent negative controls.
5. Run focused checks and `git diff --check`.
6. Run one fresh guarded `dev L2` only after deterministic closure is green.

## DoD

1. `r117` is recorded in active docs and the machine-readable owner matrix as `M31`.
2. Generic specialist-choice question under active `time` collect no longer loses specialist-followup owner evidence on the success path.
3. Final meta/trace preserve `pending_question_target=specialist`, `pending_question_interaction=booking_specialist_followup`, `pending_question_owner=booking_specialist_followup`, `active_question_relation=referent_followup`, and `expected_reply_type=time`.
4. Focused deterministic runtime/evidence regressions are green.
5. Fresh guarded `dev L2` no longer stops on `Можно выбрать мастера?`.

## Expensive run control (mandatory)

- Hypothesis:
  - after bounded `M31` owner preservation, a fresh guarded `dev L2` will no longer stop on dialog `1`, turn `6`, `Можно выбрать мастера?`
- Expected measurable effect:
  - the next guarded run preserves specialist-followup owner evidence for that turn and either proceeds beyond it or surfaces a different first blocker family
- Stop condition:
  - run exactly one fresh guarded `dev L2` plus strict audit; if the same success-path owner gap persists or the run becomes preflight-invalid, stop the expensive loop and reopen RCA instead of chaining another rerun

## Checks

- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o92-l2-dev-20260312-a1-r1 --status done --strict-artifacts`
- `pytest -q truffles-api/tests/test_message_endpoint.py -k "choose_specialist_question_rewrites_collect_to_master_info or booking_specialist_followup_persists_preference or generic_specialist_choice_followup"`
- `pytest -q truffles-api/tests/test_booking_quality_response_guard.py -k "specialist_followup or choose_specialist"`
- `pytest -q truffles-api/tests/test_interaction_owner_matrix_contract.py`
- `git diff --check`

## Execution status

- `done`

## Evidence

1. `/tmp/booking_quality/p1.6o92-l2-dev-20260312-a1-r1/summary.json`
2. `/tmp/booking_quality/p1.6o92-l2-dev-20260312-a1-r1/brief.md`
3. `/tmp/booking_quality/p1.6o92-l2-dev-20260312-a1-r1/scenarios.json`
4. `/tmp/booking_quality/p1.6o92-l2-dev-20260312-a1-r1/responses.jsonl`
5. `/tmp/booking_quality/p1.6o92-l2-dev-20260312-a1-r1/trace_bundle.jsonl`
6. `/tmp/booking_quality/p1.6o92-l2-dev-20260312-a1-r1/manual_audit.md`
7. `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o92-l2-dev-20260312-a1-r1 --status done --strict-artifacts`
8. `pytest -q truffles-api/tests/test_message_endpoint.py -k "generic_specialist_choice_followup or booking_specialist_followup_persists_preference or named_master_question_rewrites_collect_to_master_info or bare_named_specialist_question_rewrites_collect_to_master_info"` -> `5 passed`
9. `pytest -q truffles-api/tests/test_booking_quality_response_guard.py -k "generic_specialist_choice_followup or specialist_followup"` -> `5 passed`
10. `python3 -m py_compile truffles-api/app/routers/webhook/decision.py truffles-api/tests/test_message_endpoint.py truffles-api/tests/test_booking_quality_response_guard.py`
11. `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o94-post-m31-proof-lane-a1.md`

## Rollback

1. Keep `P1.6o92` proof truth intact.
2. Revert only the bounded `M31` owner-preservation delta if adjacent rows regress.
3. Do not mutate the matrix to hide surfaced evidence.

## No-go

- no phrase fitting for `выбрать мастера`
- no generic `master` info waiver when semantic owner already emitted specialist-choice collect semantics
- no second expensive run before deterministic closure is green

## Risks / blockers

- `M31` may require prompt/schema tightening in addition to runtime owner preservation because current success output leaves the interaction axes blank
- adjacent generic `master` info rows can regress if the `M31` boundary is made too broad
- fresh proof remains non-canonical until infra/tool-evidence red is separately closed

## Residual architecture debt (mandatory)

### Current residuals accepted in this block

- closure proof remains on `dev L2`; `lock/replay/full` stay blocked
- specialist-choice semantics still depend on `decision.py`, not a fully migrated owner handler

### Why not in this block

- this block only closes surfaced row `M31`

### Risk if deferred

- generic specialist-choice turns keep exposing semantic-owner/runtime-owner disagreement under active booking collect

### Linked follow-up Task Package(s)

- `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o94-post-m31-proof-lane-a1.md`
- `P1.7` acceptance replay gate after the proof lane is green enough to advance

### Expiry/trigger to stop deferral

- before any next guarded `lock/replay/full`

## Next-block contract (mandatory)

### Next block objective

- run `P1.6o94` so fresh guarded proof either confirms `M31` is gone or truthfully surfaces the next first-fail family

### First deterministic check command

- `pytest -q truffles-api/tests/test_message_endpoint.py -k "generic_specialist_choice_followup or booking_specialist_followup_persists_preference or named_master_question_rewrites_collect_to_master_info or bare_named_specialist_question_rewrites_collect_to_master_info" && pytest -q truffles-api/tests/test_booking_quality_response_guard.py -k "generic_specialist_choice_followup or specialist_followup" && pytest -q truffles-api/tests/test_interaction_owner_matrix_contract.py && git diff --check`

### Blocked-by conditions

- `M31` is not synced truthfully into active docs and the owner matrix
- generic specialist-choice deterministic reproduction is not green
- live runtime fingerprint for the post-`M31` proof lane is not proven against current `HEAD`

### Owner role for closure

- `Hands` for bounded implementation, `Top Architect + Brain` for proof-lane admission and closure signoff

# TP-2026-03-09-p1.6o56-standalone-named-specialist-booking-normalization-a1.md

- Название/цель: закрыть surfaced `r66` blocker family, где generated scenario трактует standalone named-specialist opener `Можно к мастеру Асем?` как generic `master` info request, тогда как runtime и judge последовательно считают этот ход booking collect с missing service и ожидают `service_choice`.
- Parent TP: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Canon refs: `AGENTS.md`, `STATE.md`, `STRUCTURE.md`, `STRATEGY/REQUIREMENTS.md`, `docs/runbooks/BOOKING_CONFIRM_VERIFY.md`, `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o55-timeout-pending-slot-question-guidance-evidence-contract-a1.md`
- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect remove worktree + branch after merge`

## One web search (mandatory before implementation)

- Query: `Rasa forms missing required slot after user specifies preference official docs`
- Time (UTC): `2026-03-09T11:08:00Z`
- Sources:
  - `https://rasa.com/docs/rasa/forms/`
- Ready solutions found:
  - when a user provides a preference but not the required slot, form behavior should keep slot-filling ownership and ask for the missing required slot rather than switching to a separate info-answer contract;
  - deterministic layer should preserve the active slot contract, not overfit on a surface label.
- Decision (`reuse/integrate/build`): `reuse + integrate`
  - reuse existing booking collect expectation with missing `service_choice`;
  - integrate one deterministic tag normalization for standalone named-specialist booking openers;
  - build no new runtime phrase branch.
- Rejected options:
  - weakening `info_section_miss` for `master` tags;
  - teaching runtime to emit fake `master` info for standalone booking requests;
  - changing product contract for missing-service booking intake.

## Root cause (mandatory)

- Symptom:
  - guarded `dev L2` run `p1.6o6-l2-dev-20260309-a1-r66` stops on dialog 2 turn 3 with `expected_info_section_miss` / `info_section_miss` for `Можно к мастеру Асем?`.

- Minimal reproduction:
  1. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r66/scenarios.json` dialog 2 turn 3 and confirm generated scenario keeps tag `master` with `info_sections=[master,specialist]` and no booking `reply_type=service_choice`;
  2. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r66/responses.jsonl` dialog 2 turn 3 and confirm runtime returns booking collect (`На какую услугу хотите записаться? После этого сразу проверю свободное время.`) with `expected_reply_type=service_choice`;
  3. inspect `scripts/booking_dialog_scenarios.py` and confirm standalone `master` turns get info expectations from `EXPECT_INFO_SECTIONS`, while the only `master -> booking` normalization currently requires active `reply_type=time`.

- Evidence:
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r66/summary.json`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r66/brief.md`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r66/scenarios.json`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r66/responses.jsonl`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r66/trace_bundle.jsonl`
  - `scripts/booking_dialog_scenarios.py`

- Five Whys:
  1. Why did the turn fail: scenario expected `master` info sections, but runtime asked for missing service.
  2. Why did scenario expect info sections: tag `master` always merges `EXPECT_INFO_SECTIONS[master]` by default.
  3. Why was the tag not normalized to booking: existing `master -> booking` normalization only runs under active `reply_type=time`.
  4. Why is this a contract gap: standalone named-specialist booking openers express preference plus booking intent, not a generic specialist-info request.
  5. Why is this deterministic rather than runtime: runtime and judge already agree on booking collect; only scenario governance is overclaiming info semantics.

- Root cause statement:
  - scenario taxonomy has no standalone normalization for named-specialist booking openers, so `master` surfaces without active booking context keep generic info expectations even when the utterance actually expresses booking intent plus missing service.

- Fix mechanism:
  - add deterministic normalization that rewrites standalone named-specialist booking openers from `master` to booking collect expectations with `reply_type=service_choice`;
  - add targeted scenario regressions;
  - rerun guarded dev beyond `r66`.

## Release safety

- Strategy: deterministic scenario-governance normalization only
- Go signals:
  - standalone named-specialist booking opener no longer expects `master` info sections
  - active time/name specialist follow-up behavior remains unchanged
- No-go signals:
  - generic specialist info questions start getting rewritten into booking collect
  - runtime code changes become necessary for this family
- Rollback verification:
  - revert only the new taxonomy normalization and its tests if generic specialist info questions over-normalize

## Invariant

1. Active `time/name` specialist follow-up families remain under their existing bounded contracts.
2. Generic specialist info questions still expect `master` info sections.
3. No runtime phrase-hardcode is introduced.

## Scope

- `scripts/booking_dialog_scenarios.py`
- `truffles-api/tests/test_booking_dialog_scenarios_script.py`
- tracker docs after rerun handoff

## Out of scope

- runtime changes
- info-oracle redesign
- acceptance/full proof closure

## Touch-list

- `scripts/booking_dialog_scenarios.py`
- `truffles-api/tests/test_booking_dialog_scenarios_script.py`
- `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o56-standalone-named-specialist-booking-normalization-a1.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`

## Plan (1..N)

1. Freeze `r66` as deterministic scenario-governance evidence.
2. Normalize standalone named-specialist booking openers to missing-service booking collect.
3. Add targeted scenario regressions.
4. Run focused checks and `git diff --check`.
5. Run one fresh guarded dev rerun beyond `r66`.

## DoD

1. `Можно к мастеру Асем?`-type standalone turns no longer expect `master` info sections.
2. Deterministic regressions pass.
3. One fresh guarded rerun no longer stops on the `r66` family.

## Checks

- `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "standalone_named_specialist or master or orphan_pending_question"`
- `git diff --check`

## Execution status

- `done`

## Evidence

1. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r66/summary.json`
2. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r66/brief.md`
3. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r66/scenarios.json`
4. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r66/responses.jsonl`
5. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r66/trace_bundle.jsonl`
6. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r66/manual_audit.md`

## Rollback

1. Revert only the standalone named-specialist normalization and targeted regressions.
2. Keep `P1.6o6` blocked; do not reinterpret `r66` as closure evidence.

## No-go

- no runtime changes for this family
- no oracle waiver for missing `master` info sections
- no phrase list for specific names

## Risks/Blockers

1. `r66` remains non-canonical proof evidence because unrelated infra/tool-evidence issues still make the run invalid for proof.
2. The next surfaced family may still be another deterministic governance drift.

## Residual architecture debt (mandatory)

- Current residuals accepted in this block:
  - standalone specialist booking normalization is still scenario-side, not a generalized scenario ontology refactor
- Why not in this block:
  - this slice only closes the surfaced deterministic family
- Risk if deferred:
  - generated runs keep stopping on scenario-side overclaims instead of revealing the next runtime blocker
- Linked follow-up Task Package(s):
  - `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`
- Expiry/trigger to stop deferral:
  - next guarded rerun after bounded deterministic closure

## Next-block contract (mandatory)

- Next block objective:
  - execute `P1.6o57` and run guarded `r68` so the next surfaced blocker is no longer the standalone named-specialist booking normalization family from `r66`
- First deterministic check command:
  - `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "standalone_named_specialist or master or orphan_pending_question" && git diff --check`
- Blocked-by conditions:
  - deterministic scenario regressions are not green
  - `r66` audit not reflected in tracker docs
  - fresh rerun preflight invalid
- Owner role for closure:
  - `Hands` for deterministic closure + rerun evidence, `Top Architect + Brain` for proof-bundle promotion

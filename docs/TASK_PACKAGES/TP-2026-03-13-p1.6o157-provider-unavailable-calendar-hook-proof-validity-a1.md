# TP-2026-03-13-p1.6o157-provider-unavailable-calendar-hook-proof-validity-a1

- Название/цель: после truthfully non-canonical `P1.6o156` закрыть bounded proof-validity blocker, в котором strict `tool_evidence` делает rerun `infra_invalid` по `calendar_hook_missing` на observed `provider_unavailable` calendar-failure turn, хотя auto-hook policy по дизайну не отправляет calendar hook на failure outcomes.

## Canon refs

- Parent closure program: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Main remediation: `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- Process reset / class triage: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o118-class-level-closure-process-reset-a1.md`
- Prior proof lane: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o156-post-p1.6o155-proof-lane-a1.md`
- Prior structural slice: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o155-collect-slot-order-partial-date-slot-constraint-continuity-a1.md`
- State baseline: `STATE.md`

## Git context

- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `origin/main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect` remove branch + worktree after merge

## One web search (mandatory before implementation)

- Query: `site:developers.openai.com/api/docs/guides/trace-grading trace grading tool calls reasoning steps handoffs`
- Date/time: `2026-03-13T19:30:50+05:00`
- Sources opened:
  - OpenAI Trace grading guide: `https://developers.openai.com/api/docs/guides/trace-grading`
- Source quality:
  - official vendor documentation
- Found reusable ideas:
  - trace grading should evaluate observed tool calls, reasoning steps, and handoffs rather than only a final output;
  - eval logic should bind judgments to the executed trajectory so targeted improvements are possible.
- Decision:
  - `reuse`
  - reuse the trace-grading principle for proof validity: strict hook requirements must bind to observed hook-eligible calendar trajectory, not to every calendar failure signal regardless of whether auto-hook policy would execute.
- Rejected options:
  - weakening strict tool-evidence globally;
  - admitting a new semantic child from `P1.6o156` while the executed failing turn is infra-contaminated;
  - treating `provider_unavailable` calendar failures as hook-required by fiat when the auto-hook sender explicitly suppresses them.

## Root cause (mandatory)

- Symptom:
  - `P1.6o156` stopped at `max_failures_reached:1` and strict audit marked the run `infra_valid=false` via `tool_evidence:calendar_hook_missing` on `dialog 1 / turn 11 / "Можно на 19:00?"`, where runtime observed `tool_action=calendar.book_slot` with `tool_decision=provider_unavailable`.
- Minimal reproduction:
  1. Open `/tmp/booking_quality/p1.6o156-l2-dev-20260313-a1-r1/summary.json` and confirm `tool_evidence.reasons == ["calendar_hook_missing"]` with `calendar_intent_turns=1`, `calendar_tool_events=1`, and `calendar_hook_events=0`.
  2. Open `/tmp/booking_quality/p1.6o156-l2-dev-20260313-a1-r1/responses.jsonl` and confirm the failing turn records `intent=calendar.book_slot`, `tool_decision=provider_unavailable`, and `tool_verifier=pre_execute`.
  3. Inspect `_llm_quality_should_send_calendar_hook()` in `ops/diagnose.py` and confirm it returns `False` for calendar outcomes outside `{success,pending}`.
  4. Inspect `_llm_quality_build_tool_evidence_status()` in `ops/diagnose.py` and confirm it still requires `calendar_hook_missing` whenever calendar opportunity is observed, even if no hook-eligible calendar turn existed.
- Evidence:
  - `/tmp/booking_quality/p1.6o156-l2-dev-20260313-a1-r1/summary.json`
  - `/tmp/booking_quality/p1.6o156-l2-dev-20260313-a1-r1/responses.jsonl`
  - `/tmp/booking_quality/p1.6o156-l2-dev-20260313-a1-r1/manual_audit.json`
  - `ops/diagnose.py`
  - `truffles-api/tests/test_booking_quality_tool_evidence_gate.py`
- Five Whys:
  1. Why is `P1.6o156` non-canonical? Because strict audit marked `infra_valid=false`.
  2. Why did infra validity fail? Because `tool_evidence` required a calendar hook that was never sent.
  3. Why was no calendar hook sent? Because auto-hook logic suppresses calendar hooks for failure outcomes like `provider_unavailable`.
  4. Why did strict evidence still require the hook? Because hook requirement is keyed to broad calendar opportunity, not to observed hook-eligible opportunity.
  5. Why does that matter architecturally? Because proof routing gets blocked by an internal eval-tooling mismatch instead of a true runtime/preflight blocker, so the worktree cannot truthfully decide whether the surfaced family is semantic or merely proof-contaminated.
- Root cause statement:
  - strict tool-evidence currently treats every observed calendar opportunity as hook-required, while the auto-hook sender intentionally skips failure outcomes; this makes `provider_unavailable` calendar turns fail proof validity through a self-contradictory gate.
- Fix mechanism:
  - record observed hook-eligible calendar opportunities in coverage stats using the same predicate as the auto-hook sender;
  - require `calendar_hook_missing` only when at least one hook-eligible calendar opportunity existed in the executed prefix;
  - add regression tests for `provider_unavailable`/no-hook validity and for hook-eligible missing-hook invalidity.

## Reuse-first decision (mandatory)

- Reuse:
  - existing `_llm_quality_should_send_calendar_hook()` predicate
  - existing `_llm_quality_build_tool_evidence_status()` gate
  - existing tool-evidence gate regression suite in `truffles-api/tests/test_booking_quality_tool_evidence_gate.py`
- Integrate:
  - wire hook-eligibility accounting into existing coverage stats instead of introducing a new proof framework
- Build:
  - no new dependency or framework

## Invariant

1. Strict tool-evidence remains strict for observed hook-eligible calendar turns.
2. `P1.6o156` remains truthfully non-canonical proof evidence; no semantic child is admitted from it.
3. No baseline/acceptance artifact is updated from `P1.6o156`.
4. The fix stays inside proof tooling and does not touch runtime semantic routing.

## Scope

- `ops/diagnose.py`
- `truffles-api/tests/test_booking_quality_tool_evidence_gate.py`
- truth sync docs for `P1.6o156` / `P1.6o157`

## Out of scope

- semantic fix for `dialog 1 / turn 11 / "Можно на 19:00?"`
- runtime/provider remediation for actual `provider_unavailable`
- acceptance replay/full or baseline updates

## Touch-list

- `ops/diagnose.py`
- `truffles-api/tests/test_booking_quality_tool_evidence_gate.py`
- `STATE.md`
- `docs/SESSIONS/SESSION-2026-02-19-llm-first-firebreak-a1.md`
- `docs/SESSION_INDEX.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o156-post-p1.6o155-proof-lane-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o157-provider-unavailable-calendar-hook-proof-validity-a1.md`

## Plan

1. Freeze `P1.6o156` truth as a non-canonical proof-validity blocker and publish `P1.6o157`.
2. Add bounded hook-eligibility accounting using the same predicate that drives auto calendar-hook sending.
3. Refine strict tool-evidence so `calendar_hook_missing` is required only when a hook-eligible calendar opportunity was actually observed.
4. Add regression tests for provider-unavailable failure and hook-eligible missing-hook cases.
5. Run targeted deterministic checks and sync truth.
6. Publish the next admissible proof rerun only after deterministic closure of this tooling slice.

## DoD

1. Observed `provider_unavailable` calendar failures without hook-eligible auto-hook path no longer become `infra_valid=false` only because `calendar_hook_missing` fired.
2. Observed hook-eligible calendar turns without a hook still fail strict tool-evidence.
3. Targeted proof-tooling checks are green.
4. Active docs/state/session reflect that `P1.6o156` was a proof-validity blocker and `P1.6o157` is the selected continuation.

## Checks

- `python3 -m py_compile ops/diagnose.py truffles-api/tests/test_booking_quality_tool_evidence_gate.py`
- `pytest -q truffles-api/tests/test_booking_quality_tool_evidence_gate.py`
- `git diff --check`
- `SESSION_AGENT=a1 scripts/session_check.sh`

## Evidence

1. `/tmp/booking_quality/p1.6o156-l2-dev-20260313-a1-r1/summary.json`
2. `/tmp/booking_quality/p1.6o156-l2-dev-20260313-a1-r1/responses.jsonl`
3. `/tmp/booking_quality/p1.6o156-l2-dev-20260313-a1-r1/manual_audit.json`
4. `ops/diagnose.py`
5. `truffles-api/tests/test_booking_quality_tool_evidence_gate.py`

## Rollback

1. Revert the bounded hook-eligibility accounting and its tests.
2. Keep `P1.6o156` truth frozen as non-canonical.
3. Do not reinterpret `P1.6o156` as admissible semantic proof.

## No-go

- no semantic child TP from `P1.6o156`
- no weakening of strict tool-evidence once a hook-eligible calendar opportunity is observed
- no runtime semantic patch inside this proof-validity slice
- no new expensive rerun before deterministic closure

## Risks / blockers

- over-relaxing hook requirements would weaken strict proof evidence for real calendar hook opportunities
- under-scoping the fix would leave future fail-fast reruns infra-invalid on the same provider-unavailable contour
- even after proof validity is restored, the next rerun may still surface a real semantic/runtime family

## Residual architecture debt (mandatory)

### Current residuals accepted in this block

- surfaced turn `dialog 1 / turn 11` remains semantically unclassified until proof validity is restored
- proof bundle closure is still incomplete
- runtime/provider availability can still make future proof runs non-canonical for real infra reasons

### Why not in this block

- this block is limited to proof admissibility, not semantic/runtime remediation

### Risk if deferred

- the team will keep opening or suppressing semantic families from proof runs that are invalid only because the hook gate contradicts the hook sender

### Linked follow-up Task Package(s)

- next proof-lane TP after deterministic closure of `P1.6o157`

### Expiry/trigger to stop deferral

- before any new `dev/acceptance` proof rerun after `P1.6o156`

## Next-block contract (mandatory)

### Next block objective

- publish and run one fresh proof-lane rerun after deterministic closure of `P1.6o157`, then truthfully decide whether the surfaced `dialog 1 / turn 11` family remains first fail on admissible proof evidence.

### First deterministic check command

- `python3 -m py_compile ops/diagnose.py truffles-api/tests/test_booking_quality_tool_evidence_gate.py && pytest -q truffles-api/tests/test_booking_quality_tool_evidence_gate.py && git diff --check`

### Blocked-by conditions

- `P1.6o156` non-canonical proof truth is not synced in active docs/state/session
- targeted `P1.6o157` tool-evidence gate tests are not green
- the team attempts another expensive rerun before deterministic closure of this tooling slice

### Owner role for closure

- `Top Architect + Brain`

## Execution status

- `done`

## Execution result

- `ops/diagnose.py` now records observed hook-eligible calendar opportunities with the same predicate as `_llm_quality_should_send_calendar_hook(...)` and only requires `calendar_hook_missing` when such a hook-eligible calendar turn actually appeared in the executed prefix.
- Provider-unavailable calendar failures without hook-eligible auto-hook path no longer become `infra_valid=false` just because the hook sender intentionally suppressed the hook, while hook-eligible calendar turns still keep strict hook requirements intact.
- Deterministic validation is green:
  - `python3 -m py_compile ops/diagnose.py truffles-api/tests/test_booking_quality_tool_evidence_gate.py`
  - `pytest -q truffles-api/tests/test_booking_quality_tool_evidence_gate.py`
  - `git diff --check`
  - `SESSION_AGENT=a1 scripts/session_check.sh`
- Published `P1.6o158` as the next admissible proof continuation.

# TP-2026-03-08-p1.6o16-booking-time-guidance-question-owner-a1

- Название/цель: закрыть новый `P1.6o6` blocker, где booking-active вопрос про guidance по времени (`На какое время лучше записаться?`) считается обычным missing-slot collect, повторяет generic `booking_prompt`, не даёт содержательного guidance и снова приводит к `booking_slot_stall`.
- Parent TP: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Canon refs: `AGENTS.md`, `STATE.md`, `STRUCTURE.md`, `STRATEGY/REQUIREMENTS.md`, `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`, `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o1-semantic-envelope-question-model-a1.md`, `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o12-daypart-partial-datetime-parity-a1.md`, `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o14-policy-validation-degraded-collect-progress-contract-a1.md`
- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`

## One web search (mandatory before implementation)

- Query: `site:rasa.com/docs forms unhappy path requested slot user asks a question`
- Time (UTC): `2026-03-08T03:55:00Z`
- Sources:
  - `https://rasa.com/docs/rasa/forms/`
- Ready solutions found:
  - while a slot-collection loop is active, question-like turns that do not fill the requested slot must be handled as explicit unhappy paths rather than blindly re-asking the same slot;
  - the requested slot should remain active after the interrupt is answered, instead of losing booking continuity.
- Decision (`reuse/integrate/build`): `reuse + integrate`
  - reuse the existing `expected_reply_type=time` booking continuity contract and integrate one explicit unhappy-path owner for question-like time-guidance turns;
  - do not build a new side router or weaken the `booking_slot_stall` oracle.
- Rejected options:
  - weakening `booking_slot_stall` in quality oracles;
  - treating the turn as resolved time-slot progress without actual datetime evidence;
  - adding a phrase-hardcoded `лучше записаться` branch in `decision.py`.

## Root cause (mandatory)

- Symptom:
  - fresh guarded `dev L2` rerun `p1.6o6-l2-dev-20260308-a1-r11` stopped with `semantic_valid=false`, `stop_reason=max_failures_reached:1`
  - top failure: `booking_slot_stall`, `dialog_id=1`, `turn_index=5`, message `На какое время лучше записаться?`
  - the turn happens under active booking with `service=Маникюр`, `expected_reply_type=time`

- Minimal reproduction:
  1. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r11/summary.json`
  2. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r11/brief.md`
  3. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r11/responses.jsonl` for `message_id=LLM-QUAL-p1.6o6-l2-dev-20260308-a1-r11-001-05-b3aa7f`
  4. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r11/trace_bundle.jsonl` for the same `message_id`
  5. inspect `truffles-api/app/routers/webhook/decision.py` around policy-core collect handling and `truffles-api/app/routers/webhook/booking.py` around booking prompt repetition / info interrupt handling

- Evidence:
  - policy-core semantic envelope is internally consistent: `intent=booking`, `action=collect`, `tool_action=collect`, `subject_kind=service`, `capability=bookability`, `resolution_mode=clarify_missing_time`, `entity_refs=[svc:manicure]`
  - referent resolution succeeds: `resolved_referent=Маникюр`, `referent_source=policy_service_query`
  - runtime still chooses generic `booking_prompt`, `missing_slot=datetime`, `expected_reply_reason=booking_prompt`
  - bot text on turn 5 is only `На какую дату и время вам удобно?`, so guarded quality classifies the turn as `booking_flow_break / booking_slot_stall`
  - same run is also `infra_valid=false` because transport degraded with `CHATFLOW_BILLING_BLOCKED`, so `r11` is not admissible closure evidence even though the semantic stall is still visible in the trace bundle

- Five Whys:
  1. Why did guarded quality fail: the system repeated the same generic time prompt instead of handling the user's question about the slot.
  2. Why did the system repeat the generic prompt: policy-core/runtime treated the turn as plain missing-slot collect with `clarify_missing_time`.
  3. Why was there no better branch: current unhappy-path handling around active `expected_reply_type=time` recognizes slot answers and some info interrupts, but not question-like time-guidance requests.
  4. Why is that architecturally incomplete: `P1.6o1` introduced the question model, but runtime still lacks a contract for "question about the missing slot" versus "answer filling the missing slot" under booking-active continuity.
  5. Why did the proof run also become infra-invalid: the stalled dialog then hit provider transport degradation, so the run stopped early and cannot serve as canonical proof even though the semantic defect is already exposed.

- Root cause statement:
  - booking-active question-like turns about the currently requested `time` slot have no canonical semantic owner path: they are neither recognized as slot progress nor as a bounded unhappy-path guidance interrupt, so runtime falls back to repeating the generic `booking_prompt` and produces `booking_slot_stall`.

- Fix mechanism:
  - add one explicit unhappy-path contract for question-like time-guidance turns under active booking / `expected_reply_type=time`;
  - answer with grounded schedule guidance (or bounded clarify/info guidance) while preserving `expected_reply_type=time` continuity;
  - prove it with exact message-endpoint and quality-guard regressions before the next guarded rerun.

## Invariant

1. No oracle weakening for `booking_slot_stall`.
2. No phrase-hardcode routing in runtime core.
3. No fake datetime progress without actual slot evidence.
4. Booking continuity must remain active after the guidance answer.
5. Existing `P1.6o8` style-reference/media behavior must not regress.

## Scope

- `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o16-booking-time-guidance-question-owner-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `truffles-api/app/routers/webhook/decision.py`
- `truffles-api/app/routers/webhook/booking.py`
- `truffles-api/app/services/expected_reply_contract.py`
- `prompts/llm_policy_core.md`
- `truffles-api/tests/test_message_endpoint.py`
- `truffles-api/tests/test_booking_quality_response_guard.py`

## Out of scope

- full appointment recommendation engine
- live calendar suggestion generation
- unrelated transport/provider infra remediation
- new acceptance `lock/replay`

## Touch-list

- `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o16-booking-time-guidance-question-owner-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `truffles-api/app/routers/webhook/decision.py`
- `truffles-api/app/routers/webhook/booking.py`
- `truffles-api/app/services/expected_reply_contract.py`
- `prompts/llm_policy_core.md`
- `truffles-api/tests/test_message_endpoint.py`
- `truffles-api/tests/test_booking_quality_response_guard.py`

## Plan (1..N)

1. Freeze `r11` evidence and prove the exact unhappy-path gap on the turn `На какое время лучше записаться?`.
2. Define the minimal semantic contract for question-like time-guidance under active booking.
3. Implement the runtime/prompt change without weakening slot-progress or info-interrupt guards.
4. Add exact message-endpoint and quality-guard regressions.
5. Re-run deterministic contour, then return to one fresh guarded `dev L2`.

## DoD

1. Booking-active question-like time-guidance turns no longer fall into generic `booking_prompt` stall.
2. The runtime returns grounded guidance or bounded clarify guidance and keeps `expected_reply_type=time` continuity.
3. Exact deterministic regressions are green.
4. A fresh guarded `dev L2` no longer fails on `dialog_id=1`, `turn_index=5`.

## Checks

- `pytest -q truffles-api/tests/test_message_endpoint.py::test_llm_policy_core_time_guidance_question_under_expected_time_keeps_progress_contract`
- `pytest -q truffles-api/tests/test_booking_quality_response_guard.py::test_evaluate_turn_allows_booking_time_guidance_reply_without_slot_progress_stall`
- `pytest -q truffles-api/tests/test_message_endpoint.py`
- `pytest -q truffles-api/tests/test_booking_quality_response_guard.py`
- `git diff --check`

## Execution status

- `in_progress`

## Evidence

1. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r11/summary.json`
2. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r11/brief.md`
3. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r11/responses.jsonl`
4. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r11/trace_bundle.jsonl`
5. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r11/manual_audit.md`

## Rollback

1. Revert only the guidance owner change if it steals real slot answers or regresses booking continuity.
2. Keep `P1.6o6` blocked until a fresh guarded rerun is green and audited.

## No-go

- no weakening of `booking_slot_stall`
- no transport/env workaround presented as semantic closure
- no generic phrase branch for `лучше записаться`

## Risks/Blockers

1. A too-broad guidance detector may hijack real slot answers into info/unhappy-path replies.
2. A too-weak guidance reply may still fail quality as another `booking_slot_stall`.
3. Transport degradation in `r11` means the next guarded rerun also requires stable infra to become admissible proof.

## Residual architecture debt (mandatory)

- Current residuals accepted in this block:
  - `P1.6o6` proof bundle remains open until one fresh guarded rerun is green and audited.
- Why not in this block:
  - this block only closes the new time-guidance unhappy-path family surfaced by `r11`.
- Risk if deferred:
  - closure proof remains blocked by a still-open booking-active semantic family.
- Linked follow-up Task Package(s):
  - `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`
- Expiry/trigger to stop deferral:
  - before any new guarded `lock/replay` attempt.

## Next-block contract (mandatory)

- Next block objective:
  - implement the time-guidance unhappy-path contract, rerun deterministic contour, then return to guarded `dev L2`
- First deterministic check command:
  - `pytest -q truffles-api/tests/test_message_endpoint.py::test_llm_policy_core_time_guidance_question_under_expected_time_keeps_progress_contract && pytest -q truffles-api/tests/test_booking_quality_response_guard.py::test_evaluate_turn_allows_booking_time_guidance_reply_without_slot_progress_stall`
- Blocked-by conditions:
  - exact regressions are not green
  - runtime `:18184` is not restarted from the latest worktree state before the next guarded rerun
  - guarded runtime transport remains infra-invalid
- Owner role for closure:
  - `Hands` for code/tests/evidence, `Top Architect + Brain` for proof-bundle closure decision

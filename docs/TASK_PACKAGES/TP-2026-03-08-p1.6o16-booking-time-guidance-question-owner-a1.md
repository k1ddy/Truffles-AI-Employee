# TP-2026-03-08-p1.6o16-pending-question-interaction-model-gap-a1

- Название/цель: doc-first rescope surfaced `r11` blocker from one booking time-guidance phrasing (`На какое время лучше записаться?`) into the broader pending-question interaction-model gap. Freeze canon/oracle vocabulary before any new runtime code or expensive `dev L2`.
- Note: file path keeps the original `booking-time-guidance` name for continuity, but the TP scope is broader and binding.
- Parent TP: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Canon refs: `AGENTS.md`, `STATE.md`, `STRUCTURE.md`, `STRATEGY/REQUIREMENTS.md`, `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`, `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`, `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`, `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o1-semantic-envelope-question-model-a1.md`, `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o2-canonical-dialog-state-referent-state-a1.md`, `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o3-referent-resolver-capability-arbitration-a1.md`, `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o5-oracle-acceptance-contract-realignment-a1.md`, `docs/TASK_PACKAGES/TP-2026-03-07-p1.6m-booking-expected-reply-alternate-slot-capture-a1.md`, `docs/runbooks/BOOKING_CONFIRM_VERIFY.md`
- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`

## One web search (mandatory before implementation)

- Query: `site:rasa.com/docs forms unhappy path requested slot user asks a question`
- Time (UTC): `2026-03-08T03:55:00Z`
- Sources:
  - `https://rasa.com/docs/rasa/forms/`
- Ready solutions found:
  - when a slot-collection loop is active, question-like turns that do not fill the requested slot should be handled as explicit unhappy paths instead of blindly re-asking the same slot;
  - after the side-question is handled, the requested slot should remain active instead of losing booking continuity.
- Decision (`reuse/integrate/build`): `reuse + integrate`
  - reuse the existing `expected_reply` / requested-slot continuity contract;
  - integrate explicit pending-question interaction semantics into the canon/oracle before any new runtime owner code;
  - do not build a new side router and do not weaken `booking_slot_stall`.
- Rejected options:
  - weakening `booking_slot_stall` in quality oracles;
  - treating the turn as resolved time-slot progress without actual datetime evidence;
  - adding a phrase-hardcoded `лучше записаться` branch in runtime core.

## Root cause (mandatory)

- Symptom:
  - guarded rerun `p1.6o6-l2-dev-20260308-a1-r11` stopped with `semantic_valid=false`, `infra_valid=false`, `stop_reason=max_failures_reached:1`
  - top semantic failure: `booking_slot_stall`, `dialog_id=1`, `turn_index=5`, message `На какое время лучше записаться?`
  - the turn happens under active booking with grounded service context and `expected_reply_type=time`

- Minimal reproduction:
  1. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r11/summary.json`
  2. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r11/brief.md`
  3. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r11/responses.jsonl` for `message_id=LLM-QUAL-p1.6o6-l2-dev-20260308-a1-r11-001-05-b3aa7f`
  4. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r11/trace_bundle.jsonl` for the same `message_id`
  5. inspect `prompts/llm_policy_core.md`, `truffles-api/app/routers/webhook/context_manager.py`, and `docs/runbooks/BOOKING_CONFIRM_VERIFY.md` for the current `pending_question_contract` / oracle vocabulary

- Evidence:
  - policy-core already preserves `aboutness`: `intent=booking`, `action=collect`, `subject_kind=service`, `capability=bookability`, `resolution_mode=clarify_missing_time`, grounded `entity_refs`
  - canonical state already preserves the active requested slot, but `pending_question_contract` does not encode the interaction act the user is performing relative to that slot
  - oracle/runbook/taxonomy were still expecting slot progress or generic interrupt, with no explicit class for `question about the requested slot`
  - runtime therefore collapses the turn into ordinary `booking_prompt`, and guarded quality correctly reports `booking_slot_stall`
  - the same run is `infra_valid=false` because transport degraded with `CHATFLOW_BILLING_BLOCKED`, so `r11` is valid RCA evidence but not closure evidence

- Five Whys:
  1. Why did guarded quality fail: the system repeated generic collect guidance instead of handling the user's question about the requested slot.
  2. Why did runtime repeat generic collect guidance: it had no contract-level distinction between `fill the slot` and `ask about the slot`.
  3. Why was that distinction missing: `P1.6o1`..`P1.6o15` froze `aboutness/state/referent/capability/oracle`, but not the interaction semantics over an active pending question.
  4. Why was the gap not caught earlier: scenario/open-world expansions mutated surface forms inside the existing ontology, so they could not surface an act family absent from docs/runbook/taxonomy.
  5. Why did the process detect it only post factum: the guarded proof bundle was acting as a truthfulness gate, but the canon was not yet a completeness gate for collect unhappy-path interaction classes.

- Root cause statement:
  - the system models `what is this about?`, but not `what is the user trying to do with the current pending question state?`; because that interaction axis is missing from canon/oracle/runtime, multiple live collect unhappy-path turns collapse into the same generic `booking_prompt` outcome.

- Fix mechanism:
  - freeze the missing pending-question interaction vocabulary in canon/docs/runbook;
  - define explicit child slices for runbook/taxonomy alignment, bounded runtime ownership, and guarded rerun gating;
  - only then implement the first bounded runtime owner for `ask_about_requested_slot(time)` without weakening any oracle.

## Binding rescope

- `На какое время лучше записаться?` is the first surfaced evidence sample, not the scope boundary.
- This TP does not authorize a phrase-level runtime fix.
- No new expensive `dev L2` / `lock` / `replay` is admissible until the docs below are synchronized on the same interaction-act vocabulary.

## Interaction taxonomy (binding)

- `fill_requested_slot`: user directly provides the missing slot value.
- `ask_about_requested_slot`: user asks for advice, clarification, or guidance about the currently requested slot before filling it.
- `slot_constraint`: user narrows the requested slot (`утром`, `после обеда`, `не позже шести`) without fully filling it.
- `slot_compare`: user compares slot options (`утром или вечером`, `сегодня или завтра`) relative to the active requested slot.
- `mixed_fill_plus_question`: user partially answers the slot and adds a side-question or comparison in the same turn.

## Atomic child-block matrix (binding)

| Block | Objective | Status | Closure mode |
|---|---|---|---|
| `P1.6o16a Canon + master TP sync` | freeze the interaction-model gap in requirements/master TP/program TP/this TP so `P1.6o16` is no longer framed as a one-phrase bug | `in_progress` | docs-only sync |
| `P1.6o16b Runbook/oracle/taxonomy sync` | align runbook and scenario/oracle vocabulary around the pending-question interaction classes and resume contract | `queued` | docs/taxonomy contract |
| `P1.6o16c Runtime owner for ask_about_requested_slot(time)` | implement the first bounded runtime owner for the surfaced `time` family while preserving `expected_reply_type=time` continuity | `queued` | deterministic closure |
| `P1.6o16d Family matrix + guarded rerun gate` | prove the family matrix on deterministic contour, then run one fresh guarded `dev L2` (`r12`) before any `lock/replay` | `queued` | audited rerun |

## Invariant

1. No oracle weakening for `booking_slot_stall`.
2. No phrase/regex hardcode in runtime core as a semantic owner.
3. No fake slot progress without actual slot evidence.
4. Resume contract for the active requested slot must remain explicit.
5. No new expensive `dev L2` before docs/runbook sync is complete.

## Scope

- `STRATEGY/REQUIREMENTS.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o16-booking-time-guidance-question-owner-a1.md`
- `docs/runbooks/BOOKING_CONFIRM_VERIFY.md`

## Out of scope

- runtime code changes
- prompt/schema changes
- `scripts/booking_dialog_scenarios.py` implementation delta
- deterministic runtime test additions
- new guarded `dev L2` / `lock` / `replay`
- unrelated transport/provider remediation

## Touch-list

- `STRATEGY/REQUIREMENTS.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o16-booking-time-guidance-question-owner-a1.md`
- `docs/runbooks/BOOKING_CONFIRM_VERIFY.md`

## Plan (1..N)

1. Freeze the broader interaction-model gap in canon and active master/program TPs.
2. Freeze the pending-question interaction vocabulary and the rule that `r11` is evidence of a family, not a phrase.
3. Align proof-bundle/runbook language so `booking_slot_stall` is evaluated against explicit resume-contract evidence, not only wording.
4. Make the next runtime child slice explicit and block new expensive runs until that bounded slice is defined.

## DoD

1. `P1.6o16` is documented as a pending-question interaction-model gap, not as a one-phrase time-guidance fix.
2. The interaction vocabulary (`fill_requested_slot`, `ask_about_requested_slot`, `slot_constraint`, `slot_compare`, `mixed_fill_plus_question`) is frozen in canon/TPs/runbook.
3. The proof-bundle TP and runbook state that `booking_slot_stall` is invalid only when neither slot progress nor explicit pending-question interaction evidence exists.
4. The next bounded runtime child slice is explicit.
5. No new expensive `dev L2` is allowed before this doc sync is complete.

## Checks

- `rg -n "Pending-question interaction contract|fill_requested_slot|ask_about_requested_slot|slot_constraint|slot_compare|mixed_fill_plus_question" STRATEGY/REQUIREMENTS.md docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md docs/TASK_PACKAGES/TP-2026-03-08-p1.6o16-booking-time-guidance-question-owner-a1.md docs/runbooks/BOOKING_CONFIRM_VERIFY.md`
- `git diff --check`

## Execution status

- `in_progress (doc-first rescope)`
- requirements/master TP/program TP/runbook are being synchronized in the current worktree
- runtime child slices `P1.6o16c` / `P1.6o16d` have not started yet

## Evidence

1. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r11/summary.json`
2. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r11/brief.md`
3. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r11/responses.jsonl`
4. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r11/trace_bundle.jsonl`
5. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r11/manual_audit.md`
6. `STRATEGY/REQUIREMENTS.md`
7. `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
8. `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
9. `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`
10. `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o16-booking-time-guidance-question-owner-a1.md`
11. `docs/runbooks/BOOKING_CONFIRM_VERIFY.md`

## Rollback

1. Revert only the doc-first rescope if Top Architect / Brain reject the new interaction taxonomy.
2. Keep `P1.6o6` blocked until an approved replacement canon exists; do not fallback to phrase-level runtime fixes.

## No-go

- no weakening of `booking_slot_stall`
- no phrase-level semantic routing in runtime core
- no using `r11` as closure evidence
- no runtime child start before canon/runbook sync is complete
- no new expensive `dev L2` while the doc-first contract is still drifting

## Risks/Blockers

1. The doc-first rescope may still miss another pending-question interaction class; if so, the canon must grow before runtime code.
2. Scenario/oracle taxonomy can still drift if `P1.6o16b` is skipped and the team jumps directly into runtime code.
3. Transport degradation remains a separate blocker for admissible guarded reruns even after the semantic contract is fixed.

## Residual architecture debt (mandatory)

- Current residuals accepted in this block:
  - bounded runtime owner implementation for `ask_about_requested_slot(time)` is not done yet
  - family-matrix deterministic proof and guarded `r12` remain open
- Why not in this block:
  - this phase is intentionally docs-first to close the process/model gap before more code
- Risk if deferred:
  - the team can keep shipping reactive one-family fixes without ever closing the ontology gap
- Linked follow-up Task Package(s):
  - `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`
  - this TP child slices `P1.6o16c` and `P1.6o16d`
- Expiry/trigger to stop deferral:
  - before any fresh guarded `dev L2` for `P1.6o6`

## Next-block contract (mandatory)

- Next block objective:
  - start the bounded runtime child slice for `ask_about_requested_slot(time)` on top of the synced doc/runbook contract, preserve `expected_reply_type=time` resume continuity, and only then reopen the guarded `dev L2` path
- First deterministic check command:
  - `rg -n "Pending-question interaction contract|fill_requested_slot|ask_about_requested_slot|slot_constraint|slot_compare|mixed_fill_plus_question" STRATEGY/REQUIREMENTS.md docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md docs/TASK_PACKAGES/TP-2026-03-08-p1.6o16-booking-time-guidance-question-owner-a1.md docs/runbooks/BOOKING_CONFIRM_VERIFY.md && git diff --check`
- Blocked-by conditions:
  - requirements / TP / runbook are not synchronized on the new interaction-act vocabulary
  - the bounded runtime child slice is not frozen in this TP / parent TP
  - runtime `http://127.0.0.1:18184` is not restarted from the post-doc worktree state before the next guarded window
  - guarded runtime transport remains infra-invalid
- Owner role for closure:
  - `Hands` for the bounded runtime child slice and evidence, `Top Architect + Brain` for closure gating

# TP-2026-03-14-p1.6o178-post-p1.6o177-proof-lane-a1

- Название/цель: выполнить один свежий guarded `dev L2` rerun после детерминированного закрытия `P1.6o177`, чтобы доказать, что surfaced `confirm_candidate_missing` proof-validity blocker ушёл и `P1.6o176` снова стал admissible для truth routing следующего класса.

## Canon refs

- Binding roadmap: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o135-beauty-salon-canonical-control-plane-roadmap-a1.md`
- Prior deterministic closure: `docs/TASK_PACKAGES/TP-2026-03-14-p1.6o177-confirm-evidence-candidate-proof-validity-a1.md`
- Prior proof lane: `docs/TASK_PACKAGES/TP-2026-03-14-p1.6o176-post-p1.6o175-proof-lane-a1.md`
- Class-level routing: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o118-class-level-closure-process-reset-a1.md`
- Parent closure program: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Main remediation: `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- State baseline: `STATE.md`

## Git context

- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `origin/main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect` remove branch + worktree after merge

## One web search (mandatory before implementation)

- Not applicable in this block: proof-only continuation, no implementation.
- Decision:
  - `reuse`
  - reuse the existing guarded `dev L2` proof contour and strict artifact audit to determine whether the covered `P1.6o177` proof-validity slice moved off the blocker path.

## Root cause (mandatory)

- Symptom:
  - `P1.6o177` can close deterministically, but there is still no fresh proof showing the covered `confirm_candidate_missing` blocker is gone from the next guarded rerun.
- Minimal reproduction:
  1. finish `P1.6o177` deterministic closure;
  2. verify `/admin/version` on `:18184` matches current `HEAD`;
  3. execute one fresh guarded `dev L2` rerun with explicit `--quality-lane dev` and `--allow-pending-previous` because the previous audited lock run is intentionally non-canonical;
  4. strict-audit the run and determine whether class routing is now admissible.
- Evidence:
  - `docs/TASK_PACKAGES/TP-2026-03-14-p1.6o177-confirm-evidence-candidate-proof-validity-a1.md`
  - `/tmp/booking_quality/p1.6o176-l2-dev-20260314-a1-r1/summary.json`
  - `/tmp/booking_quality/p1.6o176-l2-dev-20260314-a1-r1/manual_audit.json`
- Five Whys:
  1. Why is another proof block needed? Because deterministic closure alone does not prove the expensive lane moved past the invalid proof-validity blocker.
  2. Why not route the semantic family from `P1.6o176` now? Because `infra_valid=false` made that class routing inadmissible.
  3. Why reuse the same live proof contour? Because parity drift would make the next result non-comparable.
  4. Why `--allow-pending-previous`? Because the latest lock-mode run remains intentionally `incomplete` after fail-fast proof, even though manual audit is done and truthfully frozen.
  5. Why one run only? Because this block is proof/classification, not another expensive loop.
- Root cause statement:
  - after `P1.6o177`, the only missing evidence is one fresh strict-audited proof rerun that shows whether `confirm_candidate_missing` is gone and whether the next surfaced family is now admissible for routing.
- Fix mechanism:
  - verify runtime parity, run one guarded `dev L2`, strict-audit it, and select the next continuation only from the admissible result.

## Reuse-first decision (mandatory)

- Reuse:
  - existing `ops/diagnose.py llm-quality` proof-lane contour
  - existing strict audit command
  - live runtime on `:18184`
- Integrate:
  - no code changes in this block
- Build:
  - none

## Release safety

- Rollout: none; proof-only block
- Go signal:
  - `/admin/version.git_commit == HEAD`
  - strict audit completes for the fresh rerun
- No-go signal:
  - runtime parity mismatch
  - preflight invalid
  - strict audit missing or artifact bundle incomplete
- Rollback:
  - no runtime/code rollback in this block; only stop after audit and publish truthful routing

## Invariant

1. `P1.6o177` deterministic closure remains intact.
2. No new runtime/oracle patch is allowed before the rerun result is audited.
3. Proof stays on a fingerprint-verified runtime on `:18184`.
4. Evidence remains contract-first and class-routed.

## Scope

- runtime parity check on `:18184`
- one fresh guarded `dev L2`
- strict audit of the run
- admissibility/class routing from the surfaced result
- truthful docs/state/session sync

## Out of scope

- new structural runtime/oracle changes before proof result
- lock/replay/full acceptance bundle
- baseline updates
- scenario regeneration changes beyond the single run

## Touch-list

- `docs/TASK_PACKAGES/TP-2026-03-14-p1.6o177-confirm-evidence-candidate-proof-validity-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-14-p1.6o178-post-p1.6o177-proof-lane-a1.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `STATE.md`
- `docs/SESSIONS/SESSION-2026-02-19-llm-first-firebreak-a1.md`
- `docs/SESSION_INDEX.md`
- `/tmp/booking_quality/p1.6o178-l2-dev-20260314-a1-r1/**`

## Plan

1. Verify runtime parity on `:18184`.
2. Execute exactly one fresh guarded `dev L2` rerun with `--allow-pending-previous`.
3. Strict-audit the run.
4. Determine whether semantic class routing is now admissible.
5. Sync truth and publish the next admissible continuation.

## DoD

1. A fresh guarded rerun exists under `/tmp/booking_quality/p1.6o178-l2-dev-20260314-a1-r1`.
2. Strict audit records `artifact_integrity.valid=true`.
3. `P1.6o177` is proven off the `confirm_candidate_missing` blocker path or a new blocker/class is truthfully documented.
4. Active docs/state/session show `P1.6o178` as the proof checkpoint after `P1.6o177`.

## Checks

- `python3 - <<'PY'
import json, subprocess, urllib.request
head = subprocess.check_output(['git', 'rev-parse', 'HEAD'], text=True).strip()
with urllib.request.urlopen('http://127.0.0.1:18184/admin/version', timeout=10) as r:
    runtime = json.load(r).get('git_commit')
print({'head': head, 'runtime': runtime, 'match': head == runtime})
PY`
- `scripts/llm_quality_guarded.sh --mode lock --run-id p1.6o178-l2-dev-20260314-a1-r1 --allow-pending-previous -- --base-url http://127.0.0.1:18184 --client-slug demo_salon ... --quality-lane dev`
- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o178-l2-dev-20260314-a1-r1 --status done --strict-artifacts`
- `git diff --check`

## Evidence

1. `/tmp/booking_quality/p1.6o178-l2-dev-20260314-a1-r1/summary.json`
2. `/tmp/booking_quality/p1.6o178-l2-dev-20260314-a1-r1/brief.md`
3. `/tmp/booking_quality/p1.6o178-l2-dev-20260314-a1-r1/scenarios.json`
4. `/tmp/booking_quality/p1.6o178-l2-dev-20260314-a1-r1/responses.jsonl`
5. `/tmp/booking_quality/p1.6o178-l2-dev-20260314-a1-r1/trace_bundle.jsonl`
6. `/tmp/booking_quality/p1.6o178-l2-dev-20260314-a1-r1/manual_audit.json`

## Rollback

1. No code rollback in this proof-only block.
2. If parity or audit is invalid, stop and keep `P1.6o177` as the active deterministic closure with no new semantic class routing.

## No-go

- no structural patching before strict audit
- no baseline update from this run
- no acceptance claims from a `dev` rerun

## Risks / blockers

- another proof-validity blocker may still keep the rerun infra-invalid
- runtime instability can still keep the run non-canonical for class routing
- next admissible family may move into a different slice once infra validity is restored

## Residual architecture debt (mandatory)

### Current residuals accepted in this block

- proof remains non-canonical until a later full closure bundle exists
- proof-validity and class-routing admission still converge slice by slice, not from a single generated contract source

### Why not in this block

- this block is proof-only and exists to verify the blocker removal

### Risk if deferred

- the worktree could drift into unsanctioned new fixes without proof that `P1.6o177` actually restored admissible rerun classification

### Linked follow-up Task Package(s)

- `docs/TASK_PACKAGES/TP-2026-03-14-p1.6o179-timeout-completed-booking-name-fill-continuity-a1.md`

### Expiry/trigger to stop deferral

- immediately after `P1.6o178` finishes and the next blocker/family is known

## Next-block contract (mandatory)

### Next block objective

- close the surfaced timeout/degrade completed-booking continuity family so matched final-slot booking fills do not collapse into `pack_fact_fallback`.

### First deterministic check command

- `python3 -m py_compile truffles-api/app/routers/webhook/decision.py truffles-api/tests/test_message_endpoint.py && pytest -q truffles-api/tests/test_message_endpoint.py -k "timeout_completed_booking_name_routes_into_booking_flow"`

### Blocked-by conditions

- `P1.6o178` has not finished with strict audit
- surfaced result is not yet classified from artifacts
- docs/state/session are not synced with truthful run status

### Owner role for closure

- `Top Architect + Brain`

## Execution status

- `done`

## Execution result

- Fresh guarded `dev L2` rerun `p1.6o178-l2-dev-20260314-a1-r1` completed with `artifact_integrity.valid=true`, `infra_valid=true`, `semantic_valid=false`, `stop_reason=max_failures_reached:1`, `run_integrity_reasons=[run_completion_gap]`, and `oracle_arbitration.winner=contract`.
- `P1.6o177` is off the blocker path: `tool_evidence_valid=true` and `tool_evidence_reasons=[]`, so the prior `confirm_candidate_missing` proof-validity blocker did not recur.
- Surfaced first fail is a new admissible timeout/degrade family on `dialog 1 / turn 11 / "Меня зовут Лена."`:
  - `question_contract` correctly matched `answer_slot=name`, `answer_value=Лена`;
  - `llm_policy_core` then failed with `error=deadline_exceeded`;
  - runtime rewrote the turn into `policy_core_timeout_fact_fallback` / `pack.fact_fallback`, emitted FACT-like `off_topic`, and tripped strict `fact_without_evidence`.
- Published `P1.6o179` as the next admissible bounded structural block for timeout completed-booking continuity.

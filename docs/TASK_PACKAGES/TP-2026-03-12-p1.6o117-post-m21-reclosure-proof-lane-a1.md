# TP-2026-03-12-p1.6o117-post-m21-reclosure-proof-lane-a1

- Название/цель: после deterministic re-closure `P1.6o116` выполнить один fresh guarded `dev L2`, чтобы доказать, что reopened `M21` больше не является first fail, и truthfully surface-ить следующую remaining family, если она есть.

## Canon refs

- Parent closure program: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Main remediation: `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- Prior proof lane: `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o115-post-m41-proof-lane-a1.md`
- Deterministic closure prerequisite: `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o116-active-name-deictic-time-occupancy-followup-governance-a1.md`
- Reopened matrix row: `truffles-api/app/knowledge/generic/INTERACTION_OWNER_MATRIX.yaml`
- State baseline: `STATE.md`

## Git context

- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect` remove branch + worktree after merge

## One web search (mandatory before implementation)

- Query: `site:www.uvicorn.org/settings uvicorn host port admin version runtime fingerprint`
- Date/time: `reused official proof-lane reference from P1.6o115 (2026-03-12T23:30:00+05:00)`
- Sources opened:
  - official Uvicorn settings docs: `https://www.uvicorn.org/settings/`
- Source quality:
  - official product documentation
- Found reusable ideas:
  - proof runtime startup should stay explicit and reproducible via CLI flags;
  - fingerprint validation must stay external to the app process and be recorded before the guarded run starts.
- Decision:
  - `reuse`
  - reuse the established live proof-run startup pattern on `:18184` and record `/admin/version == HEAD` before the single guarded run.
- Rejected options:
  - detached startup patterns that hide runtime parity;
  - additional expensive runs in the same block.

## Root cause (mandatory)

- Symptom:
  - after `P1.6o116`, deterministic re-closure is green, but there is no fresh proof run showing reopened `M21` disappeared from the first-fail position.
- Minimal reproduction:
  1. Use `p1.6o115-l2-dev-20260312-a1-r1` as the pre-fix proof baseline.
  2. Confirm it failed first on dialog `1`, turn `4`, text `А если это время занято?` with stale generated `reply_type=service_choice` while runtime canonically preserved `booking_time_availability_followup` and `expected_reply_type=name`.
  3. Confirm `P1.6o116` now deterministically normalizes the same deictic occupancy wording back to active-name requested-slot follow-up expectations.
  4. Run one fresh guarded `dev L2` on a live runtime whose `/admin/version` matches current `HEAD`.
  5. Audit the resulting run strictly and compare the new first fail against reopened `M21`.
- Evidence:
  - `/tmp/booking_quality/p1.6o115-l2-dev-20260312-a1-r1/summary.json`
  - `/tmp/booking_quality/p1.6o115-l2-dev-20260312-a1-r1/brief.md`
  - `/tmp/booking_quality/p1.6o115-l2-dev-20260312-a1-r1/responses.jsonl`
  - `/tmp/booking_quality/p1.6o115-l2-dev-20260312-a1-r1/trace_bundle.jsonl`
  - `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o116-active-name-deictic-time-occupancy-followup-governance-a1.md`
- Five Whys:
  1. Why is closure still incomplete? Because deterministic re-closure alone does not prove the expensive lane rolled past reopened `M21`.
  2. Why is a proof lane needed? Because the quality chain admits closure only when a fresh guarded run confirms the reopened family is no longer first.
  3. Why can we run it now? Because `P1.6o116` boundedly re-closes the reopened deictic occupancy variant on deterministic contour.
  4. Why must runtime be fingerprinted first? Because preflight/runtime mismatch makes the run non-canonical.
  5. Why only one run? Because the goal is admission of the next family, not another reactive rerun loop.
- Root cause statement:
  - the remaining gap after `P1.6o116` is proof, not implementation: without one fresh guarded `dev L2` on a verified live runtime, reopened `M21` remains only deterministically re-closed and the next remaining family stays unknown.
- Fix mechanism:
  - start a live worktree runtime on `:18184` and prove `/admin/version == HEAD`;
  - execute exactly one guarded `dev L2` run and strict audit;
  - sync docs/state/matrix truthfully based on the new first-fail outcome.

## Reuse-first decision (mandatory)

- Reuse:
  - existing guarded entrypoint `scripts/llm_quality_guarded.sh`
  - existing audit command `python3 ops/diagnose.py llm-quality-audit`
  - existing live runtime startup pattern used in prior proof-lane blocks
- Integrate:
  - no runtime code changes unless the proof run surfaces a new deterministic or bounded family
- Build:
  - none planned in this block

## Release safety

- Rollout: none; local proof-only block
- Go signal:
  - runtime fingerprint matches `HEAD`
  - guarded run artifacts are valid enough to audit (`artifact_integrity.valid=true`)
  - first fail is no longer reopened `M21`
- No-go signal:
  - invalid runtime fingerprint preflight
  - incomplete/invalid guarded artifacts
  - fresh first fail remains the same reopened `M21` family
- Rollback:
  - stop the proof lane, record evidence truthfully, do not reinterpret invalid/incomplete runs as closure evidence

## Invariant

1. Reopened `M21` stays deterministically closed by `P1.6o116`.
2. Existing deictic time/day requested-slot follow-up regressions remain green.
3. No runtime/product contract weakening.
4. Exactly one fresh guarded `dev L2` run in this block.

## Scope

- proof-lane runtime startup + fingerprint validation
- one guarded `dev L2`
- strict audit
- doc/state/session sync for the resulting admission

## Out of scope

- new implementation before the proof run surfaces a new row
- acceptance `lock/replay/full`
- broad requested-slot taxonomy redesign

## Touch-list

- `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o117-post-m21-reclosure-proof-lane-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o116-active-name-deictic-time-occupancy-followup-governance-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o115-post-m41-proof-lane-a1.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `STATE.md`
- `STRUCTURE.md`
- `docs/SESSIONS/SESSION-2026-02-19-llm-first-firebreak-a1.md`
- `truffles-api/app/knowledge/generic/INTERACTION_OWNER_MATRIX.yaml`
- `truffles-api/tests/test_interaction_owner_matrix_contract.py`

## Plan (1..N)

1. Publish `P1.6o117` with proof-lane scope and runtime-fingerprint requirements.
2. Re-run the deterministic prerequisite from `P1.6o116`.
3. Start live worktree runtime on `:18184` and prove `/admin/version == HEAD`.
4. Execute exactly one fresh guarded `dev L2` and strict audit.
5. Sync matrix/docs/state/session with truthful post-run admission.

## DoD

1. Fresh guarded `dev L2` is executed on a fingerprint-verified live runtime.
2. Reopened `M21` is no longer the first fail family.
3. The next first fail, if any, is captured truthfully with artifacts.
4. Docs/state/session are synced to the new admission truth.

## Checks

- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o115-l2-dev-20260312-a1-r1 --status done --strict-artifacts`
- `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "deictic_time_availability_followup or deictic_day_availability_followup or occupancy_followup"`
- `pytest -q truffles-api/tests/test_interaction_owner_matrix_contract.py`
- `TEST_MODE=1 scripts/llm_quality_guarded.sh --mode lock --run-id p1.6o117-l2-dev-20260312-a1-r1 --allow-pending-previous --allow-repeat-fingerprint -- --base-url http://127.0.0.1:18184 --client-slug demo_salon --mode llm --count 10 --min-turns 10 --max-turns 15 --include-media --scenario-coverage booking,info,interrupt,handoff --tool-hooks auto --jid-mode unique --allow-non-allowlist --judge-mode all --quality-lane dev --run-economy-gate block --fail-on-thresholds --max-failures 1 --reset-before-dialog`
- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o117-l2-dev-20260312-a1-r1 --status done --strict-artifacts`
- `git diff --check`
- `SESSION_AGENT=a1 scripts/session_check.sh`

## Execution status

- `done`
- live worktree runtime was started on `127.0.0.1:18184`, `/admin/version.git_commit` was verified against `HEAD` `21afcead084aa3fdfc5702c5547fb40d74970b5a`, and exactly one fresh guarded `dev L2` run `p1.6o117-l2-dev-20260312-a1-r1` was executed
- audited run truth:
  - `artifact_integrity.valid=true`
  - `infra_valid=false`
  - `semantic_valid=false`
  - `stop_reason=max_failures_reached:1`
- reopened `M21` is no longer the first fail family
- the first surfaced failing turn is `dialog 1 / turn 12 / "Меня зовут Динара."`
- the surfaced signal is not admitted as a new matrix row because the run remains non-canonical for closure, but it is sufficient for `P1.6o118` class triage:
  - `question_contract` matched `name`
  - `llm_policy_core` timed out
  - `llm_policy_plan_delta` overrode to `fact` / `pack.fact_fallback`
  - strict contract reason is `fact_without_evidence`
- the next admissible block is structural `Track C`, published as `P1.6o119`, not another row-local child TP

## Evidence

1. `/tmp/booking_quality/p1.6o115-l2-dev-20260312-a1-r1/summary.json`
2. `/tmp/booking_quality/p1.6o115-l2-dev-20260312-a1-r1/brief.md`
3. `/tmp/booking_quality/p1.6o115-l2-dev-20260312-a1-r1/scenarios.json`
4. `/tmp/booking_quality/p1.6o115-l2-dev-20260312-a1-r1/responses.jsonl`
5. `/tmp/booking_quality/p1.6o115-l2-dev-20260312-a1-r1/trace_bundle.jsonl`
6. `/tmp/booking_quality/p1.6o115-l2-dev-20260312-a1-r1/manual_audit.md`
7. `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o116-active-name-deictic-time-occupancy-followup-governance-a1.md`
8. `/tmp/booking_quality/p1.6o117-l2-dev-20260312-a1-r1/summary.json`
9. `/tmp/booking_quality/p1.6o117-l2-dev-20260312-a1-r1/brief.md`
10. `/tmp/booking_quality/p1.6o117-l2-dev-20260312-a1-r1/scenarios.json`
11. `/tmp/booking_quality/p1.6o117-l2-dev-20260312-a1-r1/responses.jsonl`
12. `/tmp/booking_quality/p1.6o117-l2-dev-20260312-a1-r1/trace_bundle.jsonl`
13. `/tmp/booking_quality/p1.6o117-l2-dev-20260312-a1-r1/manual_audit.md`
14. `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o118-class-level-closure-process-reset-a1.md`

## Rollback

1. Stop the local runtime and keep `P1.6o116` deterministic closure intact.
2. If the run is invalid/incomplete, record it truthfully and keep `P1.6o117` open.
3. Do not mutate the matrix to hide surfaced evidence.

## No-go

- no second expensive run in this block
- no default-path runtime patch before the proof run surfaces the next family
- no reuse of invalid preflight evidence as closure proof

## Risks / blockers

- local runtime may fail fingerprint validation or die before the guarded run starts
- the next surfaced result may be runtime, scenario, oracle, or infra, and must not be pre-labeled before evidence or forced into another row-child loop without class triage

## Residual architecture debt (mandatory)

### Current residuals accepted in this block

- the expensive proof lane has now run once, but the result remains non-canonical for closure (`infra_valid=false`, `semantic_valid=false`)
- timeout/degrade still contains a second semantic engine that can override matched booking-collect turns into `pack_fact_fallback`
- active-name requested-slot scenario-governance still duplicates part of runtime truth instead of being generated from the owner matrix

### Why not in this block

- this block is only the proof admission after reopened `M21` deterministic closure; it surfaces the next class truthfully but does not implement the structural follow-up

### Risk if deferred

- the team can misread the surfaced turn as another row-local bug and fall back into bounded timeout patches instead of one structural owner/degrade block

### Linked follow-up Task Package(s)

- `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o119-timeout-degrade-owner-boundary-first-slice-a1.md`
- `P1.7` acceptance replay gate after the closure program proof lane is green enough to advance

### Expiry/trigger to stop deferral

- before any further guarded `dev/acceptance` rerun beyond `P1.6o117`

## Next-block contract (mandatory)

### Next block objective

- execute `P1.6o119`: land the first structural `Track C` timeout/degrade owner-boundary slice so matched booking-collect continuity wins before degraded info-hints or `pack_fact_fallback` can take ownership.

### First deterministic check command

- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o117-l2-dev-20260312-a1-r1 --status done --strict-artifacts && pytest -q truffles-api/tests/test_interaction_owner_matrix_contract.py && git diff --check`

### Blocked-by conditions

- `P1.6o117` runtime fingerprint and audited artifacts are not synced truthfully into active docs/state
- the surfaced timeout/degrade signal is not classified explicitly under `P1.6o118`
- `P1.6o119` is not published as the structural next block

### Owner role for closure

- `Top Architect + Brain`

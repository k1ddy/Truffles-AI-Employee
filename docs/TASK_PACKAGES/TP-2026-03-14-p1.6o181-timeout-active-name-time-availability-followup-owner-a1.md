# TP-2026-03-14-p1.6o181-timeout-active-name-time-availability-followup-owner-a1

- Название/цель: закрыть surfaced `P1.6o180` timeout/degrade family, где fresh guarded rerun показал `dialog 1 / turn 5 / "А есть ли возможность записаться на 14:00?"`: после timeout на active `name` collect runtime сохраняет generic `booking_prompt`, но теряет canonical `booking_time_availability_followup` meta/trace continuity и уходит в `policy_core_degraded_collect` вместо bounded active-name requested-slot followup с resume `name`.

## Canon refs

- Binding roadmap: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o135-beauty-salon-canonical-control-plane-roadmap-a1.md`
- Prior proof lane: `docs/TASK_PACKAGES/TP-2026-03-14-p1.6o180-post-p1.6o179-proof-lane-a1.md`
- Related owner slice: `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o63-active-name-time-availability-followup-owner-a1.md`
- Related timeout boundary slice: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o119-timeout-degrade-owner-boundary-first-slice-a1.md`
- Parent closure program: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Main remediation: `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- State baseline: `STATE.md`

## Git context

- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `origin/main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect` remove branch + worktree after merge

## One web search (mandatory before implementation)

- Query: `site:stately.ai/docs guards context preserve workflow state on guarded transitions`
- Time: `2026-03-14T13:15:42+05:00`
- Sources opened:
  - `https://stately.ai/docs/guards`
- Source quality:
  - official product documentation
- Ready solutions found:
  - guarded transitions should preserve already-proven workflow context instead of falling through to a generic branch;
  - the guard should encode only bounded transition criteria, while context carries the previously established workflow state.
- Decision (`reuse/integrate/build`): `reuse + integrate`
  - reuse the existing active-name requested-slot followup response/meta/trace contract;
  - integrate one bounded timeout recovery branch that restores this contract when timeout happens after active `name` collect is already established.
- Rejected options:
  - weakening scenario/oracle expectations to accept generic degraded `booking_prompt`;
  - phrase-hardcode for `14:00` / `есть ли возможность` in policy core;
  - broad timeout rewrite outside the active-name requested-slot contour.

## Root cause (mandatory)

- Symptom:
  - `P1.6o180` is admissible (`infra_valid=true`) but stops on `dialog 1 / turn 5 / "А есть ли возможность записаться на 14:00?"` with `expected_meta_mismatch` + `expected_trace_miss`.
- Minimal reproduction:
  1. inspect `/tmp/booking_quality/p1.6o180-l2-dev-20260314-a1-r1/responses.jsonl` for sample `LLM-QUAL-p1.6o180-l2-dev-20260314-a1-r1-001-05-abd340`;
  2. confirm turn `4` already restored active booking collect under timeout with `booking_slots={service=Маникюр, datetime=завтра 15:00}` and `expected_reply_type=name`;
  3. confirm turn `5` is a question-like alternative exact-time probe, but `question_contract` logs `decision=missed` / `answer_error=blocked_by_info` and `llm_policy_core` times out;
  4. confirm final runtime meta/trace fall through to generic `policy_core_degraded_collect`, preserving only `expected_reply_type=name` but emitting no `pending_question_act=time`, no `booking_time_availability_followup`, and no `active_question_relation=ask_about_requested_slot`;
  5. inspect `truffles-api/app/routers/webhook/decision.py` and confirm timeout degrade currently has bounded recoveries for owner-boundary collect, slot-fill followup, completed-booking continuity, specialist followup, master interrupt, and pending-slot-question guidance, but no bounded active-name requested-slot followup recovery after timeout.
- Evidence:
  - `/tmp/booking_quality/p1.6o180-l2-dev-20260314-a1-r1/summary.json`
  - `/tmp/booking_quality/p1.6o180-l2-dev-20260314-a1-r1/brief.md`
  - `/tmp/booking_quality/p1.6o180-l2-dev-20260314-a1-r1/responses.jsonl`
  - `/tmp/booking_quality/p1.6o180-l2-dev-20260314-a1-r1/trace_bundle.jsonl`
  - `/tmp/booking_quality/p1.6o180-l2-dev-20260314-a1-r1/manual_audit.json`
  - `truffles-api/app/routers/webhook/decision.py`
- Five Whys:
  1. Why did the rerun fail? Because timeout degrade emitted a generic name prompt without the required requested-slot followup contract.
  2. Why did runtime emit the generic name prompt? Because after `question_contract` was blocked by info and `llm_policy_core` timed out, degraded collect fell back to the broad `policy_core_degraded_collect` path.
  3. Why did the broad path win? Because there is no bounded timeout recovery that recognizes active `name` collect plus a question-like requested-slot probe over already grounded booking time.
  4. Why is that a bug? Because the canonical runtime contract for this envelope is already known: keep `expected_reply_type=name`, preserve `pending_question_target=time`, emit `booking_time_availability_followup`, and do not silently overwrite `booking.datetime`.
  5. Why is this not a scenario-only fix? Because the scenario already expects the canonical contract, the judge passes the product reply, and the runtime trace/meta are what drift from the contract under timeout.
- Root cause statement:
  - timeout degrade has no bounded recovery for active-name requested-slot followup, so once `question_contract` is blocked and `llm_policy_core` times out, runtime preserves only generic `name` collect and drops the required requested-slot interaction contract.
- Fix mechanism:
  - add one bounded timeout recovery that detects active booking `name` collect plus a question-like requested-slot probe over grounded booking time, reuses the existing active-name time-availability followup response, restores `pending_question_*` / `active_question_relation` continuity, and keeps explicit timeout provenance in `decision_meta/decision_trace`.

## Reuse-first decision (mandatory)

- Reuse:
  - `_build_active_name_time_availability_followup_response(...)`
  - existing active-name requested-slot followup meta/trace contract
  - existing timeout degrade accounting / retry / audit path
- Integrate:
  - bounded timeout recovery before the generic degraded collect fallback
  - targeted runtime regression for the surfaced contour
- Build:
  - no new semantic parser, no new timeout framework, no scenario rewrite in this block

## Release safety

- Rollout: bounded timeout active-name requested-slot followup recovery only
- Go signal:
  - timeout-degraded active-name requested-slot turns preserve `booking_time_availability_followup` contract and keep `expected_reply_type=name`
- No-go signal:
  - generic degraded collect still wins on the surfaced contour
  - runtime silently mutates committed `booking.datetime` to the alternate slot
  - unrelated time or info questions start claiming the active-name followup owner
- Rollback:
  - revert only the bounded timeout active-name requested-slot recovery branch and its targeted regression test

## Invariant

1. No oracle or scenario weakening.
2. No phrase-hardcode in core.
3. No silent overwrite of grounded `booking.datetime`.
4. Existing timeout owner-boundary, completed-booking, slot-fill, and specialist followup recoveries remain green.
5. No expensive proof rerun before deterministic closure is green.

## Scope

- freeze `P1.6o180` truth in active docs/state/session
- publish `P1.6o181`
- implement bounded timeout active-name requested-slot recovery in runtime
- add targeted deterministic regression
- publish the next proof lane only after deterministic closure is green

## Out of scope

- new scenario/oracle rules
- broader timeout statechart redesign
- acceptance lock/replay/full
- multilingual/open-world expansion

## Touch-list

- `docs/TASK_PACKAGES/TP-2026-03-14-p1.6o180-post-p1.6o179-proof-lane-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-14-p1.6o181-timeout-active-name-time-availability-followup-owner-a1.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `STATE.md`
- `docs/SESSIONS/SESSION-2026-02-19-llm-first-firebreak-a1.md`
- `docs/SESSION_INDEX.md`
- `truffles-api/app/routers/webhook/decision.py`
- `truffles-api/tests/test_message_endpoint.py`

## Plan

1. Freeze `P1.6o180` truth as an admissible timeout active-name requested-slot family and publish `P1.6o181`.
2. Reuse the existing active-name time-availability followup response/meta/trace contract inside the timeout degrade path.
3. Add one targeted runtime regression for the surfaced contour.
4. Run focused checks and session gate.
5. Sync canon and publish the next admissible proof lane.

## DoD

1. `P1.6o180` truth is synced and `P1.6o181` is the selected continuation.
2. Timeout-degraded active-name requested-slot turns no longer fall through to generic `policy_core_degraded_collect` on deterministic contour.
3. Runtime preserves explicit timeout provenance plus canonical `booking_time_availability_followup` meta/trace continuity.
4. Focused deterministic checks and session gate are green.
5. The next admissible block is a fresh post-`P1.6o181` proof lane.

## Checks

- `python3 -m py_compile truffles-api/app/routers/webhook/decision.py truffles-api/tests/test_message_endpoint.py`
- `pytest -q truffles-api/tests/test_message_endpoint.py -k "timeout_active_name_time_availability_followup_keeps_name_resume or llm_policy_core_active_name_time_availability_followup_keeps_name_resume or degraded_timeout_matched_name_continuity_uses_owner_boundary"`
- `git diff --check`
- `SESSION_AGENT=a1 scripts/session_check.sh`

## Evidence

1. `/tmp/booking_quality/p1.6o180-l2-dev-20260314-a1-r1/summary.json`
2. `/tmp/booking_quality/p1.6o180-l2-dev-20260314-a1-r1/brief.md`
3. `/tmp/booking_quality/p1.6o180-l2-dev-20260314-a1-r1/responses.jsonl`
4. `/tmp/booking_quality/p1.6o180-l2-dev-20260314-a1-r1/trace_bundle.jsonl`
5. `/tmp/booking_quality/p1.6o180-l2-dev-20260314-a1-r1/manual_audit.json`
6. `truffles-api/app/routers/webhook/decision.py`
7. `truffles-api/tests/test_message_endpoint.py`
8. updated `STATE.md` / session docs

## Rollback

1. Keep `P1.6o180` proof truth intact.
2. Revert only the bounded timeout active-name requested-slot recovery and targeted regression.
3. Do not mutate proof artifacts or gates.

## No-go

- no oracle weakening
- no phrase-hardcode in core
- no new expensive proof rerun before deterministic closure is green

## Risks / blockers

- overbroad timeout gating could steal unrelated info-like time questions
- alternate slot parsing could accidentally mutate grounded booking state instead of preserving it as sidecar context
- proof remains non-canonical until the next rerun is audited

## Residual architecture debt (mandatory)

### Current residuals accepted in this block

- timeout requested-slot followup continuity still lives in `decision.py`, not in a dedicated boundary state artifact
- proof remains `dev` lane only until the next rerun confirms the surfaced family moved off first fail

### Why not in this block

- this block closes only the surfaced timeout active-name requested-slot runtime family

### Risk if deferred

- active-name requested-slot questions under timeout keep collapsing to generic degraded collect and mask later families

### Linked follow-up Task Package(s)

- post-`P1.6o181` proof lane TP to be published after deterministic closure

### Expiry/trigger to stop deferral

- immediately after deterministic closure and next strict-audited rerun selection

## Next-block contract (mandatory)

### Next block objective

- run one fresh guarded proof rerun after deterministic closure of `P1.6o181` and determine whether the surfaced timeout active-name requested-slot family moved off first fail.

### First deterministic check command

- `python3 -m py_compile truffles-api/app/routers/webhook/decision.py truffles-api/tests/test_message_endpoint.py && pytest -q truffles-api/tests/test_message_endpoint.py -k "timeout_active_name_time_availability_followup_keeps_name_resume"`

### Blocked-by conditions

- targeted deterministic checks are not green
- `P1.6o180` truth is not synced in active docs/state/session
- any new expensive rerun is attempted before this deterministic closure is complete

### Owner role for closure

- `Top Architect + Brain`

## Execution status

- `done`

## Execution result

- Timeout-degraded active `name` collect now restores the canonical `booking_time_availability_followup` contract for explicit requested-slot probes instead of falling through to generic `policy_core_degraded_collect`.
- Added a targeted runtime regression for the surfaced contour `А есть ли возможность записаться на 14:00?` under timeout-degraded active booking `name` resume.
- Focused deterministic checks are green and the next admissible block is the fresh post-`P1.6o181` proof lane.

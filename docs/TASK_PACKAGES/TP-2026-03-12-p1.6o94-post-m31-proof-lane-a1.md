# TP-2026-03-12-p1.6o94-post-m31-proof-lane-a1

- Название/цель: после deterministic closure `P1.6o93` выполнить один fresh guarded `dev L2`, чтобы доказать, что `M31` больше не является first fail, и truthfully surface-ить следующую remaining family, если она есть.

## Canon refs

- Parent closure program: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Main remediation: `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- Prior proof lane: `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o92-post-m19-reclosure-proof-lane-a1.md`
- Deterministic closure prerequisite: `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o93-generic-specialist-choice-followup-owner-a1.md`
- Owner matrix baseline: `truffles-api/app/knowledge/generic/INTERACTION_OWNER_MATRIX.yaml`
- State baseline: `STATE.md`

## Git context

- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect` remove branch + worktree after merge

## One web search (mandatory before implementation)

- Query: `site:www.uvicorn.org/settings uvicorn host port app-dir env-file`
- Date/time: `2026-03-12T14:35:07+05:00`
- Sources opened:
  - official Uvicorn settings docs: `https://www.uvicorn.org/settings/`
- Source quality:
  - official product documentation
- Found reusable ideas:
  - keep proof runtime startup explicit with CLI `--host`, `--port`, and `--app-dir` flags;
  - runtime fingerprint validation must stay explicit via `/admin/version` before the guarded run starts.
- Decision:
  - `reuse`
  - use the established live proof-run startup pattern on `:18184`.
- Rejected options:
  - detached launch patterns that hide runtime parity;
  - additional expensive runs in the same block.

## Root cause (mandatory)

- Symptom:
  - after `P1.6o93`, deterministic closure is green, but there is no fresh proof run showing `M31` disappeared from the first-fail position.
- Minimal reproduction:
  1. Use `p1.6o92-l2-dev-20260312-a1-r1` as the pre-fix proof baseline.
  2. Confirm it failed first on dialog `1`, turn `6`, text `Можно выбрать мастера?`.
  3. Confirm `P1.6o93` now deterministically preserves `booking_specialist_followup` ownership and resumes `time` for the same family.
  4. Run one fresh guarded `dev L2` on a live runtime whose `/admin/version` matches current `HEAD`.
  5. Audit the resulting run strictly and compare the new first fail, if any, against `M31`.
- Evidence:
  - `/tmp/booking_quality/p1.6o92-l2-dev-20260312-a1-r1/summary.json`
  - `/tmp/booking_quality/p1.6o92-l2-dev-20260312-a1-r1/responses.jsonl`
  - `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o93-generic-specialist-choice-followup-owner-a1.md`
- Five Whys:
  1. Why is closure still incomplete? Because deterministic closure alone does not prove the expensive lane rolled past `M31`.
  2. Why is a proof lane needed? Because the quality chain admits closure only when a fresh guarded run confirms the surfaced family is no longer first.
  3. Why can we run it now? Because `P1.6o93` boundedly closes the runtime owner gap on deterministic contour.
  4. Why must runtime be fingerprinted first? Because preflight/runtime mismatch makes the run non-canonical.
  5. Why only one run? Because the goal is admission of the next family, not another reactive multi-run loop.
- Root cause statement:
  - the remaining gap after `P1.6o93` is proof, not implementation: without one fresh guarded `dev L2` on a verified live runtime, `M31` remains only deterministically closed and the next remaining family stays unknown.
- Fix mechanism:
  - start a live worktree runtime on `:18184` and prove `/admin/version == HEAD`;
  - execute exactly one guarded `dev L2` run and strict audit;
  - sync docs/state/matrix truthfully based on the new first-fail outcome.

## Reuse-first decision (mandatory)

- Reuse:
  - existing guarded entrypoint `scripts/llm_quality_guarded.sh`
  - existing audit command `python3 ops/diagnose.py llm-quality-audit`
  - existing live runtime startup pattern used in `P1.6o84`, `P1.6o86`, `P1.6o88`, `P1.6o90`, and `P1.6o92`
- Integrate:
  - no runtime code changes unless the proof run surfaces a new deterministic or bounded family
- Build:
  - none planned in this block

## Release safety

- Rollout: none; local proof-only block
- Go signal:
  - runtime fingerprint matches `HEAD`
  - guarded run artifacts are valid enough to audit (`artifact_integrity.valid=true`)
  - first fail is no longer `M31`
- No-go signal:
  - invalid runtime fingerprint preflight
  - incomplete/invalid guarded artifacts
  - fresh first fail remains the same `M31` family
- Rollback:
  - stop the proof lane, record evidence truthfully, do not reinterpret invalid/incomplete runs as closure evidence

## Invariant

1. `M31` stays deterministically closed by `P1.6o93`.
2. Named-specialist follow-up rows and generic master-info rows remain boundedly green.
3. No runtime/product contract weakening.
4. Exactly one fresh guarded `dev L2` run in this block.

## Scope

- proof-lane runtime startup + fingerprint validation
- one guarded `dev L2`
- strict audit
- doc/state/session sync for the resulting admission

## Out of scope

- new runtime implementation before the proof run surfaces a new row
- acceptance `lock/replay/full`
- matrix redesign

## Touch-list

- `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o94-post-m31-proof-lane-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o93-generic-specialist-choice-followup-owner-a1.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `STATE.md`
- `STRUCTURE.md`
- `docs/SESSIONS/SESSION-2026-02-19-llm-first-firebreak-a1.md`
- `truffles-api/app/knowledge/generic/INTERACTION_OWNER_MATRIX.yaml`
- `truffles-api/tests/test_interaction_owner_matrix_contract.py`

## Plan (1..N)

1. Publish `P1.6o94` with proof-lane scope and runtime-fingerprint requirements.
2. Re-run the deterministic prerequisite from `P1.6o93`.
3. Start live worktree runtime on `:18184` and prove `/admin/version == HEAD`.
4. Execute exactly one fresh guarded `dev L2` and strict audit.
5. Sync matrix/docs/state/session with truthful post-run admission.

## DoD

1. Fresh guarded `dev L2` is executed on a fingerprint-verified live runtime.
2. `M31` is no longer the first fail family.
3. The next first fail, if any, is captured truthfully with artifacts.
4. Docs/state/session are synced to the new admission truth.

## Checks

- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o92-l2-dev-20260312-a1-r1 --status done --strict-artifacts`
- `pytest -q truffles-api/tests/test_message_endpoint.py -k "generic_specialist_choice_followup or booking_specialist_followup_persists_preference or named_master_question_rewrites_collect_to_master_info or bare_named_specialist_question_rewrites_collect_to_master_info"`
- `pytest -q truffles-api/tests/test_booking_quality_response_guard.py -k "generic_specialist_choice_followup or specialist_followup"`
- `pytest -q truffles-api/tests/test_interaction_owner_matrix_contract.py`
- `TEST_MODE=1 scripts/llm_quality_guarded.sh --mode lock --run-id p1.6o94-l2-dev-20260312-a1-r1 --allow-pending-previous --allow-repeat-fingerprint -- --base-url http://127.0.0.1:18184 --client-slug demo_salon --mode llm --count 10 --min-turns 10 --max-turns 15 --include-media --scenario-coverage booking,info,interrupt,handoff --tool-hooks auto --jid-mode unique --allow-non-allowlist --judge-mode all --quality-lane dev --run-economy-gate block --fail-on-thresholds --max-failures 1 --reset-before-dialog`
- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o94-l2-dev-20260312-a1-r1 --status done --strict-artifacts`
- `git diff --check`
- `SESSION_AGENT=a1 scripts/session_check.sh`

## Execution status

- `done`

## Evidence

1. `/tmp/booking_quality/p1.6o92-l2-dev-20260312-a1-r1/summary.json`
2. `/tmp/booking_quality/p1.6o92-l2-dev-20260312-a1-r1/brief.md`
3. `/tmp/booking_quality/p1.6o92-l2-dev-20260312-a1-r1/responses.jsonl`
4. `/tmp/booking_quality/p1.6o92-l2-dev-20260312-a1-r1/trace_bundle.jsonl`
5. `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o93-generic-specialist-choice-followup-owner-a1.md`
6. `git rev-parse HEAD` -> `21afcead084aa3fdfc5702c5547fb40d74970b5a`
7. `curl -sf http://127.0.0.1:18184/admin/version` -> `{\"version\":\"main\",\"git_commit\":\"21afcead084aa3fdfc5702c5547fb40d74970b5a\",\"build_time\":null}`
8. `/tmp/booking_quality/p1.6o94-l2-dev-20260312-a1-r1/summary.json`
9. `/tmp/booking_quality/p1.6o94-l2-dev-20260312-a1-r1/brief.md`
10. `/tmp/booking_quality/p1.6o94-l2-dev-20260312-a1-r1/scenarios.json`
11. `/tmp/booking_quality/p1.6o94-l2-dev-20260312-a1-r1/responses.jsonl`
12. `/tmp/booking_quality/p1.6o94-l2-dev-20260312-a1-r1/trace_bundle.jsonl`
13. `/tmp/booking_quality/p1.6o94-l2-dev-20260312-a1-r1/manual_audit.md`
14. `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o94-l2-dev-20260312-a1-r1 --status done --strict-artifacts`
15. `TEST_MODE=1 scripts/llm_quality_guarded.sh --mode lock --run-id p1.6o94-l2-dev-20260312-a1-r1 --allow-pending-previous --allow-repeat-fingerprint -- --base-url http://127.0.0.1:18184 --client-slug demo_salon --mode llm --count 10 --min-turns 10 --max-turns 15 --include-media --scenario-coverage booking,info,interrupt,handoff --tool-hooks auto --jid-mode unique --allow-non-allowlist --judge-mode all --quality-lane dev --run-economy-gate block --fail-on-thresholds --max-failures 1 --reset-before-dialog`

## Rollback

1. Stop the local runtime and keep `P1.6o93` deterministic closure intact.
2. If the run is invalid/incomplete, record it truthfully and keep `P1.6o94` open.
3. Do not mutate the matrix to hide surfaced evidence.

## No-go

- no second expensive run in this block
- no default-path runtime patch before the proof run surfaces the next family
- no reuse of invalid preflight evidence as closure proof

## Risks / blockers

- local runtime may fail fingerprint validation or die before the guarded run starts
- `r118` may surface a compound row that spans runtime owner evidence and scenario-governance, and must not be collapsed into a scenario-only waiver

## Residual architecture debt (mandatory)

### Current residuals accepted in this block

- closure proof is still on the `dev L2` lane only; `lock/replay/full` remain blocked
- specialist-choice recovery still lives in `decision.py`, not yet in a dedicated owner handler

### Why not in this block

- this block is only the proof admission after `M31` deterministic closure; it does not implement the next surfaced family

### Risk if deferred

- closure lane stays stuck on deterministic-only evidence without a truthfully admitted next family

### Linked follow-up Task Package(s)

- future bounded child TP for the next surfaced row, if any
- `P1.7` acceptance replay gate after the closure program proof lane is green enough to advance

### Expiry/trigger to stop deferral

- before any further guarded `dev/acceptance` rerun beyond `P1.6o94`

## Next-block contract (mandatory)

### Next block objective

- publish and run `P1.6o95`: close surfaced row `M32` so active-name deictic day requested-slot follow-up `У вас есть свободные слоты на этот день?` preserves `booking_time_availability_followup` ownership and canonical scenario expectations.

### First deterministic check command

- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o94-l2-dev-20260312-a1-r1 --status done --strict-artifacts && pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "active_name_time_availability_followup or grounded_time_availability_probe or deictic_day_availability_followup" && pytest -q truffles-api/tests/test_message_endpoint.py -k "active_name_time_availability_followup or deictic_time_availability_followup or deictic_day_availability_followup" && pytest -q truffles-api/tests/test_interaction_owner_matrix_contract.py && git diff --check`

### Blocked-by conditions

- `P1.6o94` proof evidence and surfaced `M32` admission are not synced truthfully into active docs and owner-matrix state
- exact surfaced deterministic reproductions for the active-name deictic day requested-slot family are not green
- bounded `M32` closure is not published before the next guarded rerun

### Owner role for closure

- `Top Architect + Brain`

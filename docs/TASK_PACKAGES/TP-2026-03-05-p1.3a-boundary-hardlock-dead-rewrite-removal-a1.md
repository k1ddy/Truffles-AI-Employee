# TP-2026-03-05-p1.3a-boundary-hardlock-dead-rewrite-removal-a1

- Название/цель: P1.3a удалить latent semantic rewrite paths из deterministic boundary при включенном single-owner hard lock.
- Parent TP: `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`.
- Canon refs: `AGENTS.md`, `STATE.md`, `STRATEGY/REQUIREMENTS.md`, `SPECS/SYSTEM_REFERENCE.md`.
- Branch: `fix/llm-first-firebreak-2026-02-19`.
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`.

## One web search (mandatory before implementation)

- Query: `site:csrc.nist.gov fail secure default deny policy enforcement`
- Time (UTC): `2026-03-05T13:45:20Z`
- Sources:
  - `https://csrc.nist.gov/glossary/term/fail_secure`
  - `https://csrc.nist.gov/glossary/term/deny_by_default`
  - `https://csrc.nist.gov/pubs/sp/800/53/r5/upd1/final`
- Ready solutions found:
  - Fail-secure and deny-by-default principles: on uncertainty/failure keep secure state and block by default.
- Decision (`reuse/integrate/build`): `integrate`
  - Keep boundary deterministic layer fail-closed and remove dormant rewrite code paths.
- Rejected options:
  - Keep dormant rewrite branch behind invariant assumptions.

## Root cause (mandatory)

- Symptom:
  - Boundary code still contains rewrite branches despite hard-lock invariant always enabled.
- Minimal reproduction:
  - `rg -n "rewrite_collect_to_info|policy_interrupt_contract_applied|_normalize_policy_action_from_tool_action" truffles-api/app/routers/webhook/decision.py`
- Evidence:
  - `policy_interrupt_contract` has blocked-path and dormant `else` rewrite branch.
  - `_normalize_policy_action_from_tool_action` exists as action-mutation helper but is unused in runtime.
- Five Whys:
  1. Why present: legacy compatibility branch left after hard-lock rollout.
  2. Why risky: latent mutation path can be accidentally reactivated.
  3. Why architecture violation: deterministic boundary should validate/block, not rewrite semantics.
  4. Why not enough to “trust invariant”: physical dead code still expands regression surface.
  5. Why fix now: P1.3 requires fail-closed boundary by implementation, not only config assumptions.
- Root cause statement:
  - Runtime keeps dormant semantic mutation code that contradicts single-owner boundary contract.
- Fix mechanism:
  - Delete dormant rewrite branch and dead normalization helper.

## Invariant

1. Deterministic boundary remains fail-closed.
2. Any detected semantic delta is blocked and observable in trace/meta.
3. No deterministic path mutates owner semantic outcome post-hoc.

## Scope

- `truffles-api/app/routers/webhook/decision.py`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-05-p1.3a-boundary-hardlock-dead-rewrite-removal-a1.md`

## Out of scope

- P1.3 quality-gate threshold wiring in acceptance controller.
- P1.4 contract test shield expansion.
- P1.5 realism/replay.

## Plan (1..N)

1. Remove dormant `policy_interrupt_contract` rewrite branch.
2. Remove dead `_normalize_policy_action_from_tool_action` helper.
3. Run deterministic test pack.
4. Record evidence and move to next sub-block.

## DoD

1. No `rewrite_collect_to_info` branch in runtime path.
2. No `policy_interrupt_contract_applied` metadata path in runtime code.
3. No `_normalize_policy_action_from_tool_action` helper in `decision.py`.
4. Deterministic test pack green.

## Checks

- Static:
  - `rg -n "rewrite_collect_to_info|policy_interrupt_contract_applied|_normalize_policy_action_from_tool_action" truffles-api/app/routers/webhook/decision.py`
- Deterministic:
  - `pytest -q truffles-api/tests/test_message_endpoint.py`
  - `pytest -q truffles-api/tests/test_booking_chaos_dialogs.py`
  - `pytest -q truffles-api/tests/test_booking_quality_response_guard.py`
  - `pytest -q truffles-api/tests/test_demo_salon_eval.py`

## Execution status

- `done`
- Static check results:
  - `rewrite_collect_to_info`, `policy_interrupt_contract_applied`, `_normalize_policy_action_from_tool_action` not found in `decision.py`.
- Deterministic check results:
  - `test_message_endpoint.py`: `282 passed`.
  - `test_booking_chaos_dialogs.py`: `1 passed`.
  - `test_booking_quality_response_guard.py`: `57 passed`.
  - `test_demo_salon_eval.py`: `17 passed`.

## Evidence

1. Code diff (P1.3a only).
2. Static check output.
3. Deterministic test output.

## Rollback

1. `git revert <commit_with_p1.3a_changes>`
2. Re-run deterministic checks.

## No-go

- No semantic rewrite fallback in deterministic boundary.
- No feature-flagged reintroduction of deleted branches.

## Risks/Blockers

1. Hidden dependency on removed dead paths in tests/telemetry.
2. Monolith context may hide adjacent semantic coupling.

## Residual architecture debt (mandatory)

- Current residuals accepted in this block:
  - P1.3b acceptance-lane hard gate for semantic override metric.
  - `P1.4`, `P1.5` pending.
- Why not in this block:
  - Atomic closure discipline.
- Risk if deferred:
  - Boundary quality gate remains partially enforced in runtime only.
- Linked follow-up Task Package(s):
  - `TP-2026-03-05-p1.3b-...`.
- Expiry/trigger to stop deferral:
  - Before P1.4 starts.

## Next-block contract (mandatory)

- Next block objective:
  - `P1.3b` enforce semantic override metric as blocking acceptance gate.
- First deterministic check command:
  - `rg -n "keyword_override_rate|max_keyword_override_rate|semantic_override" ops/diagnose.py`
- Blocked-by conditions:
  - Any regression in deterministic pack.
- Owner role for closure:
  - `Top Architect + Brain`

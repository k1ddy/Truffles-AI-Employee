# TP-2026-03-14-p1.6o199-partial-date-availability-slot-constraint-lexical-variant-a1

- Название/цель: закрыть surfaced `Track A` scenario-governance drift после proof lane `P1.6o198`, где lexical variant `У вас есть время на завтра?` всё ещё генерируется как `slot_compare(time)`, хотя runtime канонически держит turn как bounded `slot_constraint(time)` под активным `time` collect.

## Canon refs

- Binding roadmap: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o135-beauty-salon-canonical-control-plane-roadmap-a1.md`
- Prior proof lane: `docs/TASK_PACKAGES/TP-2026-03-14-p1.6o198-post-p1.6o197-proof-lane-a1.md`
- Historical closure for same family: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o149-partial-date-availability-slot-constraint-governance-a1.md`
- Parent closure program: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Main remediation: `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- State baseline: `STATE.md`

## Git context

- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `origin/main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect` remove branch + worktree after merge

## One web search (mandatory before implementation)

- Query: `site:docs.python.org Python re word boundary raw string`
- Date/time: `2026-03-14T17:45:00+05:00`
- Sources opened:
  - official Python `re` docs: `https://docs.python.org/3/library/re.html`
- Source quality:
  - official primary documentation
- Found reusable ideas:
  - keep regex broadening bounded with one optional lexical branch instead of duplicating multiple exact-phrase checks;
  - prefer reusable grouped surfaces over one-sentence literal hardcodes so sanitize and repair stay aligned.
- Decision:
  - `reuse + integrate`
  - reuse the existing partial-date slot-constraint normalization path and integrate one bounded lexical-surface broadening for `есть время` availability probes.
- Rejected options:
  - runtime patching for a generator-only drift;
  - oracle weakening to accept both `slot_compare` and `slot_constraint` for the same contour;
  - exact-sentence hardcode for only `У вас есть время на завтра?`.

## Root cause (mandatory)

- Symptom:
  - fresh guarded `dev L2` `p1.6o198-l2-dev-20260314-a1-r1` no longer fails on reopened `M19`, but now fails on dialog `1`, turn `3`, text `У вас есть время на завтра?` with `expected_meta_mismatch` / `expected_trace_miss` because generated expectations still require `slot_compare(time)` while runtime canonically emits `slot_constraint(time)` under timeout owner-boundary collect.
- Minimal reproduction:
  1. inspect `/tmp/booking_quality/p1.6o198-l2-dev-20260314-a1-r1/scenarios.json` dialog `1`, turn `3`;
  2. confirm the generated turn is tagged `slot_compare` and expects `pending_question_act=slot_compare` plus `pending_question_interaction slot_compare/time`;
  3. inspect `/tmp/booking_quality/p1.6o198-l2-dev-20260314-a1-r1/responses.jsonl` and `trace_bundle.jsonl` for the same turn;
  4. confirm runtime produces `expected_reply_reason=policy_core_timeout_owner_boundary`, `policy_core_guard_recovery=timeout_owner_boundary_collect`, `pending_question_act=slot_constraint`, `pending_question_target=time`, and `pending_question_interaction=slot_constraint` with grounded `answer_slot=datetime`, `answer_value=завтра`;
  5. run `_sanitize_llm_turns(...)` locally on `У вас есть время на завтра?` and confirm the turn remains stale `slot_compare`;
  6. inspect `scripts/booking_dialog_scenarios.py` and confirm the current partial-date normalization path recognizes `Есть ли ...` availability probes but misses the `у вас есть время ...` lexical variant.
- Evidence:
  - `/tmp/booking_quality/p1.6o198-l2-dev-20260314-a1-r1/summary.json`
  - `/tmp/booking_quality/p1.6o198-l2-dev-20260314-a1-r1/manual_audit.json`
  - `/tmp/booking_quality/p1.6o198-l2-dev-20260314-a1-r1/scenarios.json`
  - `/tmp/booking_quality/p1.6o198-l2-dev-20260314-a1-r1/responses.jsonl`
  - `/tmp/booking_quality/p1.6o198-l2-dev-20260314-a1-r1/trace_bundle.jsonl`
  - `scripts/booking_dialog_scenarios.py`
  - `truffles-api/tests/test_booking_dialog_scenarios_script.py`
- Five Whys:
  1. Why did the run fail? Because scenario meta/trace still expected `slot_compare(time)` while runtime emitted `slot_constraint(time)`.
  2. Why did scenario expect that? Because the generated turn stayed tagged `slot_compare`.
  3. Why did that happen after `P1.6o149`? Because the previous normalization covered the earlier lexical surface `Есть ли у вас время ...`, not the surfaced `У вас есть время ...` variant.
  4. Why is runtime already ahead? Because the turn grounds only a partial date under active `time` collect, and timeout owner-boundary recovery already preserves canonical `slot_constraint(time)` evidence.
  5. Why is this still Track A and not runtime? Because judge passes, runtime meta/trace are coherent, and the mismatch is entirely between generated expectation taxonomy and the already-observed runtime contract.
- Root cause statement:
  - the generator's partial-date availability normalization path is still keyed to an incomplete question-surface envelope, so `у вас есть время ...` lexical variants escape the existing `slot_compare -> slot_constraint` rewrite and keep stale compare expectations.
- Fix mechanism:
  - broaden the bounded lexical surface recognized by the existing partial-date availability normalization helper so `есть время` variants normalize to `slot_constraint` under active `time` collect;
  - add sanitize + post-coverage repair regressions for the surfaced positive variant and its compare-style negative sibling;
  - keep runtime untouched and publish the next proof lane only after deterministic closure is green.

## Reuse-first decision (mandatory)

- Reuse:
  - existing partial-date slot-constraint normalization helper in `scripts/booking_dialog_scenarios.py`
  - existing sanitize/repair deterministic harness in `truffles-api/tests/test_booking_dialog_scenarios_script.py`
- Integrate:
  - one bounded lexical-surface broadening for `есть время` occupancy phrasing
  - targeted sanitize/repair regressions
- Build:
  - no runtime changes planned in this block

## Release safety

- Rollout: deterministic scenario-governance only; no production runtime change
- Go signal:
  - `У вас есть время на завтра?` canonicalizes to `slot_constraint(time)` under active `time` collect
  - explicit-alternative compare sibling remains `slot_compare`
  - `git diff --check` and `SESSION_AGENT=a1 scripts/session_check.sh` stay green
- No-go signal:
  - generic compare surfaces are rewritten into `slot_constraint`
  - runtime files become necessary in this block
- Rollback:
  - revert only the new generator lexical broadening and matching deterministic tests/docs

## Invariant

1. `P1.6o197` stays closed.
2. `P1.6o198` proof truth stays intact.
3. Runtime behavior is untouched in this block.
4. Genuine temporal comparison turns remain `slot_compare`.
5. No oracle weakening.

## Scope

- bounded scenario-governance closure for the surfaced `у вас есть время ...` partial-date availability variant
- deterministic tests
- docs/state/session sync for truthful closure routing

## Out of scope

- new guarded `dev L2` in this block
- acceptance `lock/replay/full`
- runtime semantic changes
- matrix-wide ontology redesign

## Touch-list

- `scripts/booking_dialog_scenarios.py`
- `truffles-api/tests/test_booking_dialog_scenarios_script.py`
- `docs/TASK_PACKAGES/TP-2026-03-14-p1.6o198-post-p1.6o197-proof-lane-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-14-p1.6o199-partial-date-availability-slot-constraint-lexical-variant-a1.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `STATE.md`
- `docs/SESSIONS/SESSION-2026-02-19-llm-first-firebreak-a1.md`
- `docs/SESSION_INDEX.md`

## Plan

1. Publish `P1.6o199` with RCA + one web search + bounded touch-list.
2. Broaden the existing partial-date availability normalization to recognize `есть время` occupancy phrasing under active `time` collect.
3. Add deterministic sanitize/repair regressions for positive and negative cases.
4. Run targeted deterministic checks and hygiene gates.
5. Sync canon truthfully and publish the next admissible proof lane.

## DoD

1. `У вас есть время на завтра?` canonicalizes to `slot_constraint(time)` under active `time` collect.
2. Generated expectations preserve `pending_question_act=slot_constraint`, `pending_question_target=time`, and `expected_reply_type=time` on this family.
3. `У вас есть время на завтра утром или вечером?` remains `slot_compare`.
4. Deterministic tests are green.
5. `P1.6o199` is synced truthfully in active docs/state/session with the next admissible proof block identified.

## Checks

- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o198-l2-dev-20260314-a1-r1 --status done --strict-artifacts`
- `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "slot_compare_partial_date_availability"`
- `python3 -m py_compile scripts/booking_dialog_scenarios.py truffles-api/tests/test_booking_dialog_scenarios_script.py`
- `git diff --check`
- `SESSION_AGENT=a1 scripts/session_check.sh`

## Evidence

1. `/tmp/booking_quality/p1.6o198-l2-dev-20260314-a1-r1/summary.json`
2. `/tmp/booking_quality/p1.6o198-l2-dev-20260314-a1-r1/brief.md`
3. `/tmp/booking_quality/p1.6o198-l2-dev-20260314-a1-r1/scenarios.json`
4. `/tmp/booking_quality/p1.6o198-l2-dev-20260314-a1-r1/responses.jsonl`
5. `/tmp/booking_quality/p1.6o198-l2-dev-20260314-a1-r1/trace_bundle.jsonl`
6. `/tmp/booking_quality/p1.6o198-l2-dev-20260314-a1-r1/manual_audit.json`

## Rollback

1. Revert only the new generator lexical broadening and matching deterministic docs/tests.
2. Keep `P1.6o198` proof evidence intact.
3. Do not reinterpret this class as runtime defect if deterministic closure is rolled back.

## No-go

- no runtime edits
- no new expensive run before deterministic scenario regression is green
- no weakening oracle to accept contradictory `slot_compare` / `slot_constraint` contracts for the same turn
- no exact-sentence literal hardcode

## Risks / blockers

- the new lexical surface is close to legitimate compare turns, so explicit alternatives must remain protected
- sanitize and post-coverage repair must stay aligned or stale expectations will reappear after generation
- the next surfaced class after this closure may again be proof-only and non-canonical until a fresh rerun finishes beyond the observed prefix

## Residual architecture debt (mandatory)

### Current residuals accepted in this block

- scenario ontology still depends on localized lexical heuristics for pending-question cross-products
- proof lane remains partially non-canonical until a fresh rerun completes beyond the observed prefix

### Why not in this block

- this block is a bounded generator lexical extension, not a broader scenario compiler redesign

### Risk if deferred

- fresh expensive runs will keep stopping on a false-red lexical variant of an already closed Track A family and hide the next real blocker

### Linked follow-up Task Package(s)

- `docs/TASK_PACKAGES/TP-2026-03-14-p1.6o200-post-p1.6o199-proof-lane-a1.md`

### Expiry/trigger to stop deferral

- before any next guarded `dev L2` / `lock` / `replay`

## Next-block contract (mandatory)

### Next block objective

- publish and run the next fresh guarded `dev L2` proof lane after `P1.6o199` deterministic closure to prove this lexical variant moved off first fail and truthfully surface the next class.

### First deterministic check command

- `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "slot_compare_partial_date_availability" && python3 -m py_compile scripts/booking_dialog_scenarios.py truffles-api/tests/test_booking_dialog_scenarios_script.py && git diff --check`

### Blocked-by conditions

- `P1.6o199` deterministic closure is not green
- `P1.6o198` truth is not synced into active docs/state/session
- live runtime fingerprint for the next proof lane is not verified against current `HEAD`

### Owner role for closure

- `Top Architect + Brain`

## Execution status

- `done`

## Execution result

- `scripts/booking_dialog_scenarios.py` now treats bounded `есть время` occupancy phrasing as availability surface inside the existing partial-date normalization path, and pure partial-date fills no longer ignore that occupancy query signal.
- `truffles-api/tests/test_booking_dialog_scenarios_script.py` now covers both lexical variants (`Есть ли у вас время ...` and `У вас есть время ...`) together with explicit-alternative negative siblings in sanitize and post-coverage repair flows.
- Targeted deterministic checks are green and `P1.6o200` is published as the next admissible proof lane.

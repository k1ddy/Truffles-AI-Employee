# TP-2026-03-09-p1.6o48-slot-compare-time-guidance-owner-a1.md

- Название/цель: закрыть surfaced `r54` blocker, где short deictic follow-up `А на какое время?` под активным `expected_reply_type=time` и после live-availability step не сохраняет `slot_compare(time)` owner, а деградирует в `info_clarify`.
- Parent TP: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Canon refs: `AGENTS.md`, `STATE.md`, `STRUCTURE.md`, `STRATEGY/REQUIREMENTS.md`, `docs/runbooks/BOOKING_CONFIRM_VERIFY.md`, `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o47-reschedule-followup-scenario-governance-a1.md`, `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o28-live-availability-slot-question-temporal-scope-owner-a1.md`
- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect remove worktree + branch after merge`

## One web search (mandatory before implementation)

- Query: `Rasa forms unexpected user input slot question follow-up official docs`
- Time (UTC): `2026-03-09T07:42:00Z`
- Sources:
  - `https://rasa.com/docs/rasa/forms/`
  - `https://rasa.com/docs/reference/primitives/patterns/`
- Ready solutions found:
  - active collect/form state should keep ownership of follow-up turns that clarify or compare the requested slot instead of dropping them into generic fallback;
  - interruption/follow-up handling should reuse the active collect contract and resume the missing slot after answering the follow-up.
- Decision (`reuse/integrate/build`): `reuse + integrate`
  - reuse the existing `pending_question_interaction -> booking_slot_guidance` owner instead of adding a new phrase router;
  - integrate `slot_compare(time)` into the same bounded owner family already used for `ask_about_requested_slot(time)`;
  - build no semantic regex branch in runtime core.
- Rejected options:
  - phrase-hardcode `А на какое время?` in runtime;
  - waiving `expected_meta_mismatch` / `expected_trace_miss`;
  - treating the turn as generic `info_clarify`.

## Root cause (mandatory)

- Symptom:
  - guarded `dev L2` run `p1.6o6-l2-dev-20260309-a1-r54` fails on dialog 1 turn 3 with `expected_meta_mismatch`, `expected_trace_miss`, and `judge_fail`.

- Minimal reproduction:
  1. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r54/scenarios.json` dialog 1 turns 2-3
  2. observe turn 2 is live-availability booking follow-up (`mixed_fill_plus_question`) and turn 3 is `slot_compare`
  3. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r54/trace_bundle.jsonl` turn 3 and confirm `question_contract -> missed`, `llm_policy_core -> invalid_schema`, `policy_core_mode -> degraded_fallback`, final `booking_interrupt -> info_reply`
  4. inspect `prompts/llm_policy_core.md` and `truffles-api/app/routers/webhook/decision.py` and verify `booking_slot_guidance` branches only special-case `ask_about_requested_slot`, not `slot_compare`

- Evidence:
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r54/scenarios.json`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r54/responses.jsonl`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r54/trace_bundle.jsonl`
  - `prompts/llm_policy_core.md`
  - `truffles-api/app/routers/webhook/decision.py`

- Five Whys:
  1. Why did the turn fail: scenario expected `slot_compare(time)` evidence, runtime replied with generic `info_clarify`.
  2. Why did runtime reply with `info_clarify`: `llm_policy_core` degraded on `invalid_schema`, then fallback path treated the turn as generic booking interrupt.
  3. Why did policy-core degrade: the prompt/contract does not explicitly teach short deictic time-comparison follow-ups as a first-class pending-question act.
  4. Why was runtime unable to recover even if policy-core had succeeded: `booking_slot_guidance` branches only recognize `ask_about_requested_slot(time)`, not `slot_compare(time)`.
  5. Why is this architectural and not a phrase bug: the missing owner is contract-level `slot_compare(time)` over an active pending time question, not one specific surface phrase.

- Root cause statement:
  - `slot_compare(time)` is present in the schema/taxonomy, but it is not fully wired into the policy-core prompt or the bounded runtime guidance owner, so short live-calendar follow-ups collapse into degraded `info_clarify`.

- Fix mechanism:
  - extend the policy-core prompt contract to model `slot_compare(time)` explicitly under active `pending_question_contract(datetime)`;
  - reuse the existing `booking_slot_guidance` owner for `slot_compare(time)` across collect, temporal-scope validation, and `calendar.list_slots/missing_slot` branches;
  - add deterministic regressions for schema acceptance, runtime guidance, and guard acceptance.

## Release safety

- Strategy: bounded contract extension only
- Go signals:
  - `slot_compare(time)` produces the same resume contract shape as the existing time-guidance owner
  - `ask_about_requested_slot(time)` regressions stay green
- No-go signals:
  - new phrase routing in runtime core
  - weakening `expected_trace_miss` / `expected_meta_mismatch`
- Rollback verification:
  - revert only the `slot_compare(time)` owner extension if it broadens unrelated families

## Invariant

1. `P1.6o47` reschedule-governance closure remains deterministic-only and untouched in runtime.
2. Existing `ask_about_requested_slot(time)` bounded owner remains unchanged.
3. Runtime core stays contract-driven; no text regex/keyword branch is added for the surfaced phrase.

## Scope

- `prompts/llm_policy_core.md`
- `truffles-api/app/routers/webhook/decision.py`
- `truffles-api/tests/test_intent.py`
- `truffles-api/tests/test_message_endpoint.py`
- `truffles-api/tests/test_booking_quality_response_guard.py`
- tracker docs after rerun handoff:
  - `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o47-reschedule-followup-scenario-governance-a1.md`
  - `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
  - `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
  - `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`

## Out of scope

- acceptance tool-evidence infra blockers
- broad live-calendar redesign
- generic invalid-schema rescue for unrelated `other` turns

## Touch-list

- `prompts/llm_policy_core.md`
- `truffles-api/app/routers/webhook/decision.py`
- `truffles-api/tests/test_intent.py`
- `truffles-api/tests/test_message_endpoint.py`
- `truffles-api/tests/test_booking_quality_response_guard.py`
- `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o48-slot-compare-time-guidance-owner-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o47-reschedule-followup-scenario-governance-a1.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`

## Plan (1..N)

1. Freeze `r54` as bounded runtime evidence for `slot_compare(time)` owner gap.
2. Extend the prompt/runtime contract so `slot_compare(time)` reuses `booking_slot_guidance`.
3. Add deterministic regressions for schema/runtime/guard acceptance.
4. Run focused checks and `git diff --check`.
5. Run one fresh guarded dev rerun beyond `r54`.

## DoD

1. `slot_compare(time)` is explicitly modeled in the policy-core prompt contract.
2. Runtime guidance owner preserves `pending_question_interaction=slot_compare` across collect / validation / missing-slot branches.
3. Focused deterministic regressions pass.
4. One fresh guarded rerun no longer stops on the `r54` family.

## Checks

- `pytest -q truffles-api/tests/test_intent.py -k "pending_question_contract or slot_compare"`
- `pytest -q truffles-api/tests/test_message_endpoint.py -k "slot_compare_time_guidance or pending_question_act_time_guidance or list_slots_missing_slot_pending_question"`
- `pytest -q truffles-api/tests/test_booking_quality_response_guard.py -k "slot_compare_guidance or missing_slot_slot_compare or booking_slot_stall"`
- `git diff --check`

## Execution status

- `done`
- deterministic prompt/runtime regressions are green
- fresh guarded `r55` no longer stopped on the `r54` `slot_compare(time)` family and rolled to bounded runtime `P1.6o49`

## Evidence

1. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r54/summary.json`
2. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r54/brief.md`
3. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r54/scenarios.json`
4. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r54/responses.jsonl`
5. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r54/trace_bundle.jsonl`
6. `/tmp/booking_quality/p1.6o47-l1-junit.xml`
7. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r55/summary.json`
8. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r55/brief.md`
9. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r55/responses.jsonl`
10. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r55/trace_bundle.jsonl`
11. `/tmp/booking_quality/p1.6o6-l2-dev-20260309-a1-r55/manual_audit.md`

## Rollback

1. Revert only the `slot_compare(time)` prompt/runtime owner extension and matching tests.
2. Keep `P1.6o6` blocked; do not reinterpret `r54` as closure evidence.

## No-go

- no phrase-hardcode for `А на какое время?`
- no oracle waiver for missing slot-compare meta/trace
- no acceptance promotion from dev-lane evidence

## Risks/Blockers

1. Prompt-only improvement may not eliminate all live invalid-schema variance; the next surfaced family may still be another degrade-path branch.
2. Acceptance remains infra-invalid for unrelated tool-evidence gaps.

## Residual architecture debt (mandatory)

- Current residuals accepted in this block:
  - policy-core invalid-schema rescue still depends on prompt quality rather than a semantic secondary owner
  - degraded collect paths that preserve `resolution_mode=referent_followup` still do not guarantee `referent_resolver` trace/meta evidence
- Why not in this block:
  - this slice only wires the already-modeled `slot_compare(time)` act into the existing bounded owner
- Risk if deferred:
  - short deictic time-comparison turns keep collapsing into generic fallback
  - adjacent degraded-collect families can still fail canon on missing referent evidence
- Linked follow-up Task Package(s):
  - `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o49-referent-followup-degraded-collect-trace-contract-a1.md`
  - `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`
- Expiry/trigger to stop deferral:
  - next surfaced `slot_compare(time)` or adjacent short-deictic live-calendar family after this block

## Next-block contract (mandatory)

- Next block objective:
  - preserve referent trace/meta evidence when degraded collect keeps semantic `referent_followup` envelope, then rerun beyond `r55`
- First deterministic check command:
  - `pytest -q truffles-api/tests/test_message_endpoint.py -k "referent_followup_degraded_collect" && pytest -q truffles-api/tests/test_booking_quality_response_guard.py -k "resolved_referent_trace_missing or referent_followup" && git diff --check`
- Blocked-by conditions:
  - deterministic regressions for degraded-collect referent evidence are not green
  - `r55` audit not reflected in tracker docs
  - fresh rerun preflight invalid
- Owner role for closure:
  - `Hands` for implementation + rerun evidence, `Top Architect + Brain` for proof-bundle promotion

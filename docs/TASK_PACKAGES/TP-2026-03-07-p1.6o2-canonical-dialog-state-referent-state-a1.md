# TP-2026-03-07-p1.6o2-canonical-dialog-state-referent-state-a1

- Название/цель: ввести один canonical dialog state / referent state для `demo_salon`, чтобы `service/master/branch/booking_ref` и pending-question contract жили в одном state owner, а текущие carryover-острова стали только совместимыми view.
- Parent TP: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Canon refs: `AGENTS.md`, `STATE.md`, `STRUCTURE.md`, `STRATEGY/REQUIREMENTS.md`, `TECH.md`, `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`, `docs/TASK_PACKAGES/TP-2026-03-06-p1.6-local-realism-canonical-lock-a1.md`
- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`

## One web search (mandatory before implementation)

- Query: `site:learn.microsoft.com bot framework conversation state user state official`
- Time (UTC): `2026-03-07T09:53:58Z`
- Sources:
  - `https://learn.microsoft.com/en-us/microsoft-365/agents-sdk/state-concepts`
  - `https://learn.microsoft.com/en-us/azure/bot-service/bot-builder-dialog-manage-complex-conversation-flow`
- Ready solutions found:
  - conversation-scoped state should remain the single owner of per-dialog progress and pending question state;
  - backward-compatible views are acceptable only as derived projections, not as separate competing state buckets;
  - state must be saved at the end of the turn after updates to preserve multi-turn continuity.
- Decision (`reuse/integrate/build`): `integrate + build`
  - integrate the conversation-state / pending-question discipline from the official bot references;
  - build a Truffles-specific canonical dialog-state object because current runtime continuity is split across carryover islands and booking/session fragments.
- Rejected options:
  - keep `service_carryover` / `consult_context` as co-equal owners;
  - postpone canonical state until after resolver work;
  - use broad realism runs as the primary way to discover state drift before the owner model is implemented.

## Root cause (mandatory)

- Symptom:
  - current runtime stores semantic continuity in multiple islands: `class_carryover`, `service_carryover`, `consult_context`, booking state, expected-reply state, session memory.
- Minimal reproduction:
  1. inspect `truffles-api/app/routers/webhook/context_manager.py`
  2. inspect `truffles-api/app/routers/webhook/response.py`
  3. inspect `truffles-api/app/routers/webhook/decision.py`
  4. inspect `r22` artifact trail for lost current service referent
- Evidence:
  - `truffles-api/app/routers/webhook/context_manager.py:329`
  - `truffles-api/app/routers/webhook/context_manager.py:520`
  - `truffles-api/app/routers/webhook/context_manager.py:659`
  - `truffles-api/app/routers/webhook/response.py:1733`
  - `/tmp/booking_quality/p1.6-l2-dev-seed42-r22/manual_audit.md`
- Five Whys:
  1. Why does referent continuity break: there is no single state owner.
  2. Why is there no single state owner: state evolved per feature family.
  3. Why does that matter now: deictic and follow-up turns depend on stable current objects.
  4. Why can't `service_carryover` solve it: it is only a narrow TTL string and not a first-class entity state.
  5. Why is this systemic: every next family will continue to leak between islands until state ownership is unified.
- Root cause statement:
  - runtime continuity is fragmented across multiple state islands with different semantics and lifetimes, so object continuity is not preserved as a first-class contract.
- Fix mechanism:
  - introduce a canonical dialog-state / referent-state object;
  - persist current referents and pending question contract there;
  - keep legacy stores only as compatibility projections during migration.

## Invariant

1. State unification must not weaken existing booking safety or expected-reply contracts.
2. No business meaning is injected via hardcoded text rules.
3. Legacy reads may remain temporarily, but writes must converge to one canonical owner.

## Scope

- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `truffles-api/app/routers/webhook/context_manager.py`
- `truffles-api/app/routers/webhook/decision.py`
- `truffles-api/app/routers/webhook/response.py`
- `truffles-api/app/routers/webhook/booking.py`
- `truffles-api/app/services/expected_reply_contract.py`
- `ops/diagnose.py`
- `truffles-api/tests/test_message_endpoint.py`
- `truffles-api/tests/test_booking_quality_response_guard.py`
- `truffles-api/tests/test_demo_salon_eval.py`
- `truffles-api/tests/test_expected_reply_contract.py`

## Out of scope

- referent resolution logic
- capability arbitration logic
- `_legacy` cleanup outside active path
- expensive runs

## Plan (1..N)

1. Define canonical dialog-state shape, owner id, and compatibility-projection rules.
2. Route state writes to the canonical owner.
3. Project compatibility reads for legacy carryover consumers and mark projection source in trace/meta.
4. Add deterministic tests for service/master continuity across consult -> info -> booking follow-ups.

## DoD

1. Canonical state stores current `service/master/branch/booking_ref` referents and pending question contract.
2. Legacy carryover stores are compatibility views, not competing owners.
3. Trace/meta explicitly show `canonical_state_owner` and `projection_source` when a legacy view is used.
4. Deterministic tests show current service continuity survives cross-turn flows.
5. No expensive run is started in this block.

## Checks

- `pytest -q truffles-api/tests/test_expected_reply_contract.py`
- `pytest -q truffles-api/tests/test_message_endpoint.py::test_canonical_dialog_state_preserves_current_service_across_consult_to_booking_followup`
- `pytest -q truffles-api/tests/test_message_endpoint.py::test_legacy_service_carryover_reads_from_canonical_dialog_state`
- `pytest -q truffles-api/tests/test_demo_salon_eval.py::test_demo_salon_eval_records_canonical_service_projection`
- `pytest -q truffles-api/tests/test_booking_quality_response_guard.py::test_canonical_state_projection_evidence_required_for_service_referent`
- `pytest -q truffles-api/tests/test_message_endpoint.py`
- `pytest -q truffles-api/tests/test_demo_salon_eval.py`
- `pytest -q truffles-api/tests/test_booking_quality_response_guard.py`
- `ruff check truffles-api/app/routers/webhook/context_manager.py truffles-api/app/routers/webhook/decision.py truffles-api/app/routers/webhook/response.py truffles-api/app/routers/webhook/booking.py truffles-api/app/services/expected_reply_contract.py truffles-api/tests/test_message_endpoint.py truffles-api/tests/test_demo_salon_eval.py truffles-api/tests/test_expected_reply_contract.py`
- `git diff --check`

## Execution status

- `deterministic contour green`
- exact deterministic checks green:
  - `pytest -q truffles-api/tests/test_expected_reply_contract.py`
  - `pytest -q truffles-api/tests/test_message_endpoint.py::test_canonical_dialog_state_preserves_current_service_across_consult_to_booking_followup`
  - `pytest -q truffles-api/tests/test_message_endpoint.py::test_legacy_service_carryover_reads_from_canonical_dialog_state`
  - `pytest -q truffles-api/tests/test_demo_salon_eval.py::test_demo_salon_eval_records_canonical_service_projection`
  - `pytest -q truffles-api/tests/test_booking_quality_response_guard.py::test_canonical_state_projection_evidence_required_for_service_referent`
- broader deterministic status:
  - `pytest -q truffles-api/tests/test_message_endpoint.py` green
  - `pytest -q truffles-api/tests/test_booking_quality_response_guard.py` green
  - `pytest -q truffles-api/tests/test_demo_salon_eval.py` green
  - `E568` blocker removed: direct booking request on a stale `service_choice` turn now stays on booking prompt instead of degrading into `service_not_found`

## Evidence

1. `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
2. `/tmp/booking_quality/p1.6-l2-dev-seed42-r22/manual_audit.md`
3. future deterministic outputs listed in `Checks`

## Rollback

1. revert only canonical-state write path changes
2. keep `P1.6o` open and retain legacy state until replacement is safe

## No-go

- no direct phrase-based referent inference
- no second independent semantic state owner
- no expensive runs before deterministic closure
- no grep-style `-k` selector as primary closure evidence for this block

## Risks/Blockers

1. Too-wide migration can touch unrelated flows; keep scope on active `demo_salon` runtime path.
2. Compatibility projections can mask stale reads if not instrumented.

## Residual architecture debt (mandatory)

- Current residuals accepted in this block:
  - referent resolver not implemented yet
  - oracle still not realigned
- Why not in this block:
  - state ownership must stabilize before resolver/arbitration work
- Risk if deferred:
  - resolver work will continue to read fragmented state
- Linked follow-up Task Package(s):
  - `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o3-referent-resolver-capability-arbitration-a1.md`
- Expiry/trigger to stop deferral:
  - before implementing deictic resolution logic

## Next-block contract (mandatory)

- Next block objective:
  - execute `P1.6o3` so current referent state is actually consumed for deictic and capability-bearing turns
- First deterministic check command:
  - `pytest -q truffles-api/tests/test_message_endpoint.py::test_canonical_dialog_state_preserves_current_service_across_consult_to_booking_followup && pytest -q truffles-api/tests/test_message_endpoint.py::test_legacy_service_carryover_reads_from_canonical_dialog_state`
- Blocked-by conditions:
  - `P1.6o1` not green
  - canonical state owner not implemented
- Owner role for closure:
  - `Top Architect + Brain`

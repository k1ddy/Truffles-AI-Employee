# TP-2026-03-12-p1.6o95-active-name-deictic-day-availability-followup-owner-a1

- Название/цель: закрыть surfaced `M32`, где fresh proof lane `P1.6o94` показал compound owner gap на фразе `У вас есть свободные слоты на этот день?` под активным `expected_reply_type=name`: policy-core already emits `pending_question_act=ask_about_requested_slot`, `pending_question_target=time`, `active_question_relation=ask_about_requested_slot`, `temporal_scope=specific_time`, but runtime does not preserve `booking_time_availability_followup` ownership for the `capability=bookability` variant, and scenario-governance does not recognize deictic day phrasing as the same grounded requested-slot family.

## Canon refs

- Parent verdict: `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o60a-remaining-closure-architecture-verdict-a1.md`
- Closure program: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Main remediation: `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- Prior proof lane: `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o94-post-m31-proof-lane-a1.md`
- Related bounded rows: `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o72-active-name-deictic-time-availability-owner-a1.md`, `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o61-grounded-time-specialist-availability-owner-a1.md`
- Owner matrix artifact: `truffles-api/app/knowledge/generic/INTERACTION_OWNER_MATRIX.yaml`
- State baseline: `STATE.md`

## Git context

- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect` remove branch + worktree after merge

## One web search (mandatory before implementation)

- Query: `interruptions preserve requested slot ask again site:rasa.com/docs/rasa/forms`
- Date/time: `2026-03-12T15:07:00+05:00`
- Sources opened:
  - official Rasa forms docs: `https://rasa.com/docs/rasa/forms/`
- Source quality:
  - official product documentation
- Found reusable ideas:
  - once slot-filling is already on a later resume axis, interruption-like follow-up questions must preserve the active requested-slot owner explicitly instead of silently resetting to a generic prompt;
  - the contract should keep the requested slot visible even when the user asks a clarifying side question with deictic phrasing.
- Decision:
  - `reuse + integrate`
  - reuse the existing `booking_time_availability_followup` owner and scenario-governance pipeline;
  - integrate a bounded deictic day/date detector in scenario-governance and widen the runtime owner guard so `bookability` does not collapse the same semantic family.
- Rejected options:
  - accepting generic `booking_prompt/name` without pending-question owner evidence as equivalent closure;
  - adding phrase hardcode in runtime core for the exact string `на этот день`;
  - broadening the helper to all booking collect turns regardless of `specific_time` / `ask_about_requested_slot` evidence.

## Root cause (mandatory)

- Symptom:
  - fresh guarded `dev L2` `p1.6o94-l2-dev-20260312-a1-r1` stops at dialog `1`, turn `6`, user text `У вас есть свободные слоты на этот день?`
- Minimal reproduction:
  1. Inspect `/tmp/booking_quality/p1.6o94-l2-dev-20260312-a1-r1/scenarios.json` dialog `1`, turn `6`.
  2. Inspect `/tmp/booking_quality/p1.6o94-l2-dev-20260312-a1-r1/responses.jsonl` for `message_id=LLM-QUAL-p1.6o94-l2-dev-20260312-a1-r1-001-06-007b23`.
  3. Inspect `/tmp/booking_quality/p1.6o94-l2-dev-20260312-a1-r1/trace_bundle.jsonl` for the same turn and confirm:
     - policy-core succeeds with `next_question=name`, `pending_question_act=ask_about_requested_slot`, `pending_question_target=time`, `active_question_relation=ask_about_requested_slot`, `temporal_scope=specific_time`, `resolution_mode=referent_followup`;
     - runtime final meta/trace falls back to generic `booking_prompt` / `question_contract:policy_core_degraded_collect` instead of `booking_time_availability_followup`;
     - generated scenario still expects stale `service_choice` because the sanitizer does not classify `на этот день` as a grounded requested-slot follow-up.
  4. Compare the turn against existing deterministic coverage for active-name deictic time follow-up to show the surfaced variant differs only by deictic day/date phrasing and `capability=bookability`.
- Evidence:
  - `/tmp/booking_quality/p1.6o94-l2-dev-20260312-a1-r1/summary.json`
  - `/tmp/booking_quality/p1.6o94-l2-dev-20260312-a1-r1/brief.md`
  - `/tmp/booking_quality/p1.6o94-l2-dev-20260312-a1-r1/scenarios.json`
  - `/tmp/booking_quality/p1.6o94-l2-dev-20260312-a1-r1/responses.jsonl`
  - `/tmp/booking_quality/p1.6o94-l2-dev-20260312-a1-r1/trace_bundle.jsonl`
  - `/tmp/booking_quality/p1.6o94-l2-dev-20260312-a1-r1/manual_audit.md`
- Five Whys:
  1. Why did the turn fail? The proof lane still surfaced expectation and judge failures on this turn.
  2. Why were expectation failures raised? Scenario-governance left the turn on stale `booking/service_choice` expectations instead of active-name requested-slot follow-up.
  3. Why did runtime still look degraded even after policy-core emitted the requested-slot axes? The active-name follow-up owner helper rejected the row because `capability=bookability`, so runtime did not stamp `booking_time_availability_followup` trace/meta.
  4. Why did scenario-governance miss the family? Its grounded requested-slot detector only recognizes explicit times or deictic `this time/slot` phrasing, not deictic day/date references such as `на этот день`.
  5. Why is this a new row instead of a replay of `P1.6o72`? `P1.6o72` bounded deictic time follow-up with runtime owner evidence already present; `r118` surfaces the adjacent deictic day/date variant where both runtime-owner admission and scenario normalization are incomplete.
- Root cause statement:
  - active-name requested-slot follow-up is only partially compiled: runtime owner preservation is still too narrow for the `bookability + specific_time + ask_about_requested_slot` variant, and scenario-governance still misses deictic day/date phrasing that belongs to the same grounded requested-slot family.
- Fix mechanism:
  - freeze surfaced row `M32` in the machine-readable owner matrix;
  - widen the bounded runtime helper so active-name requested-slot follow-up preserves `booking_time_availability_followup` for the `bookability` variant when the existing `specific_time` / `ask_about_requested_slot` contract is present;
  - extend scenario-governance grounded requested-slot detection to cover deictic day/date references like `на этот день` / `на эту дату`;
  - add targeted runtime and scenario regressions for the exact surfaced phrasing and adjacent negatives.

## Reuse-first decision (mandatory)

- Reuse:
  - existing `booking_time_availability_followup` runtime owner
  - existing `_looks_like_grounded_time_availability_probe` scenario-governance hook
  - existing response-guard contract for active-name requested-slot follow-up
  - existing owner-matrix artifact and contract test
- Integrate:
  - bounded runtime helper widening for `bookability`
  - bounded deictic day/date recognition in scenario normalization
  - exact surfaced-phrase regressions
- Do not build:
  - new generic pending-question owner
  - new phrase-router outside the existing helper boundaries

## Execution profile

- TP mode: `implementation`
- Doc touch budget (files): `10`
- Code dominance: `required`
- Override token: `none`
- Why this profile fits: `r118` surfaced a bounded adjacent follow-up family with clear reuse targets in both runtime and scenario-governance.

## Release safety

- Strategy: bounded owner-preservation + scenario-governance normalization only
- Go signals:
  - active-name deictic day/date requested-slot follow-up now stamps `booking_time_availability_followup` meta/trace
  - scenario sanitizer rewrites the surfaced phrase to the same canonical `expected_reply_type=name` follow-up contract
  - adjacent explicit-time and deictic-time rows remain green
- No-go signals:
  - helper broadening causes unrelated booking/name prompts to acquire pending-question owner evidence
  - generic `booking_prompt` without pending-question owner still wins for the surfaced family
  - explicit-time or specialist-follow-up rows regress
- Rollback verification:
  - revert only the bounded `M32` helper and scenario-normalization delta if adjacent rows regress

## Invariant

1. Existing active-name deictic-time follow-up rows remain green.
2. Generic booking prompts without grounded requested-slot semantics must not gain `booking_time_availability_followup` evidence.
3. No unsupported live-availability claim is introduced.
4. Exactly one fresh guarded `dev L2` runs only after deterministic closure is green.

## Scope

- publish surfaced row `M32`
- implement bounded runtime owner preservation for active-name deictic day/date requested-slot follow-up
- normalize scenario-governance for the same phrasing family
- add targeted deterministic/runtime regressions

## Out of scope

- prompt/schema redesign
- acceptance `lock/replay/full`
- broader requested-slot paraphrase expansion beyond the bounded deictic day/date family surfaced by `r118`

## Touch-list

- `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o95-active-name-deictic-day-availability-followup-owner-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o94-post-m31-proof-lane-a1.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `scripts/booking_dialog_scenarios.py`
- `truffles-api/app/knowledge/generic/INTERACTION_OWNER_MATRIX.yaml`
- `truffles-api/app/routers/webhook/decision.py`
- `truffles-api/tests/test_booking_dialog_scenarios_script.py`
- `truffles-api/tests/test_message_endpoint.py`
- `truffles-api/tests/test_interaction_owner_matrix_contract.py`
- `STATE.md`

## Plan (1..N)

1. Freeze `r118` as audited proof evidence and publish `M32`.
2. Widen the bounded runtime helper so `bookability` does not suppress active-name requested-slot follow-up owner preservation.
3. Extend scenario-governance grounded requested-slot detection to deictic day/date phrasing.
4. Add targeted deterministic runtime + scenario regressions with adjacent negatives.
5. Run focused checks and `git diff --check`.
6. Publish the next proof-lane block only after deterministic closure is green.

## DoD

1. `r118` is recorded truthfully as surfaced `M32` in active docs and the owner matrix.
2. Exact surfaced phrasing `У вас есть свободные слоты на этот день?` preserves `booking_time_availability_followup` meta/trace under active `name` resume.
3. Scenario normalization rewrites the same family to canonical `expected_reply_type=name` follow-up expectations.
4. Focused deterministic runtime/scenario regressions are green.
5. The next admissible block is the fresh proof lane, not another reactive code patch.

## Expensive run control (mandatory)

- Hypothesis:
  - after bounded `M32` runtime + scenario closure, a fresh guarded `dev L2` will no longer stop on dialog `1`, turn `6`, `У вас есть свободные слоты на этот день?`
- Expected measurable effect:
  - targeted deterministic checks turn green, and the next guarded proof lane either moves past this row or surfaces a different first-fail family
- Stop condition:
  - no guarded run inside this block; stop after deterministic closure and publish the next proof TP

## Checks

- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o94-l2-dev-20260312-a1-r1 --status done --strict-artifacts`
- `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "active_name_time_availability_followup or grounded_time_availability_probe"`
- `pytest -q truffles-api/tests/test_message_endpoint.py -k "active_name_time_availability_followup or deictic_time_availability_followup or deictic_day_availability_followup"`
- `pytest -q truffles-api/tests/test_interaction_owner_matrix_contract.py`
- `python3 -m py_compile scripts/booking_dialog_scenarios.py truffles-api/app/routers/webhook/decision.py truffles-api/tests/test_booking_dialog_scenarios_script.py truffles-api/tests/test_message_endpoint.py`
- `git diff --check`

## Execution status

- `done`

## Evidence

1. `/tmp/booking_quality/p1.6o94-l2-dev-20260312-a1-r1/summary.json`
2. `/tmp/booking_quality/p1.6o94-l2-dev-20260312-a1-r1/brief.md`
3. `/tmp/booking_quality/p1.6o94-l2-dev-20260312-a1-r1/scenarios.json`
4. `/tmp/booking_quality/p1.6o94-l2-dev-20260312-a1-r1/responses.jsonl`
5. `/tmp/booking_quality/p1.6o94-l2-dev-20260312-a1-r1/trace_bundle.jsonl`
6. `/tmp/booking_quality/p1.6o94-l2-dev-20260312-a1-r1/manual_audit.md`
7. `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o94-l2-dev-20260312-a1-r1 --status done --strict-artifacts`
8. `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "active_name_time_availability_followup or grounded_time_availability_probe or deictic_day_availability_followup"` -> `3 passed`
9. `pytest -q truffles-api/tests/test_message_endpoint.py -k "active_name_time_availability_followup or deictic_time_availability_followup or deictic_day_availability_followup"` -> `4 passed`
10. `pytest -q truffles-api/tests/test_booking_quality_response_guard.py -k "active_name_deictic_time_availability_followup"` -> `1 passed`
11. `pytest -q truffles-api/tests/test_interaction_owner_matrix_contract.py` -> `2 passed`
12. `python3 -m py_compile scripts/booking_dialog_scenarios.py truffles-api/app/routers/webhook/decision.py truffles-api/tests/test_booking_dialog_scenarios_script.py truffles-api/tests/test_message_endpoint.py truffles-api/tests/test_interaction_owner_matrix_contract.py`
13. `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o96-post-m32-proof-lane-a1.md`

## Rollback

1. Keep `P1.6o94` proof truth intact.
2. Revert only the bounded `M32` runtime/scenario delta if adjacent rows regress.
3. Do not mutate the matrix to hide surfaced evidence.

## No-go

- no phrase hardcode branch in runtime core for the exact surfaced sentence
- no broad helper widening outside `specific_time + ask_about_requested_slot + expected_reply_type=name`
- no second expensive run before deterministic closure is green

## Risks / blockers

- bounded helper widening may accidentally stamp pending-question evidence on generic booking prompts if the guard is too broad
- scenario normalization could overfit if deictic day/date recognition is not kept within the grounded requested-slot family
- proof remains non-canonical until infra/tool-evidence red is separately closed

## Residual architecture debt (mandatory)

### Current residuals accepted in this block

- active-name requested-slot follow-up is still compiled in `decision.py`, not in a dedicated owner handler
- closure proof remains on `dev L2`; `lock/replay/full` stay blocked

### Why not in this block

- this block only closes surfaced row `M32`

### Risk if deferred

- deictic day/date requested-slot turns keep surfacing semantic-owner/runtime-owner disagreement and stale scenario expectations

### Linked follow-up Task Package(s)

- future proof lane TP after deterministic closure of `M32`
- `P1.7` acceptance replay gate after the closure lane is green enough to advance

### Expiry/trigger to stop deferral

- before any next guarded `dev/acceptance` rerun beyond the `M32` proof admission

## Next-block contract (mandatory)

### Next block objective

- publish and run `P1.6o96`: prove on one fresh guarded `dev L2` that `M32` is gone from the first-fail position and truthfully admit the next family, if any

### First deterministic check command

- `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "active_name_time_availability_followup or grounded_time_availability_probe or deictic_day_availability_followup" && pytest -q truffles-api/tests/test_message_endpoint.py -k "active_name_time_availability_followup or deictic_time_availability_followup or deictic_day_availability_followup" && pytest -q truffles-api/tests/test_interaction_owner_matrix_contract.py && git diff --check`

### Blocked-by conditions

- `M32` is not synced truthfully into active docs and the owner matrix
- exact surfaced deterministic reproductions are not green
- `P1.6o94` proof artifacts are not strictly audited
- live runtime fingerprint for `P1.6o96` is not proven against current `HEAD`

### Owner role for closure

- `Hands` for bounded implementation, `Top Architect + Brain` for proof-lane admission and closure signoff

# TP-2026-03-08-p1.6o29-mixed-slot-question-stale-expectation-scrub-a1

- Название/цель: закрыть новый `r28` deterministic blocker family, где scenario sanitize уже ретегирует `ask_about_requested_slot` в `mixed_fill_plus_question`, но stale `expect.meta_any/trace_contains` из исходного override сохраняют `pending_question_act=ask_about_requested_slot` и после этого дают ложный `expected_meta_mismatch` / `expected_trace_miss`.
- Parent TP: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Canon refs: `AGENTS.md`, `STATE.md`, `STRUCTURE.md`, `STRATEGY/REQUIREMENTS.md`, `docs/runbooks/BOOKING_CONFIRM_VERIFY.md`, `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`, `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o28-live-availability-slot-question-temporal-scope-owner-a1.md`
- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect remove worktree + branch after merge`

## One web search (mandatory before implementation)

- Query: `site:rasa.com/docs forms slot filling interruption mixed answer follow up requested_slot`
- Time (UTC): `2026-03-08T11:12:00Z`
- Sources:
  - `https://rasa.com/docs/rasa/forms/`
- Ready solutions found:
  - when the active requested slot is answered and questioned in the same turn, the form contract should keep the requested slot active, but expectations must follow the normalized interaction type instead of preserving stale interruption metadata from the pre-normalized label.
- Decision (`reuse/integrate/build`): `reuse + integrate`
  - reuse existing `mixed_fill_plus_question` ontology and resume contract;
  - integrate a sanitizer step that scrubs stale `pending_question_act` / `pending_question_interaction` override evidence after tag normalization;
  - build no new runtime path.
- Rejected options:
  - widening runtime oracle to accept both `mixed_fill_plus_question` and stale `ask_about_requested_slot` evidence;
  - reverting `P1.6o28` runtime fix;
  - adding phrase hardcodes for `завтра` / `свободный слот` in runtime core.

## Root cause (mandatory)

- Symptom:
  - guarded `dev L2` rerun `p1.6o6-l2-dev-20260308-a1-r28` still stopped on dialog `1`, turn `2` with `expected_meta_mismatch` / `expected_trace_miss`.

- Minimal reproduction:
  1. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r28/scenarios.json` for dialog `1`, turn `2`
  2. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r28/responses.jsonl` for message id `LLM-QUAL-p1.6o6-l2-dev-20260308-a1-r28-001-02-73c345`
  3. compare `scripts/booking_dialog_scenarios.py` normalize path for `ask_about_requested_slot -> mixed_fill_plus_question`
  4. inspect `truffles-api/tests/test_booking_dialog_scenarios_script.py` mixed-slot expectation coverage

- Evidence:
  - scenario turn text became `Какой у вас есть свободный слот на завтра?` with final tag `mixed_fill_plus_question`.
  - scenario expectation still carried `meta_any.pending_question_act=ask_about_requested_slot` and `trace_contains[pending_question_interaction]`.
  - runtime responded coherently via `calendar.list_slots` with concrete `temporal_scope=specific_time`, live slot evidence, and resume prompt; there was no runtime regression from `P1.6o28`.
  - mismatch is therefore in scenario/oracle cleanup after tag normalization, not in runtime.

- Five Whys:
  1. Why did `r28` fail: scenario expected pending-question act evidence that runtime no longer emits for a mixed slot-fill + question turn.
  2. Why did scenario expect that evidence: original override metadata from `ask_about_requested_slot` survived after tag normalization.
  3. Why did it survive: `_merge_expectations(...)` merged final-tag defaults with stale override payload without scrubbing obsolete keys.
  4. Why is that wrong: `mixed_fill_plus_question` contract intentionally keeps only resume/question-contract evidence and does not require `pending_question_act`.
  5. Why did this surface now: `P1.6o28` fixed the runtime boundary, so the remaining mismatch is now purely scenario-side.

- Root cause statement:
  - scenario sanitizer normalizes tags to `mixed_fill_plus_question` but does not scrub stale override metadata/trace entries inherited from the pre-normalized `ask_about_requested_slot` label, producing false expectation drift against correct runtime behavior.

- Fix mechanism:
  - scrub `pending_question_act` and `pending_question_interaction` from override expectations whenever final tags normalize to `mixed_fill_plus_question`;
  - add deterministic regression for the exact `r28` shape;
  - then run one fresh guarded rerun to verify the old mismatch family no longer surfaces first.

## Release safety

- Strategy: deterministic scenario/oracle cleanup only
- Go signals:
  - normalized mixed turns keep `pending_question_target=time` and `question_contract`, but drop stale `pending_question_act` / `pending_question_interaction`
  - runtime evidence from `P1.6o28` remains untouched
- No-go signals:
  - runtime-core changes for this block
  - weakening expectation contract to allow contradictory tags/metadata simultaneously
- Rollback verification:
  - revert only the scenario sanitize override scrub if unrelated mixed-turn families regress

## Invariant

1. `mixed_fill_plus_question` stays a scenario/oracle contract, not a runtime semantic rewrite.
2. `pending_question_target=time` and resume contract must remain for mixed turns.
3. No runtime-core changes in this block.
4. Expensive rerun remains exactly one fresh guarded iteration after deterministic closure.

## Scope

- `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o29-mixed-slot-question-stale-expectation-scrub-a1.md`
- `scripts/booking_dialog_scenarios.py`
- `truffles-api/tests/test_booking_dialog_scenarios_script.py`

## Out of scope

- runtime decision changes
- oracle evaluator changes outside scenario sanitize
- lock/replay acceptance evidence

## Touch-list

- `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o29-mixed-slot-question-stale-expectation-scrub-a1.md`
- `scripts/booking_dialog_scenarios.py`
- `truffles-api/tests/test_booking_dialog_scenarios_script.py`

## Plan (1..N)

1. Freeze `r28` as non-closure evidence and isolate the mismatch as scenario-side.
2. Scrub stale override keys after final tag normalization to `mixed_fill_plus_question`.
3. Add deterministic regression for the exact `r28` shape.
4. Run one fresh guarded rerun `r29` only after deterministic checks are green.

## DoD

1. Mixed-slot normalized turns no longer retain stale `pending_question_act` / `pending_question_interaction` override evidence.
2. Deterministic scenario-script regression is green.
3. `r28` is recorded as non-closure evidence; `P1.6o28` runtime closure stands.
4. Next expensive step is exactly one fresh guarded rerun `r29`.

## Expensive run control (mandatory)

- Hypothesis:
  - after stale mixed-turn expectations are scrubbed, the next guarded rerun will not fail first on the `r28` expectation-only family.
- Expected measurable effect:
  - either the run proceeds beyond dialog `1`, turn `2`, or a different blocker family surfaces.
- Stop condition:
  - run exactly one fresh guarded rerun plus strict audit; if the same mismatch persists, stop and reopen RCA instead of chaining more reruns.

## Checks

- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r28 --status done --strict-artifacts`
- `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "mixed_fill_plus_question or pending_question or service_grounded_booking or orphan_pending_question"`
- `git diff --check`

## Execution status

- `done`

## Evidence

1. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r29/summary.json`
2. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r29/brief.md`
3. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r29/scenarios.json`
4. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r29/responses.jsonl`
5. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r29/trace_bundle.jsonl`
6. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r29/manual_audit.md`
7. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r28/summary.json`
8. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r28/manual_audit.md`

## Rollback

1. Revert only the mixed-turn expectation scrub if unrelated scenario families regress.
2. Keep `P1.6o6` blocked; do not reinterpret `r28` as closure evidence.

## No-go

- no runtime-core edits
- no oracle weakening to accept contradictory scenario contracts
- no fresh guarded rerun before deterministic scenario regression is green

## Risks/Blockers

1. Another adjacent scenario-state drift may surface once stale mixed overrides are scrubbed.
2. Infra tool-evidence invalidity remains orthogonal and can still make the guarded run non-canonical.

## Residual architecture debt (mandatory)

- Current residuals accepted in this block:
  - fresh guarded proof remains open via `P1.6o6` after this deterministic cleanup
- Why not in this block:
  - this block only repairs scenario expectation drift
- Risk if deferred:
  - expensive runs keep surfacing false scenario mismatches after correct runtime fixes
- Linked follow-up Task Package(s):
  - `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`
- Expiry/trigger to stop deferral:
  - before the next fresh guarded rerun

## Next-block contract (mandatory)

- Next block objective:
  - bounded closure is complete; audited `r29` reopens deterministic follow-up `P1.6o30` instead of the stale mixed-turn family
- First deterministic check command:
  - `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "slot_constraint or mixed_fill_plus_question or pending_question" && git diff --check`
- Blocked-by conditions:
  - `P1.6o30` is not deterministically green
  - guarded preflight is not green
- Owner role for closure:
  - `Hands` for doc/code/evidence, `Top Architect + Brain` for closure gating

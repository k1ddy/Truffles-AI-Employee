# TP-2026-03-08-p1.6o23-mixed-date-slot-question-taxonomy-oracle-a1

- Название/цель: закрыть новый `r20` blocker family, где pure-tag `ask_about_requested_slot` на surface `Есть ли свободные слоты на завтра?` не ретегируется в mixed family, хотя runtime корректно потребляет `завтра` как partial `datetime` answer и идёт в `calendar.list_slots`.
- Parent TP: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Canon refs: `AGENTS.md`, `STATE.md`, `STRUCTURE.md`, `STRATEGY/REQUIREMENTS.md`, `docs/runbooks/BOOKING_CONFIRM_VERIFY.md`, `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`, `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o21-mixed-fill-slot-question-taxonomy-oracle-a1.md`, `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o22-consult-cta-check-booking-owner-boundary-a1.md`
- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect remove worktree + branch after merge`

## One web search (mandatory before implementation)

- Query: `site:rasa.com/docs forms active loop slot filling date question interruptions`
- Time (UTC): `2026-03-08T09:26:10Z`
- Sources:
  - `https://rasa.com/docs/rasa/forms/`
- Ready solutions found:
  - active-loop slot filling must tolerate turns that both constrain the slot and ask a follow-up question;
  - ontology/oracle must distinguish mixed slot-filling turns from pure interruptions, not force every date-constrained question into one bucket.
- Decision (`reuse/integrate/build`): `reuse + integrate`
  - reuse the existing `mixed_fill_plus_question` family;
  - integrate date-constraint detection into scenario sanitize/oracle instead of inventing a new runtime path;
  - build only the missing date-constraint normalization for this bounded family.
- Rejected options:
  - weakening pure `ask_about_requested_slot` expectations;
  - changing runtime semantics for `calendar.list_slots`;
  - introducing a new ad hoc mixed tag just for `завтра`.

## Root cause (mandatory)

- Symptom:
  - fresh guarded `dev L2` rerun `p1.6o6-l2-dev-20260308-a1-r20` stopped on `dialog_id=1`, `turn_index=2`, message `Есть ли свободные слоты на завтра?`

- Minimal reproduction:
  1. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r20/brief.md`
  2. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r20/responses.jsonl` and `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r20/trace_bundle.jsonl` for `message_id=LLM-QUAL-p1.6o6-l2-dev-20260308-a1-r20-001-02-ae8a2a`
  3. inspect `scripts/booking_dialog_scenarios.py` around mixed slot-question sanitize rules
  4. inspect `truffles-api/tests/test_booking_dialog_scenarios_script.py` mixed-family regressions

- Evidence:
  - Scenario tagged the turn as pure `ask_about_requested_slot` and therefore required `pending_question_interaction` trace/meta.
  - Runtime matched `datetime=завтра`, executed `calendar.list_slots`, and kept `expected_reply_type=time`.
  - Failure reasons are expectation-only (`expected_meta_mismatch`, `expected_trace_miss`), not tool/action errors.
  - The surface is mixed: it supplies a date constraint and asks availability in the same turn.

- Five Whys:
  1. Why did `r20` fail: oracle expected a pure pending-question act while runtime executed partial slot fill + availability path.
  2. Why did oracle expect that: sanitize rules only retagged time/daypart mixed surfaces, not date-constrained ones.
  3. Why is that wrong: `на завтра` is the same class of partial datetime constraint as `на утро`.
  4. Why did runtime look reasonable: under active booking collect, consuming `завтра` and listing slots is a valid owner path.
  5. Why did this surface now: `P1.6o22` removed the previous expected-reply boundary bug, so the next adjacent taxonomy/oracle gap became visible.

- Root cause statement:
  - mixed slot-question sanitize/oracle remains too narrow and only recognizes time/daypart constraints, so date-constrained availability questions are still scored as pure `ask_about_requested_slot`.

- Fix mechanism:
  - extend mixed slot-question sanitize rules from time/daypart-only to general datetime constraint signals that include relative dates / weekdays;
  - keep using the existing `mixed_fill_plus_question` family and shared resume-contract oracle.

## Release safety

- Strategy: taxonomy/oracle-only closure; no runtime changes
- Go signals:
  - `r20` is recorded as audited non-closure evidence
  - date-constrained slot questions sanitize into `mixed_fill_plus_question`
  - focused scenario-script regressions prove pure and mixed families still diverge correctly
- No-go signals:
  - weakening pure `ask_about_requested_slot`
  - changing runtime behavior in this block
  - adding new tags for the same ontology
- Rollback verification:
  - revert only the bounded date-constraint normalize rule if pure slot-question turns start losing strict expectations

## Invariant

1. No runtime-core semantic changes in this block.
2. Pure `ask_about_requested_slot(time)` expectations stay strict.
3. Mixed date/time constraint + slot-question turns continue to use the shared resume contract.
4. No new tags; reuse `mixed_fill_plus_question`.

## Scope

- `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o23-mixed-date-slot-question-taxonomy-oracle-a1.md`
- `scripts/booking_dialog_scenarios.py`
- `truffles-api/tests/test_booking_dialog_scenarios_script.py`

## Out of scope

- fresh guarded rerun after deterministic closure
- runtime changes in `decision.py`
- acceptance `lock/replay`

## Touch-list

- `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o23-mixed-date-slot-question-taxonomy-oracle-a1.md`
- `scripts/booking_dialog_scenarios.py`
- `truffles-api/tests/test_booking_dialog_scenarios_script.py`

## Plan (1..N)

1. Record `r20` as audited non-closure evidence and freeze the mixed date-constraint ontology gap.
2. Generalize mixed slot-question sanitize from time/daypart-only to datetime-constraint signals that include date markers.
3. Add deterministic scenario-script regressions for `на завтра`-style surfaces.
4. Sync active TPs so the next expensive step becomes fresh guarded `r21` only after this deterministic closure.

## DoD

1. `r20` is recorded as non-closure evidence.
2. `Есть ли свободные слоты на завтра?` no longer inherits pure `ask_about_requested_slot` expectations.
3. Pure `ask_about_requested_slot` surfaces still keep strict `pending_question_interaction` requirements.
4. Focused scenario-script tests are green.
5. The next admissible expensive step is fresh guarded `r21`.

## Expensive run control (mandatory)

- Hypothesis:
  - after `P1.6o23`, fresh guarded `r21` will no longer stop on the date-constrained mixed slot-question surface from `r20`
- Expected measurable effect:
  - `r21` no longer fails with `expected_meta_mismatch + expected_trace_miss` for `dialog_id=1`, `turn_index=2`; either the run proceeds past that turn or a different first blocker family surfaces
- Stop condition:
  - run exactly one fresh guarded `r21` plus strict audit; if the same mixed-family expectation miss persists or the run becomes infra-invalid, stop the expensive loop and reopen RCA instead of chaining another rerun

## Checks

- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r20 --status done --strict-artifacts`
- `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "mixed_fill_plus_question or pending_question or tomorrow"`
- `git diff --check`

## Execution status

- `done`

## Evidence

1. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r20/summary.json`
2. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r20/brief.md`
3. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r20/failure_families.json`
4. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r20/scenarios.json`
5. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r20/responses.jsonl`
6. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r20/trace_bundle.jsonl`
7. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r20/manual_audit.md`

## Rollback

1. Revert only the bounded date-constraint normalize rule if it over-rewrites pure slot-question turns.
2. Keep `P1.6o6` blocked; do not reinterpret `r20` as closure evidence.

## No-go

- no runtime behavior changes
- no oracle weakening for pure `ask_about_requested_slot`
- no fresh guarded rerun before deterministic closure is green

## Risks/Blockers

1. Another adjacent mixed family may surface on the next guarded rerun.
2. Infra invalidity remains orthogonal and does not close `P1.6o6`.

## Residual architecture debt (mandatory)

- Current residuals accepted in this block:
  - fresh guarded proof remains open via `P1.6o6` after this deterministic taxonomy/oracle closure
- Why not in this block:
  - this block only realigns ontology/oracle and cannot itself provide guarded proof
- Risk if deferred:
  - valid runtime mixed date-constraint turns keep failing as expectation drift, blocking the next surfaced family
- Linked follow-up Task Package(s):
  - `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`
- Expiry/trigger to stop deferral:
  - before fresh guarded `r21`

## Next-block contract (mandatory)

- Next block objective:
  - keep `P1.6o23` closed via `r21` rollover evidence and continue with bounded `P1.6o24` before fresh guarded `r22`
- First deterministic check command:
  - `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "master_info_interrupt or specialist_target or u_vas" && git diff --check`
- Blocked-by conditions:
  - `P1.6o24` is not deterministically green
  - runtime `http://127.0.0.1:18184` is not refreshed after the next code delta
  - guarded preflight is not green
- Owner role for closure:
  - `Hands` for doc/code/evidence, `Top Architect + Brain` for closure gating

# TP-2026-03-12-p1.6o99-active-name-timeout-specialist-choice-followup-a1

- Название/цель: закрыть surfaced `M34`, где fresh proof lane `P1.6o98` показал compound runtime + scenario-governance gap на фразе `Могу выбрать Айгерим?` под активным `expected_reply_type=name`: timeout-degraded runtime уходит в generic `policy_core_degraded_collect`, хотя bounded active-name named-specialist follow-up уже существует как канон, а scenario-governance не распознаёт `выбрать + имя специалиста` как ту же specialist-followup семью и оставляет stale `service_choice` expectations.

## Canon refs

- Parent verdict: `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o60a-remaining-closure-architecture-verdict-a1.md`
- Closure program: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Main remediation: `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- Prior proof lane: `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o98-post-m33-proof-lane-a1.md`
- Related bounded rows: `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o74-active-name-named-specialist-followup-owner-a1.md`, `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o40-timeout-specialist-followup-owner-recovery-a1.md`
- Owner matrix artifact: `truffles-api/app/knowledge/generic/INTERACTION_OWNER_MATRIX.yaml`
- State baseline: `STATE.md`

## Git context

- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect` remove branch + worktree after merge

## One web search (mandatory before implementation)

- Query: `site:docs.python.org/3/library/re.html python re unicode word boundary alternation regex`
- Date/time: `2026-03-12T16:25:00+05:00`
- Sources opened:
  - official Python `re` docs: `https://docs.python.org/3/library/re.html`
- Source quality:
  - official language documentation
- Found reusable ideas:
  - Unicode-safe boundary patterns should stay explicit and narrow when matching person-name tokens;
  - bounded alternation/prefix matching is preferable to broad substring heuristics when admitting a new phrasing family.
- Decision:
  - `reuse + integrate`
  - reuse the existing active-name specialist-followup runtime path and scenario-repair helpers;
  - integrate one bounded `выбр* + specialist-name` phrase detector in scenario-governance and one bounded timeout specialist-followup recovery for active `name` collect.
- Rejected options:
  - weakening scenario expectations to accept stale `service_choice` or generic `policy_core_degraded_collect` as equivalent closure;
  - introducing a new generic name-entity router in runtime core;
  - phrase-hardcoding only the exact string `Могу выбрать Айгерим?`.

## Root cause (mandatory)

- Symptom:
  - fresh guarded `dev L2` `p1.6o98-l2-dev-20260312-a1-r1` stops at dialog `1`, turn `5`, user text `Могу выбрать Айгерим?`
- Minimal reproduction:
  1. Inspect `/tmp/booking_quality/p1.6o98-l2-dev-20260312-a1-r1/scenarios.json` dialog `1`, turn `5`.
  2. Confirm generated expectations still require `reply_type=service_choice` with `question_contract expected_reply_type=service_choice`.
  3. Inspect `/tmp/booking_quality/p1.6o98-l2-dev-20260312-a1-r1/responses.jsonl` and `/tmp/booking_quality/p1.6o98-l2-dev-20260312-a1-r1/trace_bundle.jsonl` for `message_id=LLM-QUAL-p1.6o98-l2-dev-20260312-a1-r1-001-05-b3332a`.
  4. Confirm runtime returns generic `booking_prompt` with `expected_reply_type=name`, `expected_reply_reason=policy_core_degraded_collect`, and no `pending_question_interaction=specialist_followup` despite the active-name specialist-followup canon.
  5. Compare the turn with existing active-name named-specialist and timeout specialist-followup closures to show the remaining gap is the question-like `выбрать + имя специалиста` phrasing under timeout + stale scenario expectation.
- Evidence:
  - `/tmp/booking_quality/p1.6o98-l2-dev-20260312-a1-r1/summary.json`
  - `/tmp/booking_quality/p1.6o98-l2-dev-20260312-a1-r1/brief.md`
  - `/tmp/booking_quality/p1.6o98-l2-dev-20260312-a1-r1/scenarios.json`
  - `/tmp/booking_quality/p1.6o98-l2-dev-20260312-a1-r1/responses.jsonl`
  - `/tmp/booking_quality/p1.6o98-l2-dev-20260312-a1-r1/trace_bundle.jsonl`
  - `/tmp/booking_quality/p1.6o98-l2-dev-20260312-a1-r1/manual_audit.md`
- Five Whys:
  1. Why did the run fail? Because strict contract evaluation hit `expected_meta_mismatch` and `expected_trace_miss` on turn `5`.
  2. Why did scenario expectations stay stale? Because scenario-governance did not classify `выбрать + имя специалиста` as the same active-name named-specialist follow-up family and left the turn on generic `service_choice`.
  3. Why did runtime still look generic? Because the timeout specialist-followup recovery only handled active `time` collect, not the active `name` variant with grounded booking time.
  4. Why is this not just scenario drift? Because the runtime trace/meta also failed to preserve `pending_question_interaction=specialist_followup` and `active_question_relation=referent_followup`; it silently degraded to generic `policy_core_degraded_collect`.
  5. Why is this a new row instead of replaying `M23` or `P1.6o40`? `M23` bounded declarative active-name named-specialist follow-up, and `P1.6o40` bounded timeout specialist-followup under active `time`; `r120` surfaces the adjacent question-like `выбрать + имя специалиста` variant under active `name` timeout degrade.
- Root cause statement:
  - active-name named-specialist follow-up is still only partially compiled: timeout degrade does not recover specialist-followup ownership when `expected_reply_type=name`, and scenario-governance does not recognize `выбрать + имя специалиста` as the same named-specialist follow-up family.
- Fix mechanism:
  - freeze surfaced row `M34` in the machine-readable owner matrix;
  - extend timeout specialist-followup recovery so active `name` collect preserves specialist preference and explicit `policy_core_timeout_specialist_followup` evidence;
  - normalize scenario-governance for question-like `выбр* + specialist-name` phrasing under active-name specialist follow-up;
  - add targeted runtime, response-guard, and scenario regressions.

## Reuse-first decision (mandatory)

- Reuse:
  - existing `_resolve_specialist_name_hint_with_trace` hint recovery
  - existing `_format_specialist_followup_prompt`
  - existing active-name named-specialist follow-up contract in runtime and response guard
  - existing scenario-governance repair path for active-name specialist follow-up
- Integrate:
  - bounded timeout active-name specialist-followup recovery
  - bounded `выбр* + specialist-name` detector in scenario-governance
  - targeted deterministic tests for the surfaced phrasing and adjacent existing families
- Do not build:
  - new generic phrase router
  - new standalone specialist name extractor outside the existing hint path

## Execution profile

- TP mode: `implementation`
- Doc touch budget (files): `11`
- Code dominance: `required`
- Override token: `none`
- Why this profile fits: `r120` surfaced a bounded adjacent named-specialist variant that reuses existing specialist-followup architecture on both runtime and scenario sides.

## Release safety

- Strategy: bounded timeout recovery + scenario-governance normalization only
- Go signals:
  - active-name question-like named specialist choice now preserves `pending_question_interaction=specialist_followup` with `expected_reply_type=name`
  - timeout degrade path acknowledges the chosen specialist and resumes customer-name collect
  - scenario-governance canonicalizes the same phrasing to active-name specialist-followup expectations instead of `service_choice`
  - adjacent declarative active-name named-specialist follow-up and active-time timeout specialist-followup remain green
- No-go signals:
  - generic active-name booking/name prompts start inheriting specialist-followup ownership without a real specialist name
  - scenario normalization rewrites non-specialist `выбрать ...` turns into named-specialist follow-up
  - existing `M23` / timeout-specialist rows regress
- Rollback verification:
  - revert only the bounded `M34` timeout-recovery + scenario-normalization delta if adjacent rows regress

## Invariant

1. Existing active-name named-specialist follow-up rows remain green.
2. Existing timeout specialist-followup under active `time` remains green.
3. No generic `service_choice` reopening is accepted as equivalent closure.
4. No expensive run in this block.

## Scope

- publish surfaced row `M34`
- implement bounded timeout specialist-followup recovery for active `name` collect
- normalize `выбр* + specialist-name` scenario-governance under the same family
- add targeted deterministic runtime/response-guard/scenario regressions

## Out of scope

- new guarded `dev L2`
- acceptance `lock/replay/full`
- broader named-entity or specialist-choice ontology redesign

## Touch-list

- `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o99-active-name-timeout-specialist-choice-followup-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o98-post-m33-proof-lane-a1.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `scripts/booking_dialog_scenarios.py`
- `truffles-api/app/routers/webhook/decision.py`
- `truffles-api/app/knowledge/generic/INTERACTION_OWNER_MATRIX.yaml`
- `truffles-api/tests/test_booking_dialog_scenarios_script.py`
- `truffles-api/tests/test_message_endpoint.py`
- `truffles-api/tests/test_booking_quality_response_guard.py`
- `truffles-api/tests/test_interaction_owner_matrix_contract.py`
- `STATE.md`

## Plan (1..N)

1. Freeze `r120` as audited proof evidence and publish `M34`.
2. Extend timeout specialist-followup recovery to active `name` collect without broadening unrelated degrade paths.
3. Normalize `выбр* + specialist-name` scenario-governance under active-name specialist-followup.
4. Add targeted deterministic regressions with adjacent active-name and timeout-specialist coverage.
5. Run focused checks and `git diff --check`.
6. Publish the next proof-lane block only after deterministic closure is green.

## DoD

1. `r120` is recorded truthfully as surfaced `M34` in active docs and the owner matrix.
2. `Могу выбрать Айгерим?` under active `name` collect now preserves specialist-followup trace/meta and resumes `name` collect.
3. Scenario-governance canonicalizes the same phrasing to active-name specialist-followup expectations instead of `service_choice`.
4. Focused deterministic runtime/response-guard/scenario regressions are green.
5. The next admissible block is the fresh proof lane `P1.6o100`.

## Expensive run control (mandatory)

- Hypothesis:
  - after bounded `M34` runtime + scenario closure, a fresh guarded `dev L2` will no longer stop on dialog `1`, turn `5`, `Могу выбрать Айгерим?`
- Expected measurable effect:
  - targeted deterministic checks turn green, and the next guarded proof lane either moves past this row or surfaces a different first-fail family
- Stop condition:
  - no guarded run inside this block; stop after deterministic closure and publish the next proof TP

## Checks

- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o98-l2-dev-20260312-a1-r1 --status done --strict-artifacts`
- `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "active_name_specialist_followup or choose_named_specialist"`
- `pytest -q truffles-api/tests/test_message_endpoint.py -k "timeout_active_name_specialist_followup or timeout_specialist_followup_keeps_time_collect"`
- `pytest -q truffles-api/tests/test_booking_quality_response_guard.py -k "active_name_named_specialist_followup or timeout_active_name_named_specialist_followup"`
- `pytest -q truffles-api/tests/test_interaction_owner_matrix_contract.py`
- `python3 -m py_compile scripts/booking_dialog_scenarios.py truffles-api/app/routers/webhook/decision.py truffles-api/tests/test_booking_dialog_scenarios_script.py truffles-api/tests/test_message_endpoint.py truffles-api/tests/test_booking_quality_response_guard.py truffles-api/tests/test_interaction_owner_matrix_contract.py`
- `git diff --check`

## Execution status

- `done`

## Evidence

1. `/tmp/booking_quality/p1.6o98-l2-dev-20260312-a1-r1/summary.json`
2. `/tmp/booking_quality/p1.6o98-l2-dev-20260312-a1-r1/brief.md`
3. `/tmp/booking_quality/p1.6o98-l2-dev-20260312-a1-r1/scenarios.json`
4. `/tmp/booking_quality/p1.6o98-l2-dev-20260312-a1-r1/responses.jsonl`
5. `/tmp/booking_quality/p1.6o98-l2-dev-20260312-a1-r1/trace_bundle.jsonl`
6. `/tmp/booking_quality/p1.6o98-l2-dev-20260312-a1-r1/manual_audit.md`
7. `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o98-l2-dev-20260312-a1-r1 --status done --strict-artifacts`
8. `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "active_name_specialist_followup or choose_named_specialist"` -> `3 passed, 95 deselected`
9. `pytest -q truffles-api/tests/test_message_endpoint.py -k "timeout_active_name_specialist_followup or timeout_specialist_followup_keeps_time_collect"` -> `2 passed, 378 deselected`
10. `pytest -q truffles-api/tests/test_booking_quality_response_guard.py -k "active_name_named_specialist_followup or timeout_active_name_named_specialist_followup"` -> `2 passed, 91 deselected`
11. `pytest -q truffles-api/tests/test_interaction_owner_matrix_contract.py` -> `2 passed`
12. `python3 -m py_compile scripts/booking_dialog_scenarios.py truffles-api/app/routers/webhook/decision.py truffles-api/tests/test_booking_dialog_scenarios_script.py truffles-api/tests/test_message_endpoint.py truffles-api/tests/test_booking_quality_response_guard.py truffles-api/tests/test_interaction_owner_matrix_contract.py`
13. `git diff --check`
14. `SESSION_AGENT=a1 scripts/session_check.sh`

## Rollback

1. Keep `P1.6o98` proof truth intact.
2. Revert only the bounded `M34` timeout-recovery and scenario-normalization delta if adjacent rows regress.
3. Do not mutate the matrix to hide surfaced evidence.

## No-go

- no broad specialist-name routing outside the existing hint path
- no weakening active-name named-specialist follow-up expectations to generic `name` collect without specialist evidence
- no second expensive run before deterministic closure is green

## Risks / blockers

- bounded timeout recovery can accidentally stamp specialist-followup on generic active-name booking prompts if name hints are not kept strict
- scenario normalization can overfit if `выбр* + specialist-name` matching is not kept on capitalized name tokens only
- proof remains non-canonical until infra/tool-evidence red is separately closed

## Residual architecture debt (mandatory)

### Current residuals accepted in this block

- timeout specialist-followup recovery still lives in `decision.py`, not in a dedicated migrated owner handler
- closure proof remains on `dev L2`; `lock/replay/full` stay blocked

### Why not in this block

- this block only closes surfaced row `M34`

### Risk if deferred

- question-like named-specialist turns under active `name` collect keep collapsing to generic timeout booking prompts and hide later remaining families

### Linked follow-up Task Package(s)

- `P1.6o100` post-`M34` proof lane
- `P1.7` acceptance replay gate after the closure lane is green enough to advance

### Expiry/trigger to stop deferral

- before any next guarded `dev/acceptance` rerun beyond the `M34` proof admission

## Next-block contract (mandatory)

### Next block objective

- publish and run `P1.6o100`: prove on one fresh guarded `dev L2` that `M34` is gone from the first-fail position and truthfully admit the next family, if any.

### First deterministic check command

- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o98-l2-dev-20260312-a1-r1 --status done --strict-artifacts && pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "active_name_specialist_followup or choose_named_specialist" && pytest -q truffles-api/tests/test_message_endpoint.py -k "timeout_active_name_specialist_followup or timeout_specialist_followup_keeps_time_collect" && pytest -q truffles-api/tests/test_booking_quality_response_guard.py -k "active_name_named_specialist_followup or timeout_active_name_named_specialist_followup" && pytest -q truffles-api/tests/test_interaction_owner_matrix_contract.py && git diff --check`

### Blocked-by conditions

- `M34` is not synced truthfully into active docs and the owner matrix
- exact surfaced deterministic reproductions are not green
- `P1.6o98` proof artifacts are not strictly audited
- live runtime fingerprint for `P1.6o100` is not proven against current `HEAD`

### Owner role for closure

- `Top Architect + Brain`

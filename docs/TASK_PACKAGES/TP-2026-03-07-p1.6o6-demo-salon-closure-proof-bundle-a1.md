# TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1

- Название/цель: собрать единственный допустимый closure proof bundle для `demo_salon` после завершения архитектурных child blocks: deterministic contour -> fresh guarded `dev L2` -> guarded `lock` -> guarded `replay` -> strict audit -> master/STATE handoff.
- Parent TP: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Canon refs: `AGENTS.md`, `STATE.md`, `STRUCTURE.md`, `STRATEGY/REQUIREMENTS.md`, `TECH.md`, `docs/runbooks/BOOKING_CONFIRM_VERIFY.md`, `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`, `docs/TASK_PACKAGES/TP-2026-03-06-p1.6-local-realism-canonical-lock-a1.md`
- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`

## One web search (mandatory before implementation)

- Query: `site:martinfowler.com canary release testing rollback evidence`
- Time (UTC): `2026-03-07T13:53:02Z`
- Sources:
  - `https://martinfowler.com/articles/patterns-of-distributed-systems/canary-release.html`
- Ready solutions found:
  - canary/closure evidence must be staged, explicit, and reversible rather than mixed with ad-hoc debugging loops
  - rollback discipline belongs to the release proof flow, not to runtime semantic routing
- Decision (`reuse/integrate/build`): `reuse + integrate`
  - reuse the existing guarded `llm-quality` wrapper, audit flow, and artifact index discipline
  - integrate them in strict proof order: deterministic contour -> fresh `dev L2` -> acceptance `lock` -> acceptance `replay` -> strict audit
- Rejected options:
  - reusing historical incomplete runs as closure evidence
  - skipping the audited `lock` step and comparing replay against mixed historical baselines

## Root cause (mandatory)

- Symptom:
  - historical work repeatedly mixed debug loops with acceptance evidence, partial runs, and budget-driven reruns.
- Minimal reproduction:
  1. inspect `docs/TASK_PACKAGES/TP-2026-03-06-p1.6-local-realism-canonical-lock-a1.md`
  2. inspect `/tmp/booking_quality/p1.6-l2-dev-seed42-r20/manual_audit.md`
  3. inspect `/tmp/booking_quality/p1.6-l2-dev-seed42-r21/manual_audit.md`
  4. inspect `/tmp/booking_quality/p1.6-l2-dev-seed42-r22/manual_audit.md`
- Evidence:
  - existing audited partial runs `r20/r21/r22`
  - guarded runbook and chain controller docs
- Five Whys:
  1. Why was there budget_stop drift: expensive runs were used as the main debug loop.
  2. Why is that unacceptable now: proof bundle must validate closure, not discover new random issues by accident.
  3. Why do we need one explicit block: without it, closure evidence will again mix with in-flight debugging.
  4. Why must audits be mandatory: invalid/incomplete evidence cannot become baseline.
  5. Why is this still architecture work: process economics and closure discipline directly affect whether architecture claims are trustworthy.
- Root cause statement:
  - closure evidence was historically polluted by debug-loop behavior, so `demo_salon` needs one explicit proof-bundle block with strict ordering, audit gates, and no reuse of invalid evidence.
- Fix mechanism:
  - run one bounded proof bundle after all architecture child blocks are green;
  - treat any new family as reopening the program;
  - forbid false closure on partial evidence.

## Invariant

1. No expensive rerun without code delta or newly justified proof step.
2. `demo_salon` remains the only heavy acceptance lane.
3. Invalid/incomplete/manual-audit-pending artifacts are never used as closure evidence.

## Scope

- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-06-p1.6-local-realism-canonical-lock-a1.md`
- `docs/runbooks/BOOKING_CONFIRM_VERIFY.md`
- `ops/diagnose.py`
- guarded run outputs under `/tmp/booking_quality`
- `STATE.md` update by Brain/Top Architect after closure

## Out of scope

- new runtime behavior fixes without opening a new child block
- multi-pack `P6` proof
- canary/full rollout

## Plan (1..N)

1. Verify all prior child blocks are green and synced in docs.
2. Run full deterministic contour once on the final runtime.
3. Run exactly one fresh guarded `dev L2`.
4. If green, run exactly one guarded `lock`.
5. If green, run exactly one guarded `replay` against that lock.
6. Audit every expensive run strictly and hand off master/STATE evidence.
7. If any new family appears, reopen `P1.6o` and stop the proof bundle.

## DoD

1. All upstream child blocks are green.
2. One fresh guarded `dev L2` is green and audited.
3. One fresh guarded `lock` is green and audited.
4. One fresh guarded `replay` is green and audited against that lock.
5. No invalid or incomplete artifacts are used for closure.
6. Master TP and `STATE.md` can truthfully declare `demo_salon` closure for currently known families.

## Checks

- `pytest -q truffles-api/tests/test_message_endpoint.py`
- `pytest -q truffles-api/tests/test_booking_quality_response_guard.py`
- `pytest -q truffles-api/tests/test_demo_salon_eval.py`
- `pytest -q truffles-api/tests/test_booking_chaos_dialogs.py`
- `pytest -q truffles-api/tests/test_pack_runtime_service.py`
- `scripts/llm_quality_guarded.sh --mode lock --run-id <dev-L2-or-lock-id> -- --base-url http://127.0.0.1:18184 --client-slug demo_salon ...`
- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/<run-id> --status done --strict-artifacts`

## Execution status

- `in_progress`
- latest bounded closures:
  - `P1.6o58` is done: `calendar.book_slot -> specialist_missing` now preserves explicit specialist follow-up contract/evidence; `r72` no longer stopped on the `r71` family
  - `P1.6o59` is done: `llm_policy_core` prompt now keeps active-slot free-slot questions in `ask_about_requested_slot(time)` / `collect`; `r73` no longer stopped on the `r72` family
  - `P1.6o60` is done on bounded contour: `M7` semantic/runtime/tests are implemented and fresh guarded `dev L2` `r77` surfaced a different first fail
  - `P1.6o61` is done on bounded contour: grounded-time specialist availability now preserves explicit owner + `time -> name` transition; fresh guarded `dev L2` `r81` surfaced a different first fail
  - `P1.6o62` is done on bounded contour: repeated-timeout `ask_about_requested_slot(time)` now preserves row-scoped timeout degrade; fresh guarded `dev L2` `r82` surfaced a different first fail
  - `P1.6o63` is done on bounded contour: active-name alternate-time availability now preserves explicit owner + resume `name`; fresh guarded `dev L2` `r83` surfaced a different first fail
  - `P1.6o64` is done on bounded contour: timeout-mixed-date governance now accepts post-grounding `name` resume after deterministic `datetime` grounding; fresh guarded `dev L2` `r90` surfaced a different first fail
  - `P1.6o65` is done on bounded contour: invalid-schema named specialist follow-up now preserves explicit specialist-followup recovery on the degraded path; fresh guarded `dev L2` `r91` surfaced a different first fail
  - `P1.6o66` is done on bounded contour: success-path named specialist follow-up now preserves explicit specialist-followup owner evidence; fresh guarded `dev L2` `r92` surfaced a different first fail
  - `P1.6o67` is done on bounded contour: ambiguous lower-bound time phrases now preserve generated `time` resume contract; fresh guarded `dev L2` `r93` surfaced a different first fail
  - `P1.6o68` is done on bounded contour: invalid-schema service-grounded degraded booking now preserves grounded service + `time`; fresh guarded `dev L2` `r94` surfaced a different first fail
  - `P1.6o69` is done on bounded contour: declarative time-window constraints now preserve `slot_constraint(time)` ownership; fresh guarded `dev L2` `r95` surfaced a different first fail
  - `P1.6o70` is done on bounded contour: invalid-schema booking-request named-specialist follow-up now preserves specialist-followup recovery; fresh guarded `dev L2` `r96` surfaced a different first fail
  - `P1.6o71` is done on bounded contour: stale `check_booking/service_choice` governance is closed; fresh guarded `dev L2` `r101` surfaced a different first fail
  - `P1.6o72` is done on bounded contour: active-name deictic requested-slot follow-up now preserves grounded-time ownership; fresh guarded `dev L2` `r102` surfaced a different first fail
  - `P1.6o73` is done on bounded contour: question-like daypart exact-time fill now grounds explicit `datetime` and resumes `name`; fresh guarded `dev L2` `r103` surfaced a different first fail
  - `P1.6o74` is done on bounded contour: active-name declarative named-specialist follow-up now preserves specialist preference and resume `name`; fresh guarded `dev L2` `r104` surfaced a different first fail
  - `P1.6o75` is done on bounded contour: active-time standalone consult topic shift now clears or replaces stale booking `time` resume with consult/service-choice ownership; fresh guarded `dev L2` `r105` surfaced a different first fail
  - `P1.6o76` is done on bounded contour: first-time requested-slot timeout fallback now preserves `ask_about_requested_slot(time)` interaction evidence instead of collapsing into generic collect; fresh guarded `dev L2` `r106` surfaced a different first fail
  - `P1.6o77` is done on bounded contour: active-time daypart-preference slot-fill turns no longer emit phantom `hours` / `duration` info-class signals; fresh guarded `dev L2` `r112` surfaced a different first fail
- current surfaced blocker:
  - `r112` first strict fail is `dialog_id=1`, `turn_index=4`, message `А сколько это будет стоить?`
  - true root cause starts on turn `3`: `Мне нужно что-то после 15:00.` is matched as booking `time` input but keeps `slot_confirmation_required=true` / `booking_confirm_reject`; the follow-up pricing interrupt then loses grounded service carryover and reopens `service_choice`, so strict contract fails on `expected_meta_mismatch` / `expected_trace_miss`
  - follow-up block is `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o78-booking-confirm-pricing-interrupt-service-choice-drift-a1.md`
- prerequisite status:
  - `P1.6o1`, `P1.6o2`, `P1.6o3`, `P1.6o4`, `P1.6o5`, `P1.6o7`, `P1.6o8`, `P1.6o9`, `P1.6o10`, `P1.6o11`, and `P1.6o12` deterministic contours are green in this worktree
  - guarded runtime preflight on `http://127.0.0.1:18184` is reachable
  - judge key resolution is available through existing Truffles key resolution (`truffles-api/.env` / container env)
- guarded `dev L2` chain so far:
  - `r1` failed with `booking_slot_stall` on `dialog_index=2`, `turn_index=5`, message `Могу прийти в 5 часов?`; follow-up `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o7-expected-time-hour-phrase-interrupt-arbitration-a1.md`
  - `r2` surfaced timeout style-reference fallback; follow-up `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o8-timeout-style-reference-media-guard-a1.md`
  - `r3` surfaced choose-specialist master interrupt gap; follow-up `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o9-specialist-choice-master-interrupt-a1.md`
  - `r4` removed the prior blockers and surfaced `fact_without_evidence` false-red on `service_clarify`; follow-up `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o10-service-clarify-fact-evidence-oracle-boundary-a1.md`
  - `r5` removed the prior false-red and surfaced `info_section_miss` false-red on `master_query + clarify_missing_subject`; follow-up `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o11-master-query-clarify-info-oracle-boundary-a1.md`
  - `r6` finished and was audited: `infra_valid=true`, `semantic_valid=false`, `stop_reason=max_failures_reached:1`, `pass_rate=0.9783`, `strict_pass_rate=0.9783`, `hq1_bad_turn_count=1`, `hq1_class_counts={booking_flow_break:1}`, `manual_audit=done`
  - `r6` top failure: `booking_slot_stall`, `dialog_index=4`, `turn_index=4`, message `Может быть, на утро?`
  - `r6` removed the prior `P1.6o11` blocker and opened follow-up `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o12-daypart-partial-datetime-parity-a1.md`
- refreshed deterministic preflight after `P1.6o12`:
  - `pytest -q truffles-api/tests/test_message_endpoint.py -k "daypart or same_day_info or question_without_datetime or question_like_info_does_not_match_deterministic or llm_policy_core_list_slots_keeps_context_datetime_when_expected_service_choice"` -> `12 passed, 310 deselected`
  - `pytest -q truffles-api/tests/test_message_endpoint.py` -> `322 passed, 2 warnings`
  - `pytest -q truffles-api/tests/test_booking_quality_response_guard.py` -> `67 passed`
  - `pytest -q truffles-api/tests/test_demo_salon_eval.py` -> `20 passed`
  - `pytest -q truffles-api/tests/test_booking_chaos_dialogs.py` -> `1 passed`
  - `pytest -q truffles-api/tests/test_pack_runtime_service.py` -> `22 passed`
  - `ruff check truffles-api/app/schemas/intent.py truffles-api/app/contracts/decision.py truffles-api/app/routers/webhook/decision.py truffles-api/app/routers/webhook/context_manager.py truffles-api/app/routers/webhook/response.py truffles-api/app/routers/webhook/info.py truffles-api/app/routers/webhook/booking.py truffles-api/app/services/intent_service.py truffles-api/app/services/pack_runtime_service.py truffles-api/app/services/tool_registry_service.py` -> `pass`
  - `git diff --check` -> `clean`
- `P1.6o6` is ready for one fresh guarded `dev L2` (`r7`); `lock/replay` remain blocked until that run is green and audited
- `r7` finished and was audited: `infra_valid=true`, `semantic_valid=false`, `stop_reason=max_failures_reached:1`, `pass_rate=0.9444`, `strict_pass_rate=0.9444`, `hq1_bad_turn_count=0`, `manual_audit=done`
- `r7` top failure: `expected_info_section_miss + info_section_miss + judge_fail`, `dialog_id=2`, `turn_index=3`, message `Какова стоимость услуг на новый день?`
- `r7` exposed an early owner-boundary defect: pricing interrupt under active `reschedule/booking` is blocked by `collect_slot_order_invalid` before `policy_collect_info_interrupt_owner` can arbitrate the turn
- follow-up `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o13-reschedule-pricing-interrupt-slot-order-boundary-a1.md` is now `done` on deterministic contour
- refreshed deterministic preflight after `P1.6o13`:
  - `pytest -q truffles-api/tests/test_message_endpoint.py -k "policy_collect_interrupt_arbitration_rewrites_price_question_to_info or reschedule_pricing_interrupt_bypasses_collect_slot_order_guard or collect_slot_order_invalid"` -> `4 passed, 319 deselected`
  - `pytest -q truffles-api/tests/test_llm_policy_core.py -k "derive_policy_info_refs"` -> `2 passed, 12 deselected`
  - `pytest -q truffles-api/tests/test_booking_quality_response_guard.py -k "pricing_service_clarify_fallback_under_booking_interrupt or service_clarify or canonical_state_projection"` -> `5 passed, 63 deselected`
  - `pytest -q truffles-api/tests/test_message_endpoint.py` -> `323 passed, 2 warnings`
  - `pytest -q truffles-api/tests/test_booking_quality_response_guard.py` -> `68 passed`
  - `pytest -q truffles-api/tests/test_demo_salon_eval.py` -> `20 passed`
  - `git diff --check` -> `clean`
- `r8` was `INVALID`: guarded preflight failed because `http://127.0.0.1:18184/admin/version` was unreachable; the run is not admissible proof evidence
- firebreak runtime on `:18184` was restarted from the current worktree before the next rerun
- `r9` finished and was audited: `infra_valid=true`, `semantic_valid=false`, `stop_reason=max_failures_reached:1`, `manual_audit=done`
- `r9` removed the prior `P1.6o13` blocker and surfaced a new `booking_slot_stall` family on message `В какое время можно записаться?`; runtime degraded with `semantic_temporal_scope_missing`, but the turn still landed in ordinary `booking_prompt`
- follow-up `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o14-policy-validation-degraded-collect-progress-contract-a1.md` is now `done` on deterministic contour
- refreshed deterministic preflight after `P1.6o14`:
  - `pytest -q truffles-api/tests/test_message_endpoint.py -k "semantic_temporal_scope_missing or collect_slot_order_invalid_degrades_to_datetime_prompt"` -> `2 passed, 322 deselected`
  - `pytest -q truffles-api/tests/test_booking_quality_progress_gate.py` -> `13 passed`
  - `pytest -q truffles-api/tests/test_message_endpoint.py` -> `324 passed, 2 warnings`
  - `pytest -q truffles-api/tests/test_booking_chaos_dialogs.py` -> `1 passed`
  - `pytest -q truffles-api/tests/test_booking_quality_response_guard.py` -> `68 passed`
  - `git diff --check` -> `clean`
- `r10` finished and was audited: `infra_valid=false`, `semantic_valid=false`, `stop_reason=max_failures_reached:1`, `manual_audit=done`
- `r10` removed the prior `P1.6o14` blocker and surfaced a new named-master interrupt family on message `Можно к мастеру Айгерим?`; audit reasons were `expected_info_section_miss + info_section_miss`, and the same run still carried `hardcode_core_violation` (`core_phrase_branching_detected`) in `truffles-api/app/services/pack_runtime_service.py`
- follow-up `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o15-named-master-booking-interrupt-hardcode-gate-a1.md` is now `done` on deterministic contour
- refreshed deterministic preflight after `P1.6o15`:
  - `pytest -q truffles-api/tests/test_pack_runtime_service.py -k "named_master or choose_specialist"` -> `4 passed, 20 deselected`
  - `pytest -q truffles-api/tests/test_message_endpoint.py -k "named_master or choose_specialist"` -> `3 passed, 322 deselected`
  - `pytest -q truffles-api/tests/test_booking_quality_status_gate.py -k "hardcode_core_gate or phrase_branching"` -> `7 passed, 89 deselected`
  - `pytest -q truffles-api/tests/test_booking_quality_response_guard.py` -> `68 passed`
  - `pytest -q truffles-api/tests/test_message_endpoint.py` -> `325 passed, 2 warnings`
  - `python3 - <<..._llm_quality_collect_hardcode_core_violations...` -> `violations=[]`
  - `git diff --check` -> `clean`
- full `pytest -q truffles-api/tests/test_demo_salon_eval.py` rerun was started after the `P1.6o15` delta but killed after several minutes with no output; it is not counted as proof evidence for this block
- `r11` finished and was audited: `infra_valid=false`, `semantic_valid=false`, `stop_reason=max_failures_reached:1`, `manual_audit=done`
- `r11` top failure is `booking_slot_stall`, `dialog_id=1`, `turn_index=5`, message `На какое время лучше записаться?`
- `r11` trace shows a new semantic unhappy-path gap: under active booking with `expected_reply_type=time`, policy-core/runtime kept `action=booking_prompt`, `intent=booking`, `resolution_mode=clarify_missing_time`, and repeated the generic time prompt instead of handling the question about the missing slot
- `r11` is also non-admissible proof evidence because transport degraded on the same turn (`CHATFLOW_BILLING_BLOCKED` -> `infra_valid=false`)
- follow-up `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o16-booking-time-guidance-question-owner-a1.md` is now `done` as the bounded `ask_about_requested_slot(time)` closure block
- `r11` is treated as the first surfaced instance of a broader pending-question interaction gap, not as a one-phrase time-guidance bug
- `r12` finished on the refreshed firebreak runtime and was audited: guarded preflight was valid, `stop_reason=max_failures_reached:1`, `manual_audit=done`
- `r12` did not resurface the closed `ask_about_requested_slot(time)` family from `r11`; the first failing turn became `dialog_id=1`, `turn_index=8`, message `Могу ли я записаться к Айгерим?`
- `r12` surfaced a new adjacent family instead:
  - scenario taxonomy tagged the turn as `ask_about_requested_slot` and expected `pending_question_act=ask_about_requested_slot`
  - runtime kept `pending_question_act=fill_requested_slot`, `action=booking_prompt`, `expected_reply_type=time`, and ignored the bare named specialist as a semantic owner signal
  - this means the next open gap is not `P1.6o16` again, but a deeper target-axis / bare named-specialist family
- follow-up `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o17-pending-question-target-axis-specialist-constraint-a1.md` is now deterministically closed
- `r13` finished and was audited on current-head runtime in `quality-lane=dev`, but remained non-closure evidence: `infra_valid=false`, `semantic_valid=false`, `stop_reason=max_failures_reached:1`, `dialogs_seen=1/10`
- `r13` did not resurface the closed pending-question families from `r11`/`r12`; the first failing turn became `dialog_id=1`, `turn_index=1`, message `Здравствуйте, хочу записаться на маникюр.`
- `r13` surfaced a new timeout/degraded family instead:
  - `policy_core_degrade_reason=policy_error:deadline_exceeded`, `policy_core_guard_recovery=pack_fact_fallback`, `booking_blocked_reason=intent_decomp_missing`
  - the same surface deterministically satisfies `_is_booking_request=true`, `_looks_like_info_query=true`, and `get_pack_service_hint=Маникюр`
  - this means the next open gap is a bounded timeout degraded booking-intake guard drift, reopened as `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o18-timeout-booking-intake-degrade-guard-a1.md`
- bounded follow-up `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o18-timeout-booking-intake-degrade-guard-a1.md` is now deterministically closed
- `r14` finished and was audited on refreshed current-head runtime, but remained non-closure evidence: `infra_valid=false`, `semantic_valid=false`, `stop_reason=max_failures_reached:1`, `dialogs_seen=1/10`
- `r14` did not resurface the timeout degraded booking-intake family from `r13`; the first failing turn became `dialog_id=1`, `turn_index=2`, message `Какой у вас есть слоты на эту неделю?`
- `r14` surfaced a new taxonomy/oracle false-positive instead:
  - runtime emitted `pending_question_act=ask_about_requested_slot`, `pending_question_target=time`, `tool_action=calendar.list_slots`
  - scenario taxonomy expected `pending_question_target=specialist`, `intent=master`, and `booking_interrupt info_reply` because specialist-reference regex falsely matched `у вас`
  - bounded follow-up `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o19-pending-question-specialist-regex-false-positive-a1.md` is now deterministically closed
- `r15` finished and was audited on refreshed current-head runtime: `infra_valid=true`, `semantic_valid=false`, `stop_reason=max_failures_reached:1`, `dialogs_seen=1/10`, `manual_audit=done`
- `r15` did not resurface the closed taxonomy / first-turn timeout families from `r13`/`r14`; the first failing turn became `dialog_id=1`, `turn_index=9`, message `Когда у вас есть свободные слоты?`
- `r15` surfaced a new timeout degraded pending-slot-question family instead:
  - `decision_meta` shows `policy_core_mode=degraded_fallback`, `policy_core_degrade_reason=policy_error:timeout`, `action=reply`, `intent=policy_core_guard`
  - runtime replied with generic clarify, dropped `expected_reply_type=time`, and emitted no `pending_question_interaction` / `question_contract` evidence
  - this gap was reopened as `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o20-timeout-pending-slot-question-fallback-a1.md` and deterministically closed before `r18`
- `r18` no longer stopped on the `r15` timeout family; it rolled to a new taxonomy/oracle-only family on `Есть ли свободные слоты на утро?`, reopened as `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o21-mixed-fill-slot-question-taxonomy-oracle-a1.md`
- `r19` no longer stopped on the `r18` mixed-turn expectation miss; it rolled to a new runtime family at `dialog_id=2`, `turn_index=3`, message `Проверьте, пожалуйста, мою запись на в пятницу на 20:00.`
  - runtime kept stale `expected_reply_type=service_choice`
  - `question_contract` matched a service answer and short-circuited into `consult_reply`
  - `check_booking` owner-path never executed, so scenario contract failed on `expected_meta_mismatch + expected_trace_miss + judge_fail`
  - this means the next open gap is a bounded expected-reply owner-boundary bug, reopened as `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o22-consult-cta-check-booking-owner-boundary-a1.md`
- `r20` no longer stopped on the `r19` expected-reply boundary bug; it rolled to a new taxonomy/oracle-only family at `dialog_id=1`, `turn_index=2`, message `Есть ли свободные слоты на завтра?`
  - scenario still required pure `ask_about_requested_slot` trace/meta
  - runtime legitimately consumed `завтра` as partial `datetime` answer, executed `calendar.list_slots`, and kept the resume contract
  - this means the next open gap is bounded mixed date-slot-question taxonomy/oracle drift, reopened as `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o23-mixed-date-slot-question-taxonomy-oracle-a1.md`
- `r21` no longer stopped on the `r20` mixed-date family; it rolled to a new taxonomy/oracle-only family at `dialog_id=1`, `turn_index=3`, message `А кто будет делать маникюр?`
  - scenario still required pending-question act evidence
  - runtime coherently routed the turn through `booking_info_contract`, emitted `booking_interrupt decision=info_reply`, and preserved `expected_reply_type=time`
  - this means the next open gap is bounded generic master-info interrupt taxonomy/oracle drift, reopened as `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o24-generic-master-info-interrupt-taxonomy-resume-contract-a1.md`
- `r22` and `r23` were invalid operator attempts (`invalid_preflight` / manual-audit-gate stop) and are not admissible proof evidence.
- `r24` no longer stopped on the `r21` generic-master family; it rolled to a new scenario-governance family at `dialog_id=1`, `turn_index=6`, message `Да, подтверждаю.`
  - generated booking coverage forced `check_booking` and `confirm` into one dialog
  - turn `5` executed `calendar.get_booking -> not_found` coherently
  - the next forced `confirm` turn was semantically invalid, so the blocker is scenario-governance, not runtime owner drift
  - this means the next open gap is bounded booking check/confirm scenario-governance drift, reopened as `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o25-booking-check-confirm-scenario-governance-a1.md`
- bounded follow-up `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o25-booking-check-confirm-scenario-governance-a1.md` is now deterministically closed
- `r25` no longer stopped on the `r24` forced `check_booking -> confirm` pair; it rolled to a new scenario-state family at `dialog_id=1`, `turn_index=7`, message `На какое время у вас есть слоты?`
  - scenario taxonomy still required `pending_question_act/target=time` and `question_contract expected_reply_type=time`
  - the same dialog had already passed through `handoff` / `check_booking`, so the active pending-slot contract was gone before that turn
  - runtime coherently answered with generic booking/service-choice clarify, so the blocker is scenario-state governance, not runtime owner drift
  - this means the next open gap is bounded orphan pending-question scenario-state drift, reopened as `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o26-orphan-pending-question-scenario-state-contract-a1.md`
- bounded follow-up `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o26-orphan-pending-question-scenario-state-contract-a1.md` is now deterministically closed
- `r26` no longer stopped on the orphan pending-question family; it rolled to a new scenario-state family at `dialog_id=1`, `turn_index=2`, message `Есть ли свободные слоты на завтра?`
  - scenario kept service-grounded booking turn `1` on `reply_type=service_choice`, so active `time` collect never opened in scenario state
  - the next mixed slot follow-up stayed outside the existing mixed/pending-slot family while runtime coherently listed slots and kept `expected_reply_type=time`
  - this means the next open gap is bounded booking-tag stateful normalization drift, reopened as `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o27-booking-tag-stateful-normalization-a1.md`
- bounded follow-up `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o27-booking-tag-stateful-normalization-a1.md` is now deterministically closed
- `r27` no longer stopped on the booking-tag normalization family; it rolled to a new runtime family at `dialog_id=1`, `turn_index=2`, message `На какое время есть свободные слоты?`
  - policy-core already emitted `pending_question_act=ask_about_requested_slot`, `pending_question_target=time`, and `tool_action=calendar.list_slots`
  - capability validation then rewrote the turn on `semantic_temporal_scope_missing` before the bounded guidance owner could run
  - runtime preserved only generic `booking_prompt` / `expected_reply_type=time`, so the blocker is post-LLM runtime compression, not scenario drift
  - this means the next open gap is bounded live-availability slot-question temporal-scope owner miss, reopened as `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o28-live-availability-slot-question-temporal-scope-owner-a1.md`
- bounded follow-up `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o28-live-availability-slot-question-temporal-scope-owner-a1.md` is now closed; `r28` no longer stopped on the temporal-scope owner miss and rolled to a deterministic scenario family at `dialog_id=1`, `turn_index=2`, message `Какой у вас есть свободный слот на завтра?`
  - runtime coherently answered via `calendar.list_slots` with concrete temporal scope and resume prompt
  - scenario sanitize still preserved stale `pending_question_act` / `pending_question_interaction` override evidence after retagging the turn into `mixed_fill_plus_question`
  - this reopened deterministic follow-up `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o29-mixed-slot-question-stale-expectation-scrub-a1.md`
- bounded follow-up `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o29-mixed-slot-question-stale-expectation-scrub-a1.md` is now deterministically closed; `r29` no longer stopped on the stale mixed-override family and rolled to a new deterministic scenario family at `dialog_id=1`, `turn_index=3`, message `Мне подходит утро, например, в 10:00.`
  - scenario taxonomy still preserved `slot_constraint` expectations on an explicit datetime-answer turn
  - runtime correctly matched `10:00` as slot progress and advanced collect to `name`
  - this reopens deterministic follow-up `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o30-slot-constraint-answer-normalization-a1.md`
- `P1.6o30` is now deterministically closed; `r30` no longer stopped on the explicit slot-answer taxonomy family and rolled to a new runtime observability family at `dialog_id=1`, `turn_index=3`, message `Есть ли свободные слоты на этой неделе?`
  - `llm_policy_core` correctly resolved `pending_question_act=ask_about_requested_slot`, `pending_question_target=time`, and `tool_action=calendar.list_slots`
  - runtime executed the normal `tool_registry missing_slot` path, preserved `expected_reply_type=time`, and replied `На какую дату и время вам удобно?`
  - final `decision_meta/decision_trace` lost `pending_question_interaction` evidence, so the blocker is post-tool runtime compression, not taxonomy drift
  - this reopens bounded follow-up `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o31-tool-registry-missing-slot-pending-question-evidence-a1.md`
- `P1.6o31` is now bounded-runtime closed; `r31` no longer stopped on the missing pending-question evidence family and rolled to a new deterministic taxonomy family at `dialog_id=1`, `turn_index=3`, message `Хотелось бы к Айгерим.`
  - turn `2` already passed with `source=booking_info_contract`, `pending_question_target=specialist`, and `expected_reply_type=time`
  - runtime and judge both kept active `time` collect on turn `3`
  - scenario state drifted back to `service_choice` for specialist-reference booking follow-ups, so the blocker is deterministic taxonomy/oracle drift, not runtime
  - this reopens deterministic follow-up `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o32-specialist-reference-time-collect-normalization-a1.md`
- `P1.6o53` is now bounded-runtime closed: fresh guarded `r64` no longer stopped on the specialist-followup hint-timeout family from `r63`, but surfaced a new timeout master-info interrupt gap on `Можно к мастеру Динара?`, reopened as `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o54-timeout-master-info-interrupt-name-collect-owner-a1.md`.
- `P1.6o54` is now bounded-runtime closed: fresh guarded `r65` no longer stopped on the timeout master-info interrupt family, but surfaced a new evidence-contract gap where timeout pending-slot-question guidance still audited as `fact_without_evidence`, reopened as `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o55-timeout-pending-slot-question-guidance-evidence-contract-a1.md`.
- `P1.6o55` is now bounded-runtime closed: fresh guarded `r66` no longer stopped on the timeout pending-slot-question evidence family, but surfaced a deterministic scenario-governance drift on standalone named-specialist opener `Можно к мастеру Асем?`, reopened as `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o56-standalone-named-specialist-booking-normalization-a1.md`.
- `P1.6o56` is now deterministically closed: fresh guarded `r67` no longer stopped on the standalone named-specialist family, but surfaced a new deterministic trace-expectation drift on dialog 1 turn 5 (`У вас есть слоты на утро?`) where scenario still requires `slot_constraint` pending-question trace while runtime and judge treat the turn as valid partial datetime fill and advance collect to `name`. `r64`..`r67` remain architecturally useful but non-canonical for proof because unrelated tool-evidence infra still keeps `infra_valid=false`, so `P1.6o6` now waits on deterministic closure of `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o57-question-like-daypart-fill-trace-normalization-a1.md`, then exactly one fresh guarded `r68`.

## Evidence

1. all child-block deterministic outputs
2. `/tmp/booking_quality/p1.6o6-l2-dev-20260307-a1-r1/manual_audit.md`
3. `/tmp/booking_quality/p1.6o6-l2-dev-20260307-a1-r6/summary.json`
4. `/tmp/booking_quality/p1.6o6-l2-dev-20260307-a1-r6/brief.md`
5. `/tmp/booking_quality/p1.6o6-l2-dev-20260307-a1-r6/failure_families.json`
6. `/tmp/booking_quality/p1.6o6-l2-dev-20260307-a1-r6/manual_audit.md`
7. refreshed deterministic preflight outputs from `P1.6o12`
8. future guarded proof-bundle artifacts
9. `STATE.md` closure handoff
10. `/tmp/booking_quality/p1.6o6-l2-dev-20260307-a1-r7/summary.json`
11. `/tmp/booking_quality/p1.6o6-l2-dev-20260307-a1-r7/brief.md`
12. `/tmp/booking_quality/p1.6o6-l2-dev-20260307-a1-r7/failure_families.json`
13. `/tmp/booking_quality/p1.6o6-l2-dev-20260307-a1-r7/manual_audit.md`
14. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r9/summary.json`
15. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r9/brief.md`
16. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r9/manual_audit.md`
17. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r10/summary.json`
18. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r10/brief.md`
19. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r10/manual_audit.md`
20. refreshed deterministic preflight outputs from `P1.6o14`
21. refreshed deterministic preflight outputs from `P1.6o15`
22. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r11/summary.json`
23. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r11/brief.md`
24. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r11/manual_audit.md`
25. `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o16-booking-time-guidance-question-owner-a1.md`
26. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r12/summary.json`
27. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r12/brief.md`
28. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r12/failure_families.json`
29. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r12/scenarios.json`
30. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r12/responses.jsonl`
31. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r12/trace_bundle.jsonl`
32. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r12/manual_audit.md`
33. `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o17-pending-question-target-axis-specialist-constraint-a1.md`

## Rollback

1. do not hide failure; rollback only the last regressing child block and reopen `P1.6o`
2. discard failed proof-bundle artifacts as closure evidence

## No-go

- no canary/full in this block
- no reusing partial `r20/r21/r22` as closure evidence
- no false `done` if a new family appears

## Risks/Blockers

1. A new family may still appear after `M26` implementation and reopen the proof chain again.
2. Guarded runtime/judge prerequisites may fail and block the proof bundle.
3. `r106` proves that bounded child closures still need explicit separation between matched booking time-answer turns and hours-info signals under active `time` collect; proof continuation is now blocked on deterministic `M26` closure, not on `M25`.

## Residual architecture debt (mandatory)

- Current residuals accepted in this block:
  - `P6` multi-pack proof remains outside this `demo_salon` closure bundle
- Why not in this block:
  - this block is only the `demo_salon` closure proof
- Risk if deferred:
  - `demo_salon` architecture claims remain unproven in real guarded evidence
- Linked follow-up Task Package(s):
  - `docs/TASK_PACKAGES/TP-2026-03-06-p6c2-p6-acceptance-closure-a1.md`
- Expiry/trigger to stop deferral:
  - before any canary/full rollout discussion

## Next-block contract (mandatory)

- Next block objective:
  - implement deterministic closure for the surfaced `r112` booking-confirm pricing-interrupt row (`P1.6o78`) and keep the next guarded rerun blocked until those booking-confirm pricing/service-carryover checks are green
- First deterministic check command:
  - `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r112 --status done --strict-artifacts && git diff --check`
- Blocked-by conditions:
  - `P1.6o78` row contract is not frozen
  - booking-confirm pricing-interrupt `M27` regressions are not green
  - acceptance tool-evidence infra remains red
- Owner role for closure:
  - `Hands` for rerun/evidence, `Top Architect + Brain` for closure gating

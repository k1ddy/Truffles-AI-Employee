# TP-2026-03-13-p1.6o147-timeout-slot-question-info-lock-degraded-collect-boundary-a1

- Название/цель: после surfaced `P1.6o146` закрыть bounded boundary slice, в котором active booking `time` slot-question turns under timeout slot-fill followup lose pending-question contract evidence when `basic_info_lock` and `policy_core` timeout collapse the turn into generic `policy_core_degraded_collect`.

## Canon refs

- Binding roadmap: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o135-beauty-salon-canonical-control-plane-roadmap-a1.md`
- Prior proof lane: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o146-post-p1.6o145-proof-lane-a1.md`
- Prior owner slice: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o145-active-time-slot-question-hours-info-interrupt-owner-a1.md`
- Class-level routing: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o118-class-level-closure-process-reset-a1.md`
- Parent closure program: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Main remediation: `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- State baseline: `STATE.md`

## Git context

- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `origin/main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect` remove branch + worktree after merge

## One web search (mandatory before implementation)

- Query: `site:stately.ai/docs guards context transitions retry state`
- Date/time: `2026-03-13T17:10:02+05:00`
- Sources opened:
  - official Stately/XState docs: `https://stately.ai/docs/guards`
  - official Stately/XState docs: `https://stately.ai/docs/context`
  - official Stately/XState docs: `https://stately.ai/docs/transitions`
- Source quality:
  - official product documentation
- Found reusable ideas:
  - when timeout/degrade transitions compete with contextual recovery, the boundary should pick the next state from explicit guards over preserved context rather than falling through to generic degrade handling;
  - context must keep the active slot-question contract available until the timeout transition decides whether to preserve collect continuity or truly degrade.
- Decision:
  - `reuse`
  - reuse the guarded-transition pattern for timeout slot-question continuity: preserve the active `time` slot-question contract before `basic_info_lock` and timeout degrade collapse the turn into generic `policy_core_degraded_collect`.
- Rejected options:
  - oracle-only relaxation for missing meta/trace;
  - another phrase-hardcode for `во сколько`/`свободные слоты`;
  - broad disablement of `basic_info_lock`.

## Root cause (mandatory)

- Symptom:
  - `P1.6o146` surfaces `dialog 1 / turn 2 / "Во сколько у вас свободные слоты?"` as the new first fail: runtime no longer rewrites the turn into `hours` info reply, but it still loses slot-question contract evidence and falls into generic `policy_core_degraded_collect`.
- Minimal reproduction:
  1. Open `/tmp/booking_quality/p1.6o146-l2-dev-20260313-a1-r2/responses.jsonl`.
  2. Inspect `message_id=LLM-QUAL-p1.6o146-l2-dev-20260313-a1-r2-001-02-589294`.
  3. Confirm runtime meta shows:
     - `action=booking_prompt`
     - `intent=booking`
     - `source=llm_policy_core`
     - `expected_reply_type=time`
     - `expected_reply_reason=policy_core_degraded_collect`
     - `policy_core_mode=degraded_fallback`
     - `policy_core_degrade_reason=policy_error:timeout`
     - `pending_question_act/target/interaction = null`
     - `interaction_owner=timeout booking slot-fill owner boundary`
     - `expected_reply_blocked_by_info=true`
  4. Inspect the same turn trace and confirm:
     - `question_contract decision=missed answer_error=blocked_by_info`
     - `carryover_guard decision=ignored reason=basic_info_lock`
     - `llm_policy_core ok=false error=timeout`
     - `question_contract decision=set reason=policy_core_degraded_collect expected_reply_type=time`
     - no `pending_question_interaction` trace for `ask_about_requested_slot`
  5. Confirm judge passed the response while contract-first oracle failed on missing meta/trace evidence.
- Evidence:
  - `/tmp/booking_quality/p1.6o146-l2-dev-20260313-a1-r2/summary.json`
  - `/tmp/booking_quality/p1.6o146-l2-dev-20260313-a1-r2/brief.md`
  - `/tmp/booking_quality/p1.6o146-l2-dev-20260313-a1-r2/responses.jsonl`
  - `/tmp/booking_quality/p1.6o146-l2-dev-20260313-a1-r2/trace_bundle.jsonl`
  - `/tmp/booking_quality/p1.6o146-l2-dev-20260313-a1-r2/manual_audit.json`
- Five Whys:
  1. Why did the turn fail? Because runtime omitted the expected slot-question meta/trace contract.
  2. Why was the contract omitted? Because `question_contract` was marked `missed` under `blocked_by_info` before timeout degrade selected the fallback path.
  3. Why did timeout degrade not preserve the slot-question contract? Because the active timeout slot-fill followup boundary does not re-emit slot-question continuity once `basic_info_lock` suppresses carryover and `policy_core` times out.
  4. Why is this architectural? Because boundary determinism still drops active collect context under a lexical info lock instead of resolving timeout transitions from preserved context.
  5. Why is this not oracle-only drift? Because judge passed the response text, but runtime trace itself shows the contract evidence was lost before oracle comparison.
- Root cause statement:
  - active booking `time` slot-question turns under timeout slot-fill followup currently let `basic_info_lock` plus `policy_core` timeout erase pending-question contract evidence, so runtime falls through to generic `policy_core_degraded_collect` even though the response should stay on bounded slot-question continuity.
- Fix mechanism:
  - preserve or re-derive active slot-question contract evidence through the timeout slot-fill followup boundary before generic `policy_core_degraded_collect`;
  - keep contract-first meta/trace aligned with that bounded boundary recovery.

## Reuse-first decision (mandatory)

- Reuse:
  - existing timeout owner boundary path in `truffles-api/app/routers/webhook/decision.py`
  - existing slot-question guidance contract tests
  - existing contract-first response-guard checks
- Integrate:
  - extend the current timeout boundary path; do not add a separate semantic router
- Build:
  - none

## Invariant

1. `llm_policy_core` remains the semantic owner of the turn.
2. `P1.6o146` stays the canonical proof artifact for this surfaced family.
3. Real `hours` info questions and real degraded collect turns outside this bounded contour keep their existing behavior.
4. No oracle/gate weakening is introduced.

## Scope

- bounded timeout boundary continuity for active booking `time` slot-question turns under `basic_info_lock`
- targeted runtime + guard tests
- truth sync docs/state/session

## Out of scope

- new proof rerun before deterministic closure
- broad redesign of `basic_info_lock`
- scenario-generator changes as primary fix

## Touch-list

- `truffles-api/app/routers/webhook/decision.py`
- `truffles-api/tests/test_message_endpoint.py`
- `truffles-api/tests/test_booking_quality_response_guard.py`
- `STATE.md`
- `docs/SESSIONS/SESSION-2026-02-19-llm-first-firebreak-a1.md`
- `docs/SESSION_INDEX.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o147-timeout-slot-question-info-lock-degraded-collect-boundary-a1.md`

## Plan

1. Freeze `P1.6o146` truth as timeout boundary continuity drift, not a reopen of the prior `hours` owner slice.
2. Add one bounded recovery path so active slot-question contract evidence survives `basic_info_lock` + timeout degrade on the surfaced contour.
3. Add targeted runtime + response-guard regressions.
4. Run bounded deterministic checks.
5. Sync truth and publish the next proof-lane continuation only after deterministic closure.

## DoD

1. The surfaced `P1.6o146` turn no longer falls through to generic `policy_core_degraded_collect` without slot-question meta/trace evidence.
2. Runtime emits bounded slot-question continuity evidence on the covered contour.
3. Existing `hours` info interrupts and generic degraded collects remain unchanged outside this contour.
4. Targeted runtime + response-guard tests are green.
5. Canon/state/session reflect `P1.6o146` result and `P1.6o147` closure truthfully.

## Checks

- `python3 -m py_compile truffles-api/app/routers/webhook/decision.py truffles-api/tests/test_message_endpoint.py truffles-api/tests/test_booking_quality_response_guard.py`
- `pytest -q truffles-api/tests/test_message_endpoint.py -k "slot_question and degraded_collect"`
- `pytest -q truffles-api/tests/test_booking_quality_response_guard.py -k "slot_question and degraded_collect"`
- `git diff --check`
- `SESSION_AGENT=a1 scripts/session_check.sh`

## Evidence

1. `/tmp/booking_quality/p1.6o146-l2-dev-20260313-a1-r2/summary.json`
2. `/tmp/booking_quality/p1.6o146-l2-dev-20260313-a1-r2/brief.md`
3. `/tmp/booking_quality/p1.6o146-l2-dev-20260313-a1-r2/responses.jsonl`
4. `/tmp/booking_quality/p1.6o146-l2-dev-20260313-a1-r2/trace_bundle.jsonl`
5. `/tmp/booking_quality/p1.6o146-l2-dev-20260313-a1-r2/manual_audit.json`

## Rollback

1. Revert the bounded timeout slot-question continuity recovery and its targeted tests.
2. Keep `P1.6o146` truth frozen as the surfaced family.
3. Do not reinterpret the proof result as judge/oracle-only drift.

## No-go

- no new proof rerun before deterministic closure
- no broad disablement of `basic_info_lock`
- no phrase/regex shortcuts in runtime core
- no weakening of `expected_meta_mismatch` or `expected_trace_miss` gates outside the bounded contour

## Risks / blockers

- under-scoping may keep the response text correct but still miss contract evidence
- over-scoping may suppress legitimate degraded-collect fallbacks on unrelated turns
- response-guard relaxation may accidentally mask real contract gaps if not bounded by trace/meta evidence

## Residual architecture debt (mandatory)

### Current residuals accepted in this block

- full boundary-statechart extraction is still incomplete
- timeout/degrade logic still lives inside the giant router
- proof bundle closure remains blocked on the next fresh rerun

### Why not in this block

- this block is limited to the surfaced timeout slot-question continuity family from `P1.6o146`

### Risk if deferred

- proof reruns will keep reopening degraded collect turns with correct text but missing contract evidence

### Linked follow-up Task Package(s)

- next proof-lane TP after deterministic closure of `P1.6o147`

### Expiry/trigger to stop deferral

- before any new guarded rerun after `P1.6o146`

## Next-block contract (mandatory)

### Next block objective

- run one fresh guarded proof rerun after deterministic closure of `P1.6o147`, then truthfully classify the next surfaced family.

### First deterministic check command

- `python3 -m py_compile truffles-api/app/routers/webhook/decision.py truffles-api/tests/test_message_endpoint.py truffles-api/tests/test_booking_quality_response_guard.py && git diff --check`

### Blocked-by conditions

- `P1.6o147` deterministic checks are not green
- `P1.6o146` truth is not synced in active docs/state/session

### Owner role for closure

- `Top Architect + Brain`

## Execution status

- `done`

## Execution result

- Completed `P1.6o147` on deterministic contour:
  - degraded fallback now suppresses the lexical `hours` info lock only on the bounded timeout slot-question surface of the active booking timeout slot-fill followup boundary;
  - the surfaced runtime contour now re-enters `timeout_pending_slot_question -> booking_slot_guidance` instead of generic `policy_core_degraded_collect`;
  - bounded meta/trace evidence is restored with `pending_question_act=ask_about_requested_slot`, `pending_question_target=time`, `pending_question_interaction=ask_about_requested_slot`, plus suppression observability `timeout_slot_question_info_lock_surface=true` and `suppressed_info_intents=[hours]`.
- Deterministic checks completed:
  - `python3 -m py_compile truffles-api/app/routers/webhook/decision.py truffles-api/tests/test_message_endpoint.py truffles-api/tests/test_booking_quality_response_guard.py`
  - `pytest -q truffles-api/tests/test_message_endpoint.py -k "timeout_pending_slot_question or active_time_slot_question_hours_phrase"`
  - `pytest -q truffles-api/tests/test_booking_quality_response_guard.py -k "timeout_pending_slot_guidance or timeout_booking_slot_fill_followup"`
  - `git diff --check`
  - `SESSION_AGENT=a1 scripts/session_check.sh`
- Published `P1.6o148` as the next admissible continuation.

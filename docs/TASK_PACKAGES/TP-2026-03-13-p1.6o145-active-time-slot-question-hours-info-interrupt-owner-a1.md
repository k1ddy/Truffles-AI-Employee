# TP-2026-03-13-p1.6o145-active-time-slot-question-hours-info-interrupt-owner-a1

- Название/цель: после surfaced `P1.6o144` закрыть bounded runtime-owner slice, в котором active booking time slot-question turns are deterministically overridden into `hours` info interrupt, even though `llm_policy_core` already resolved the turn as booking collect with `pending_question_target=time`.

## Canon refs

- Binding roadmap: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o135-beauty-salon-canonical-control-plane-roadmap-a1.md`
- Prior proof lane: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o144-post-p1.6o143-proof-lane-a1.md`
- Prior slot-question convergence slice: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o121-booking-slot-guidance-scenario-convergence-first-slice-a1.md`
- Prior pending soft-pass boundary slice: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o143-pending-soft-pass-booking-resume-slot-timeout-boundary-a1.md`
- Class-level routing: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o118-class-level-closure-process-reset-a1.md`
- Parent closure program: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Main remediation: `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- State baseline: `STATE.md`

## Git context

- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `origin/main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect` remove branch + worktree after merge

## One web search (mandatory before implementation)

- Query: `site:stately.ai/docs guards transitions precedence context state`
- Date/time: `2026-03-13T16:46:55+05:00`
- Sources opened:
  - official Stately/XState docs: `https://stately.ai/docs/guards`
  - official Stately/XState docs: `https://stately.ai/docs/transitions`
  - official Stately/XState docs: `https://stately.ai/docs/context`
- Source quality:
  - official product documentation
- Found reusable ideas:
  - when two candidate transitions compete, the runtime must pick the branch from explicit guard/context priority rather than from a late interrupt override;
  - context should preserve the already-selected booking contract so the boundary can compare interrupt eligibility against it.
- Decision:
  - `reuse`
  - reuse the guarded-priority approach for the active booking slot-question boundary: booking collect ownership must stay ahead of generic `hours` info interrupt when the turn is already attached to `pending_question_target=time`.
- Rejected options:
  - scenario/oracle-only re-tagging;
  - weakening the trace/meta oracle to accept the wrong runtime owner;
  - phrase-hardcoding `свободные слоты` inside core semantic routing.

## Root cause (mandatory)

- Symptom:
  - `P1.6o144` surfaces `dialog 1 / turn 2 / "Во сколько у вас свободные слоты?"` as the new first fail: runtime returns `hours` info reply under `booking_info_contract`, while scenario expects active booking slot-question continuity.
- Minimal reproduction:
  1. Open `/tmp/booking_quality/p1.6o144-l2-dev-20260313-a1-r1/responses.jsonl`.
  2. Inspect `message_id=LLM-QUAL-p1.6o144-l2-dev-20260313-a1-r1-001-02-e637c4`.
  3. Confirm runtime meta/trace show:
     - `expected_reply_type=time`
     - `expected_reply_reason=booking_prompt`
     - `llm_policy_core.intent=booking`, `action=collect`, `pending_question_act=ask_about_requested_slot`, `pending_question_target=time`
     - `policy_interrupt_contract decision=semantic_override_blocked reason_code=policy_collect_info_interrupt_owner`
     - final `action=reply`, `intent=hours`, `source=booking_info_contract`
  4. Confirm failure family is `expected_meta_mismatch + expected_trace_miss`, but primary drift is runtime owner override, not missing artifacts.
- Evidence:
  - `/tmp/booking_quality/p1.6o144-l2-dev-20260313-a1-r1/summary.json`
  - `/tmp/booking_quality/p1.6o144-l2-dev-20260313-a1-r1/brief.md`
  - `/tmp/booking_quality/p1.6o144-l2-dev-20260313-a1-r1/responses.jsonl`
  - `/tmp/booking_quality/p1.6o144-l2-dev-20260313-a1-r1/trace_bundle.jsonl`
- Five Whys:
  1. Why did the turn fail? Because runtime emitted `hours` info intent instead of booking slot-question continuity.
  2. Why did runtime do that? Because deterministic interrupt ownership overrode the already-valid booking collect plan.
  3. Why was the override wrong? Because the turn was still anchored to active booking `time` contract and asked about free slots, not about opening hours.
  4. Why is this architectural? Because boundary determinism is still acting like a second semantic engine over active booking turns.
  5. Why is this not just oracle drift? Because trace explicitly records the runtime override before oracle comparison.
- Root cause statement:
  - active booking slot-question turns currently let `hours` info-interrupt ownership outrank the already-selected booking collect contract, so runtime rewrites valid `pending_question_target=time` turns into info replies.
- Fix mechanism:
  - introduce one bounded owner-priority guard so active booking slot-question continuity wins over generic `hours` info interrupt on this contour;
  - keep contract-first trace/meta evidence aligned with that owner decision.

## Reuse-first decision (mandatory)

- Reuse:
  - existing booking interrupt / info interrupt guard path in `truffles-api/app/routers/webhook/decision.py`
  - existing contract-first trace/meta tests
- Integrate:
  - adjust owner priority in the bounded runtime path; do not add another semantic router
- Build:
  - none

## Invariant

1. `llm_policy_core` remains the semantic owner of the turn.
2. `P1.6o144` stays the canonical proof artifact for this surfaced family.
3. Real informational `hours` questions outside active booking continuity keep their existing behavior.
4. No oracle/gate weakening is introduced.

## Scope

- bounded runtime-owner priority for active booking time slot-question vs `hours` info interrupt
- targeted runtime + guard tests
- truth sync docs/state/session

## Out of scope

- new proof rerun before deterministic closure
- broad info-interrupt redesign
- scenario-generator changes as primary fix

## Touch-list

- `truffles-api/app/routers/webhook/decision.py`
- `truffles-api/tests/test_message_endpoint.py`
- `truffles-api/tests/test_booking_quality_response_guard.py`
- `STATE.md`
- `docs/SESSIONS/SESSION-2026-02-19-llm-first-firebreak-a1.md`
- `docs/SESSION_INDEX.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o145-active-time-slot-question-hours-info-interrupt-owner-a1.md`

## Plan

1. Freeze `P1.6o144` truth as runtime owner-priority drift, not oracle-only mismatch.
2. Add one bounded guard in `decision.py` so active booking `time` slot-question continuity is preserved before generic `hours` info-interrupt takeover.
3. Add targeted runtime regressions for helper arbitration and the surfaced end-to-end contour.
4. Run bounded deterministic checks.
5. Sync truth and publish the next proof-lane continuation only after deterministic closure.

## DoD

1. Active booking `pending_question_target=time` turns with slot-question relation no longer get rewritten into `hours` info interrupt on the surfaced contour.
2. Real `hours` info questions outside active slot-question continuity keep their existing interrupt behavior.
3. Targeted runtime regressions are green.
4. Canon/state/session reflect `P1.6o145` truthfully and the next admissible step returns to one fresh guarded proof rerun.

## Checks

- `python3 -m py_compile truffles-api/app/routers/webhook/decision.py truffles-api/tests/test_message_endpoint.py`
- `pytest -q truffles-api/tests/test_message_endpoint.py -k "policy_collect_interrupt_arbitration_preserves_active_time_slot_question_owner or keeps_hours_interrupt_without_active_slot_question_owner or active_time_slot_question_hours_phrase_keeps_booking_guidance"`
- `git diff --check`
- `SESSION_AGENT=a1 scripts/session_check.sh`

## Evidence

1. `/tmp/booking_quality/p1.6o144-l2-dev-20260313-a1-r1/summary.json`
2. `/tmp/booking_quality/p1.6o144-l2-dev-20260313-a1-r1/brief.md`
3. `/tmp/booking_quality/p1.6o144-l2-dev-20260313-a1-r1/responses.jsonl`
4. `/tmp/booking_quality/p1.6o144-l2-dev-20260313-a1-r1/trace_bundle.jsonl`
5. `truffles-api/app/routers/webhook/decision.py`
6. `truffles-api/tests/test_message_endpoint.py`

## Rollback

1. Revert the bounded owner-priority guard and its targeted regressions.
2. Keep `P1.6o144` truth frozen as the surfaced family.
3. Do not reinterpret the surfaced class as oracle drift without new proof evidence.

## No-go

- no scenario/oracle-only re-tagging as the primary fix
- no weakening of trace/meta expectations to accept the wrong runtime owner
- no phrase/regex hardcode for `свободные слоты`
- no new proof rerun before deterministic closure

## Risks / blockers

- under-scoping may still allow `hours` override through another active time-slot relation
- over-scoping may suppress legitimate `hours` info interrupts during booking
- targeted tests may miss a second callsite if helper arbitration is not wired consistently

## Residual architecture debt (mandatory)

### Current residuals accepted in this block

- collect-vs-info arbitration still lives in `decision.py`, not in a fully extracted boundary statechart
- `hours` lexical signal still exists and can compete with booking semantics outside this bounded contour
- proof bundle closure remains blocked on the next fresh rerun

### Why not in this block

- this block is limited to the surfaced owner-priority drift from `P1.6o144`

### Risk if deferred

- proof reruns will keep reopening the same active booking `hours` interrupt override instead of progressing to the next class

### Linked follow-up Task Package(s)

- next proof-lane TP after deterministic closure of `P1.6o145`

### Expiry/trigger to stop deferral

- before any new guarded rerun after `P1.6o144`

## Next-block contract (mandatory)

### Next block objective

- run exactly one fresh guarded proof rerun after deterministic closure of `P1.6o145` and classify the next surfaced family.

### First deterministic check command

- `python3 -m py_compile truffles-api/app/routers/webhook/decision.py truffles-api/tests/test_message_endpoint.py && git diff --check`

### Blocked-by conditions

- `P1.6o145` deterministic checks are not green
- `P1.6o144` truth is not synced in active docs/state/session

### Owner role for closure

- `Top Architect + Brain`

## Execution status

- `done`


## Execution result

- Closed the surfaced owner-priority drift on deterministic contour:
  - `truffles-api/app/routers/webhook/decision.py` now preserves active booking `time` slot-question continuity before generic `hours` info-interrupt arbitration when the turn is still attached to slot-question relation state.
  - the bounded guard is contract-first: it reads booking goal + `pending_question_target=time` + active slot-question relation state, and does not add phrase-hardcode.
  - real `hours` info interrupts outside this bounded contour remain eligible for collect-to-info takeover.
- Targeted deterministic checks are green:
  - `python3 -m py_compile truffles-api/app/routers/webhook/decision.py truffles-api/tests/test_message_endpoint.py`
  - `pytest -q truffles-api/tests/test_message_endpoint.py -k "policy_collect_interrupt_arbitration_preserves_active_time_slot_question_owner or keeps_hours_interrupt_without_active_slot_question_owner or active_time_slot_question_hours_phrase_keeps_booking_guidance"`
  - `git diff --check`
  - `SESSION_AGENT=a1 scripts/session_check.sh`
- Published `P1.6o146` as the next admissible proof-only continuation after deterministic closure of this slice.

# TP-2026-03-11-p1.6o76-initial-timeout-requested-slot-owner-a1

- Название/цель: закрыть surfaced `r105` blocker family, где после bounded `M24` closure fresh guarded `dev L2` больше не падает на standalone consult topic shift, но under active `time` collect first requested-slot follow-up `В какое время у вас есть слоты?` still times out in `llm_policy_core` and collapses into generic `booking_prompt` with `policy_core_degraded_collect` instead of preserving `ask_about_requested_slot(time)` ownership.

## Canon refs

- Parent verdict: `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o60a-remaining-closure-architecture-verdict-a1.md`
- Owner matrix: `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o60b-remaining-closure-owner-matrix-a1.md`
- Program TP: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Proof bundle: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`
- Prior child: `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o75-active-time-consult-topic-shift-service-choice-owner-a1.md`
- Related bounded closures: `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o16-booking-time-guidance-question-owner-a1.md`, `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o62-timeout-ask-about-requested-slot-booking-limit-a1.md`
- STATE: `STATE.md`
- CA: remaining closure row after `M24`

## One web search (mandatory before implementation)

- Query: `site:rasa.com/docs/rasa/forms requested slot unhappy path slot question`
- Date/time: `2026-03-11T20:18:06+05:00`
- Sources opened:
  - official Rasa docs: `https://rasa.com/docs/rasa/forms/`
- Source quality:
  - official product documentation
- Found reusable ideas:
  - when the user asks a question during active slot collection, the unhappy-path owner must still return to the same requested slot instead of silently degrading to a generic prompt;
  - fallback handling should be scoped to the active slot interaction, not only to the generic form-wide collect state.
- Decision:
  - `reuse/integrate`
  - reuse the existing requested-slot contract (`ask_about_requested_slot(time)`), timeout-degrade observability, and `question_contract` boundary; integrate one bounded first-time timeout row instead of reopening the whole timeout subsystem.
- Rejected options:
  - accepting generic `booking_prompt` as success for requested-slot follow-up turns after timeout;
  - increasing pipeline budget / timeout knobs as the primary closure mechanism;
  - phrase-fitting `в какое время` in runtime core.

## Root cause (mandatory)

- Symptom:
  - fresh guarded `dev L2` run `p1.6o6-l2-dev-20260311-a1-r105` no longer stops on `M24`, but the next first strict fail appears in dialog `1`, turn `2`, user text `В какое время у вас есть слоты?`
- Minimal reproduction:
  1. Inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r105/scenarios.json` dialog `1`, turns `1-2`.
  2. Confirm turn `1` starts booking for `маникюр` and leaves canonical resume on `reply_type=time`.
  3. Confirm turn `2` is tagged `ask_about_requested_slot` and expects `pending_question_act=ask_about_requested_slot`, `pending_question_target=time`, and `expected_reply_type=time`.
  4. Inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r105/trace_bundle.jsonl` dialog `1`, turn `2` and confirm `llm_policy_core` times out, then `policy_core_guard` emits `timeout_booking_collect` -> `degraded_collect` and only resets `question_contract.expected_reply_type=time`.
  5. Confirm the same turn has no `pending_question_interaction` / `pending_question_act=ask_about_requested_slot` trace/meta and replies with generic `На какую дату и время вам удобно?`
- Evidence:
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r105/summary.json`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r105/brief.md`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r105/scenarios.json`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r105/responses.jsonl`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r105/trace_bundle.jsonl`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r105/manual_audit.md`
- Five Whys:
  1. Why did the row fail? The bot answered a requested-slot question with generic booking collect.
  2. Why did it answer generically? `llm_policy_core` timed out and the fallback path reused generic `booking_prompt`.
  3. Why did fallback lose the requested-slot interaction? The row was inferred from the current user turn, and the timeout-degrade path only preserved `expected_reply_type=time`, not the active requested-slot act/target.
  4. Why is this not a reopen of `M11`? `M11` closed repeated-timeout escalation after the requested-slot row was already established; `r105` shows the first timeout on the first requested-slot follow-up still drops the row before that owner becomes explicit.
  5. Why is this a new bounded family? `P1.6o16`, `P1.6o62`, and `P1.6o75` closed adjacent requested-slot and topic-shift families, but the first-time timeout owner for a newly asked requested-slot question under active `time` collect was never frozen.
- Root cause statement:
  - timeout-degraded booking collect still lacks a bounded owner for first-time `ask_about_requested_slot(time)` turns: when `llm_policy_core` times out before semantic owner output is available, fallback preserves only generic `time` resume and drops the requested-slot interaction contract.
- Fix mechanism:
  - freeze matrix row `M25` for first-time timeout requested-slot follow-up ownership under active `time` collect;
  - preserve `pending_question_act=ask_about_requested_slot`, `pending_question_target=time`, `pending_question_interaction=ask_about_requested_slot`, and `question_contract.expected_reply_type=time` across the timeout-degrade path;
  - add targeted deterministic regressions for the exact sequence `booking_prompt(time) -> requested-slot question -> policy_core timeout`.

## Reuse-first decision (mandatory)

- Reuse:
  - existing requested-slot contract from `P1.6o16`
  - existing repeated-timeout owner from `P1.6o62`
  - existing timeout-degrade observability in `decision.py`
- Integrate:
  - one bounded first-time timeout row on top of the current `question_contract` / requested-slot boundary
- Do not build:
  - new timeout subsystem
  - new requested-slot router
  - oracle waiver for generic `booking_prompt`

## Execution profile

- TP mode: `implementation`
- Doc touch budget (files): `10`
- Code dominance: `required`
- Override token: `none`
- Why this profile fits: surfaced family is a bounded timeout owner gap on a single active-time requested-slot row.

## Invariant

1. `P1.6o75/M24` remains closed.
2. `P1.6o16` and `P1.6o62` requested-slot rows remain behaviorally consistent.
3. No global timeout/budget relaxation as closure for this row.
4. Timeout/degrade provenance remains explicit in `decision_meta` / `decision_trace`.
5. No phrase hardcode in runtime core.

## Scope

- publish surfaced `r105` family as matrix row `M25`
- implement bounded first-time timeout requested-slot owner only

## Out of scope

- acceptance lock/replay
- scenario generator redesign
- infra cleanup for `hardcode_core_violation` or qdrant/outbox health
- reopening `M24` after it already rolled to a different family

## Touch-list

- `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o76-initial-timeout-requested-slot-owner-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o60b-remaining-closure-owner-matrix-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o60a-remaining-closure-architecture-verdict-a1.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`
- `truffles-api/app/routers/webhook/decision.py`
- `truffles-api/tests/test_message_endpoint.py`
- `truffles-api/tests/test_booking_quality_response_guard.py`

## Plan (1..N)

1. Freeze `r105` as audited non-closure evidence and publish `M25`.
2. Confirm the exact first-time timeout degrade seam for current-turn requested-slot questions under active `time` collect.
3. Implement a bounded timeout owner that preserves requested-slot interaction evidence on fallback.
4. Add targeted deterministic regressions plus one adjacent negative case for generic time collect.
5. Run focused checks and `git diff --check`.
6. Authorize a fresh guarded `dev L2` only after deterministic green.

## DoD

1. `r105` is recorded in active docs as audited non-closure evidence.
2. First-time timeout on an active requested-slot follow-up no longer collapses into generic `booking_prompt`.
3. Final meta/trace preserve `pending_question_act=ask_about_requested_slot`, `pending_question_target=time`, and `expected_reply_type=time` with explicit timeout provenance.
4. Adjacent generic time-collect rows remain unchanged.
5. Focused deterministic timeout regressions are green.
6. Fresh guarded `dev L2` no longer stops on the `r105` family.

## Checks

- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r105 --status done --strict-artifacts`
- `pytest -q truffles-api/tests/test_message_endpoint.py -k "requested_slot and timeout"`
- `pytest -q truffles-api/tests/test_booking_quality_response_guard.py -k "requested_slot and timeout"`
- `git diff --check`

## Execution status

- `closed_bounded`
- deterministic runtime/test closure is complete in this worktree
- focused checks are green: `pytest -q truffles-api/tests/test_message_endpoint.py -k "timeout_pending_slot_question"` -> `6 passed`; `pytest -q truffles-api/tests/test_booking_quality_response_guard.py -k "timeout_pending_slot_guidance or requested_slot_contract"` -> `2 passed`; `git diff --check` -> pass; `SESSION_AGENT=a1 scripts/session_check.sh` -> pass
- fresh guarded `dev L2` `p1.6o6-l2-dev-20260311-a1-r106` rolled to a different family and no longer surfaced `M25` as the first strict fail
- direct dev replay attempts `r107`..`r109` self-blocked at manual-audit preflight and were audited as invalid/non-evidence; they do not change the bounded closure verdict

## Evidence

1. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r105/summary.json`
2. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r105/brief.md`
3. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r105/scenarios.json`
4. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r105/responses.jsonl`
5. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r105/trace_bundle.jsonl`
6. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r105/manual_audit.md`
7. `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o75-active-time-consult-topic-shift-service-choice-owner-a1.md`
8. `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o76-initial-timeout-requested-slot-owner-a1.md`
9. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r106/summary.json`
10. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r106/brief.md`
11. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r106/scenarios.json`
12. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r106/responses.jsonl`
13. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r106/trace_bundle.jsonl`
14. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r106/manual_audit.md`

## Rollback

1. Keep `P1.6o6` blocked.
2. Revert only the future bounded `M25` delta if it regresses adjacent requested-slot or generic time-collect rows.
3. Do not roll back prior bounded rows (`M24`, `M11`, `M16`).

## No-go

- no oracle weakening to accept generic `booking_prompt` on requested-slot follow-up timeout
- no global timeout-budget tuning as the only fix
- no phrase-hardcode branch outside the bounded first-time timeout requested-slot row
- no changes to unrelated consult or booking-management families

## Risks / blockers

- `r105` remains non-closure evidence because `infra_valid=false`, `semantic_valid=false`, and run-level `hardcode_core_violation` is still inherited from pre-existing core diff outside this bounded row.
- The surfaced family is timeout-driven, so repeated latency amplifiers (`dedup_backend=db_fallback`) can still make the row noisy even after the owner contract is fixed.
- The true first semantic miss starts on turn `2`, but turn `1` keeps only generic `time` resume, so regressions must preserve adjacent generic time-collect behavior carefully.

## Residual architecture debt (mandatory)

### Current residuals accepted in this block

- Timeout-degrade requested-slot ownership remains split across semantic owner, fallback guard, and `question_contract`.
- Run-level hardcode gate red is still outside this bounded row.

### Why not in this block

- `M25` is specifically about first-time timeout on requested-slot follow-up under active `time` collect. Broadening into all timeout or hardcode-gate issues would violate bounded-row closure.

### Risk if deferred

- Later guarded reruns can continue to hide requested-slot follow-up behavior behind generic degraded collect and reopen already-closed requested-slot rows ambiguously.

### Linked follow-up Task Package(s)

- `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o77-active-time-daypart-preference-info-signal-false-positive-a1.md`

### Expiry/trigger to stop deferral

- before the next guarded rerun after `M25`, if first-time requested-slot timeout still lacks deterministic green evidence.

## Next-block contract (mandatory)

### Next block objective

- implement `M26`, then rerun focused deterministic booking/time-info separation checks before any new guarded `dev L2`.

### First deterministic check command

- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r106 --status done --strict-artifacts && git diff --check`

### Blocked-by conditions

- `M26` is not published in the owner matrix
- active-time daypart-preference false-info regressions are not green
- runtime still emits `hours` / `duration` info signals on matched booking time-answer turns

### Owner role for closure

- `Hands` for deterministic implementation/evidence, `Top Architect + Brain` for guarded-rerun authorization and closure

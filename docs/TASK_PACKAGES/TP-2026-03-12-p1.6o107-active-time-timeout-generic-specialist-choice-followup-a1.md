# TP-2026-03-12-p1.6o107-active-time-timeout-generic-specialist-choice-followup-a1

- Название/цель: закрыть surfaced `M38`, где fresh proof lane `P1.6o106` показал bounded timeout-degrade owner gap на фразе `Могу я поменять специалиста?` под активным `expected_reply_type=time`: generic specialist-change side question теряет `pending_question_target=specialist`, упирается в `policy_core_timeout_degrade_booking_limit`, и уходит в `handoff/pending` вместо bounded specialist-target interrupt с сохранённым `time` resume.

## Canon refs

- Parent verdict: `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o60a-remaining-closure-architecture-verdict-a1.md`
- Closure program: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Main remediation: `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- Prior proof lane: `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o106-post-m37-proof-lane-a1.md`
- Related bounded rows: `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o40-timeout-specialist-followup-owner-recovery-a1.md`, `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o54-timeout-master-info-interrupt-name-collect-owner-a1.md`, `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o93-generic-specialist-choice-followup-owner-a1.md`, `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o99-active-name-timeout-specialist-choice-followup-a1.md`
- Owner matrix artifact: `truffles-api/app/knowledge/generic/INTERACTION_OWNER_MATRIX.yaml`
- State baseline: `STATE.md`

## Git context

- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect` remove branch + worktree after merge

## One web search (mandatory before implementation)

- Query: `site:rasa.com/docs/rasa/forms unhappy path interruptions slot filling specialist follow-up`
- Date/time: `2026-03-12T18:45:27+05:00`
- Sources opened:
  - official Rasa forms docs: `https://rasa.com/docs/rasa/forms/`
- Source quality:
  - official product documentation
- Found reusable ideas:
  - interruption/unhappy-path handling should preserve the active slot-filling objective and answer the side question without collapsing into a generic fallback owner;
  - bounded interruption handlers should keep the original requested slot active while surfacing the side-question target explicitly.
- Decision:
  - `reuse + integrate`
  - reuse the existing timeout specialist-followup and timeout master-info interrupt recovery paths;
  - integrate one bounded active-time timeout recovery for generic specialist-change side questions so the turn preserves `pending_question_target=specialist` and resumes `time` collect instead of escalating.
- Rejected options:
  - weakening scenario expectations to accept `handoff/pending` as equivalent closure for this family;
  - broad timeout/clarify-limit redesign outside the bounded generic specialist-change row;
  - adding a new phrase router in core.

## Root cause (mandatory)

- Symptom:
  - fresh guarded `dev L2` `p1.6o106-l2-dev-20260312-a1-r1` stops at dialog `2`, turn `8`, user text `Могу я поменять специалиста?`.
- Minimal reproduction:
  1. Inspect `/tmp/booking_quality/p1.6o106-l2-dev-20260312-a1-r1/scenarios.json` dialog `2`, turn `8` and confirm the turn expects a specialist-target side-question contract with `reply_type=time`.
  2. Inspect `/tmp/booking_quality/p1.6o106-l2-dev-20260312-a1-r1/responses.jsonl` for `message_id=LLM-QUAL-p1.6o106-l2-dev-20260312-a1-r1-002-08-c04ea7`.
  3. Confirm runtime returns `action=escalate`, `intent=policy_timeout_degrade`, `source=policy_core_guard`, `conversation_state=pending`, and blank `pending_question_target` instead of preserving a specialist-target interrupt.
  4. Inspect `/tmp/booking_quality/p1.6o106-l2-dev-20260312-a1-r1/trace_bundle.jsonl` for the same turn and confirm the final guard event is `llm_policy_plan_delta -> policy_core_timeout_degrade_booking_limit` rather than `timeout_specialist_followup` or `timeout_master_info_interrupt`.
  5. Compare the surfaced turn against existing active-time timeout specialist-followup and active-name timeout master-info rows to show the remaining gap is the generic specialist-change variant under active `time` collect.
- Evidence:
  - `/tmp/booking_quality/p1.6o106-l2-dev-20260312-a1-r1/summary.json`
  - `/tmp/booking_quality/p1.6o106-l2-dev-20260312-a1-r1/brief.md`
  - `/tmp/booking_quality/p1.6o106-l2-dev-20260312-a1-r1/scenarios.json`
  - `/tmp/booking_quality/p1.6o106-l2-dev-20260312-a1-r1/responses.jsonl`
  - `/tmp/booking_quality/p1.6o106-l2-dev-20260312-a1-r1/trace_bundle.jsonl`
  - `/tmp/booking_quality/p1.6o106-l2-dev-20260312-a1-r1/manual_audit.md`
- Five Whys:
  1. Why did the run fail? Because strict contract evaluation hit `expected_meta_mismatch` and `expected_trace_miss` on the generic specialist-change side question.
  2. Why did meta/trace mismatch? Because runtime escalated to `pending` via timeout clarify-limit instead of answering the side question and preserving `pending_question_target=specialist`.
  3. Why did timeout clarify-limit win? Because the existing timeout specialist-followup recovery only covers named specialist preference, while timeout master-info interrupt recovery only covers active `name` collect.
  4. Why is this not just scenario drift? Because runtime trace/meta lost the specialist-target interrupt contract and emitted `policy_core_timeout_degrade_booking_limit` / `handoff`, which is a real owner gap even before oracle comparison.
  5. Why is this a new row instead of replaying `P1.6o40`, `P1.6o54`, `P1.6o93`, or `P1.6o99`? Those rows closed named specialist follow-up, active-name timeout interrupt, or success-path generic specialist choice. `r124` surfaces the adjacent generic specialist-change question under active `time` collect and timeout degrade.
- Root cause statement:
  - active-time generic specialist-choice is still only partially compiled: on timeout/clarify-limit the runtime has no bounded owner recovery for generic specialist-change side questions, so it escalates and loses the specialist-target interrupt contract instead of answering the side question and resuming `time` collect.
- Fix mechanism:
  - freeze surfaced row `M38` in the machine-readable owner matrix;
  - extend bounded timeout recovery so active-time generic specialist-change questions preserve `pending_question_target=specialist`, `booking_interrupt_info=true`, and `expected_reply_type=time` instead of `handoff/pending`;
  - add targeted runtime/response-guard/matrix regressions.

## Reuse-first decision (mandatory)

- Reuse:
  - existing `_is_timeout_master_info_interrupt_candidate(...)`
  - existing timeout specialist-followup recovery path
  - existing `_handle_booking_interrupt(...)` specialist/master reply path
  - existing active-time specialist expectation contract in `scripts/booking_dialog_scenarios.py`
- Integrate:
  - bounded active-time timeout recovery for generic specialist-change side questions
  - targeted regressions for the surfaced phrasing and adjacent timeout specialist/master rows
- Do not build:
  - new generic specialist router
  - new fallback taxonomy

## Execution profile

- TP mode: `implementation`
- Doc touch budget (files): `10`
- Code dominance: `required`
- Override token: `none`
- Why this profile fits: `r124` surfaced a bounded timeout owner gap adjacent to already-closed timeout specialist/master rows, so reuse is available and no broader redesign is needed inside this block.

## Release safety

- Strategy: bounded timeout recovery only
- Go signals:
  - `Могу я поменять специалиста?` under active `time` collect no longer escalates to `pending/handoff`
  - runtime preserves `pending_question_target=specialist`, `booking_interrupt_info=true`, and `expected_reply_type=time`
  - adjacent named-specialist timeout recovery and active-name timeout master-info rows remain green
- No-go signals:
  - generic specialist-change turns still hit `policy_core_timeout_degrade_booking_limit`
  - unrelated timeout booking turns start inheriting specialist-target metadata
  - the fix weakens clarify-limit or handoff gates globally
- Rollback verification:
  - revert only the bounded `M38` timeout-recovery delta and targeted regressions if adjacent timeout rows regress

## Invariant

1. Existing timeout specialist-followup rows remain green.
2. Existing timeout master-info interrupt rows remain green.
3. No weakening of clarify-limit or handoff rules outside the bounded `M38` family.
4. No guarded rerun in this block.

## Scope

- publish surfaced row `M38`
- implement bounded active-time timeout recovery for generic specialist-change side questions
- add targeted runtime + response-guard + matrix regressions
- sync active docs/state/session and publish the next proof-lane block ID

## Out of scope

- fresh guarded `dev L2`
- acceptance `lock/replay/full`
- broader timeout-degrade redesign

## Touch-list

- `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o106-post-m37-proof-lane-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o107-active-time-timeout-generic-specialist-choice-followup-a1.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `STATE.md`
- `STRUCTURE.md`
- `docs/SESSIONS/SESSION-2026-02-19-llm-first-firebreak-a1.md`
- `truffles-api/app/knowledge/generic/INTERACTION_OWNER_MATRIX.yaml`
- `truffles-api/app/routers/webhook/decision.py`
- `truffles-api/app/routers/webhook/booking.py`
- `truffles-api/tests/test_message_endpoint.py`
- `truffles-api/tests/test_booking_quality_response_guard.py`
- `truffles-api/tests/test_interaction_owner_matrix_contract.py`

## Plan (1..N)

1. Freeze `r124` as audited proof evidence and publish `M38`.
2. Extend bounded timeout recovery for active-time generic specialist-change side questions without broadening unrelated timeout rows.
3. Add targeted deterministic runtime/response-guard/matrix regressions.
4. Run focused checks and `git diff --check`.
5. Publish the next proof-lane block ID only after deterministic closure is green.

## DoD

1. `r124` is recorded truthfully as surfaced `M38` in active docs and the owner matrix.
2. `Могу я поменять специалиста?` under active `time` collect no longer escalates to `pending/handoff` and instead preserves specialist-target interrupt evidence while resuming `time` collect.
3. Timeout provenance remains explicit in `decision_meta/decision_trace`.
4. Focused deterministic runtime/response-guard/matrix regressions are green.
5. The next admissible block is the fresh proof lane `P1.6o108`.

## Expensive run control (mandatory)

- Hypothesis:
  - after bounded `M38` runtime closure, a fresh guarded `dev L2` will no longer stop on dialog `2`, turn `8`, `Могу я поменять специалиста?`
- Expected measurable effect:
  - targeted deterministic checks turn green, and the next guarded proof lane either moves past this row or surfaces a different first-fail family
- Stop condition:
  - no guarded run inside this block; stop after deterministic closure and publish the next proof TP ID

## Checks

- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o106-l2-dev-20260312-a1-r1 --status done --strict-artifacts`
- `pytest -q truffles-api/tests/test_message_endpoint.py -k "timeout_specialist_followup_keeps_time_collect or timeout_master_info_interrupt"` -> `3 passed, 379 deselected`
- `pytest -q truffles-api/tests/test_booking_quality_response_guard.py -k "timeout_specialist_followup or timeout_master_info_interrupt"` -> `1 passed, 94 deselected`
- `pytest -q truffles-api/tests/test_interaction_owner_matrix_contract.py` -> `2 passed`
- `python3 -m py_compile truffles-api/app/routers/webhook/decision.py truffles-api/app/routers/webhook/booking.py truffles-api/tests/test_message_endpoint.py truffles-api/tests/test_booking_quality_response_guard.py truffles-api/tests/test_interaction_owner_matrix_contract.py` -> `pass`
- `git diff --check` -> `pass`
- `SESSION_AGENT=a1 scripts/session_check.sh` -> `Session OK`

## Execution status

- `done`
- timeout-degraded generic specialist-change turns under active `time` collect now route through the specialist-target master-info interrupt instead of `policy_core_timeout_degrade_booking_limit`
- the bounded interrupt reply now keeps the explicit `time` follow-up prompt, so the side question is answered without losing the active booking resume axis
- `M38` is closed on deterministic contour and the next admissible block is the fresh proof lane `P1.6o108`

## Evidence

1. `/tmp/booking_quality/p1.6o106-l2-dev-20260312-a1-r1/summary.json`
2. `/tmp/booking_quality/p1.6o106-l2-dev-20260312-a1-r1/brief.md`
3. `/tmp/booking_quality/p1.6o106-l2-dev-20260312-a1-r1/scenarios.json`
4. `/tmp/booking_quality/p1.6o106-l2-dev-20260312-a1-r1/responses.jsonl`
5. `/tmp/booking_quality/p1.6o106-l2-dev-20260312-a1-r1/trace_bundle.jsonl`
6. `/tmp/booking_quality/p1.6o106-l2-dev-20260312-a1-r1/manual_audit.md`
7. `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o106-l2-dev-20260312-a1-r1 --status done --strict-artifacts`
8. `truffles-api/app/routers/webhook/decision.py`
9. `truffles-api/app/routers/webhook/booking.py`
10. `truffles-api/tests/test_message_endpoint.py`
11. `truffles-api/tests/test_booking_quality_response_guard.py`
12. `truffles-api/app/knowledge/generic/INTERACTION_OWNER_MATRIX.yaml`
13. `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o108-post-m38-proof-lane-a1.md`

## Rollback

1. Keep `P1.6o106` proof truth intact.
2. Revert only the bounded `M38` timeout-recovery delta and targeted regressions if adjacent timeout rows regress.
3. Do not mutate the matrix to hide surfaced evidence.

## No-go

- no global clarify-limit or handoff rewrite
- no new phrase router in runtime core
- no second expensive run before deterministic closure is green

## Risks / blockers

- bounded timeout recovery can accidentally stamp specialist-target interrupt metadata on unrelated timeout turns if gating is too broad
- adjacent timeout specialist/master rows can regress if the new candidate detector is not kept strict
- proof remains non-canonical until unrelated infra/tool-evidence red is separately closed

## Residual architecture debt (mandatory)

### Current residuals accepted in this block

- timeout owner recovery still lives as a bounded pair across `decision.py` + `booking.py`, not in a dedicated migrated owner handler
- closure proof remains on `dev L2`; `lock/replay/full` stay blocked

### Why not in this block

- this block only closes surfaced row `M38`

### Risk if deferred

- generic specialist-change turns under timeout degrade keep escalating to handoff and hide later remaining families

### Linked follow-up Task Package(s)

- `P1.6o108` post-`M38` proof lane
- `P1.7` acceptance replay gate after the closure lane is green enough to advance

### Expiry/trigger to stop deferral

- before any next guarded `dev/acceptance` rerun beyond the `M38` proof admission

## Next-block contract (mandatory)

### Next block objective

- publish and run `P1.6o108`: prove on one fresh guarded `dev L2` that `M38` is gone from the first-fail position and truthfully admit the next family, if any.

### First deterministic check command

- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o106-l2-dev-20260312-a1-r1 --status done --strict-artifacts && pytest -q truffles-api/tests/test_message_endpoint.py -k "timeout_specialist_followup_keeps_time_collect or timeout_master_info_interrupt" && pytest -q truffles-api/tests/test_booking_quality_response_guard.py -k "timeout_specialist_followup or timeout_master_info_interrupt" && pytest -q truffles-api/tests/test_interaction_owner_matrix_contract.py && git diff --check`

### Blocked-by conditions

- `P1.6o106` proof evidence and `M38` admission are not synced truthfully into active docs and the owner matrix
- exact surfaced deterministic reproductions are not green
- `P1.6o107` deterministic closure is not synced truthfully into active docs and the owner matrix
- `P1.6o108` is not published as the next proof block

### Owner role for closure

- `Top Architect + Brain`

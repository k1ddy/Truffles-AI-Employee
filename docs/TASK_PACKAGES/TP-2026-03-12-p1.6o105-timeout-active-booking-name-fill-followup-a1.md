# TP-2026-03-12-p1.6o105-timeout-active-booking-name-fill-followup-a1

- Название/цель: закрыть surfaced `M37`, где fresh proof lane `P1.6o104` показал bounded runtime timeout-degrade gap на фразе `Меня зовут Динара.`: при активной booking state и уже заполненных `service/datetime` timeout boundary теряет slot-fill follow-up contract, допускает `policy_core_timeout_fact_fallback`, и отдаёт generic `off_topic` FACT-like reply без evidence вместо bounded booking follow-up.

## Canon refs

- Parent verdict: `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o60a-remaining-closure-architecture-verdict-a1.md`
- Closure program: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Main remediation: `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- Prior proof lane: `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o104-post-m36-proof-lane-a1.md`
- Related bounded rows: `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o18-timeout-booking-intake-degrade-guard-a1.md`, `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o55-timeout-pending-slot-question-guidance-evidence-contract-a1.md`
- Owner matrix artifact: `truffles-api/app/knowledge/generic/INTERACTION_OWNER_MATRIX.yaml`
- State baseline: `STATE.md`

## Git context

- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect` remove branch + worktree after merge

## One web search (mandatory before implementation)

- Query: `site:rasa.com/docs/rasa/forms active loop slot filling interruption resume next requested slot`
- Date/time: `2026-03-12T18:06:37+05:00`
- Sources opened:
  - official Rasa forms docs: `https://rasa.com/docs/rasa/forms/`
- Source quality:
  - official product documentation
- Found reusable ideas:
  - when an active slot-filling flow is interrupted, the runtime should incorporate any newly filled slot value and resume the next missing slot instead of switching to an unrelated fallback mode;
  - fallback/degrade logic should preserve the active collection objective rather than compressing it into a generic reply owner.
- Decision:
  - `reuse + integrate`
  - reuse existing `_update_booking_from_messages(...)` and `_next_booking_prompt(...)` slot-fill helpers;
  - integrate one bounded timeout recovery that applies the explicit slot fill before pack fact fallback becomes eligible.
- Rejected options:
  - weakening `fact_without_evidence` for timeout-degraded turns;
  - treating explicit customer-name fill as acceptable `off_topic` FACT fallback;
  - broad timeout fallback rewrite outside the bounded active-booking slot-fill family.

## Root cause (mandatory)

- Symptom:
  - fresh guarded `dev L2` `p1.6o104-l2-dev-20260312-a1-r1` stops at dialog `1`, turn `12`, user text `Меня зовут Динара.`.
- Minimal reproduction:
  1. Inspect `/tmp/booking_quality/p1.6o104-l2-dev-20260312-a1-r1/responses.jsonl` and confirm `action=reply`, `intent=off_topic`, `policy_core_mode=degraded_fallback`, `policy_core_guard_recovery=pack_fact_fallback`, and strict reason `fact_without_evidence`.
  2. Inspect `/tmp/booking_quality/p1.6o104-l2-dev-20260312-a1-r1/trace_bundle.jsonl` for the same turn and confirm timeout boundary only records `llm_policy_plan_delta -> policy_core_timeout_fact_fallback`.
  3. Confirm the same turn still has active booking state with `service=Маникюр`, `datetime=в субботу`, and `booking.active=true`.
  4. Reproduce existing helper behavior locally: `_update_booking_from_messages(...)` plus `_next_booking_prompt(...)` on that booking state and message produce a bounded follow-up prompt (`Подскажите, пожалуйста, точное время.`), proving the runtime already has the reusable pieces to stay inside booking collect.
  5. Inspect timeout-degrade branch in `truffles-api/app/routers/webhook/decision.py` and confirm explicit slot fills are not consumed before `pack_fact_fallback` eligibility is evaluated.
- Evidence:
  - `/tmp/booking_quality/p1.6o104-l2-dev-20260312-a1-r1/summary.json`
  - `/tmp/booking_quality/p1.6o104-l2-dev-20260312-a1-r1/brief.md`
  - `/tmp/booking_quality/p1.6o104-l2-dev-20260312-a1-r1/responses.jsonl`
  - `/tmp/booking_quality/p1.6o104-l2-dev-20260312-a1-r1/trace_bundle.jsonl`
  - `/tmp/booking_quality/p1.6o104-l2-dev-20260312-a1-r1/manual_audit.md`
  - `truffles-api/app/routers/webhook/decision.py`
- Five Whys:
  1. Why did the run fail? Because timeout degrade returned a FACT-like reply without evidence.
  2. Why did timeout degrade return fact fallback? Because `pack_fact_fallback` stayed eligible on this turn.
  3. Why did `pack_fact_fallback` stay eligible? Because timeout boundary never incorporated the explicit booking slot fill from `Меня зовут Динара.` before evaluating fallback branches.
  4. Why is that wrong? Because active booking state was still open and reusable slot-fill helpers could already transform the same turn into the next bounded booking follow-up.
  5. Why is this a new row instead of replaying `P1.6o18` or `P1.6o55`? `P1.6o18` protected first-turn booking intake; `P1.6o55` realigned timeout pending-slot-question owner identity. `r123` surfaces the adjacent timeout family where current-turn slot fill is dropped before the next missing-slot follow-up is derived.
- Root cause statement:
  - timeout-degraded booking fallback consumes explicit current-turn booking slot fills too late: active booking state is incomplete, but the timeout branch evaluates `pack_fact_fallback` before merging the new name into booking state and deriving the next bounded booking prompt.
- Fix mechanism:
  - freeze surfaced row `M37` in the machine-readable owner matrix;
  - add one bounded timeout recovery that applies `_update_booking_from_messages(...)` + `_next_booking_prompt(...)` before pack fact fallback when active incomplete booking state is present and the current turn fills a booking slot;
  - keep timeout provenance explicit in `decision_meta/decision_trace`;
  - add targeted runtime, response-guard, and matrix regressions.

## Reuse-first decision (mandatory)

- Reuse:
  - existing `_update_booking_from_messages(...)` slot extraction
  - existing `_next_booking_prompt(...)` follow-up derivation
  - existing timeout-degrade observability path
  - existing owner matrix contract + deterministic test
- Integrate:
  - bounded timeout booking slot-fill follow-up recovery before `pack_fact_fallback`
  - targeted regressions for explicit name fill under active booking timeout degrade
- Do not build:
  - a new generic slot parser
  - a new fallback taxonomy or oracle waiver

## Execution profile

- TP mode: `implementation`
- Doc touch budget (files): `10`
- Code dominance: `required`
- Override token: `none`
- Why this profile fits: `r123` surfaced a bounded timeout runtime family that can reuse existing booking-slot update/follow-up helpers instead of adding new semantics.

## Release safety

- Strategy: bounded timeout recovery only
- Go signals:
  - explicit name fill under active timeout-degraded booking no longer reaches `pack_fact_fallback`
  - runtime emits a bounded booking follow-up prompt with explicit timeout provenance
  - adjacent timeout info/slot-guidance rows stay green
- No-go signals:
  - timeout path starts auto-booking or mutating unrelated fallback owners
  - `fact_without_evidence` is suppressed instead of removing the FACT-like fallback
  - generic non-booking `name` turns start entering booking collect
- Rollback verification:
  - revert only the bounded timeout slot-fill follow-up recovery and targeted tests if unrelated timeout turns regress

## Invariant

1. No weakening of `fact_without_evidence` or timeout-degrade observability gates.
2. Existing timeout pending-slot-question and timeout specialist-followup closures remain green.
3. Non-booking `name` turns must not gain booking collect ownership.
4. No new expensive guarded run in this block.

## Scope

- publish surfaced row `M37`
- implement bounded timeout active-booking slot-fill follow-up recovery
- add targeted deterministic runtime + response-guard + matrix regressions
- sync active docs/state/session and publish next proof-lane block

## Out of scope

- new guarded `dev L2`
- acceptance `lock/replay/full`
- broader timeout-degrade redesign

## Touch-list

- `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o104-post-m36-proof-lane-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o105-timeout-active-booking-name-fill-followup-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o106-post-m37-proof-lane-a1.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `STATE.md`
- `STRUCTURE.md`
- `docs/SESSIONS/SESSION-2026-02-19-llm-first-firebreak-a1.md`
- `truffles-api/app/knowledge/generic/INTERACTION_OWNER_MATRIX.yaml`
- `truffles-api/app/routers/webhook/decision.py`
- `truffles-api/tests/test_message_endpoint.py`
- `truffles-api/tests/test_booking_quality_response_guard.py`
- `truffles-api/tests/test_interaction_owner_matrix_contract.py`

## Plan (1..N)

1. Freeze `r123` as audited proof evidence and publish `M37`.
2. Reuse booking slot-fill helpers so timeout degrade incorporates explicit name fill before evaluating `pack_fact_fallback`.
3. Add targeted runtime + response-guard regressions and sync the owner matrix.
4. Run focused checks and `git diff --check`.
5. Publish the next proof-lane block only after deterministic closure is green.

## DoD

1. `r123` is recorded truthfully as surfaced `M37` in active docs and the owner matrix.
2. `Меня зовут Динара.` under active timeout-degraded booking no longer reaches `pack_fact_fallback` and instead resumes the bounded booking follow-up contract.
3. Timeout provenance remains explicit in `decision_meta/decision_trace`.
4. Focused deterministic runtime/response-guard/matrix regressions are green.
5. The next admissible block is the fresh proof lane `P1.6o106`.

## Expensive run control (mandatory)

- Hypothesis:
  - after bounded `M37` runtime closure, a fresh guarded `dev L2` will no longer stop on dialog `1`, turn `12`, `Меня зовут Динара.`
- Expected measurable effect:
  - targeted deterministic checks turn green, and the next guarded proof lane either moves past this row or surfaces a different first-fail family
- Stop condition:
  - no guarded run inside this block; stop after deterministic closure and publish the next proof TP

## Checks

- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o104-l2-dev-20260312-a1-r1 --status done --strict-artifacts`
- `pytest -q truffles-api/tests/test_message_endpoint.py -k "timeout_booking_slot_fill_followup or timeout_pending_slot_question or timeout_specialist_followup"` -> `7 passed, 374 deselected`
- `pytest -q truffles-api/tests/test_booking_quality_response_guard.py -k "timeout_booking_slot_fill_followup or timeout_pending_slot_question"` -> `1 passed, 93 deselected`
- `pytest -q truffles-api/tests/test_interaction_owner_matrix_contract.py` -> `2 passed`
- `python3 -m py_compile truffles-api/app/routers/webhook/decision.py truffles-api/tests/test_message_endpoint.py truffles-api/tests/test_booking_quality_response_guard.py truffles-api/tests/test_interaction_owner_matrix_contract.py` -> `pass`
- `git diff --check` -> `pass`
- `SESSION_AGENT=a1 scripts/session_check.sh` -> `Session OK`

## Execution status

- `done`
- timeout-degrade now merges current-turn booking slot fill into active booking state before `pack_fact_fallback` is evaluated
- the next admissible block is the fresh proof lane `P1.6o106`

## Evidence

1. `/tmp/booking_quality/p1.6o104-l2-dev-20260312-a1-r1/summary.json`
2. `/tmp/booking_quality/p1.6o104-l2-dev-20260312-a1-r1/brief.md`
3. `/tmp/booking_quality/p1.6o104-l2-dev-20260312-a1-r1/scenarios.json`
4. `/tmp/booking_quality/p1.6o104-l2-dev-20260312-a1-r1/responses.jsonl`
5. `/tmp/booking_quality/p1.6o104-l2-dev-20260312-a1-r1/trace_bundle.jsonl`
6. `/tmp/booking_quality/p1.6o104-l2-dev-20260312-a1-r1/manual_audit.md`
7. `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o104-l2-dev-20260312-a1-r1 --status done --strict-artifacts`
8. `truffles-api/app/routers/webhook/decision.py`
9. `truffles-api/tests/test_message_endpoint.py`
10. `truffles-api/tests/test_booking_quality_response_guard.py`
11. `truffles-api/app/knowledge/generic/INTERACTION_OWNER_MATRIX.yaml`
12. `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o106-post-m37-proof-lane-a1.md`

## Rollback

1. Keep `P1.6o104` proof truth intact.
2. Revert only the bounded timeout slot-fill follow-up recovery and targeted regressions if adjacent timeout rows regress.
3. Do not mutate the matrix to hide surfaced evidence.

## No-go

- no oracle weakening for timeout-degraded turns
- no new generic slot parser or phrase router
- no second expensive run before deterministic closure is green

## Risks / blockers

- bounded timeout slot-fill recovery can accidentally consume unrelated self-introduction turns if booking-state gating is not strict enough
- partial-datetime bookings can still have adjacent follow-up gaps after this row closes
- proof remains non-canonical until unrelated infra/tool-evidence red is separately closed

## Residual architecture debt (mandatory)

### Current residuals accepted in this block

- timeout/degrade slot-fill recovery still lives in `decision.py`, not in a dedicated owner handler
- closure proof remains on `dev L2`; `lock/replay/full` stay blocked

### Why not in this block

- this block only closes surfaced row `M37`

### Risk if deferred

- explicit booking slot fills under timeout degrade keep collapsing to FACT-like fallback and hide later remaining families

### Linked follow-up Task Package(s)

- `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o106-post-m37-proof-lane-a1.md`
- `P1.7` acceptance replay gate after the closure lane is green enough to advance

### Expiry/trigger to stop deferral

- before any next guarded `dev/acceptance` rerun beyond the `M37` proof admission

## Next-block contract (mandatory)

### Next block objective

- publish and run `P1.6o106`: prove on one fresh guarded `dev L2` that `M37` is gone from the first-fail position and truthfully admit the next family, if any.

### First deterministic check command

- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o104-l2-dev-20260312-a1-r1 --status done --strict-artifacts && pytest -q truffles-api/tests/test_message_endpoint.py -k "timeout_booking_slot_fill_followup or timeout_pending_slot_question or timeout_specialist_followup" && pytest -q truffles-api/tests/test_booking_quality_response_guard.py -k "timeout_booking_slot_fill_followup or timeout_pending_slot_question" && pytest -q truffles-api/tests/test_interaction_owner_matrix_contract.py && git diff --check`

### Blocked-by conditions

- `P1.6o104` proof evidence and `M37` admission are not synced truthfully into active docs and the owner matrix
- exact surfaced deterministic reproductions are not green
- `P1.6o106` is not published as the next proof block

### Owner role for closure

- `Top Architect + Brain`

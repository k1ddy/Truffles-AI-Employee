# TP-2026-03-12-p1.6o87-booking-tag-requested-slot-governance-a1

- Название/цель: закрыть surfaced `M29` scenario-governance drift, где после bounded `M28` closure fresh guarded `dev L2` больше не падает на explicit exact-time `slot_compare`, но turn `На какое время у вас есть свободные слоты?` всё ещё генерируется как generic `booking/service_choice` после booking-management contamination, хотя runtime канонически держит `ask_about_requested_slot(time)`.

## Canon refs

- Parent closure program: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Main remediation: `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- Historical requested-slot owner closure: `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o16-booking-time-guidance-question-owner-a1.md`
- Historical free-slot owner closure: `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o59-live-availability-slot-question-policy-core-contract-a1.md`
- Proof lane that surfaced the row: `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o86-post-m28-proof-lane-a1.md`
- State + matrix baseline: `STATE.md`, `truffles-api/app/knowledge/generic/INTERACTION_OWNER_MATRIX.yaml`

## Git context

- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect` remove branch + worktree after merge

## One web search (mandatory before implementation)

- Query: `site:rasa.com/docs/rasa/forms requested_slot unhappy paths question while form active`
- Date/time: `2026-03-12T11:31:00+05:00`
- Sources opened:
  - official Rasa forms docs: `https://rasa.com/docs/rasa/forms/`
- Source quality:
  - official product documentation
- Found reusable ideas:
  - while a slot is active, user questions should still be interpreted relative to the requested slot rather than collapsing to an unrelated form state;
  - unhappy-path handling depends on preserving the current requested slot context truthfully.
- Decision:
  - `reuse + integrate`
  - reuse the existing requested-slot normalization patterns in `scripts/booking_dialog_scenarios.py` and integrate one bounded rule for booking-tag free-slot questions under active `time` collect.
- Rejected options:
  - weakening scenario expectations to accept stale `service_choice`;
  - runtime patching to mimic stale generator governance;
  - broad rewrite of booking-management coverage in this block.

## Root cause (mandatory)

- Symptom:
  - fresh guarded `dev L2` `p1.6o86-l2-dev-20260312-a1-r2` no longer stops on `M28`, but now fails on dialog `1`, turn `2`, text `На какое время у вас есть свободные слоты?` with `expected_meta_mismatch` / `expected_trace_miss` because generated expectations still require generic `booking/service_choice`.
- Minimal reproduction:
  1. Inspect `/tmp/booking_quality/p1.6o86-l2-dev-20260312-a1-r2/scenarios.json` dialog `1`, turn `2`.
  2. Confirm the generated turn is tagged `booking` and expects `reply_type=service_choice` plus `question_contract expected_reply_type=service_choice`.
  3. Inspect `/tmp/booking_quality/p1.6o86-l2-dev-20260312-a1-r2/responses.jsonl` dialog `1`, turn `2`.
  4. Confirm runtime produces `source=booking_slot_guidance`, `pending_question_act=ask_about_requested_slot`, `pending_question_target=time`, `expected_reply_type=time`.
  5. Compare with existing generator rules and confirm there is no bounded normalization for booking-tag requested-slot questions without date/day/range under active `time` collect.
- Evidence:
  - `/tmp/booking_quality/p1.6o86-l2-dev-20260312-a1-r2/scenarios.json`
  - `/tmp/booking_quality/p1.6o86-l2-dev-20260312-a1-r2/responses.jsonl`
  - `/tmp/booking_quality/p1.6o86-l2-dev-20260312-a1-r2/trace_bundle.jsonl`
  - `scripts/booking_dialog_scenarios.py`
  - `truffles-api/tests/test_booking_dialog_scenarios_script.py`
- Five Whys:
  1. Why did the run fail? Because scenario meta/trace still expected generic `service_choice` instead of requested-slot `time` guidance.
  2. Why did scenario expect that? Because the generated turn stayed tagged as `booking` with stale `service_choice` override.
  3. Why was it not normalized away? Because current normalization covers mixed slot questions and explicit time fills, but not booking-tag free-slot questions without temporal scope.
  4. Why is runtime already ahead? Because under active `time` collect, the question is canonically an `ask_about_requested_slot(time)` follow-up.
  5. Why is this a bounded scenario-governance row? Because runtime behavior is canonical; the remaining defect is stale generated expectation/trace state for this requested-slot surface.
- Root cause statement:
  - the scenario generator lacks a bounded normalization that converts booking-tag free-slot questions without date/day/range back to canonical `ask_about_requested_slot(time)` under active `time` collect, so stale `service_choice` expectations survive after runtime has already kept the requested-slot contract.
- Fix mechanism:
  - add bounded requested-slot normalization for booking-tag free-slot questions under active `time` collect;
  - add deterministic sanitize/repair regressions;
  - sync matrix/docs/state truthfully and keep the next expensive proof step blocked until deterministic closure is green.

## Reuse-first decision (mandatory)

- Reuse:
  - existing requested-slot / mixed-slot normalization helpers in `scripts/booking_dialog_scenarios.py`
  - existing deterministic generator test harness in `truffles-api/tests/test_booking_dialog_scenarios_script.py`
- Integrate:
  - one bounded normalization for booking-tag requested-slot questions
  - targeted sanitize/repair regressions
- Build:
  - no runtime changes planned in this block

## Release safety

- Rollout: deterministic scenario-governance only; no production runtime change
- Go signal:
  - booking-tag free-slot questions normalize to canonical `ask_about_requested_slot(time)`
  - genuine booking-intake/service-choice turns stay unchanged
  - `git diff --check` and `session_check` remain green
- No-go signal:
  - generator rewrites legitimate `check_booking` or service-intake turns into requested-slot guidance
  - runtime-related files become necessary in this block
- Rollback:
  - revert only the new generator normalization and matching tests

## Invariant

1. `M28` stays boundedly cleared by `P1.6o85` + `P1.6o86`.
2. Genuine `check_booking` and genuine booking-intake/service-choice turns remain intact.
3. Runtime behavior is untouched in this block.
4. No oracle weakening.

## Scope

- bounded scenario-governance closure for booking-tag requested-slot questions under active `time` collect
- deterministic tests
- matrix/docs/state/session sync

## Out of scope

- new guarded `dev L2`
- acceptance `lock/replay`
- runtime semantic changes

## Touch-list

- `scripts/booking_dialog_scenarios.py`
- `truffles-api/tests/test_booking_dialog_scenarios_script.py`
- `truffles-api/app/knowledge/generic/INTERACTION_OWNER_MATRIX.yaml`
- `truffles-api/tests/test_interaction_owner_matrix_contract.py`
- `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o87-booking-tag-requested-slot-governance-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o88-post-m29-proof-lane-a1.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `STATE.md`
- `STRUCTURE.md`
- `docs/SESSIONS/SESSION-2026-02-19-llm-first-firebreak-a1.md`

## Plan (1..N)

1. Publish `P1.6o87` with RCA + one web search + bounded touch-list.
2. Add bounded normalization for booking-tag requested-slot questions under active `time` collect.
3. Add deterministic sanitize/repair regressions plus matrix contract update.
4. Run targeted deterministic checks and hygiene gates.
5. Sync active docs/state/session with factual deterministic closure and next admissible proof block.

## DoD

1. Booking-tag free-slot question surfaces like `На какое время у вас есть свободные слоты?` canonicalize to requested-slot `time` guidance under active `time` collect.
2. Generated expectations preserve `pending_question_act=ask_about_requested_slot`, `pending_question_target=time`, `expected_reply_type=time`.
3. Genuine booking-intake/service-choice turns stay unchanged.
4. Deterministic tests are green.
5. `M29` is truthfully synced in matrix/docs/state with the next admissible proof block identified.

## Checks

- `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "slot_question or check_booking"`
- `pytest -q truffles-api/tests/test_interaction_owner_matrix_contract.py`
- `git diff --check`
- `SESSION_AGENT=a1 scripts/session_check.sh`

## Execution status

- `done`

## Evidence

1. `/tmp/booking_quality/p1.6o86-l2-dev-20260312-a1-r2/scenarios.json`
2. `/tmp/booking_quality/p1.6o86-l2-dev-20260312-a1-r2/responses.jsonl`
3. `/tmp/booking_quality/p1.6o86-l2-dev-20260312-a1-r2/trace_bundle.jsonl`
4. `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "slot_question or check_booking"` -> `13 passed, 80 deselected`
5. `pytest -q truffles-api/tests/test_interaction_owner_matrix_contract.py` -> `2 passed`
6. `git diff --check`
7. `SESSION_AGENT=a1 scripts/session_check.sh`
8. updated matrix/docs/state/session references

## Rollback

1. Revert only the new generator normalization and matching deterministic docs/tests.
2. Keep `P1.6o86` proof evidence intact.
3. Do not reinterpret `M29` as runtime defect if deterministic closure is rolled back.

## No-go

- no runtime edits
- no new expensive run in this block
- no weakening requested-slot contract globally
- no phrase hardcode for only one exact string

## Risks / blockers

- booking-tag requested-slot normalization sits close to genuine booking-intake/service-choice turns and must stay bounded.
- booking-management contamination can hide more than one generator defect, so the fix must avoid over-normalizing unrelated management rows.

## Residual architecture debt (mandatory)

### Current residuals accepted in this block

- Scenario ontology still relies on localized normalization heuristics for some pending-question cross-products.
- Acceptance/proof lane remains blocked until a fresh rerun confirms `M29` is gone.

### Why not in this block

- This block is a bounded scenario-governance repair, not a broader scenario ontology redesign.

### Risk if deferred

- New expensive runs will keep stopping on false-red requested-slot governance and hide later remaining families.

### Linked follow-up Task Package(s)

- `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o88-post-m29-proof-lane-a1.md`

### Expiry/trigger to stop deferral

- before any next guarded `dev L2` / `lock` / `replay`

## Next-block contract (mandatory)

### Next block objective

- run the next fresh proof admission after `M29` deterministic closure: one guarded `dev L2` to confirm the booking-tag requested-slot row is gone and to truthfully surface the next family.

### First deterministic check command

- `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "slot_question or check_booking" && pytest -q truffles-api/tests/test_interaction_owner_matrix_contract.py && git diff --check`

### Blocked-by conditions

- `P1.6o87` deterministic tests are red
- matrix/docs/state are not synced truthfully for `M29`
- `P1.6o86` proof evidence is not preserved

### Owner role for closure

- `Top Architect + Brain`

# TP-2026-03-12-p1.6o84-m27-proof-lane-and-next-row-admission-a1

- Название/цель: провести первый proof lane для resolver-driven `M27`: подтвердить bounded runtime slice на свежем guarded `dev L2` после deterministic gate и, по факту, либо закрыть `M27` на bounded contour, либо truthfully допустить следующий surfaced row вместо продолжения documentary claim.

## Canon refs

- Parent closure program: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Main remediation: `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- Redesign reset: `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o79-executable-interaction-core-redesign-reset-a1.md`
- Base canon sync: `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o80-base-canon-interaction-model-sync-a1.md`
- Machine-readable matrix: `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o81-machine-readable-owner-matrix-a1.md`
- Persisted interaction state: `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o82-persisted-interaction-state-a1.md`
- Owner resolver slice: `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o83-owner-resolver-m27-vertical-slice-a1.md`
- Tactical row: `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o78-booking-confirm-pricing-interrupt-service-choice-drift-a1.md`
- Runbook: `docs/runbooks/BOOKING_CONFIRM_VERIFY.md`
- STATE: `STATE.md`

## Git context

- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect` remove branch + worktree after merge

## One web search (mandatory before implementation)

- Query: `site:docs.pytest.org junitxml pytest official docs`
- Date/time: `2026-03-12T10:37:21+05:00`
- Sources opened:
  - official pytest docs: `https://docs.pytest.org/en/stable/how-to/output.html`
- Source quality:
  - official project documentation
- Found reusable ideas:
  - `--junitxml` is the supported machine-readable test report contract for CI/build tooling;
  - proof promotion should reuse machine-readable JUnit evidence instead of inventing a custom L1 manifest for the same deterministic gate.
- Decision:
  - `reuse + integrate`
  - reuse pytest JUnit XML as the deterministic `L1` evidence artifact for the `M27` proof lane, and integrate it into this block before the expensive `dev L2`.
- Rejected options:
  - ad-hoc handwritten JSON for deterministic evidence;
  - screenshot/manual-only proof without machine-readable `L1` artifact.

## Root cause (mandatory)

- Symptom:
  - `P1.6o83` made `M27` executable on deterministic contour, but there is still no fresh audited `dev L2` on the current worktree runtime to prove whether the resolver-driven slice removes the historical `r112` blocker or merely shifts the first strict fail.
- Minimal reproduction:
  1. Re-run the `P1.6o83` deterministic gate and confirm the targeted resolver/matrix regressions are green.
  2. Audit the frozen surfaced evidence in `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r112` and confirm it is historical, non-canonical proof input only.
  3. Inspect `/tmp/booking_quality/_index/latest_by_mode/lock.json` and confirm the latest expensive lane record is still stale/incomplete (`p1.6o6-l2-dev-20260311-a1-r112`), so the next fresh `dev L2` needs an explicit stale-index override.
  4. Check `http://127.0.0.1:18184/admin/health` and confirm the current worktree runtime must be refreshed before any new expensive run.
- Evidence:
  - `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o83-owner-resolver-m27-vertical-slice-a1.md`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r112/summary.json`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r112/responses.jsonl`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r112/trace_bundle.jsonl`
  - `/tmp/booking_quality/_index/latest_by_mode/lock.json`
  - `docs/runbooks/BOOKING_CONFIRM_VERIFY.md`
- Five Whys:
  1. Why is `M27` not closed yet? Because deterministic green alone is not closure evidence for this program.
  2. Why is there still no admissible proof? Because no fresh guarded `dev L2` has been executed on the resolver-driven runtime slice.
  3. Why can’t we just reuse `r112`? Because `r112` is pre-fix historical surfacing evidence and remains `infra_valid=false`, `semantic_valid=false`.
  4. Why does the next run require extra discipline? Because the expensive lane index still points at a stale incomplete run, and the local runtime on `:18184` is currently absent.
  5. Why is this still architecture work and not just QA? Because the redesign claim is specifically that executable `interaction_state + owner_matrix + resolver` changes the first-fail outcome under guarded runtime evidence.
- Root cause statement:
  - the redesign chain is still missing the first fresh proof-lane execution on a refreshed current worktree runtime, so `M27` remains open even though its deterministic slice is green; stale expensive-lane index state and missing runtime availability block truthful admission unless this block performs the run explicitly.
- Fix mechanism:
  - publish one proof-lane TP with explicit stale-index/runtime handling;
  - produce machine-readable `L1` JUnit evidence for the deterministic gate;
  - start the current worktree runtime on `:18184` with firebreak-compatible env overrides;
  - execute exactly one fresh guarded `dev L2` and strict audit;
  - sync canon truthfully based on whether `M27` disappears or a new first-fail row surfaces.

## Reuse-first decision (mandatory)

- Reuse:
  - `P1.6o83` targeted deterministic corpus
  - existing `scripts/llm_quality_guarded.sh` guard/index discipline
  - historical `r112` artifact bundle as frozen before-fix evidence
- Integrate:
  - one fresh local `uvicorn` runtime on `:18184`
  - one JUnit `L1` proof artifact
  - one guarded `dev` run on the same runtime
- Build:
  - no new runner, no new oracle, no new matrix row logic in this block

## Release safety

- Rollout: local proof lane only, no production rollout
- Go signal:
  - deterministic gate green with JUnit artifact
  - refreshed runtime reachable on `http://127.0.0.1:18184`
  - fresh guarded `dev L2` audited with complete artifacts
- No-go signal:
  - runtime preflight fails
  - deterministic gate regresses
  - first new expensive run is invalid/incomplete without resume path
- Rollback procedure:
  - stop the temporary local `uvicorn` runtime on `:18184`
  - keep runtime/code unchanged if the run only surfaces a new row
  - revert only doc sync if evidence mapping is wrong

## Invariant

1. `P1.6o79..P1.6o83` remain binding.
2. Exactly one fresh expensive `dev L2` is allowed in this block.
3. No `lock/replay/full` run starts from this block.
4. No manual index tampering in `/tmp/booking_quality/_index`.
5. No acceptance/bar lowering or oracle weakening to “close” `M27`.

## Scope

- publish the proof-lane TP and exact fresh-run contract
- revalidate deterministic `M27` gate and write JUnit evidence
- refresh the worktree runtime on `:18184`
- run exactly one guarded `dev L2` and strict audit
- sync active docs/state/session truthfully from the new evidence

## Out of scope

- new runtime logic beyond starting the current worktree runtime
- `lock/replay/canary/full`
- matrix migration for rows other than the first surfaced post-`M27` outcome
- model-routing parity fix

## Touch-list

- `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o84-m27-proof-lane-and-next-row-admission-a1.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o78-booking-confirm-pricing-interrupt-service-choice-drift-a1.md`
- `STATE.md`
- `STRUCTURE.md`
- `docs/SESSIONS/SESSION-2026-02-19-llm-first-firebreak-a1.md`
- `/tmp/booking_quality/p1.6o84-l1-20260312-a1-r1/pytest-junit.xml`
- `/tmp/booking_quality/p1.6o84-l2-dev-20260312-a1-r1/*`

## Plan (1..N)

1. Publish `P1.6o84` with RCA + one web search + exact guarded-run contract.
2. Re-run the `P1.6o83` deterministic gate and emit JUnit `L1` evidence.
3. Refresh the local worktree runtime on `http://127.0.0.1:18184`.
4. Execute exactly one guarded `dev L2` with stale-index override justified by the frozen pre-fix `r112`.
5. Run strict audit for the fresh expensive lane.
6. Update active docs/state/session with the factual outcome and the next admissible block.

## DoD

1. `P1.6o84` is published with exact run contract.
2. Deterministic `M27` gate is green and has machine-readable JUnit evidence.
3. Fresh current-worktree runtime is reachable on `:18184`.
4. Exactly one guarded `dev L2` is executed and audited.
5. Canon truthfully records either bounded `M27` closure or the next surfaced row.
6. Next-block contract is updated from evidence, not guessed.

## Checks

- `pytest -q truffles-api/tests/test_interaction_owner_matrix_contract.py truffles-api/tests/test_owner_resolver.py truffles-api/tests/test_message_endpoint.py -k "owner_resolver_m27 or booking_confirmation_pending or reschedule_pricing_interrupt" --junitxml /tmp/booking_quality/p1.6o84-l1-20260312-a1-r1/pytest-junit.xml`
- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r112 --status done --strict-artifacts`
- `TEST_MODE=1 scripts/llm_quality_guarded.sh --mode lock --run-id p1.6o84-l2-dev-20260312-a1-r1 --owner-file truffles-api/app/knowledge/generic/INTERACTION_OWNER_MATRIX.yaml --owner-file truffles-api/app/services/interaction_owner_matrix_service.py --owner-file truffles-api/app/services/owner_resolver.py --owner-file truffles-api/app/routers/webhook/decision.py --owner-file truffles-api/tests/test_interaction_owner_matrix_contract.py --owner-file truffles-api/tests/test_owner_resolver.py --owner-file truffles-api/tests/test_message_endpoint.py --quick-check "pytest -q truffles-api/tests/test_interaction_owner_matrix_contract.py truffles-api/tests/test_owner_resolver.py truffles-api/tests/test_message_endpoint.py -k 'owner_resolver_m27 or booking_confirmation_pending or reschedule_pricing_interrupt'" --allow-pending-previous -- --base-url http://127.0.0.1:18184 --client-slug demo_salon --count 10 --mode llm --min-turns 10 --max-turns 15 --include-media --media-mode text --media-kind photo --scenario-coverage booking,info,interrupt,handoff --batch-size 5 --retry-count 2 --retry-backoff 0.6 --min-wait 0.2 --max-wait 0.4 --jid-mode unique --allow-non-allowlist --timeout-profile realistic --timeout 30.0 --poll-timeout 25.0 --poll-interval 0.5 --trace-timeout 25.0 --trace-interval 0.5 --manager-mode simulate --manager-channel telegram --manager-actions take,resolve --manager-wait 1.0 --pending-mode ack --ack-text 'ок' --tool-hooks auto --tool-confirm-text 'да' --tool-cancel-text 'отмена' --tool-calendar-text 'проверь запись' --tool-hook-wait 0.8 --tool-hook-limit 2 --tool-evidence-policy strict --console-env /home/zhan/secrets/console-contract.env --console-mode real --history-max 20 --max-failures 1 --regression-tolerance 0.02 --lexicon-regex-delta-gate block --delta-gate-base-ref origin/main --hardcode-core-gate block --hardcode-core-base-ref origin/main --run-economy-gate block --run-economy-base-ref origin/main --manual-audit-gate block --forensic-sla-gate block --oracle-conflict-gate block --secret-transport-gate block --scenario-governance-gate block --scenario-governance-registry /tmp/booking_quality/_scenario_governance_registry.json --judge-mode all --judge-sample 0.1 --judge-model gpt-4o-mini --judge-base-url https://api.openai.com --judge-timeout 25.0 --judge-max-tokens 320 --judge-cache-max-entries 5000 --quality-lane dev`
- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o84-l2-dev-20260312-a1-r1 --status done --strict-artifacts`
- `TEST_MODE=1 scripts/llm_quality_guarded.sh --mode lock --run-id p1.6o84-l2-dev-20260312-a1-r2 --owner-file truffles-api/app/knowledge/generic/INTERACTION_OWNER_MATRIX.yaml --owner-file truffles-api/app/services/interaction_owner_matrix_service.py --owner-file truffles-api/app/services/owner_resolver.py --owner-file truffles-api/app/routers/webhook/decision.py --owner-file truffles-api/tests/test_interaction_owner_matrix_contract.py --owner-file truffles-api/tests/test_owner_resolver.py --owner-file truffles-api/tests/test_message_endpoint.py --quick-check "pytest -q truffles-api/tests/test_interaction_owner_matrix_contract.py truffles-api/tests/test_owner_resolver.py truffles-api/tests/test_message_endpoint.py -k 'owner_resolver_m27 or booking_confirmation_pending or reschedule_pricing_interrupt'" --allow-pending-previous -- --base-url http://127.0.0.1:18184 --client-slug demo_salon --count 10 --mode llm --min-turns 10 --max-turns 15 --include-media --media-mode text --media-kind photo --scenario-coverage booking,info,interrupt,handoff --batch-size 5 --retry-count 2 --retry-backoff 0.6 --min-wait 0.2 --max-wait 0.4 --jid-mode unique --allow-non-allowlist --timeout-profile realistic --timeout 30.0 --poll-timeout 25.0 --poll-interval 0.5 --trace-timeout 25.0 --trace-interval 0.5 --manager-mode simulate --manager-channel telegram --manager-actions take,resolve --manager-wait 1.0 --pending-mode ack --ack-text 'ок' --tool-hooks auto --tool-confirm-text 'да' --tool-cancel-text 'отмена' --tool-calendar-text 'проверь запись' --tool-hook-wait 0.8 --tool-hook-limit 2 --tool-evidence-policy strict --console-env /home/zhan/secrets/console-contract.env --console-mode real --history-max 20 --max-failures 1 --regression-tolerance 0.02 --lexicon-regex-delta-gate block --delta-gate-base-ref origin/main --hardcode-core-gate block --hardcode-core-base-ref origin/main --run-economy-gate block --run-economy-base-ref origin/main --manual-audit-gate block --forensic-sla-gate block --oracle-conflict-gate block --secret-transport-gate block --scenario-governance-gate block --scenario-governance-registry /tmp/booking_quality/_scenario_governance_registry.json --judge-mode all --judge-sample 0.1 --judge-model gpt-4o-mini --judge-base-url https://api.openai.com --judge-timeout 25.0 --judge-max-tokens 320 --judge-cache-max-entries 5000 --quality-lane dev`
- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o84-l2-dev-20260312-a1-r2 --status done --strict-artifacts`
- `git diff --check`
- `SESSION_AGENT=a1 scripts/session_check.sh`

## Execution status

- `done`
- deterministic `L1` gate is green with machine-readable JUnit evidence:
  - `pytest ... --junitxml /tmp/booking_quality/p1.6o84-l1-20260312-a1-r1/pytest-junit.xml` -> `4 passed`
- historical surfaced blocker `r112` was re-audited as frozen non-canonical input:
  - `artifact_integrity.valid=true`
  - `infra_valid=false`
  - `semantic_valid=false`
- first guarded attempt `p1.6o84-l2-dev-20260312-a1-r1` failed at runtime fingerprint preflight (`admin_version_unreachable`) because the detached local `:18184` runtime did not survive outside its launch shell; strict audit recorded it as non-canonical preflight failure and it was not used as proof evidence
- refreshed live worktree runtime on `http://127.0.0.1:18184` served `git_commit=21afcead084aa3fdfc5702c5547fb40d74970b5a`, matching current `HEAD`
- fresh guarded `dev L2` `p1.6o84-l2-dev-20260312-a1-r2` completed and was strictly audited:
  - `artifact_integrity.valid=true`
  - `infra_valid=false`
  - `semantic_valid=false`
  - `stop_reason=max_failures_reached:1`
- bounded proof result:
  - `M27` is no longer the first strict fail; dialog `1` preserved booking-side info interrupts for pricing/promo/duration while keeping `expected_reply_type=time`
  - the new first fail is dialog `1`, turn `8`, user text `Можно записаться на 15:00?`
  - runtime canonically consumed the explicit exact-time candidate and advanced to `expected_reply_type=name`
  - generated scenario still expected `pending_question_act=slot_compare`, `pending_question_target=time`, and `expected_reply_type=time`
  - this surfaced family is a scenario-governance/oracle drift adjacent to `P1.6o45` / `M22`, not a remaining `M27` runtime carryover defect

## Evidence

1. `/tmp/booking_quality/p1.6o84-l1-20260312-a1-r1/pytest-junit.xml`
2. `/tmp/booking_quality/p1.6o84-l2-dev-20260312-a1-r1/summary.json`
3. `/tmp/booking_quality/p1.6o84-l2-dev-20260312-a1-r1/manual_audit.md`
4. `/tmp/booking_quality/p1.6o84-l2-dev-20260312-a1-r2/summary.json`
5. `/tmp/booking_quality/p1.6o84-l2-dev-20260312-a1-r2/brief.md`
6. `/tmp/booking_quality/p1.6o84-l2-dev-20260312-a1-r2/scenarios.json`
7. `/tmp/booking_quality/p1.6o84-l2-dev-20260312-a1-r2/responses.jsonl`
8. `/tmp/booking_quality/p1.6o84-l2-dev-20260312-a1-r2/trace_bundle.jsonl`
9. `/tmp/booking_quality/p1.6o84-l2-dev-20260312-a1-r2/manual_audit.md`
10. updated `STATE.md` / remediation docs / session log

## Rollback

1. Stop the temporary `uvicorn` process on `:18184`.
2. Revert only the doc sync if the recorded outcome is wrong.
3. Do not touch `P1.6o83` runtime code in this block unless a separate bug is proven.

## No-go

- no second fresh expensive `dev L2` in this block
- no `lock/replay/full`
- no `--allow-repeat-fingerprint`
- no baseline update from the fresh `dev` run
- no manual edit of audit/index artifacts

## Risks / blockers

- `:18184` is currently down, so the runtime refresh is a real precondition, not paperwork.
- Guarded wrapper will require `--allow-pending-previous` because the latest lock index entry is stale/incomplete `r112`.
- The fresh run may surface a new row immediately; that is valid outcome and must not be hidden behind “M27 probably fixed”.

## Residual architecture debt (mandatory)

### Current residuals accepted in this block

- `decision.py` still hosts the bounded resolver hook instead of a fully extracted interaction engine.
- Only `M27` has executable runtime row data.
- fresh `r2` surfaced a new scenario-governance row; acceptance `PG checklist` is still blocked until that row is closed.

### Why not in this block

- The first required proof is narrower: verify that the resolver-driven slice changes the expensive-lane outcome before promoting to acceptance work.

### Risk if deferred

- the new `slot_compare` exact-time scenario drift would keep producing false-red expensive runs even though the resolver-driven `M27` runtime slice is already behaving canonically.

### Linked follow-up Task Package(s)

- `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o85-slot-compare-explicit-time-fill-scenario-governance-a1.md`

### Expiry/trigger to stop deferral

- before any `lock/replay` admission or before claiming the owner-resolver architecture is generally validated beyond `M27`

## Next-block contract (mandatory)

### Next block objective

- close the newly surfaced scenario-governance family from `r2`: question-like explicit exact-time candidate `Можно записаться на 15:00?` must normalize to canonical `time -> name` slot fill under active `slot_compare/time` context instead of remaining a stale `slot_compare/time` expectation.

### First deterministic check command

- `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "slot_compare and explicit_time" && git diff --check`

### Blocked-by conditions

- `P1.6o84` fresh guarded `dev L2` evidence is not synced into active docs
- deterministic scenario/oracle regression for the new family is not reproduced locally
- `M27` bounded closure is not recorded truthfully before opening the next row

### Owner role for closure

- `Top Architect + Brain`

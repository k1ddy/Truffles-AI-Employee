# TP-2026-03-13-p1.6o136-post-p1.6o134-fresh-proof-rerun-a1

- Название/цель: после binding roadmap reset и deterministic closure `P1.6o134` выполнить ровно один fresh guarded `dev L2`, чтобы truthfully surface-ить следующий admissible class на recovered runtime и только потом открывать structural implementation.

## Canon refs

- Binding roadmap: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o135-beauty-salon-canonical-control-plane-roadmap-a1.md`
- Parent closure program: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Main remediation: `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- Process reset / class triage: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o118-class-level-closure-process-reset-a1.md`
- Prior proof-process admission: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o134-audited-infra-non-canonical-lock-retry-admission-a1.md`
- State baseline: `STATE.md`

## Git context

- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `origin/main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect` remove branch + worktree after merge

## One web search (mandatory before implementation)

- Query: `site:www.uvicorn.org/settings uvicorn command line host port settings admin version`
- Date/time: `reused official proof-lane reference from 2026-03-13`
- Sources opened:
  - official Uvicorn settings docs: `https://www.uvicorn.org/settings/`
- Source quality:
  - official product documentation
- Found reusable ideas:
  - proof runtime startup should remain explicit and reproducible via CLI flags;
  - runtime parity must be recorded before the guarded rerun begins.
- Decision:
  - `reuse`
  - reuse the established `:18184` proof-runtime pattern and record `/admin/version == HEAD` before the single rerun.
- Rejected options:
  - detached runtime startup that hides parity;
  - multiple expensive reruns in the same block.

## Root cause (mandatory)

- Symptom:
  - `P1.6o134` restored proof-lane admission, but the worktree still lacks one fresh admissible rerun that decides what the next truthful remaining class is on the recovered runtime.
- Minimal reproduction:
  1. Read `STATE.md` at the post-`P1.6o134` truth.
  2. Confirm the current next admissible step is one fresh proof rerun on recovered runtime.
  3. Confirm `/admin/version` on `127.0.0.1:18184` matches current `HEAD`.
  4. Execute exactly one guarded `dev L2` rerun with `--allow-non-canonical-lock-retry`.
  5. Strictly audit the run and classify the first surfaced class under `P1.6o118` rules.
- Evidence:
  - `STATE.md`
  - `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o134-audited-infra-non-canonical-lock-retry-admission-a1.md`
  - `/tmp/booking_quality/p1.6o132-l2-dev-20260313-a1-r1/summary.json`
  - `/tmp/booking_quality/p1.6o132-l2-dev-20260313-a1-r1/manual_audit.json`
- Five Whys:
  1. Why is the worktree still blocked? Because `P1.6o134` only re-opened admissible proof-lane mechanics.
  2. Why is another local patch forbidden before rerun? Because there is no fresh truthful signal for the next class yet.
  3. Why must the runtime be fingerprint-verified? Because non-parity would make the run non-canonical again.
  4. Why exactly one run? Because the goal is class routing, not another expensive loop.
  5. Why use `--allow-non-canonical-lock-retry`? Because the previous same-fingerprint lock was infra-invalid and `P1.6o134` explicitly made this audited retry admissible.
- Root cause statement:
  - the remaining gap after `P1.6o134` is proof evidence, not deterministic code: without one fresh fingerprint-verified rerun, the worktree cannot truthfully choose the next structural implementation block.
- Fix mechanism:
  - verify `HEAD` and runtime parity;
  - execute exactly one guarded `dev L2` rerun with the audited non-canonical lock retry override;
  - strict-audit the run;
  - publish the next structural implementation block only from the surfaced class.

## Reuse-first decision (mandatory)

- Reuse:
  - existing `ops/diagnose.py llm-quality` proof-lane command contour
  - existing strict audit command `python3 ops/diagnose.py llm-quality-audit`
  - recovered live runtime on `127.0.0.1:18184`
- Integrate:
  - no code changes before the rerun result is known
- Build:
  - none in this block

## Release safety

- Rollout: none; local proof-only block
- Go signal:
  - `/admin/version.git_commit == HEAD`
  - rerun artifacts are audit-complete enough for strict audit
  - surfaced class is routed truthfully without row-local patching
- No-go signal:
  - runtime fingerprint mismatch
  - invalid/incomplete rerun artifacts
  - attempt to patch runtime/oracle before the rerun result is classified
- Rollback:
  - stop the rerun path, keep the current code intact, and record invalid/incomplete proof truthfully

## Invariant

1. `P1.6o134` deterministic/process closure remains intact.
2. No new runtime/oracle/audit code changes happen in this block.
3. Exactly one fresh guarded rerun is allowed here.
4. The surfaced result is routed by class, not by `Mxx` reflex.

## Scope

- verify `HEAD` and runtime parity
- execute exactly one fresh guarded `dev L2`
- strict audit the run
- sync docs/state/session with the surfaced class and next admissible structural block

## Out of scope

- structural implementation itself
- acceptance `lock/replay/full`
- any second expensive rerun in the same block

## Touch-list

- `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o136-post-p1.6o134-fresh-proof-rerun-a1.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `STATE.md`
- `docs/SESSIONS/SESSION-2026-02-19-llm-first-firebreak-a1.md`
- `docs/SESSION_INDEX.md`
- `/tmp/booking_quality/p1.6o136-l2-dev-20260313-a1-r1/**`
- `/tmp/booking_quality/p1.6o136-l2-dev-20260313-a1-r2/**`
- `/tmp/booking_quality/p1.6o136-l2-dev-20260313-a1-r3/**`

## Plan (1..N)

1. Verify `HEAD` and runtime fingerprint parity.
2. Run exactly one fresh guarded `dev L2` rerun with `--allow-non-canonical-lock-retry`.
3. Strict-audit the rerun.
4. Classify the surfaced first fail via `P1.6o118` rules.
5. Sync docs/state/session and open the next structural implementation block.

## DoD

1. Invalid preflight attempts `r1` and `r2` are strictly audited and the one admissible fresh rerun is executed as `p1.6o136-l2-dev-20260313-a1-r3`.
2. Runtime fingerprint parity with `HEAD` is recorded.
3. Strict audit completes for `r1`, `r2`, and surfaced rerun `r3`.
4. Active docs/state/session record the surfaced class truthfully.
5. The next code block is selected only from the surfaced class.

## Checks

- `git rev-parse HEAD`
- `curl -sf http://127.0.0.1:18184/admin/version`
- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o136-l2-dev-20260313-a1-r1 --status done --strict-artifacts`
- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o136-l2-dev-20260313-a1-r2 --status done --strict-artifacts`
- `python3 ops/diagnose.py llm-quality --base-url http://127.0.0.1:18184 --client-slug demo_salon --count 10 --mode llm --min-turns 10 --max-turns 15 --include-media --media-mode text --media-kind photo --scenario-coverage booking,info,interrupt,handoff --batch-size 5 --retry-count 2 --retry-backoff 0.6 --min-wait 0.2 --max-wait 0.4 --jid-mode unique --allow-non-allowlist --timeout-profile realistic --timeout 30.0 --poll-timeout 25.0 --poll-interval 0.5 --trace-timeout 25.0 --trace-interval 0.5 --manager-mode simulate --manager-channel telegram --manager-actions take,resolve --manager-wait 1.0 --pending-mode ack --ack-text 'ок' --tool-hooks auto --tool-confirm-text 'да' --tool-cancel-text 'отмена' --tool-calendar-text 'проверь запись' --tool-hook-wait 0.8 --tool-hook-limit 2 --tool-evidence-policy strict --reset-before-dialog --console-env /home/zhan/secrets/console-contract.env --console-mode real --output-dir /tmp/booking_quality/p1.6o136-l2-dev-20260313-a1-r3 --run-id p1.6o136-l2-dev-20260313-a1-r3 --history-max 20 --quality-lane dev --fail-on-thresholds --max-failures 1 --regression-tolerance 0.02 --max-post-llm-semantic-rewrite-rate 0.0 --max-keyword-override-rate 0.0 --lexicon-regex-delta-gate block --delta-gate-base-ref origin/main --hardcode-core-gate block --hardcode-core-base-ref origin/main --run-economy-gate block --run-economy-base-ref origin/main --manual-audit-gate block --forensic-sla-gate block --oracle-conflict-gate block --secret-transport-gate block --scenario-governance-gate block --scenario-governance-registry /tmp/booking_quality/_scenario_governance_registry.json --judge-mode all --judge-sample 0.1 --judge-model gpt-4o-mini --judge-base-url https://api.openai.com --judge-timeout 25.0 --judge-max-tokens 320 --judge-cache-max-entries 5000 --allow-non-canonical-lock-retry`
- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o136-l2-dev-20260313-a1-r3 --status done --strict-artifacts`
- `git diff --check`
- `SESSION_AGENT=a1 scripts/session_check.sh`

## Execution status

- `done`

## Execution result

- `r1` stopped as `invalid_preflight` because auto lane inference required the acceptance chain controller; fix was explicit `--quality-lane dev`.
- `r2` stopped as `invalid_preflight` because the manual-audit gate still had pending `r1`; both `r1` and `r2` were then closed by strict manual audit.
- The one admissible fresh rerun is `p1.6o136-l2-dev-20260313-a1-r3` on fingerprint-verified runtime `:18184`.
- `r3` remained non-canonical as a fail-fast prefix (`infra_valid=false`, `semantic_valid=false`, `artifact_integrity.valid=true`, `run_integrity_reasons=[run_completion_gap]`), but the surfaced first fail is admissible because the failing turn has full `decision_meta` + `decision_trace` evidence.
- Surfaced first fail: `Track A` expectation/oracle drift, not a runtime owner reopen.
- Failing family: `reason:expected_trace_miss|type:turn|category:expectation|stage:booking|state:bot_active` at `dialog 1 / turn 6 / "Я хочу записаться к Айгерим."`.
- Runtime truth on the failing turn: `expected_reply_type=time`, `pending_question_target=specialist`, `active_question_relation=referent_followup`, `pending_question_interaction=specialist_followup`, `pending_question_owner=booking_specialist_followup`, with `question_contract` + `booking prompt` trace but no matching handwritten `pending_question_interaction` trace entry.
- Therefore the next admissible structural block is `P1.6o137` contract-compiler first slice; no new runtime-owner patch or boundary-statechart work is admitted here.

## Evidence

1. `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o136-post-p1.6o134-fresh-proof-rerun-a1.md`
2. `/tmp/booking_quality/p1.6o136-l2-dev-20260313-a1-r1/manual_audit.json`
3. `/tmp/booking_quality/p1.6o136-l2-dev-20260313-a1-r2/manual_audit.json`
4. `/tmp/booking_quality/p1.6o136-l2-dev-20260313-a1-r3/summary.json`
5. `/tmp/booking_quality/p1.6o136-l2-dev-20260313-a1-r3/brief.md`
6. `/tmp/booking_quality/p1.6o136-l2-dev-20260313-a1-r3/manual_audit.json`
7. `/tmp/booking_quality/p1.6o136-l2-dev-20260313-a1-r3/failure_families.json`
8. `/tmp/booking_quality/p1.6o136-l2-dev-20260313-a1-r3/responses.jsonl`

## Rollback

1. Stop the local runtime/rerun path.
2. Record invalid/incomplete artifacts truthfully.
3. Do not publish a structural implementation block from non-canonical evidence.

## No-go

- no local runtime/oracle patch before this rerun finishes
- no second expensive rerun in this block
- no class routing from stale `P1.6o132` or `P1.6o134` artifacts alone
- no statechart work before the rerun-selected structural block

## Risks / blockers

- runtime can still degrade or timeout mid-run
- hardcode-core gate can remain red even if the first fail moves
- the surfaced class can still belong to a broader known structural track rather than a narrow local defect

## Residual architecture debt (mandatory)

### Current residuals accepted in this block

- surfaced `Track A` class is now known, but the structural compiler slice is not implemented yet
- runtime/oracle truth is still split until `P1.6o137` lands

### Why not in this block

- this block was proof-only; implementation begins in `P1.6o137`

### Risk if deferred

- the worktree would keep carrying stale handwritten expectation logic even after fresh proof already identified the structural class

### Linked follow-up Task Package(s)

- `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o137-track-a-contract-compiler-active-time-specialist-followup-a1.md`

### Expiry/trigger to stop deferral

- immediately after `P1.6o136` finishes and the surfaced class is known

## Next-block contract (mandatory)

### Next block objective

- publish and start `P1.6o137`, the first contract-compiler slice selected by the surfaced `Track A` expectation/oracle class.

### First deterministic check command

- `python3 -m py_compile truffles-api/app/services/scenario_contract_compiler.py scripts/booking_dialog_scenarios.py ops/diagnose.py && git diff --check`

### Blocked-by conditions

- `P1.6o137` deterministic compiler slice is not green
- `P1.6o136` surfaced class is not synced truthfully in active docs/state/session

### Owner role for closure

- `Top Architect + Brain`

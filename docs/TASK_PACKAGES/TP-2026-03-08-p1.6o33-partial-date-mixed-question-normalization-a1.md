# TP-2026-03-08-p1.6o33-partial-date-mixed-question-normalization-a1.md

- Название/цель: закрыть новый `r32` deterministic blocker family, где partial-date booking turns (`Можно ли записаться на завтра?`) under active `reply_type=time` остаются в `mixed_fill_plus_question` с `pending_question_target=time`, хотя runtime и judge трактуют их как обычное продолжение time collect.
- Parent TP: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Canon refs: `AGENTS.md`, `STATE.md`, `STRUCTURE.md`, `STRATEGY/REQUIREMENTS.md`, `docs/runbooks/BOOKING_CONFIRM_VERIFY.md`, `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`, `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o32-specialist-reference-time-collect-normalization-a1.md`
- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect remove worktree + branch after merge`

## One web search (mandatory before implementation)

- Query: `site:rasa.com/docs/rasa/forms requested_slot partial date follow up question yes no`
- Time (UTC): `2026-03-08T12:00:00Z`
- Sources:
  - `https://rasa.com/docs/rasa/forms/`
- Ready solutions found:
  - when a form is active, inputs that do not create a distinct interruption should continue the requested-slot contract; question-shaped follow-ups can still be handled as slot-progress if they fill or constrain the requested slot rather than opening a separate unhappy-path owner.
- Decision (`reuse/integrate/build`): `reuse + integrate`
  - reuse the existing active `reply_type=time` collect contract;
  - integrate deterministic normalization so partial-date booking follow-ups do not keep `mixed_fill_plus_question` contract when they do not express an availability question family;
  - build no new runtime logic until deterministic taxonomy is aligned.
- Rejected options:
  - changing runtime to emit pending-question evidence for a family that judge already accepts as ordinary collect;
  - weakening oracle to accept both `mixed_fill_plus_question` and ordinary collect on the same turn;
  - phrase-hardcoding runtime prompts.

## Root cause (mandatory)

- Symptom:
  - guarded `dev L2` rerun `p1.6o6-l2-dev-20260308-a1-r32` progressed beyond the `P1.6o32` family, then stopped on dialog `1`, turn `4`, text `Можно ли записаться на завтра?` with `expected_meta_mismatch`.

- Minimal reproduction:
  1. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r32/scenarios.json` for dialog `1`, turns `1-4`
  2. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r32/responses.jsonl` for message id `LLM-QUAL-p1.6o6-l2-dev-20260308-a1-r32-001-04-...`
  3. compare scenario expectation `mixed_fill_plus_question + pending_question_target=time` against runtime meta/trace
  4. inspect `scripts/booking_dialog_scenarios.py` normalization for mixed slot-question tags after active time collect

- Evidence:
  - runtime:
    - deterministically matched `answer_slot=datetime`, `answer_value=завтра`
    - kept `expected_reply_type=time`
    - judge marked the turn `pass`
  - runtime did not emit `pending_question_target=time`; it treated the turn as ordinary booking continuation and then degraded on `policy_validation:collect_slot_order_invalid`
  - scenario still expected `mixed_fill_plus_question` semantics solely because the user turn was question-shaped and contained a partial date
  - therefore the first surfaced blocker is deterministic taxonomy overclaim, not a new runtime defect.

- Five Whys:
  1. Why did `r32` fail: scenario expected pending-question meta on a turn runtime treated as ordinary collect.
  2. Why did scenario expect that: tag `mixed_fill_plus_question` survived on a date-only question-shaped booking turn.
  3. Why is that wrong here: the turn did not ask about slot availability; it only constrained the active time slot to `завтра`.
  4. Why was it not normalized: current sanitizer handles explicit time fills and specialist-reference drifts, but not partial-date question-shaped booking turns without availability semantics.
  5. Why did it surface now: `P1.6o32` removed the prior specialist-reference drift, so this adjacent mixed/date overclaim became first failure.

- Root cause statement:
  - scenario sanitizer overlabels partial-date booking turns as `mixed_fill_plus_question` under active time collect even when the utterance only constrains the requested slot and does not express a real availability question.

- Fix mechanism:
  - normalize partial-date question-shaped booking turns out of `mixed_fill_plus_question` into ordinary booking/time-collect expectations when active `reply_type=time` is already open and no availability-query cues are present;
  - add deterministic regressions for positive and negative cases;
  - then run one fresh guarded rerun `r33`.

## Release safety

- Strategy: deterministic scenario/oracle normalization only
- Go signals:
  - partial-date booking turns under active time collect no longer require `pending_question_target=time`
  - actual availability questions (`свободные слоты`, `во сколько`) remain in `mixed_fill_plus_question`
- No-go signals:
  - runtime-core edits in this block
  - weakening oracle to accept contradictory contracts for the same surface family
- Rollback verification:
  - revert only the new mixed-question normalization if unrelated mixed slot-question families regress

## Invariant

1. `P1.6o32` stays closed.
2. Active time collect must remain `time`; partial-date constraints must not reopen pending-question target semantics by default.
3. Real availability questions must remain distinguishable from pure partial-date slot constraints.
4. Fresh expensive iteration stays exactly one guarded rerun after deterministic closure.

## Scope

- `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o33-partial-date-mixed-question-normalization-a1.md`
- `scripts/booking_dialog_scenarios.py`
- `truffles-api/tests/test_booking_dialog_scenarios_script.py`

## Out of scope

- runtime collect-slot-order validation redesign
- pending-question runtime ownership changes
- acceptance replay/full promotion

## Touch-list

- `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o33-partial-date-mixed-question-normalization-a1.md`
- `scripts/booking_dialog_scenarios.py`
- `truffles-api/tests/test_booking_dialog_scenarios_script.py`

## Plan (1..N)

1. Freeze `r32` as non-closure evidence and isolate the blocker as deterministic mixed/date taxonomy drift.
2. Normalize partial-date booking turns out of `mixed_fill_plus_question` when they only constrain the active time slot.
3. Add deterministic regressions for positive and negative cases.
4. Run one fresh guarded rerun `r33` only after deterministic checks are green.

## DoD

1. Partial-date booking turns under active time collect no longer keep `mixed_fill_plus_question` expectation drift.
2. Real availability-question turns remain in `mixed_fill_plus_question`.
3. Deterministic scenario-script regression is green.
4. Next expensive step is exactly one fresh guarded rerun `r33`.

## Expensive run control (mandatory)

- Hypothesis:
  - after mixed/date normalization, the next guarded rerun will not fail first on the `r32` partial-date overclaim family.
- Expected measurable effect:
  - either the run proceeds beyond dialog `1`, turn `4`, or a different blocker family surfaces.
- Stop condition:
  - run exactly one fresh guarded rerun plus strict audit; if the same mismatch persists, stop and reopen RCA instead of stacking more normalization patches.

## Checks

- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r32 --status done --strict-artifacts`
- `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "partial_date or mixed_fill_plus_question or specialist_reference or pending_question"`
- `git diff --check`

## Execution status

- `in_progress`

## Evidence

1. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r32/summary.json`
2. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r32/brief.md`
3. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r32/scenarios.json`
4. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r32/responses.jsonl`
5. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r32/trace_bundle.jsonl`
6. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r32/manual_audit.md`

## Rollback

1. Revert only the partial-date mixed-question normalization if unrelated mixed/date families regress.
2. Keep `P1.6o6` blocked; do not reinterpret `r32` as closure evidence.

## No-go

- no runtime-core edits
- no oracle weakening to accept contradictory `mixed_fill_plus_question` / ordinary collect contracts
- no fresh guarded rerun before deterministic scenario regression is green

## Risks/Blockers

1. A different runtime family may surface immediately after deterministic drift is removed.
2. Infra tool-evidence invalidity remains orthogonal and can still make the guarded run non-canonical.

## Residual architecture debt (mandatory)

- Current residuals accepted in this block:
  - collect-slot-order runtime boundary remains open until a post-normalization rerun proves it is first blocker
- Why not in this block:
  - current first blocker is still deterministic expectation drift, and runtime should not be rewritten to satisfy a contradictory scenario
- Risk if deferred:
  - expensive runs keep stopping before the next real runtime blocker can surface honestly
- Linked follow-up Task Package(s):
  - `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`
- Expiry/trigger to stop deferral:
  - before the next guarded rerun after `P1.6o33`

## Next-block contract (mandatory)

- Next block objective:
  - close bounded `P1.6o33`, then run one fresh guarded rerun `r33` to verify that partial-date booking turns no longer drift to `mixed_fill_plus_question`
- First deterministic check command:
  - `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "partial_date or mixed_fill_plus_question or specialist_reference or pending_question" && git diff --check`
- Blocked-by conditions:
  - `P1.6o33` is not deterministically green
  - guarded preflight is not green
  - runtime `http://127.0.0.1:18184` is unavailable
- Owner role for closure:
  - `Hands` for doc/code/evidence, `Top Architect + Brain` for closure gating

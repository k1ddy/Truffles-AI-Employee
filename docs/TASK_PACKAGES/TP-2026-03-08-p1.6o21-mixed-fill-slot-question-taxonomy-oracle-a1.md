# TP-2026-03-08-p1.6o21-mixed-fill-slot-question-taxonomy-oracle-a1

- Название/цель: закрыть новый `r18` blocker family, где scenario taxonomy/oracle сводит mixed turn (`Есть ли свободные слоты на утро?`) к чистому `ask_about_requested_slot`, хотя runtime корректно распознаёт partial `datetime` answer + availability question и уходит в `calendar.list_slots`.
- Parent TP: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Canon refs: `AGENTS.md`, `STATE.md`, `STRUCTURE.md`, `STRATEGY/REQUIREMENTS.md`, `docs/runbooks/BOOKING_CONFIRM_VERIFY.md`, `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`, `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o20-timeout-pending-slot-question-fallback-a1.md`
- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect remove worktree + branch after merge`

## One web search (mandatory before implementation)

- Query: `site:rasa.com/docs forms from_entity requested_slot custom validation active loop`
- Time (UTC): `2026-03-08T08:55:04Z`
- Sources:
  - `https://rasa.com/docs/rasa/forms/`
- Ready solutions found:
  - active slot collection can accept partial slot filling from the same user turn while the conversation still stays inside the active loop;
  - validation/oracle should distinguish between slot filling and leaving the loop, not force all mixed turns into one unhappy-path bucket.
- Decision (`reuse/integrate/build`): `reuse + integrate`
  - reuse the existing `mixed_fill_plus_question` ontology instead of inventing another tag family;
  - integrate stricter sanitize/oracle rules so mixed time-answer + slot-question surfaces stop inheriting the pure `ask_about_requested_slot` contract;
  - build only the missing taxonomy/oracle normalization for this bounded family.
- Rejected options:
  - weakening the oracle for pure `ask_about_requested_slot` turns;
  - retagging the runtime result as wrong when it legitimately filled partial `datetime` and listed slots;
  - overwriting mixed surfaces with generic fallback text instead of fixing ontology.

## Root cause (mandatory)

- Symptom:
  - fresh guarded `dev L2` rerun `p1.6o6-l2-dev-20260308-a1-r18` stopped on `dialog_id=1`, `turn_index=2`, message `Есть ли свободные слоты на утро?`

- Minimal reproduction:
  1. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r18/brief.md`
  2. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r18/responses.jsonl` and `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r18/trace_bundle.jsonl`
  3. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r18/scenarios.json` for dialog `1`, turn `2`
  4. inspect `scripts/booking_dialog_scenarios.py` around pending-question tags / sanitize / expectation maps

- Evidence:
  - Scenario tagged the turn as `ask_about_requested_slot` and therefore expected `pending_question_act=ask_about_requested_slot`, `pending_question_target=time`, and `pending_question_interaction` trace.
  - Runtime instead deterministically matched `datetime=утром`, preserved booking flow, called `calendar.list_slots`, and kept `question_contract.expected_reply_type=time`.
  - Failure reasons are pure expectation mismatches (`expected_meta_mismatch`, `expected_trace_miss`) at contract stage, not runtime action/tool errors.
  - The surfaced surface form is semantically mixed: it both constrains the requested slot (`утро`) and asks about availability.

- Five Whys:
  1. Why did `r18` fail: oracle expected a pure pending-question act, while runtime executed a partial-answer + availability path.
  2. Why did oracle expect that: scenario taxonomy kept the generated turn under `ask_about_requested_slot`.
  3. Why is that wrong: the text contains both a time constraint and a slot question, so it is not a pure `ask_about_requested_slot` turn.
  4. Why did the runtime look reasonable: active booking collect can legitimately consume the partial `datetime` answer and query availability in the same turn.
  5. Why did this surface only after `P1.6o20`: the timeout-degrade family is gone, so the next blocker moved to ontology/oracle handling of mixed slot turns.

- Root cause statement:
  - scenario sanitize/oracle still compress mixed `partial slot fill + availability question` turns into pure `ask_about_requested_slot`, so valid runtime behavior (`datetime` match + `calendar.list_slots`) is scored as a contract miss.

- Fix mechanism:
  - normalize mixed time-answer + slot-question surfaces into the existing `mixed_fill_plus_question` family during scenario sanitize;
  - relax that family's oracle from mandatory `pending_question_interaction` to the common resume contract (`question_contract.expected_reply_type=time`), so both valid owners remain admissible.

## Release safety

- Strategy: taxonomy/oracle-only closure; no runtime behavior changes
- Go signals:
  - `r18` is recorded as audited non-closure evidence
  - mixed surfaces sanitize into `mixed_fill_plus_question`
  - deterministic scenario-script tests prove pure `ask_about_requested_slot` and mixed surfaces diverge correctly
- No-go signals:
  - weakening pure `ask_about_requested_slot` expectations
  - changing runtime semantics in this block
  - masking unrelated expectation failures under broad oracle waivers
- Rollback verification:
  - revert only the taxonomy/oracle normalization if pure pending-question turns start losing strict expectations

## Invariant

1. No runtime-core semantic changes in this block.
2. Pure `ask_about_requested_slot(time)` expectations stay strict.
3. Mixed slot-fill + question turns remain inside booking resume contract.
4. No new tags; reuse `mixed_fill_plus_question`.

## Scope

- `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o21-mixed-fill-slot-question-taxonomy-oracle-a1.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o20-timeout-pending-slot-question-fallback-a1.md`
- `scripts/booking_dialog_scenarios.py`
- `truffles-api/tests/test_booking_dialog_scenarios_script.py`

## Out of scope

- fresh guarded rerun after deterministic closure
- runtime changes in `decision.py`
- acceptance `lock/replay`

## Touch-list

- `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o21-mixed-fill-slot-question-taxonomy-oracle-a1.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o20-timeout-pending-slot-question-fallback-a1.md`
- `scripts/booking_dialog_scenarios.py`
- `truffles-api/tests/test_booking_dialog_scenarios_script.py`

## Plan (1..N)

1. Record `r18` as audited non-closure evidence and freeze the mixed-turn ontology gap.
2. Normalize mixed time-answer + slot-question surfaces during scenario sanitize.
3. Realign `mixed_fill_plus_question` expectations to the shared booking resume contract.
4. Add deterministic scenario-script regressions.
5. Sync active TPs so the next expensive step becomes fresh guarded `r19` only after this deterministic closure.

## DoD

1. `r18` is recorded in active TPs as non-closure evidence.
2. `Есть ли свободные слоты на утро?` no longer inherits pure `ask_about_requested_slot` expectations.
3. Pure `ask_about_requested_slot` surfaces still require `pending_question_interaction` evidence.
4. Focused scenario-script tests are green.
5. The next admissible expensive step is fresh guarded `r19`.

## Expensive run control (mandatory)

- Hypothesis:
  - after `P1.6o21`, fresh guarded `r19` will no longer stop on the mixed surface `Есть ли свободные слоты на утро?` as a pure `ask_about_requested_slot` expectation miss
- Expected measurable effect:
  - `r19` no longer fails with `expected_meta_mismatch + expected_trace_miss` for `dialog_id=1`, `turn_index=2`; either the run proceeds past that turn or a different first blocker family surfaces
- Stop condition:
  - run exactly one fresh guarded `r19` plus strict audit; if the same mixed-family expectation miss persists or the run becomes infra-invalid, stop the expensive loop and reopen RCA instead of chaining another rerun

## Checks

- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r18 --status done --strict-artifacts`
- `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "mixed_fill_plus_question or pending_question or u_vas"`
- `git diff --check`

## Execution status

- `done`
- `r18` is audited non-closure evidence for this bounded family.
- deterministic taxonomy/oracle closure is green in the current worktree.
- fresh guarded `r19` no longer stopped on the mixed slot-question expectation miss and instead surfaced a different runtime owner-boundary family, reopened as `P1.6o22`.

## Evidence

1. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r18/summary.json`
2. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r18/brief.md`
3. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r18/failure_families.json`
4. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r18/scenarios.json`
5. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r18/responses.jsonl`
6. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r18/trace_bundle.jsonl`
7. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r18/manual_audit.md`
8. `scripts/booking_dialog_scenarios.py`
9. `truffles-api/tests/test_booking_dialog_scenarios_script.py`
10. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r19/brief.md`
11. `/tmp/booking_quality/p1.6o6-l2-dev-20260308-a1-r19/manual_audit.md`

## Rollback

1. Revert only the bounded taxonomy/oracle normalization if mixed surfaces are over-rewritten.
2. Keep `P1.6o6` blocked; do not reinterpret `r18` as closure evidence.

## No-go

- no runtime behavior changes
- no oracle weakening for pure `ask_about_requested_slot`
- no fresh guarded rerun before deterministic closure is green

## Risks/Blockers

1. Another adjacent mixed family may surface on the next guarded rerun.
2. Infra invalidity from missing tool confirm artifacts remains orthogonal and does not close `P1.6o6`.

## Residual architecture debt (mandatory)

- Current residuals accepted in this block:
  - fresh guarded proof remains open via `P1.6o6` after this deterministic taxonomy/oracle closure
  - the next surfaced family moved to expected-reply owner-boundary handling, tracked separately in `P1.6o22`
- Why not in this block:
  - this block only realigns ontology/oracle and cannot itself provide guarded proof
- Risk if deferred:
  - valid runtime mixed turns keep failing as expectation drift, blocking the next surfaced family
- Linked follow-up Task Package(s):
  - `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o22-consult-cta-check-booking-owner-boundary-a1.md`
  - `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`
- Expiry/trigger to stop deferral:
  - before fresh guarded `r20`

## Next-block contract (mandatory)

- Next block objective:
  - close bounded `P1.6o22`, then run fresh guarded `dev L2` `r20` on refreshed runtime to verify that the consult-CTA vs booking-verification family from `r19` no longer surfaces first
- First deterministic check command:
  - `pytest -q truffles-api/tests/test_message_endpoint.py -k "consult_booking_cta or check_booking or expected_reply" && git diff --check`
- Blocked-by conditions:
  - `P1.6o22` is not deterministically green
  - runtime `http://127.0.0.1:18184` is not refreshed after the next code delta
  - guarded preflight is not green
- Owner role for closure:
  - `Hands` for doc/code/evidence, `Top Architect + Brain` for closure gating

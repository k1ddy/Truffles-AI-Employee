# TP-2026-03-11-p1.6o65-invalid-schema-specialist-followup-truth-gate-a1

- Название/цель: закрыть surfaced `r90` blocker family, где после bounded `M13` closure свежий guarded `dev L2` больше не падает на timeout-mixed-date governance, но turn `Можно к мастеру Асем?` под активным `expected_reply_type=time` ловит `llm_policy_core invalid_schema`, затем деградирует в `booking_interrupt info_reply` + `truth_gate master` и теряет `booking_specialist_followup` owner вместо сохранения specialist follow-up contract с resume `time`.
- Parent TP: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Canon refs: `AGENTS.md`, `STATE.md`, `STRUCTURE.md`, `STRATEGY/REQUIREMENTS.md`, `docs/runbooks/BOOKING_CONFIRM_VERIFY.md`, `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o60a-remaining-closure-architecture-verdict-a1.md`, `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o60b-remaining-closure-owner-matrix-a1.md`, `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o37-booking-specialist-followup-owner-contract-a1.md`, `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o40-timeout-specialist-followup-owner-recovery-a1.md`, `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o51-question-like-specialist-followup-collect-owner-a1.md`, `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o64-timeout-mixed-date-availability-governance-a1.md`
- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect remove worktree + branch after merge`

## One web search (mandatory before implementation)

- Query: `site:rasa.com/docs/rasa/forms requested slot side questions interruptions`
- Time (local): `2026-03-09 18:04 +0500`
- Sources:
  - `https://rasa.com/docs/rasa/forms/`
- Source quality:
  - official Rasa forms documentation (primary vendor reference)
- Ready solutions found:
  - active slot interruptions should preserve the requested-slot resume contract even on unhappy paths;
  - degrade may clarify or resume, but it must not silently reinterpret an active-slot follow-up as unrelated FAQ/info.
- Decision (`reuse/integrate/build`): `reuse + integrate`
  - reuse the existing `booking_specialist_followup` owner, specialist-hint/master-signal recovery, and bounded degrade evidence path;
  - integrate one new matrix row (`M14`) for invalid-schema degraded fallback on named specialist follow-up under active `time` collect;
  - do not build a phrase router and do not weaken truth/oracle acceptance.
- Rejected options:
  - treating `r90` as a reopen of `M4` or `M13` without freezing the invalid-schema degrade family;
  - accepting generic `master` truth reply as graceful degrade for booking specialist follow-up;
  - broad prompt/model-routing redesign before bounded runtime recovery is proven insufficient.
- Reuse note:
  - no new web query was run for this turnover; this TP deliberately reuses the exact official-source search already recorded in `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o60a-remaining-closure-architecture-verdict-a1.md` because the surfaced family is still an active requested-slot interruption contract under forms-like slot collection.

## Root cause (mandatory)

- Symptom:
  - fresh guarded `dev L2` run `p1.6o6-l2-dev-20260311-a1-r90` stops at dialog `1`, turn `4`, user text `Можно к мастеру Асем?`

- Minimal reproduction:
  1. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r90/summary.json`, `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r90/brief.md`, and `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r90/manual_audit.md`
  2. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r90/scenarios.json` dialog `1`, turn `4` and confirm expectation requires `reply_type=time`, `meta_any.pending_question_target=specialist`, and `pending_question_interaction decision=booking_specialist_followup`
  3. inspect `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r90/responses.jsonl` and `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r90/trace_bundle.jsonl` for dialog `1`, turn `4` and confirm: `question_contract decision=missed answer_error=blocked_by_info`, `llm_policy_core error=invalid_schema`, `policy_core_mode=degraded_fallback`, then `booking_interrupt decision=info_reply` and `truth_gate intent=master`, while final meta drops `pending_question_target`, `pending_question_interaction`, and `active_question_relation`
  4. inspect `truffles-api/app/routers/webhook/decision.py` degraded fallback / booking interrupt path and confirm current recovery covers timeout/hint families but not non-retryable `invalid_schema` specialist follow-up under active `time` collect
  5. inspect existing targeted tests in `truffles-api/tests/test_message_endpoint.py` and `truffles-api/tests/test_booking_quality_response_guard.py` and confirm there is no bounded regression for `invalid_schema -> truth_gate` collapse on named specialist follow-up

- Evidence:
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r90/summary.json`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r90/brief.md`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r90/scenarios.json`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r90/responses.jsonl`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r90/trace_bundle.jsonl`
  - `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r90/manual_audit.md`
  - `truffles-api/app/routers/webhook/decision.py`
  - `truffles-api/tests/test_message_endpoint.py`
  - `truffles-api/tests/test_booking_quality_response_guard.py`

- Five Whys:
  1. Why did `r90` fail: the user asked to book with Asem, but the bot answered with generic `master` info instead of preserving booking follow-up.
  2. Why did runtime answer with generic info: `question_contract` was blocked by info, `llm_policy_core` failed with non-retryable `invalid_schema`, and the degraded path fell through `booking_interrupt info_reply` into `truth_gate`.
  3. Why did specialist-followup owner not recover: current degraded recovery relies on semantic payload or timeout/hint recovery, but `invalid_schema` leaves `pending_question_target` and `active_question_relation` empty.
  4. Why was truth-gate allowed to win: after policy failure, the active booking row is not protected against direct `master` info resolution, so generic info/truth compresses the turn before specialist follow-up recovery can run.
  5. Why is this a new bounded family instead of a reopen: `P1.6o51` closed the normal named specialist follow-up row, and `P1.6o64` closed the previous governance row; `r90` is a distinct invalid-schema degrade gap with a judge-failing product symptom.

- Root cause statement:
  - non-retryable `llm_policy_core invalid_schema` under active `time` collect has no relation-preserving specialist-followup recovery: once semantic payload is empty, the degraded path allows `booking_interrupt info_reply` + `truth_gate master` to compress a named specialist booking follow-up into generic info, dropping the active pending-question contract.

- Fix mechanism:
  - publish a new matrix row (`M14`) for named specialist follow-up under `invalid_schema` degraded fallback;
  - add a bounded runtime recovery in the degraded fallback / booking interrupt boundary so active booking turns with explicit named specialist signal preserve `booking_specialist_followup` semantics, `pending_question_target=specialist`, and resume `time`;
  - emit explicit policy-failure provenance together with preserved specialist-followup trace/meta;
  - add targeted deterministic regressions plus adjacent negative cases before the next guarded rerun.

## Reuse-first plan (mandatory)

- **Internal reuse:** `P1.6o37`, `P1.6o40`, `P1.6o51`, `P1.6o53`, `booking_specialist_followup`, `resolve_master_intent`, bounded degraded-fallback evidence path in `decision.py`.
- **External reuse:** official Rasa forms guidance for active requested-slot interruptions and unhappy-path resume semantics.
- **Why not reinvent the wheel:** the platform already has specialist-followup owners and partial degraded recovery; the missing piece is a narrow invalid-schema boundary that keeps the same row alive instead of collapsing to truth/info.

## Execution profile (mandatory for non-doc blocks)

- **TP mode:** `implementation`
- **Doc touch budget (files):** `8`
- **Code dominance:** `required`
- **Override token:** `none`
- **Why this profile fits:** the surfaced family is a bounded runtime degrade boundary with an already frozen matrix row and clear adjacent regressions.

## Release safety

- Strategy: bounded runtime recovery only; no prompt/model-routing redesign in this block
- Go signals:
  - active `time` + named specialist follow-up no longer collapses to `truth_gate master` when `llm_policy_core` fails with `invalid_schema`
  - final trace/meta preserve specialist-followup owner plus explicit policy failure provenance
  - adjacent rows `M4` and `M13` remain green on deterministic contour
- No-go signals:
  - accepting generic `master` truth reply as success for booking specialist follow-up
  - broadening recovery beyond active booking + explicit named specialist signal
  - removing policy-failure evidence from `decision_trace` / `decision_meta`
- Rollback verification:
  - revert only the bounded `M14` runtime/test delta if adjacent specialist-followup or truth-gate rows regress

## Invariant

1. Generic `master` truth reply is never an acceptable closure for named specialist follow-up under active `time` collect.
2. `P1.6o51/M4` and `P1.6o64/M13` must not regress.
3. Recovery must remain observable via `decision_meta` / `decision_trace` and explicit policy-failure provenance.
4. Exactly one fresh guarded `dev L2` runs after deterministic green.

## Scope

- publish the surfaced invalid-schema specialist-followup family as matrix row `M14`
- then implement the bounded runtime recovery for that row only

## Out of scope

- global prompt rewrite or model-routing alignment
- tool-evidence infra remediation
- acceptance `lock/replay`

## Touch-list

- `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o65-invalid-schema-specialist-followup-truth-gate-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o60b-remaining-closure-owner-matrix-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o60a-remaining-closure-architecture-verdict-a1.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`
- `truffles-api/app/routers/webhook/decision.py`
- `truffles-api/tests/test_message_endpoint.py`
- `truffles-api/tests/test_booking_quality_response_guard.py`

## Plan (1..N)

1. Freeze `r90` as audited non-closure evidence and publish `M14`.
2. Add bounded invalid-schema specialist-followup recovery before booking interrupt / truth-gate compression.
3. Add targeted runtime + evidence regressions with adjacent negative cases.
4. Run focused checks and `git diff --check`.
5. Run one fresh guarded `dev L2` only after deterministic closure is green.

## DoD

1. `r90` is recorded in active docs as audited non-closure evidence.
2. Named specialist follow-up under active `time` collect no longer collapses to `truth_gate master` when `llm_policy_core` fails with `invalid_schema`.
3. Final meta/trace preserve `pending_question_target=specialist`, specialist-followup relation/decision, `expected_reply_type=time`, and explicit policy-failure provenance.
4. Focused deterministic runtime/evidence regressions are green.
5. Fresh guarded `dev L2` no longer stops on the `r90` family.

## Expensive run control (mandatory)

- Hypothesis:
  - after the bounded `M14` runtime recovery, fresh guarded `dev L2` will no longer stop on dialog `1`, turn `4`, `Можно к мастеру Асем?`
- Expected measurable effect:
  - the next guarded run preserves specialist-followup owner evidence for that turn and either proceeds beyond it or surfaces a different first blocker family
- Stop condition:
  - run exactly one fresh guarded `dev L2` plus strict audit; if the same `invalid_schema -> truth_gate` collapse persists or the run becomes preflight-invalid, stop the expensive loop and reopen RCA instead of chaining another rerun

## Checks

- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r90 --status done --strict-artifacts`
- `python3 - <<'PY'`
  `import json, pathlib`
  `run = pathlib.Path('/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r90')`
  `rows = [json.loads(line) for line in (run / 'responses.jsonl').read_text().splitlines()]`
  `row = rows[3]`
  `print(row['turn_text'])`
  `print(row['decision_meta']['source'], row['decision_meta']['intent'], row['decision_meta']['expected_reply_type'])`
  `print(row['decision_trace'][2]['stage'], row['decision_trace'][2]['error'])`
  `print(row['decision_trace'][7]['stage'], row['decision_trace'][7]['decision'])`
  `PY`
- `pytest -q truffles-api/tests/test_message_endpoint.py -k "booking_specialist_followup or invalid_schema or truth_gate or referent_followup"`
- `pytest -q truffles-api/tests/test_booking_quality_response_guard.py -k "specialist_followup or truth_gate or expected_meta_mismatch"`
- `git diff --check`

## Execution status

- `done`
- `r90` RCA is frozen and audited
- deterministic `M14` closure is green via bounded invalid-schema specialist-followup recovery
- fresh guarded `dev L2` run `p1.6o6-l2-dev-20260311-a1-r91` no longer stopped on `r90/M14`; the first fail rolled to dialog `1`, turn `5`, `Я хочу записаться к Айгерим.`

## Evidence

1. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r90/summary.json`
2. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r90/brief.md`
3. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r90/scenarios.json`
4. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r90/responses.jsonl`
5. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r90/trace_bundle.jsonl`
6. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r90/manual_audit.md`
7. `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o60b-remaining-closure-owner-matrix-a1.md`
8. `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o64-timeout-mixed-date-availability-governance-a1.md`
9. `truffles-api/app/routers/webhook/decision.py`
10. `truffles-api/tests/test_message_endpoint.py`
11. `truffles-api/tests/test_booking_quality_response_guard.py`
12. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r91/summary.json`
13. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r91/brief.md`
14. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r91/scenarios.json`
15. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r91/responses.jsonl`
16. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r91/trace_bundle.jsonl`
17. `/tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r91/manual_audit.md`

## Rollback

1. Keep `P1.6o6` blocked.
2. Revert only the future bounded `M14` runtime/test delta if it regresses adjacent specialist-followup rows.

## No-go

- no generic `master` truth/info fallback accepted as closure for `M14`
- no phrase-fitting on `Асем` / named specialist surfaces in runtime core
- no fresh guarded rerun before deterministic runtime regressions are green

## Risks/Blockers

1. `invalid_schema` may be amplified by latency/model-routing drift, but this block only closes the bounded runtime recovery.
2. Recovery may become too broad and misclassify genuine generic `master` info interrupts if negative tests are weak.
3. `r91` surfaced a different success-path specialist-followup family, so proof continuation is blocked on `P1.6o66`, not on `M14`.
4. `r90` and `r91` remain non-canonical proof evidence (`infra_valid=false`, `semantic_valid=false`), so this block can only claim bounded closure plus rerun rollover.

## Residual architecture debt (mandatory)

- Current residuals accepted in this block:
  - acceptance tool-evidence infra remains outside this bounded row fix
  - `r91` surfaced a new success-path specialist-followup family after `M14` closure
- Why not in this block:
  - this slice only closes one invalid-schema specialist-followup degrade row
- Risk if deferred:
  - future guarded runs may incorrectly blame `M14` for a different success-path semantic contract gap
- Linked follow-up Task Package(s):
  - `docs/TASK_PACKAGES/TP-2026-03-11-p1.6o66-policy-core-named-specialist-followup-owner-a1.md`
  - `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`
- Expiry/trigger to stop deferral:
  - before the next guarded rerun beyond `r91`

## Next-block contract (mandatory)

- Next block objective:
  - implement `M15`, then run one fresh guarded `dev L2` so `r91` is no longer the first surfaced family after `M14` closure
- First deterministic check command:
  - `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o6-l2-dev-20260311-a1-r91 --status done --strict-artifacts && git diff --check`
- Blocked-by conditions:
  - `M15` is not published in the owner matrix
  - success-path specialist-followup deterministic regressions are not green
  - acceptance tool-evidence infra remains red
- Owner role for closure:
  - `Hands` for bounded implementation, `Top Architect + Brain` for matrix/program/proof sync

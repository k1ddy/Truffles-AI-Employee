# TP-2026-03-12-p1.6o92-post-m19-reclosure-proof-lane-a1

- Название/цель: после deterministic re-closure `P1.6o91` выполнить один fresh guarded `dev L2`, чтобы доказать, что reopened `M19` больше не является first fail, и truthfully surface-ить следующую remaining family, если она есть.

## Canon refs

- Parent closure program: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Main remediation: `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- Prior proof lane: `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o90-post-m30-proof-lane-a1.md`
- Deterministic closure prerequisite: `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o91-invalid-schema-booking-request-specialist-catalog-match-a1.md`
- Owner matrix baseline: `truffles-api/app/knowledge/generic/INTERACTION_OWNER_MATRIX.yaml`
- State baseline: `STATE.md`

## Git context

- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect` remove branch + worktree after merge

## One web search (mandatory before implementation)

- Query: `site:www.uvicorn.org/settings uvicorn host port app-dir env-file`
- Date/time: `2026-03-12T12:30:05+05:00`
- Sources opened:
  - official Uvicorn settings docs: `https://www.uvicorn.org/settings/`
- Source quality:
  - official product documentation
- Found reusable ideas:
  - keep proof runtime startup explicit with CLI `--host`, `--port`, and `--app-dir` flags;
  - runtime fingerprint validation must stay explicit via `/admin/version`.
- Decision:
  - `reuse`
  - use the established live proof-run startup pattern on `:18184`.
- Rejected options:
  - detached launch patterns that hide runtime parity;
  - additional expensive runs in the same block.

## Root cause (mandatory)

- Symptom:
  - after `P1.6o91`, deterministic closure is green, but there is no fresh proof run showing reopened `M19` disappeared from the first-fail position.
- Minimal reproduction:
  1. Use `p1.6o90-l2-dev-20260312-a1-r1` as the pre-fix proof baseline.
  2. Confirm it failed first on dialog `1`, turn `5`, text `Можно записаться к Айгерим?`.
  3. Confirm `P1.6o91` now deterministically recovers that family to `booking_specialist_followup` via branch catalog.
  4. Run one fresh guarded `dev L2` on a live runtime whose `/admin/version` matches current `HEAD`.
  5. Audit the resulting run strictly and compare the new first fail, if any, against reopened `M19`.
- Evidence:
  - `/tmp/booking_quality/p1.6o90-l2-dev-20260312-a1-r1/summary.json`
  - `/tmp/booking_quality/p1.6o90-l2-dev-20260312-a1-r1/responses.jsonl`
  - `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o91-invalid-schema-booking-request-specialist-catalog-match-a1.md`
- Five Whys:
  1. Why is closure still incomplete? Because bounded deterministic closure alone does not prove the expensive lane rolled past reopened `M19`.
  2. Why is a proof lane needed? Because the quality chain admits closure only when a fresh guarded run confirms the surfaced family is no longer first.
  3. Why can we run it now? Because `P1.6o91` boundedly re-closes the reopened runtime family.
  4. Why must runtime be fingerprinted first? Because preflight/runtime mismatch makes the run non-canonical.
  5. Why only one run? Because the goal is admission of the next family, not another reactive multi-run loop.
- Root cause statement:
  - the remaining gap after `P1.6o91` is proof, not implementation: without one fresh guarded `dev L2` on a verified live runtime, reopened `M19` remains only deterministically re-closed and the next remaining family stays unknown.
- Fix mechanism:
  - start a live worktree runtime on `:18184` and prove `/admin/version == HEAD`;
  - execute exactly one guarded `dev L2` run and strict audit;
  - sync docs/state/matrix truthfully based on the new first-fail outcome.

## Reuse-first decision (mandatory)

- Reuse:
  - existing guarded entrypoint `scripts/llm_quality_guarded.sh`
  - existing audit command `python3 ops/diagnose.py llm-quality-audit`
  - existing live runtime startup pattern used in `P1.6o84`, `P1.6o86`, `P1.6o88`, and `P1.6o90`
- Integrate:
  - no runtime code changes unless the proof run surfaces a new deterministic or bounded family
- Build:
  - none planned in this block

## Release safety

- Rollout: none; local proof-only block
- Go signal:
  - runtime fingerprint matches `HEAD`
  - guarded run artifacts are valid enough to audit (`artifact_integrity.valid=true`)
  - first fail is no longer reopened `M19`
- No-go signal:
  - invalid runtime fingerprint preflight
  - incomplete/invalid guarded artifacts
  - fresh first fail remains the same reopened `M19` family
- Rollback:
  - stop the proof lane, record evidence truthfully, do not reinterpret invalid/incomplete runs as closure evidence

## Invariant

1. `M30` stays cleared by `P1.6o89` + `P1.6o90`.
2. Reopened `M19` stays deterministically re-closed by `P1.6o91`.
3. No runtime/product contract weakening.
4. Exactly one fresh guarded `dev L2` run in this block.

## Scope

- proof-lane runtime startup + fingerprint validation
- one guarded `dev L2`
- strict audit
- doc/state/session sync for the resulting admission

## Out of scope

- new runtime implementation before the proof run surfaces a new row
- acceptance `lock/replay/full`
- matrix redesign

## Touch-list

- `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o92-post-m19-reclosure-proof-lane-a1.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `STATE.md`
- `STRUCTURE.md`
- `docs/SESSIONS/SESSION-2026-02-19-llm-first-firebreak-a1.md`
- `truffles-api/app/knowledge/generic/INTERACTION_OWNER_MATRIX.yaml`
- `truffles-api/tests/test_interaction_owner_matrix_contract.py`

## Plan (1..N)

1. Publish `P1.6o92` with proof-lane scope and runtime-fingerprint requirements.
2. Re-run the deterministic prerequisite check from `P1.6o91`.
3. Start live worktree runtime on `:18184` and prove `/admin/version == HEAD`.
4. Execute exactly one fresh guarded `dev L2` and strict audit.
5. Sync matrix/docs/state/session with truthful post-run admission.

## DoD

1. Fresh guarded `dev L2` is executed on a fingerprint-verified live runtime.
2. Reopened `M19` is no longer the first fail family.
3. The next first fail, if any, is captured truthfully with artifacts.
4. Docs/state/session are synced to the new admission truth.

## Checks

- `pytest -q truffles-api/tests/test_message_endpoint.py -k "branch_catalog_match or invalid_schema_question_like_named_specialist_followup_keeps_time_collect"`
- `pytest -q truffles-api/tests/test_booking_quality_response_guard.py -k "invalid_schema_specialist_followup or specialist_followup_owner_gap"`
- `pytest -q truffles-api/tests/test_interaction_owner_matrix_contract.py`
- `TEST_MODE=1 scripts/llm_quality_guarded.sh --mode lock --run-id p1.6o92-l2-dev-20260312-a1-r1 --allow-pending-previous --allow-repeat-fingerprint -- --base-url http://127.0.0.1:18184 --client-slug demo_salon --mode llm --count 10 --min-turns 10 --max-turns 15 --include-media --scenario-coverage booking,info,interrupt,handoff --tool-hooks auto --jid-mode unique --allow-non-allowlist --judge-mode all --quality-lane dev --run-economy-gate block --fail-on-thresholds --max-failures 1 --reset-before-dialog`
- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o92-l2-dev-20260312-a1-r1 --status done --strict-artifacts`
- `git diff --check`
- `SESSION_AGENT=a1 scripts/session_check.sh`

## Execution status

- `done`
- fresh guarded `dev L2` `p1.6o92-l2-dev-20260312-a1-r1` ran on a fingerprint-verified live runtime and no longer stopped on reopened `M19`
- proof result: the new first fail is dialog `1`, turn `6`, user text `Можно выбрать мастера?`
- surfaced family is admitted as new open row `M31`: policy-core success-path generic specialist-choice under active `time` collect, where semantic owner emits `subject_kind=specialist`, `resolution_mode=referent_followup`, `next_question=name`, but runtime still compresses the turn into generic `booking_info_contract/master`

## Evidence

1. `/tmp/booking_quality/p1.6o90-l2-dev-20260312-a1-r1/summary.json`
2. `/tmp/booking_quality/p1.6o90-l2-dev-20260312-a1-r1/responses.jsonl`
3. `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o91-invalid-schema-booking-request-specialist-catalog-match-a1.md`
4. `/tmp/booking_quality/p1.6o92-l2-dev-20260312-a1-r1/summary.json`
5. `/tmp/booking_quality/p1.6o92-l2-dev-20260312-a1-r1/brief.md`
6. `/tmp/booking_quality/p1.6o92-l2-dev-20260312-a1-r1/scenarios.json`
7. `/tmp/booking_quality/p1.6o92-l2-dev-20260312-a1-r1/responses.jsonl`
8. `/tmp/booking_quality/p1.6o92-l2-dev-20260312-a1-r1/trace_bundle.jsonl`
9. `/tmp/booking_quality/p1.6o92-l2-dev-20260312-a1-r1/manual_audit.md`
10. `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o93-generic-specialist-choice-followup-owner-a1.md`

## Rollback

1. Stop the local runtime and keep `P1.6o91` deterministic closure intact.
2. If the run is invalid/incomplete, record it truthfully and keep `P1.6o92` open.
3. Do not mutate the matrix to hide surfaced evidence.

## No-go

- no second expensive run in this block
- no default-path runtime patch before the proof run surfaces the next family
- no reuse of invalid preflight evidence as closure proof

## Risks / blockers

- local runtime may fail fingerprint validation or die before the guarded run starts
- the next surfaced row may be runtime, scenario, or oracle, and must not be pre-labeled before evidence

## Residual architecture debt (mandatory)

### Current residuals accepted in this block

- closure proof is still on the `dev L2` lane only; `lock/replay/full` remain blocked
- specialist recovery still lives in `decision.py`, not yet in a dedicated owner handler
- new open row `M31` is admitted after the proof lane and remains unimplemented

### Why not in this block

- this block is only the proof admission after reopened `M19` deterministic re-closure; it does not implement the newly surfaced generic specialist-choice owner gap

### Risk if deferred

- active-time specialist-choice turns can still collapse into generic `booking_info_contract/master` / `master_service_not_found`, leaving semantic owner parity unresolved on the success path

### Linked follow-up Task Package(s)

- `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o93-generic-specialist-choice-followup-owner-a1.md`
- `P1.7` acceptance replay gate after the closure program proof lane is green enough to advance

### Expiry/trigger to stop deferral

- before any next guarded `lock/replay/full`

## Next-block contract (mandatory)

### Next block objective

- publish and execute `P1.6o93` so generic specialist-choice question `Можно выбрать мастера?` under active `time` collect no longer collapses from the successful specialist-choice policy plan into generic `booking_info_contract/master`.

### First deterministic check command

- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o92-l2-dev-20260312-a1-r1 --status done --strict-artifacts && pytest -q truffles-api/tests/test_message_endpoint.py -k "choose_specialist_question_rewrites_collect_to_master_info or booking_specialist_followup_persists_preference" && pytest -q truffles-api/tests/test_interaction_owner_matrix_contract.py && git diff --check`

### Blocked-by conditions

- `P1.6o92` proof evidence is not synced truthfully into active docs and matrix state
- the guarded run remains invalid/incomplete or unaudited
- `M31` is not frozen truthfully in the owner matrix before implementation

### Owner role for closure

- `Top Architect + Brain`

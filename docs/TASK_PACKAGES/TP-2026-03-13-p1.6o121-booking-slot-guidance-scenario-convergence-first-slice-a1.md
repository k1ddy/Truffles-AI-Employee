# TP-2026-03-13-p1.6o121-booking-slot-guidance-scenario-convergence-first-slice-a1

- Название/цель: после truthful `P1.6o120` class triage запустить первый structural `Track A` block вместо нового row-child: converged scenario-governance/oracle expectations for active booking slot-guidance from canonical runtime interaction contract, so generic availability questions under active `time` collect derive `booking_slot_guidance` / `ask_about_requested_slot(time)` instead of stale `slot_compare` tags/meta.

## Canon refs

- Parent closure program: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Main remediation: `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- Process reset / class triage: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o118-class-level-closure-process-reset-a1.md`
- Prior structural slice: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o119-timeout-degrade-owner-boundary-first-slice-a1.md`
- Proof-lane admission: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o120-post-track-c-proof-lane-a1.md`
- Machine-readable owner matrix: `truffles-api/app/knowledge/generic/INTERACTION_OWNER_MATRIX.yaml`
- State baseline: `STATE.md`

## Git context

- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `origin/main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect` remove branch + worktree after merge

## One web search (mandatory before implementation)

- Query: `site:stately.ai/docs state machine tests derive expected transitions from context and guards`
- Date/time: `2026-03-13T10:03:00+05:00`
- Sources opened:
  - official Stately docs: `https://stately.ai/docs/machines`
  - official Stately docs: `https://stately.ai/docs/guards`
- Source quality:
  - official product documentation
- Found reusable ideas:
  - transition truth should come from one machine/context model rather than parallel handwritten expectations;
  - guards/context-derived transitions should be asserted through contract paths, not copied into a second ad hoc ruleset.
- Decision:
  - `reuse`
  - reuse canonical runtime interaction contract (`InteractionState` + owner semantics + pending-question contract) as the source for scenario expectations instead of adding another phrase-local normalization.
- Rejected options:
  - another regex/tag scrub only in `scripts/booking_dialog_scenarios.py`
  - a big-bang rewrite of the whole scenario generator

## Root cause (mandatory)

- Symptom:
  - `P1.6o120` no longer fails first on timeout/degrade, but still fails first on expectation/oracle drift for `dialog 1 / turn 2 / "Какие дни у вас доступны?"`.
- Minimal reproduction:
  1. Audit `/tmp/booking_quality/p1.6o120-l2-dev-20260313-a1-r1`.
  2. Inspect `scenarios.json` for `dialogs[0].turns[1]`; confirm generated expectation sets `tags=["slot_compare"]`, `meta_any.pending_question_act=["slot_compare"]`, and `trace_contains.pending_question_act="slot_compare"`.
  3. Inspect `responses.jsonl` / `trace_bundle.jsonl` for message `LLM-QUAL-p1.6o120-l2-dev-20260313-a1-r1-001-02-b7549c`; confirm runtime canonically returns `decision_meta.source="booking_slot_guidance"`, `expected_reply_type="time"`, `pending_question_act="ask_about_requested_slot"`, `pending_question_target="time"`, and `active_question_relation="ask_about_requested_slot"`.
  4. Confirm there is no timeout/degrade path on the surfaced turn: `policy_core_mode="policy_core"`, `llm_policy_core.ok=true`, `llm_timeout=false`.
  5. Confirm the strict failure family is `expected_meta_mismatch` + `expected_trace_miss`, not runtime semantic invalidity.
- Evidence:
  - `/tmp/booking_quality/p1.6o120-l2-dev-20260313-a1-r1/summary.json`
  - `/tmp/booking_quality/p1.6o120-l2-dev-20260313-a1-r1/brief.md`
  - `/tmp/booking_quality/p1.6o120-l2-dev-20260313-a1-r1/scenarios.json`
  - `/tmp/booking_quality/p1.6o120-l2-dev-20260313-a1-r1/responses.jsonl`
  - `/tmp/booking_quality/p1.6o120-l2-dev-20260313-a1-r1/trace_bundle.jsonl`
  - `/tmp/booking_quality/p1.6o120-l2-dev-20260313-a1-r1/manual_audit.md`
- Five Whys:
  1. Why did the turn fail? Because generated oracle expected `slot_compare` semantics while runtime preserved requested-slot guidance semantics.
  2. Why did oracle expect `slot_compare`? Because scenario-governance still classifies this family from its own tag heuristics instead of deriving the expected pending-question act from canonical runtime interaction state.
  3. Why is that possible after runtime fixes? Because scenario/oracle still duplicates transition knowledge in a separate ruleset.
  4. Why is this now the first surfaced remaining class? Because `P1.6o119` removed the prior timeout/degrade first-fail family from the live proof lane.
  5. Why must the next fix be structural? Because another local scrub would only re-label this one surface and leave the duplicated truth source intact for adjacent availability/follow-up phrasings.
- Root cause statement:
  - scenario-governance still owns an independent pending-question classification path for active booking availability questions and therefore overclaims `slot_compare` where canonical runtime contract already says `booking_slot_guidance` + `ask_about_requested_slot(time)`.
- Fix mechanism:
  - derive the surfaced scenario expectation family from canonical runtime interaction semantics already available to the generator path, rather than from a standalone `slot_compare` heuristic;
  - keep the first slice bounded to active booking slot-guidance under missing `datetime`, grounded `service`, and `expected_reply_type=time`;
  - prove the new expectation path with generator tests before any next proof-lane rerun.

## Reuse-first decision (mandatory)

- Reuse:
  - existing owner-matrix / interaction-state canon
  - existing scenario generator pipeline in `scripts/booking_dialog_scenarios.py`
  - existing scenario contract tests in `truffles-api/tests/test_booking_dialog_scenarios_script.py`
- Integrate:
  - reuse canonical pending-question semantics instead of inventing a new oracle-only taxonomy for this family
- Build:
  - only the bounded convergence helper/wiring required to remove duplicate `slot_compare` truth for this family

## Release safety

- Rollout: none; local deterministic structural block
- Go signal:
  - surfaced `slot_compare` expectation is replaced by canonical slot-guidance expectation in generator outputs
  - targeted generator/contract tests are green
  - no runtime contract weakening
- No-go signal:
  - fix relies on phrase-local hardcode rather than canonical interaction semantics
  - runtime and oracle still disagree on pending-question act for the same bounded family
  - targeted tests remain red or regress adjacent scenario families
- Rollback:
  - revert only the bounded `Track A` convergence slice and its tests; keep `P1.6o120` surfaced evidence truth intact

## Invariant

1. `P1.6o119` deterministic `Track C` closure stays intact.
2. No new semantic branching is added to runtime core to force oracle agreement.
3. Scenario/oracle acceptance continues to validate contract evidence, not byte-identical phrasing.
4. This slice stays bounded to active booking slot-guidance under missing `datetime`.

## Scope

- active booking scenario generation for generic availability/day questions when `service` is grounded but `datetime` is still missing
- convergence of generated `meta_any` / `trace_contains` with canonical `booking_slot_guidance` / `ask_about_requested_slot(time)` contract
- targeted deterministic generator tests

## Out of scope

- whole-generator rewrite
- unrelated scenario-governance families outside the active booking slot-guidance envelope
- new live proof-lane rerun inside this block
- broader success-path owner extraction (`Track B`)

## Touch-list

- `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o121-booking-slot-guidance-scenario-convergence-first-slice-a1.md`
- `scripts/booking_dialog_scenarios.py`
- `truffles-api/tests/test_booking_dialog_scenarios_script.py`
- `STATE.md`
- `docs/SESSIONS/SESSION-2026-02-19-llm-first-firebreak-a1.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `STRUCTURE.md`

## Plan (1..N)

1. Freeze `P1.6o120` evidence as the bounded `Track A` admission.
2. Inspect the generator branch that emits `slot_compare` for active booking availability turns.
3. Replace the duplicated oracle classification for this bounded family with canonical slot-guidance expectation derived from runtime interaction semantics already carried by scenario context.
4. Update targeted generator tests to assert `booking_slot_guidance` / `ask_about_requested_slot(time)` instead of `slot_compare`.
5. Sync docs/state/session truthfully and stop before a new proof lane.

## DoD

1. The surfaced `P1.6o120` family is closed on deterministic contour without changing runtime semantic ownership.
2. Generated scenario expectations for the bounded family now align with canonical runtime interaction contract.
3. Targeted generator tests are green.
4. The next admissible expensive step is a fresh proof-lane TP, not another scenario-local child patch.

## Checks

- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o120-l2-dev-20260313-a1-r1 --status done --strict-artifacts`
- `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "slot_compare_requested_slot_overclaim or slot_compare_days_availability or preserves_generic_slot_compare_without_exact_time or rewrites_check_booking_like_query_without_prior_management_tag"`
- `pytest -q truffles-api/tests/test_booking_quality_response_guard.py -k "booking_slot_stall_not_reported_for_ask_about_requested_slot_contract"`
- `git diff --check`
- `SESSION_AGENT=a1 scripts/session_check.sh`

## Execution status

- `deterministic_closure_green`
- bounded `Track A` convergence is landed in `scripts/booking_dialog_scenarios.py`: stale `slot_compare` turns now canonicalize to requested-slot guidance only on the `slot_compare` path when the text is a non-comparative availability question under active `time` collect
- targeted generator regressions are green for:
  - existing `slot_compare -> ask_about_requested_slot` overclaim
  - surfaced `Какие дни у вас доступны?` availability phrasing
  - negative guard preserving true compare phrasing
  - adjacent `check_booking` family staying out of requested-slot normalization
- `test_booking_quality_response_guard.py -k "booking_slot_stall_not_reported_for_ask_about_requested_slot_contract"` is green
- a broad full-file run on `truffles-api/tests/test_booking_dialog_scenarios_script.py` surfaced adjacent red cases outside this bounded family in the dirty worktree, so it is not used as closure evidence for `P1.6o121`
- next admissible step is a fresh proof-lane TP after deterministic closure of this `Track A` slice

## Evidence

1. `/tmp/booking_quality/p1.6o120-l2-dev-20260313-a1-r1/summary.json`
2. `/tmp/booking_quality/p1.6o120-l2-dev-20260313-a1-r1/brief.md`
3. `/tmp/booking_quality/p1.6o120-l2-dev-20260313-a1-r1/scenarios.json`
4. `/tmp/booking_quality/p1.6o120-l2-dev-20260313-a1-r1/responses.jsonl`
5. `/tmp/booking_quality/p1.6o120-l2-dev-20260313-a1-r1/trace_bundle.jsonl`
6. `/tmp/booking_quality/p1.6o120-l2-dev-20260313-a1-r1/manual_audit.md`
7. `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o118-class-level-closure-process-reset-a1.md`
8. `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o120-post-track-c-proof-lane-a1.md`
9. `scripts/booking_dialog_scenarios.py`
10. `truffles-api/tests/test_booking_dialog_scenarios_script.py`

## Rollback

1. Revert only the bounded scenario-convergence slice and its targeted tests.
2. Keep `P1.6o120` surfaced `Track A` evidence intact in docs/state.
3. Do not mutate the matrix or oracle to hide the failing family without real convergence.

## No-go

- no phrase-fitting or regex-hardcode in runtime core
- no new row-child TP for this same family before the structural slice lands
- no weakening of oracle/gates or reclassification of invalid runs as closure evidence
- no generator-only scrub that leaves canonical pending-question semantics duplicated in another place

## Risks / blockers

- existing scenario helper paths may still mix lexical tags with state-derived expectations for adjacent availability turns
- over-broad convergence can regress genuinely distinct `slot_compare` families
- structural reuse may require careful factoring to avoid duplicating runtime contract logic in a second helper

## Residual architecture debt (mandatory)

### Current residuals accepted in this block

- scenario-governance still remains a separate executable layer even after this first convergence slice
- broader timeout/degrade migration beyond `P1.6o119` remains open
- acceptance proof (`lock/replay/full`) remains blocked

### Why not in this block

- this block is intentionally the first bounded `Track A` slice for the newly surfaced active booking slot-guidance family

### Risk if deferred

- the worktree will keep surfacing adjacent generator/oracle mismatches even when runtime semantic truth is already correct

### Linked follow-up Task Package(s)

- future proof-lane TP after `P1.6o121` deterministic closure
- later broader `Track A` convergence slices for adjacent oracle families
- later `Track B` resolver coverage expansion
- `P1.7` acceptance replay gate after structural tracks land

### Expiry/trigger to stop deferral

- before any further guarded `dev/acceptance` rerun beyond `P1.6o121`

## Next-block contract (mandatory)

### Next block objective

- after deterministic closure of `P1.6o121`, publish and run one fresh proof-lane block to verify the surfaced `Track A` family is no longer the first fail and to truthfully classify the next remaining class, if any.

### First deterministic check command

- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o120-l2-dev-20260313-a1-r1 --status done --strict-artifacts && pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py && git diff --check`

### Blocked-by conditions

- `P1.6o120` surfaced expectation/oracle drift is not synced truthfully in active docs/state
- targeted scenario-generator contract tests are not green
- the proposed fix still depends on stale `slot_compare` oracle knowledge instead of canonical interaction semantics

### Owner role for closure

- `Top Architect + Brain`

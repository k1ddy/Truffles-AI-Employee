# TP-2026-03-14-p1.6o195-scenario-generation-read-timeout-inner-retry-a1

- Название/цель: закрыть surfaced proof-lane blocker после `P1.6o194`, где scenario generation now truthfully surfaces inner error detail `The read operation timed out`, но generated LLM batch still has no inner retry because guarded proof runs keep `scenario_llm_max_attempts=1`.

## Canon refs

- Binding roadmap: `docs/TASK_PACKAGES/TP-2026-03-13-p1.6o135-beauty-salon-canonical-control-plane-roadmap-a1.md`
- Prior deterministic closure: `docs/TASK_PACKAGES/TP-2026-03-14-p1.6o193-scenario-generation-progress-error-detail-a1.md`
- Blocked proof lane: `docs/TASK_PACKAGES/TP-2026-03-14-p1.6o194-post-p1.6o193-proof-lane-a1.md`
- Parent closure program: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Main remediation: `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- State baseline: `STATE.md`

## One web search (mandatory before implementation)

- Exact query: `OpenAI Python SDK retries timeouts README github openai-python`
- Date/time: `2026-03-14 16:26 +05:00`
- Opened sources:
  - official OpenAI Python SDK README (`https://github.com/openai/openai-python`)
- Found ready solutions:
  - request timeouts are retryable transport failures;
  - the official client retries them twice by default with short backoff.
- Decision: `reuse/integrate`
  - reuse the existing inner batch retry mechanism already present in `scripts/booking_dialog_scenarios.py` instead of building a new retry path;
  - wire guarded proof runs so generated LLM scenario batches no longer default to a single attempt on retryable read timeouts.
- Rejected variants:
  - another rerun without changing inner retry behavior;
  - broad timeout-budget rewrite before using the existing retry mechanism;
  - disabling timeout protection.
- Source quality:
  - primary/official source: `openai/openai-python` README.

## Git context

- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `origin/main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect` remove branch + worktree after merge

## Root cause (mandatory)

- Symptom:
  - `p1.6o194-l2-dev-20260314-a1-r1` now reports `scenario_generation_timeout (batch=2 attempt=1 event=batch_attempt_error error="The read operation timed out")` before `scenarios.json` is emitted.
- Minimal reproduction:
  1. run guarded proof lane `P1.6o194` on parity-verified runtime `http://127.0.0.1:18184`;
  2. let `scripts/booking_dialog_scenarios.py` request an LLM batch with `request_timeout=60`;
  3. observe `batch_attempt_error.error="The read operation timed out"`;
  4. note that guarded proof config still uses `scenario_llm_max_attempts=1`, so the batch exits without any inner retry.
- Evidence:
  - `/tmp/booking_quality/p1.6o194-l2-dev-20260314-a1-r1/summary.json`
  - `/tmp/booking_quality/p1.6o194-l2-dev-20260314-a1-r1/brief.md`
  - `/tmp/booking_quality/p1.6o194-l2-dev-20260314-a1-r1/manual_audit.json`
  - `ops/diagnose.py`
  - `scripts/booking_dialog_scenarios.py`
  - `truffles-api/tests/test_diagnose_run_command.py`
- Five Whys:
  1. Why is the blocker now precise enough to route a deterministic slice? Because the rerun now surfaces the inner error detail.
  2. Why did scenario generation still fail? Because a batch request hit a read timeout.
  3. Why was there no recovery inside the batch? Because guarded proof runs still set inner max attempts to one.
  4. Why is that a tooling blocker rather than product evidence? Because no scenarios or turns were emitted.
  5. Why is inner retry the narrowest next slice? Because the generator already has a bounded retry mechanism; guarded proof simply does not reuse it.
- Root cause statement:
  - scenario generation fails closed on the first read timeout because guarded proof runs keep `scenario_llm_max_attempts=1`, even though the generator already supports bounded inner retries.
- Fix mechanism:
  - reuse the existing inner batch retry path by raising the guarded proof default for `scenario_llm_max_attempts` to a bounded value and add targeted tests that protect the new effective proof contour.

## Reuse-first decision (mandatory)

- Reuse:
  - existing inner retry loop in `scripts/booking_dialog_scenarios.py`
  - existing scenario-generation command assembly in `ops/diagnose.py`
  - existing tests in `truffles-api/tests/test_diagnose_run_command.py`
- Integrate:
  - keep the fix inside proof-tooling configuration only; no runtime business logic changes
- Build:
  - add only the bounded default/reuse change and targeted tests for the surfaced contour

## Release safety

- Rollout: local tooling-only change
- Go signal:
  - generated LLM scenario batches get bounded inner retry on read timeouts
- No-go signal:
  - retry budget becomes unbounded or proof gates are weakened
- Rollback:
  - revert only the bounded inner-retry configuration change and tests; keep `P1.6o194` blocked truth intact

## Invariant

1. No weakening of proof gates.
2. Retry remains bounded.
3. No runtime/business behavior changes.
4. No extra proof rerun before deterministic closure.

## Scope

- raise the guarded proof default for inner scenario-generation LLM attempts in `ops/diagnose.py`
- add targeted tests in `truffles-api/tests/test_diagnose_run_command.py`
- sync canon docs/state/session after deterministic closure

## Out of scope

- fresh guarded rerun before deterministic closure
- broad timeout-budget rewrite
- any booking/runtime semantic changes

## Touch-list

- `ops/diagnose.py`
- `truffles-api/tests/test_diagnose_run_command.py`
- `STATE.md`
- `docs/SESSIONS/SESSION-2026-02-19-llm-first-firebreak-a1.md`
- `docs/SESSION_INDEX.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-14-p1.6o194-post-p1.6o193-proof-lane-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-14-p1.6o195-scenario-generation-read-timeout-inner-retry-a1.md`

## Plan

1. Add targeted unit coverage that protects the effective inner retry count for generated LLM scenario batches.
2. Reuse the existing inner retry path by raising the guarded proof default from one attempt to a bounded higher value.
3. Re-run targeted tooling tests and static checks.
4. Sync `P1.6o194` truth as an inner read-timeout proof blocker and mark `P1.6o195` complete if green.
5. Publish the next proof-lane TP.

## DoD

1. Generated LLM scenario batches no longer default to a single inner attempt in guarded proof runs.
2. Targeted tooling tests are green.
3. Docs/state/session truthfully record `P1.6o194` as blocked on inner read timeout and publish the next admissible rerun TP.

## Checks

- `python3 -m py_compile ops/diagnose.py truffles-api/tests/test_diagnose_run_command.py`
- `pytest -q truffles-api/tests/test_diagnose_run_command.py -k "scenario_timeout or generate_batch"`
- `git diff --check`
- `SESSION_AGENT=a1 scripts/session_check.sh`

## Evidence

1. `/tmp/booking_quality/p1.6o194-l2-dev-20260314-a1-r1/summary.json`
2. `/tmp/booking_quality/p1.6o194-l2-dev-20260314-a1-r1/brief.md`
3. `/tmp/booking_quality/p1.6o194-l2-dev-20260314-a1-r1/manual_audit.json`
4. `ops/diagnose.py`
5. `scripts/booking_dialog_scenarios.py`
6. `truffles-api/tests/test_diagnose_run_command.py`
7. updated docs listed above

## Rollback

1. Revert only the bounded inner-retry configuration change and tests.
2. Keep `P1.6o194` invalid-run truth intact.
3. Do not mutate proof artifacts.

## No-go

- no silent pass when scenario generation times out
- no extra proof rerun before deterministic closure of this blocker
- no runtime or oracle changes in this block

## Risks / blockers

- even with inner retry, scenario generation may still hit timeout budget and surface a different blocker
- retry increases proof-lane wall time
- another proof-validity blocker may surface once batch retries are enabled

## Residual architecture debt (mandatory)

### Current residuals accepted in this block

- proof admission still depends on synchronous OpenAI transport probing and scenario-generation subprocess execution
- guarded rerun remains blocked until post-fix proof evidence exists

### Why not in this block

- this slice is only to reuse the existing bounded inner retry path, not to redesign scenario generation or timeout policy

### Risk if deferred

- the worktree stays blocked on a retryable inner read timeout even though the generator already has a bounded retry mechanism

### Linked follow-up Task Package(s)

- next proof-lane TP after deterministic closure of `P1.6o195`

### Expiry/trigger to stop deferral

- immediately after deterministic closure and one fresh guarded rerun classification

## Next-block contract (mandatory)

### Next block objective

- run exactly one fresh guarded rerun after `P1.6o195` so proof verifies whether the inner read-timeout blocker moved off the path and the next admissible result surfaces.

### First deterministic check command

- `python3 -m py_compile ops/diagnose.py truffles-api/tests/test_diagnose_run_command.py && pytest -q truffles-api/tests/test_diagnose_run_command.py -k "scenario_timeout or generate_batch"`

### Blocked-by conditions

- `P1.6o195` deterministic closure is not green
- `P1.6o194` invalid-run truth is not synced
- targeted tooling tests are not green

### Owner role for closure

- `Top Architect + Brain`

## Execution status

- `todo`

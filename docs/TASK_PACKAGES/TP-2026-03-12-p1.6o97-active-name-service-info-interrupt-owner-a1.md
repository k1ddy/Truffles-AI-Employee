# TP-2026-03-12-p1.6o97-active-name-service-info-interrupt-owner-a1

- Название/цель: закрыть surfaced `M33`, где proof lane `P1.6o96` показал, что factual `catalog.service_query` side-question `Какова цена на маникюр?` под активным `expected_reply_type=name` снова схлопывается в generic `booking_interrupt/time`: policy-core already keeps `goal=booking`, `next_question=name`, and persisted interaction state still knows `interaction_target=time` / `interaction_relation=ask_about_requested_slot`, но tool-response follow-up re-derives slot order and drops the active `name` resume.

## Canon refs

- Parent verdict: `docs/TASK_PACKAGES/TP-2026-03-09-p1.6o60a-remaining-closure-architecture-verdict-a1.md`
- Closure program: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Main remediation: `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- Prior proof lane: `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o96-post-m32-proof-lane-a1.md`
- Executable owner core: `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o81-machine-readable-owner-matrix-a1.md`, `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o83-owner-resolver-m27-vertical-slice-a1.md`
- Owner matrix artifact: `truffles-api/app/knowledge/generic/INTERACTION_OWNER_MATRIX.yaml`
- State baseline: `STATE.md`

## Git context

- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`
- Base ref: `main`
- Merge policy: `merge only, no rebase`
- Cleanup: `Brain/Top Architect` remove branch + worktree after merge

## One web search (mandatory before implementation)

- Query: `site:stately.ai/docs history states interruption resume state machine`
- Date/time: `2026-03-12T16:05:00+05:00`
- Sources opened:
  - official Stately docs: `https://stately.ai/docs/parent-states`
- Source quality:
  - official product documentation
- Found reusable ideas:
  - interruption handling should preserve the active parent-state contract instead of recalculating a generic substate when the side-question finishes;
  - explicit state ownership is more robust than re-deriving progression from partial slot completeness after every side-question.
- Decision:
  - `reuse + integrate`
  - reuse the executable owner-matrix + persisted `interaction_state` path instead of adding another local phrase/slot-order patch;
  - integrate the new row into `owner_resolver` and let the tool-response branch preserve the active `name` resume contract when matrix-owned factual side-question rows match.
- Rejected options:
  - phrase-hardcode for the exact pricing sentence;
  - treating `expected_reply_type=time` as acceptable because booking `datetime` is still only partially grounded;
  - broad unconditional bypass of `_derive_booking_followup_prompt` for every `catalog.service_query` reply.

## Root cause (mandatory)

- Symptom:
  - fresh guarded `dev L2` `p1.6o96-l2-dev-20260312-a1-r1` stops at dialog `1`, turn `4`, user text `Какова цена на маникюр?`
- Minimal reproduction:
  1. Inspect `/tmp/booking_quality/p1.6o96-l2-dev-20260312-a1-r1/responses.jsonl` for `message_id=LLM-QUAL-p1.6o96-l2-dev-20260312-a1-r1-001-04-f7762f`.
  2. Confirm previous turns already moved the dialog into active `name` resume under requested-slot follow-up (`turn 2`: `Есть ли свободные слоты на завтра?`; `turn 3`: `Мне нужно примерно в 15:00.`).
  3. Confirm the failing turn returns a truthful pricing answer and judge `pass`, but final meta still says `expected_reply_type=time`, `expected_reply_reason=booking_interrupt`, `interaction_owner=question_contract:policy_core_degraded_collect`.
  4. Compare the same turn against persisted `interaction_state` and policy-core output: `goal=booking`, `next_question=name`, `open_questions=['name']`, grounded service referent, and existing `interaction_target=time` / `interaction_relation=ask_about_requested_slot` are all still present.
- Evidence:
  - `/tmp/booking_quality/p1.6o96-l2-dev-20260312-a1-r1/summary.json`
  - `/tmp/booking_quality/p1.6o96-l2-dev-20260312-a1-r1/brief.md`
  - `/tmp/booking_quality/p1.6o96-l2-dev-20260312-a1-r1/responses.jsonl`
  - `/tmp/booking_quality/p1.6o96-l2-dev-20260312-a1-r1/trace_bundle.jsonl`
  - `/tmp/booking_quality/p1.6o96-l2-dev-20260312-a1-r1/manual_audit.md`
- Five Whys:
  1. Why did the turn fail? Because runtime reported the next expected reply as `time`, while the scenario contract expected the active `name` resume to survive the pricing side-question.
  2. Why did runtime reopen `time`? Because the tool-response follow-up branch re-derived booking progression from partial slot completeness and produced generic `booking_interrupt` follow-up.
  3. Why did that override the active resume? Because the executable owner layer only handled `M27`-style info rows and did not yet own this `catalog.service_query` factual interrupt family.
  4. Why is this not just scenario drift? Judge passes the pricing answer, but trace/meta/outcome show the wrong owner and resume axis; this is a runtime owner gap, not a text-quality failure.
  5. Why is this a new row instead of replaying `M27` or `M32`? `M27` preserved `time` resume for booking-confirm pricing interrupt via `tool_action=info`; `M32` preserved requested-slot follow-up under `collect`; `r119` surfaces the adjacent factual `catalog.service_query` side-question under active `name` resume.
- Root cause statement:
  - the executable owner core is only partially applied: persisted `interaction_state` still knows the active requested-slot follow-up contract, but the tool-response branch re-derives generic slot-order follow-up for factual `catalog.service_query` replies because no owner-matrix row yet claims this active-name service-info interrupt family.
- Fix mechanism:
  - freeze `M33` in the machine-readable owner matrix with runtime-match/effects for active-name factual service-info interrupt;
  - generalize `owner_resolver` to resolve any row with `runtime_match` / `runtime_effects`, not just `M27`;
  - invoke resolver on the tool-response path before slot-order follow-up derivation, preserve `interaction_owner`, and keep `expected_reply_type=name` when the new row matches;
  - add targeted unit + integration regressions and keep adjacent `M27` behavior green.

## Reuse-first decision (mandatory)

- Reuse:
  - existing machine-readable owner matrix artifact and schema
  - existing `owner_resolver` service and runtime hook pattern from `P1.6o83`
  - existing `interaction_state` persistence from `P1.6o82`
  - existing booking prompt suppression for factual `catalog.service_query` replies
- Integrate:
  - new `M33` matrix runtime row
  - generic resolver iteration over machine-readable runtime rows
  - bounded tool-response preservation of matrix-owned active resume
- Do not build:
  - new phrase router
  - new fallback booking interrupt owner outside the matrix/resolver path

## Execution profile

- TP mode: `implementation`
- Doc touch budget (files): `10`
- Code dominance: `required`
- Override token: `none`
- Why this profile fits: surfaced family is a bounded adjacent owner row that fits the already-published executable matrix/resolver architecture.

## Release safety

- Strategy: bounded executable-owner extension only
- Go signals:
  - factual `catalog.service_query` side-question under active-name requested-slot follow-up keeps `expected_reply_type=name`
  - interaction owner is preserved through `owner_resolver` instead of generic `booking_interrupt`
  - reply body stays pricing/info-only without booking prompt leakage
  - adjacent `M27` owner-resolver path remains green
- No-go signals:
  - generic `catalog.service_query` rows outside active-name requested-slot follow-up start inheriting `name` resume incorrectly
  - booking prompt text leaks into factual pricing replies
  - `M27` or other resolver rows regress
- Rollback verification:
  - revert only the bounded `M33` resolver/runtime/matrix delta if adjacent rows regress

## Invariant

1. Existing `M27` resolver behavior remains green.
2. Factual pricing replies stay truthful and do not append booking prompt text.
3. No phrase-hardcode branch is added in runtime core.
4. Exactly one next expensive run happens only after deterministic closure is green.

## Scope

- publish surfaced row `M33`
- extend executable owner-matrix / resolver path for the new row
- preserve active `name` resume for matrix-owned factual service-info interrupt
- add targeted unit + runtime regressions

## Out of scope

- scenario/oracle redesign
- acceptance `lock/replay/full`
- broader info-interrupt canon changes outside the bounded `M33` row

## Touch-list

- `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o97-active-name-service-info-interrupt-owner-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-12-p1.6o96-post-m32-proof-lane-a1.md`
- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- `contracts/policy/interaction_owner_matrix.v1.jsonschema`
- `truffles-api/app/knowledge/generic/INTERACTION_OWNER_MATRIX.yaml`
- `truffles-api/app/services/owner_resolver.py`
- `truffles-api/app/routers/webhook/decision.py`
- `truffles-api/tests/test_owner_resolver.py`
- `truffles-api/tests/test_message_endpoint.py`
- `truffles-api/tests/test_interaction_owner_matrix_contract.py`
- `STATE.md`

## Plan (1..N)

1. Freeze `r119` as audited proof evidence and publish `M33`.
2. Extend the executable owner matrix/resolver path to match the new factual service-info interrupt row.
3. Preserve active `name` resume on the tool-response branch without re-deriving generic slot order for matrix-owned rows.
4. Add targeted unit + runtime regressions with adjacent `M27` coverage.
5. Run focused checks and `git diff --check`.
6. Publish the next proof-lane block only after deterministic closure is green.

## DoD

1. `r119` is recorded truthfully as surfaced `M33` in active docs and the owner matrix.
2. Active-name pricing interrupt `Какова цена на маникюр?` preserves `expected_reply_type=name` and owner evidence instead of generic `booking_interrupt/time`.
3. The fix goes through the executable matrix/resolver path, not a new phrase-specific runtime branch.
4. Focused deterministic unit + runtime regressions are green.
5. The next admissible block is the fresh proof lane `P1.6o98`.

## Expensive run control (mandatory)

- Hypothesis:
  - after bounded `M33` executable-owner closure, a fresh guarded `dev L2` will no longer stop on dialog `1`, turn `4`, `Какова цена на маникюр?`
- Expected measurable effect:
  - targeted deterministic checks turn green, and the next guarded proof lane either moves past this row or surfaces a different first-fail family
- Stop condition:
  - no guarded run inside this block; stop after deterministic closure and publish the next proof TP

## Checks

- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o96-l2-dev-20260312-a1-r1 --status done --strict-artifacts`
- `pytest -q truffles-api/tests/test_owner_resolver.py truffles-api/tests/test_interaction_owner_matrix_contract.py`
- `pytest -q truffles-api/tests/test_message_endpoint.py -k "owner_resolver_m27 or owner_resolver_m33"`
- `python3 -m py_compile truffles-api/app/services/owner_resolver.py truffles-api/app/routers/webhook/decision.py truffles-api/tests/test_owner_resolver.py truffles-api/tests/test_message_endpoint.py truffles-api/tests/test_interaction_owner_matrix_contract.py`
- `git diff --check`

## Execution status

- `done`

## Evidence

1. `/tmp/booking_quality/p1.6o96-l2-dev-20260312-a1-r1/summary.json`
2. `/tmp/booking_quality/p1.6o96-l2-dev-20260312-a1-r1/brief.md`
3. `/tmp/booking_quality/p1.6o96-l2-dev-20260312-a1-r1/responses.jsonl`
4. `/tmp/booking_quality/p1.6o96-l2-dev-20260312-a1-r1/trace_bundle.jsonl`
5. `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o96-l2-dev-20260312-a1-r1 --status done --strict-artifacts`
6. `pytest -q truffles-api/tests/test_owner_resolver.py truffles-api/tests/test_interaction_owner_matrix_contract.py` -> `5 passed`
7. `pytest -q truffles-api/tests/test_message_endpoint.py -k "owner_resolver_m27 or owner_resolver_m33"` -> `3 passed, 376 deselected`
8. `python3 -m py_compile truffles-api/app/services/owner_resolver.py truffles-api/app/routers/webhook/decision.py truffles-api/tests/test_owner_resolver.py truffles-api/tests/test_message_endpoint.py truffles-api/tests/test_interaction_owner_matrix_contract.py`

## Rollback

1. Keep `P1.6o96` proof truth intact.
2. Revert only the bounded `M33` owner-resolver/runtime delta if adjacent rows regress.
3. Do not mutate the matrix to hide surfaced evidence.

## No-go

- no phrase fitting for the exact pricing sentence
- no generic `booking_interrupt/time` waiver for matrix-owned active-name service-info rows
- no second expensive run before deterministic closure is green

## Risks / blockers

- the tool-response branch can still regress to slot-order derivation if matrix-owned rows are not exempted from `_derive_booking_followup_prompt`
- broad runtime matching could incorrectly preserve `name` on unrelated service-query rows if the row boundary is not kept on existing interaction-owner contract
- proof remains non-canonical until infra/tool-evidence red is separately closed

## Residual architecture debt (mandatory)

### Current residuals accepted in this block

- factual service-info interrupt ownership is still enforced inside `decision.py`, not a dedicated migrated owner handler
- closure proof remains on `dev L2`; `lock/replay/full` stay blocked

### Why not in this block

- this block only closes surfaced row `M33`

### Risk if deferred

- active-name factual side-questions keep reopening generic `time` collect and hide the active interaction-owner contract

### Linked follow-up Task Package(s)

- `P1.6o98` post-`M33` proof lane
- `P1.7` acceptance replay gate after the closure lane is green enough to advance

### Expiry/trigger to stop deferral

- before any next guarded `dev/acceptance` rerun beyond the `M33` proof admission

## Next-block contract (mandatory)

### Next block objective

- publish and run `P1.6o98`: prove on one fresh guarded `dev L2` that `M33` is gone from the first-fail position and truthfully admit the next family, if any.

### First deterministic check command

- `python3 ops/diagnose.py llm-quality-audit --run-dir /tmp/booking_quality/p1.6o96-l2-dev-20260312-a1-r1 --status done --strict-artifacts && pytest -q truffles-api/tests/test_owner_resolver.py truffles-api/tests/test_interaction_owner_matrix_contract.py && pytest -q truffles-api/tests/test_message_endpoint.py -k "owner_resolver_m27 or owner_resolver_m33" && git diff --check`

### Blocked-by conditions

- `M33` is not synced truthfully into active docs and the owner matrix
- executable owner-resolver regressions are not green
- live runtime fingerprint for `P1.6o98` is not verified against current `HEAD`

### Owner role for closure

- `Top Architect + Brain`

# TP-2026-03-07-p3.1r-demo-salon-booking-contact-evidence-contract-a1

- Название/цель: закрыть свежий `demo_salon` `dev L2` blocker `booking_commit_without_required_contact`, сохранив fail-closed booking contract и устранив ложный strict-red через явный contact-evidence contract на успешном `calendar.book_slot`.
- Parent TP: `docs/TASK_PACKAGES/TP-2026-03-06-p1.6-local-realism-canonical-lock-a1.md`
- Related prior TP: `docs/TASK_PACKAGES/TP-2026-03-06-p3.1-book-slot-contact-minimum-fail-closed-a1.md`
- Canon refs: `AGENTS.md`, `STATE.md`, `STRATEGY/REQUIREMENTS.md`, `TECH.md`, `docs/runbooks/BOOKING_CONFIRM_VERIFY.md`, `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`

## One web search (mandatory before implementation)

- Query: `site:opentelemetry.io docs events attributes logs structured data semantic conventions`
- Time (UTC): `2026-03-07T04:13:52Z`
- Sources:
  - `https://opentelemetry.io/docs/specs/semconv/general/attributes/`
  - `https://opentelemetry.io/docs/specs/otel/logs/data-model/`
- Ready solutions found:
  - machine-readable event/log attributes must carry stable structured outcome fields instead of forcing downstream analysis to infer meaning from free text
  - semantic conventions are intended to make later automated analysis and correlation reliable across producers/consumers
- Decision (`reuse/integrate/build`): `integrate + build`
  - integrate explicit contact-source evidence into successful booking meta/trace
  - build a local guard adjustment because the failure is in Truffles runtime/quality evidence, not in an external reusable component
- Rejected options:
  - weaken `booking_commit_without_required_contact` thresholds or mark the run green manually
  - rerun expensive `dev L2` before code delta
  - add prompt/lexicon tuning for booking contact collection

## Root cause (mandatory)

- Symptom:
  - the first fresh post-cleanup `demo_salon` guarded `dev L2` run `p1.6-l2-dev-seed42-r20` stopped at `dialog_index=1`, `turn_index=4` with strict reason `booking_commit_without_required_contact`
  - bot reply was `Заявка на запись принята. Менеджер подтвердит время.` after `calendar.book_slot` returned `ok`
- Minimal reproduction:
  1. inspect `/tmp/booking_quality/p1.6-l2-dev-seed42-r20/responses.jsonl` for `dialog_index=1`, `turn_index=4`
  2. inspect `/tmp/booking_quality/p1.6-l2-dev-seed42-r20/manual_audit.md`
  3. inspect `truffles-api/app/services/tool_registry_service.py` successful `calendar.book_slot` path
  4. inspect `truffles-api/app/routers/webhook/decision.py` tool call path into `execute_tool_action(...)`
  5. inspect `ops/diagnose.py::_llm_quality_has_booking_commit_without_required_contact(...)`
- Evidence:
  - forensic artifacts:
    - `/tmp/booking_quality/p1.6-l2-dev-seed42-r20/summary.json`
    - `/tmp/booking_quality/p1.6-l2-dev-seed42-r20/brief.md`
    - `/tmp/booking_quality/p1.6-l2-dev-seed42-r20/responses.jsonl`
    - `/tmp/booking_quality/p1.6-l2-dev-seed42-r20/trace_bundle.jsonl`
    - `/tmp/booking_quality/p1.6-l2-dev-seed42-r20/manual_audit.md`
  - turn forensic facts:
    - `tool_decision=ok`
    - `appointment_id=4ad46e02-7ff5-4d18-869c-af79b7755b31`
    - `profile_sync.name_synced=false`
    - `profile_sync.phone_synced=true`
    - `profile_sync.phone_source=remote_jid`
    - `customer_phone_source=user_profile`
    - no `customer_name_source` in success meta
  - local DB evidence for the same conversation user after the stopped run:
    - conversation `56204556-cc47-4621-9007-722e48bcf65b`
    - user `1007e6e5-c54d-4b95-94e3-66d0bc8b4d4f`
    - `users.name='Айгуль'`, `users.phone='99961433752'`
  - existing `P3.1` contract already allows user-profile name / phone fallback in `resolve_booking_contact_minimum(...)`; the quality failure is therefore an evidence-contract mismatch, not proof of a newly reopened unsafe booking write
- Five Whys:
  1. Why did the strict gate fire: the quality guard could not prove that `name + phone` contact minimum existed on the successful commit.
  2. Why could it not prove the name: successful `calendar.book_slot` meta emits `customer_phone_source`, but not `customer_name_source`.
  3. Why was phone evidence partially misleading: the decision layer pre-resolves remote-JID fallback into a bare `user_phone`, so the downstream contact resolver reports `user_profile` unless the original source is preserved explicitly.
  4. Why does that become a blocker: the first fresh expensive run after cleanup stopped on this family by design, so an observability gap now blocks honest progress.
  5. Why is this not a prompt problem: the runtime already committed through the intended booking/tool path; the missing proof is in machine-readable meta/trace and guard interpretation.
- Root cause statement:
  - the runtime already accepts valid booking contact minimum via existing user-profile name plus remote-JID phone fallback, but the successful `calendar.book_slot` path does not expose enough machine-readable contact-source evidence, and the quality guard therefore misclassifies a valid commit as `booking_commit_without_required_contact`.
- Fix mechanism:
  - preserve `user_phone_source` through the booking tool call path
  - emit explicit contact-source evidence on successful `calendar.book_slot`
  - teach the quality guard to accept explicit source evidence for successful booking commits
  - add focused regressions; no expensive rerun in this block

## Invariant

1. `calendar.book_slot` must remain fail-closed when resolved `name + phone` are actually missing.
2. No threshold/oracle weakening is allowed.
3. No prompt/lexicon/demo-specific patch-fitting is allowed as a fix mechanism.
4. No new expensive `dev L2 / lock / replay` run starts before this evidence-contract block is green on deterministic checks.

## Scope

- `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-06-p1.6-local-realism-canonical-lock-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-07-p3.1r-demo-salon-booking-contact-evidence-contract-a1.md`
- `truffles-api/app/routers/webhook/decision.py`
- `truffles-api/app/services/booking_transition_owner.py`
- `truffles-api/app/services/tool_registry_service.py`
- `ops/diagnose.py`
- `truffles-api/tests/test_booking_appointments.py`
- `truffles-api/tests/test_booking_quality_response_guard.py`
- `truffles-api/tests/test_message_endpoint.py`

## Out of scope

- new heavy `demo_salon` quality run in this block
- re-opening `P3.1` fail-closed contact gate semantics unless RCA proves an actual unsafe create path
- canary/lock/replay/full

## Plan (1..N)

1. Record the blocker and RCA in TP/master docs.
2. Preserve source-of-truth contact evidence through the successful booking tool path.
3. Update the quality guard to consume explicit contact-source evidence before inferring failure.
4. Add focused regressions for booking success meta and guard parity.
5. Rerun deterministic checks only.

## DoD

1. Successful `calendar.book_slot` meta/trace includes explicit contact-source evidence sufficient for later machine validation.
2. `booking_commit_without_required_contact` still fires when contact minimum is truly missing.
3. The fresh `r20` family is closed on deterministic evidence without a new expensive rerun.
4. Docs clearly state that the next allowed expensive action is one fresh guarded `demo_salon` `dev L2` only after this block is green.

## Checks

- `pytest -q truffles-api/tests/test_booking_appointments.py -k "contact_minimum or remote_jid_phone_fallback"`
- `pytest -q truffles-api/tests/test_message_endpoint.py -k "book_slot_uses_booking_phone_from_context or book_slot_missing_phone_sets_expected_reply_phone or book_slot_passes_remote_jid_phone_source_to_tool"`
- `pytest -q truffles-api/tests/test_message_endpoint.py`
- `pytest -q truffles-api/tests/test_booking_quality_response_guard.py`
- `pytest -q truffles-api/tests/test_demo_salon_eval.py`
- `pytest -q truffles-api/tests/test_booking_chaos_dialogs.py`
- `ruff check truffles-api/app/routers/webhook/decision.py truffles-api/app/services/booking_transition_owner.py truffles-api/app/services/tool_registry_service.py ops/diagnose.py truffles-api/tests/test_booking_appointments.py truffles-api/tests/test_booking_quality_response_guard.py truffles-api/tests/test_message_endpoint.py`
- `git diff --check`

## Execution status

- `done`
- Deterministic closure completed; no new expensive rerun was started in this block.

## Evidence

1. `/tmp/booking_quality/p1.6-l2-dev-seed42-r20/summary.json`
2. `/tmp/booking_quality/p1.6-l2-dev-seed42-r20/brief.md`
3. `/tmp/booking_quality/p1.6-l2-dev-seed42-r20/responses.jsonl`
4. `/tmp/booking_quality/p1.6-l2-dev-seed42-r20/trace_bundle.jsonl`
5. `/tmp/booking_quality/p1.6-l2-dev-seed42-r20/manual_audit.md`
6. local DB query for conversation/user pair `56204556-cc47-4621-9007-722e48bcf65b` / `1007e6e5-c54d-4b95-94e3-66d0bc8b4d4f`
7. deterministic closure outputs on `2026-03-07`:
   - `pytest -q truffles-api/tests/test_booking_appointments.py -k "contact_minimum or remote_jid_phone_fallback"` -> `4 passed, 73 deselected`
   - `pytest -q truffles-api/tests/test_message_endpoint.py -k "book_slot_uses_booking_phone_from_context or book_slot_missing_phone_sets_expected_reply_phone or book_slot_passes_remote_jid_phone_source_to_tool"` -> `3 passed, 297 deselected`
   - `pytest -q truffles-api/tests/test_message_endpoint.py` -> `300 passed, 2 warnings`
   - `pytest -q truffles-api/tests/test_booking_quality_response_guard.py` -> `60 passed`
   - `pytest -q truffles-api/tests/test_demo_salon_eval.py` -> `17 passed`
   - `pytest -q truffles-api/tests/test_booking_chaos_dialogs.py` -> `1 passed`
   - `ruff check ...` -> `All checks passed!`
   - `git diff --check` -> `clean`

## Rollback

1. revert only the contact-evidence/meta/guard patch
2. keep `demo_salon` expensive runs blocked until deterministic contour is green again

## No-go

- no new expensive rerun before code delta and deterministic closure
- no weakening of the strict quality gate
- no semantic shortcuts in runtime core

## Risks/Blockers

1. if DB/user-profile fallback is judged non-canonical by product contract, the block will turn into a real `P3.1` semantic reopening instead of an observability fix
2. success-meta additions can break stale tests that were asserting a smaller meta surface

## Residual architecture debt (mandatory)

- Current residuals accepted in this block:
  - no fresh post-fix `demo_salon` `dev L2` evidence yet
  - `P1.7` still not created
  - `P6` remains deferred
- Why not in this block:
  - this block only restores trustworthy booking-contact evidence on the already-running `demo_salon` lane
- Risk if deferred:
  - every future expensive `demo_salon` run can stop on the same false-red family
- Linked follow-up Task Package(s):
  - `docs/TASK_PACKAGES/TP-2026-03-06-p1.6-local-realism-canonical-lock-a1.md`
  - `TBD` for `P1.7 Acceptance replay + regression gate`
- Expiry/trigger to stop deferral:
  - before the next guarded `demo_salon` `dev L2`

## Next-block contract (mandatory)

- Next block objective:
  - run exactly one fresh guarded `demo_salon` `dev L2` on `http://127.0.0.1:18184` and verify whether the next blocker family is real after contact-evidence parity closure
- First deterministic check command:
  - `pytest -q truffles-api/tests/test_message_endpoint.py && pytest -q truffles-api/tests/test_booking_quality_response_guard.py && pytest -q truffles-api/tests/test_demo_salon_eval.py && pytest -q truffles-api/tests/test_booking_chaos_dialogs.py`
- Blocked-by conditions:
  - fresh guarded `dev L2` budget window / run-id / `PG checklist` not prepared
  - `OPENAI_API_KEY` / judge access missing for the guarded run
  - runtime `http://127.0.0.1:18184` not refreshed to the current worktree state
- Owner role for closure:
  - `Top Architect + Brain`

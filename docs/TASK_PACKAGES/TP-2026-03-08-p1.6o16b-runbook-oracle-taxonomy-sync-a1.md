# TP-2026-03-08-p1.6o16b-runbook-oracle-taxonomy-sync-a1

- Название/цель: закрыть `P1.6o16b` как отдельный child slice — синхронизировать scenario/oracle taxonomy для pending-question interaction acts до runtime-owner кода, чтобы missing family больше не оставался безымянным в generator/guard contracts.
- Parent TP: `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o16-booking-time-guidance-question-owner-a1.md`
- Canon refs: `AGENTS.md`, `STATE.md`, `STRUCTURE.md`, `STRATEGY/REQUIREMENTS.md`, `docs/runbooks/BOOKING_CONFIRM_VERIFY.md`, `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`, `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`, `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o16-booking-time-guidance-question-owner-a1.md`
- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`

## One web search (mandatory before implementation)

- Query: `site:rasa.com/docs forms unhappy path requested slot user asks a question`
- Time (UTC): `2026-03-08T04:50:54Z`
- Sources:
  - `https://rasa.com/docs/rasa/forms/`
- Ready solutions found:
  - slot-filling loops need explicit unhappy-path handling for turns that discuss the requested slot instead of filling it;
  - the requested slot should stay active after that interaction, so the resume contract remains explicit.
- Decision (`reuse/integrate/build`): `reuse + integrate`
  - reuse the existing `expected_reply` / `question_contract` continuity;
  - integrate explicit interaction-act taxonomy into scenario/oracle contracts;
  - do not weaken `booking_slot_stall` and do not introduce tag-only waivers.
- Rejected options:
  - using `allow_booking_stall=true` as a taxonomy substitute;
  - adding the new family only as free-text examples without structured oracle fields;
  - jumping straight to runtime-owner code while generator/guard taxonomy still drifts.

## Root cause (mandatory)

- Symptom:
  - `P1.6o16` doc-first rescope proved that `r11` was not just one phrase bug, but the current generator/oracle taxonomy still lacked a first-class contract for pending-question interaction acts.
  - Without a named class in taxonomy, expensive runs could only surface the family post factum.

- Minimal reproduction:
  1. inspect `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o16-booking-time-guidance-question-owner-a1.md`
  2. inspect `scripts/booking_dialog_scenarios.py` around allowed tags, fallback templates, `EXTRA_TURNS`, and `_merge_expectations`
  3. inspect `ops/diagnose.py` around `booking_slot_stall` evaluation

- Evidence:
  - `scripts/booking_dialog_scenarios.py` previously named `wrong_slot`, `interrupt`, `time_alt`, `delay`, etc., but had no explicit act class for `question about the currently requested slot`
  - `_merge_expectations` only knew existing slot/info/handoff tags, so a new interaction family had no auto-generated structured oracle contract
  - `ops/diagnose.py` could already enforce `booking_slot_stall`, but had no positive path for explicit pending-question interaction evidence

- Five Whys:
  1. Why did the family surface only in guarded runs: the taxonomy had no canonical name or structured oracle for it.
  2. Why did taxonomy miss it: prior work focused on `aboutness/state/referent/capability`, not interaction acts over the pending question.
  3. Why is that dangerous: unnamed families either hide behind weak labels or reopen late through expensive runs.
  4. Why not solve it with `allow_booking_stall`: that would be a scenario waiver, not a real contract.
  5. Why is this block needed before runtime-owner code: otherwise `P1.6o16c` would still build against an unstable oracle.

- Root cause statement:
  - the pending-question interaction family was absent from both scenario taxonomy and positive oracle evidence rules, so the process had no stable contract to express or verify this class before runtime implementation.

- Fix mechanism:
  - add canonical interaction tags and structured expect defaults in `scripts/booking_dialog_scenarios.py`;
  - add an evidence-only positive oracle path in `ops/diagnose.py` for explicit pending-question interaction contracts;
  - prove both with targeted deterministic tests.

## Invariant

1. No runtime owner behavior changes in this block.
2. No weakening of `booking_slot_stall`.
3. No tag-only waiver: positive acceptance requires explicit `decision_meta` / `decision_trace` evidence.
4. Existing slot-fill tags remain the contract for `fill_requested_slot`; this block adds only the missing interaction classes.

## Scope

- `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o16b-runbook-oracle-taxonomy-sync-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o16-booking-time-guidance-question-owner-a1.md`
- `scripts/booking_dialog_scenarios.py`
- `ops/diagnose.py`
- `truffles-api/tests/test_booking_dialog_scenarios_script.py`
- `truffles-api/tests/test_booking_quality_response_guard.py`

## Out of scope

- runtime owner implementation for `ask_about_requested_slot(time)`
- prompt/schema/runtime execution contract changes
- fresh guarded `dev L2`, `lock`, or `replay`

## Touch-list

- `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o16b-runbook-oracle-taxonomy-sync-a1.md`
- `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o16-booking-time-guidance-question-owner-a1.md`
- `scripts/booking_dialog_scenarios.py`
- `ops/diagnose.py`
- `truffles-api/tests/test_booking_dialog_scenarios_script.py`
- `truffles-api/tests/test_booking_quality_response_guard.py`

## Plan (1..N)

1. Freeze `P1.6o16b` as the taxonomy/oracle child slice.
2. Add the missing pending-question interaction classes to scenario generation and fallback surfaces.
3. Auto-generate structured oracle expectations for those classes so they cannot pass on wording or tag labels alone.
4. Teach the response guard to accept explicit interaction evidence while keeping `booking_slot_stall` fail-closed otherwise.
5. Prove the slice with targeted deterministic tests.

## DoD

1. Scenario taxonomy explicitly names `ask_about_requested_slot`, `slot_constraint`, `slot_compare`, `mixed_fill_plus_question`.
2. Those tags auto-populate structured `expect.meta_any` / `expect.trace_contains` instead of relying on text wording.
3. `ops/diagnose.py` accepts explicit pending-question interaction evidence only when the resume contract is also explicit.
4. Tags alone do not become a waiver.
5. Targeted deterministic tests are green.

## Checks

- `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "pending_question or merge_expectations"`
- `pytest -q truffles-api/tests/test_booking_quality_response_guard.py -k "pending_question_interaction or booking_slot_stall"`
- `git diff --check`

## Execution status

- `done`
- targeted taxonomy/oracle checks are green in the current worktree:
  - `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "pending_question or merge_expectations"` -> `7 passed, 26 deselected`
  - `pytest -q truffles-api/tests/test_booking_quality_response_guard.py -k "pending_question_interaction or booking_slot_stall"` -> `7 passed, 63 deselected`
  - `git diff --check` -> `clean`

## Evidence

1. `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o16b-runbook-oracle-taxonomy-sync-a1.md`
2. `scripts/booking_dialog_scenarios.py`
3. `ops/diagnose.py`
4. `truffles-api/tests/test_booking_dialog_scenarios_script.py`
5. `truffles-api/tests/test_booking_quality_response_guard.py`

## Rollback

1. Revert only the taxonomy/oracle sync if it introduces tag drift or weakens guard behavior.
2. Keep `P1.6o16c` blocked until this slice is green again.

## No-go

- no runtime-owner branching
- no `allow_booking_stall` waivers as acceptance mechanism
- no phrase/regex oracle for the new family
- no expensive guarded run before `P1.6o16c`

## Risks/Blockers

1. New scenario tags could silently bypass guard logic if structured expectations are not auto-populated.
2. Guard logic could be weakened if explicit interaction evidence does not also require resume-contract evidence.
3. Parent TP drift if `P1.6o16` child statuses are not synchronized.

## Residual architecture debt (mandatory)

- Current residuals accepted in this block:
  - runtime owner for `ask_about_requested_slot(time)` is still missing
  - guarded `r12` remains blocked
- Why not in this block:
  - this slice is only taxonomy/oracle synchronization
- Risk if deferred:
  - `P1.6o16c` would still build against an unstable oracle and could scenario-fit again
- Linked follow-up Task Package(s):
  - `docs/TASK_PACKAGES/TP-2026-03-08-p1.6o16-booking-time-guidance-question-owner-a1.md`
- Expiry/trigger to stop deferral:
  - before any bounded runtime owner implementation starts

## Next-block contract (mandatory)

- Next block objective:
  - start `P1.6o16c` and implement the bounded runtime owner for `ask_about_requested_slot(time)` against the now-frozen taxonomy/oracle contract
- First deterministic check command:
  - `pytest -q truffles-api/tests/test_booking_dialog_scenarios_script.py -k "pending_question or merge_expectations" && pytest -q truffles-api/tests/test_booking_quality_response_guard.py -k "pending_question_interaction or booking_slot_stall" && git diff --check`
- Blocked-by conditions:
  - new interaction tags lack structured oracle defaults
  - guard accepts tags alone without explicit meta/trace evidence
  - parent `P1.6o16` child status is not synchronized
- Owner role for closure:
  - `Hands` for code/tests/evidence, `Top Architect + Brain` for closure gating

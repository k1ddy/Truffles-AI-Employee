# TP-2026-03-07-p1.6o4-runtime-ownership-cleanup-backedge-reduction-a1

- Название/цель: убрать duplicate semantic derivation и сократить `_legacy -> decision.py` back-edge в активном `demo_salon` runtime path, чтобы semantic envelope и canonical state использовались одним owner path, а не несколькими параллельными реконструкторами.
- Parent TP: `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o-demo-salon-architecture-closure-program-a1.md`
- Canon refs: `AGENTS.md`, `STATE.md`, `STRUCTURE.md`, `STRATEGY/REQUIREMENTS.md`, `TECH.md`, `docs/TASK_PACKAGES/TP-2026-02-21-consultant-contract-first-remediation-a1.md`
- Branch: `fix/llm-first-firebreak-2026-02-19`
- Worktree: `/home/zhan/worktrees/fix-llm-first-firebreak-2026-02-19`

## One web search (mandatory before implementation)

- Status: `completed`
- Exact query: `site:martinfowler.com strangler fig application incremental refactoring`
- Executed at: `2026-03-07T17:32:03+05:00`
- Opened sources:
  - Martin Fowler, `Strangler Fig`, 2024-08-22, `https://martinfowler.com/bliki/StranglerFigApplication.html`
- Source quality:
  - high-signal primary architecture reference from Martin Fowler / Thoughtworks
- Found ready-made solutions:
  - incremental modernization via explicit seams, small isolated components, and transitional architecture instead of claiming full replacement at once
  - outcome-first decomposition before code movement
- Decision: `integrate + build`
  - reuse the strangler principle as the decomposition rule;
  - build only a bounded active-path seam for `demo_salon` runtime ownership cleanup;
  - do not claim platform-wide monolith breakup in this block
- Rejected options:
  - full `_legacy`/`decision.py` rewrite now: scope explosion and weak closure signal for `P1.6o`
  - pure docs-only decomposition: would not remove runtime backedges or duplicate semantic derivation
- Intended decision surface:
  - how to shrink an active monolith path without pretending the full decomposition is done

## Root cause (mandatory)

- Symptom:
  - `decision.py` remains the real orchestrator and `_legacy.py` still re-exports it into many modules.
- Minimal reproduction:
  1. inspect `truffles-api/app/routers/webhook/_legacy.py`
  2. inspect import graph under `truffles-api/app/routers/webhook`
  3. inspect active runtime path after `P1.6o1..P1.6o3`
- Evidence:
  - `truffles-api/app/routers/webhook/_legacy.py:1`
  - `truffles-api/app/routers/webhook/decision.py` line count and imports
- Five Whys:
  1. Why do fixes leak back into the monolith: active modules still depend on it.
  2. Why is that dangerous: semantic meaning can be derived again in fallback code.
  3. Why can't we decompose everything now: scope would explode beyond the active lane.
  4. Why do we still need this block: without it, the first new fix will drift back to the old owner pattern.
  5. Why is active-path cleanup enough for this program: `demo_salon` closure only requires the active runtime path to stop duplicating semantic ownership.
- Root cause statement:
  - runtime path still contains false modularity; multiple modules depend on `decision.py` through `_legacy`, so semantic ownership can leak back into the monolith even after upstream contract fixes.
- Fix mechanism:
  - identify the active `demo_salon` path after `P1.6o1..P1.6o3`;
  - move/keep ownership there;
  - reduce `_legacy` dependence and duplicate semantic derivation in that bounded path.

## Invariant

1. This block is bounded to the active `demo_salon` path only.
2. No broad monolith rewrite in one step.
3. No semantic owner duplication remains in the touched path.

## Scope

- `truffles-api/app/services/reasoning_core.py`
- `truffles-api/app/routers/webhook/_legacy.py`
- `truffles-api/app/routers/webhook/decision.py`
- active path modules identified during `P1.6o1..P1.6o3`
- deterministic tests that cover the touched path

## Out of scope

- full monolith decomposition
- platform-wide import graph cleanup
- expensive runs

## Touch-list

- `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o4-runtime-ownership-cleanup-backedge-reduction-a1.md`
- `truffles-api/app/services/reasoning_core.py`
- `truffles-api/app/routers/webhook/runtime_primitives.py`
- `truffles-api/app/routers/webhook/_legacy.py`
- `truffles-api/app/routers/webhook/decision.py`
- `truffles-api/app/routers/webhook/info.py`
- `truffles-api/app/routers/webhook/context_manager.py`
- `truffles-api/app/routers/webhook/response.py`
- `truffles-api/tests/test_message_endpoint.py`

## Plan (1..N)

1. Trace the active `demo_salon` semantic path after prior blocks.
2. Remove duplicate semantic derivation from that path.
3. Reduce `_legacy` dependence in the same bounded path.
4. Lock the path with deterministic regressions.

## DoD

1. The active `demo_salon` path uses one semantic owner path.
2. Touched modules no longer depend on `_legacy` where the active path can avoid it.
3. Deterministic regressions prove no behavior drift on the repaired path.

## Checks

- `pytest -q truffles-api/tests/test_message_endpoint.py::test_llm_policy_core_service_query_does_not_align_to_turn_service_hint truffles-api/tests/test_message_endpoint.py::test_llm_policy_core_direct_info_reply_keeps_policy_service_owner`
- `pytest -q truffles-api/tests/test_message_endpoint.py`
- `pytest -q truffles-api/tests/test_booking_quality_response_guard.py`
- `pytest -q truffles-api/tests/test_demo_salon_eval.py`
- `ruff check truffles-api/app/services/reasoning_core.py truffles-api/app/routers/webhook/_legacy.py truffles-api/app/routers/webhook/decision.py truffles-api/app/routers/webhook/runtime_primitives.py truffles-api/app/routers/webhook/info.py truffles-api/app/routers/webhook/context_manager.py truffles-api/app/routers/webhook/response.py truffles-api/tests/test_message_endpoint.py`
- `git diff --check`

## Execution status

- `completed`
- closure date: `2026-03-07`
- upstream dependency status: `P1.6o1`, `P1.6o2`, `P1.6o3` already green in this worktree
- deterministic closure: `green`

## Evidence

1. Active-path ownership seam:
   - shared runtime primitives extracted to `truffles-api/app/routers/webhook/runtime_primitives.py`
   - active-path modules now consume shared primitives directly instead of re-deriving them through `_legacy`
   - direct post-hoc `turn_service_hint` override removed from the policy-core direct-info path in `truffles-api/app/routers/webhook/decision.py`
2. Deterministic outputs:
   - `pytest -q truffles-api/tests/test_message_endpoint.py::test_llm_policy_core_service_query_does_not_align_to_turn_service_hint truffles-api/tests/test_message_endpoint.py::test_llm_policy_core_direct_info_reply_keeps_policy_service_owner` -> `2 passed`
   - `pytest -q truffles-api/tests/test_message_endpoint.py` -> `310 passed, 2 warnings`
   - `pytest -q truffles-api/tests/test_booking_quality_response_guard.py` -> `63 passed`
   - `pytest -q truffles-api/tests/test_demo_salon_eval.py` -> `19 passed`
   - `ruff check truffles-api/app/services/reasoning_core.py truffles-api/app/routers/webhook/_legacy.py truffles-api/app/routers/webhook/decision.py truffles-api/app/routers/webhook/runtime_primitives.py truffles-api/app/routers/webhook/info.py truffles-api/app/routers/webhook/context_manager.py truffles-api/app/routers/webhook/response.py truffles-api/tests/test_message_endpoint.py` -> `All checks passed!`
   - `git diff --check` -> `clean`

## Rollback

1. revert only active-path ownership cleanup changes
2. keep semantic envelope/state/resolver work intact

## No-go

- no fake “monolith fully solved” claim
- no sweeping unrelated refactor
- no expensive runs before deterministic closure

## Risks/Blockers

1. Import cycles can appear during extraction.
2. Active-path boundary can be mis-scoped without careful trace audit.

## Residual architecture debt (mandatory)

- Current residuals accepted in this block:
  - `_legacy` remains outside the bounded active path
- Why not in this block:
  - program scope is `demo_salon` closure, not full platform decomposition
- Risk if deferred:
  - future work outside the cleaned path can still leak into the monolith
- Linked follow-up Task Package(s):
  - `docs/TASK_PACKAGES/TP-2026-03-07-p1.6o6-demo-salon-closure-proof-bundle-a1.md`
- Expiry/trigger to stop deferral:
  - before any claim that modularity is fixed platform-wide

## Next-block contract (mandatory)

- Next block objective:
  - execute `P1.6o5` or, if already done, move to `P1.6o6` proof bundle on the cleaned path
- First deterministic check command:
  - `pytest -q truffles-api/tests/test_message_endpoint.py`
- Blocked-by conditions:
  - upstream semantic envelope/state/resolver blocks not green
  - active path not yet traced and bounded
- Owner role for closure:
  - `Top Architect + Brain`
